// @ts-nocheck
export default {version:'5.012', date:'2026-08-14', changes:[
  'Issue #274 — new Bullseye tile, src/views/bullseye-tile.js, embedded in At A Glance right after the Patch Heatmap (plain import, not lazyPanel() — same "tile inside an already-lazy view" pattern PatchHeatmap itself uses). From Notes 66\'s owner sketch: a radial scatter answering "is the district tight around the centre or splattered into the outer band," a distribution gestalt the sorted-bar Leaderboard tile can\'t show. Four departures from the original sketch, each with its own reason (three mirror the issue\'s framing, the fourth is this file\'s own call — full prose in the component\'s header comment): (1) angle = market/state sector, or patch sector once a state is selected, not loc# — loc-number ordering carries no information, sector placement answers "is one market carrying the other" at the same glance; (2) three EQUAL-AREA rings (radius boundaries at maxR*sqrt(1/3) and maxR*sqrt(2/3)), not equal-radius — equal radii would give the outer (underperforming) band several times the inner circle\'s area, crowding top performers while giving laggards a generous field, backwards from what the eye should read as "crowded vs sparse"; (3) dot colour is redundant with ring/band membership (never the only signal for above/at/below target), using the corrected status trio from #276 step 1 (#10b981/#f59e0b/#f43f5e) as literal hex rather than the --good/--warn/--crit CSS custom properties, since that PR (#280) isn\'t on main yet and this tile shouldn\'t have to wait on it landing first; (4) the 4-metric set (OEPE/Labor %/TPPH/T-Red After %) is the same getVal/getTarget/fmt/higherBetter SHAPE as the Leaderboard\'s inline META, not the identical metric list — sales is dropped (no static per-store target field; sales targets are monthly/dynamic) and TPPH is added, because a bullseye radius fundamentally needs a real per-store target, and the issue is explicit: reuse DEFAULT_TARGETS/resolveLaborTarget rather than invent a second notion of target. Radius is a continuous 3-segment map from signed percent-vs-target (positive always = good, regardless of higher-or-lower-is-better) onto the three ring boundaries, so there is no radius jump at a band edge; ±3% of target is the "at target" tolerance band, clamped at 20% beating (dead centre) and 30% below (outer edge). Sector arc width is proportional to member count (a 20-store sector and a 7-store sector get proportionally different arc widths, not equal halves); within a sector, stores are spread evenly by loc-sorted rank — never by the metric\'s own value, which would silently smuggle a second ranking into a dimension the spec reserves for market structure only. Same store.p/store.t objects PatchHeatmap already reads (pipeline.js\'s buildStore/compute6wk, already auto-first sourced) — no new data-sourcing surface, and resolveLaborTarget(t) used for the Labor % target rather than t.tLabor directly, per #153\'s 4-competing-fields history. Required per-dot hover tooltip (name/loc/band/value/target/Δ%/rank) built from scratch, positioned by the dot\'s own SVG-unit coordinates converted to a percentage of the container — no reusable floating-tooltip component exists anywhere else in the codebase to reuse (checked); a native title attribute rides alongside as a touch/no-JS fallback. Click-through to onOpenStore(loc) matches the existing direct-click drill-down pattern already used elsewhere in At A Glance. No new lazy-loading concern for the component itself: bullseye-tile.js is reached only via at-a-glance.js\'s own static import graph, itself already behind lazyPanel() as a whole view — measured in isolation (component + at-a-glance.js wiring only, changelog entries stashed out): eager total 507.52 KB gzip / headroom 342.48 KB vs 507.53 KB / 342.47 KB immediately before, noise-level. The one real eager-chunk cost is this changelog entry\'s own prose, since changelog-latest.js (unlike changelog-data.js) IS a static App.js import by design (see its header) — final eager total with the changelog entries included: 508.85 KB gzip / headroom 341.15 KB, a real +1.32 KB from this entry\'s text, not from the component. 1370/1370 tests pass (1358 existing + 12 already-landed since 5.010, zero new tests needed — a presentational tile with no new engine-level logic; existing metric-source/pipeline/labor-basis coverage already exercises every function this component calls into). Browser-verified via dev server + headless Chromium: app loads and the At A Glance view mounts with zero console/page errors; no authenticated Supabase session in this sandbox, so the tile\'s own `if (!allCells.length) return null` guard is what\'s actually exercised here (same known data-availability limitation noted throughout this session\'s other browser checks) rather than a populated bullseye — the syntax/wiring/build/bundle checks above are what stand in for a full data-live pass. PR review round: #280 merged while this sat open, so the stated reason for hardcoding hex (tokens not on main yet) no longer held -- swapped GOOD_COLOR/WARN_COLOR/CRIT_COLOR to var(--good)/var(--warn)/var(--crit); this tile is --good\'s first consumer (it shipped in #280 unreferenced, since green couldn\'t be bulk-converted there -- three different greens were in play). Confirmed in the browser rather than assumed: var() does NOT resolve in canvas fillStyle or SVG paint set via setAttribute, but React sets style props through element.style.fill/.stroke (the CSSOM path, not setAttribute), verified directly -- a synthetic SVG circle with style.fill=\'var(--good)\' resolved to rgb(16,185,129) and style.stroke=\'var(--crit)\' to rgb(244,63,94). Also fixed the state default: `(INV_ORG_COORDS[loc]||{}).state || \'OK\'` was the exact shape of the v4.426 bug CLAUDE.md records (a store silently joining Oklahoma\'s arc if its coords entry were ever missing) -- changed the fallback to an explicit \'Unassigned\' sentinel that gets its own sector instead of blending into a real market; still unreachable today (all 27 STORE_NAMES entries have coords) but now fails visibly if that ever changes. Rebased onto post-#280 main a second time (only the changelog files conflicted; at-a-glance.js auto-merged clean). 1370/1370 tests still pass, build clean.',
]};
