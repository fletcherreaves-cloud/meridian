// @ts-nocheck
export default {version:'4.967', date:'2026-08-11', changes:[
  'OEPE switched app-wide to exclude parked/held time — issue #183, item 3 of the #184 dispatch. src/lib/supabase.js\'s loadQsrActSummary (the cloud-fresh DAR-derived OEPE, backing the One-Pager and the DAR fallback in metric-source.js) computed OEPE as (dt_untilserve − dt_untilstore) ÷ dt_trans_cnt — the WITH-parked-time variant. src/views/graded-visits.js\'s hourMetrics already had the correct w/o-park formula (subtracting dt_heldtime), so the same metric name meant two different numbers depending on which panel showed it. #185 (2026-08-11) already settled which formula is right: reconciled EXACTLY against a real QSRSoft Service report, 27 stores × 10 days, r(our gap, their gap) = 0.9958 — subtract dt_heldtime, keep every car in the denominator; the alternative (drop held cars from the denominator too) is ruled out. New shared src/utils/oepe.js (oepeSeconds/oepeWithParkSeconds) is now the ONE definition both callers use — graded-visits.js\'s hourMetrics refactored to call it (zero behavior change there, confirmed by its existing test suite passing unchanged), loadQsrActSummary\'s _finalizeQsrAct now calls it too and additionally exposes the old with-park value as a named diagnostic field (oepeWithPark) — never scored, never tOepe\'s basis, per the dispatch\'s explicit instruction. Required summing a field the rollup pipeline never carried: qsr_daily_activity_rollup gains a dt_heldtime column (supabase/schema-qsr-rollup-dt-heldtime.sql — owner needs to run this against the live project) and scripts/qsrsoft-dar-pull.mjs\'s refreshRollup() now sums it; historical rows outside the pull\'s normal recent-window re-cover need a one-time QSRSOFT_DAR_FORCE_FULL=1 run to backfill, or will simply age out of the 60-90 day table. tOepe (the TARGET) is deliberately left unchanged — the owner already measured the district-wide impact of switching bases and it moves exactly one store, not a district-wide fake win (see #185). Also corrected two now-stale doc comments describing the old with-park formula (engine/metric-provenance.js\'s Metric Lineage panel text, engine/metric-source.js). 6 new tests for the shared helper (src/__tests__/oepe-shared.test.js): heldtime subtraction, missing-heldtime treated as 0 not null-propagating, the with-park diagnostic staying >= the w/o-park value. Entry chunk 2744.15 KB → 2747.19 KB gzip 825.10 KB → 826.23 KB (+1.13 KB gzip, almost entirely this changelog text).',
]};
