// @ts-nocheck
export default {version:'5.190', date:'2026-08-26', changes:[
  'Performance Review continuity, Phase 2 (dispatch #149): fixed a real bug in '
  + '`review-engine.js`\'s `autoPopulateKPIs` -- it unconditionally overwrote every `src:\'auto\'` '
  + 'KPI actual on every run (unlike target fields, which check for an existing value first), so a '
  + 'manual correction was silently destroyed the next time a review was opened. Fix: '
  + 'auto-populate keeps refreshing the raw field freely (correct -- always show the freshest cloud '
  + 'data); a correction is now a separate, append-only override RECORD instead, resolved via the '
  + 'new `applyReviewOverrides()` and never touched by auto-populate.\n\n'
  + 'Built the exact lock/override mechanism from the owner\'s own words (plan doc decision #4): '
  + 'every `src:\'auto\'` KPI actual cell in `performance-reviews.js`\'s `KPIGrid` is now read-only '
  + 'by default. An authorized overrider (per the new `canOverrideLockedActual()` in '
  + '`permissions.js` -- `levelsAbove(reviewedRole, callerRole) >= 2` on dispatch #148\'s 7-rung '
  + 'ladder, PLUS an unconditional admin/owner escape hatch) gets a pencil affordance that opens a '
  + 'form: new value + a reason dropdown with exactly 3 options -- Inaccurate Data / Incomplete '
  + 'Data / Something Else (free-text explanation required for Something Else, validated both '
  + 'client-side and by a DB check constraint). The resolved value (override-if-present else '
  + 'auto-populated) is what both display AND scoring see -- resolved ONCE per render/score pass '
  + '(`applyReviewOverrides`) and handed to every consumer (KPIGrid, `computeScores`, '
  + '`computeScoreBreakdown`, the print/PDF exports), so none of them need their own '
  + 'override-awareness. A simple expandable "Override History" section shows the full audit trail '
  + '(who/when/old value/new value/reason) on every review.\n\n'
  + 'New `supabase/schema.sql` table `review_overrides` (append-only -- no UPDATE/DELETE policy, so '
  + 'a correction is a new row, never an edit) with an RLS insert policy enforcing the SAME '
  + 'hierarchy-distance rule server-side via two new SQL functions, `role_level()` and '
  + '`review_role_to_ladder()` -- the real enforcement boundary; the client check only mirrors it '
  + 'for the UI. This SQL has NOT been applied to production yet -- see this dispatch\'s PR body '
  + 'for the exact statements a human needs to run against Supabase.',
]};
