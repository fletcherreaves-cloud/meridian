// @ts-nocheck
export default {version:'5.221', date:'2026-08-27', changes:[
  'Dispatch #179 -- widened the Item Journey / Diagnose raw-item forensic pull '
  + '(scripts/qsrsoft-variance-pull.mjs\'s `actionable` selection feeding qsr_raw_item_detail) from '
  + 'the top ~20 highest-|$|-variance WRINs/store to the top ~50, ahead of the owner\'s 3-day EOM '
  + 'physical inventory count starting 2026-08-29. The `|$| >= 50` actionability filter and the '
  + '`hasItemId` (rawItemId != null) requirement are unchanged -- only the `.slice(0, 20)` bound '
  + 'moved to `.slice(0, 50)`.\n\n'
  + 'The existing code comment frames this cap as bounding REQUEST VOLUME (a rate-limit/runtime '
  + 'concern for the scheduled daily pull, not a UX choice), so this was measured before shipping '
  + 'rather than changed on assumption. This agent session had no eBOS credentials (no '
  + 'QSRSOFT_USERNAME/PASSWORD, QSRSOFT_EBOS_TOKEN, or QSRSOFT_TOKEN/QSRSOFT_COGNITO_TOKEN in its '
  + 'environment -- confirmed by reading the script\'s own 3-path auth ladder and the session env), '
  + 'so it could not run the live raw_detail loop itself. Instead it pulled real timing from GitHub '
  + 'Actions\' own run history for this exact workflow (21 successful scheduled runs, 2026-08-05 '
  + 'through 2026-08-27, all at the current top-20 cap) via actions_list/get_job_logs -- a genuine '
  + 'measurement of the thing being changed, not a simulation.\n\n'
  + 'Three runs\' job logs were read in full (timestamps between the script\'s own "period ... x 27 '
  + 'stores" start log and its "27/27 stores ... raw-detail rows" completion log, i.e. the actual '
  + 'variance+yields+waste+transfers+raw_detail loop across all 27 stores, isolated from checkout/ '
  + 'npm-ci/Playwright-install/auth overhead): 2026-08-27 = 133.6s (537 raw-detail rows saved, near '
  + 'the 540 = 20x27 ceiling); 2026-08-26 (the slowest of the 21-run sample, for unexplained -- '
  + 'likely ordinary API/network -- reasons, not data volume: 536 raw-detail rows, almost identical '
  + 'to 08-27\'s 537) = 287.7s; 2026-08-13 (early in the month, fewer accumulated actionable WRINs) '
  + '= 84.6s with 413 raw-detail rows. Across all 21 successful scheduled runs in the sample, total '
  + 'job time (queue-adjusted) ranged 118-484s against the workflow\'s 60-minute timeout.\n\n'
  + 'raw_detail requests dominate this loop by count (~537 of ~645 total eBOS requests per run at '
  + 'the current cap, i.e. ~83%) with no explicit per-request throttling in the code (sequential '
  + 'fetches, no delay, no rate-limit backoff observed in any sampled run -- only Supabase upserts '
  + 'retry). Modeling raw_detail-specific time as ~83% of each measured loop and scaling it 2.5x '
  + '(20->50 requests/store) while holding the other ~17% (variance/yields/waste/transfers) fixed: '
  + 'the WORST observed day (08-26) projects to roughly 646s (~10.8 min) loop time, ~754s (~12.6 '
  + 'min) total script time including auth -- still under 5x the 60-minute budget. The typical/fast '
  + 'days (08-27, 08-13) project to 4-7 min total. No rate limiting (429s) was observed in any of '
  + 'the three logs read in full.\n\n'
  + 'Verdict: 50 is safe with a large margin (>4x headroom on worst-case-observed scaling) -- shipped '
  + 'as-is, no reduced interim number needed, and no change to the workflow\'s 60-minute timeout '
  + '(out of this dispatch\'s scope and unnecessary given the margin).\n\n'
  + 'Comment above the .slice() updated to reflect 50 and cite this measurement. Three other places '
  + 'described the raw_detail pull\'s previous top-20 cap as a documented reason qsr_raw_item_detail '
  + 'is the wrong source for full-coverage questions (Condiments never selected, low-dollar) -- '
  + 'updated to top-50 for accuracy since the underlying point (a dollar-filtered subset, not the '
  + 'full item universe) is unchanged either way: src/engine/count-cycle.js\'s header comment, '
  + 'src/views/eom-dashboard.js\'s dispatch-#97 comment, and '
  + 'src/__tests__/dispatch-97-cadence-onhand.test.js\'s header (noting the coverage gap the test '
  + 'guards against is unaffected by the widened cap). memory/project-eom-item-journey.md\'s '
  + 'top-20 references updated the same way. dispatch-178\'s own memory file already anticipated '
  + 'this change ("about to become top ~50 per dispatch #179") and was left as-is (dispatch #178 is '
  + 'explicitly out of scope here). Older DATED investigation logs (memory/notes-58-queue.md, '
  + 'memory/dispatch-45.md, memory/analysis-zscore-dry-run-2026-08-20.md) describe a top-20 slice '
  + 'as it existed AT THE TIME of those investigations and were deliberately left untouched -- '
  + 'archive, not a live spec.\n\n'
  + 'Full suite: 3017/3017 passing (291 files, no new/changed test count -- this is a Node pull '
  + 'script with no Vitest coverage of its own, per the dispatch\'s own verification note; '
  + 'dispatch-97-cadence-onhand.test.js re-run explicitly, still 3/3). Build clean; eager total '
  + '552.65 KB, 297.35 KB under the 850 KB budget (no src/ import-graph change -- this touches a '
  + 'Node script, not app code).',
]};
