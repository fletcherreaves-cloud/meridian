// @ts-nocheck
export default {version:'5.100', date:'2026-08-22', changes:[
  'Dispatch #60 -- the hourCycle CI fix (v5.099/b72d377) was correct and completely unguarded. '
  + '1ca02ee merged on a clean local 1952/1952 and broke main for seven consecutive commits, two '
  + 'from an unrelated session, because Intl.DateTimeFormat\'s hour12:false leaves midnight\'s '
  + 'rendering ("00" vs "24") to the runtime\'s default hourCycle resolution, which is NOT portable '
  + 'across Node/ICU versions -- measured identical on the sandbox\'s Node 22 whether hourCycle:\'h23\' '
  + 'is present or not, so no behavioural test on one Node version can guard this.\n\n'
  + 'New src/__tests__/ratchet-intl-hourcycle.test.js -- a source-level (not behavioural) zero-'
  + 'tolerance guard scanning src/ AND scripts/ for any Intl.DateTimeFormat call that requests an '
  + 'hour without an explicit hourCycle, or uses bare hour12. Demonstrated both directions: fails '
  + 'naming the exact file:line when hourCycle:\'h23\' is reverted, passes with it restored.\n\n'
  + 'The sweep this dispatch also called for found a SECOND, previously-unfixed live instance of '
  + 'the identical pattern: scripts/qsrsoft-onhand-pull.mjs\'s centralHour() used hour12:false with '
  + 'no hourCycle, feeding a formatted hour string into a numeric business-hours range check. '
  + 'Currently latent (the 8am-6pm CT window never spans midnight, so "00" vs "24" both fail the '
  + 'check the same way) but fixed to hourCycle:\'h23\' anyway, before either bound ever changes. '
  + 'Every other Intl.DateTimeFormat call in the codebase either omits hour entirely or already '
  + 'pins hourCycle -- confirmed by the guard test itself, not by eyeballing a grep. The various '
  + 'toLocaleTimeString/toLocaleDateString display-only calls across the panels (projections.js, '
  + 'at-a-glance.js, eom-dashboard.js, eom-share-view.js, etc.) were checked and are NOT the same '
  + 'bug class -- none forces a 24-hour cycle or compares formatted output against a literal, so '
  + 'en-US\'s stable 12-hour locale default applies uniformly regardless of runtime.\n\n'
  + 'Also corrected a factual error introduced by the original hotfix\'s own comment/commit message: '
  + 'it blamed "Node 24 in CI." Reading the actual CI job log\'s setup-node step (not its unrelated '
  + '"Node 20 deprecated, runner uses Node 24 by default" banner, which is about the Actions '
  + 'runner\'s own JS-action execution layer, not the pinned test Node) confirms CI\'s real test-'
  + 'runner Node is 20.20.2, exactly matching ci.yml\'s node-version:20 pin -- CLAUDE.md\'s own "20" '
  + 'claim needed no correction, only the hotfix comment did.\n\n'
  + 'ci.yml\'s verify job now runs a Node matrix ([20, 22]) instead of a single pinned version, so '
  + 'this whole class of ICU/Node-version divergence is caught going forward, not just this one '
  + 'instance -- doubles the job\'s wall time but touches no deployment-count budget (GitHub Actions '
  + 'minutes are a separate quota from Vercel\'s deploy-count cap).\n\n'
  + '1954/1954 tests (2 new). Build clean, no behaviour change to forms-completion.js beyond the '
  + 'corrected comment. Full status: memory/dispatch-60-ci-node-parity.md.',
]};
