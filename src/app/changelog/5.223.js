// @ts-nocheck
export default {version:'5.223', date:'2026-08-27', changes:[
  'Dispatch #180 -- finished "API over email" for promoAmt/promoPct, the last remaining item from '
  + '#165\'s audit (memory/audit-emailed-stream-redundancy-2026-08-27.md) that already measured a '
  + '97-98% field-level match between the emailed glimpseRows.promoAmt and the auto-pulled '
  + 'qsr_cash_sheet.metrics.promo_amt -- flagged, not fixed. #175 shipped the identical fix shape '
  + 'the same evening for cashOS/posOver and explicitly left promo out of scope; this closes it.\n\n'
  + 'Same two gaps as #175, confirmed by reading the function boundaries before touching anything: '
  + '(1) loadOpsCashSheet (src/lib/supabase.js) had NO promoAmt/promoPct camelCase aliasing at all '
  + '-- the promoAmt/promoPct lines that DID exist in the file belong to loadGlimpse/loadCash (the '
  + 'EMAIL loaders), not loadOpsCashSheet (the auto/API loader). (2) metric-source.js\'s '
  + 'promoAmt/promoPct chains had no opsCashRows source -- email (glimpseRows) then manual '
  + '(ctrlRows) only.\n\n'
  + 'One wrinkle #175 didn\'t have: qsr_cash_sheet.metrics carries promo_amt but NO promo_pct '
  + 'column (unlike cash_over_or_short, which was a direct alias for cashOSPct) -- confirmed live. '
  + 'So promoPct is DERIVED net-sales-weighted, copying discPct\'s exact pattern immediately above '
  + 'it in loadOpsCashSheet: promoPct: (r.net_sales_amt > 0 && r.promo_amt != null) ? '
  + 'r.promo_amt / r.net_sales_amt : null. promoAmt is a straight alias like every sibling *Amt '
  + 'field.\n\n'
  + 'metric-source.js: added [\'opsCashRows\', \'promoAmt\']/[\'opsCashRows\', \'promoPct\'] as the '
  + 'FIRST source in each chain, ahead of glimpseRows/ctrlRows -- same convention #175 just used. '
  + 'Email and manual sources kept in the chain as a fallback for any (loc, date) the ops-pull '
  + 'hasn\'t reached yet, not removed. promoCnt still deliberately absent (no auto/emailed stream '
  + 'emits it -- unchanged from before this dispatch).\n\n'
  + 'node scripts/gen-loader-emits.mjs --write regenerated the loader field map (opsCashRows '
  + 'gained promoAmt/promoPct in its EMITS list) per the standing dev rule.\n\n'
  + 'New tests: src/__tests__/dispatch-180-promo-auto-first.test.js (18 tests) -- reconciliation- '
  + 'style coverage proving both metrics resolve from ds.opsCashRows when present, fall back to '
  + 'email (glimpseRows) then manual (ctrlRows) for a (loc, date) opsCashRows doesn\'t cover, keep '
  + 'per-day precedence (opsCashRows and glimpseRows can each win on different days in the same '
  + 'ds -- not all-or-nothing), preserve \'any\'-mode genuine 0 values, confirm an email/manual- '
  + 'only device (no opsCashRows at all) still resolves both metrics exactly as before, and a '
  + 'direct-math check on the promoPct derive matching discPct\'s pattern. metric-chains.test.js '
  + 'and metric-source-order.test.js still pass. Full suite: 3037/3037 (292 files, +1 new file / '
  + '+18 tests). Build clean; eager total 552.72 KB, 297.28 KB under the 850 KB budget (no new '
  + 'static top-level import).',
]};
