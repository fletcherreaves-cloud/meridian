// @ts-nocheck
export default {version:'5.150', date:'2026-08-25', changes:[
  'Dispatch #109: Performance Review metric batch -- wire real gaps, confirm what already '
  + 'worked, correct the doc that said otherwise.\n\n'
  + 'Owner asked for 8 things across Performance Review\'s KPI auto-fill. A background '
  + 'investigation scoped each against the real code before this dispatch was written -- some '
  + 'were already built, some were real gaps, one was a structural finding gating the rest '
  + '(no ingestion path visible for 5 Supabase tables by a plain grep). Re-measured live with '
  + 'the service-role key before touching any code: all 5 tables (digital_app_monthly, '
  + 'mcdelivery_monthly, roster_statistics, roster_role_counts, turnover_monthly) hold real, '
  + 'current rows (214-370 each, dated 2026-08, real store locs, plausible values -- e.g. '
  + 'McDelivery restaurant-wait ~180-230 sec against a 240 sec target). The dispatch\'s own '
  + 'grep for named save-function call sites undercounted: qsrsoft-digital-app-pull.mjs and '
  + 'qsrsoft-mcdelivery-pull.mjs both exist, both are on scheduled GitHub Actions, and both '
  + 'write via an inline supabase.from(...).upsert(...) instead of calling the named '
  + 'save*Monthly helper the grep was looking for -- same undercounting shape CLAUDE.md '
  + 'already documents for the QSRSoft token migration. So items #2 and #6\'s actuals are '
  + 'confirmed genuinely live, not just code-correct.\n\n'
  + 'Item #1 (Delivery Wait Time): wired. Actual now reads mcdeliveryRows.restaurantTimeSec '
  + '(confirmed against people-reports.js\'s own field comments and the yearly workbook\'s '
  + '"McDelivery Restaurant Wait Time" column -- NOT mcDeliveryTimeSec, the courier leg). '
  + 'Target mapped to tMcdWait. Flipped manual -> auto.\n\n'
  + 'Item #2 (Digital App / Delivery GC-R-D): actuals were ALREADY wired before this dispatch '
  + '-- not rebuilt. Fixed the real gaps: added the missing target mappings (tDigAppGCRD, '
  + 'tMcdGCRD) and corrected performance-reviews.js\'s methodology modal, which still listed '
  + 'both under "Manual Entry Required" despite the code already auto-filling them.\n\n'
  + 'Item #3 (Labor % / OEPE / R2P / KVS): actual-sourcing in autoPopulateKPIs moved off '
  + 'hand-filtered ds.opsRows/ds.laborRows onto metric-source.js\'s metricAvg -- the app\'s own '
  + 'auto-first standard (glimpse/DAR/controls before a manual upload), closing the exact '
  + 'bypass CLAUDE.md\'s sourcing rule exists to prevent.\n\n'
  + 'Item #4 (pre-April targets): autoPopulateKPIs computed ONE merged-target snapshot outside '
  + 'its per-month loop and applied it to the whole review period -- a store\'s April upload '
  + 'could silently apply to January too. Added mergedTargetsForLocMonth(ds, loc, year, month), '
  + 'keying off ds.allMonthlyTargets the same way Planning > Yearly already does '
  + '(`all[year+\'-\'+m]`), so each month resolves its OWN target and a month with none falls '
  + 'through to the yearly/DEFAULT tier instead of borrowing a neighbor\'s. Does NOT build the '
  + '"combination of manual targets + actuals" blending rule the owner proposed for genuinely '
  + 'missing months -- that is an operational-mechanics decision only the owner can make, not '
  + 'guessed here per the dispatch\'s explicit instruction. Open below.\n\n'
  + 'Item #5 (Total Profit): deriveTotalProfitVsTarget existed, was unit-tested, and was never '
  + 'called. Wired into autoPopulateKPIs off the SAME month\'s already-resolved Labor%/'
  + 'Op-Supplies plus FOB% (read from fobRows.fobPct + officialTgts.tFOBTarget, since the '
  + 'review\'s own foodOB metric scores in dollars, not percent, and can\'t feed the % legs '
  + 'directly). Flipped manual -> auto.\n\n'
  + 'Item #6 (Shift Cert Managers / Headcount / 0-90 Turnover): actuals were ALREADY wired -- '
  + 'not rebuilt. Fixed: config metadata flipped manual -> auto to match; added target '
  + 'mappings (tShiftLeaders/tHeadcount/tToCrew090). Flagged, not silently assumed: '
  + '"Shift Certified Managers" <-> "Shift Leader Target" is not a perfect name match -- wired '
  + 'as the closest real target since the org\'s own source docs use "Shift Leader"/"Shift '
  + 'Manager"/"Cert. Swing Mgr" interchangeably for the same role level, but documented inline '
  + 'for the owner to correct if wrong.\n\n'
  + 'Item #7 (Shift Verifications by GM, Retention Prg. execution): untouched, as instructed -- '
  + 'both were deliberately deferred previously (memory/notes-32-queue.md) and again in this '
  + 'exact request ("can wire up later").\n\n'
  + 'Item #8 (pre-wire the customization picker): audited metric-source.js\'s METRIC_SOURCES '
  + 'for review-relevant metrics with BOTH a real auto-first chain AND a real DEFAULT_TARGETS '
  + 'field, not yet in KPI_REGISTRY. Added 4: avgCheck, tRedBPct (T-Reds Before %), posOverAmt, '
  + 'cashOSAmt -- each a direct target-field-name match with an unambiguous better-direction, '
  + 'matching the existing tpph/cashOSPct/tRedAPct/parkPct precedent (src:\'auto\' describes '
  + 'real underlying data; autoPopulateKPIs has not been taught to read the actual side for '
  + 'any of the 4 yet, same as those 3 pre-existing entries -- named explicitly as follow-up, '
  + 'not silently implied). Also checked dispatch #107\'s newer yearly-workbook fields '
  + '(tVoiceEAD/t1800Contacts/tMcdStars) the dispatch flagged as possible candidates: they do '
  + 'NOT qualify -- parseTargets is the only place those column headers appear anywhere in the '
  + 'codebase, so there is a target but no actual-data source for any of the three. Left out of '
  + 'the picker per the dispatch\'s own rule that a documented gap beats a half-wired metric.\n\n'
  + 'Full per-item resolution, the live-data measurement transcript, and what is left open for '
  + 'the owner: memory/dispatch-109.md Resolution section. Tests: 12 new/rewritten cases across '
  + 'review-target-autofill.test.js and kpi-registry.test.js (one pre-existing test rewritten -- '
  + 'it asserted every REVIEW_METRIC_TARGET_FIELD value exists in DEFAULT_TARGETS, which the 6 '
  + 'new yearly-workbook-only fields never will by design). Full suite green (2349/2349). Build '
  + 'clean. Entry chunk unaffected by this work specifically (all changes are inside the '
  + 'already-lazy performance-reviews chunk, no new App.js-level static import) -- measured in '
  + 'isolation against the pre-dispatch commit: 1765.05 KB / 523.59 KB gzip -> 1766.72 KB / '
  + '524.24 KB gzip (+1.67 KB / +0.65 KB gzip). The entry-chunk numbers in this build reflect '
  + 'dispatch #106\'s merged forecast-reports.js code-splitting, not this work.',
]};
