// @ts-nocheck
export default {version:'5.204', date:'2026-08-27', changes:[
  'Dispatch #158, item 1 -- the Leadership (Above-Store) One-Pager only offered 3 fixed period '
  + 'presets (MTD / Last wk / Last mo). Added a 4th "Custom" option, alongside the existing pills, '
  + 'that reveals two <input type="date"> fields exactly like the Store One-Pager\'s own '
  + 'rangeMode===\'custom\' pattern (one-pager.js). Every builder this panel already calls -- '
  + 'buildCurrentState, buildReviewActuals, matchedVsLY, metricAvg, the fobByRange-derived '
  + 'aggregates, buildScheduleActuals, buildPerLocationRows -- already takes a plain {s,e} range '
  + 'object, so the custom range flows through with zero engine changes, same as the 3 existing '
  + 'presets. Header/print/AI-brief text shows a human-readable label ("Aug 1 - Aug 26, 2026"), '
  + 'not the raw ISO range.\n\n'
  + 'DateRangeControl vs hand-rolled: hand-rolled the custom inputs, mirroring one-pager.js, '
  + 'rather than adopting the shared DateRangeControl component. This panel\'s 3 presets '
  + '(mtd/lastweek/lastmonth) are period-anchored, not day-count -- the exact class '
  + 'memory/panel-contract.md\'s own date-mode table already carves out via '
  + 'report-subscriptions.js\'s identical mtd/lastweek/lastmonth picker, deliberately left off '
  + 'DateRangeControl in dispatch #30 because a day-count preset would misrepresent what '
  + '"Month-to-date" means. report-subscriptions.js has no custom option at all; one-pager.js is '
  + 'the only precedent in this codebase for period-anchored-presets-plus-custom, so its inline '
  + 'pattern is what this panel now mirrors too.\n\n'
  + 'Item 2 (DO/OM/Owner scope tiers) -- investigated, NOT built; the data does not exist yet to '
  + 'build against. Re-measured staff_assignments (dispatch #150\'s reports-to graph, the engine '
  + 'resolveScope()/whoOversees() already exist for exactly this) via SUPABASE_SERVICE_ROLE_KEY '
  + '(sb_secret_... Bearer token): content-range */0 -- zero rows. Calibrated three ways in the '
  + 'same session: the identical credential returns 208 real rows on qsrsoft_kb and 15199 on '
  + 'lifelenz_schedule (so the credential itself reads live tenant data fine), and a deliberately '
  + 'bogus column on staff_assignments returns 42703 (a real Postgres "column does not exist" '
  + 'error), not */0 -- so the empty result is a genuine empty table, not a broken probe. Also '
  + 'checked org_config.org_roles: it DOES define do/om/owner/vp/etc as role+permission records '
  + '(the RBAC catalog), but that is a role CATALOG, not a person-to-store assignment map, and '
  + 'profiles has only 3 rows total (all admin/manager), none with role do or om -- nobody is '
  + 'actually assigned those roles yet. Also opened data/org-structure/Organization_Structure.xlsx '
  + '(the owner\'s own upload) directly: its Locations sheet has Owner/Operator, Organization, '
  + 'Supervisor, GM columns (confirmed by reading the real header row) but no DO or no OM column '
  + 'at all -- and even those existing columns are NOT parsed into ds by parseOrgStructure() '
  + '(src/parsers/index.js), which only extracts the "1st Schedule Week" date for the Retention '
  + 'Rollup pre-populate (dispatch #146); Owner/Operator/Supervisor/GM never reach the app. The '
  + 'existing Supervisor-patch dropdown this panel already has comes from a separate, '
  + 'hand-maintained seed (constants.js supervisorGroups()/orgAssignments()), not this workbook. '
  + 'Conclusion: no currently-populated data source for DO/OM tiers exists anywhere in this '
  + 'deployment. Building the dropdown today would show empty groups for every DO/OM tier -- '
  + 'exactly the outcome the dispatch said not to ship. Blocking prerequisite: populate '
  + 'staff_assignments (dispatch #150/#151\'s own backfill, owner-approval-gated per CLAUDE.md, '
  + 'out of this dispatch\'s scope to run) with real do/om rows, or hand-enter equivalent '
  + '`profiles.role` + `accessible_locs` rows for the actual DO/OM people. Once either exists, the '
  + 'dropdown extension itself is small: resolveScope()/whoOversees() already do the store-set '
  + 'math, so it is mostly wiring, not new engine work. "Owner" tier needs no new data model at '
  + 'all -- single-owner deployment, so it is the existing \'all\' scope with a different label, '
  + 'not a real new grouping, per the dispatch\'s own note.\n\n'
  + 'New render test (dispatch-158-onepager-custom-range.test.js) renders the real '
  + 'AboveStoreOnePager component end to end: confirms all 4 period pills exist, that no date '
  + 'inputs render until Custom is picked, that picking Custom + typing two dates drives the '
  + 'header\'s displayed range (both the raw ISO window and the human-readable label), and that a '
  + 'custom range with real-shaped ds rows reaches every downstream builder without throwing. '
  + 'Full suite 270/270 files, 2861/2861 tests (+3 net new). Build clean; entry chunk gzip '
  + 'unchanged (lazy-loaded, still not in the eager path). above-store-onepager.js\'s own lazy '
  + 'chunk: 34.97 KB -> 36.09 KB gzip 10.73 KB -> 11.02 KB (+0.29 KB gzip).',
]};
