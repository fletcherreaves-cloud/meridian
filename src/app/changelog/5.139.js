// @ts-nocheck
export default {version:'5.139', date:'2026-08-24', changes:[
  'Dispatch #94 Phase 2+3: the tol-based KPI status Phase 1 shipped for Store Dash -> Unified '
  + 'Targets now rolls up district-wide and feeds Coaching -- both built on the SAME comparison '
  + '(one implementation, engine/tolerance-status.js, exported for every consumer instead of '
  + 'each re-deriving it). Phase 1\'s inline METRICS/SPEC/valuesForLoc/statusCol all moved out of '
  + 'store-dash.js into that new module verbatim; UnifiedTargetsPanel now imports them.\n\n'
  + 'Phase 2 -- At A Glance gets a new "Tolerance Status" tile (🎯, DEF_SECS, same pattern as the '
  + 'SAGE Scheduled Runs tile): count of stores currently out of tolerance, broken out by metric '
  + '(worst-miss headline in restaurant words) and by store. Verified against real district data '
  + '(service-role pull, 27 stores, last 60 days): 270 of a possible 405 store x metric checks '
  + 'resolved (both a current value and an official target present), 190 green / 41 yellow / 39 '
  + 'red. Worst single-metric miss: Base Food % (0 green / 0 yellow / 27 red -- see caveat below).\n\n'
  + 'Phase 3 -- an out-of-tolerance metric becomes a finding in the SAME buildBrief pipeline '
  + 'GMCoachingBrief already reads (not a separate code path). Only for metrics with no existing '
  + 'dedicated finding (oepe/labor/park/tpph/r2p keep their own, richer rules) -- closes a real '
  + 'gap: the FOB waste family (Comp/Raw Waste, Condiment, Emp Meal, Stat Var, Base Food, FOB-'
  + 'Over-Base, Total Food Cost), Crew Labor %, and KVS Time had NO finding coverage at all '
  + 'before this. Verified end-to-end against the real buildStore/buildBrief consumer with live '
  + 'district data (not tolStatusesForStore in isolation): 3 real stores produced 2-5 new tol* '
  + 'findings apiece with correct severity/category.\n\n'
  + 'CAVEAT surfaced by this verification, not fixed here (pre-existing, inherited from Phase 1\'s '
  + 'metric wiring, out of this dispatch\'s scope): Base Food %\'s qsr_fob-derived current value '
  + '(totalBaseFood/prodSalesAmt, ~23-24% on every store measured) does not appear to be the same '
  + 'quantity as its official target field tFOBBase (~4-4.6%) -- a ~5x scale gap that reads red on '
  + 'every single store, every time. Flagged for the owner rather than silently "fixed" without a '
  + 'confirmed correct definition for either side.\n\n'
  + 'Suite 2258/2258 (6 new: 2 Phase 2 + 4 Phase 3, one cross-checking the rollup tile against '
  + 'UnifiedTargetsPanel on identical data per the "two panels disagree" rule). Build clean; '
  + '2 pre-existing tests updated for the file move (metric-direction.test.js now reads '
  + 'tolerance-status.js instead of store-dash.js; ratchet-raw-metric-rows.test.js\'s CEILING '
  + 'lowered 161->158, its own documented remediation for 3 raw-row reads moving out of '
  + 'src/views/). Entry chunk: 519.46 KB gzip, was 516.38 KB (+3.08 KB -- pipeline.js is eager-'
  + 'imported by App.js and now pulls in tolerance-status.js for Phase 3); headroom 328.68 KB of '
  + 'the 850 KB budget.',
]};
