// @ts-nocheck
export default {version:'5.187', date:'2026-08-26', changes:[
  'Dispatch #147: print/export for the next 3 highest-value gaps found by the print/export sweep '
  + '-- Yearly Projections, Schedule Summary, Promo/Discount ROI. Same established pattern as '
  + 'dispatches #122/#129/#134/#136/#143: a lazy-loaded `ExportDropdown` (from `store-dash.js`, '
  + 'React.lazy so the 145 KB module only loads on first click of the control) for CSV/JSON export, '
  + 'plus a full-content printable HTML report opened via `window.open` -- never bare '
  + '`window.print()` against a scrolled container.\n\n'
  + '**Yearly Projections** (`src/views/yearly-projections.js`): both views covered. Sales Pace '
  + 'exports/prints the full store-by-store Annual Target / YTD Actual / YTD vs Plan / Proj Full '
  + 'Year / FY vs Target table plus OK/FL/Grand subtotals for the selected year. Target Categories '
  + 'exports/prints whichever category tab (OEPE/CSAT/Digital/People/Labor-FOB) is currently active, '
  + 'lifted up from `TargetCategoriesView` (which owns the tab state) via a new `onExportReady` '
  + 'callback -- same lifting pattern `signals.js` uses for its Scanner/LiveOps tabs.\n\n'
  + '**Schedule Summary** (`src/views/schedule-summary.js`): exports/prints the currently selected '
  + 'week\'s all-store band in full -- Labor %, Sched vs Fcst hours, TPMH, Fixed/Floor/F+F % per '
  + 'store, every row not just what\'s scrolled into view, plus the district roll-up hero stats. '
  + '`StationBreakdown`\'s signature is untouched (schedule-retention.js depends on it exactly '
  + 'as-is, per the dispatch\'s own do-not-touch).\n\n'
  + '**Promo/Discount ROI** (`src/views/promo-roi.js`): exports/prints both levers\' full verdict '
  + 'tables (pays/costs/neutral per store, Lift %/Sales per day/Give-away per day/GP Δ per day) at '
  + 'whichever incremental-margin assumption is currently set on the slider, plus district hero '
  + 'stats and the matched-day methodology note.\n\n'
  + 'All three verified to carry the underlying numbers (not chart images) in both the CSV/JSON '
  + 'export and the printable report, and to respect whatever scope/filter (year, week, margin %, '
  + 'category tab) is currently active.\n\n'
  + '**Fixed a real infinite-render-loop bug this same dispatch introduced, before it ever shipped:** '
  + '`TargetCategoriesView`\'s `onExportReady` effect (yearly-projections.js) depended on `ok`/`fl`, '
  + 'which were plain `.filter()` calls -- a fresh array reference every render, not memoized like '
  + '`rows`/`byLoc` next to them. Every render re-triggered the effect, which called `onExportReady` '
  + '(a parent `setState`), which re-rendered this component, forever. This silently hung EVERY '
  + '`npx vitest run` of the full suite this session -- the process never printed a summary, just an '
  + 'endless stream of happy-dom `AbortError` teardown noise, which looked like environment flakiness '
  + 'until isolating each changed test file found the one that never returned '
  + '(dispatch-107-yearly-projections-panel.test.js). Fixed by wrapping `ok`/`fl` in `useMemo(...,[rows])`. '
  + 'Full suite: 2663 tests, 49.96s (was: never completed). Build clean, eager payload 544.18 KB gzip '
  + '(budget 850 KB).',
]};
