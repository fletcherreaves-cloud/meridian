// @ts-nocheck
export default {version:'5.014', date:'2026-08-15', changes:[
  'Issue #295 — Bullseye tile (#274/#282) had four hardcoded rgba(255,255,255,X) sites — the three SVG ring-guide strokes (equal-area boundary circles) and the inactive metric-tab background — that render invisibly against the light theme\'s own light surface: a white-on-white wash, not just low-contrast. A regression from day one that only #276/#280\'s dark-first review focus let ship unnoticed. Fixed by switching to the theme-aware --bdr/--bdr2/--surf2 tokens already proven working elsewhere in this same component (the tile\'s own card border and header already use var(--bdr), unaffected by this bug) — every theme×mode combination in meridian.css defines real, surf-distinct values for all three, confirmed by inspection of all 5 theme families × 2 modes. Two ring guides (inner/middle boundary, r1/r2) use --bdr; the outer boundary (maxR) keeps its slightly-stronger original relationship (.14 alpha vs .10) via --bdr2, matching the visual hierarchy the original rgba values encoded. Version numbered 5.014 (not 5.013) to avoid a collision with #286, which already claims 5.013 on its own unmerged branch — shipped standalone rather than folded into #286\'s rebase since the two changes are unrelated (light-mode contrast bug vs. #276\'s red-token conversion) and mixing them would make either diff harder to review in isolation. Not full browser-verified: this session\'s dev-server + headless-Chromium check hit an unrelated sandbox limitation (meridian.css did not attach as a real stylesheet to the page, all CSS custom properties read empty regardless of theme) that predates and is independent of this fix — verified instead by confirming the exact same token mechanism already renders correctly for this component\'s own chrome (pre-existing, unaffected code) and that every theme/mode combination defines the tokens with real values, rather than claiming a browser pass that did not actually happen. 1370/1370 tests pass, build clean (eager 509.65 KB gzip / headroom 340.35 KB, noise-level — a token-value swap, zero new imports).',
]};
