// @ts-nocheck
export default {version:'5.048', date:'2026-08-17', changes:[
  'Issue #369 -- allLocs (src/views/at-a-glance.js) was a bare .filter().map() re-running on every render regardless of whether `stores` changed, allocating a fresh array identity each time. allLocs appears in ~20 dependency-array positions in the same file, so every memo listing it recomputed unconditionally -- pure overhead (allocation + comparison) delivering none of the caching benefit. Wrapped in React.useMemo keyed on `stores`.\n\nIssue #386 (root cause, found while checking whether `stores` is itself stable per #369\'s own instruction) -- App.js\'s mergedTargets read raw `ds` while rawStores (the 27-store buildStore pass, the single most expensive computation in the app per #386\'s trace: 76,503ms across 14 runs, 93% of all render time in one real session) already debounces/defers `ds` via dsDebounced/dsDeferred specifically to cut how often that pass reruns. Since rawStores\'s own deps include mergedTargets, a mergedTargets that recomputed on every raw `ds` identity change forced rawStores to recompute just as often as before the debounce existed -- silently defeating it. This is the mechanism behind #386\'s measured "rawStores(buildStore x27) ran 10x in one session" despite the debounce already being in place.\n\nFix: mergedTargets now reads dsDeferred instead of raw ds, reusing the same dsDebounced/dsDeferred pair rawStores already computes (moved up, no second declaration). settings/userTargets/lockedProjections (the other identity-compared deps flagged for audit by #369) confirmed already stable -- all three are plain useState values, never rebuilt on unrelated renders.\n\nNot done, and reported honestly rather than guessed: full before/after click-trace numbers (compute:weekProjections run count/total, worst App-tree render, the close-modal figure) per #386\'s stated verification bar -- that needs the owner\'s real browser session with ?trace=1, which this environment cannot reproduce. The dep-stability fix and its mechanism are measured directly from the code (mergedTargets literally listed `ds`, not `dsDeferred`, while sitting immediately upstream of a memo that deliberately debounces the same value) -- the fix is confident, but the resulting run-count/total-ms drop needs a real trace to state as a number. Also not touched, per #386\'s own attack order: forecastDay profiling and any deferred-rendering redesign wait on that re-measurement.\n\n4252/4252 tests pass, build clean (506.65 KB gzip eager / 850 KB budget, no shift -- logic-only dependency-array changes).',
]};
