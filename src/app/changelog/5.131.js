// @ts-nocheck
export default {version:'5.131', date:'2026-08-24', changes:[
  'Dispatch #87: the self-reported residue of #628 (dispatch #86 is CLOSED, do not re-dispatch --'
  + ' its work already shipped). Three small, bounded items.\n\n'
  + '(1) Pinned metricAvg against silent absorption -- #86 asked for this, #628 correctly left '
  + 'metricAvg untouched but shipped without a regression test. Nothing stopped a future refactor '
  + 'from quietly redirecting it into metricSumRatio and moving numbers on ~70 other call sites. '
  + 'New test asserts the mean-of-daily answer on the same uneven-volume fixture metricSumRatio '
  + 'already uses -- one number, not a new harness. Confirmed revert-sensitive: temporarily '
  + 'redirected metricAvg to call metricSumRatio, watched the pin (and 10 collateral tests) fail, '
  + 'reverted.\n\n'
  + '(2) Per-metric numerator/denominator assertions for the other 9 rollup-capable ratio metrics '
  + '(tpph, avgCheck, cashOSPct, tRedAPct, tRedBPct, compWaste, rawWaste, statVar, spph) -- '
  + 'previously only laborPct and discPct had dedicated arithmetic tests; the rest were covered '
  + 'only by a membership check. Table-driven off rollupCapableMetricKeys(), two assertions per '
  + 'metric: derive.fn(a,b)===a/b (pins input order and a plain division) and a hand-computed '
  + 'Sum/Sum on an uneven-volume fixture using the metric\'s true semantic numerator/denominator '
  + '(not just whichever order derive.inputs happens to list). Confirmed revert-sensitive: '
  + 'reversed tpph\'s derive.inputs to [\'actHrs\',\'gc\'], watched exactly one test fail (the '
  + 'tpph arithmetic assertion), reverted.\n\n'
  + '(3) Converted memory/dispatch-77.md\'s 28 file:NNN line-number citations to anchors (symbol '
  + 'names, quoted phrases) -- the third stale-citation find in two days made line numbers in this '
  + 'fast-moving file provably unreliable (a citation fixed hours earlier was already wrong again '
  + 'by the next PR). Also converted 7 adjacent (:NNN)-style citations in the same file for '
  + 'consistency, beyond the dispatch\'s own literal grep bar. Every substituted anchor verified '
  + 'greppable against the actual current source (UnifiedTargetsPanel/RankingView METRICS arrays, '
  + 'CORR_PREDICTORS entries, MultiStoreComparison, at-a-glance\'s lbData, bullseye-tile\'s '
  + 'METRICS, etc.) -- several had already drifted 1-30 lines from the day-old citations. Scoped '
  + 'to dispatch-77.md only, per the dispatch\'s own instruction not to sweep the ~490 other '
  + 'archival citations in the repo.\n\n'
  + 'Suite 2209/2209, build clean, 518.32 KB gzip eager payload (850 KB budget) -- test-file and '
  + 'memory-doc changes only, no production code touched. Full writeup: memory/dispatch-87.md.',
]};
