// @ts-nocheck
export default {version:'5.086', date:'2026-08-20', changes:[
  'Dispatch #48 -- two new inventory security rules (INV-003 waste-unexplained, INV-005 phantom gains), both inactive, both threshold-guarded, plus the identity-vault extension the next one (INV-004) needs.\n\n'
  + 'INV-003 ("variance unmatched by logged waste") reads unexplainedVariance = max(0, |variance| - (raw_waste + comp_waste)) -- dispatch #45 Part C\'s own measured evidence that only 44.1% of INV-001\'s flags carry any logged waste at all. INV-005 ("phantom gains") reads positiveVariance = max(0, variance), targeting the SAME direction dispatch #40\'s own sign-convention finding identified: variance = exp_usage - act_usage exactly (measured live, not assumed), so a positive value is actual usage coming in below theoretical -- a gain, not a shrink. Ships without the plan\'s "recent negative-variance history" qualifier -- stated explicitly, not silently: the qsr_variance_stat rows a prior-period join would need are already sitting in the table (it now holds 4 periods, 2026-05 through 2026-08, not the 1 period an earlier draft of this migration incorrectly claimed), so this is a scope decision this pass, not a data gap.\n\n'
  + 'Both rules built min_stdev in from the start, not discovered live: an offline leave-one-out simulation of storeBaseline()\'s exact peer computation found positiveVariance\'s peer-baseline stdev distribution measurably degenerate (5.0% exact-zero baselines vs INV-003\'s 0.05%) before either rule ever ran against real data -- the same defect class dispatch #45b had to fix live on INV-001/INV-002, caught this time before shipping. min_value (15, each rule\'s own population median) and min_denominator (10, INV-001\'s established exp_usage floor) both re-measured live the same day. INV-003 reuses security_findings.exoneration_share automatically -- computeItemFindingsForRule() already computes it for any flagged INV rule, not just INV-001, so no new column or code path was needed; a new test proves this through the real call site rather than just asserting it as a fact.\n\n'
  + 'Identity-vault extension: qsr_waste.emp_token (new column, same additive pattern as audit_rows.emp_token), wired into scripts/qsrsoft-variance-pull.mjs\'s waste-pull via the SAME tokenizeRows() helper already used for register-audit rows. Documented, not silently shipped: qsr_waste.manager is an eID string, audit_rows.emp is a name string -- tokenizing both lands in two SEPARATE, unrelated identity spaces for the same real person, since no eID<->name mapping exists anywhere in this codebase (the Employee Roster pull deliberately discards names for privacy). Closes the stated privacy requirement (no plaintext identifier ever reaches security_findings) in full; the cross-reference convenience is a named open item, not a hidden one -- closing it would mean storing a new name<->eID PII pairing, an explicit owner decision, not an implied part of this dispatch.\n\n'
  + 'INV-004 itself (the manager x day-part x store waste-log-padding rule this vault extension unblocks) is not built this pass -- qsr_waste has no wrin (event-level, not item-level, so it cannot support the plan\'s literal "group by item"), and a day-part sales denominator source is not yet identified. Per the dispatch\'s own sequencing note ("if time runs short, ship the vault extension alone -- it\'s independently valuable"), the vault extension ships now and INV-004 queues next.\n\n'
  + 'Also closes an engineer action from a rebase-discovered PM commit: CASH-003 was rebuilt live against production as an absolute manualRefAmt dollar threshold (6 occurrences/4 employees/$70 total over an 80-day backfill, threshold 5, active=true) after its planned count field was confirmed to not exist at all -- but shipped with no checked-in migration and no MEASURED_MAX guard entry. Added schema-security-rules-phase1g.sql matching the live state exactly, plus the missing threshold-guard coverage.\n\n'
  + '14 new tests (threshold-guard coverage for both new migrations plus CASH-003\'s, call-site wiring, exoneration-share proof), full suite 1785/1785, build clean, no entry-chunk impact. Full writeup: memory/dispatch-48-inv003-inv005-identity-vault.md.',
]};
