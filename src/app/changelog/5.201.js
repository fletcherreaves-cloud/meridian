// @ts-nocheck
export default {version:'5.201', date:'2026-08-27', changes:[
  'Fixed: selecting the "Custom" period in labor-tools.js\'s OperatorSummaryPanel blanked the '
  + 'entire panel -- header, Period/Group/Focus/Sort controls, and the two date-range inputs the '
  + 'user needed to actually set a custom range -- with no way out except closing the whole panel '
  + '(dispatch #156, flagged but not fixed by dispatch #155/v5.200). Root cause: the panel\'s only '
  + 'empty-data gate was `if(!ds||!ds.loaded||opStats.length===0) return <"No Data Loaded" screen>`, '
  + 'and cStart/cEnd both start as \'\' -- so the instant "Custom" is clicked, before either date '
  + 'input has a value, `range` resolves null and `opStats` resolves to `[]`, tripping the gate.\n\n'
  + 'Sibling panel LaborAnalyticsPanel, confirmed by direct read to NOT share this bug: its one '
  + 'gate is `hasData = ds && (laborRows.length>0 || ctrlRows.length>0 || qsrActSummaryRows.length>0 '
  + '|| opsLaborRows.length>0)` -- independent of range/selPeriod/locStats -- and its controls bar '
  + 'renders unconditionally past that gate; a zero-row period degrades locally (kpiCards() returns '
  + 'null, each tab shows its own inline "no data for this period" message) without blanking the '
  + 'panel. OperatorSummaryPanel is now brought into line with that already-correct pattern: split '
  + 'into (1) `hasData`, a range-independent check across every stream opStats actually reads from '
  + '(directly: ds.fobRows/ds.qsrFobRows; via metricAvg/metricRate\'s METRIC_SOURCES chains for '
  + 'sales/laborPct/tpph/oepe/otHrs/cashOSPct: laborRows/ctrlRows/qsrActSummaryRows/opsLaborRows/'
  + 'glimpseRows/cashRows), gating the genuine "nothing loaded at all" full-panel empty state; and '
  + '(2) the panel\'s own pre-existing (previously unreachable) `sortedOps.length===0` placeholder '
  + 'inside the "Group cards" results area, now wired up to handle "this range resolved zero rows" '
  + 'without touching the header, controls, or date inputs. No behavior change for any non-custom '
  + 'period, which already worked correctly.\n\n'
  + 'New render-based test (dispatch-156-operator-summary-custom-period.test.js) renders the real '
  + 'OperatorSummaryPanel, clicks "Custom", and asserts the Period pills and both date inputs stay '
  + 'present/interactive -- verified to fail against the pre-fix gate (blanks to "No Data Loaded", '
  + 'zero date inputs in the DOM) and pass post-fix. A companion assertion on LaborAnalyticsPanel '
  + 'guards the untouched sibling against a future regression.\n\n'
  + 'ratchet-raw-metric-rows.test.js\'s CEILING raised 158 -> 160: the new hasData check reads '
  + 'ds.laborRows/ds.ctrlRows twice (once each), mirroring LaborAnalyticsPanel\'s own already-'
  + 'counted hasData existence check -- an EXISTENCE check for the empty-panel gate, not a metric-'
  + 'VALUE read (opStats still sources every value through metricAvg/metricRate, unchanged).\n\n'
  + 'Speed check: entry chunk gzip unchanged (small structural change inside an existing panel '
  + 'file, no new import) -- see PR body for exact before/after numbers.',
]};
