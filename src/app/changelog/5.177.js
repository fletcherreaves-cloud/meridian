// @ts-nocheck
export default {version:'5.177', date:'2026-08-25', changes:[
  'Dispatch #138 — new "Time Punches" panel: qsr_punch_times (dispatch #124\'s automated pull, '
  + 'un-tokenized by #126) had ZERO files under src/views referencing it, so the pull had been '
  + 'running with nowhere to look at its own output. Owner: "where do i find the time punches."\n\n'
  + 'Built as a companion to Crew Schedule Lookup (src/views/crew-schedule-panel.js, dispatch '
  + '#123/#125), per the dispatch\'s own instruction to reuse that shape rather than design fresh: '
  + 'same RoutePanelShell/DateRangeControl/LocationSelector, same un-tokenized-name convention '
  + '(names render directly, no reveal-click step), same ordinary panel RBAC -- '
  + 'perm:\'analytics.store\' in panel-registry.js, backstopped by qsr_punch_times\' own '
  + 'accessible_locs RLS (supabase/schema-qsr-punch-times.sql), not a stricter gate.\n\n'
  + 'New loadPunchTimes() loader in src/lib/supabase.js, mirroring loadLifeLenzShiftAssignments\'s '
  + 'shape (loc list + date range scoping, paginated via fetchAll). qsr_punch_times has no derived '
  + 'business-day dt column (schema header: boundary NOT confirmed) -- the loader widens its raw '
  + 'start_date_time query by a day on each side, and the panel narrows back to the exact requested '
  + 'range client-side via the ONE shared businessDate() helper (src/utils/date.js), never assuming '
  + 'start_date_time\'s calendar date is the business day.\n\n'
  + 'Meal-pairing shape grounded in real data before designing the UI, per the dispatch\'s own '
  + 'instruction: live service-role query (2026-08-25) against every punch for one geid across a '
  + '3-week window showed exactly one \'shift\' row per worked business day, spanning the whole '
  + 'shift, and exactly one \'meal\' row whose start/end falls strictly inside that shift\'s '
  + '[start,end] window (e.g. shift 2026-05-27T15:00-23:02Z, its meal 2026-05-27T20:22-20:54Z). '
  + 'pairPunchesByShift() nests each meal under the shift punch (same geid) whose window contains '
  + 'the meal start, rendered as an indented sub-row, not a sibling; a meal with no enclosing shift '
  + 'in the fetched window renders as its own flagged "unmatched" entry rather than being dropped or '
  + 'misattributed. in_modified/out_modified (the schema\'s own "real loss-prevention signal, '
  + 'unconsumed today") now surface as a visible ✏ edit flag on both shift and meal rows, per scope '
  + 'item 4 -- not dropped from the query.\n\n'
  + 'Nav: new \'time-punches\' entry, section:\'people\' (same section as Crew Schedule), route:true '
  + '(deep-linkable, same "would I ever want to send someone a link to this?" reasoning as its '
  + 'sibling). Do-NOT list honored: no reveal-click/token step, no RBAC gate stricter than Crew '
  + 'Schedule Lookup\'s, no speculative print/export.\n\n'
  + '24 new tests in time-punches-panel.test.js (pairing rule, business-day bucketing, directory '
  + 'grouping/search/selection, plus call-site rendering tests proving names render unconditionally '
  + 'and the modified-flag/unmatched-meal cases actually reach the DOM) + panel-registry.test.js/'
  + 'shell-nav-snapshot.test.js updated for the 13th route panel and new People-section nav entry. '
  + 'Full suite 2628/2628 passing (247 files, up from 2604/2604 on main -- +24 new, all in the new '
  + 'file); build clean; new lazy chunk time-punches-panel (7.55 KB / gzip 2.73 KB); entry gzip '
  + '467.95 KB (467.52 KB on main, +0.43 KB, changelog text only), eager payload 539.13/850 KB gzip '
  + '(538.70 KB on main, +0.43 KB).',
]};
