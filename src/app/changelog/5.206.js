// @ts-nocheck
export default {version:'5.206', date:'2026-08-27', changes:[
  'Fixed: Dispatch #161 -- Performance Review\'s FOB $ actual (`mo.foodOB`, `autoPopulateKPIs` in '
  + '`src/engine/review-engine.js`) read the manual Ops Report upload array (`ds.fobRows`) '
  + 'unconditionally, instead of the auto-pulled `qsr_fob` stream via `fobByRange()` '
  + '(`src/engine/one-pager-data.js`) that both One-Pagers already use (fixed there in v5.203) -- '
  + 'flagged as follow-up work in v5.205\'s own PR body.\n\n'
  + 'Reconciliation measured FIRST, before any code change, per this dispatch\'s explicit gate: '
  + 'read `fob_rows` (manual, one row per loc/month keyed on the 1st) and `qsr_fob` (auto, daily '
  + 'cumulative-snapshot) directly from Supabase via the service-role credential (apikey + '
  + 'Authorization: Bearer, both real-row 200s, not `*/0`) and ran the real `fobByRange()` against '
  + 'the qsr_fob rows for the same store/month the manual figure covers. Six store-months checked: '
  + 'loc 3708 Dec 2025 - May 2026 (5 months) and loc 5183 May 2026. Every one matched the manual '
  + 'figure to the penny -- e.g. loc 3708 May 2026: manual fob_dollar $14,198.17 / sales '
  + '$338,637.69 vs auto fobByRange fob$ $14,198.17 / prodSales $338,637.69 (float-precision-only '
  + 'delta, $0.00000000002). This makes sense once you look at fobByRange\'s own diff logic: for a '
  + 'FULL calendar-month range (autoPopulateKPIs\' `monthRange(m)`), segStart always equals the '
  + 'month start, so `baseSnap` is null and the "diff" is just that month\'s settled cumulative '
  + 'snapshot itself -- the same once-a-month-settle total the manual workbook was built from. No '
  + 'divergence found, so the swap proceeded per the dispatch\'s own stop/proceed rule.\n\n'
  + 'Fix: `autoPopulateKPIs` now calls `fobByRange(qsrFobRowsForLoc, range)` (the review\'s own '
  + '`ds.qsrFobRows`, pre-filtered to this review\'s loc since `fobByRange` has no `locs` param -- '
  + 'same v5.203 pattern) for each month, and uses its `fob$` directly (already the right dollar '
  + 'figure -- not `fobPct x prodSales` or another derivation). `autoFob.prodSales > 0` gates '
  + '"the auto source actually has this month" (fobByRange\'s own convention: a pre-settle/'
  + 'component-only result reads back all-zero, not absent), so an unsettled or missing month '
  + 'falls through to the manual `ds.fobRows` figure exactly as before -- manual is now an '
  + 'explicit last-resort fallback, never the primary source. `fobM`/`fr` (manual rows) are left '
  + 'untouched for the fobPct leg of the separate `totalProfit` derivation a few lines down -- out '
  + 'of this dispatch\'s scope (FOB $ actual only). `MANUAL_ONLY_METRICS` (metric-source.js) was '
  + 'never involved -- `foodOB` was never in that list; it\'s always been a hand-rolled path '
  + 'specific to this function, so there was nothing to remove there.\n\n'
  + 'Tests: new `dispatch-161-review-fob-auto-source.test.js` -- auto wins over a deliberately '
  + 'DIVERGING manual figure for the same month (proves the source actually switched, not just '
  + 'that the two happen to agree in production); falls back to manual when qsr_fob has nothing '
  + 'for that month (both when qsr_fob covers a different month only, and when `ds.qsrFobRows` is '
  + 'entirely absent); and confirms no cross-store leakage (another loc\'s qsr_fob rows don\'t get '
  + 'picked up for this review\'s loc). Existing `review-target-autofill.test.js` '
  + '"Total Profit vs Target" tests (which carry no `ds.qsrFobRows`) still pass unchanged via the '
  + 'manual fallback.\n\n'
  + 'Full suite: 273/273 files, 2870/2870 tests (271/2868 passed cleanly in the full run; the '
  + 'other 2 -- dispatch-157-segmented-review-ui.test.js and dispatch-157-period-status.test.js, '
  + 'both unrelated to this change -- failed only under severe shared-sandbox CPU contention '
  + '(load average 10-39 on a 4-core box from concurrent sibling dispatch sessions) and were '
  + 'independently re-confirmed 5/5 passing in an isolated re-run). Net +1 file/+4 tests over '
  + 'origin/main\'s 272-file baseline. Build clean; entry chunk gzip 476.24 KB -> 478.71 KB '
  + '(+2.47 KB -- the new `fobByRange` import pulls one-pager-data.js, vs-ly.js, schedule-summary.js '
  + 'and labor-basis.js into the eager entry chunk for the first time, since review-engine.js is '
  + 'itself a static App.js import; well within the 850 KB budget, 299.89 KB headroom remains).',
]};
