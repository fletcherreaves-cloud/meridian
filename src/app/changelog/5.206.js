// @ts-nocheck
export default {version:'5.206', date:'2026-08-27', changes:[
  'Added: Performance Review continuity, build item #6 (dispatch #162) -- departure/termination '
  + 'handling. Owner\'s own words, the exact mechanism (plan doc\'s "Second-pass gap review", '
  + 'resolved item B): "Do the auto finalize but require approval in the ability to override it. '
  + 'The approval and potential override should come from a job title code qualified to perform '
  + 'the review or above." A departure (qsr_employee_tenure termination_entry_date set, or a '
  + 'detected role change out of GM/AM/DM/SM) now auto-finalizes that person\'s still-open review '
  + 'period(s) this year with no manual step, and is reviewable/reopenable only by whoever the '
  + 'EXISTING decision #4 hierarchy check says is qualified.\n\n'
  + 'src/engine/departure.js (new): detectDeparture() reads qsr_employee_tenure rows (matched by '
  + 'the review\'s unified `person` geid) -- two triggers only, matching the plan doc exactly: '
  + 'termination_entry_date set (or employment_status otherwise inactive), or still-active but the '
  + 'job_title_code no longer classifies into any of GM/AM/DM/SM (a role CHANGE within those four, '
  + 'e.g. GM->SM, is a transfer/promotion, already handled by dispatch #154\'s segmented scoring -- '
  + 'never a departure). applyDepartureAutoFinalize() calls the EXISTING transitionReview(id, half, '
  + 'newStatus, notes) (dispatch #157) for every open half, never a second writer of '
  + 'review.periods; idempotent (already-approved or already-auto-finalized halves are skipped, so '
  + 're-running it for the same still-departed person on every panel load does not append repeat '
  + 'statusHistory entries). isDepartureHandled() is the "auto-clear" signal a future new-manager '
  + 'panel (build item #7, NOT built here) would filter a departed person out on -- derived purely '
  + 'from statusHistory (walks backward: the most recent `to:\'draft\'` means a human reopened a '
  + 'wrong departure record and it reports NOT handled again, even with an earlier auto_finalized '
  + 'entry).\n\n'
  + 'Distinguishable from a human transition in TWO ways, per the plan doc\'s "visibly different in '
  + 'the UI, not just in the data": (1) a NEW status value, `auto_finalized` '
  + '(review-engine.js\'s REVIEW_STATUSES), never reachable from any Submit/Approve/Return/Reopen '
  + 'button -- only from this dispatch\'s own auto-trigger -- rendered with its own distinct purple '
  + 'StatusBadge everywhere REVIEW_STATUSES already drives UI; (2) the statusHistory `notes` field '
  + 'carries a machine-greppable `[AUTO-FINALIZE]` prefix (isAutoFinalizeNote()) naming the '
  + 'departure reason.\n\n'
  + 'Approval authority (permissions.js\'s new canApproveDeparture): confirmed by reading the code '
  + '(not assumed) that decision #4\'s existing mechanism is canOverrideLockedActual -- built from '
  + 'levelsAbove() + REVIEW_ROLE_TO_LADDER + an unconditional Admin/Developer escape hatch (decision '
  + '#6-C), gated at ">=2 rungs" for "who may override a locked KPI actual". canApproveDeparture '
  + 'reuses the EXACT SAME primitives (canOverrideLockedActual itself is untouched) but at ">=1 '
  + 'rung" instead, because it answers a different question the owner\'s quote states directly: the '
  + 'person\'s own NORMAL REVIEWER (one rung up -- decision #4\'s other worked example, "if a GM is '
  + 'being reviewed then the Supervisor does the review") already qualifies to approve/reopen a '
  + 'departure finalize, where canOverrideLockedActual\'s stricter 2-rung bar specifically does not. '
  + 'StatusActionBar (performance-reviews.js) gates its new "Approve as Final"/"Reopen" pair on '
  + 'this check, not the plain reviews.approve permission the ordinary Submitted->Approved flow '
  + 'uses; KPI editing locks the same as an Approved review while auto_finalized.\n\n'
  + 'Data source (reported, not assumed): grep confirmed NOTHING in src/ loaded qsr_employee_tenure '
  + 'client-side before this dispatch -- only dispatch #150\'s one-time service-role backfill script '
  + 'and its own test read it. src/engine/tenure-roles.js (new) extracts that backfill script\'s own '
  + 'job-code classification (GM_JOB_CODES/DM_AM_JOB_CODES/SM_JOB_CODES/isActiveTenureRow/'
  + 'classifyRosterAssignment, logic unchanged) into src/engine/ so this dispatch\'s '
  + 'detectDeparture() and the backfill script share ONE definition instead of drifting -- the '
  + 'backfill script now re-exports from this new path so its own existing test keeps working '
  + 'unchanged. src/lib/supabase.js\'s new loadEmployeeTenure() is the first real client-side '
  + 'reader (narrow column selection -- no PII columns fetched, a review already has its own '
  + 'display name), wired into `ds.tenureRows` in App.js the same eager-load way dispatch #157 '
  + 'wired staffAssignments. PerformanceReviewsPanel runs the departure sweep in a useEffect keyed '
  + 'on ds.tenureRows\' identity, over every THIS-YEAR review, the moment tenure data lands -- no '
  + 'button.\n\n'
  + 'Documented, not silently assumed away: AS/OM/DO departure detection has no coverage yet '
  + '(qsr_employee_tenure is geid-keyed; AS/OM/DO use a name-string identity per decision #5 -- '
  + 'detectDeparture correctly reports "not departed" for them, never a false claim of coverage); '
  + 'and a promotion from GM/AM/DM/SM INTO AS/OM currently looks identical to a departure from job-'
  + 'code data alone (decision #5\'s own gap -- AS/OM have no roster code at all), which is exactly '
  + 'the failure mode the owner\'s own design already covers via Reopen ("e.g. if the departure '
  + 'record was wrong").\n\n'
  + 'Out of scope, per the dispatch: the new-manager notification panel itself (build item #7 -- '
  + 'confirmed by grep, zero matches for new-manager/NewManagerPanel anywhere in src/), the job-code '
  + 'config table (#8) and email wiring (#9), multi-role-per-person (#10), and populating '
  + 'staff_assignments in production (owner-approval-gated, still zero rows).\n\n'
  + 'New tests: departure detection (departed-by-termination, departed-by-role-exit, the common '
  + 'no-departure case, no-tenure-row-match, no-person-set); applyDepartureAutoFinalize calling the '
  + 'real transitionReview and verified by reading the review back through getReviews() (not the '
  + 'local object), distinguishable statusHistory, already-approved-half-untouched, idempotency; '
  + 'isDepartureHandled\'s reopen/re-approve/normal-flow cases; canApproveDeparture\'s qualified/'
  + 'unqualified/admin-escape-hatch cases (permissions.test.js, alongside canOverrideLockedActual); '
  + 'and a render test through the REAL PerformanceReviewsPanel -> ReviewEditor -> StatusActionBar '
  + 'chain (this project\'s "verification must touch the call site" standing rule) -- the sweep '
  + 'actually firing on load, the distinguishable badge/note text, KPI lock, gated Approve/Reopen '
  + 'by role, and the transition landing for real when clicked.',
]};
