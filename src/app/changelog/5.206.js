// @ts-nocheck
export default {version:'5.206', date:'2026-08-27', changes:[
  'Dispatch #160 -- standard panel-contract adoption/cleanup pass (memory/panel-contract.md) on '
  + 'the two panels just touched this session for FOB-scoping/week-picker fixes (v5.203) and the '
  + 'custom date-range picker (v5.204): src/views/above-store-onepager.js (registry id '
  + '\'above-store\') and src/views/one-pager.js (registry id \'leader-one-pager\', the "Leadership '
  + 'One-Pager"). NOT src/app/panel-registry.js\'s \'one-pager\' id -- that resolves to a wholly '
  + 'different component, StoreOnePager in analytics.js, which this dispatch never touches; caught '
  + 'by reading App.js\'s actual modal-dispatch wiring rather than trusting the dispatch brief\'s own '
  + 'label-to-file mapping, which had this swapped.\n\n'
  + 'Item 1 (Shell) -- both panels hand-rolled the exact backdrop/card/close-button pattern '
  + 'ModalShell/RoutePanelShell exist to replace (the anti-pattern '
  + 'ratchet-modal-backdrop-bypass.test.js guards). Converted BOTH to route:true + RoutePanelShell '
  + '(shell now lives inside each component, same pattern as sched-hub/perf-reviews/eom-dashboard/ '
  + 'count-cycle) rather than ModalShell -- independently evaluated per item 4 below and both '
  + 'judged genuine "would I ever want to send someone a link to this?" destinations, same bar '
  + 'the existing thirteen route panels were held to.\n\n'
  + 'Item 2 (Date mode) -- confirmed dispatch #158\'s custom-range addition to '
  + 'above-store-onepager.js already lands as the period-anchored-presets-plus-custom pattern this '
  + 'contract calls for (inherited unchanged, not re-litigated). one-pager.js\'s own '
  + 'week/mtd/ytd/custom range picker was already hand-rolled the same way pre-existing -- checked '
  + 'against DateRangeControl and left as-is for the identical reason dispatch #158\'s PR body '
  + 'gives: these presets are period-anchored (a business period, not a day-count), the same class '
  + 'report-subscriptions.js\'s picker was deliberately never converted for.\n\n'
  + 'Item 3 (Scope/location) -- ASYMMETRIC, on purpose, and load-bearing: above-store-onepager.js\'s '
  + 'scope was a plain \'all\'|\'ok\'|\'fl\'|\'grp:X\'|storeId string (identical shape to '
  + 'report-subscriptions.js\'s own persisted scope) behind a hand-rolled All/OK/FL toggle + patch '
  + '<select> + store <select> -- converted to LocationSelector (mode \'full\') with the same '
  + 'scopeToSelectorValue/selectorValueToScope translation pair report-subscriptions.js already '
  + 'established, translated at the UI boundary only; the persisted/derived `locs` computation is '
  + 'byte-identical to before. one-pager.js\'s scope was LEFT HAND-ROLLED, deliberately: its picker '
  + 'is a preset (Org/OK/FL/Owner/Supervisor) THEN a per-store pill row that toggles ANY individual '
  + 'store on/off on top of whatever preset was last applied -- an arbitrary multi-select "fine '
  + 'control" affordance LocationSelector\'s {level,id} value cannot represent (it is a single-level '
  + 'All/State/Patch/Store choice, not an arbitrary set). Converting would have deleted that fine- '
  + 'control capability, not just reskinned it, so it was left as-is and documented here rather than '
  + 'silently skipped -- same posture as labor-allocation.js\'s DAYS_BACK exception in '
  + 'memory/panel-contract.md.\n\n'
  + 'Item 4 (route:true / Actions) -- both panels converted to route:true (panel-registry.js), '
  + 'App.js dispatcher moved from setShowX(true) to goRoute(id), showAboveStore/showLeaderOnePager '
  + 'state removed entirely (anyModalOpen chain + Escape sweep updated, same pattern as every prior '
  + 'route conversion); My Reports\' above-store quick-launch (aboveStoreInit) kept as local App '
  + 'state, not URL-encoded, matching perf-reviews\' own initialTab/initialCustomizeSection '
  + 'precedent. panel-registry.test.js\'s route-panel ratchet updated thirteen -> fifteen (sorted '
  + 'list + ids-paired-with-goRoute check). Separately, one-pager.js genuinely had a 3+ grouped- '
  + 'action button-sprawl problem: Print / Discussion sheet / Weekly Review / Download Word '
  + '(filled) / Download Word (blank) -- five "produce a document" buttons crowding the header. '
  + 'Collapsed into one ActionMenu ("🖨 Reports"), leaving Save (a commit action, not an export) and '
  + 'the Cascade level select as their own direct header controls. above-store-onepager.js has no '
  + 'comparable action cluster (AI Analyze + Print, two buttons) so ActionMenu was not forced there.\n\n'
  + 'Item 5 (Mobile wide-table scrolling) -- both panels\' existing wide tables (drilldown + '
  + 'per-store rollup in above-store-onepager.js; the per-location table in one-pager.js) already '
  + 'use overflowX:\'auto\' on their scroll containers -- verified against '
  + 'ratchet-mobile-table-horizontal-scroll.test.js\'s detector directly, no hits in either file '
  + 'before or after this change, so its CEILING (2, both in dt-speedofservice.js) is untouched.\n\n'
  + 'ratchet-modal-backdrop-bypass.test.js: CEILING 76 -> 75 (above-store-onepager.js\'s hand-rolled '
  + 'backdrop line converted). one-pager.js\'s own hand-rolled backdrop was ALSO converted, but its '
  + 'zIndex:4000 sat between `inset:0,` and `background:\'rgba(0,0,0` on that source line, so the '
  + 'ratchet\'s regex never counted it in the first place -- its removal does not move the number, '
  + 'even though it is the exact same anti-pattern, fixed the same way, confirmed by re-measuring '
  + 'the live regex against both files before and after rather than assuming symmetry.\n\n'
  + 'New render-based tests (touching the real components, not just their helpers, per this '
  + 'project\'s "verification must touch the call site" standing rule): '
  + 'dispatch-160-panel-contract.test.js (AboveStoreOnePager -- RoutePanelShell header/Back button, '
  + 'no stale ✕, headerExtra actions, LocationSelector pills + OK-state-pill scope change) and '
  + 'dispatch-160-onepager-panel.test.js (OnePagerPanel -- RoutePanelShell header/Back button, the '
  + 'ActionMenu collapsing/revealing its five items, Save + Cascade select staying direct, and the '
  + 'hand-rolled Org/OK/FL + per-store scope picker still present, proving item 3\'s asymmetric call '
  + 'was actually implemented, not just decided). The latter mocks src/lib/supabase.js (same '
  + 'pattern as crew-schedule-panel.test.js) because OnePagerPanel\'s whole body is gated on '
  + 'loadQsrFob\'s promise settling -- unmocked, that call attempts a real fetch that hangs in this '
  + 'sandbox until test teardown rather than resolving.\n\n'
  + 'Full suite 274/274 files, 2878/2878 tests (+2 files/+12 tests net new). Two full runs each hit '
  + 'a handful of unrelated PerformanceReviewsPanel timeouts (dispatch #157/#159 tests, 5000ms act() '
  + 'timeouts) purely from this sandbox\'s own concurrent-session CPU contention -- confirmed by '
  + 'reproducing them in isolation with a quiet CPU, where all 3 files/8 tests pass clean every time; '
  + 'this dispatch never touches review-engine.js or PerformanceReviewsPanel. Build clean, entry '
  + 'chunk gzip 476.20 KB (unchanged -- both panels stay lazy, confirmed via lazy-panel-not-static.test.js). '
  + 'Per-panel lazy chunk, measured both sides via a same-session before/after build (swap the two '
  + 'view files back to HEAD, build, swap back -- not git stash, after this session\'s worktree-stash '
  + 'shared-ref hazard cost real time to detect and recover from): above-store-onepager.js '
  + '36.09 KB -> 34.98 KB gzip 11.02 KB -> 10.89 KB (-0.13 KB gzip); one-pager.js 42.69 KB -> 42.06 KB '
  + 'gzip 14.05 KB -> 13.92 KB (-0.13 KB gzip). Both panels got SMALLER despite the new imports -- the '
  + 'removed hand-rolled backdrop/header/dropdown markup outweighed RoutePanelShell/LocationSelector/ '
  + 'ActionMenu\'s own footprint.\n\n'
  + 'Not merging this myself per the task instructions -- a separate verification pass happens before merge.',
]};
