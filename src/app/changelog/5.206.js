// @ts-nocheck
export default {version:'5.206', date:'2026-08-27', changes:[
  'Dispatch #163 -- re-measured the pooled-Math.max freshness gap (#171) that CLAUDE.md\'s '
  + '"Adding a new automated pull" standing rule still cited as open ("At-A-Glance\'s freshness '
  + 'check takes a single Math.max across feeds... that is WHY the six days went unnoticed"). '
  + 'MEASURED, not assumed: read src/views/at-a-glance.js and src/views/analytics.js directly '
  + 'before writing any fix. Found #171 was already fully closed on 2026-08-16 (v5.034, '
  + 'dispatch15) and refined since (v5.058) -- CLAUDE.md\'s own text had simply gone stale, the '
  + 'same "rule describes code which no longer exists" trap the file names for dispatch #61/#85. '
  + 'src/engine/stream-freshness.js\'s STREAMS array checks each of the 10 auto/emailed streams '
  + 'independently (worstStream()/streamFreshness()), clamped to <=asOf so LifeLenz\'s forward-'
  + 'published schedule cannot read as fresher than it is, with manual laborRows deliberately '
  + 'excluded (a stale device-local upload must never mask an auto stream going dark). It is '
  + 'wired into TWO places: at-a-glance.js\'s Action Checklist (worstAuto -> autoItems, names the '
  + 'single worst stream by label + days-old + "auto-sync may be down") and analytics.js\'s Data '
  + 'Manager panel (LaborAnalyticsPanel), which lists every source\'s own last-updated date with '
  + 'its own color-coded staleDot -- the always-reachable per-stream detail view, not gated by a '
  + 'debug flag. The pooled "Data: Aug 16 (0d ago)" footer figure is UNCHANGED and correctly so '
  + '(v5.034\'s own note: a pooled max is the right tool for "what\'s our freshest data", wrong '
  + 'only for "is anything dark" -- which is what moved to the per-stream check).\n\n'
  + 'So no code fix was needed in at-a-glance.js/analytics.js -- writing one would have '
  + 'duplicated a mechanism that already exists and is already tested (stream-freshness.test.js\'s '
  + '"#171 verification bar" cases 1-2, plus at-a-glance-checklist-freshness.test.js\'s panel-'
  + 'level render test using the real sales_ledger_daily Aug 12-16 gap). What this dispatch '
  + 'actually fixes: (1) CLAUDE.md\'s "Adding a new automated pull" rule item 2 and the backfill '
  + 'section\'s #171 mention both rewritten to state the fix is done and point a NEW stream\'s '
  + 'checklist item at "add its dsField to STREAMS", not "build per-stream freshness" from '
  + 'scratch. (2) sync-failure-watch.yml carried the identical stale "takes a single Math.max '
  + 'across five feeds" comment describing the pre-fix state -- rewritten to describe the current '
  + 'two-layer split. (3) Added one more render-based test naming LifeLenz specifically (schedRows '
  + 'dark 30 days, all 9 sibling streams fresh) -- the panel-level suite previously only exercised '
  + 'the sales_ledger_daily gap; this reproduces the actually-named Aug 6-11 LifeLenz incident '
  + 'shape at the render layer per CLAUDE.md\'s "would this verification still pass if the change '
  + 'were reverted" rule (an engine-only test can\'t tell wired-in from unwired).\n\n'
  + 'sync-failure-watch.yml question (explicitly asked, explicitly out of scope to fix): it would '
  + 'NOT have caught a LifeLenz-class "looks like success" failure. It only reacts to '
  + 'workflow_run completed events with conclusion failure/success -- a run that reports success '
  + 'while writing nothing (a token that silently degrades mid-run), or one that never completes '
  + 'at all (queued on an offline self-hosted runner, the exact failure mode dispatch #65 '
  + 'documents for the security-events pull), is invisible to it either way. That gap is real but '
  + 'is a GitHub Actions-level mechanism change -- flagged in the workflow\'s own comment as a '
  + 'follow-up, not built here, per this dispatch\'s explicit scope boundary.\n\n'
  + '2867/2867 tests (was 2866/2866 on main; +1 new). Build clean, entry chunk gzip 476.23 KB '
  + '(476.24 KB on main -- effectively unchanged, this PR touches no code on the eager-load path, '
  + 'only docs, a workflow-file comment, and a test file).',
]};
