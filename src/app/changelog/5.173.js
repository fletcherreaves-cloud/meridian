// @ts-nocheck
export default {version:'5.173', date:'2026-08-25', changes:[
  'Speed of Service + Record Day print/export (dispatch #136) — two independent, additive asks '
  + 'bundled into one dispatch (owner, 2026-08-25).\n\n'
  + '(1) Speed of Service ("Great Job! Let\'s add print/export abilities") had no print/export at '
  + 'all. Added the shared ExportDropdown (CSV/JSON, current station + on-screen sort, lazy-'
  + 'imported so store-dash.js\'s 145 KB stays out of this panel until Export is actually clicked) '
  + 'plus a full district Print Report (station summary, per-store table with its color-banded '
  + 'targets as TEXT color rather than a background fill, weekly trend / daypart / by-hour tables '
  + '-- the real numbers behind every chart on screen, not a chart image alone). The weekly-trend '
  + 'numbers are pulled from a `weeklySeriesFor()` extracted verbatim (no behavior change) out of '
  + 'DtTrendChart\'s own useMemo, so print can never disagree with what the chart draws. Dispatch '
  + '#128\'s target-color-bar/station-selector logic untouched.\n\n'
  + '(2) Record Day Intelligence ("give me a location picker and allow print/export to carry that '
  + 'scope; highlight recent breaks on any method printed and displayed") had zero location '
  + 'filtering. Added LocationSelector (mode:\'progressive\') scoping all 6 tabs; threaded as a '
  + 'pure post-computation filter (`scopeRecordData`) over computeRecords()\'s existing full-'
  + 'district result rather than an engine change -- computeRecords() itself, and its per-store '
  + 'record detection/scoring, are untouched. Top Days re-derives from the underlying per-store-'
  + 'day list (now exposed as `days`) rather than filtering the district top-20 post-hoc, so a '
  + 'single-store scope shows that store\'s own top 20, not whatever few of the district\'s top 20 '
  + 'happen to belong to it. Export/Print already read from this same scoped `viewData`, so scope '
  + 'reaches them for free. Recent-record highlighting: a "🔥 NEW" text+border chip (not a '
  + 'background tint -- print-color-adjust defaults to \'economy\' per dispatch #129\'s own '
  + 'finding, so background-only signals print nearly blank) next to any record value that also '
  + 'appears in `recentBreakers` within the existing RecentBreakersTab windowDays window -- no '
  + 'second "how recent" concept invented. Lit on Overview, Sales & Volume, Speed, Day of Week, '
  + 'and Top Days, and in the printed report\'s tables via the same lookup.\n\n'
  + 'Full suite 2571/2571 passing (243 files, same as main); build clean; entry 467.32 KB gzip '
  + '(up from baseline 464.80 KB), eager payload 538.46/850 KB gzip (up from 535.94 KB -- '
  + 'record-day.js is statically imported into App.js by prior design, so its new LocationSelector '
  + '+ scoping + highlight code adds to the entry chunk; dt-speedofservice.js is lazy-loaded, so '
  + 'its own +2.65 KB gzip growth (ExportDropdown lazily imported) stays out of the eager budget '
  + 'entirely).',
]};
