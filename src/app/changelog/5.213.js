// @ts-nocheck
export default {version:'5.213', date:'2026-08-27', changes:[
  'Added: "Scheduled" TPPH target -- Smart Targets + Projection Workspace (dispatch #167, FR '
  + '"TPPH - Calculate TPPH targets automatically"). The engine already existed and was already '
  + 'correct: `src/engine/schedule-summary.js`\'s `rollup()` computes `tpmh: fcstGC / schedHrsOf(r)` '
  + '(LifeLenz\'s own forecast transaction count over scheduled hourly-only "Punched" hours, '
  + 'deliberately excluding salaried managers) and its district rollup is already dollar/hour-'
  + 'weighted, never an average of store %s. This dispatch only SURFACES that existing, reconciled '
  + 'computation in the two places the FR asked for -- no new derivation, no change to the formula.\n\n'
  + 'New: `currentScheduleWeekRange(now)` in schedule-summary.js -- the current LifeLenz business '
  + 'week\'s date range (Wed->Tue), centralizing the Wednesday-anchor math in the one file that '
  + 'already owns WEEK_START_DOW/weekStartOf so Smart Targets and Projections don\'t each hand-roll '
  + 'their own copy.\n\n'
  + 'src/features/smart-targets.js: `computeSmartTargets` now takes an optional `now` param (default '
  + '`new Date()`, override for tests) and attaches `results.tpph.scheduled = {tpmh, schedHrs, '
  + 'fcstGC, weekStart, ...}` (or `null` with no published forward schedule) by calling '
  + '`computeScheduleRollup(ds.schedRows, [loc], currentScheduleWeekRange(now))` -- reused, not re-'
  + 'derived. Purely additive: the existing trailing-history "Trend target" (6W/12W/best-sustained-4W '
  + '+ peer stretch) is untouched. `SmartTargetPanel`: the TPPH grid cell now shows a "Sched X.XX" '
  + 'line alongside the trend figure (visible without a click, per the "say the number AND the '
  + 'decision... never hidden behind a click" voice rule), and the detail panel gets a distinctly-'
  + 'colored "SCHEDULED TARGET · THIS WEEK" box next to the existing "TREND TARGET" box, both '
  + 'labeled so the owner can see they answer different questions.\n\n'
  + 'src/features/projections.js (`ProjectionWorkflow`): genuinely new integration point -- nothing '
  + 'in this file previously read `computeSmartTargets` or schedule-summary.js. New `ScheduledTPPHRow` '
  + 'sub-row (same pattern as the existing GCARow/DaypartRow supplement rows under each store), '
  + 'computed via `computeScheduleRollup(ds.schedRows, [loc], {s: weekDays[0], e: weekDays[last]})` '
  + 'for whatever period (week/month/custom) is currently on screen -- so it always matches the '
  + 'store row\'s own date range. Gated by `settings.showScheduledTPPH!==false`, matching the '
  + 'existing GCA/Daypart settings-flag convention.\n\n'
  + 'Verification (new file dispatch-167-tpph-scheduled-target.test.js): unit-level reconciliation '
  + 'against a real store-week fixture (DeFuniak Springs, week of Jul 22-28 2026, the same fixture '
  + 'schedule-summary.test.js already reconciles to a real LifeLenz screenshot -- Schd TPMH 4.92) '
  + 'proves `computeSmartTargets(...).tpph.scheduled.tpmh` matches `computeScheduleSummary(...).tpmh` '
  + 'for the identical rows to full float precision, not just to 2dp. Render-based tests against the '
  + 'REAL `SmartTargetPanel` (clicks the actual TPPH grid cell, reads the actual detail panel DOM) '
  + 'and the REAL `ProjectionWorkflow` (renders with the fixture schedule loaded, switches to the '
  + '"All" store grouping, reads the actual "↳ Scheduled TPPH" row) both confirm 4.92 renders in the '
  + 'live component tree -- per this repo\'s "would this verification still pass if reverted?" rule, '
  + 'an engine-only test could not prove either panel is actually wired to it. One test-environment '
  + 'trap documented in the file\'s own header comment: schedule row `date` fields must be built '
  + '*after* `vi.setSystemTime()` swaps the global Date class, not before, or computeScheduleRollup\'s '
  + '`instanceof Date` range check silently drops every row.\n\n'
  + 'Speed check: entry chunk gzip 479.32 KB -> 479.82 KB (+0.50 KB), eager-payload total 550.78 KB '
  + '-> 551.28 KB (+0.50 KB), budget 850 KB -- well within budget.\n\n'
  + 'Out of scope per the dispatch: schedule-summary.js\'s `tpmh` formula/weighting (already correct, '
  + 'reused verbatim, not touched); the existing trailing-history TPPH Smart Target (additive only); '
  + 'the raw LifeLenz `lifelenz_schedule.tpmh` field and its 13 existing call sites (a different '
  + 'thing -- LifeLenz\'s own possibly-different formula, not this dispatch\'s subject); the second '
  + 'open FR (Projections vs Actuals date-range picker).',
]};
