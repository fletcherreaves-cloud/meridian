// @ts-nocheck
export default {version:'5.078', date:'2026-08-20', changes:[
  'Dispatch39 -- Security build Phase 1: real cash-domain detection rules. First dispatch in this build that produces real, ACTIVE=true output -- everything before it (Phase 0a/0b, dispatches #33-38) was substrate.\n\n'
  + 'Same-day correction folded in before this shipped: the dispatch originally excluded TvA inventory variance reasoning from a stale "no theoretical-usage table exists" comment -- re-checked directly (not re-assumed) and found QSRSoft already computes TvA-equivalent variance natively (qsr_variance_stat/qsr_raw_item_detail, already pulled, already consumed by eom-variance-raw.js for a different workflow). What\'s actually missing is EMPLOYEE attribution on that store x item x date data -- a real cross-domain correlation problem, not a single-table ratio rule -- so Phase 1 as shipped stays cash-domain only, a legitimate complete slice (plan §7 names cash-drawer variance + void/refund detection as "the single most-corroborated method" across all three research sources), not a partial delivery. A store-level TvA-spike rule (no employee attribution) is a real, separate follow-up.\n\n'
  + 'supabase/schema-security-rules-phase1.sql: activates CASH-001/CASH-002 (ACTIVE=true, thresholds unchanged -- Phase 0b\'s own fixtures, now real). Adds CASH-003 (manual refund/self-authorized refund rate, manualRefAmt per $1,000 sales, personal baseline, opportunity_factor=true -- same privileged-override class as CASH-002) and CASH-004 (promo/discount rate, promoAmt per $1,000 sales, peer baseline, opportunity_factor=false -- examined, not assumed: audit_rows has no role/authority column to check against). CASH-004\'s threshold (100 = 10%) is NOT invented -- it\'s src/utils/register-audit.js\'s own existing discPct amber band, already live in the Register Audit table, expressed in this rule\'s per-$1,000 units.\n\n'
  + 'supabase/schema-security-findings.sql: the first output table. Subject is emp_token OR wrin (never a plaintext name) -- the single most important constraint carried forward from dispatch #37/#38. emp_token is nullable and wrin (text, matching every other wrin column in this repo) exists as a co-equal subject column from day one, WIDENED BEFORE MERGE on a same-day owner heads-up: dispatch #40 is expected to add item-level (store-level TvA-spike) findings, and since this table wasn\'t live anywhere yet there was nothing to migrate -- building the wider shape now avoided a second migration next week. A check constraint enforces exactly one subject per row. Widening this surfaced a real correctness issue fixed in the same pass: Postgres unique indexes treat NULL as never equal to another NULL, so a plain nullable-emp_token unique index would have silently stopped enforcing idempotency for any future item-level finding -- fixed with a generated subject_key column (coalescing whichever subject is set) as the actual unique/onConflict target instead. Dispatch #39 itself still only ever writes emp_token findings; wrin stays null until #40. Stores the full explanation breakdown (plan §4\'s additive-list shape, sliced to one rule) alongside value/threshold/pass, never a bare score. RLS gated to the SAME tier as reveal_employee_identity() (admin/supervisor always, manager on the existing org_config toggle) -- explicitly NOT the general any-authenticated-user pattern most tables use, reasoning stated in the file: a token alone isn\'t PII, but a 4-person night crew with one flagged token isn\'t meaningfully anonymous. No write policies at all -- only the batch job\'s service-role key writes here.\n\n'
  + 'scripts/security-rules-run.mjs: the new scheduled batch job (a COMPUTE job, not a pull -- the first of its kind in this repo). Loads every ACTIVE rule, evaluates it via dispatch #36\'s unmodified evaluateRule()/security-baselines.js against a rolling audit_rows window per distinct (loc, empToken) pair, upserts one idempotent security_findings row per (rule, employee, window). A real, non-obvious behavior verified by test rather than assumed: an employee with no token yet (pre-backfill) can never be the SUBJECT of a finding, but their row still anonymously CONTRIBUTES a rate into their peers\' baseline population (personalBaseline/peerBaseline group by raw emp name, unmodified from dispatch #36) -- correct, not a bug, since excluding it would throw away real signal for zero privacy benefit (no identity is ever exposed, only a pooled number). Scheduled 11:00 UTC, one hour after QSRSoft Register Audit Pull\'s own 10:00 UTC run (this job\'s input). sync-failure-watch.yml entry added.\n\n'
  + '13 new fixture tests (security-rules-run.test.js) -- hand-computed values for both a personal-baseline and a peer-baseline rule, including the untokenized-peer-contribution behavior above, a caught test-fixture bug (missing data_required on the first draft of the fixture rules silently produced null values throughout -- fixed before it shipped, exactly the "run the test, don\'t assume it" discipline this build has followed all along). No UI -- this dispatch only produces data, per its own explicit scope; a findings-viewer is a natural next dispatch once real output exists to look at. 1652/1652 tests pass, build clean. Full writeup: memory/dispatch39-phase1-cash-rules.md.',
]};
