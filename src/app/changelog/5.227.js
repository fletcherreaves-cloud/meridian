// @ts-nocheck
export default {version:'5.227', date:'2026-08-28', changes:[
  'Dispatch #186 -- wire the eBOS "Menu Items" catalog endpoint (menuitems/{nsn}) the owner '
  + 'captured live, resolving dispatch #185\'s ID-enumeration blocker (memory/finding-menu-item-'
  + 'id-enumeration-2026-08-28.md): no known request enumerated store_menuitem_id for a store\'s '
  + 'menu, blocking any real pull of menu_item_activity2/menu_item_activity_cost. The new '
  + 'response ([{data: store_menuitem_id, value: "{item_number} - {description}"}]) is exactly '
  + 'that enumeration -- store_menuitem_id is confirmed the SAME id space those two endpoints '
  + 'key off (dispatch #185, 4194793 matched exactly).\n\n'
  + 'NEW SCRIPT (not folded into qsrsoft-variance-pull.mjs): scripts/qsrsoft-menu-items-pull.mjs, '
  + 'own weekly workflow (Sundays 11:30 UTC). This is catalog/reference data, not a daily time '
  + 'series -- pulling it daily alongside raw_detail/raw_info would be 27x a mostly-unchanged '
  + '5,466-row response for no benefit. Mirrors lifelenz-people-pull.mjs\'s existing precedent '
  + '(same "changes slowly -- weekly is plenty" reasoning, same standalone-script-plus-own-'
  + 'workflow shape) rather than bolting a cadence flag onto the daily loop. Full replace per '
  + 'store per run (delete then insert, same pattern as lifelenz-people-pull.mjs\'s '
  + 'upsertEmployees) so a discontinued item actually leaves the table. Auth via the shared '
  + 'scripts/lib/ebos-auth.mjs ladder (no new auth path -- same eBOS X-Auth-Token/X-Current-Nsn '
  + 'family every other eBOS pull already uses).\n\n'
  + 'NEW TABLE qsr_menu_items (supabase/schema-qsr-menu-items.sql), keyed (loc, '
  + 'store_menuitem_id) -- CURRENT-STATE catalog snapshot, no period column, same precedent as '
  + 'qsr_raw_item_info (dispatch #184). item_number (int) + description (text) are real columns, '
  + 'parsed from the response\'s "{N} - {text}" value string; the raw value is ALSO kept as a '
  + 'parse-failure fallback. tenant_id + full tenant-scoped RLS (select/insert/update/delete on '
  + 'tenant_id = current_tenant_id()), matching every other stream table. New mapMenuItems() in '
  + 'src/engine/eom-parsers.js, same zero-drift pull/client-parser sharing convention as every '
  + 'other eBOS mapper here -- verified against the FULL real captured sample (memory/captures/'
  + 'menu-items-list-2026-08-28.json, 5,466 rows for store 3708): every single row matches the '
  + '"digits - text" pattern, all 5,466 store_menuitem_ids unique.\n\n'
  + 'TASK 3 (the dispatch\'s real deliverable) -- cross-referenced the catalog against '
  + 'already-pulled qsr_product_mix (item = the same small item_number/menuItemNumber POS-code '
  + 'space, per dispatch #185\'s own finding) to size how much of the ~5,466-row catalog is '
  + 'actually sold. New reusable scripts/measure-menu-item-activity-subset.mjs, run live this '
  + 'session against SUPABASE_SERVICE_ROLE_KEY (qsr_product_mix content-range 0-0/2526181, '
  + 'calibrated against a 42703 on a fake column). Store 3708 (the one store with a real '
  + 'captured catalog): 331 distinct items sold in the last 90 days, 329 of them (99.4%) found '
  + 'in the 5,466-row catalog -- a 6.0% real-activity subset, and the 99.4% match is itself the '
  + 'sanity check (a POS cannot sell an item outside its own catalog). All 27 stores\' active-'
  + 'item counts (last 90d, qsr_product_mix, live): range 292-385, sum 9,241, avg 342.3/store. '
  + 'Full table + the sizing implication (a bounded activity+cost pull needs ~330-390 '
  + 'items/store/day, NOT the full catalog -- a ~15x reduction) in '
  + 'memory/finding-menu-item-activity-subset-2026-08-28.md. menu_item_activity2/'
  + 'menu_item_activity_cost themselves remain OUT of scope for this dispatch, per its own '
  + 'boundary -- a follow-up dispatch sizes and ships that pull against this number.\n\n'
  + 'VERIFICATION: qsr_menu_items did not exist in Supabase as of this session (measured live, '
  + 'PGRST205 "Could not find the table" -- same credential as above; no DDL-execution path is '
  + 'available to this agent, matching dispatch #184\'s identical handoff for qsr_raw_item_info). '
  + 'The schema (supabase/schema-qsr-menu-items.sql) needs the owner to apply it before the pull '
  + 'writes real rows -- see PR body for the exact workflow_dispatch run that confirmed the live '
  + 'eBOS fetch+parse+auth path itself works end-to-end (the genuinely new part), with the '
  + 'expected/observed Supabase write failure until the schema lands.\n\n'
  + 'New-automated-pull checklist (CLAUDE.md): (1) sync-failure-watch.yml -- added "QSRSoft Menu '
  + 'Items Catalog Pull" (own workflow, own entry, unlike #184 which rode variance-pull\'s '
  + 'existing entry). (2) stream-freshness.js STREAMS -- measured, not assumed: qsr_menu_items is '
  + 'the same EOM/variance-family shape as qsr_raw_item_info/qsr_raw_item_detail/qsr_variance_'
  + 'stat/qsr_waste/qsr_transfers, none of which are in STREAMS or have App.js ds call sites '
  + '(confirmed by grep, same finding #184\'s own changelog recorded) -- loaded on demand by the '
  + 'EOM dashboard outside the ds/STREAMS architecture, so a STREAMS entry isn\'t a fit without '
  + 'first wiring the whole family into ds, a separate bigger change out of scope here too. (3) '
  + 'Supabase table + tenant_id + RLS: done, above. (4) manual fallback: N/A, catalog data has no '
  + 'manual-upload equivalent, same as its siblings.\n\n'
  + 'New tests: 4 added to src/__tests__/eom-parsers.test.js (value-string parsing incl. a '
  + 'description that itself starts with a digit, unrecognized-shape fallback, empty/missing-id '
  + 'handling, and a full-file check against all 5,466 real captured rows). Full suite: '
  + '3063/3063 (294 files, +4 tests -- baseline was 3039 pre-#184/#186, #184 alone added 2, other '
  + 'dispatches landed on main in between added the rest). Build clean; eager total 553.13 KB, '
  + '296.87 KB under the 850 KB budget (pull-script + engine-module + schema change only, no '
  + 'client UI touched).',
]};
