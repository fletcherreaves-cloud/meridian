// @ts-nocheck
export default {version:'5.176', date:'2026-08-25', changes:[
  'Dispatch #142 — Performance Review: Sales/Labor target cascade bypass, Voice OSAT wiring, '
  + 'yearly-target decimal display. Owner report: "Sales not correct" (screenshot showed $2.4-2.8M '
  + 'actual scored "Exceeds" against a $194K-$617K target), "Labor should reflect monthly target '
  + 'when present," "monthly targets supercede yearly," Voice OSAT still shows no target despite '
  + 'real workbook data, and unwanted decimals in imported yearly targets.\n\n'
  + 'MEASURED, not assumed, before fixing (standing rule) — the dispatch\'s own root-cause pointer '
  + '(review-engine.js\'s sum(lr,\'salesTgt\')/avg(lr,\'laborTgt\') unconditionally overwriting '
  + 'officialTgts) turned out to be confirmed-DEAD CODE: parseLaborData() never emits a salesTgt/'
  + 'tSales/laborTgt/tCombLabor field on a labor row, saveLaborRows()/loadLaborRows() never '
  + 'round-trip one, and the live production labor_rows table has no such column. Removed it per '
  + 'the dispatch anyway (dead + misleading + a future landmine), replaced with an explicit '
  + 'fallback AFTER officialTgts (never instead of it) — but it changes nothing observable, so it '
  + 'could not be the actual bug. Traced the REAL mechanism instead, against live production '
  + 'Supabase (curl, service-role key, 2026-08-25):\n\n'
  + '1/3. Sales: parseYearlyTargets() already parses the workbook\'s Product Sales column into '
  + 'tProdSales with full precision, but dispatch #107\'s original yearly_targets schema + '
  + 'saveYearlyTargets()/_yearlyRowToTargets() never persisted it at all (confirmed: select=* on '
  + 'the live table returns no prod_sales-shaped key, and #107\'s own persistence test\'s field list '
  + 'deliberately excludes it). So a freshly-uploaded yearly Product Sales value survived only until '
  + 'the next login/device/reload, then silently reverted to DEFAULT_TARGETS\' hardcoded fallback -- '
  + 'measured stale by ~3x for several real high-volume stores (e.g. store 5985: DEFAULT_TARGETS.'
  + 'tProdSales $221,408.74 vs real May-2026 actual monthly sales $665,948.29, from a live '
  + 'labor_rows query) -- the same "target way too small" shape as the owner\'s screenshot. New '
  + 'yearly_targets.prod_sales column (supabase/schema-dispatch-142-sales-labor-targets.sql, '
  + 'owner needs to run it) + saveYearlyTargets/_yearlyRowToTargets mapping, with a defensive 42703 '
  + 'retry (drops just the two new columns and re-saves) so an upload never hard-fails before the '
  + 'migration runs.\n\n'
  + '2/3. Labor: a DIFFERENT, more specific bug than a missing column -- review-engine.js\'s '
  + 'REVIEW_METRIC_TARGET_FIELD.labor read the legacy \'tLabor\' field, which labor-basis.js\'s own '
  + 'LABOR_BASIS_FIELDS comment names as "static only, no monthly path -- the field the bug graded '
  + 'on" (issue #153). #153 already moved every OTHER labor-target consumer onto '
  + 'resolveLaborTarget()\'s DEFAULT_LABOR_BASIS (\'tCrewLabor\', the field a real monthly upload '
  + 'actually persists as monthly_targets.crew_labor_pct) -- Performance Review was never updated '
  + 'to follow. So a monthly-uploaded labor target could never reach the review AT ALL, regardless '
  + 'of magnitude -- exactly "Labor should reflect monthly target when present." Switched the '
  + 'mapping to tCrewLabor, and added the missing yearly-tier column (yearly_targets.'
  + 'crew_labor_pct, same migration file, same defensive retry) so DEFAULT < yearly < monthly now '
  + 'resolve through the SAME field name end-to-end -- closing "monthly supersedes yearly" too, '
  + 'which structurally could not have worked before (yearly only ever set tLabor, monthly only '
  + 'ever set tCrewLabor -- two different fields could never compete for the same slot).\n\n'
  + '4. Voice OSAT: real gap, exactly as scoped -- REVIEW_METRIC_TARGET_FIELD had no \'osat\' entry '
  + 'at all, so osatTgt never filled even with officialTgts.tOsat populated. Added osat: \'tOsat\' '
  + '(scale confirmed matching first: actual mo.osat and target tOsat are both 0-1 fraction, '
  + 'live-verified against smg_fullscale.osat_5 and yearly_targets.voice_osat_pct). Also '
  + 'investigated tOsatB2B per the "do not silently wire" instruction: it is NOT an unused gap -- '
  + 'it already feeds one-pager-data.js\'s One-Pager review (a different artifact), so it is '
  + 'correctly left out of Performance Review\'s REVIEW_METRIC_TARGET_FIELD, not a missing wire. '
  + 'Complaints/tComplaintsTarget re-confirmed untouched, as scoped (dispatch #135\'s "raw count vs '
  + '/100K rate" finding stands).\n\n'
  + '5. Decimal display: two raw, unformatted cells, both display-only fixes (parse/storage '
  + 'precision untouched) -- targets-editor.js\'s fmtVal(usd) used Number.toLocaleString()\'s '
  + 'default (up to 3 decimals, e.g. "$111,513.158"), now Math.round() to whole dollars matching '
  + 'the app\'s existing money() convention (yearly-projections.js). store-dash.js\'s '
  + 'MonthlyTargetManager getFieldVal() rounded %-scale fields via .toFixed() already but returned '
  + '$/count fields\' raw stored value straight into a number input with zero formatting -- now '
  + 'whole-dollars there too.\n\n'
  + 'Do-NOT list honored: no stored target VALUES rounded/truncated (only display cells); '
  + 'Complaints/tComplaintsTarget untouched; tOsatB2B investigated and left unwired (documented '
  + 'why, above) rather than guessed into a metric it may not match.\n\n'
  + '20 new/updated tests across review-target-autofill.test.js (labor-basis switch + precedence '
  + 'fallback + osat wiring), yearly-targets-persistence.test.js (prod_sales/crew_labor_pct '
  + 'round-trip + the 42703 graceful-degrade path), and a new dispatch-142-decimal-display.test.js. '
  + 'Full suite 2604/2604 passing (246 files, up from 2590/245 on main -- +14 new); build clean; '
  + 'entry 467.53 KB gzip (467.35 KB on main, +0.18 KB, all changelog text), eager payload 538.71/'
  + '850 KB gzip (538.53 KB on main, +0.18 KB).',
]};
