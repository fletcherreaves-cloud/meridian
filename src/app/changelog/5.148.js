// @ts-nocheck
export default {version:'5.148', date:'2026-08-24', changes:[
  'Dispatch #107: Yearly Targets -- persisted to Supabase, Planning > Yearly panel rebuilt, '
  + 'dead-end editor retired.\n\n'
  + 'Owner: "I have uploaded yearly targets workbook several times and just noticed that for '
  + 'yearly all that is tracked is Sales... Store these targets in supabase > check first '
  + 'though, they legit may already be there." Checked first, per instruction: `ds.targets` '
  + '(the real yearly-upload workbook -- OEPE/CSAT/Digital/People/Labor-FOB, already correctly '
  + 'parsed by parseYearlyTargets() and already correctly merged under monthly targets '
  + 'everywhere real -- Performance Review, forecast, tolerance-status) had ZERO Supabase '
  + 'persistence. It was rebuilt from scratch every session purely by re-parsing whatever '
  + 'workbook happened to get uploaded that session -- the actual root cause of the repeated '
  + 're-uploads, not a display bug.\n\n'
  + 'Part 1: added a `yearly_targets` Supabase table (PK loc+year, tenant_id + RLS mirroring '
  + 'the current canonical new-table pattern) and saveYearlyTargets/loadYearlyTargets/'
  + 'loadAllYearlyTargets in supabase.js, field-mapped against saveMonthlyTargets\'s own '
  + 'pattern including the #166 null-stripping fix. Wired the save into every pipeline.js '
  + 'upload branch (full rebuild and incremental re-drop identically) and the load into '
  + 'App.js\'s T1 startup tier, alongside monthly targets -- ds.targets now survives a fresh '
  + 'session/device instead of requiring re-upload.\n\n'
  + 'Part 2: the Planning > Yearly panel (yearly-projections.js) previously showed ONLY a '
  + 'Sales rollup derived from monthly targets -- the uploaded workbook itself has no Sales '
  + 'column at all. Added a "Target Categories" view alongside the unchanged Sales Pace view: '
  + 'five sub-tabs (Service & Ops, CSAT, Digital, People, Labor & FOB) matching the workbook\'s '
  + 'own grouping, per store, with OK/FL/Grand subtotals (sum for headcount-style counts, '
  + 'average for rates/times -- never an average of an average).\n\n'
  + 'Part 3: store-dash.js carried a third, disconnected localStorage-only manual yearly-'
  + 'target editor (mf_targets_yearly_*) that nothing downstream ever read. Retired it rather '
  + 'than wiring it in -- its only real effect (an "Apply as Monthly Defaults" button) copied '
  + 'into a separate legacy localStorage store, never into ds.targets or Supabase, and its '
  + 'field set was the MONTHLY operational taxonomy (T-Reds/promos/cash controls), not the '
  + 'real yearly workbook\'s categories -- keeping it next to the new Target Categories view '
  + 'would have read as a second, contradictory "yearly targets" system. Also resolves the '
  + 'naming collision with the new Supabase functions by construction. Full reasoning: '
  + 'memory/dispatch-107.md Resolution section.\n\n'
  + 'Part 4: verified (not rebuilt) that review-engine.js\'s mergedTargetsForLoc (DEFAULT < '
  + 'yearly < monthly, monthly wins) already handles a genuinely yearly-only field correctly -- '
  + 'added one test case using tOsatB2B (Overall Satisfaction B2B, no monthly_targets column '
  + 'at all) confirming both halves: the yearly value surfaces with no monthly override '
  + 'present, and a monthly override for the same field still wins once one exists.\n\n'
  + 'Verified through the real consumers, not isolated helpers: '
  + 'dispatch-107-yearly-projections-panel.test.js renders the actual YearlyProjectionsPanel, '
  + 'clicks the real Target Categories tab and CSAT sub-tab, and asserts real formatted values '
  + '(140s OEPE, 2.00% OSAT B2B) appear in the DOM, plus the Sales Pace view and its "Annual '
  + 'Target" column still render untouched and a no-upload year shows the empty state instead '
  + 'of crashing. yearly-targets-persistence.test.js exercises the real save/load functions '
  + 'against a mocked supabase-js client, asserting the actual upserted row shape and the '
  + 'null-stripping round trip. Regenerated the loader field map '
  + '(gen-loader-emits.mjs --write). Build clean; full suite green in this worktree (the only '
  + 'failures in a full run are src-no-undef.test.js copies under sibling agents\' own '
  + '.claude/worktrees, unrelated to this change). Entry chunk 522.51 KB -> 523.21 KB gzip '
  + '(+0.70 KB); eager total 524.37 KB -> 525.07 KB gzip (+0.70 KB, budget 850 KB, headroom '
  + '324.93 KB) -- the new T1 yearly-targets startup stage in App.js; store-dash.js\'s own '
  + 'lazy chunk actually shrank from Part 3\'s deletions, and yearly-projections.js is '
  + 'lazy-loaded, not part of the eager entry.',
]};
