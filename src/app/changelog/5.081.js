// @ts-nocheck
export default {version:'5.081', date:'2026-08-20', changes:[
  'Dispatch43 -- the Security panel, Phase 1: a read-only investigation surface for security_findings, which had zero references anywhere in src/ before this. The batch job (dispatches #39/#40/#42) has been writing findings; nothing read them until now.\n\n'
  + 'The central design call: grouped by SUBJECT, not by rule. The batch job\'s natural output is rule-major (4 cash rules x 670 subjects); rendered that way, one employee flagged on 3 independent signals reads as 3 unrelated rows in 3 lists -- the single most informative fact (convergence across independent signals) is exactly what rule-major rendering destroys. groupFindingsBySubject() collapses to one row per (loc, subject), sorted by how many rules agree then by worst flagged value. Implements plan §1 principle 4 (exoneration) for free -- every verdict is kept, not just flags, so a subject\'s passed rules render right next to the one they failed.\n\n'
  + 'Three new loaders in src/lib/supabase.js (loadSecurityFindings, paginated via the existing fetchAll() helper -- INV-001 alone produced 5,165+ rows on its first run; loadSecurityRules for rule metadata; loadGmIdentityRevealEnabled reading org_config client-side). Permission is gated to the EXACT tier security_findings\' RLS enforces: admin/supervisor always, manager only when org_config.gm_identity_reveal_enabled is true -- checked in two layers deliberately, since RLS returns [] to an unauthorized role, indistinguishable from "no findings" on the wire, and this panel must never let an empty read stand in for a permission check. A static permissions.js key (security.view) only decides whether the nav entry is worth showing; securityPanelAccess() inside the panel does the real, live check, including the manager org_config condition the static key structurally cannot express.\n\n'
  + 'verdictState(pass) is the honest three-state mapping (true->flagged, false->clear, null->undetermined) -- rendering null as clear was named a correctness bug in the dispatch, not a display nicety. Every rule\'s active state renders inline and a finding from an inactive rule (INV-001/INV-002, currently active=false pending dispatch #42) is visibly marked historical, never presented as current truth.\n\n'
  + 'Wired through panel-registry.js/shell.js/App.js\'s existing four-list convention (nav, onOpenModal dispatch, render, anyModalOpen/Escape-hatch inclusion) -- panel-registry.test.js (20/20, unmodified) confirms the wiring against the live code, the same suite that has caught unreachable panels before.\n\n'
  + 'A real bug found by a STRICTER test, not by inspection: the data-loading effect originally depended on [permState, dataState] -- but the effect itself SETS dataState. Setting it triggered a re-render that changed the effect\'s own dependency, React re-ran the effect, ran the PREVIOUS instance\'s cleanup first (as it always does), that cleanup set cancelled=true, and the in-flight fetch about to call setDataState(\'loaded\') silently discarded its own result. The panel got stuck on "Loading findings…" forever, immediately after the state transition meant to end that state. A weaker first-draft test (just checking the loader was called) never caught this; a stricter one asserting the actual end state ("no findings match", not stuck loading) did. Fixed by dropping dataState from the effect\'s own dependency array.\n\n'
  + '15 new tests (security-panel.test.js): all four exported pure helpers, plus component-level WIRING tests -- three permission states (admin/supervisor skip the org_config round-trip entirely; manager awaits it; an ineligible role is denied with zero network calls), subject grouping/sorting on a hand-built fixture, and the permitted-but-empty-vs-not-permitted distinction that caught the effect bug above. 1705/1705 suite passes, build clean -- security-panel is its own lazy chunk (8.33 KB / 3.31 KB gzip), not part of the eager entry bundle (510.39 -> 510.94 KB gzip from the small eager registry/permission/shell metadata additions, still 337 KB under budget).\n\n'
  + 'Phase 2 (triage state -- reviewed/dismissed/escalated) is a deliberately separate PR per the dispatch\'s own instruction. Coexistence deep-links (Register Audit tab markers, an attention-now.js candidate surface) not built this pass -- the dispatch says evaluate, don\'t force. Not verified: a live browser click-through -- this sandbox\'s Supabase project requires real magic-link auth this session cannot complete headless, stated plainly rather than claimed. Full writeup: memory/dispatch43-security-panel.md.',
]};
