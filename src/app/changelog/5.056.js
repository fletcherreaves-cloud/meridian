// @ts-nocheck
export default {version:'5.056', date:'2026-08-18', changes:[
  'Issue #365 -- EOM Supervisor\'s "OP Supplies MTD $" figure sums qsr_ebos_daily.ops_purchases over the selected calendar month -- already the right approach (loadEbosMonthlyByStore, added back in v4.395), so the dispatch\'s stated concern turned out to already be built. What was NOT already fixed, found by re-checking the query itself rather than trusting the comment claiming it was correct: `lte(\'date\', \'${y}-${m}-31\')` hardcoded 31 as the upper bound regardless of month length. MEASURED against live Supabase, not assumed -- a direct REST call with date=lte.2026-02-31 returns Postgres error 22008 ("date/time field value out of range"), since February never has a 31st. The caller\'s `if (error) { console.error(...); return {}; }` swallowed that error, so the OP Supplies column silently went blank for every month with fewer than 31 days -- February, April, June, September, November -- 5 of 12 months, every year, with nothing visible past a console.error nobody was watching.\n\nFix: computes the real last day of the month via the same `new Date(year, month, 0)` idiom eom-supervisor.js\'s own monthRange.e already uses, instead of a hardcoded 31. Re-verified live after the fix -- the identical REST call with the corrected bound (date=lte.2026-02-28) now returns real February rows that existed the whole time.\n\nThe projection column (`projOpSup`, sourced from Monthly Targets\' tOpSupply) was already handled deliberately: it\'s the honest full-month target, labeled "Projected OP Supply $" (not "MTD"), with no silent prorating against the partial-month actual -- confirmed by inspection, no change needed there.\n\nGrepped for the same `-31` hardcoded-bound signature elsewhere in src/ -- this was the only site.\n\nVerification: 5 new tests against a mock Supabase client that records the exact date bounds passed to .gte()/.lte() (the only way to assert on the bound itself -- a live call either errors or silently returns [], neither of which proves what bound was sent). 3 of the 5 (Feb non-leap, April, Feb leap-year) confirmed RED against a stash of the pre-fix code before being restored; the other 2 (31-day month, the actual per-store sum) were already green and stayed that way, confirming the fix doesn\'t regress the working case.\n\n1420/1420 tests pass (5 new), build clean (no bundle-size shift -- logic-only change in an already-loaded module).',
  ]};
