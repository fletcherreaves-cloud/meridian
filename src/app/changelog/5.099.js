// @ts-nocheck
export default {version:'5.099', date:'2026-08-21', changes:[
  'Forms dashboard, Slice 3 of 3 -- the pull script (all three slices now done). New '
  + 'scripts/qsrsoft-forms-completion-pull.mjs pulls completionDetail (POST, token-only auth '
  + 'confirmed via the owner\'s DevTools request-header panel -- no Playwright required for the '
  + 'primary path) and writes qsr_forms_completion. Two-path auth via getFreshToken() '
  + '(scripts/lib/qsrsoft-auth.mjs) -- a direct Cognito mint, no stored-token staleness -- with a '
  + 'Playwright fallback kept as a defensive second path.\n\n'
  + 'Two real bugs fixed first, owner-reproduced against the already-shipped Slice 1/2 code: '
  + '(1) the literal string "noLocation" (a real, documented request member -- submissions with '
  + 'no store attached) normalized to the garbage loc "0000NaN" via parseInt; now an explicit '
  + '\'NOLOC\' sentinel, never dropped. (2) The store-day rollup\'s local-midnight boundary used a '
  + 'FIXED UTC-5 offset, correct only during Central Daylight Time -- every row misbucketed by a '
  + 'full day for the entire CST half of the year (a completion at 22:30 CST on day N read as '
  + 'day N+1). Fixed with a real Intl.DateTimeFormat America/Chicago lookup, DST-aware by '
  + 'construction; the new pull script\'s own request-window builder had the identical flaw and '
  + 'got the same fix before ever shipping. Regression tests: a CST case, a noLocation case, and '
  + 'a four-point sweep across both 2026 DST transition days, verified against real Intl output.\n\n'
  + 'FormsCompletionPanel now shows its OWN per-stream freshness reading ("Synced today" / "Last '
  + 'synced Nd ago") -- never pooled into anything else, per #171\'s own lesson (one dead stream '
  + 'invisible behind fresh siblings). New workflow qsrsoft-forms-completion-pull.yml (twice-daily '
  + 'cron, matching every other QSRSoft pull\'s cadence) + sync-failure-watch.yml entry. Manual-'
  + 'upload fallback deliberately NOT built -- explicit scope decision, not an omission: this data '
  + 'was never tracked on paper, so there is no existing workflow to protect (see '
  + 'memory/project-forms-dashboard-slice3.md).\n\n'
  + 'Range caution, unresolved: the completionDetail response-size/pagination cap is unmeasured '
  + '(one 3-day/27-store/4,714-row capture is the only known-good data point), so the pull '
  + 'chunks requests to 3-day windows by default rather than risking a silent truncation on a '
  + 'larger backfill -- do not widen the default chunk size without measuring the actual cap '
  + 'first.\n\n'
  + '1952/1952 tests (net new: the two bug-fix regressions + a DST-transition sweep + '
  + 'apiWindowForDays() coverage + the panel\'s own freshness-line render tests). Build clean, '
  + 'entry chunk unchanged at 511.12 KB gzip. Full writeup: '
  + 'memory/project-forms-dashboard-slice3.md.',
]};
