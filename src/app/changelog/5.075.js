// @ts-nocheck
export default {version:'5.075', date:'2026-08-19', changes:[
  'Dispatch36 -- Security build Phase 0b, the substrate. Per the plan\'s own build order ("do not start by coding individual fraud rules... a rule written before this substrate exists will need to be rewritten once it does"), Phase 1 (cash-drawer variance + peer ranking) is gated on this landing first. Needs no QSRSoft access, so it wasn\'t blocked on Phase 0a\'s still-open live-verification.\n\n'
  + 'Part 1 -- Rules Registry: supabase/schema-security-rules.sql adds security_rules, matching plan §6\'s schema field-for-field (RULE_ID, DOMAIN, LOGIC_TYPE, LOGIC_EXPRESSION, THRESHOLD, SEVERITY, WEIGHT, CORROBORATION_RULES, EXONERATION_RULES, etc.), following org_config\'s exact RLS shape (authenticated read-all, admin/supervisor write) plus tenant_id per CLAUDE.md\'s standing rule -- org_config predates that rule, so its RLS shape was copied but not its missing tenant_id. src/engine/security-rules.js is the interpreter: evaluateRule(rule, dataContext, {loc}) handles LOGIC_TYPE threshold and ratio (the two the dispatch required); z-score/sequence/window-function are stubbed (implemented:false, pass:null, never thrown) so the schema accommodates all five from day one without every consumer needing its own type-check. Seeded with 2 real rules from plan §2.1 (cash-drawer over/short rate, POS overring rate) as ACTIVE=false test fixtures -- they prove the interpreter round-trips a real rule end-to-end, not a Phase 1 delivery.\n\n'
  + 'Part 2 -- Baselines + exposure normalization: read metric-source.js and vs-ly.js first, per the dispatch\'s own instruction and CLAUDE.md\'s "source data through the shared helpers" rule. Neither carries a rate-normalization or cohort-baseline primitive to extend (grepped both, confirmed) -- this is genuinely new ground, not a 4th parallel path, but src/engine/security-baselines.js follows their conventions: dollar-weighted aggregation across a window (never averaging per-row percentages) and honest nulls on zero exposure. exposureRate() is the shared normalization primitive, using memory/data-acquisition-shopping-list.md\'s exact per-$1,000-sales / per-1,000-transactions convention (scale=1000), not a new unit. Four baseline functions, each returning a mean/stdev/distribution rather than one blended number (plan §1 principle 2): personalBaseline (subject\'s own per-day history), peerBaseline (other employees at the SAME store -- audit_rows carries no role/daypart/tenure/volume-band columns to cohort on more precisely, documented as a data-limited proxy, not silently assumed correct), storeBaseline (this store vs other stores, all employees pooled per store), networkBaseline (every other employee org-wide, widening peer\'s cohort past the store boundary).\n\n'
  + 'audit_rows shape confirmed adequate for a normalized event record (who=emp, where=loc, when=date, how-tendered=cash/cashless split on refunds) by re-reading src/utils/register-audit.js\'s analyzeRegisterAudit -- extended nothing, per the plan\'s own "close to already shaped" note.\n\n'
  + '22 new fixture tests (security-rules.test.js, security-baselines.test.js) -- hand-computed expected values (dollar-weighted rates, per-day/per-member distributions, mean/stdev), including 2 tests that mirror the seeded CASH-001/CASH-002 LOGIC_EXPRESSION JSON exactly, round-tripping the actual rows the SQL file ships against the interpreter. No UI -- data-layer + interpreter only, per the dispatch\'s own scope. 1618/1618 tests pass (22 new), build clean and unaffected (no App.js import -- these are unwired substrate for Phase 1, not a bundled feature). Full writeup: memory/dispatch36-security-phase0b-substrate.md.',
]};
