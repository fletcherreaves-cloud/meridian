// @ts-nocheck
export default {version:'5.200', date:'2026-08-27', changes:[
  'Extended dispatch #153\'s OEPE/R2P/TPPH completeness fix (metricSumRatio Σnum/Σden, not '
  + 'metricAvg\'s mean-of-daily) to every OTHER call site whose range can genuinely include the '
  + 'current, still-open business day (dispatch #155). #153 scoped itself to only the Leadership '
  + 'One-Pager\'s 3 call sites and listed ~9 other files as follow-up candidates -- this dispatch '
  + 'worked through that list, converting only the ones where the bug genuinely applies.\n\n'
  + 'Converted: review-engine.js `autoPopulateKPIs` (a review actively built for the CURRENT, '
  + 'still-in-progress month passes a full-calendar-month range); at-a-glance.js\'s district TPPH '
  + 'tile (the toolbar\'s own selected period can be "this week"/MTD); signals.js\'s Park×OEPE '
  + 'quadrant tab (its own range literally ends `new Date()`); labor-tools.js\'s '
  + 'OperatorSummaryPanel + LaborAnalyticsPanel (every fixed period ends on the last CLOSED '
  + 'business day, but \'custom\' lets a user type today as the end date); sage.js\'s SAGE labor/'
  + 'ops district summaries (`_lastNDaysRange`\'s end is literally `new Date()`, reported to the '
  + 'owner as fact mid-day); store-dash.js\'s District Rankings (a preset literally called '
  + '\'Today\'); attention-now.js\'s `useAttentionFeed` slowDT detector (a live-monitoring panel '
  + 'reading the app\'s own toolbar date range, which can include today).\n\n'
  + 'Left on metricAvg, with the range-construction evidence for each: morning-brief.js\'s 8-week '
  + 'norm baseline (ends on `lastClosedBusinessDay()`, explicitly not "now"); labor-tools.js\'s '
  + '6-week trend (`wr.e = wEnd-1`, so even the most recent bucket excludes today); above-store-'
  + 'onepager.js\'s panel figures (already inherit #153\'s fix transitively via the shared '
  + '`buildCurrentState`/etc. builders) and its own drilldown rows (period selector is only mtd/'
  + 'lastweek/lastmonth, none of which can reach today -- no custom option); store-dash.js\'s '
  + 'PerformanceCalculator slider baseline (explicitly documented as ending on '
  + '`lastClosedBusinessDay()`, "low-risk, slider starting points, not a live monitoring number"). '
  + 'Full per-call-site disposition table with reasoning: this dispatch\'s PR body.\n\n'
  + 'The shared fallback helper (dispatch #153\'s private `rateMetric` in one-pager-data.js -- '
  + 'metricSumRatio first, metricAvg fallback only when metricSumRatio returns null) is relocated '
  + 'to metric-source.js and renamed `metricRate`, exported for reuse across every file above. Not '
  + 'kept as `rateMetric`: review-engine.js already exports an unrelated, already-shipped '
  + '`rateMetric(actual, target, metricCfg)` 1-4 scoring function for Performance Review KPIs -- '
  + 'same name, completely different signature and purpose, so the shared helper needed its own '
  + 'name to avoid colliding with it once review-engine.js also needed to import the metric-source '
  + 'one. one-pager-data.js now imports `metricRate` from metric-source.js instead of defining its '
  + 'own copy; its 7 internal call sites are unchanged in behavior.\n\n'
  + 'signals.js\'s `ParkOepeTab` is now exported (was module-private, only reachable through '
  + 'SignalsPanel\'s own tab-navigation state) purely so its metricRate conversion has a real '
  + 'render-based test -- no behavior change.\n\n'
  + 'Also found, NOT fixed here (out of scope -- unrelated to oepe/r2p/tpph): selecting the '
  + '"Custom" period in labor-tools.js\'s OperatorSummaryPanel/LaborAnalyticsPanel blanks the '
  + 'entire panel the instant it\'s selected, before the date inputs a user would need are ever '
  + 'reachable in the DOM (cStart/cEnd start empty, so opStats/locStats -> [] -> "No Data Loaded" '
  + 'gate fires and hides the whole controls bar, including the date pickers themselves and the '
  + 'way back to a non-custom period). Flagged for a separate follow-up dispatch.\n\n'
  + 'Speed check: entry chunk 474.97 KB gzip (baseline 474.95 KB), eager total 546.34 KB gzip '
  + '(baseline 546.29 KB, budget 850 KB) -- effectively unchanged.',
]};
