// @ts-nocheck
export default {version:'4.985', date:'2026-08-11', changes:[
  'Issue #201 — Patch Heatmap: all 27 stores as a colored status grid at the top of At A Glance, answering "which store needs me today?" spatially instead of by reading a tile strip and a list. Color = Needs Attention severity (engine/attention-feed.js\'s groupAttentionByStore — the SAME per-store rollup AttentionPanel already uses, not a new score, per the issue\'s own "use what already exists" instruction). Answers the issue\'s own open question ("needs me right now, or how is this store doing overall?") as "needs me right now" — the issue names Needs Attention as the strongest candidate for exactly that reading; Ops Score / tracking-to-plan are the "overall health" / "today\'s pace" readings and are deliberately not blended in. No charting library (CSS grid + colored cells), no animation (owner declined it in the issue — draws the eye to what\'s live rather than what\'s important), colorblind-safe (color is always paired with a glyph and, where relevant, a count — never color alone: ! for critical, • for watch, ✓ for clean, ? for no data). A store with no data renders as a distinct gray "unknown" state, never green — the same failure class the FOB Report false-all-clear fix (v4.976) addressed earlier this session. Click a cell to expand an inline detail (worst findings + an "Open store" button wired to the existing onOpenStore callback) instead of a second navigation layer. New src/views/patch-heatmap.js, lazy by construction — imported only from at-a-glance.js, which App.js already dynamic-imports (confirmed before writing anything, per the standing "check whether an affordance already exists" rule), so this adds zero weight to the entry chunk; measured, not assumed — entry chunk gzip actually went DOWN (831.60 KB -> 817.16 KB) in the same build that added this, most likely because attention-feed.js/attention-now.js gained a second (lazy) import path and rolldown re-chunked accordingly — a genuine, measured outcome, not a claimed mechanism.',
]};
