// @ts-nocheck
export default {version:'4.993', date:'2026-08-12', changes:[
  'Issue #220 — Patch Heatmap gets patch-level rollup tiles, unblocked by #219\'s band calibration. Owner, on first use of #213: "It\'s labeled Patch Heatmap, yet shows all locations individually (I like) — but why not include a tile for each patch rolled up as well?" The trap the issue named explicitly: colouring a patch tile by its worst store is that store again wearing a bigger badge — zero new information, and it goes red the moment any single store struggles. Built the other way: new patchDimensions() AGGREGATES the underlying raw numbers across a patch\'s member stores FIRST, then derives each dimension from the aggregate, reusing storeDimensions\'/#219\'s bandFromGap()/bandColor() and the same worst-of-N rule (factored into a shared statusFromDims() both store and patch tiles now call). Sales and FOB have real dollar figures this far upstream (store.pSales/pLY, fobRow.sales/fob) so those sum raw $ directly — a true dollar-weighted aggregate; Labor and Speed have no raw labor-$ or car-count surviving to this layer, so both use sales-weighted averages of the per-store value (the same convention labor-tools.js\'s own district rollup already uses for labor%, not a new invention — and an honest size-proxy for Speed specifically, since OEPE has no car-count weight anywhere else in this app either). FOB\'s target is also sales-weighted across the same member stores, mirroring computeFOBMetrics\' (analytics.js) own wTgt pattern. Controls (ctrlScore) is deliberately excluded from the patch rollup, same reasoning as #219: a composite computeOpsScore builds from several inputs, not a raw gap-to-target this function can aggregate honestly without re-deriving that scoring logic per patch. Grouping comes from supervisorGroups() (constants.js) — the LIVE, Management-editable org chart App.js already keeps in sync on every settings load — not the frozen INV_ORG_COORDS.sup snapshot the issue also mentioned as an option; they match today but only one stays correct after a reassignment. First-writer-wins on the loc->patch inverse lookup guards the issue\'s "don\'t double-count a store in two patches" requirement. UI: a patch row above the store grid (different unit, different question, laid out separately per the issue\'s own suggestion), click-to-expand showing per-dimension detail with store-membership counts, same gray-? unknown state for insufficient data, no coaching hook added (a supervisor-level "Coach this" is a natural future extension but out of scope here). 9 new tests (src/__tests__/patch-heatmap-rollup.test.js): every aggregation is checked against a hand-computed dollar/sales-weighted answer using fixtures deliberately shaped so a naive per-store average would diverge from the correct one, Controls exclusion, missing-data handling, and the core anti-pattern guard itself — a small store bad enough to be critical alone bands clean once aggregated into a much larger healthy patch. Entry chunk barely moves from the feature itself — patch-heatmap.js is lazy via at-a-glance.js, so all new code landed in that chunk (108.52->112.54 KB raw / 29.43->30.11 KB gzip); the small entry-chunk delta that remains is this changelog text, same pattern as every other entry here. Not verified live — no browser/Supabase session in this sandbox; the owner should confirm the patch row groups stores as expected and no supervisor shows an empty or duplicate patch. Full findings: memory/project-patch-heatmap-calibration-219.md (folded in — this issue was explicitly sequenced right after #219 on the same feature).',
]};
