// @ts-nocheck
export default {version:'5.182', date:'2026-08-26', changes:[
  'Dispatch #145: added EAP and EAD as new scored metrics in Performance Review\'s RGR category '
  + '(src/engine/review-engine.js). OSAT B2B and EPB2B stay OUT of scope — both held pending the '
  + 'owner\'s own investigation into what `yearly_targets.osat_b2b_pct` actually represents; '
  + '`epb2b` is completely untouched (same key, same override-only mechanism, still dormant), and '
  + 'no `osatB2B` metric was added anywhere.\n\n'
  + '**EAP — Experienced A Problem (overall).** Real, live actual: SMG FullScale\'s Overall-section '
  + '"Experienced a Problem (Yes)" % (`overallProblem`/`overall_problem`, confirmed as the in-app '
  + 'field name via loadSmgFullscale()\'s mapping, not assumed from the raw column) — a NEW metric, '
  + 'not a rename of `epb2b` (which reads the B2B-qualified version of the same question). Wired in '
  + 'autoPopulateKPIs\'s existing SMG-FullScale block (`mo.eap = sr.overallProblem`). No '
  + 'yearly-workbook target column exists for this — target is override-only via a new '
  + '`tEAPTarget` field in target-overrides.js\'s TARGET_OVERRIDE_FIELDS, following the exact '
  + 'totalProfit/complaints pattern.\n\n'
  + '**EAD — Voice Execute As Designed.** Target is real and already parsed: parseYearlyTargets\'s '
  + '`voiceEAD` → `t.tVoiceEAD` → `yearly_targets.voice_ead_pct`, now wired via '
  + '`REVIEW_METRIC_TARGET_FIELD.ead`. Actual has NO data source anywhere in the codebase — '
  + 'confirmed by the existing kpi-registry.test.js guard ("deliberately excludes the '
  + 'yearly-workbook-only fields with no actual source", already covering `voiceEAD`; '
  + 're-verified, not re-derived) — stays manual-entry, same state EPB2B is already in.\n\n'
  + 'One deviation from the dispatch\'s literal text, verified against real code before deviating '
  + '(CLAUDE.md "measure it, don\'t reason about it"): the dispatch said EAP should get "NO entry" '
  + 'in `REVIEW_METRIC_TARGET_FIELD`, but the pattern it pointed at (totalProfit/complaints) both '
  + 'DO have one there, mapping to their own override field — and target-overrides.test.js already '
  + 'enforces that sync for every TARGET_OVERRIDE_FIELDS row except foodOB. Without `eap: '
  + '\'tEAPTarget\'` there, an EAP override could never actually reach a review (the autofill loop '
  + 'only walks that one map) and the existing test would fail. Added it, matching the real '
  + 'totalProfit/complaints pattern — which is what "follow the exact pattern" already meant.\n\n'
  + 'Weight decision (RGR must not silently change its scored-weight balance): the 10 existing '
  + 'scored RGR metrics already summed to 0.95, not 1.00 (secondSide is scored:false and excluded) '
  + '— a pre-existing gap, unrelated to this dispatch. Rather than rescaling 10 unrelated weights '
  + '(a bigger, unrequested call) eap (0.03) + ead (0.02) are sized to exactly close that 0.05 gap: '
  + 'no existing metric\'s weight changes, and RGR\'s scored total now lands at exactly 1.00. Both '
  + 'are scored:true (not reference-only like secondSide) — missingReviewTargets() only flags '
  + 'scored metrics, and the dispatch\'s own verification bar requires EAP to be flagged there when '
  + 'no override target is set, which scored:false would silently defeat.\n\n'
  + 'Threshold decision: the dispatch suggested modeling EAP\'s bands on osat\'s — but osat is a '
  + 'higher-is-better SATISFACTION metric, while EAP is a lower-is-better PROBLEM RATE, same family '
  + 'as the existing epb2b metric right above it (both derive from the same FullScale "Experienced '
  + 'a Problem" question). EAP\'s t-band (t:[-0.02,0.02,0.04], lower=better) is modeled on epb2b '
  + 'instead — the closer real analog already shipped in this same category — not osat. EAD\'s '
  + 'band (t:[0,-0.10,-0.20], higher=better) reuses the same conservative shape already used for '
  + 'fsAudits/fsEcoSure/fsTablet: a %-completion metric with no live actual yet to calibrate '
  + 'against.\n\n'
  + 'Also added: blankMonthKPIs seeds eap/eapTgt/ead/eadTgt; a metric-provenance.js entry for eap '
  + '(composed:false, direct FullScale read); two SRC() help-text lines in performance-reviews.js\'s '
  + 'Customize → Metrics panel.\n\n'
  + 'Full suite 2645/2645 (248 files) — one pre-existing flake in '
  + 'dispatch-141-retention-rollup.test.js under full-suite parallel load, confirmed unrelated '
  + '(passes 17/17 in isolation, same on main) and untouched by this change; build clean. Entry '
  + '468.88 KB gzip (468.39 KB on main, +0.49 KB); eager payload 540.06/850 KB gzip (539.57 KB on '
  + 'main, +0.49 KB).',
]};
