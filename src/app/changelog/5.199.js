// @ts-nocheck
export default {version:'5.199', date:'2026-08-27', changes:[
  'Performance Review continuity, Phase 5a (dispatch #154): promotion/transfer segmented scoring '
  + '-- engine/data layer only, no UI (Phase 5b, later). A manager who transfers stores or is '
  + 'promoted mid-year now has each period of a review scored against the role/store that was '
  + 'ACTUALLY true then, instead of blended into one number, per the owner\'s own historical '
  + 'practice ("it was always awarded based on the store data in which the manager worked the '
  + 'majority of the month") and the HR-research consensus already captured in the plan doc '
  + '(an overall rating "does not need to be an average").\n\n'
  + 'New `personAssignmentTimeline(person, periodStart, periodEnd, rows)` (assignment-graph.js) '
  + '-- reconstructs one person\'s own role/store history across a date range with possibly '
  + 'multiple transitions, generalizing the file\'s existing "latest start <= date wins" rule '
  + 'across a range instead of one point in time. The common case (no transfer/promotion) '
  + 'collapses to exactly one full-period segment, cheaply. Lives in assignment-graph.js, not '
  + 'review-engine.js, because it is pure reports-to-graph data with no review concept involved.\n\n'
  + 'New majority-of-month attribution (`resolvePeriodAttribution`, review-engine.js) -- a pure '
  + 'day-overlap resolver, generalized beyond "a month" to also resolve quarter-level behavioral '
  + 'attribution (behavioralRatings has no month-level resolution, only quarterly), reusing one '
  + 'rule at both grains rather than inventing a second one.\n\n'
  + 'New `computeSegmentScores(review, cfg, segment, ds)` reuses computeScores\' own scoring math '
  + '(extracted to module level: `_metricsScoreAcross`/`_behavQuarterScore`/`_avgOfVals`, zero '
  + 'behavior change for computeScores itself) -- scores a segment against its OWN role\'s '
  + 'competencies and, when `ds` is supplied and the segment\'s store differs from review.loc, its '
  + 'OWN store\'s freshly-resolved targets. New `computeSegmentedReview(review, cfg, ds, '
  + 'assignmentRows)` ties timeline + attribution + scoring + a provisional month-weighted rollup '
  + '(`provisionalSegmentRollup`) into one pipeline; the no-transition case is proven to reduce to '
  + 'exactly `computeScores(review,cfg).year.overall`.\n\n'
  + 'Two real design decisions, documented in code comments and the PR body: (1) `staff_'
  + 'assignments.role` stores a DEFAULT_ROLES ladder id, but REVIEW_ROLE_TO_LADDER collapses AM/'
  + 'DM/SM onto one rung (\'sm_am_dm\') -- a bare ladder-id row cannot distinguish which of the '
  + 'three was actually held, so a new `LADDER_ROLE_TO_REVIEW_ROLE` map resolves gm/'
  + 'area_supervisor/om unambiguously and uses \'AM\' as a documented, non-arbitrary stand-in for '
  + 'the ambiguous rung (the only one of the three with real default competency content). (2) '
  + '`autoPopulateKPIs` is confirmed, by reading it, NOT segment-aware -- it resolves `loc` once '
  + 'from `review.loc` for the whole year, so a transferred segment\'s ACTUALS (not its targets, '
  + 'which computeSegmentScores independently re-resolves) still reflect the wrong store for any '
  + 'auto-sourced metric. Documented as a known, real limitation and explicitly flagged as future '
  + 'work, not fixed here (not one of this dispatch\'s 4 scope items).\n\n'
  + 'No live Supabase schema change -- pure engine/scoring logic reading the already-existing '
  + '`staff_assignments` table. 22 new tests (dispatch-154-assignment-timeline.test.js, '
  + 'dispatch-154-segmented-scoring.test.js).',
]};
