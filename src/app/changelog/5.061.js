// @ts-nocheck
export default {version:'5.061', date:'2026-08-18', changes:[
  'Dispatch20 #3 -- labor-standard engine (src/engine/labor-standard.js), the first slice of "the big one." Encodes an already-proven, owner-validated overnight analysis (memory/analysis-labor-allocation-2026-08-18.md + memory/analysis-labor-allocation-queries.sql, read in full before writing a line of code) as a reusable engine module -- this is porting a measured result, not deriving a new one.\n\nTwo independent pieces, per the source analysis:\n\n1. THE ALLOCATION VIEW (allocationByStoreDaypart/allocationDistrict) -- district-wide finding is 58% of drive-thru volume served UNDER the VLH guide, 42% OVER it, and the two halves have different owners: PM padding (Afternoon/Dinner scheduled 30-38% over guide) is a SCHEDULING problem, AM deficit (Breakfast loses ~12.5% at the punch) is an EXECUTION problem. `scheduled_vs_guide` and `punched_vs_scheduled` are returned as SEPARATE fields per store/daypart on purpose -- a single punched_vs_guide number cannot tell Bonifay (schedule short, execution fine) from Lindsay (schedule fine, people don\'t arrive) apart, and collapsing them would tell one of the two exactly the wrong thing to fix.\n\n2. THE OVERNIGHT STANDARD (overnightOpenness/overnightExcessByStore) -- the VLH guide is guest-count-driven and returns near-zero for a closed restaurant, so it can\'t say what a close-down should cost. The owner\'s operational standard fills that gap: close-down = 3-4 combined labor hours after close, pre-open = 1 manager + 2 crew clocked in 1hr before open = 3 labor hours. `preOpenLateNightFraction(openHour)` is one continuous formula (`clamp(6-openHour, 0, 1)`) generalizing the file\'s three discrete worked examples (5:00am opener -> all 3 pre-open hours land in Late Night; 5:30am -> half; 6:00am -> none) -- getting this boundary wrong overstated an early pass by 76% ($143k vs $81k) per the source doc, so it\'s load-bearing, not cosmetic.\n\nSix traps from the analysis, all ported deliberately rather than rediscovered: (1) store_labor_config.loc is unpadded, qsr_daily_activity.loc is zero-padded to 7 -- a shared unpad() normalizes both sides; (2) daypart boundaries are the VLH guide\'s own (Breakfast 5a-11a / Lunch 11a-2p / Afternoon 2p-5p / Dinner 5p-11p / Late Night 11p-5a), daypartOf() is a direct JS port of the SQL CASE statement so app and SQL can never drift onto two different boundary sets -- same class of trap as the 4am business-day cutover that recurred 5 times before becoming date.js\'s businessDate(); (3) Excel day-fraction decode (fracToHour = frac*24); (4) mixed-hours stores (24hr weekends folded into a fixed weekday schedule, e.g. Chickasha "24 HR W/E") get a safeForWeeklyAverage:false guard with per-weekday standards exposed separately instead of a misleading blended average; (5) ratio of sums throughout, never average-of-per-row-ratios; (6) a 24-slot completeness guard drops any (loc,dt) with fewer than 24 hour_slots before any ratio is computed, so a short DAR day can\'t silently inflate a rate.\n\nVERIFIED against real Supabase data, not just synthetic fixtures -- the dispatch\'s own stated bar ("if the engine gets Ponce right it has the config join, the fraction decode, the boundary and the 24-hour case all correct at once"): Ponce de Leon (loc 43701) store_labor_config confirms is_24hr:true; qsr_daily_activity for loc 0043701 across the 6 Late-Night hour_slots (05:00, 24:00-28:00) over the last 30 complete nights measures 13.16 punched hrs/night (dispatch: ~13.4) and a 2.19 crew-equivalent (dispatch: ~2.2) -- both within measurement noise of the dispatch\'s own figures, and overnightExcessByStore correctly routes a 24hr store to {na:true} rather than grading it against a close-down standard that doesn\'t apply to a store that never closes.\n\n33 new tests: daypartOf boundary reproduction (every named hour_slot + a 24-slot reconciliation), ratio-of-sums-vs-naive-average proof, the 24-slot completeness guard, a synthetic Group-A-vs-Group-B reproduction of the scheduled/punched split, overnight openness classification (closed vs Ponce-style open), all 9 real stores\' pre-open fractions/standards from the source table run as individually-named parametrized tests, all 9 stores\' overnight excess/shortfall verdicts, a dedicated Ponce de Leon (24hr, na) case, and the Chickasha mixed-hours guard.\n\nScope of this PR is deliberately the engine + tests only, not a panel. Still open, called out rather than silently deferred: (a) data-loading path for a real panel -- 40k+ raw hourly rows is too much to load client-side (same reason qsr_daily_activity_rollup exists for other panels), and a new server-side rollup table needs a schema change this environment cannot make (no direct Postgres/DDL access, same limitation as #374\'s recipe_item column); (b) panel/UI wiring itself. Both are natural next PRs once the owner is available to run any needed SQL.\n\n4321/4321 tests pass, build clean (507.31 KB / 850 KB gzip budget).',
]};
