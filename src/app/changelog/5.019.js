// @ts-nocheck
export default {version:'5.019', date:'2026-08-15', changes:[
  'Issue #303 — Labor Analytics rendered OT COST as "$0" for all 27 locations from missing data the same panel showed as "—" one column over, and the fix uncovered a second, larger bug in the same area of at-a-glance.js. `otCostEst` (labor-tools.js) used `(otHrs||0)>0&&(avgRate||0)>0 ? est : 0` — collapsing "otHrs/avgRate are missing" and "otHrs is a genuine 0" into the same false branch, so missing data silently rendered as a measured $0 result rather than "unknown". Fixed by checking `otHrs!=null&&avgRate!=null` explicitly before estimating, so missing inputs now flow through as null (matching OT HRS/DAY\'s existing null handling) rather than a false zero. The district-level "OT Cost (Period)" tile and its matching table-footer cell had NO guard at all (unlike the per-store rows, which already gated on `otCost>0`) — added an `otCostKnown` flag (true if any store actually had a reading) so the aggregate also reads "—" rather than "$0" when every store\'s input was missing, since summing `otCost||0` across all-null stores otherwise silently produces a real, displayable zero. Checked the issue\'s "+0 hrs" report before assuming it needed a fix: `avnFmt` already returns "—" for null and formats real values correctly (`v>0` gates the "+" prefix, so a genuine v=0 renders "0 hrs" not "+0 hrs") — "+0 hrs" only appears for a real small positive value rounding to 0 via `toFixed(0)`, which is correct, not a bug; left unchanged. Also checked `_avgZ` (labor-tools.js\'s own actVsNeed averager) against the same false-zero shape found in `otCostEst` — it already filters on `v!=null&&!isNaN(v)` with no positivity bias, so it was not carrying the same defect.\n\nSame investigation surfaced the actual root cause of the issue\'s second report — At A Glance shows "No labor data" for a period Labor Analytics shows a real % for. Two independent bugs, both in at-a-glance.js\'s `laborSec` compute block: (1) its own Act vs Need average used `avgOf(cScoped,\'actVsNeed\')||avgOf(lScoped,\'actVsNeed\')`, and `avgOf`\'s default filter is `x>0` — actVsNeed is a signed hour difference (negative=understaffed, 0=on target, both real and both legitimate), so every negative and zero reading was silently dropped, biasing the average positive. metric-source.js:178 already documents this exact hazard for this exact metric ("mode:\'any\' is load-bearing... \'pos\' would discard every NEGATIVE reading") — `laborPct`/`tpph` in the same block were already migrated to the auto-first `metricAvg` resolver for this reason (2026-08-06), but `actVsNeed` was missed. Routed through `metricAvg(ds,allLocs,effectiveDateRange,\'actVsNeed\')` to match. This is also why the tile\'s color (`clr:(laborSec.avn||0)>=0?\'rgba(255,255,255,.8)\':\'#f87171\'`) almost always painted its "good" (near-white) branch — not a color-token bug on its own, a data bug wearing one; folded the token conversion in while touching the line (#296 step 2 scope: `rgba(255,255,255,.8)` -> `var(--text)`) -- since landed, this line converged with #296 step 2\'s own independent identical conversion of the same site (both PRs picked var(--text)), so the rebase onto post-#310 main resolved it cleanly with no residual conflict. (2) The section\'s early-return gate (`if(!cRows.length&&!lRows.length)return null`) ran BEFORE laborPct/tpph existed and checked only raw manual-style row presence — a device with real auto-pulled data (glimpse/cash/DAR rollup, all reachable via metricAvg\'s own auto-first chain) but zero manual ctrlRows upload and zero labInRange rows would fail this check and show "No labor data for this period" while Labor Analytics, already auto-first via the same metricAvg, showed a real number for the identical range. Moved the gate to after laborPct/tpph are computed and broadened it to `!cRows.length&&!lRows.length&&laborPct==null&&tpph==null` — the section now renders whenever EITHER raw rows OR the auto-first resolution found something.\n\nVersion renumbered 5.016 -> 5.019: #310 landed as v5.017 while this sat open, and #321 landed as v5.018 immediately after -- landing 5.016 after 5.017/5.018 would have walked window.__MERIDIAN_VERSION__ backwards while changelog-version.test.js (which only guards desync between the two changelog files, not monotonicity) still passed. 5.016 goes unused; the changelog is a list, not a dense sequence.\n\n2740/2740 tests pass (repo-wide; no existing coverage touched these code paths), build clean (eager 507.72 KB gzip / headroom 342.28 KB, noise-level). Browser-verified boot-and-render pass against the built `dist` per `memory/feedback-verification-in-sandbox.md`\'s corrected recipe (serve `dist`, not the dev server) — app loads, zero console/page errors. Cannot verify the actual numbers from this sandbox (CORS blocks Supabase from `localhost`, so no authenticated data reaches either panel) — the owner should confirm OT Cost now shows "—" instead of "$0" where data is genuinely missing, and that At A Glance\'s labor section now matches Labor Analytics on a period previously showing "No labor data".',
]};
