// @ts-nocheck
export default {version:'5.082', date:'2026-08-20', changes:[
  'Dispatch44 -- CASH-003 re-expressed as a count rule, and the threshold guard widened from one file to the whole seed/migration family. Both items PR #481\'s own merge commit named as KNOWN OPEN.\n\n'
  + 'CASH-003 (manual refund/self-authorized refund rate) has been force-deactivated since dispatch #42 first measured it: a $8/$1,000-drawer-sales threshold against a rule whose real max is 0.7702 -- unreachable, INV-002\'s exact defect signature on a different rule shape. Re-measured live 2026-08-20 before touching anything (security_findings, n=670): 659 of 660 non-null subjects sit at EXACTLY 0.0000. Not a bent ruler and not just wrong units -- manual overrings are, as the owner has separately confirmed, a genuinely rare event, and a dollar RATE collapses to zero for nearly the whole population regardless of what constant sits above it. No dollar threshold in this rule\'s own range could flag the one real subject without being trivially gameable by rounding.\n\n'
  + 'The fix is the instrument, not the number: `manOverringAmt`\'s own QSRSoft API response has always carried an unpulled `manOverringQty` sibling -- the same Amt/Qty pairing every other override category in that response already has (overringAmt/overringQty -> posOverAmt/posOverCnt; refundCashAmt/refundCashQty). CASH-002 already proved this shape works (posOverCnt/drawerGC*1000, 10.7% flag rate, believable). schema-security-rules-phase1e.sql converts CASH-003 to the identical shape -- manualRefCnt/drawerGC, same scale, same comparator, min_denominator moved from 250 (a drawerSales-dollars floor, meaningless once the denominator is a transaction count) to CASH-002\'s own 25, since it\'s now literally the same physical quantity. `manual_ref_cnt` added to `audit_rows` (idempotent `alter table add column if not exists`), pulled in `qsrsoft-register-audit-pull.mjs` (mapRow/saveAuditRows), round-tripped in `src/lib/supabase.js` (saveAuditRows/loadAuditRows) and `scripts/security-rules-run.mjs` (mapAuditRow) -- no manual-upload equivalent exists (parseRegisterAudit\'s Excel header search has no Qty column to find), so this field is auto-pull-only, same as several others already in this table.\n\n'
  + 'threshold is explicitly CLEARED, not replaced with a guessed number. manual_ref_cnt has never been pulled -- there is no measured range to set a floor inside yet, the same discipline phase1c.sql applied to INV-002 (no min_value rather than an invented one). `evaluateRule()` needs no change at all -- rules are data, and a plain ratio\'s numerator/denominator field names are the only thing that moved. CASH-003 stays `active = false` (the batch job only fetches `active = true` rows, unaffected by this migration); the real threshold gets set from a live re-measurement after the next register-audit pull backfills real counts.\n\n'
  + 'Second item: `security-rules-thresholds.test.js` previously only guarded phase1c.sql\'s z-score pair (min_value). Widened to guard `threshold.default` on every ratio rule across schema-security-rules.sql (CASH-001/002\'s original seed), schema-security-rules-phase1.sql (CASH-004, and CASH-003\'s own now-superseded 8), and phase1e.sql (CASH-003 post-fix) -- a second SQL parser (`extractInsertRules`) added alongside the existing UPDATE-statement one for the INSERT-shaped seed files, since a positional VALUES tuple and a named SET clause need different regexes. Closes the defect class dispatch #42\'s review found one instance of, not just that one instance: CASH-001 (max 38.887 vs threshold 5), CASH-002 (max 80 vs 15), and CASH-004 (max 162.15 vs 100) all measured live and confirmed comfortably reachable -- included not because they needed a fix, but because the guard is only real protection if it covers every rule that sets an absolute comparator value, not just the two that already broke.\n\n'
  + '13 new tests: 2 in register-audit-pull.test.js (manualRefCnt maps from manOverringQty, distinct from manualRefAmt; null when the API omits it), 2 mapAuditRow assertions folded into the existing test, 4 CASH-003 wiring tests in security-rules-run.test.js (call-site, not just engine -- a real count produces a real rate, the min_denominator floor still applies now that the denominator changed fields, a zero-count subject rates exactly 0 not null, and with no threshold configured the verdict is an honest undetermined null), 5 net-new threshold-guard tests (parses all 4 source files, confirms CASH-003\'s old phase1.sql threshold really was 8/unreachable, confirms the new file explicitly clears it, confirms CASH-001/002/004 all sit inside range). 1718/1718 suite passes, build clean, no entry-chunk impact (512.81 KB eager, unchanged budget). Full writeup: memory/dispatch44-cash003-count-rule.md.',
]};
