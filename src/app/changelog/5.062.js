// @ts-nocheck
export default {version:'5.062', date:'2026-08-18', changes:[
  'Dispatch20 addendum -- price-event detection, the vs-LY young-restaurant trap, and a real count-cycle logic bug found by chasing an already-flagged anomaly rather than theorizing about it.\n\n'
  + 'Meridian could not see a price change. Built src/engine/price-events.js: detectPriceSteps() confirms a base-menu-price step only where max(price) per (loc,item,date) held flat >=14 observed days before AND >=14 after (promo tiers sit below menu price and get filtered by the max()); the naive day-over-day tier diff is pure noise, independently re-measured this session at 50-170+ "changes"/store on literally every date tried, including a control date with no known price event. Verified against 763k real qsr_product_mix rows (May16-Jul24): the unfiltered algorithm splits all 27 stores cleanly into exactly 14 (2026-06-13) and 13 (2026-06-26) confirmed repricing events, zero overlap, zero unaccounted stores -- an exact match to the dispatch\'s own claimed wave counts, with no tuning. (An earlier >=8-items-changed floor on grouping events was removed after it was measured to be WRONG -- it silently dropped 4 real stores whose confirmed change only touched 5-7 items that day.) Also caught and fixed before it shipped: qsr_product_mix.loc is zero-padded ("0013113"), every consumer this engine feeds (ds.laborRows, store.loc, userEvents) is unpadded ("13113") -- the classic join trap, normalized once at the engine boundary.\n\n'
  + 'Three consumers, not just the engine (the #366 failure mode -- an engine nobody calls): '
  + '(1) src/engine/signal-registry.js -- a Pricing metric group (days-since-last-change, items-repriced, mean-step-%), plus a seed signal (Days Since Price Change -> Guest Count, the sibling to the existing weather-sales seed). '
  + '(2) src/utils/events.js -- computeEventFactors() now merges auto-detected price-change events into userEvents on the way in (additive -- a real hand-entered event on the same date keeps its own tags and gains price_change alongside them, never replaced), so a repricing week gets its own forecast-factor bucket instead of being averaged into ordinary trailing data. No-op when ds.pmixRows is absent, verified byte-identical against every existing caller. '
  + '(3) src/views/store-dash.js -- a "Last price change" line on the District Grid StoreCard, computed once per district pass via src/engine/price-events.js\'s lastPriceChangeByStore.\n\n'
  + 'The vs-LY young-restaurant trap. A store under ~24 months old is still lapping its own grand-opening honeymoon and its vs-LY improves on its own regardless of operations. Tishomingo (opened 2024-12-16) ranked 2nd-best of 26 stores on the district\'s GC-vs-LY ranking purely from this artifact. `product_sales > 0` is not a safe "when did real trading start" anchor -- verified live: Tishomingo\'s first >0 day is 2024-12-13 at $104.97/14 transactions, a training/soft-open day; real trading starts 2024-12-16 at $10,058/816 transactions, a 58x jump the very next available day (Ponce de Leon\'s 2026-03-13 open, by contrast, is genuine -- 514+ transactions from day one, no training-day noise). Added firstRealTradingDate() and lyQuality() to src/engine/vs-ly.js (where the matched-day logic already lives, not a fourth comparison helper): a candidate day only anchors if the NEXT day also clears a 50-transaction floor, ruling out an isolated training-day spike. matchedVsLY() now additionally returns `flagged` (locs whose LY leg falls inside the honeymoon window) without changing its existing cur/ly/pct/days output for any caller. Wired into store-dash.js\'s RankingView GC-vs-LY ranking: a flagged store renders with a muted "New store" badge instead of a clean top-performer rank; a store with no comparable LY data at all renders "no LY" instead of a bare dash.\n\n'
  + 'Count Cycle: chased, rather than left flagged, #410\'s surfaced-but-not-investigated finding that all 27 stores read crit/weekly-overdue on the same day. Per the dispatch\'s own discriminator ("re-run the grade against several dates that are not today -- every date crit -> logic, only today -> stale feed"), re-graded against 6 different asOf dates on the same real 7,347-row qsr_onhand pull: every date from 08-10 onward read 27/27 crit, confirming a logic bug, not a stale feed. Root cause: 967/978 (98.9%) of ALL Condiment-class rows district-wide read active=false in the API, none Topic-6-rescued, leaving 17/27 stores with a totals[loc].Condiment of exactly 0 -- and the old `(totals[loc][c] || Infinity) * COVER_FRAC` threshold made a 0-item class permanently uncoverable, so those stores\' Food+Condiment weekly requirement was mathematically impossible to satisfy no matter what they actually counted. Fixed in src/engine/count-cycle.js: a class with zero active items is now trivially "covered" (nothing to count), while a store WITH real active items in that class still has to count them (a dedicated test proves the fix doesn\'t weaken the rule generally). Measured impact on the same real data: 27/27 crit -> 10 ok / 5 warn / 12 crit -- a real, varied, actionable distribution instead of a permanently-tripped alarm nobody can act on. Full writeup: memory/count-cycle-condiment-bug-2026-08-18.md.\n\n'
  + 'The two source memory files this dispatch cited (mcvalue-verification.sql, analysis-mcvalue-price-waves-2026-08-18.md) do not exist anywhere in this repo -- checked git log --all and the filesystem, genuinely absent. Flagged rather than silently assumed; everything above was independently re-derived and re-measured against live Supabase data instead.\n\n'
  + '4322/4322 tests pass (33 new: 15 price-events, 6 events-price-integration, 8 vs-ly, 4 count-cycle). Build clean, 508.92 KB / 850 KB gzip budget.',
]};
