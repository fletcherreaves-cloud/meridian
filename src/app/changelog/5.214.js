// @ts-nocheck
export default {version:'5.214', date:'2026-08-27', changes:[
  'Dispatch #168 -- extended At-A-Glance\'s per-tile "As of" date-span label to Store Dashboard\'s '
  + 'KPI cards. Measured before drafting the dispatch: at-a-glance.js already ships this '
  + '(_spanTag(), v4.837, wired into 8 tile headers) but store-analytics.js\'s StoreDash (the real '
  + '"Store Dashboard" panel App.js renders -- NOT src/views/store-dash.js, which is a component '
  + 'library StoreDash imports scorecards/charts from, confirmed by reading App.js\'s own '
  + 'lazyPanel() wiring before touching either file) had zero per-tile freshness labeling. Grepped '
  + 'src/utils/ for an existing date-span helper first per this repo\'s "check whether a helper '
  + 'exists before writing one" rule -- none found.\n\n'
  + 'Part 1 (refactor, At-A-Glance unchanged): extracted _spanTag\'s pure half -- the date-range-'
  + 'from-rows computation plus label/tooltip formatting, NOT the parts closed over At-A-Glance\'s '
  + 'own local today/effectiveDateRange/dateRange state -- into spanTagInfo(rows, today, {isFallback, '
  + 'fallbackLabel}) in src/utils/date.js. At-A-Glance\'s own _spanTag is now a 9-line wrapper '
  + 'around it; all 8 existing call sites are untouched. Verified byte-identical: every existing '
  + 'at-a-glance test still passes unchanged, plus a new render test asserts the exact rendered '
  + 'text/tooltip/color for both the normal and ⚠ fallback cases against the real AtAGlance '
  + 'component.\n\n'
  + 'Part 2 (new coverage): StoreDash\'s OEPE/TPPH/Labor %/Cash O/S KPI cards now each carry their '
  + 'own span tag, sourced from THEIR OWN rows via metric-source.js\'s metricSeries (never the raw '
  + 'ops/ctrl/labor row arrays directly, per the "source data through the shared helpers" standing '
  + 'rule -- ratchet-raw-metric-rows.test.js stayed at its 160 ceiling) -- not the toolbar '
  + 'dateRange, since these four are compute6wk\'s fixed 6-week trailing window and were never '
  + 'toolbar-scoped to begin with; tagging them with the toolbar range would have misstated what '
  + 'they show. Each tag still carries the toolbar-relative ⚠ fallback note (same 30-day-lookback '
  + 'semantics as At-A-Glance\'s effectiveDateRange, reused as-is per the dispatch\'s own out-of-'
  + 'scope note -- not a new threshold) when the metric\'s own rows have nothing inside the '
  + 'selected period. Period Sales/Ops Score/Controls/T2W Trend are deliberately left untagged and '
  + 'called out in the PR body rather than forced: Period Sales blends live forecast days with '
  + 'actuals (a span tag would misrepresent forecast-only days as data), and Ops Score/Controls/'
  + 'T2W Trend are composites or two-window comparisons with no single rows set to span -- the '
  + '"skip tiles with no natural rows concept" instruction, not a coverage gap.\n\n'
  + 'Verification: 3 new render-based tests against the real StoreDash/AtAGlance components '
  + '(src/__tests__/dispatch-168-span-tag.test.js) -- StoreDash\'s 4 tagged KPIs all show the '
  + 'correct span + ⚠ fallback note when the toolbar range is entirely in the future (dateRange in '
  + 'no metric\'s own rows), a same-fixture case confirming the tag renders WITHOUT ⚠ when the row '
  + 'genuinely falls inside the selected period, and an At-A-Glance Controls-tile case proving a '
  + 'section\'s span tag reflects its OWN rows (ctrlRows) rather than a district-wide date another '
  + 'section\'s rows (laborRows) would suggest. Full suite: 2931/2931 (280 files). Build clean; '
  + 'entry-chunk eager total 550.78 KB -> 551.16 KB gzip (+0.38 KB, budget 850 KB).\n\n'
  + 'Out of scope, per the dispatch: Projections, Analytics, and every other panel are untouched '
  + 'even though several share the same "what am I looking at, time-frame-wise" gap -- noted here '
  + 'rather than silently expanded into.',
]};
