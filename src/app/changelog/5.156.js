// @ts-nocheck
export default {version:'5.156', date:'2026-08-25', changes:[
  'Dispatch #115: fix the Base Food % tolerance check\'s metric-definition mismatch -- a false '
  + 'CRITICAL finding firing on all 27 stores, every period, in the Coaching pipeline dispatch '
  + '#94 built (surfaced and explicitly deferred there, see 5.139.js\'s CAVEAT).\n\n'
  + 'tolerance-status.js\'s baseFd check compared totalBaseFood/sales (QSRSoft\'s own "Base Food" '
  + 'line on the Food-Over-Base report -- confirmed by tracing scripts/qsrsoft-pull.mjs\'s '
  + 'SELECT_COLS to the actual-food-over-base endpoint) against tFOBBase, the yearly workbook\'s '
  + 'narrow "Base Food %" variance-tolerance target (~3.8-4.6%). Live-measured against real '
  + 'qsr_fob (SUPABASE_SERVICE_ROLE_KEY, 27 stores): totalBaseFood/sales sits at ~21-24% of '
  + 'sales, and reconstructs P&L Total Food Cost % almost exactly once the six FOB variance '
  + 'components (comp/raw waste, condiments, emp meals, stat var, unexplained) are added back in '
  + '-- confirming totalBaseFood is the THEORETICAL portion of total food cost, not the same '
  + 'quantity tFOBBase names (which correctly pairs with the manual fobRows.baseFoodPct side of '
  + 'this same metric, from a different report column). Reproduced the bug live against the real, '
  + 'unmodified module: 27/27 stores red, matching dispatch #94\'s original finding exactly.\n\n'
  + 'The dispatch\'s own magnitude hint favored repointing to tFOBTotal (~26-29%) instead -- '
  + 'measured that too rather than assuming it: tFOBTotal is the P&L ACTUAL total, not the '
  + 'theoretical base, and totalBaseFood misses it by a uniform ~4-5pp on every single store '
  + '(e.g. store 3708: 23.36% actual vs 27.95% target). At a 0.5% tol, that\'s still uniformly red '
  + 'on every store -- a smaller-magnitude version of the same false-positive failure, not a real '
  + 'signal. No target field in the current schema represents the theoretical-base quantity '
  + 'totalBaseFood actually is, so the fix disables the comparison (offKey:null, TOL_METRICS\' own '
  + 'established convention, already used by 8 other metrics) rather than repointing to a '
  + 'still-wrong number. TOL_SPEC\'s value-sourcing entry is untouched -- the metric still renders '
  + 'its real current value in UnifiedTargetsPanel\'s KPI table, just without a false status '
  + 'color.\n\n'
  + 'Verified end-to-end against the real Coaching pipeline (buildStore -> buildBrief, dispatch '
  + '#94 Phase 3\'s consumer), not tolStatusesForStore in isolation: pre-fix produces a tolBaseFd '
  + 'CRITICAL finding with the exact false numbers ("23.30% vs 4.00% target"); post-fix, none. '
  + 'Per the "would this verification still pass if reverted" rule, hand-confirmed the new test '
  + 'fails against a git-stash revert of the fix alone (reproducing that exact finding), then '
  + 'restored the fix and confirmed it passes again.\n\n'
  + 'Checked for the same mismatch elsewhere per the dispatch\'s own step 4 -- found it in 2 more '
  + 'places, documented but NOT fixed (separate code paths with no offKey:null-equivalent '
  + 'mechanism, real follow-on work): analytics.js\'s FOB_COMP baseFoodPct row feeds a live, '
  + 'user-visible false "Base Food" KPI card AND a Root-Cause Priority Matrix whose own caption '
  + '("Excludes Base Food") the current filter doesn\'t actually implement; at-a-glance.js '
  + 'computes the identical wrong pairing but never renders it (dead code, no visible bug today). '
  + 'Full writeup: memory/dispatch-115.md Resolution section.\n\n'
  + 'New test dispatch-115-basefd-mismatch.test.js (4 new). Suite: 230/230 files, 2380/2380 '
  + 'tests passing. Build clean; tolerance-status.js is a lazy chunk, change is comment + one '
  + 'field value -- eager-payload budget unaffected (528.03 KB gzip of 850 KB budget, 321.97 KB '
  + 'headroom).',
]};
