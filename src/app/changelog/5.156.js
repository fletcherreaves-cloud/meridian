// @ts-nocheck
export default {version:'5.156', date:'2026-08-25', changes:[
  'Dispatch #118: Visit Readiness -- Days-Since-Last-Visit column + real column headers over '
  + 'both lists, presentation-only.\n\n'
  + '`src/views/visit-readiness.js` had two lists with no real header row positioned over their '
  + 'data columns: the main per-store `StoreRow` list (Speed/Accuracy/Quality/Leadership carried '
  + 'their own inline label repeated on every single row, and `s.lastVisit` showed only a small '
  + '"last CFV 92%" caption with no days-since figure), and VisitPatterns\' "Frequency by store" '
  + 'block (its columns were named once in a plain-text caption -- "visits · avg days between · '
  + 'days since last · pass" -- rather than as real header cells aligned over each value).\n\n'
  + 'Fix: a shared `daysSince(ms)` helper (`src/engine/visit-readiness.js`) -- the exact '
  + '`Math.round((Date.now() - ms) / 864e5)` arithmetic `analyzeGradedVisits()` already used for '
  + '`freq.daysSinceLast`, now reused by both that call site and the new per-store figure instead '
  + 'of a second inline copy. `StoreRow`\'s Speed/Accuracy/Quality/Leadership mini-bars moved from '
  + 'a labeled vertical stack (one label per bar, per row) to four fixed-width `StatCol` columns '
  + '(bar + score only), with a new `StoreListHeader` component stating each label once, reusing '
  + 'the exact column widths (`STORE_SCORE_W`/`STAT_COL_W`/`DAYS_SINCE_W`/`CHEVRON_W`) so header '
  + 'cells sit directly over their matching data column at any wrap width. A new Days-Since-Visit '
  + 'column shows `daysSince(s.lastVisit?.ms) + \'d\'`, or "-" when `s.lastVisit` is null -- same '
  + 'explicit-gap convention the rest of the panel already uses for missing data. '
  + 'Frequency-by-store keeps its existing per-row widths (flex store name, 24/42/42/40) but gets '
  + 'a real header row (Store/Visits/Avg gap/Since last/Pass) in place of the old inline caption. '
  + 'Presentation-only: `computeVisitReadiness()`/`analyzeGradedVisits()`\'s scoring, weighting and '
  + 'aggregation are untouched. `CoverageGaps`/`StoreAudit`/`storeReportHTML()` untouched (already '
  + 'real `<table>`/`<th>` markup, out of scope per the dispatch).\n\n'
  + 'Verification renders the ACTUAL VisitReadinessPanel (react-dom/client createRoot) with a '
  + 'synthetic two-store fixture (one with a graded visit 40 days ago, one with none): confirms '
  + 'the header row renders exactly once and precedes both stores\' rows in DOM order, the visited '
  + 'store\'s own row shows the correct "40d" figure, the unvisited store\'s row shows "-", and '
  + 'the Frequency-by-store block gets real Visits/Avg gap/Since last/Pass header cells instead of '
  + 'the retired plain-text caption. 3 new tests in '
  + 'src/__tests__/dispatch-118-visit-readiness-headers.test.js. `npm run build` clean, eager '
  + 'payload 528.04 KB gzip (budget 850 KB, unchanged -- visit-readiness.js was already lazy). '
  + '2386/2386 tests pass (231 files, up from 2383/230 on main).',
]};
