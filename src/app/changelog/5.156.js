// @ts-nocheck
export default {version:'5.156', date:'2026-08-25', changes:[
  'Dispatch #117: MBI vs LifeLenz Accuracy -- guard against implausible LifeLenz actuals.\n\n'
  + 'Real incident (owner screenshot, week of Aug 5-11 2026): a multi-day LifeLenz pull outage '
  + '(lifelenz-pull.yml failing Aug 7-10) left Aug 5 with a small NONZERO partial-day capture in '
  + '`lifelenz_schedule.sales` ($475 actual against a $17,885 forecast, ratio 0.027) instead of the '
  + 'null a full miss produces -- the recovery run\'s LIFELENZ_SAFETY_DAYS=3 window never reached '
  + 'back far enough to re-pull it. `runAccuracy`\'s `lfzVarPct` formula in `src/features/lifelenz.js` '
  + 'only guarded `actual>0`, so the partial actual sailed through as +3665.94% and then poisoned '
  + '`groupAccByWeek`\'s plain-averaged `avgLfzAbsVar` into a meaningless 734.38% for the whole week '
  + '-- next to a normal-looking 1.34% on the MBI side for the same week.\n\n'
  + 'Fix: a plausibility guard on the LifeLenz leg only (MBI\'s `mbi.actual` comes from the separately '
  + 'sourced `forecast_snapshots` pipeline and was left untouched -- no evidence found of the same '
  + 'failure mode there). A day whose `lfz.actual` is under 15% of `lfz.forecast` is now treated the '
  + 'same way a null/zero actual already was: excluded from that day\'s `lfzVarPct` (renders '
  + '"Incomplete" instead of a wild %, with a tooltip explaining why) and excluded from the week\'s '
  + '`avgLfzAbsVar`. The underlying dollar figures still render -- only the variance math is '
  + 'suppressed. 0.15 was picked from this dispatch\'s own production measurement of '
  + '`lifelenz_schedule.sales`/`fcst_sales` (0.88-1.03 on 42 normal days spanning 2026-07-20 to '
  + '2026-08-24, vs 0.027-0.039 during the outage): a same-store trailing forecast does not miss its '
  + 'own actual by 6-7x in reality, even on a genuinely bad forecast day, because it is calibrated off '
  + 'the store\'s own recent sales rather than an unrelated guess -- a ratio that low is far more '
  + 'consistent with a partial/incomplete pull than a real closed day. A real severe-weather closure '
  + 'could in principle also read under 0.15 and get flagged as "Incomplete" too; accepted as a rare '
  + 'false positive, since "no trustworthy LFZ number for this day" reads truthfully either way and is '
  + 'far less likely than a pull artifact. Does NOT suppress a genuinely bad LifeLenz forecast -- a '
  + 'day with a large miss but a plausible actual (e.g. 2x over-forecast, actual ratio 0.5) still shows '
  + 'its real, large variance; covered by its own test.\n\n'
  + 'Out of scope per dispatch-117.md, left untouched: `lifelenz-pull.mjs`\'s SAFETY_DAYS/gap-detection '
  + 'logic (a real possible follow-up, but a riskier production-pipeline change), sync-failure-watch.yml, '
  + 'and runLifeLenzBridgeScan/the Single Store Bridge view. The Aug 5-7 production data itself is '
  + 'NOT yet backfilled -- a same-day backfill attempt failed on a currently-live, separate LifeLenz '
  + 'auth outage (see dispatch-117.md\'s correction section); this PR is UI-defensiveness only and does '
  + 'not depend on that backfill landing.\n\n'
  + 'Verification: 3 new tests in src/__tests__/dispatch-117-lifelenz-accuracy-plausibility.test.js, '
  + 'rendering the ACTUAL LifeLenzBridgePanel Accuracy tab (react-dom/client createRoot, not '
  + 'runAccuracy/groupAccByWeek in isolation) with a synthetic 7-day fixture reproducing the '
  + 'incident\'s exact shape (one implausible-partial day + one null-actual day + five normal days) -- '
  + 'confirms the partial day renders "Incomplete" (never the poisoned 3665%/734% figures) while the '
  + 'week\'s avgLfzAbsVar matches the hand-computed ~0.81% from only the 5 plausible days; confirms a '
  + 'genuinely bad forecast with a plausible actual still shows its real variance, unsuppressed; and '
  + 'confirms a normal week with no incomplete days is byte-for-byte unaffected (additive-only guard). '
  + '2382/2382 tests pass (2379 on main + 3 new; one unrelated pre-existing flaky timeout in '
  + 'visit-readiness-caption.test.js reproduced identically on main under full-suite load, passes '
  + 'standalone), build clean, entry chunk unchanged (no new imports, edit is inside an already-lazy '
  + 'panel module). Full resolution: memory/dispatch-117.md.',
]};
