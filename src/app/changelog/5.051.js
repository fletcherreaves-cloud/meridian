// @ts-nocheck
export default {version:'5.051', date:'2026-08-18', changes:[
  'Issue #391/dispatch19 -- calendar events vanish (truncated at ~733 entries, ending June 2026) even though 11,163+ cloud events hydrate every session. Named from the #391 instrumentation shipped one dispatch earlier (measure first, do not patch on suspicion): one reload showed org-events hydration writing 7274 entries, immediately followed by the pre-transition re-sync reading 0 entries from localStorage and REPLACING React state with it. App.js\'s file-load transition re-syncs userEvents from localStorage as a defensive freshness read (catching anything an import tool wrote directly to storage during file parsing) -- but it used `setUserEvents(_uploadEvents)`, replacing state wholesale with whatever localStorage held at that exact instant. That races the async org-events cloud hydration effect, which writes localStorage then calls setUserEvents with the same object -- nothing orders the two. When the synchronous re-sync read runs before hydration\'s write lands, it reads stale data (0 entries on a fresh browser, 733 on production, matching both symptoms seen) and the replace wipes out whatever hydration already put into React state. Consistent, not intermittent: the re-sync reliably wins the race, which is why this shipped invisibly.\n\nFix: merge instead of replace, reusing the exact non-destructive precedence the hydration effect itself already uses (fill missing entries, refresh ones previously stamped orgSourced, never touch a hand-entered event). A localStorage read with fewer entries than current React state can now only ADD, never shrink, regardless of which side of the race actually finishes first. Audited all six mf_events localStorage.setItem call sites in App.js for the same replace-vs-merge question: the tag/remove-event listeners and the AIBacktestScanner handler already use React\'s functional setState form (reading the latest state via `prev`, not a stale snapshot) and are race-safe by construction; the legacy tag-key migration operates on localStorage at its own execution time and only renames keys, never drops values. The pre-transition re-sync was the one genuinely unsafe site.\n\nAlso fixed: the bare `catch{}` on every mf_events localStorage.setItem call (6 sites) now logs the error instead of swallowing it silently -- flagged as a standing defect even where innocent, since it was directly why ruling out a full localStorage quota took three hours of dead-end investigation before the real writer was found by instrumentation instead.\n\nNot completed, reported rather than guessed: the full verification bar (month histogram showing non-zero counts through the forward horizon including football fixtures, the calendar rendering August events with a stated count) needs the owner\'s real authenticated browser session -- this environment cannot reproduce Supabase-authenticated calendar data. Per dispatch19\'s own instruction, the #391 instrumentation stays in for one more real-browser verification run to confirm hydration\'s write now survives the race, and gets removed in a follow-up commit once that\'s confirmed -- not removed here.\n\n#367\'s DI Compare verdict re-run (both this fix and the week-anchor fix in #373 needed before that re-run means anything) is tracked separately and not attempted here.\n\n4252/4252 tests pass, build clean (506.95 KB gzip eager / 850 KB budget, +0.3 KB from the added error-logging, no shift otherwise).',
]};
