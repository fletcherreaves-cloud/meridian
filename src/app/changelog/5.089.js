// @ts-nocheck
export default {version:'5.089', date:'2026-08-21', changes:[
  'Dispatch #53 Phases A-D -- closes dispatch #49\'s Phase 0 gate and lands its Phase 1.\n\n'
  + 'Phase A: the 403 that stopped yesterday\'s backfill was session-token expiry, not a rate limit (six uniform ~48s chunks then a deterministic cliff at ~5 minutes -- an IAM explicit-deny doesn\'t tighten with volume). Closed the remaining 48-day tail in one 3-chunk job -- 8,507 rows, 27/27 stores, ~2.5 minutes.\n\n'
  + 'Phase B/C: row 5 (names with no empID) re-measured to 0 across the full 5.7-month window -- every one of 1,140 names now resolves to an empID. Full picture: 1,089 clean (95.5%), 51 names merged into one token (4.5%), 16 tokens split across names (1.4%) -- 67 live identity defects, real regardless of whether the re-key happens. Gate rule (written before the number): G<=25 proceed. G=0 clears it with room to spare.\n\n'
  + 'Phase D: dispatch #49\'s Phase 1, and nothing beyond it. employee_identity_vault gains a nullable employee_id column (partial unique index, additive). get_or_create_employee_token() gains a 2-arg overload -- the existing 1-arg signature every live caller uses is byte-for-byte unchanged, Postgres resolves by argument count. The new overload is opportunistic-only: never overwrites an employee_id a row already has. Nothing calls it yet -- Phase 2 (reconciliation) and Phase 3 (key switch) are explicitly NOT started, since rushed reconciliation risks attributing one real person\'s findings to another.\n\n'
  + 'Adversarially probed against a real local Postgres 16 instance, 15 probes: the new overload never leaks a name (return type uuid, checked via catalog), never overwrites an existing employee_id, and the unique index genuinely rejects a real collision. Review pass caught that last one being scored backwards: the index firing is a hard failure on 1.4% of the real population (row 4\'s 16 split eIDs), and it aborted the whole call, so the caller got no token at all for a person the 1-arg path tokenizes fine. Fixed in review -- the insert now falls back to the name-keyed path on unique_violation, leaving employee_id null and the incumbent mapping untouched. Enrichment is optional; a token is not. Re-probed, 9 checks, all revert-sensitive against the reproduced failure; the constraint itself was not loosened (a direct duplicate insert is still rejected). reveal_employee_identity() re-verified unaffected -- critically, the exact NULL-role-bypass incident shape (incident-reveal-rpc-null-role-bypass-2026-08-20.md) is still correctly rejected by the trailing ELSE after this change.\n\n'
  + '1815/1815 tests (SQL-only change, no JS call site touched yet, so no new/changed JS tests). Owner action item: apply supabase/schema-identity-vault-employee-id.sql to live Supabase. Full writeup: memory/finding-phase0-gate-result-2026-08-21.md.',
]};
