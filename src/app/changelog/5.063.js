// @ts-nocheck
export default {version:'5.063', date:'2026-08-18', changes:[
  'Dispatch21 -- a real-data regression test protecting the exact price-repricing wave split price-events.js discovered in #411 (memory/dispatch-21.md\'s one optional ask: worth pinning that specific number, not just "the engine returns something").\n\n'
  + 'Pulled the same window live from Supabase (2026-05-28 -> 2026-07-12, 501,684 real qsr_product_mix rows -- too large to commit) and ran the real detectPriceSteps()/priceChangeEvents() engine against it to reproduce #411\'s finding fresh: still exactly 14 stores confirmed-repriced on 2026-06-13, 13 on 2026-06-26, zero overlap, zero unaccounted stores, matching the district\'s 27 total. Trimmed the pull to a committed fixture (src/__tests__/fixtures/price-wave-2026-06.json, 1,608 rows) containing only ONE anchor (loc, item) per store -- whichever item\'s confirmed step actually produced that store\'s wave-date event -- since priceChangeEvents() has no minimum-items floor (per #411), a single confirmed step is sufficient signal and dropping the other ~280 items/store/day changes nothing about which store repriced on which date. Verified empirically before committing: running the engine against the trimmed 1,608-row fixture reproduces the identical 14/13 split with zero other event dates, an exact match to the full 501,684-row pull.\n\n'
  + 'New src/__tests__/price-events-wave-regression.test.js pins the exact loc lists for both waves, asserts they\'re disjoint and sum to the full 27-store district, and asserts no confirmed events land on any date other than the two known wave dates -- so a future change to the pull scripts, the parser, or the step-detection window that silently drifts off this real, previously-verified result gets caught by the suite instead of by someone noticing a chart looks different months from now.\n\n'
  + '1523/1523 tests pass (4 new). Build clean, budget unchanged.',
]};
