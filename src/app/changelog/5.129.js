// @ts-nocheck
export default {version:'5.129', date:'2026-08-24', changes:[
  'Dispatch #85: six SAGE/quick-win fixes, two of the six claims measured false and left as-code. '
  + '(1) 1000-row truncation -- every PostgREST query above the 1000-row max-rows cap was silently '
  + 'returning exactly 1000 rows with no error, which is indistinguishable from "no more data" '
  + 'unless the caller re-pages. New supabase/functions/sage-chat/paginate.js (fetchAllRows) pages '
  + 'via .range() until a short page confirms the end; applied to query_daily_activity, '
  + 'query_lifelenz_labor, query_forecast_snapshots, and both legs of query_promo_roi (6 tables). '
  + 'search_qsr_kb/.limit(60) and search_project_memory/.limit(80) were measured as intentional '
  + 'top-K search caps, a different bug class, and left untouched.\n\n'
  + '(2) Static staffing summary -- buildScheduleSummary\'s per-store gap averaging consolidated '
  + 'onto the shared, #82-tested aggregateLifelenzLabor instead of a second hand-rolled average. '
  + 'A scratch numeric comparison found the original arithmetic already correct (not the #82 bug '
  + 'class the dispatch suspected), so this is a defensive consolidation, not a confirmed fix for '
  + 'the SAGE-observed +141h/day discrepancy -- that root cause is still open.\n\n'
  + '(3) SMG OSAT fraction/percent -- buildSmgSummary compared osatTop2/dtProblem (stored as 0-1 '
  + 'fractions, per _num01 in parsers/index.js, same convention smg-voice.js and morning-brief.js '
  + 'already handle correctly) against a percent-scale threshold (<90, true for any real value) '
  + 'and printed the raw fraction with a literal "%" (e.g. "0.91%"). Every store flagged regardless '
  + 'of actual standing. Fixed at the one render site using the file\'s existing _fmtPct helper.\n\n'
  + '(4) Opportunity $ nav entry and (6) the panel-registry promotion comment -- both claims were '
  + 'measured FALSE. shell-nav-snapshot.test.js (passing, 21/21 on main before this change) already '
  + 'asserts Opportunity $ renders under Test Kitchen and that renderTestKitchen() is fully derived '
  + 'from panel.kind -- dispatch #61\'s one-field-flip derivation, not a hand-maintained navPBeta '
  + 'list. No code change for either item; adding a manual nav line would have double-rendered the '
  + 'panel, the exact bug #61 fixed.\n\n'
  + '(5) Promo ROI panel -- the #599/#601 dollar-split fix shipped hours earlier this session was '
  + 'itself found biased by a later measurement (memory/finding-promo-roi-denominator-bias-2026-08'
  + '-23.md): when promo spend scales with traffic, the split reports +16.5% mean lift and 27/27 '
  + 'stores "pays" at a true effect of zero -- worse than the original bug because it is plausibly '
  + 'wrong instead of visibly broken. Added an unmissable red warning banner to the panel and a '
  + 'matching PROMO_ROI_UNRELIABLE_NOTE returned by the SAGE tool, both naming the mechanism and '
  + 'the measured numbers. The real fix needs an exogenous treatment indicator (a promo calendar) '
  + 'and stays out of scope as a design task.\n\n'
  + 'Suite 2169/2169, build clean, eager payload 518.02 KB gzip (850 KB budget, 331.98 KB headroom '
  + '-- no new eager imports). Full writeup: memory/dispatch-85.md.',
]};
