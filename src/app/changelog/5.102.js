// @ts-nocheck
export default {version:'5.102', date:'2026-08-22', changes:[
  'Dispatch #57 (#56 Part B, for real this time) -- per-person employee tenure, storage half. '
  + 'Owner-approved reversal of a deliberate decision: qsrsoft-employee-roster-pull.mjs used to '
  + 'persist ONLY aggregate role-bucket counts (roster_role_counts) and discard every '
  + 'individual-employee field it fetched. Owner: "reverse it... Let\'s just do it all. I can '
  + 'address the role level access to certain metrics/data later." That aggregate path is '
  + 'unchanged; this is additive.\n\n'
  + 'New supabase/schema-qsr-employee-tenure.sql -- one row per person, keyed on geid '
  + '(tenant_id, loc, geid), accessible_locs-scoped read RLS (not the plain tenant-only '
  + 'pattern -- this table carries a name and a pay rate). Both start dates are stored '
  + 'DISTINCTLY: org_start_date (joined the organization) and store_start_date (joined this '
  + 'store) -- they diverge often and hugely (one measured record: 8 years with the org, 2 '
  + 'months at this store), and neither reconstructs the other. hourly_pay_rate is stored per '
  + 'the owner\'s "do it all" but deliberately NOT surfaced in any panel -- an unrendered column '
  + 'is easy to gate later, a rendered one is not.\n\n'
  + 'orgStartDate was not previously fetched at all -- added to SELECT_COLS alongside '
  + 'hourlyPayRate. What stays permanently excluded: ssn, dateOfBirth, nationalOrigin, gender, '
  + 'federalMaritalStatus, address/contact fields -- a new assertNoDeniedSelectCols() guard '
  + '(scripts/qsrsoft-employee-roster-pull.mjs) fails loudly at import time if a future edit '
  + 'ever widens the allowlist to include one of them, tested against every listed field '
  + 'individually.\n\n'
  + 'src/engine/people-reports.js\'s parseEmployeeRosterApi() -- the one shared record shape '
  + 'rosterCounts()/shiftCertifiedByLoc()/the review auto-populate all still consume unchanged '
  + '-- gained orgStartDate/hourlyPayRate fields, same "0000-00-00" sentinel-to-null cleaning as '
  + 'every other date field. New toTenureRows() maps those records to the qsr_employee_tenure '
  + 'write shape (padded loc, geid as the required key -- a record without one is dropped, not '
  + 'fabricated a placeholder key).\n\n'
  + '1989/1989 tests (15 new: field-mapping + sentinel + divergence coverage in '
  + 'people-reports.test.js, write-path row-shape + denial-guard coverage in the new '
  + 'employee-roster-tenure-pull.test.js). Build clean, no client-bundle change (both touched '
  + 'files are Node-only pull-side code). No panel and no pay surfacing in this dispatch, per '
  + 'its own explicit scope -- how tenure surfaces is a separate piece once there is data to '
  + 'look at.',
]};
