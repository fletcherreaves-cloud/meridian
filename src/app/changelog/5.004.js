// @ts-nocheck
export default {version:'5.004', date:'2026-08-12', changes:[
  'Issue #242 — pre-emptive guard, no live bug when this landed. Placed ahead of the first real consumer (#211\'s Labor instantiation) per the owner\'s explicit request, not after. #241 measured qsr_labor_summary over July 2026 (31 days, all 27 stores): salaried_manager_hours and salaried_manager_dollars populate INDEPENDENTLY — only 4 of 27 stores (Chipley-St Rd 77, Cottondale, Mossy Head, Freeport) have both; 12 have exactly one (2 dollars-only: DeFuniak Springs, Bonifay; 10 hours-only: Chickasha, Seminole, Purcell, Lindsay, OKC-I240/Sooner, Ardmore-Cooper, Elgin, Tecumseh, Harrah, and Ponce de Leon); 11 have neither. Reading total_hours/gross_dollars naively on a partial store produces a number wrong in an invisible direction, not a missing one — #241 measured Bonifay\'s average wage +13.7% high (dollars carry salaried cost, hours don\'t) and Ponce de Leon\'s hours denominator +6.7% heavy (hours carry salaried time, dollars don\'t). Ponce de Leon being FL and landing in the hours-only bucket is exactly why this can\'t be keyed on FL/OK, per the issue\'s own explicit instruction — coverage must be derived from the data every time, never a hardcoded market list. Built: engine/salaried-coverage.js\'s salariedCoverage(rows) sums salaried_manager_hours/salaried_manager_dollars across whatever rows the caller passes and returns \'complete\'|\'partial\'|\'absent\' per #241\'s own spec (complete = both >0, partial = exactly one >0 -> renders "not configured" never a number, absent = neither); deriveIfSalariedComplete(rows, computeFn) wraps any future metric (average wage, Total Labor %, any total_hours-denominated productivity figure) to return null unless coverage is complete. Two guard tests: salaried-coverage.test.js (10 unit tests on the classifier — complete/dollars-only-partial/hours-only-partial/absent/missing-fields/multi-row-summing/no-market-key) and salaried-coverage-guard.test.js (5 tests, source-parses every file under src/engine|views|lib|features|app except the classifier itself for a direct .total_hours/.gross_dollars/.salaried_manager_hours/.salaried_manager_dollars read and asserts zero — currently green because #241\'s own "current state, verified" table confirmed zero real consumers exist yet; this is the regression guard for the day something reaches for one of these fields without going through salariedCoverage() first, same source-parse shape as #231\'s dead-prop guard). \\.total_hours\\b deliberately does not match .over_time_total_hours (the already-verified-safe OT field at-a-glance.js/metric-source.js/eom-supervisor.js/supabase.js already read) since the dot there is followed by "over_time_total_hours" not "total_hours" — verified the guard doesn\'t false-positive on those 5 existing safe call sites before treating it as done. Explicitly not done, per the issue\'s own scope: no loader change (loadOpsLaborSummary doesn\'t alias these fields yet — the ?? null discipline is a rule for whoever adds that alias next, not a change needed today) and no change to existing consumers (they read only already-safe OT/crew fields and work correctly). 1350/1350 tests pass (1335 + 15 new). Entry chunk unaffected — the new engine file has zero importers yet, by design (guards ahead of the first consumer, not after).',
]};
