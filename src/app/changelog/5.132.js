// @ts-nocheck
export default {version:'5.132', date:'2026-08-24', changes:[
  'Dispatch #88: the three notes-67 owner-reported bugs (Food Cost date default, Speed of '
  + 'Service DT History 15+s load, Forecast Audit "greyed out"). Every "already diagnosed" claim '
  + 'in the dispatch was re-verified before building on it, per its own instruction -- one needed '
  + 'correcting.\n\n'
  + '(1) Food Cost date default: neither of the dispatch\'s guessed shapes was right. Confirmed '
  + 'live against Supabase that qsr_fob does NOT stop in May (real prod_sales_amt through '
  + '2026-08-24), ruling out the "lapsed stream" worry. The actual cause: FOBAnalysisPanel\'s '
  + 'auto-select effect used `!selMonth` as a run-once guard, which fired on the FIRST render -- '
  + 'before the async cloud fetch (qsrFobRows, starts null) resolved -- against manual-upload-only '
  + 'months, locking in a stale month (May) and then permanently ignoring the cloud stream\'s '
  + 'newer months once they arrived. Fixed by gating the auto-select on qsrFobRows!==null. Test '
  + 'reproduces the race against unmodified code first (confirmed failing), then confirms the fix '
  + 'and a no-false-positive case. Checked FOBEOMPanel (similar name, unrelated manual-upload '
  + 'tool) -- does not share the pattern.\n\n'
  + '(2) DT History 15+s load: confirmed the panel needs hour-slot granularity (hourData/'
  + 'daypartData both aggregate by hour_slot), ruling out the daily-rollup-view fix per the '
  + 'dispatch\'s own conditional. Converted loadDtHistory from fetchAll (strictly sequential, ~58 '
  + 'serial round-trips for 90d x 27 stores x 24 slots) to the existing _pagedParallel (shared '
  + '_MAX_INFLIGHT=6 cap), adding an additive extraFilter param for the dt_trans_cnt>0 filter '
  + '_pagedParallel\'s existing shortcuts didn\'t cover. No live production Supabase session is '
  + 'reachable from this sandbox to trace a true before/after (qsr_daily_activity is RLS-'
  + 'restricted -- confirmed live, anon key sees 0 rows), same limitation #191\'s identical '
  + 'loadQsrFob migration shipped under. What WAS measured directly: a 6-test suite against a '
  + 'mock sized to the real 58-page/6-inflight shape confirms full row coverage, the filter '
  + 'reaching both the head-count query and the fetchAll fallback, ordering preserved, a failed '
  + 'page still surfacing _recordDataError under the dtHistory label, and a scheduling '
  + 'measurement: 58 sequential rounds (1771ms) vs 11 parallel rounds (336ms) at a fixed 30ms '
  + 'simulated per-request latency -- 5.27x fewer round-trip rounds, matching the analytical '
  + '58-vs-11 prediction. Real production milliseconds will differ; this isolates the scheduling '
  + 'change itself.\n\n'
  + '(3) Forecast Audit "greyed out": premise was false. git archaeology + an actual render '
  + 'confirmed the explanatory tooltip ("Select a store first") already shipped in v4.945/PR#120, '
  + 'two weeks before this dispatch was written. No code change -- added the missing regression '
  + 'test, rendering the real AppSidebar consumer so a future break in the wiring (not just the '
  + 'registry field) would be caught.\n\n'
  + 'Suite 2219/2219, build clean, 518.38 KB gzip eager payload (850 KB budget, unchanged shape -- '
  + 'no new eager imports). Full writeup: memory/dispatch-88.md.',
]};
