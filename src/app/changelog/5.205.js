// @ts-nocheck
export default {version:'5.205', date:'2026-08-27', changes:[
  'Fixed: Performance Reviews\' "Auto-fill from Uploaded Data" stopped populating OEPE/R2P/KVS/ '
  + 'Labor % past June for a GM review (store 5985), even though the owner had confirmed July and '
  + 'August already had real, live QSRSoft DAR data (measured this session via the service-role '
  + 'curl recipe: qsr_daily_activity_rollup content-range 0-0/57 for that store, July 1 - August '
  + '31; a pulled row carries real dt_untilserve/dt_untilstore/dt_heldtime/dt_trans_cnt, giving a '
  + 'real oepe=123s for 2026-08-10). Root cause: the auto-fill button was gated only on '
  + '`ds?.loaded`, which flips true off the local IDB restore alone -- a separate, much faster '
  + 'effect than App.js\'s "T1" tiered Supabase load that actually populates qsrActSummaryRows/ '
  + 'glimpseRows/opsServiceRows, the exact 3 auto/cloud sources metric-source.js\'s oepe/r2p/kvst/ '
  + 'laborPct chains check BEFORE the manual Ops Report fallback. A click in the real window '
  + 'between those two -- IDB restore is near-instant, T1 is a genuine network round-trip -- ran '
  + 'autoPopulateKPIs against a `ds` where the auto streams simply were not there yet: per-day '
  + 'resolution silently fell through past them to whatever the already-IDB-resident manual Ops '
  + 'Report rows happened to cover (this store\'s newest manual row: 2026-07-11) and to nothing at '
  + 'all beyond that -- exactly the observed Jan-Jun-populates / Jul-Dec-blank split. Ruled out '
  + 'first: the resolver\'s `daysBack` windows (App.js actually requests 60 days, not the 35/45 '
  + 'originally assumed, comfortably reaching August) and metric-source.js\'s math (the same real '
  + 'August row resolves correctly once it is actually present in `ds`). Fix: App.js now tracks '
  + 'when T1 has actually landed and passes it down (PerformanceReviewsPanel -> ReviewEditor -> '
  + 'KPITab) as `dataReady`; the button stays disabled with a "still loading live data" hint until '
  + 'then, instead of silently running against a partial `ds`. Two new tests: one exercises the '
  + 'real autoPopulateKPIs directly against a `ds` shaped like each side of the race (proving the '
  + 'exact mechanism, and that adding the missing stream alone fixes it); the other renders the '
  + 'real button and confirms it is enabled/disabled correctly by `dataReady`, and reproducibly '
  + 'fails against the pre-fix code (button stayed enabled regardless).\n\n'
  + 'Known follow-up, NOT fixed here (flagged, not chased, per this dispatch\'s own scope): the '
  + 'review\'s FOB $ actual (`mo.foodOB`) reads `ds.fobRows` directly -- the manual-only upload '
  + 'array -- rather than the auto `qsr_fob`/fobByRange path one-pager-data.js already uses '
  + 'elsewhere. Same "manual sourcing is always temporary" class as the bug just fixed, but a '
  + 'different mechanism (a wrong SOURCE, not a loading-order race) and a larger, non-trivial '
  + 'change (the auto qsr_fob $ definition needs reconciling against fobRows\' own before it can '
  + 'safely replace it) -- left for a follow-up dispatch rather than forced in here unverified.\n\n'
  + 'Full suite 271/271 files, 2863/2863 tests (+2 files/+5 tests net new). Build clean, entry '
  + 'chunk gzip 476.26 KB (476.22 KB before -- +0.04 KB, one new prop, one new state hook, and '
  + 'this changelog entry).',
]};
