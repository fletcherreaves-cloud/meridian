// @ts-nocheck
export default {version:'5.183', date:'2026-08-26', changes:[
  'Dispatch #146: Retention Rollup workshop-week marks can now be pre-populated from the '
  + 'Organization Structure workbook instead of clicking each store manually in the Training '
  + 'Retention tab. New `parseOrgStructure(wb)` (src/parsers/index.js) reads the `Scheduling '
  + 'Setup` sheet\'s OWN "1st Schedule Week" column (header on row 2, Location col 0, date col '
  + '11/L) — never the `Locations` sheet\'s mirrored copy of the same header, which is a '
  + 'cross-sheet formula (`=IFERROR(INDEX(\'Scheduling Setup\'!$L:$L,...`) whose cached value is '
  + 'empty in the committed file. Orphan/legend rows past the real ~20 stores (e.g. '
  + '"Setup/Mgr Sched") are excluded by validating `Location` against `STORE_NAMES`, not by '
  + 'position. `detectType()` routes `Organization_Structure.xlsx` (and `org structure`/'
  + '`org_structure` filename variants) here, plus a sheet-name fallback (a `Scheduling Setup` '
  + 'sheet with a `1st Schedule Week` header cell) mirroring the existing FullScale fallback '
  + 'pattern — no new detection idiom introduced.\n\n'
  + 'A new pure `classifyOrgStructureImport(orgRows, existingMarksByLoc, today)` does the actual '
  + 'decision-making — separated from the Supabase writes so it is unit-testable without a '
  + 'client or a React render. Per row: `scheduleWeekDate <= today` marks it (weekKey via the '
  + 'existing `weekStartOf()`, src/engine/schedule-summary.js); `> today` is skipped as '
  + 'future-dated and never imported (the date gate the dispatch confirmed via the GM/Sched-Mgr '
  + 'engagement-field correlation — every past-dated row has real retrospective notes, every '
  + 'future-dated row is blank); no date at all is counted separately. **Skip-if-already-marked '
  + 'is the implemented rule**, per the dispatch\'s explicit default: a store that already carries '
  + 'ANY mark (set manually from the Training Retention tab, or from a prior run of this same '
  + 'import) is left untouched, never silently overwritten by the sheet\'s date — existing marks '
  + 'are loaded via `loadRetentionMarks()` before classifying. Data Manager\'s upload handler '
  + '(App.js `handleFiles`) wires this to `saveRetentionMark(loc, weekKey)` per row and surfaces '
  + 'a dedicated, legible upload-summary block (marked / skipped-future / already-marked / '
  + 'no-date counts) rather than a generic "N rows imported" — this import silently changes '
  + 'Retention Rollup state, so the confirmation says exactly what happened.\n\n'
  + 'Verified against the real committed `data/org-structure/Organization_Structure.xlsx` '
  + '(measured live 2026-08-26): all 20 Oklahoma stores resolve with a real "1st Schedule Week" '
  + 'date (0 no-date), 11 are past/today and get marked, 9 are future-dated and skipped — split '
  + 'recomputed from today\'s date at test time, not hardcoded, so this stays correct as the '
  + 'calendar moves. Zero changes to schedule-retention.js\'s rollup/aggregation logic — this is '
  + 'purely a new import path feeding the same `sched_retention_marks` table the manual UI '
  + 'already writes to.\n\n'
  + 'New test: src/__tests__/dispatch-146-org-structure-import.test.js (14 tests) — parser '
  + 'correctness incl. the Locations-sheet-mirror trap and orphan-row skip, detectType routing '
  + '(filename + sheet-name fallback + a negative case), classifyOrgStructureImport\'s date gate '
  + 'and skip-if-already-marked default, and a real-file cross-check against the committed '
  + 'workbook with independently-sourced ground-truth serials.\n\n'
  + 'Full suite 2659/2659 (249 files, +14 from this change); build clean. Entry 469.89 KB gzip '
  + '(468.88 KB on main, +1.01 KB); eager payload 541.07/850 KB gzip (540.06 KB on main, '
  + '+1.01 KB).',
]};
