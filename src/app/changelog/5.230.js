// @ts-nocheck
export default {version:'5.230', date:'2026-08-28', changes:[
  'Dispatch #188 -- merged End of Month (fob-eom, FOBEOMPanel) into Food Cost (fob-analysis, '
  + 'FOBAnalysisPanel) as a mode/tab, per the owner\'s 2026-08-10 decision '
  + '(memory/decisions-panel-inventory-2026-08-10.md). Harvested FOBEOMPanel\'s full '
  + 'per-store, upload-driven EOM troubleshooter (5-file Excel upload, FOB status bar, '
  + 'Priority Recount / Case-Count Review / Count Compliance / Operational Issues tabs, print '
  + 'report) unchanged -- added an `embedded` mode to it that drops its own fixed-overlay/'
  + 'title/close chrome (Food Cost\'s shell now owns those) in favor of a slim toolbar. Food '
  + 'Cost gets a small pill-style "Food Cost / End of Month" mode switcher in its header, same '
  + 'shape as the Planning/Scheduling hub tab pattern; EOM mode React.lazy()-loads fob-eom.js '
  + '(and its xlsx dependency) only when actually opened, same lazy-load discipline it had as '
  + 'its own top-level lazyPanel() entry before this merge (confirmed by the build: fob-eom is '
  + 'still its own separate chunk, not folded into analytics.js\'s).\n\n'
  + 'fob-eom retired from the sidebar (kind:\'internal\' in panel-registry.js) but deliberately '
  + 'kept route:true so an old ?panel=fob-eom bookmark still validates through routing.js -- a '
  + 'new App.js effect (routePanel===\'fob-eom\') immediately redirects it into fob-analysis '
  + 'with the EOM mode preselected, rather than 404ing to the default view.\n\n'
  + 'Panel-contract pass while already rewriting the header (opportunistic, per '
  + 'memory/panel-contract.md\'s standing per-dispatch check): fixed a real double-chrome bug '
  + 'this merge exposed -- FOBAnalysisPanel was rendering its OWN hand-rolled fixed-overlay + '
  + '"FOB Analysis" title bar + "close" button UNDER the RoutePanelShell App.js already wrapped '
  + 'it in. RoutePanelShell now lives inside the component (same "shell inside the component" '
  + 'pattern as sched-hub/count-cycle/eom-dashboard), App.js renders it unwrapped, and the '
  + 'hand-rolled-backdrop ratchet (ratchet-modal-backdrop-bypass.test.js) drops 75 -> 73. Date '
  + 'mode (Food Cost: period-anchored calendar-month picker; EOM: none, per-store upload-driven) '
  + 'and print/export (Food Cost: pre-existing ExportDropdown, unchanged; EOM: the harvested '
  + 'PrintReport button) were both already contract-compliant, confirmed rather than assumed. '
  + 'Food Cost\'s hand-rolled all/OK/FL/store <select> was left as-is -- LocationSelector\'s '
  + '{level,id} shape has no direct translation for the OK/FL market-average shortcuts without a '
  + 'real conversion, out of scope for a merge dispatch.\n\n'
  + 'Verified: full suite (3063 tests, the 16 failures on this branch\'s run all independently '
  + 'reproduced on an unmodified origin/main baseline via git stash -- pre-existing sandbox-load '
  + 'timeout flakiness in PerformanceReviewsPanel/pmix-loader tests, unrelated to this change) '
  + 'and a clean production build. Eager-payload budget unchanged (480.75 KB entry / 553.10 KB '
  + 'eager total gzip, vs. 480.77 KB / 553.12 KB on baseline -- noise, not a regression).',
]};
