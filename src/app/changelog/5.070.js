// @ts-nocheck
export default {version:'5.070', date:'2026-08-19', changes:[
  'Dispatch31 -- real click trace corrects dispatch27, closes the cache-hit measurement gap. Real Mac Mini production usage data (memory/dispatch-31.md) answers the exact re-measurement both dispatch27 (PR #426) and dispatch29 (PR #428) flagged as unreachable from this sandbox\'s browser -- and it corrects a specific claim already on main.\n\n'
  + 'Correction: dispatch27 said the 4.3s modal-close remount cost "almost certainly dropped" once Workstream A\'s forecast_week_cache landed. The real trace refutes it -- modal close (✕) is still 46.5 of ~89.8s of all long-task time in the session (52%), and the new route-panel back button (←, Workstream E) costs the SAME per click (~1435ms avg). Routing gave four panels URLs; it never touched the remount cost either one pays on close. Cite dispatch31\'s numbers going forward, not dispatch27\'s framing.\n\n'
  + 'Live-verified, not just instrumented: this session has confirmed Supabase network access (unlike the sandbox that produced dispatch27/29), so rather than leaving "incomplete cache coverage vs. cache-hit-still-isn\'t-free" as two undistinguished guesses, queried forecast_week_cache directly -- 100% coverage, all 27 stores, for the entire current business week (2026-08-19 through 08-25). Cache coverage is NOT the gap right now; the cost is somewhere in the still-runs-on-every-render setup work or React itself.\n\n'
  + 'Shipped the instrumentation the dispatch asked for so this stays measured going forward instead of a one-time check: new count() export in click-trace.js (a discrete untimed tally -- mark()\'s own 1ms floor would silently drop a cache-hit count, since reading an object is a few lookups, not real work) reporting weekProjections:storeCacheHit/storeCacheMiss per render; new _mark() spans around the three previously-uninstrumented weekProjections setup blocks (eventFactors, cacheIndex, cloudActualsIndex) and around the two per-day branches (cacheReadDay wrapping fetchRecentActual -- which STILL runs on a cache hit -- vs liveForecastDay wrapping the forecastDay fallback), so the next ?clicktrace=1 session can see exactly which bucket the remaining cost sits in instead of one opaque compute:weekProjections total.\n\n'
  + 'Purely additive -- no computed value changed, every wrapped block still does exactly what it did before. New src/__tests__/click-trace.test.js (4 tests, the file\'s first-ever coverage): mark()\'s off-by-default no-op/error-propagation contract, count()\'s never-throws safety -- the properties that matter most for code running inside every real session whether or not ?clicktrace=1 is ever opened. 1569/1569 tests pass. Build clean, entry budget 511.99/850 KB.\n\n'
  + 'Not done, stated plainly per the dispatch\'s own scope: did not touch the anyModalOpen/routePanel unmount-and-rebuild pattern itself (the dispatch calls that a correction to record, not a workstream to fix today); did not assume fetchRecentActual\'s labor-row scan is the hidden cost without a fresh trace to confirm it. Full writeup: memory/dispatch31-weekprojections-instrumentation.md.',
]};
