// @ts-nocheck
export default {version:'5.085', date:'2026-08-20', changes:[
  'Dispatch45b -- the degenerate-stdev guard (dispatch #45 §A, second cause), closing a real gap left over from this session\'s own earlier work.\n\n'
  + 'evaluateZScoreRule()\'s existing guard caught a baseline stdev that is literally zero, not one that\'s merely negligible. A live subject rendered "0.04 vs threshold 2.50 -- mean 0.00, stdev 0.00 -- Flagged" -- both figures were non-zero (else the guard would have caught it) but rounded away on screen, and z = (0.04 - ~0) / ~0 exploded into a fabricated outlier. A peer population clustered near-zero turns any real value into a nonsense multi-sigma score, independent of whether the subject\'s dollar amount clears materiality -- min_numerator cannot fix this.\n\n'
  + 'Measured before choosing the mechanism, per the dispatch\'s own explicit instruction. A coefficient-of-variation floor was tried first and rejected: the real live INV-002 subjects with |z|>10 have CVs (0.25-3.5) squarely inside the population\'s own normal range (median 0.66) -- CV does not separate this failure. Raw stdev does: INV-001\'s near-zero tail is essentially the same set the existing exact-zero guard already catches (min_stdev:1, a near-no-op safety net); INV-002\'s entire population lives on a tiny scale (median stdev 0.0025), and min_stdev:0.001 (near its own p10) nulls 6 of the 10 worst measured |z|>10 offenders.\n\n'
  + 'New min_stdev gate in src/engine/security-rules.js, checked before z is computed -- same honest-null class as n < MIN_BASELINE_N, never pass:false (this is not a materiality decision). SQL for INV-001/INV-002 handed back, full-literal replacement matching the established migration shape, preserving each rule\'s other keys (min_value/min_denominator, min_numerator) verbatim.\n\n'
  + 'Folded into the still-open PR #492 (dispatch #46) rather than a new PR, so the guard lands before that PR\'s Part B (decision-sentence copy) merges -- satisfying the stated ordering (stdev fix before #46 B) without reworking already-shipped work. No panel changes needed: the "degenerate baseline" reason already flows through the Undetermined-state rendering buildDecisionSentence() already has.\n\n'
  + '5 new tests (3 engine unit, 2 call-site wiring proving the guard -- not the data -- changes the outcome), mutation-tested. 1770/1770 suite passes, build clean, no entry-chunk impact. Full writeup: memory/dispatch45b-degenerate-stdev-guard.md.',
]};
