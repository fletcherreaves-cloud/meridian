// @ts-nocheck
export default {version:'5.212', date:'2026-08-27', changes:[
  'Added: DO (and OM-scaffold) tier on the Leadership One-Pager\'s scope dropdown (dispatch #166 '
  + '-- the corrected follow-up to dispatch #158, which had investigated the wrong panel entirely: '
  + '`above-store-onepager.js`\'s flat single-tier dropdown, not `src/views/one-pager.js`\'s '
  + 'existing two-tier "Owner:"/"Supervisor:" one the owner was actually looking at).\n\n'
  + 'src/constants.js: DEF_SETTINGS.doGroups seeded with the owner-confirmed real assignment -- '
  + 'Hugh Bonner -> all 20 Oklahoma stores (union of the 5 OK supervisorGroups patches), Brad '
  + 'Denley -> all 7 Florida stores. Brad Denley is deliberately ALSO a Supervisor for a 3-store '
  + 'subset of those same 7 stores -- a dual role, same pattern operators\' own Ryan Thorley '
  + 'comment already documents, not a bug to reconcile. DEF_SETTINGS.omGroups seeded empty ({}) '
  + 'per the owner\'s explicit "scaffold only" call this session -- the map, settings shape and '
  + 'dropdown UI are wired now, no OM names invented.\n\n'
  + 'src/views/one-pager.js: two more <select> dropdowns on the existing Scope: row, same visual '
  + 'style and applyScope(label, list, level) wiring as Owner/Supervisor -- DO (settings.doGroups, '
  + 'label prefix "DO: ", level tag \'do\') and OM (settings.omGroups, prefix "OM: ", level tag '
  + '\'om\'). Row order: Org/OK/FL presets -> Owner -> DO -> Supervisor -> OM (org-chart top-down). '
  + 'Both dropdowns reuse the exact same `Object.keys(x).length ? h(\'select\'...) : null` empty-'
  + 'guard the Owner/Supervisor dropdowns already use, so the empty omGroups scaffold renders '
  + 'nothing until the owner populates it -- no separate hide-when-empty special case.\n\n'
  + 'src/views/management.js: new \'🏛 DOs\' / \'⚙ OMs\' Settings sections, backed by a new shared '
  + 'GroupsEditor({S, onUpdate, field, title, note}) component that reproduces the existing \'🏢 '
  + 'Operators\' section\'s add/rename/remove-row UI and "↺ Sync from defaults" reset exactly '
  + '(parameterized by settings field instead of a second hand-rolled implementation) -- Operators\' '
  + 'own render code is untouched, only newly reused as the template per the dispatch.\n\n'
  + 'New tests: dispatch-166-onepager-do-scope.test.js renders the REAL OnePagerPanel component '
  + '(supabase.js\'s loadQsrFob/loadActionItems mocked to resolve immediately -- otherwise the '
  + 'panel\'s `!page ? \'Loading…\' : ...` gate never clears in this sandbox, which has no live '
  + 'Supabase reachability) and proves: the DO dropdown lists Hugh Bonner/Brad Denley; picking '
  + '"Hugh Bonner" sets locs to exactly the 20 OK store ids (checked three ways -- scope label, '
  + 'store count, and which store pills render with the accent "on" border); the OM dropdown does '
  + 'NOT render while omGroups is empty. dispatch-166-management-do-om-sections.test.js renders the '
  + 'REAL Settings component through a small state-holding harness and proves the DOs/OMs sections '
  + 'list seeded names, and that an edited store-list, an added row, and a removed row all persist '
  + 'back into the SAME rendered tree via onUpdate -- not just that onUpdate was called with some '
  + 'value. Both follow this repo\'s "verification must touch the call site" standing rule.\n\n'
  + 'Speed check: entry chunk gzip 478.95 KB -> 479.35 KB (+0.40 KB), eager-payload total 550.38 KB '
  + '-> 550.81 KB (+0.43 KB), budget 850 KB -- unchanged headroom, well within budget.\n\n'
  + 'Out of scope per the dispatch: above-store-onepager.js (its own single-tier dropdown was '
  + 'already correctly left alone by dispatch #160), and any RBAC/permissions.js role-tier change '
  + '-- this is a scope filter on one panel, not a new access-control role.',
]};
