// @ts-nocheck
export default {version:'5.215', date:'2026-08-27', changes:[
  'Dispatch #169 -- product-mix item correlations in Signal Lab/Scanner (Notes 28 #5, the '
  + 'Filet-O-Fish-Fridays FR). Measured before designing: item is per-ITEM with no fixed small '
  + 'list like Weather/Calendar/Pricing -- a 20-date/27-store sample of qsr_product_mix (real '
  + 'service-role read, 207,966 rows spanning 2026-01-01..2026-08-25) found 699 distinct '
  + 'menuItemNumbers, with volume concentrated (top 10 items = 25.7% of units sold, top 50 = '
  + '60.5%, top 150 approx 87%, top 200 = 91.7%, 500+ long-tail items each in the single digits). '
  + 'So items are never hand-listed as static METRIC_CATEGORIES entries -- signal-registry.js '
  + 'gained a DYNAMIC per-item resolver: a `pmixItem:<menuItemNumber>` key shape that '
  + 'findMetric()/extractMetricValues() synthesize on the fly (label from the item\'s real desc_, '
  + 'summed soldQty across price tiers per the schema\'s (loc,date,item,price) grain), reusing the '
  + '__calendar/__priceEvents "derived, no static source table" pattern already established for '
  + 'Calendar/Pricing rather than inventing a fourth shape. pmixItemsIndex(pmixRows) builds the '
  + 'full sorted-by-volume catalog once per array identity (WeakMap-memoized, same pattern as '
  + '_priceSeriesCache) and backs both the label cache and the Signal Lab item picker.\n\n'
  + 'Signal Lab: a new ItemPicker component (search/select from the real item list, NOT a '
  + '700+-entry dropdown -- the dispatch\'s explicit call-out) sits alongside the existing '
  + 'MetricSelect on both X and Y axes, so the owner can build "Filet-O-Fish Sold Qty x Friday" '
  + 'on demand. SignalBuilder exported (same reasoning as dispatch #155\'s ParkOepeTab export) so '
  + 'a render test can drive the real search -> select -> Preview Correlation flow end to end.\n\n'
  + 'Scanner: items are NEVER folded into the default full pairwise sweep (would multiply an '
  + 'already-O(n^2) sweep by another order of magnitude to chase a mostly-noise long tail, AND '
  + 'pair items against every unrelated metric when the actual ask is item x day-of-week/weather). '
  + 'Chose a hybrid of the dispatch\'s two listed options over picking either alone: capped to the '
  + 'top PMIX_SCANNER_TOP_N=150 items by volume (the measured concentration point covering ~87% '
  + 'of units), paired ONLY against Calendar/Weather (never against each other or the rest of the '
  + 'registry), AND off by default behind an explicit "Item Mix" checkbox (opts.includeItems) so '
  + 'a plain "Run scan" has the exact same pair count/timing as before this dispatch -- verified '
  + 'by a regression test asserting byte-identical tested/metricsUsed with the flag unset vs '
  + 'explicitly false.\n\n'
  + 'allowZero (task 5): set true on the synthesized item metric -- "0 Filet-O-Fish sold on a '
  + 'Tuesday" is real signal for a Friday comparison, not missing data, matching the reasoning '
  + 'already used for wxRain/calFri. Documented finding, not glossed over: '
  + 'scripts/qsrsoft-pmix-pull.mjs already filters soldQty<=0 rows as "catalog placeholders" '
  + 'before upsert, so a literal 0-sold day never reaches ds.pmixRows as a row today -- it shows '
  + 'up as an absent (loc,date), indistinguishable from "item not in the catalog feed that day." '
  + 'allowZero:true is still correct (forward-compatible, harmless today) but does not currently '
  + 'recover the literal zero the FR\'s reasoning was written around; that gap lives in the pull '
  + 'script\'s ingest filter, out of scope here.\n\n'
  + 'Bug found and fixed while building the item x Calendar/Weather block (needed those metrics '
  + 'to actually be present in scanAllPairs\'s valMap to pair against): Calendar and Pricing '
  + 'metrics were NEVER reaching the Scanner\'s sweep at all, since v4.533/dispatch20. The '
  + 'pre-extraction loop gated every metric on `ds[m.source]` being a non-empty array -- true for '
  + 'every real-table source, but __calendar/__priceEvents are derived sources with no such array '
  + 'by design, so `ds[\'__calendar\']`/`ds[\'__priceEvents\']` were always undefined and every '
  + 'calFri/calWeekend/calMon/pxDaysSince/pxItemsChanged/pxMeanStepPct metric was silently '
  + 'skipped. Reproduced directly before fixing: scanAllPairs against an obvious Fri/Sat/Sun '
  + 'sales pattern returned metricsUsed:2 (sales, gc only), zero calendar pairs -- despite '
  + 'SEEDED_SIGNALS\'s "Friday lift" entry (which calls computeCustomSignal directly, bypassing '
  + 'this loop) implying it worked. Fixed by special-casing the two derived sources in the '
  + 'presence check; new regression test proves calFri/calWeekend now actually surface as '
  + 'scanAllPairs results, not just in computeCustomSignal -- exactly the "engine right but '
  + 'unused" shape the dispatch16 verification rule warns about, since every prior calendar test '
  + 'only exercised extractMetricValues/computeCustomSignal directly.\n\n'
  + 'Verification (acceptance test, real 27-store data): pulled the FULL captured history for '
  + 'menuItemNumber 5926 ("Filet-O-Fish") via the service-role key -- 7,916 rows, '
  + '2026-01-01..2026-08-25, all 27 stores -- as fixtures/pmix-fof-2026.json. Hand-aggregated '
  + 'first: Friday mean daily district-wide units = 568.8 vs the other six weekdays\' mean = '
  + '444.4, Friday the single highest day of the week. The registry\'s actual computeCustomSignal '
  + '(calFri x pmixItem:5926, daily, district) reproduces this as r=+0.145, n=6,305, p approx 0 -- '
  + 'positive and highly significant, reproducing the Notes 28 #5 anchor case through the real '
  + 'engine, not a synthetic fixture. A render-based test drives the actual Signal Lab UI end to '
  + 'end (search "filet" -> confirm Filet-O-Fish shown, McChicken filtered out -> select -> '
  + 'Preview Correlation -> real "+1.000 / n = 30 matched pairs / Strong link" renders) against a '
  + 'small deterministic fixture. Scanner item-coverage tests prove: default sweep unaffected '
  + '(regression guard), includeItems:true surfaces a constructed Friday-linked item while '
  + 'staying bounded (growth is O(items x few calendar/weather keys), not O(items^2) or '
  + 'O(items x everything)), itemTopN cap respected, no-op with no pmixRows loaded or at monthly '
  + 'granularity.\n\n'
  + 'Full suite: 2967/2967 (284 files, +51 new). Build clean; entry-chunk eager total '
  + '551.64 KB -> 552.46 KB gzip (+0.82 KB -- signal-registry.js is eagerly imported by App.js '
  + 'via computeAllCustomSignals, so its growth reaches the entry chunk; still 297.54 KB under '
  + 'the 850 KB budget). signals.js lazy chunk (Signal Lab/Scanner panel itself, not eager): '
  + '26.89 KB -> 27.79 KB gzip.\n\n'
  + 'Out of scope, per the dispatch: the Pricing Engine itself; any change to price-events.js\'s '
  + 'price-CHANGE detection (untouched, this is about sell-through volume, a different signal); '
  + 'familyGroup-level rollups (the FR\'s named case is item-level, a family-group detour would '
  + 'have delayed it for a nice-to-have).',
]};
