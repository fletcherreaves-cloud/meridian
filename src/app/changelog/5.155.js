// @ts-nocheck
export default {version:'5.155', date:'2026-08-25', changes:[
  'Dispatch #114: wire the already-auto-pulled Product Mix cloud stream into ProductMixPanel, '
  + 'additive to the existing manual-upload path.\n\n'
  + '`scripts/qsrsoft-pmix-pull.mjs` (scheduled GitHub Action) has been pushing real, per-store, '
  + 'per-date rows to `qsr_product_mix` -> `ds.pmixRows` (App.js\'s `configureLazyFill`, '
  + '`loadPmixRows` in `src/lib/supabase.js`) for a while, but `ProductMixPanel` '
  + '(`src/views/labor-tools.js`) only ever read `ds.pmixData` -- the manual-upload, per-file, '
  + 'lifetime-cumulative blob with no store or date breakdown. Confirmed by grep before touching '
  + 'any code: zero references to `pmixRows` anywhere in the panel.\n\n'
  + 'Fix: a Source toggle (Cloud / Manual) in the panel header, defaulting to Cloud whenever '
  + '`ds.pmixRows` has rows (auto/emailed-first, freshest-wins standing rule) and falling back to '
  + 'Manual only when cloud is empty -- unchanged pre-dispatch behavior in that case. Cloud tab '
  + 'adds a per-store selector (same `sNameC`/`<select>` idiom `DARDaypartPanel` already uses in '
  + 'this file) and date-range quick-picks (7/30/90/180/All, anchored to the newest date actually '
  + 'present in the data rather than wall-clock \"today\"), reuses the exact same family-group '
  + 'aggregation/bar-chart/discount-exposure shape the manual view already had, and adds a Top '
  + 'Items table using the item-level grain (`item`/`desc`/`price`) the manual rollup collapses '
  + 'away -- genuinely new information. Manual tab is byte-for-byte the pre-existing experience: '
  + 'same aggregation, same summary tiles, same #302 column-validation error banner.\n\n'
  + 'Explicitly out of scope per dispatch-114.md: the Pricing Engine / Filet-O-Fish-Fridays '
  + 'correlation work CLAUDE.md names as the eventual destination -- this is the data-wiring '
  + 'foundation only.\n\n'
  + 'Verification renders the ACTUAL ProductMixPanel (react-dom/client createRoot, not an '
  + 'isolated aggregation helper) with a synthetic ds.pmixRows fixture shaped exactly like '
  + 'loadPmixRows\'s real output across two real store locs: confirms per-store re-aggregation on '
  + 'selector change (one store\'s family names disappear when the other is selected, not a '
  + 'district-wide sum), the Top Items table, the untouched Manual tab rendering the same fixture\'s '
  + 'ds.pmixData unchanged, and the cloud-first / manual-fallback default in both directions. 7 new '
  + 'tests in src/__tests__/dispatch-114-product-mix-cloud.test.js. ProductMixPanel was already '
  + 'lazy-loaded via lazyPanel() before this dispatch, so the entry chunk is untouched -- eager '
  + 'total unchanged at 528.02 KB gzip (budget 850 KB); only the lazy labor-tools chunk grew. '
  + '2383/2383 tests pass, build clean. Full resolution: memory/dispatch-114.md Resolution section.',
]};
