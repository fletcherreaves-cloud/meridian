// @ts-nocheck
export default {version:'5.088', date:'2026-08-21', changes:[
  'Dispatch #51 -- makes dispatch #49\'s Phase 0 identity-vault-re-key gate measurable as a repeatable SQL query, after a bespoke API measurement script re-implemented (and lost) the proven pull\'s two-path auth/Playwright fallback/retry handling and failed twice on a widened window.\n\n'
  + 'Additive nullable audit_rows.emp_id, populated by the SAME qsrsoft-register-audit-pull.mjs that already has a proven 80-day/14,528-row/27-store backfill -- no new pull logic, no re-implemented auth. mapRow() carries the Register Audit API\'s own empID field (confirmed by name from a live DEBUG run, dispatch #49) alongside the existing name-keyed emp field; saveAuditRows() writes it in both the server-side script and its client-side twin (src/lib/supabase.js), which manual uploads correctly leave null since parseRegisterAudit has no ID column to read. onConflict key and audit_rows\' (loc,date,emp) PK are untouched.\n\n'
  + 'Scope held hard: additive only, nothing reads the column yet, does not touch the identity vault or get_or_create_employee_token(), does not open Phase 1, does not reconcile anything. Once backfilled 2026-03-01 -> today, Phase 0\'s five numbers become one SQL query instead of an API re-pull -- the same failed run already banked Row 1\'s denominator (1,140 distinct names / 36,631 rows) with no re-measurement needed.\n\n'
  + 'Also fixed a stale mapRow() comment calling manOverringQty an "UNVERIFIED FIELD NAME" -- confirmed absent three independent ways as of today, the mapping itself (documented no-op to null) is unchanged.\n\n'
  + '3 new tests, full suite 1815/1815, build clean, no bundle impact (scripts/ isn\'t bundled). Backfill not yet run -- gated on the schema migration landing in live Supabase first (owner action item, confirmed not yet applied before writing any code). Full writeup: memory/dispatch-51.md.',
]};
