// @ts-nocheck
export default {version:'5.118', date:'2026-08-23', changes:[
  'Dispatch #77 -- metric direction (which way is good) was declared independently in at least '
  + '8 places under two flag names (lowerBetter / the inverse higherBetter), and three metrics '
  + 'contradicted themselves across sites: Labor %, R2P, and Discount % each had one site saying '
  + 'the opposite of every other site in the app -- R2P even contradicted its own sibling table, '
  + 'whose label literally read "R2P (lower=better)". Owner-ruled 2026-08-23, all three: '
  + 'lower-better, no two-sided third state ("labor has a target, for simplification, at/below is '
  + 'good and over is bad"). Fixed the 3 wrong sites in analytics.js and store-dash.js.\n\n'
  + 'Encoded direction ONCE: a new direction field (\'lower\'|\'higher\', omitted when genuinely '
  + 'undecided) on 16 of 59 METRIC_SOURCES keys -- only the ones with a traceable correspondence '
  + 'to an already-declared table entry, so this does not invent a judgment call. park and '
  + 'actVsNeed are deliberately left unset: park was already pulled from readiness scoring '
  + '(pipeline.js #181) after a real 27-store quadrant measurement refuted a single-axis '
  + '"less parking is always better" read; actVsNeed is a signed hour gap, not a monotone '
  + 'quantity. New metricDirection()/rankableMetricKeys() helpers. Guard test '
  + '(metric-direction.test.js) mirrors metric-chains.test.js\'s read-real-source-and-diff idiom, '
  + 'confirmed revert-sensitive by stashing the 3 fixes and watching exactly the predicted 2 '
  + 'checks fail. Migration of the 4 pre-existing panel-side tables (~86 sites) is explicitly out '
  + 'of scope, per the dispatch -- no ninth declaration site was added.\n\n'
  + "Correction to the dispatch's own \"Do NOT\" list: its chart.js-is-a-dead-dependency claim is "
  + 'false (chart.js/auto is live in store-dash.js and dt-speedofservice.js, 8 call sites) -- '
  + 'verified per the standing "a reviewer\'s root cause is a hypothesis until reproduced" rule, '
  + 'not touched.\n\n'
  + 'Step 3: new Top/Bottom Performers panel (Test Kitchen, analytics.district). Ranks individual '
  + 'stores only -- never rolls multiple stores into one number, which satisfies "never average '
  + 'averages" by construction. Metric -> scope (LocationSelector, reused) -> window -> ranked '
  + 'list, reusing metricSeries and visit-readiness.js\'s Bar (now exported). Count-completeness '
  + 'guard: a store covering under half its best-covered peer\'s day-count is separated into an '
  + '"Insufficient data" list, never blended into the ranking. Render test '
  + '(top-bottom-performers-panel.test.js) exercises the real panel: sales ranking flips end-for-'
  + 'end when switching to the lower-better Labor % metric, and a 1-day store with a deliberately '
  + 'extreme value never leaks into the ranked list. Confirmed revert-sensitive by injecting a '
  + 'direction-ignoring sort.\n\n'
  + 'Full adjudication table, the additional declaration sites found beyond what the dispatch '
  + 'named, and the park conflict resolution are in memory/dispatch-77.md.\n\n'
  + '2 new test files, build clean, entry-eager payload 517.17 KB gzip (budget 850 KB) -- the new '
  + 'panel is fully lazy (6.11 kB / 2.45 kB gzip own chunk).',
]};
