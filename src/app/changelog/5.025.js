// @ts-nocheck
export default {version:'5.025', date:'2026-08-16', changes:[
  'Issue #302 -- `parsePMixData` (`src/parsers/index.js`, the manual Product Mix upload fallback) summed the export as if every row were a flat leaf. The real export is hierarchical: a Menu Item # with more than one price point gets a SUBTOTAL row (Units Sold already summed across its price tiers) immediately followed by one DETAIL row per price point, and the sheet ends with a grand-total row. Measured on the real export the owner supplied and posted to #302 (`Product_Mix_20260815.xlsx`, 4,544 rows / 530 items, 452 multi-price + 78 single-price): parent == sum(children) on 452 of 452 groups, zero mismatches. Naive summing measured 306,015 units vs a true 204,708 -- a 1.495x overstatement (the issue title\'s original ~3x figure was measured on a different day\'s export, 2026-08-14, which apparently ran closer to 2 rows/item on average; different real files, not a contradiction -- flagging per the dispatch\'s explicit "don\'t silently adopt my number" rather than picking one).\n\nFix: group by Menu Item #, keep only the FIRST row encountered per item in sheet order (the subtotal for multi-price items, the only row for single-price items, which have no separate subtotal line and were the reason a "Price contains a range" subtotal-detection heuristic was explicitly rejected -- it would misclassify all 78 of them). This also transparently handles the OTHER sheets in the same workbook (PMIX/Margin/QCR), which invert the convention -- item # only on the parent row, blank on children -- since those children were already filtered by the pre-existing `r[C.item]==null` skip and each parent\'s item # appears exactly once, making the dedup a no-op there. The grand-total row (empty Desc, a nonsense Price) is dropped explicitly by its empty Desc rather than relied upon to fall out of the dedup.\n\nAlso fixed the "three more ways the same function is silently wrong" column-validation half of the issue: `Family Group`/`Disc Qty`/`Offer Discount $` are only present on an "all metrics" export -- a default export omits them, and the parser used to silently default family to \'Other\' and discounts to 0, making a missing column indistinguishable from real zero data. `parsePMixData` now returns `{rows:[], byFamily:{}, error:"..."}` naming the missing column(s) instead, and `ProductMixPanel` (`labor-tools.js`) surfaces that error as a banner rather than rendering a silent empty/zero panel. The `Adj PMIX Sales` vs `$ Sold` dollar-column defect (the fourth item in the original issue body\'s Fix list) is deliberately deferred, not silently dropped -- dispatch8\'s own explicit IN/OUT scope for this pass named only the hierarchy fix and the column-validation fix, not the dollar figure; the `loc`/`date` grain gap is also explicitly OUT (the export has no store column anywhere and no date column outside the sheet name -- a UI decision, not a parsing one).\n\nVerification: added `src/__tests__/pmix-parser.test.js`, reproducing the exact Hamburger group (385-unit subtotal + 8 detail rows summing to 385) and the grand-total-row structure documented in `memory/qsrsoft-report-catalog.md` from the real measured export, plus the column-validation failure paths. This proves the parser logic against real, measured numbers, but is NOT the full 4,544-row file -- I do not have the raw `Product_Mix_20260815.xlsx` in this session (only the issue\'s posted analysis and the prior session\'s catalog notes survived; its scratchpad did not), so I could not independently re-derive the full-file 204,708-vs-306,015 total myself. Asking the owner to attach the export so that reconciliation can be run end-to-end before this is treated as fully closed, per the standing measure-don\'t-reason rule and the issue\'s own explicit verification bar.\n\n1387/1387 tests pass (16 new), build clean (508.68 KB gzip eager, 341.32 KB headroom, +0.68 KB -- the changelog entry text itself is the only eager-chunk growth here, via changelog-latest.js; ProductMixPanel/the parser fix are in lazy-loaded modules).',
]};
