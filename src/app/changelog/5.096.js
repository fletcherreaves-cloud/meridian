// @ts-nocheck
export default {version:'5.096', date:'2026-08-21', changes:[
  'Dispatch #56 Part D -- "a first-time flag and a fifth consecutive flag are completely different '
  + 'situations and the panel currently presents them identically." Two new pieces, both extending '
  + 'src/engine/security-drilldown.js per the dispatch\'s own "link, don\'t re-render" instruction -- '
  + 'no new data source, everything comes from security_findings already loaded at panel-open.\n\n'
  + 'Subject-level history rollup: buildSubjectTimeline() flattens a subject\'s per-rule window '
  + 'history into one oldest-to-newest cross-rule list, rendered above the per-rule breakdown as '
  + '"Subject history: flagged N of M evaluations since <date>" plus the flattened list once there '
  + 'is more than one window (a single-window subject\'s timeline is identical to the one verdict '
  + 'already shown right below it).\n\n'
  + 'Instance / pattern / trend: classifySubjectShape() answers a different, more precise question '
  + 'than dispatch #46\'s existing classifySubjectTrend() (chronic/new/improving/clear -- "is it '
  + 'still going on," a two-state boolean story). This asks how many times, and in what '
  + 'arrangement -- instance (flagged once), pattern (2+ times with a gap, asserted even at n=2 '
  + 'since it\'s a count not a direction), trend (an unbroken run of 3+ consecutive flagged '
  + 'windows, direction from first value vs last), or insufficient-history (a consecutive run '
  + 'shorter than the 3-window minimum -- the exact "do not label a shape from two windows" case '
  + 'the dispatch itself warns against, same discipline dispatch #52 already applied declining a '
  + 'z-test on 1-4 flagged cash rules). Renders BESIDE the existing chronic/new line, not instead '
  + 'of it -- checked whether classifySubjectTrend already answered this before building anything, '
  + 'and it answers a narrower question, so this is genuinely additive.\n\n'
  + 'corroboration_rules finding-level cross-link: Part A already mapped corroboration_rules/'
  + 'exoneration_rules (populated in security_rules, dropped by loadSecurityRules() until that PR) '
  + 'and surfaced them in the static rule directory. corroboratingFlags() is the other half -- on a '
  + 'flagged verdict, which of that rule\'s corroborating rules are ALSO currently flagged for the '
  + 'same subject -- rendered as "Corroborated by X -- also flagged for this subject."\n\n'
  + '1900/1900 tests (20 net new: 15 engine unit tests covering classifySubjectShape\'s full state '
  + 'space, buildSubjectTimeline\'s flatten/sort/empty-input honesty, and corroboratingFlags\' '
  + 'flagged/hygiene-excluded cases; 5 render tests through the real SecurityPanel). Build clean, '
  + 'entry chunk unchanged (security-panel.js already lazy). Parts B and E are next per the '
  + 'owner\'s own sequencing (event_details\' schema is confirmed, and a /reporting/v2/people/ path '
  + 'family surfaced by the time-punches capture may fold Part B\'s hire-date investigation into '
  + 'the same probe); Job C Batches 2+ deferred as low-risk. Full writeup: '
  + 'memory/dispatch56-part-d.md.',
]};
