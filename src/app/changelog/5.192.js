// @ts-nocheck
export default {version:'5.192', date:'2026-08-26', changes:[
  'Performance Review continuity, Phase 3b (dispatch #151): wires dispatch #150\'s reports-to '
  + 'assignment graph into `reviews`\' actual Postgres RLS -- real hierarchy-scoped visibility '
  + 'and write-scoping, not just `accessible_locs`.\n\n'
  + 'New `profiles.person` (text, nullable, admin-set): maps a Supabase login onto its identity '
  + 'in the `staff_assignments` graph (a geid for a roster-sourced role, or a plain supervisor '
  + 'name string for AS/OM/DO -- the same identity space that table already uses). No reliable '
  + 'automatic Supabase-login -> geid/name link exists today (confirmed by grep), so this is a '
  + 'manual field -- editable inline in `src/views/admin.js`\'s existing per-user profile panel, '
  + 'next to the existing role/accessible-locs editors, following the exact same inline-edit '
  + 'pattern.\n\n'
  + 'New SQL function `public.person_oversees_loc(caller_person, loc, asof)`: a SQL mirror of '
  + '`whoOversees()`\'s store -> current holder -> that holder\'s current holder chain-walk, '
  + 'callable from RLS (which can\'t call the JS engine directly -- same constraint dispatch #149 '
  + 'hit for `levelsAbove()`). Deliberately diverges from `whoOversees()`\'s "throw on a cycle" '
  + 'behavior: an RLS predicate can\'t throw per-row without breaking every query touching a '
  + 'cyclic record, so this caps the walk at depth 10 (the real hierarchy is at most 5 rungs) and '
  + 'silently returns false past the cap instead. New JS-side sibling `personOversees()` '
  + '(`src/engine/assignment-graph.js`) -- a thin wrapper around the EXISTING `whoOversees()`, not '
  + 'a second implementation -- for client-side UI gating; it diverges from the SQL version in the '
  + 'opposite direction (no depth cap, catches `AssignmentCycleError` -> false instead). The two '
  + 'CAN DRIFT if one changes without the other, same flagged risk as #149\'s ladder mirrors.\n\n'
  + '`reviews` RLS: new ADDITIVE read policy `"reviews: hierarchy scope read"` grants SELECT when '
  + 'the caller\'s `profiles.person` is set and `person_oversees_loc()` says they oversee the '
  + 'review\'s store -- alongside (never replacing) the existing `accessible_locs`-based policies, '
  + 'so a profile with no `person` mapping yet (every profile today) keeps whatever access it '
  + 'already had. Closes the write-scoping gap dispatch #148 explicitly deferred here: '
  + '`"reviews: authenticated write"`/`"authenticated update"` now route through a new '
  + '`reviews_write_allowed(loc)` helper requiring (a) admin/owner, unconditional, OR (b) the '
  + 'caller\'s `person_oversees_loc()` relationship to THIS SPECIFIC review\'s store, OR (c) for a '
  + 'caller with no `person` mapping yet, the same `accessible_locs`-based scope the read policies '
  + 'already grant (so a legitimately-scoped caller doesn\'t regress before being migrated onto '
  + 'the `person` graph). All three combined with OR, per the dispatch\'s own step-3 options -- see '
  + 'the PR body for why all three rather than just one.\n\n'
  + 'NOT applied to production yet -- schema.sql documents the exact SQL and the live verification '
  + 'steps; see this dispatch\'s PR body. `review_overrides`\' own RLS is untouched by design -- '
  + 'confirmed its read policy already mirrors `reviews`\' visibility via subquery, so it inherits '
  + 'this dispatch\'s new grant automatically.',
]};
