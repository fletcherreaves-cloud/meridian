// @ts-nocheck
export default {version:'5.080', date:'2026-08-20', changes:[
  'Dispatch42 -- baseline-relative detection: the z-score LOGIC_TYPE, converting INV-001/INV-002 to a peer-relative comparison, plus a minimum-exposure floor widened from an INV-001 special case to EVERY denominator-bearing rule -- including the just-turned-real CASH-001..004.\n\n'
  + 'Part 1 (z-score): closes the gap where INV-001/INV-002 declared baseline_type "store" and the batch job computed and persisted a real baseline into security_findings.baseline_context -- but evaluateRule() never read it, so an inherently high-variance item flagged at all 27 stores, every run, forever. evaluateRule() gains an additive {loc, baseline} option; new evaluateZScoreRule() computes z = (value - baseline.mean) / baseline.stdev, compared against threshold (now sigma, not a rate). Two independent gates required to flag: statistically unusual (z) AND materially significant (an optional min_value) -- failing materiality is a real "clear" (pass:false), not an honest-null. Both call sites in scripts/security-rules-run.mjs reordered to compute the baseline BEFORE evaluating -- caught and fixed a real bug in the same pass: fieldsFromExpr() only branched on logic_type==="ratio", which a z-score rule\'s identical numerator/denominator shape would have silently fallen through to the wrong branch.\n\n'
  + 'Part 2 (exposure floor, widened mid-dispatch): the brief originally scoped this to INV-001\'s exp_usage. Widened same-day for two reasons -- the engine already had ONE shared choke point for a zero denominator (evalRatio/evalThreshold both had the identical if(!denominatorSum) guard), so making the floor per-rule data (logic_expression.min_denominator) instead of an INV-001-only branch is the SIMPLER build; and #487 landed 9,947 real audit_rows same-day, meaning CASH-001..004 -- active=true right now, unlike the deactivated INV rules -- were about to score real data for the first time with the identical tiny-denominator pathology already measured on the inventory side. A cash false positive puts a person\'s name in an investigation queue.\n\n'
  + 'All four floors measured against live data 2026-08-20, none guessed: INV-001 (exp_usage, floor 10) converts 423/5,302 subjects (8.0%) to an honest null, 87.9% keep a real verdict. INV-002 gets NO floor -- measured minimum storeMonthSales is $2.1M, four orders of magnitude from zero, so a floor would be dead configuration. CASH-001/003/004 (drawerSales, floor 250) converts 24/670 (3.6%). CASH-002 (drawerGC, floor 25) converts 23/670 (3.4%), including 2 real subjects whose raw rates (200 and 1692.3) were direct garbage-ratio cases -- corroborated by the owner\'s own raw-row check finding a stored t_red_b_pct of 172 from a single drawer_gc=1 day (not a rule input, but the same mechanism). None of the four floors comes close to nulling the estate.\n\n'
  + 'schema-security-rules-phase1c.sql converts INV-001/INV-002 in place (z-score, min_value carrying forward dispatch #40\'s original thresholds as a materiality floor). New schema-security-rules-phase1d.sql adds the CASH floors via an idempotent jsonb merge -- CASH-001..004 stay logic_type "ratio", NOT converted to z-score, only floor-protected. Cash mapping itself re-confirmed sound (dispatch #42 section 5a), not re-litigated: no rows dropped, scale agrees with the manual path, the flagged _pct columns feed no rule.\n\n'
  + '19 new tests (13 engine-level in security-rules.test.js covering both z-score gates, every honest-null path, and the exposure floor as a genuinely SHARED mechanism on a plain ratio rule; 6 in security-rules-run.test.js -- all WIRING tests per the #366 standing rule, spanning both the inventory and cash call sites, including a 6-store hand-computed z-score fixture and proof the reorder is what makes it work). 1685/1685 suite passes, build clean, no bundle impact. SQL not yet run against live Supabase -- handed back for the owner to apply. Full writeup: memory/dispatch42-baseline-relative-detection.md.',
]};
