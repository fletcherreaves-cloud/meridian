// @ts-nocheck
export default {version:'5.216', date:'2026-08-27', changes:[
  'Dispatch #170 -- Product Mix Cloud tab "never populates, waited several minutes" (owner report, '
  + 'v5.207, LIVE bug). Measured before fixing, not assumed: `loadPmixRows(daysBack=400)\' '
  + '(src/lib/supabase.js) is the ONE loader for ds.pmixRows (single call site, App.js\'s '
  + 'configureLazyFill, called with zero args -> the 400-day default runs every time). '
  + '`qsr_product_mix\'s real data starts 2026-01-01 (confirmed: order=date.asc,limit=1), so 400 '
  + 'days back is not "the last 400 days" -- it is the entire table. Live service-role measurement, '
  + '2026-08-27: content-range 0-0/2526181 for that exact default query (2,526,181 rows). A real '
  + 'paginated fetch at 7/14/30-day windows (75,577 / 150,613 / 325,423 rows; 4.38s / 7.68s / '
  + '18.33s, 6-way concurrency matching _pagedParallel\'s real _MAX_INFLIGHT cap) gives a consistent '
  + '~18,000 rows/sec measured throughput; extrapolated to the old 2.5M-row default, ~140s '
  + '(~2.3 minutes) -- this IS "waited several minutes, never populated" (owner\'s exact words), not '
  + 'a genuine hang. _MAX_INFLIGHT=6 (supabase.js) already caps _pagedParallel\'s concurrency (read '
  + 'before assuming otherwise) -- so the "2,500+ parallel requests" hazard the dispatch flagged as '
  + 'a thing to check was already handled; not touched here since measurement showed it was not '
  + 'part of this bug.\n\n'
  + 'The tension the dispatch called out: ProductMixPanel\'s own UI only ever shows a 30-day default '
  + '(cloudRange, filtered client-side from whatever ds.pmixRows already holds) -- but dispatch '
  + '#169\'s Signal Lab/Scanner item correlations (shipped v5.215, an hour before this bug was '
  + 'reported) genuinely need real historical breadth (its own acceptance test pulled 7,916 rows '
  + 'across ~237 days for one item). Simply shrinking loadPmixRows\'s default would have silently '
  + 'broken #169.\n\n'
  + 'Fix: the SAME two-tier, bounded-default/explicit-opt-in shape #169 itself just established for '
  + 'the Scanner\'s item coverage. `loadPmixRows`\'s default trimmed 400 -> 40 days (measured: '
  + '436,137 rows, ~24s -- comfortably covers the panel\'s own 7D/30D quick views, the function '
  + 'itself unchanged, only its default). metric-source.js gained a parallel WIDE lazy-fill tier '
  + '(`ensureLazyFillWide`/`isLazyFillWidePending`/`isLazyFillWideLoaded`/`isLazyFillWideError`, a '
  + 'new `wideLoaders` map alongside the existing `loaders` map on the same configureLazyFill hook) '
  + '-- once resolved it REPLACES the SAME ds[src] field, so every pre-existing reader of '
  + 'ds.pmixRows (signal-registry.js\'s extractMetricValues/scanAllPairs, events.js\'s price-change '
  + 'detection, store-dash.js\'s price-change badge) needs zero changes. Deliberately scoped to '
  + 'pmixRows only, not a general per-source range API -- auditRows/wasteRows are unaffected (no '
  + 'wideLoaders entry registered for them). App.js registers `wideLoaders: { pmixRows: () => '
  + 'loadPmixRows(400) }` -- the OLD default, unchanged, since it was already measured to cover the '
  + 'table\'s real history.\n\n'
  + 'Consumers: signals.js\'s ItemPicker (Signal Lab, mounts straight into the wide tier -- item '
  + 'search and correlation both need breadth, no reason to also fire the narrow fetch first) and '
  + 'the Scanner\'s "Item Mix" (includeItems) toggle both switched from ensureLazyFill to '
  + 'ensureLazyFillWide. ProductMixPanel (labor-tools.js): 7D/30D stay on the bounded default; '
  + '90D/180D/All now trigger ensureLazyFillWide on selection and show an explicit "Loading Full '
  + 'History..." (or a distinct error state on failure) until it resolves -- never silently render '
  + 'the bounded 40-day set under a "180D"/"All Time" label, the exact "range option lies about '
  + 'what it shows" trap the dispatch called out. Not addressed here, flagged for awareness: '
  + 'signal-registry.js\'s Pricing metrics (pxDaysSince/pxItemsChanged/pxMeanStepPct) also read '
  + 'ds.pmixRows unconditionally in scanAllPairs (not gated behind includeItems) -- if only '
  + 'ProductMixPanel has been opened (bounded 40-day tier) and the Scanner is run without the Item '
  + 'Mix checkbox, their window narrows from the old ~237-day effective default to 40 days. '
  + 'Pre-existing incidental coupling (Pricing shipped before #169\'s own opt-in pattern), not part '
  + 'of the reported bug -- out of scope per the dispatch\'s own scope-creep guard.\n\n'
  + 'Verification, reproduce-then-fix per the dispatch\'s bar: the live 2.5M-row/~140s measurement '
  + 'above IS the "reproduce it live" step (this sandbox has a working service-role credential this '
  + 'session -- SUPABASE_SERVICE_ROLE_KEY, standard apikey+Bearer PostgREST recipe against the real '
  + 'qsr_product_mix table). A new vitest suite (dispatch-170-pmix-loader-scope.test.js) then proves '
  + 'the fix reproducibly in CI: loadPmixRows() with no args now asks for a ~40-day cutoff, not '
  + '~400; a scheduling-shape "before/after" test (same technique dt-history-pagination.test.js\'s '
  + 'own suite uses) at row counts scaled 1:1000 from the two live-measured real counts (2,527 vs '
  + '437 -- preserving the measured 5.79x ratio) shows the new default clearing in well under a '
  + 'third of the old default\'s round-trip rounds under the real _MAX_INFLIGHT=6 cap. A '
  + 'render-based test against the REAL ProductMixPanel (dispatch-170-product-mix-panel-scoped.'
  + 'test.js, driven through the actual configureLazyFill/ensureLazyFill wiring, not a pre-seeded '
  + 'ds) proves the default view populates once the bounded loader resolves, the wide loader is '
  + 'never called for it, and 180D genuinely blocks on + genuinely surfaces the wide fetch (success '
  + 'and failure paths both). A dedicated breadth-preservation suite '
  + '(dispatch-170-signal-lab-wide-breadth.test.js) proves dispatch #169\'s correlation feature is '
  + 'NOT regressed: ItemPicker calls the wide loader, never the bounded one, and a real '
  + 'computeCustomSignal run over the resolved wide data reproduces the full 237-day/"n = 237 '
  + 'matched pairs"/+1.000 result -- a narrow, Friday-less 4-row slice is passed as the alternate '
  + 'loader specifically so a false pass (silently using the bounded data) is not possible.\n\n'
  + 'Full suite: 2981/2981 (287 files, +3 new files / +14 tests). Build clean; entry-chunk gzip '
  + '480.59 KB -> 480.65 KB (+0.06 KB, no new static top-level import -- metric-source.js\'s wide-'
  + 'tier addition is a few more module-level functions, negligible), eager total 552.42 KB -> '
  + '552.57 KB, still 297.43 KB under the 850 KB budget.',
]};
