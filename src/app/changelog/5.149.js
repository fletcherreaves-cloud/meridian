// @ts-nocheck
export default {version:'5.149', date:'2026-08-24', changes:[
  'Dispatch #106: Forecast Accuracy weekly-cadence view, then merged with LifeLenz Bridge into '
  + 'one Forecast Reports panel.\n\n'
  + 'Phase A: Forecast Accuracy (analytics.js\'s ForecastAccuracyPanel) already had a real '
  + 'period picker but no week-by-week/day-by-day breakdown table like LifeLenz\'s own native '
  + 'Forecast Accuracy Analysis screen. Added a new "Weekly / Daily Breakdown" section, '
  + 'additive to the unchanged period-aggregate MAPE view -- groups the backtest\'s daily '
  + 'forecast/actual rollup into weeks anchored on settings.weekStartDay (3=Wed, McDonald\'s '
  + 'standard, the existing prop this panel already receives -- not a new setting). '
  + 'Dollar-weighted across every selected location (sum $ then take variance from the sums, '
  + 'never an average of per-day percentages).\n\n'
  + 'Phase B: merged ForecastAccuracyPanel and LifeLenzBridgePanel ("MBI vs LifeLenz Accuracy") '
  + 'into one new parent, ForecastReportsPanel (src/features/forecast-reports.js), with an '
  + 'internal tab switcher -- this app\'s existing "one panel, internal tabs" pattern '
  + '(eom-dashboard.js, security-panel.js), not a new registry-level parent/child concept. Both '
  + 'tabs reuse the real panel components as-is (a thin shell, nothing duplicated) and stay '
  + 'mounted simultaneously so a completed backtest or scan survives switching tabs and back. '
  + 'panel-registry.js: retired the two standalone route:true entries in favor of one new '
  + '`forecast-reports` (route:true, tkOrder 5); fcst-accuracy/lifelenz-bridge converted to '
  + 'kind:\'hub-tab\' (same pattern as sched-summary/labor-analytics), not deleted, so a stale '
  + 'deep link or the At-A-Glance MAPE tile still lands on the right internal tab.\n\n'
  + '⚠️ PROPOSAL, not owner-confirmed: the merged parent\'s label is "Forecast Reports" -- the '
  + 'dispatch named this or "Forecasting Center" as candidates and asked for owner confirmation '
  + 'before shipping either. Flagged here rather than presented as decided.\n\n'
  + 'Verified through the real consumers: dispatch-106-forecast-reports.test.js renders the '
  + 'actual ForecastReportsPanel, runs a real backtest, switches tabs via the rendered '
  + 'switcher (checked via each pane\'s inline style.display, not textContent), confirms the '
  + 'backtest survives the switch, and independently re-derives the Phase A weekly $ totals '
  + 'from the same deterministic mock-forecast formula to verify real Wed-start week '
  + 'boundaries. Registry/nav tests (panel-registry, routing, shell-nav-snapshot) updated to '
  + 'match the new registry shape, not left passing by coincidence. Build clean; full suite '
  + '225/225 files, 2336/2336 tests. Entry chunk 1761.65 KB -> 1566.23 KB raw (522.90 KB -> '
  + '456.11 KB gzip) -- LifeLenzBridgePanel\'s static App.js import removed; both merged panels '
  + 'now load together in one lazy chunk only when Forecast Reports is opened. Full reasoning: '
  + 'memory/dispatch-106.md Resolution section.',
]};
