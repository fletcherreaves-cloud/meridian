// @ts-nocheck
export default {version:'5.009', date:'2026-08-14', changes:[
  'Issue #270 phase 1 — SAGE\'s system prompt (src/views/sage.js) built itself entirely from manual, device-local upload arrays (ds.laborRows/ctrlRows/opsRows), bypassing metric-source.js\'s auto-first resolver — on a device with cloud/emailed data and zero manual uploads, SAGE\'s labor/ops/controls summaries were silently empty or stale while buildFobSummary (already on ds.qsrFobRows, the working precedent) worked fine. Four defects fixed: (1) hasData gated on ds?.laborRows?.length>0 only -> new exported sageHasData(ds) checks any resolvable stream (qsrActSummaryRows/glimpseRows/cashRows/qsrFobRows/salesLedgerRows/opsCashRows/opsLaborRows/opsServiceRows/ctrlRows/laborRows/schedRows); (2) buildLaborSummary/buildOpsSummary/buildControlsSummary rewritten onto metricSeries()/metricAvg() (same auto-first resolver buildFobSummary already trusted) instead of raw ds.laborRows/opsRows/ctrlRows reads, store roster switched to Object.keys(STORE_NAMES) (static, always populated) after confirming ds.storeIds is itself manual-labor-derived and would have kept the fix silently broken; (3) the system-prompt\'s data-inventory block renamed "DATA COVERAGE" and now reports what the summaries actually resolved (auto-first store-days via a new _resolvedDayCount helper) instead of raw manual .length counts — the FOB line was wrong even before this fix (read ds.fobRows, but buildFobSummary reads ds.qsrFobRows); (4) buildControlsSummary\'s 60-day mean |cashOSAmt| was labeled "NOTABLE EXCEPTIONS" and SAGE reported it to the owner as "open exceptions" — relabeled "STORES WITH AN ELEVATED 60-DAY AVERAGE (not open exceptions — a mean, worth a look, not a flagged incident)". New metric-source.js chain: otDollar (OT hours\' dollar sibling, opsLaborRows -> ctrlRows -> laborRows, needed for buildLaborSummary\'s OT-dollar column; already covered by metric-chains.test.js\'s EMITS map, no test-infra change needed). Incidental fixes made while rewriting the same touched lines: laborPct/park/discPct are 0-1 fractions per parsePct()\'s convention but were concatenated raw with \'%\' (e.g. "0.28%" instead of "28.00%") -- new _fmtPct() helper fixes every site touched; buildControlsSummary\'s elevated-discount threshold was dead code (s.discPct > 3, a fraction compared against literal 3, could never trigger) -> fixed to > 0.03. Found but deliberately NOT fixed here, out of phase-1 scope: ds.loaded is ALSO manual-labor-derived (pipeline.js sets ds.loaded = ds.laborRows.length > 0 in 2+ places), meaning every `if (!ds.loaded)` gate app-wide (10+ in analytics.js alone) is a candidate for the same silent-failure-on-cloud-only-device class of bug fixed here for SAGE specifically -- flagged in code comments and memory/project-sage-manual-sourcing-270.md, not swept app-wide (that is its own issue-sized change). Phase 2 (server-side prompt assembly) and buildScheduleSummary (LifeLenz/schedule rows, not flagged by the issue) untouched, per the issue\'s own stated scope. New test file src/__tests__/sage-manual-sourcing.test.js (9 tests): builds a ds with ONLY cloud streams populated and zero manual arrays, asserts every rewritten summary + sageHasData + the DATA COVERAGE block resolve real data with correct formatting -- the exact "device that never had a manual upload" scenario the issue describes. 1367/1367 tests pass (1358 existing + 9 new), build clean (eager 507.90 KB gzip / headroom 342.10 KB, unchanged -- no new eager imports). PR review round: buildControlsSummary\'s totalDays gate counted discPct-resolved days only while the section also reports cashOSAmt, which chains through glimpseRows independently of discPct\'s opsCashRows/ctrlRows chain -- a device with emailed Glimpse but no ops-pull cash rows and no manual Controls upload had every cash-O/S day resolved and the section still returned null; gate now unions resolved days across both metrics, new test proves it on a glimpseRows-only ds. storeCount at sage.js\'s system-prompt header was still `ds?.storeIds?.length` (the same manual-labor-derived field the rest of this fix worked around) -- SAGE\'s opening line would have read "managing 0 locations" on the exact device this fix targets; switched to Object.keys(STORE_NAMES).length. The CURRENT OPERATIONAL DATA header still said "from uploaded files" after the TOOL USAGE RULES line nearby was already corrected -- fixed to match. 10 new tests total, 1368/1368 pass.',
]};
