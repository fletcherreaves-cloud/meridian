// @ts-nocheck
export default {version:'5.168', date:'2026-08-25', changes:[
  'Dispatch #123: Crew Schedule Lookup -- search an employee, see their upcoming schedule.\n\n'
  + 'LifeLenz name-field investigation: assignedEmploymentId (already fetched by lifelenz-pull.mjs\'s '
  + 'ShiftsForSchedulePeriod) IS a real key into a name-bearing type. Confirmed via the owner\'s own '
  + 'prior live DevTools capture (memory/project-lifelenz-schedule-jobs.md, 2026-07-24, endpoint #8, '
  + 'GetSchedulableEmploymentsForPeriod): employmentsInScheduleTimeRange.edges[].node carries '
  + 'computedName/firstName/lastName keyed by id, the SAME id assignedEmploymentId points at. No '
  + 'LIFELENZ_TOKEN was available in this sandbox to re-run a fresh introspection query live, so this '
  + 'PR relies on that already-captured, dated, sourced response rather than fabricating a new one -- '
  + 'stated plainly per the dispatch\'s own "measure it, don\'t reason about it" bar. The pull script '
  + '(pullShiftAssignments) is defensive either way: it logs loudly if a fresh roster response no '
  + 'longer carries a usable id, and degrades every affected shift to its opaque assignedEmploymentId '
  + '("Employee #12345") rather than guessing.\n\n'
  + 'Pull: extended scripts/lifelenz-pull.mjs (not a new script -- reuses the one auth + schedule '
  + 'discovery + weekly ShiftsForSchedulePeriod fetch pullJobHours already does). Added '
  + 'shiftStartTime/shiftEndTime to SHIFTS_QUERY (real fields, same 2026-07-24 capture) and a new '
  + 'GetSchedulableEmploymentsForPeriod roster query, minimal fields only (id/computedName/firstName/'
  + 'lastName -- explicitly NOT dateOfBirth/schoolId/email/pay-rate history). New pure engine fn '
  + 'shiftsForEmployeeSchedule (src/engine/lifelenz-shift-jobs.js) reduces shifts to per-employee, '
  + 'per-SHIFT rows (distinct from the existing per-week rollupShiftsByEmployee). Watched: same '
  + '"LifeLenz Daily Sync" workflow already in sync-failure-watch.yml -- no new workflow file, so no '
  + 'watcher edit needed. Two-path auth: inherited from the same script/session (no separate auth '
  + 'path to duplicate). Manual-upload fallback: deliberately NOT added -- this is a schedule '
  + 'sync (closer to Register Audit than a P&L report), no manual-entry precedent exists for '
  + 'per-shift LifeLenz data anywhere in this app, and CLAUDE.md\'s own standing rule is that manual '
  + 'is last-resort fill, not a default to add preemptively.\n\n'
  + 'PII: emp_token ONLY -- no raw name column anywhere. New table lifelenz_shift_assignments '
  + '(supabase/schema-lifelenz-shift-assignments.sql) stores assigned_employment_id (opaque, safe as '
  + 'a display fallback) + emp_token (references employee_identity_vault); tokenized via the SAME '
  + 'identity-vault.js getOrCreateToken/tokenizeRows scripts/qsrsoft-register-audit-pull.mjs already '
  + 'uses for audit_rows.emp_token -- one RPC call per distinct name per store-batch, never a raw '
  + 'insert. RLS is gated to the SAME tier as reveal_employee_identity()/security_findings '
  + '(admin/supervisor always; manager only with org_config.gm_identity_reveal_enabled) -- '
  + 'deliberately stricter than most operational tables\' `using(true)` pattern, reusing security_'
  + 'findings\' own stated reasoning ("a token alone isn\'t PII, but a small-crew schedule can be '
  + 'practically de-anonymizing even in token form"). No insert/update/delete policy -- writes are '
  + 'service-role only. Seam for dispatch #124 (actual punch times, separate/parallel, not built '
  + 'here): indexed on (loc, date, emp_token) for that future join.\n\n'
  + 'RBAC decision, stated explicitly per the dispatch\'s own request rather than defaulted: reuses '
  + 'security-panel.js\'s securityPanelAccess() UNCHANGED, same tier top to bottom (RLS + client '
  + 'gate). Considered and rejected a "GM always sees their own store, no reveal toggle needed" '
  + 'carve-out -- would need either a new SECURITY DEFINER RPC or a bespoke RLS predicate on this '
  + 'table alone, both new/under-reviewed surface on the same identity-vault machinery that had a '
  + 'real NULL-role RLS bypass incident days before this dispatch; RLS already scopes which rows a '
  + 'GM\'s own client can even receive, so a GM without the reveal flag still sees their own crew\'s '
  + 'shifts in full, just token-labeled instead of named -- not a new restriction, the existing one. '
  + 'Full reasoning in src/views/crew-schedule-panel.js\'s header comment.\n\n'
  + 'Name-search design: no bulk/automatic reveal. Search matches assigned_employment_id always, and '
  + 'a real name only once that employee has been individually revealed this session (RevealName\'s '
  + 'existing click + required-reason + logged path, reused unmodified from store-analytics.js) -- a '
  + 'bulk reveal-everything-in-scope would make search work immediately but would flood '
  + 'identity_reveal_log (an append-only, indefinite-retention, evidence-grade audit trail) with '
  + 'routine convenience unlocks instead of deliberate investigative lookups.\n\n'
  + 'Panel: new "Crew Schedule" (src/views/crew-schedule-panel.js), route:true from day one '
  + '(RoutePanelShell, own URL), lazy-loaded via lazyPanel() (own chunk, not the eager entry). Shared '
  + 'DateRangeControl (seeded with a forward-looking today->+13d custom range, since the shared '
  + 'presets are trailing/backward-looking and this is an UPCOMING schedule) + LocationSelector '
  + '(mode:progressive) -- no hand-rolled pickers. Multi-select employee search, per-employee shift '
  + 'list (date/time/store/role/absent flag), per-stream freshness line (not pooled into an existing '
  + 'freshness check).\n\n'
  + 'Adjacent finding, NOT touched by this dispatch: src/views/skills-matrix.js / employee_skills '
  + 'already stores + renders raw employee names with no tokenization and `using(true)` RLS, '
  + 'predating dispatch #37\'s identity-vault architecture -- a genuine pre-existing gap this dispatch '
  + 'did not create and is out of scope to fix here; worth a dedicated follow-up dispatch.\n\n'
  + '30 new tests (10 shiftsForEmployeeSchedule in lifelenz-shift-jobs.test.js, 20 in the new '
  + 'crew-schedule-panel.test.js including a real RBAC-denial render test for an ineligible role); '
  + 'full suite 2467/2467 passing (237 files, up from main\'s 2462); build clean. New panel is its '
  + 'own lazy chunk (crew-schedule-panel, 6.34 KB / gzip 2.65 KB); entry gzip 458.08 KB unchanged '
  + '(noise-level), eager-payload 529.22 KB (budget 850 KB).',
]};
