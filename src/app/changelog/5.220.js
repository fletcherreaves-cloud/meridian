// @ts-nocheck
export default {version:'5.220', date:'2026-08-27', changes:[
  'Dispatch #175 -- finished "API over email" for cashOS/posOver, closing the gap #172\'s audit '
  + 'flagged but left out of scope. Owner asked (follow-up to #172): is this data not already '
  + 'auto-pulled through script -- could the chains prefer the auto pull over the email pull? '
  + 'Measured: qsr_cash_sheet (ds.opsCashRows) is live and fresh (content-range 0-0/25043, latest '
  + 'dt = today) and already carries cash_over_or_short and overring_amt/overring_qty -- everything '
  + 'the email pipeline needs -- but two gaps kept it from being auto-first.\n\n'
  + '(1) loadOpsCashSheet (src/lib/supabase.js) aliased every other qsr_cash_sheet metric to '
  + 'camelCase (cashOSAmt, cashRefAmt, tRedAAmt, discAmt, drawerOpens, etc.) except posOverAmt/'
  + 'posOverCnt -- confirming dispatch #165\'s own PR note (src/app/changelog/5.207.js) that this '
  + 'pair "reconciles 97-98% but has no opsCashRows fallback wired, and the loader does not yet '
  + 'alias those fields to camelCase." Added posOverAmt: r.overring_amt, posOverCnt: r.overring_qty, '
  + 'same pattern as every sibling field.\n\n'
  + '(2) src/engine/metric-source.js: cashOSAmt/cashOSPct\'s chain had opsCashRows listed THIRD -- '
  + 'srcs: [glimpseRows, cashRows, opsCashRows, ctrlRows] -- backwards from the standing auto-first '
  + 'rule even before #172, and more clearly wrong now that #172 proved glimpseRows.cashOS was '
  + 'silently 0 on every row until its fix landed. Moved opsCashRows to the front: '
  + '[opsCashRows, glimpseRows, cashRows, ctrlRows]. Both email sources stay in the chain as a '
  + 'fallback for any (loc, date) the ops-pull hasn\'t reached yet -- not removed, since #172 already '
  + 'made them correct. Comment above the chain, which misdescribed the array order even before this '
  + 'change ("manual Controls, then emailed Glimpse/Cash Sheet, then the auto-pulled..."), rewritten '
  + 'to match the actual resulting order.\n\n'
  + 'posOverAmt/posOverCnt had NO opsCashRows source at all -- srcs: [glimpseRows, cashRows, '
  + 'ctrlRows]. Added [\'opsCashRows\', \'posOverAmt\']/[\'opsCashRows\', \'posOverCnt\'] as the '
  + 'first source in each chain, same convention cashRefAmt/cashRefCnt already use. Verified '
  + 'parseCtrlData (src/parsers/index.js) does emit posOverAmt/posOverCnt (real field, not a '
  + 'single-source stub) before keeping ctrlRows as the last-resort manual fallback.\n\n'
  + 'promoAmt/promoPct (also flagged reconciling well in #165) are explicitly out of scope, matching '
  + 'what the owner asked about; a full re-audit and deprecating the email path were both out of '
  + 'scope too -- both email sources stay live as fallback per the standing "manual/emailed fallback '
  + 'always kept" rule.\n\n'
  + 'node scripts/gen-loader-emits.mjs --write regenerated the loader field map (opsCashRows gained '
  + 'posOverAmt/posOverCnt in its EMITS list) per the standing dev rule.\n\n'
  + 'New tests: src/__tests__/dispatch-175-cash-auto-first.test.js (13 tests) -- reconciliation-style '
  + 'coverage proving both metrics resolve from ds.opsCashRows when present, fall back to email '
  + '(glimpse then cash) only for a (loc, date) opsCashRows doesn\'t cover, fall back to manual '
  + 'ctrlRows last, keep per-day precedence (opsCashRows and glimpseRows can each win on different '
  + 'days in the same ds -- not all-or-nothing), preserve \'any\'-mode 0/negative values, and confirm '
  + 'an email-only device (no opsCashRows at all, #271\'s original bug class) still resolves both '
  + 'metrics exactly as before. metric-source-order.test.js and metric-chains.test.js still pass '
  + '(21/21). Full suite: 3017/3017 (291 files, +1 new file / +13 tests). Build clean; eager total '
  + '552.63 KB, 297.37 KB under the 850 KB budget (no new static top-level import).',
]};
