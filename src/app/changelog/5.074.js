// @ts-nocheck
export default {version:'5.074', date:'2026-08-19', changes:[
  'Dispatch35 -- Register Audit auto-pull implemented against the confirmed endpoint. Last piece of security build Phase 0a; Phase 1 (cash-drawer variance + peer ranking) can start once this lands. Dispatch34\'s live DevTools capture (owner-run) confirmed the real endpoint (GET .../reports/mcd/controlsCash/regAudit, one call per date range across ALL 27 stores) and real field names -- dispatch33\'s shipped scaffold had deliberately thrown instead of guessing.\n\n'
  + 'Restructured the pull shape: chunked date-range calls (21-day windows, matching lifelenz-pull\'s own chunking) covering all 27 stores per call, row-level work happening after the response comes back -- not the scaffold\'s old per-(loc,date) loop.\n\n'
  + 'mapRow() implemented field-by-field, cross-referenced against the ACTUAL consumer (src/utils/register-audit.js\'s analyzeRegisterAudit), not just parseRegisterAudit\'s column list: resolved manOverringAmt vs posOverAmt (two distinct concepts in the Excel export -- Manual Refund/Overring $ vs POS Overrings $/Cnt, never the same column); resolved drawerGC (maps to transactions -- it\'s the denominator of the engine\'s own avgCheck computation); confirmed avgCheck/cashOSPct/promoPct/tRedBPct-Avg/tRedAPct-Avg are read by NEITHER analyzeRegisterAudit NOR parseRegisterAudit\'s own derivation logic (the manual path just reads QSRSoft\'s pre-computed Excel columns, it doesn\'t derive them) -- populated as honest rate/avg ratios anyway for schema completeness, explicitly documented as non-load-bearing today; resolved employee identity (empName, not empID -- matches 5+ months of manually-uploaded history\'s own PK convention, avoiding a split-brain between manual and auto rows for the same person).\n\n'
  + 'nsn->loc zero-pad conversion applied once inside mapRow(), covered by a dedicated test (this repo\'s 4th-5th prior incident class from exactly this bug, v4.809/823/827/831).\n\n'
  + 'Real bug found and fixed before it shipped (second time this exact class has been caught this dispatch sequence): the script called createClient() unconditionally at module scope, which would have crashed mapRow()\'s own unit tests under vitest (no Supabase env vars set there) the same way dispatch33\'s src/lib/supabase.js import crash did -- guarded it the same way.\n\n'
  + 'Two-path auth added (direct via getFreshToken + a Playwright fallback mirroring qsrsoft-ops-pull.mjs\'s exact runAll/resolveToken/re-mint-on-401 pattern) even though dispatch35\'s own checklist didn\'t re-list it -- CLAUDE.md\'s standing rule requires it, and ops-pull proves this same host sometimes still needs it despite #312\'s direct-token fix.\n\n'
  + 'Checklist closed: cron enabled (10:00 UTC, matching DAR/eBOS) per dispatch35\'s explicit instruction -- NOT live-verified from this session (same blocker as dispatch33: no QSRSoft credentials, network egress still blocked), flagged prominently in the workflow\'s own header with a recommendation to spot-check a manual run first. sync-failure-watch.yml entry added. audit_rows tenant_id/RLS already confirmed live in dispatch33\'s own session. Manual Excel fallback untouched. Backfill not run (no live access).\n\n'
  + '12 new tests (register-audit-pull.test.js) against fixture data shaped like dispatch34\'s documented real field names -- covers every derivation above plus zero-denominator safety (null, never NaN/Infinity) and nsn padding. This verifies the mapping LOGIC, not that it matches the real API\'s actual output -- that still needs a live workflow_dispatch run + spot-check against dispatch34\'s own 2026-08-12..08-18 capture window before trusting any risk-scoring output. 1596/1596 tests pass, build clean (unaffected). Full writeup: memory/dispatch35-register-audit-implementation.md.',
]};
