// @ts-nocheck
export default {version:'5.077', date:'2026-08-20', changes:[
  'Dispatch38 -- Security build: reveal-UI for the Register Audit panel. Closes the gap dispatch #37\'s own retrofit deliberately opened: analyzeRegisterAudit strips plaintext names from its output ("blind mode" working as designed), which left store-analytics.js\'s Register Audit panel showing \'Unknown\'/\'?\' everywhere it used to show a name, with no way for an authorized viewer to see who\'s flagged.\n\n'
  + 'New RevealName component (src/views/store-analytics.js) -- the ONE place that resolves a token back to a real name: click -> required reason (window.prompt, matching eom-dashboard.js\'s established "Approved by / reason" pattern) -> calls reveal_employee_identity() (dispatch #37\'s RPC, unmodified -- all role-gating/logging logic stays server-side, this component does none of it) -> caches the resolved name. A pre-backfill row (e.id === \'Unknown\') renders plain text, no click target -- nothing to reveal yet. An RPC rejection (wrong role, org toggle off, unknown token) shows the RPC\'s own message, never a swallowed generic error.\n\n'
  + 'Reveal-state cache lifted to RegisterAuditTab (token->name map + onReveal callback, declared before any early return per the rules of hooks) and threaded into both the four table sections (Overview/T-Reds/Refunds/Cash -- mechanical swap, e.emp||\'Unknown\' cells replaced with RevealName) and RegisterAuditNarrative\'s five prose paragraphs (Cash O/S, Void & Refund, Discount & Meal, Drawer Open Frequency, Recommended Actions) -- so revealing one employee once resolves them everywhere else in the same panel view, no repeat prompt or RPC call. The five narrative sites needed real restructuring, not a mechanical swap: each paragraph\'s `text` field changed from a flat template-literal string to a mixed array of strings and RevealName elements (a component can\'t be inserted into an already-flattened string) -- Drawer Open Frequency and Recommended Actions additionally needed their `.map(e=>e.emp).join(\', \')` list-building rewritten as interleaved arrays, since a name can\'t be joined into a string before it\'s been revealed. No change to the render loop itself -- React already accepts an array as a single children argument, the same pattern this codebase already uses elsewhere (e.g. SECTIONS.map(...) passed as one child list).\n\n'
  + 'AI-insight prompt builder deliberately left OUT of the reveal mechanism per the dispatch\'s own scoping (no click target/rendered DOM to attach one to, prompts regenerate fresh every run) -- fixed a real, separate bug found while reading it: it still read e.emp, a field dispatch #37 already removed, so it had silently gone stale to always \'?\' since that PR landed. Now reads e.id (the token) instead.\n\n'
  + '9 new fixture/integration tests -- reveal-name.test.js (RevealName\'s state machine in isolation: null/pre-backfill/cached/unrevealed states, the full click->prompt->RPC->onReveal path, cancelled/blank-reason no-op, RPC-rejection message surfaced verbatim) using the repo\'s established createRoot/act render-test pattern (at-a-glance-checklist-freshness.test.js) + vi.mock(\'../lib/supabase.js\') (blob-sync.test.js\'s pattern); register-audit-tab-reveal.test.js -- an integration smoke test mounting the ACTUAL RegisterAuditTab consumer (not just RevealName standalone), per the "would this verification still pass if the change were reverted" rule: confirms one real click resolves the name in BOTH the table cell AND the narrative paragraph from the one shared cache, proving the prop-threading actually landed at all 9 call sites, not just that the component works alone.\n\n'
  + 'Cannot be verified against a live reveal from this sandbox -- same constraint as every prior dispatch in this sequence, no live Supabase session with a real admin/supervisor role. 1639/1639 tests pass, build clean and unaffected (store-analytics.js is not eagerly bundled). Full writeup: memory/dispatch38-reveal-ui.md.',
]};
