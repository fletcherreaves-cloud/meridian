// @ts-nocheck
export default {version:'5.050', date:'2026-08-18', changes:[
  'computeEventFactors() (src/utils/events.js) re-scanned the FULL cross-store ds.laborRows array twice per calendar event -- once via .find() for the nearest labor row, once via .filter() for the same-day-of-week baseline set -- despite already running only once per weekProjections pass (not 189x; see the call-site comment at at-a-glance.js:1517, added under #386/#392). With #391\'s calendar-loss fix now merged (mf_events no longer silently discarding real events on the pre-transition re-sync), userEvents grows toward its true size -- the owner\'s own figure is ~733 -> ~11,000 event-days -- and that O(events x laborRows) shape was the real, bounded risk flagged when reviewing #391\'s performance impact.\n\nFix: index laborRows once per call, by loc (order-preserving, for the nearest-row lookup) and by loc+day-of-week (sales>0 only, for the baseline set) -- both exactly the predicates the original two full-array scans already applied, just computed once instead of re-applied per event. Verified byte-identical output against the pre-fix implementation across both event-count scenarios via a Node benchmark (not a guess): 27 synthetic stores x 500 days of labor data (13,500 rows, since no service-role key is available in this environment to measure the real row count) x {733, ~11,000} events. Results: 733 events -- 369.7ms -> 35.4ms (10.4x); ~11,000 events -- 4,715.0ms -> 843.5ms (5.6x). The absolute ~844ms at production event-count is a real, honest number, not eliminated by the fix -- the win is from replacing a full-district scan with a per-store one, not from removing the O(events) factor entirely.\n\nLanded after #392 (same render path) per the review that asked for it, to avoid a merge conflict rather than a logic conflict.\n\n4252/4252 tests pass. Build clean: 506.70 KB gzip eager (850 KB budget), +0.05 KB from 5.048\'s 506.65 KB -- no shift.',
]};
