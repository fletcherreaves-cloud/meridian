// @ts-nocheck
export default {version:'5.057', date:'2026-08-18', changes:[
  'Issue #361 -- a residual from #348 (Scheduling/Opportunity formula fix): the Scheduling panel and Schedule Summary band disagreed on the same store/week\'s labor % by 0.06pp (23.29% vs 23.35%) even after #348 aligned their hours/GC formulas. scheduling.js\'s wAvgLaborPct (rebuilt for #348 against real Duncan rows) weights daily labor % by ACTUAL sales -- correct, since laborPct = labor$/actualSales, so Σlabor$/Σsales = Σ(laborPct×sales)/Σsales reconstructs exactly. engine/schedule-summary.js\'s rollup() weighted the same figure by FORECAST sales instead -- the two only ever agree when a week\'s daily actual and forecast sales happen to track each other closely, which is why this shipped unnoticed until a week where they didn\'t.\n\nFix: rollup() (and the district-level aggregation loop that reuses it) now weight by r.sales (actual), matching wAvgLaborPct. Added a new `sales` field (Σ actual sales) alongside the existing `fcstSales` on both the per-store and district rollup objects, since the labor-% weighting needs the actual-sales denominator explicitly, not just for display.\n\nEvery existing test in schedule-summary.test.js used fixtures where sales and fcstSales were equal (built from a LifeLenz screenshot whose "Sales Forecast" column was the only sales figure captured), so the weighting basis never mattered to them -- updated all of them to set both fields explicitly (same value, so their expected results are unchanged) rather than leaving `sales` undefined under the new code path. Added a dedicated test with two days where forecast and actual sales genuinely diverge (day 1: forecast $20k/actual $5k; day 2: forecast $5k/actual $20k) to isolate exactly what changed -- confirms the rollup now weights by actual sales, and that it matches wAvgLaborPct on the identical rows (the two panels no longer disagree).\n\nAlso resolved without code changes, verified against the actual shipped state rather than assumed still-open: #362 (manual laborRows must never mask a stale auto stream) is the same principle #171/v5.034 already implemented (manual excluded entirely from the per-stream freshness check, already merged). #360 (`sales_ledger_daily` reported empty for Aug 12-16, framed at the time as permanently lost data) was a self-corrected misdiagnosis on record in memory/pm-handoff-2026-08-15.md -- the rows were never actually lost (qsr_sales_mix held the same window, already wired into the Digital Sales tile by #347/v5.031); the only real fix needed was the freshness alarm, which is #171, already shipped.\n\n4 new tests confirmed RED against a stash of the pre-fix engine before being restored. 1420/1420 tests pass, build clean (no bundle-size shift -- logic-only change in an already-loaded module).',
  ]};
