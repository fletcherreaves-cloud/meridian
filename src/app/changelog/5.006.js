// @ts-nocheck
export default {version:'5.006', date:'2026-08-12', changes:[
  'Issue #248 — xlsx (141.56 KB gzip / 424.88 KB raw, the largest eager modulepreloaded file after the entry chunk itself) was statically imported in three eager-graph files: App.js (the entry point), parsers/index.js (statically reached from BOTH App.js and engine/pipeline.js), and parsers/inventory-parse.js (statically reached from pipeline.js) — for a library that only runs when someone uploads or exports a spreadsheet. Confirmed views/fob-eom.js and views/sage.js also import xlsx directly but are both ALREADY behind lazyPanel() dynamic imports (fob-eom.js for the FOB EOM panel, sage.js for SAGE\'s Excel export) — their own xlsx imports were never reaching the eager set and needed no change; verified by reading App.js\'s lazyPanel() registrations before touching anything, not assumed. engine/pipeline.js\'s own `import * as XLSX from \'xlsx\'` had ZERO usages in the file (grep-confirmed) — same dead-import shape as #232\'s Chart.js finding — deleted outright. Deleting the dead import alone would have reclaimed nothing, per the issue\'s own explicit warning: parsers/index.js\'s live import was still pinning the library regardless. Read App.js\'s upload handler (handleFiles) before choosing a design, per the issue\'s explicit instruction not to assume: it was ALREADY async (every file in the batch is awaited via `file.arrayBuffer()`), so awaiting a dynamic `import(\'xlsx\')` at the one point it calls XLSX.read() costs nothing extra — no async ripple needed into parsers/index.js\'s or inventory-parse.js\'s many synchronous parse functions (parseRaw/parseProjectionsFile/sniffSheetType/parseSMGFullScale/parseInventoryData all take an ALREADY-parsed `wb`, never call XLSX.read themselves, and every higher-level parser in parsers/index.js routes through parseRaw rather than touching XLSX directly). Built src/lib/xlsx-lazy.js: loadXLSX(), a memoized `import(\'xlsx\')` — first caller anywhere in the app pays for the fetch, every later call (any module) resolves instantly from the same cached promise. parsers/index.js and inventory-parse.js each keep their own module-scoped `let XLSX = null` populated by an exported ensureXLSXReady()-style async function; App.js\'s handleFiles awaits all three (loadXLSX + both ensure-functions) via Promise.all right before its one XLSX.read() call site, so by the time execution reaches any downstream parser, XLSX is already resident in every file\'s own module scope — none of them needed to become async themselves. Also deleted parsers/index.js\'s readFile() helper: defined, exported nowhere, called nowhere (grep-confirmed zero callers) — the file\'s only two XLSX.read() call sites, gone for free with the dead code. Three existing unit tests (sales-ledger-labor-stub, smg-fullscale-dataonly, yearly-targets-parser) call parsers/index.js\'s exported functions directly, bypassing handleFiles\'s readiness await — each now awaits ensureParsersXLSXReady() itself before invoking the parser under test; caught by running the full suite before trusting the change, not assumed clean. MEASURED, both sides same session per this repo\'s standing rule: eager total (entry + modulepreload) 648.24 KB -> 507.15 KB gzip, a REAL reduction of 141.09 KB — essentially the full xlsx chunk, confirmed by re-reading dist/index.html\'s modulepreload list after the build (11 items pre-#232-restructure down to 3: rolldown-runtime, preload-helper, date — xlsx-*.js no longer among them). Headroom under the 850 KB budget: 201.76 KB -> 342.85 KB. Verified in a headless Chromium smoke pass (dev server, no authenticated Supabase session in this sandbox so the actual upload flow can\'t be click-walked): app loads clean, zero console/page errors beyond the expected blocked-network ones (weather API, Google Fonts, Supabase — all sandboxed). 1358/1358 tests pass (unchanged count — 3 existing tests fixed, no new tests needed since parseRaw/parseInventoryData etc. keep their exact synchronous signatures and existing coverage already exercises them).',
]};
