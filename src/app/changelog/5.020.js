// @ts-nocheck
export default {version:'5.020', date:'2026-08-15', changes:[
  'Issue #312 scope 3 (pulled ahead of #292 by #322) — the owner\'s OT Cost tile reading "—" everywhere on every store, after #303 (v5.019) correctly stopped rendering it as a false "$0", turned out to be #303 exposing a real gap rather than causing one: the auto stream feeding it was empty. Traced end to end — `qsrsoft-ops-pull.mjs` selects `overTimeTotalHours`/`overTimeTotalDollars` -> Supabase `qsr_labor_summary` -> mapped to `otHrs`/`otDollar` (`src/lib/supabase.js`) -> `ds.opsLaborRows` (`App.js`) -> already the first, auto-first source in `metric-source.js` -- no app-side change needed, the sourcing was built and waiting on rows. The one broken link: `qsrsoft-ops-pull.mjs` still read `process.env.QSRSOFT_TOKEN` directly at its three invocation modes (`QSRSOFT_CASH_ANOMALY_CHECK`, `QSRSOFT_PULSE_PULL`, default full-ops/backfill) -- a ~1h-TTL Cognito token stored as a GitHub secret, stale ~23 of every 24 hours, the same root cause #321 already fixed for `turnover-pull.mjs`. All three converted to pass `getFreshToken` (the function itself) into `runAll()`, which now resolves a token per (endpoint, date) via a new `resolveToken()` helper -- manual token/if-else-Playwright branching replaced with a generic try/catch around `runAll()` -> `viaPlaywright()`, matching `turnover-pull`\'s established shape. `QSRSOFT_TOKEN` removed from `qsrsoft-ops-pull.yml`\'s env block (secret itself stays defined in GitHub, per the #312 scope-4 hold).\n\nUnlike `turnover-pull`, the TTL cache itself had to become expiry-aware IN THIS PR, not the later 13-script batch: `qsrsoft-ops-pull.mjs` is explicitly backfill-capable (`QSRSOFT_OPS_START_DATE`/`END_DATE`, a day-by-day loop) and CLAUDE.md records a 27-month API backfill as routine at ~6.6s/day, ~1.5h against a ~1h token -- a single mint-once cache would sail past expiry mid-backfill and start 401ing silently. `getFreshToken()` (`scripts/lib/qsrsoft-auth.mjs`) now decodes the token\'s own JWT `exp` claim and re-mints proactively at a 5-minute margin (55-minute assumed-TTL fallback if `exp` can\'t be decoded), plus a `forceRemint` escape hatch for a caller rejected despite a nominally-fresh cached token -- both the proactive and reactive paths, deliberately, per the dispatch\'s explicit "your call which, both are defensible."\n\nLive-verified against production (`workflow_dispatch`, run 31913030549, 2026-08-15, no `AUTH_FAILED`/Playwright-fallback lines -- the direct mint-per-request path worked end to end): pulled 2026-08-11 through 2026-08-15, 1050 rows upserted across all 6 sub-endpoints (cash/labor/laborDetail/service/salesMix/peaks -- this script writes 5 tables total). `qsr_labor_summary` received 135 rows (27 stores x 5 dates). Checked directly against production Supabase (anon key, RLS-honoring REST read): `over_time_total_hours`/`over_time_total_dollars` are present (non-null) on all 135 store-days, with 23 showing a real non-zero OT reading (1.08-21.18 hrs, $25.63-$437.49) -- the exact operational reality the owner reported that the dashboard couldn\'t see. Full log and query output in `memory/project-qsrsoft-cognito-auth-312.md`.\n\nDeliberately not done, per the dispatch: no `sync-failure-watch.yml` entry -- "QSRSoft Operations Report Pull" is already watched and was already passing throughout the outage, which is the actual point: a workflow can succeed while silently returning zero rows, and failure-watching can\'t see that; closing that gap is #269\'s completeness-ledger\'s job, not a missing watch entry. No historical backfill run -- now that the daily pull is confirmed working, how far back to refill is the owner\'s call on cost and runtime, not ours to decide unilaterally.\n\n1371/1371 tests pass, build clean (no bundle-size impact -- Node-script-only change, entry chunk untouched). The remaining 12 of 14 QSRSoft scripts stay queued behind both `turnover-pull` and `ops-pull` proving out on their real (non-manually-triggered) schedules for several consecutive days before the next conversion batch starts.',
]};
