// @ts-nocheck
export default {version:'5.146', date:'2026-08-24', changes:[
  'Dispatch #105: LifeLenz Bridge -> "MBI vs LifeLenz Accuracy" -- real date-range control, '
  + 'Wednesday-start weekly grouping, and (folded in mid-build after the owner\'s same-day '
  + 'correction) a no-guessing sourcing fix plus a genuine backward-looking accuracy view.\n\n'
  + 'Part 1 (date-range + weekly view): runLifeLenzBridgeScan takes an optional explicit '
  + '{start,end} range -- the original anchor-relative "next 14 days from last actual" default is '
  + 'unchanged when no range is passed, so nothing regresses for the existing quick-glance '
  + 'workflow. A "Custom Date Range" toggle + two date inputs, shared by Single Store and '
  + 'District mode, hand that range through explicitly. Weekly grouping (groupDaysByWeek) reuses '
  + 'the ALREADY-EXISTING week-start-aware weekStartOf(date, wsd) from src/utils/date.js -- no new '
  + 'date helper needed, it already accepted an explicit week-start day. settings.weekStartDay '
  + '(DEF_SETTINGS default 3 = Wednesday, McDonald\'s standard) is read directly, never '
  + 'hardcoded.\n\n'
  + 'Correction folded in (memory/dispatch-105.md\'s "CORRECTION" section, owner: "Labor Analysis '
  + 'I though was on auto pull, please check"): the dispatch\'s original premise -- that no '
  + 'historical LifeLenz forecast archive exists -- was false. scripts/lifelenz-pull.mjs already '
  + 'writes fcst_sales/sales daily into lifelenz_schedule, and loadLifeLenzSchedule() already maps '
  + 'it into ds.schedRows (NOT ds.lifelenzSchedule as the correction text literally said -- '
  + 'verified against the real loader before writing any code). computeLifeLenzAdjustment now '
  + 'prefers ds.schedRows.fcstSales (\'auto\', real, no upload needed) over ds.laborRows.projSales '
  + '(\'manual\', also real, upload-dependent) over the historical-bias pattern estimate '
  + '(\'pattern\', the only actual guess) -- "no guessing" when a real number already exists. Row '
  + 'badges: AUTO / MANUAL / PATTERN, replacing the old binary DIRECT/PATTERN.\n\n'
  + 'Part 2 (MBI vs LifeLenz historical accuracy, unblocked by the correction): a new Accuracy '
  + 'mode reconciles Meridian\'s own recorded forecast history (forecast_snapshots via '
  + 'loadForecastSnapshots -- deliberately NOT a live forecastDay replay over past dates, which '
  + 'would leak today\'s model/calibration into what it would have predicted at the time) against '
  + 'LifeLenz\'s own recorded forecast history (ds.schedRows), for the same closed days, grouped '
  + 'Wednesday-start -- modeled on LifeLenz\'s own native Forecast Accuracy Analysis screen '
  + '(Forecast/Actual/Variance columns). A date with data on only one side still renders, with the '
  + 'missing side shown as "-" rather than silently dropped.\n\n'
  + 'Also folded in: renamed panel-registry.js\'s lifelenz-bridge label to the owner-confirmed '
  + '"MBI vs LifeLenz Accuracy" (superseding the earlier "Forecast Reconciliation" proposal), and '
  + 'added route:true (owner: "put in a url page while you are at it") -- same routePanel/'
  + 'RoutePanelShell pattern as fcst-accuracy, wired in App.js (goRoute(\'lifelenz-bridge\'), a new '
  + 'routePanel===\'lifelenz-bridge\' render gate). Removed the now-dead showLifeLenzBridge state '
  + '-- including a stray setShowLifeLenzBridge(false) call in the Escape-key sweep that '
  + 'src/__tests__/src-no-undef.test.js caught as a live ReferenceError (would have aborted every '
  + 'setter after it in that sweep on every Escape press, same failure class the sweep\'s own '
  + 'comment already documents from a past incident).\n\n'
  + 'Scope note: this dispatch\'s original brief was Part 1 only, with Part 2 explicitly out of '
  + 'scope pending an owner data-availability decision. That decision landed mid-build as a '
  + 'verified correction to memory/dispatch-105.md itself (confirmed via git history on the '
  + 'working branch, and independently re-verified against the real lifelenz-pull.mjs/supabase.js '
  + 'code before any Part 2 code was written) -- Part 2 was built in the same pass rather than '
  + 'deferred, per that correction\'s explicit direction.\n\n'
  + 'Verified through the real LifeLenzBridgePanel consumer (dispatch-105-lifelenz-bridge-'
  + 'daterange.test.js), not the engine functions in isolation: a selected custom range changes '
  + 'which days are scanned and shown; the Wednesday-start weekly grouping produces "Aug 19 - Aug '
  + '25" / "Aug 26 - Sep 1" boundaries for a range spanning a real 2026 Wednesday, matching '
  + 'LifeLenz\'s own reference screenshot; a settings.weekStartDay=0 (Sunday) override produces '
  + 'Sunday-start boundaries instead, proving it is read from settings and not hardcoded; the '
  + 'auto-vs-manual sourcing priority is checked with a date carrying BOTH a schedRows and a '
  + 'laborRows entry (auto wins) -- confirmed this actually detects a regression by temporarily '
  + 'reverting the priority order and watching the test fail, then restoring it; and Accuracy '
  + 'mode\'s date range re-queries forecast_snapshots for the new window. Also renamed shell-nav-'
  + 'snapshot.test.js\'s frozen nav snapshot and panel-registry.test.js\'s route-panel ratchet '
  + '(now eleven, not ten) to match.\n\n'
  + 'Full suite 2316/2316, 0 regressions. Build clean. Entry chunk gzip 519.95 KB -> 522.50 KB '
  + '(+2.55 KB -- LifeLenzBridgePanel is imported statically by App.js, not lazyPanel()\'d, '
  + 'pre-existing before this change); eager total 521.81 KB -> 524.36 KB gzip (budget 850 KB, '
  + 'headroom 325.64 KB).',
]};
