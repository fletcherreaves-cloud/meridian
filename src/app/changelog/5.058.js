// @ts-nocheck
export default {version:'5.058', date:'2026-08-18', changes:[
  'Dispatch17 step 5 (#292) -- loadPmixRows (src/lib/supabase.js) already existed but had zero call sites in App.js, so qsr_product_mix was unreachable from the app entirely (confirmed by grep before this change). Added pmixRows to metric-source.js\'s LAZY_FILL_SOURCES (same shape as auditRows/wasteRows -- raw item-level rows for pattern/detection analysis, not a per-day scalar with a fallback chain) and registered loadPmixRows in App.js\'s configureLazyFill call, so a future consumer reaches it via ensureLazyFill(\'pmixRows\') on demand. No static top-level import, no entry-chunk cost -- loadPmixRows is one more named export off supabase.js, which App.js already imports statically as a whole module.\n\nAlso corrected stream-freshness.js\'s header comment, which said pmixRows had zero App.js call sites (true before this commit, stale after). Deliberately still excluded from the STREAMS freshness list -- a lazy-fill field stays absent from ds until a consumer opens it, and streamFreshness() already treats an absent field as "not loaded this session" rather than an incident, same reasoning auditRows/wasteRows already rely on.\n\nNo panel consumer yet -- out of scope for this PR per dispatch17\'s own ordering (the backfill has to land first).\n\n4276/4276 tests pass (1 updated for the new LAZY_FILL_SOURCES entry).',
]};
