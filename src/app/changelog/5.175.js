// @ts-nocheck
export default {version:'5.175', date:'2026-08-25', changes:[
  'Dispatch #140 — Training Retention: moved into the Scheduling & Labor hub, dropped the '
  + 'internal editorial line, week-anchored range picker, broader location selector, per-metric '
  + 'sparklines + layout pass. Five items from the owner\'s two-round feedback on dispatch #134\'s '
  + 'standalone report, bundled into one dispatch for one engineer.\n\n'
  + '1. Hub move (same shape as dispatch #135\'s Targets Editor -> Performance Review > Customize): '
  + 'schedule-retention.js now exports a content-only ScheduleRetentionSection (no own '
  + 'RoutePanelShell) instead of the standalone ScheduleRetentionPanel. Added as a new '
  + '"Training Retention" tab to App.js\'s SCHED_TABS/SchedulingHubPanel (now exported for real '
  + 'hub-tab render testing, same reason lazyPanel is exported). panel-registry.js\'s '
  + 'sched-retention entry flipped nav+route:true -> kind:\'hub-tab\' (ROUTE_IDS ratchet: '
  + 'thirteen -> twelve, same demotion #106 did for fcst-accuracy/lifelenz-bridge). The old '
  + '?panel=sched-retention deep link now redirects via the modal===\'sched-retention\' handler '
  + '(setSchedTab(\'retention\') + goRoute(\'sched-hub\')), same pattern as sched-summary/'
  + 'targets-editor -- opens the hub and lands on the tab, not a 404, not a standalone panel.\n\n'
  + '2. Owner: "let\'s drop the worth a coaching visit comment on this page... I don\'t want to '
  + 'have a store see that comment directly." buildNarrative()\'s regression branch dropped its '
  + 'editorial tail; the factual pp-change/before-after numbers stay unchanged, on-screen and in '
  + 'print (buildPrintHTML reads the same buildNarrative() output -- confirmed no separate '
  + 'hardcoded copy existed, so one fix covers both). Grepped the shipped file for the literal '
  + 'phrase: zero matches.\n\n'
  + '3. Owner: "needs... the ability to set start timeframe and end time frame (weekly basis) ... '
  + 'let\'s do weeks." Replaced the calendar-day DateRangeControl with a new WeekRangeControl -- '
  + 'two <select>s (start/end week, "Wk of M/D" labels) populated from the distinct LifeLenz '
  + 'business weeks actually present for the selected store (computeStoreWeeks\' own weekKey, '
  + 'never re-derived), clamping so start can\'t land after end. New exported defaultWeekRange() '
  + 'pure helper windows to the trailing 12 weeks by default (or all of them if fewer exist), '
  + 'independently tested. Resets only when the STORE changes, not on every schedRows refresh, so '
  + 'a background data reload never clobbers a manually-picked range.\n\n'
  + '4. Broadened LocationSelector from mode:\'store\' to mode:\'progressive\' (All -> State -> '
  + 'Patch -> Store), sitting on dispatch #139\'s live-patch buildLocationHierarchy fix (confirmed '
  + 'already on main before starting). This report is inherently single-store (one location\'s own '
  + 'before/after history) -- picking a State or Patch now shows its own "pick a Store above" '
  + 'empty state (distinct from the initial "pick a location" state) rather than attempting a '
  + 'cross-store view. Dispatch #141 (patch/operator/org/state rollup) had not been dispatched yet '
  + 'when this shipped, so there was nothing to coordinate scope shapes with live -- documented the '
  + 'choice in-file for whoever picks up #141 next: reuse the same {level,id} LocationSelector '
  + 'value shape, and this per-store panel is deliberately NOT where the rollup gets built.\n\n'
  + '5. Owner: "I like the labor trend chart, do one for Sched vs Forecast hours and TPMH... '
  + 'Maybe make the findings box shorter and spread the text out and free up some room and then '
  + 'stack the charts for effect." Generalized the old laborPct-only inline sparkline into '
  + 'sparklineFor(accessor), driven by a new METRICS registry shared with the week-by-week table '
  + '(one definition per metric instead of two hand-written copies). Renders a small stacked '
  + 'sparkline for all 8 metrics -- Labor %, Sched vs Fcst Hrs, Scheduled Hrs, Forecast Hrs, Schd '
  + 'TPMH, Fixed %, Floor %, Fixed+Floor % -- each keeping the proven pre/post-workshop dot '
  + 'coloring. Narrative box shrunk (8px/12px padding vs 10px/14px) and its bullets now flow '
  + 'horizontally (flex-wrap) instead of stacking one-per-line, freeing the vertical room the new '
  + 'stacked-chart strip uses underneath it.\n\n'
  + 'Do-NOT list honored: computeStoreWeeks/aggregateSpan/splitWeeksAtMark math untouched (UI/copy/'
  + 'scope only); print/export still works from the hub tab; no other SCHED_TABS sibling touched.\n\n'
  + 'Updated shell-nav-snapshot.test.js (Training Retention + its unique 🎓 icon drop out of the '
  + 'sidebar snapshot, same as sched-summary/labor-analytics never appearing there) and '
  + 'panel-registry.test.js\'s ROUTE_IDS ratchet. dispatch-134-schedule-retention.test.js\'s '
  + 'component-wiring tests updated for the renamed export and the progressive-mode State -> '
  + 'Patch -> Store click path; added a coaching-visit-removal assertion, a broader-scope empty-'
  + 'state test, and a defaultWeekRange describe block. New dispatch-140-schedule-retention-hub.'
  + 'test.js renders the REAL SchedulingHubPanel (not an isolated helper) -- confirms the registry '
  + 'entry, the tab bar, a real tab click, and the initialTab deep-link redirect all show the real '
  + 'ScheduleRetentionSection content, per the "would this verification still pass if reverted" '
  + 'standing rule (a test only importing the section directly could pass unchanged with the '
  + 'SCHED_TABS wiring deleted).\n\n'
  + 'Full suite 2590/2590 passing (245 files, up from 2582/244 on main -- +8 new); build clean; '
  + 'entry unchanged at 467.35 KB gzip (467.32 KB on main, +0.03 KB noise), eager payload 538.53/'
  + '850 KB gzip (538.50 KB on main) -- the new controls/sparklines/metrics registry all live in '
  + 'the lazy-loaded schedule-retention chunk (13.5 KB -> 16.2 KB raw), not the entry bundle.',
]};
