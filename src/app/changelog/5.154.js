// @ts-nocheck
export default {version:'5.154', date:'2026-08-25', changes:[
  'Dispatch #41: reconcile the two Model Health Score implementations into one canonical '
  + 'function, close the floor-masking bug it had regardless, and give a red grade a real '
  + 'consequence.\n\n'
  + 'forecast.js carried two independently-maintained 0-100 Model Health scorers since the same '
  + 'commit two months ago (modelHealthScore, feeding at-a-glance.js; computeModelHealth -- note '
  + 'the swapped argument order -- feeding ModelHealthBadge), both rendered on the same screen '
  + 'for the same store at the same time in store-analytics.js. Measured before touching any '
  + 'code (a throwaway fixture harness reusing forecast.test.js\'s ds/laborRows builder pattern, '
  + 'logged and discarded, not committed): on identical inputs the two disagreed by up to 17-25 '
  + 'points depending on scenario, AND -- independent of that disagreement -- modelHealthScore '
  + 'alone already had a floor-masking bug: a store with perfect Calibration/Freshness/Sample '
  + 'but a catastrophic 25-30% MAPE (Accuracy a true 0/25) still weighted-summed to 75/100, a '
  + 'green "Healthy" grade, because three healthy components diluted one real failure.\n\n'
  + 'Fix, in forecast.js only, weights and 75/50 cutoffs unchanged (no evidence supports '
  + 'different numbers):\n'
  + '1) modelHealthScore is now canonical -- true-zero component floors, 6W->4W->full MAPE-'
  + 'window priority (both already had this shape; kept as-is).\n'
  + '2) Fixed the settings-fingerprint dead field for real. di.settingsFp was being compared '
  + 'against settings._fp / settings._settingsFp -- grepped the whole src tree, neither is ever '
  + 'assigned anywhere in the app, so one function\'s -10 penalty fired on every calibrated '
  + 'store unconditionally and the other\'s never fired. Now computes the CURRENT settings\' '
  + 'fingerprint inline the same way backtest.js\'s calibrateStore already stamps di.settingsFp '
  + '(JSON.stringify of lyOutlierThreshold + opsNorm), so the comparison is real.\n'
  + '3) Added a weakest-link override gate on top of the weighted sum (not a 5th weighted '
  + 'bucket): never calibrated, OR data >45 days stale, OR Accuracy itself scores a true 0 caps '
  + 'the grade AND the displayed score at red, regardless of the weighted total -- this is what '
  + 'closes the floor-masking bug above. The existing MAPE-drift check (2W meaningfully worse '
  + 'than 6W) is promoted from a small point deduction into a softer 4th gate condition: it '
  + 'holds a store out of green (caps at yellow) without forcing red, since drift is a leading '
  + 'indicator the Accuracy number alone hasn\'t caught up to yet, not a proven failure. The '
  + 'gate clamps the returned SCORE (not just the .grade label) because at-a-glance.js\'s own '
  + 'checklist item and district tally both bucket by modelHealthScore(...).score directly '
  + '(score>=75/score>=50 thresholds), not by .grade -- clamping only the label would have made '
  + 'the gate invisible on the district-wide view while still showing correctly on the two '
  + 'places that read .grade as a string (store-analytics.js\'s confidence bar, '
  + 'ModelHealthBadge).\n'
  + '4) computeModelHealth is now a thin adapter: calls modelHealthScore(loc, ds, settings) '
  + '(real argument order) and reshapes its {reasons} array into computeModelHealth\'s existing '
  + '{components, notes} shape by category. Zero code changes in at-a-glance.js, '
  + 'store-analytics.js, or model-health-badge.js -- both call sites keep their existing '
  + 'signatures untouched.\n'
  + '5) Step 4 (a red grade gets a consequence): forecastRange/forecastRangeAsync -- the '
  + 'functions store-analytics.js\'s StoreDash actually calls to build the displayed projection '
  + '-- now compute the canonical health ONCE per range call and, only when the grade is red, '
  + 'pass forceModel:\'simple\' through to forecastDay so every day in that range uses the '
  + 'Simple/trailing model (T3M/T6W/T3W median, this project\'s own v4.483 27-store backtest '
  + 'winner) instead of whatever engineered model triggered the red grade. Scoped narrowly: '
  + 'forecastDay\'s forceModel argument already exists and already short-circuits '
  + 'getModelAssignment when set, so backtest.js and the Forecast Accuracy Report -- which pass '
  + 'their own forceModel to test "what would model X have produced" -- are structurally '
  + 'untouched; nothing in getModelAssignment, saveModelOverride, or the Model Assignment '
  + 'backtest/override system changed.\n\n'
  + 'Also removed store-dash.js\'s dead ModelHealthBadge import (imported, never rendered -- '
  + 'flagged as an optional adjacent cleanup by the dispatch).\n\n'
  + 'Explicitly not touched, per the dispatch\'s own scope: MAPE vs WAPE, persisting Model '
  + 'Health as a time-series, validating the 75/50 cutoffs or 30/25/25/20 weights against real '
  + 'outcomes, and Dialed-In\'s calibration engine / backtest.js / the Model Assignment system '
  + 'itself.\n\n'
  + 'Verification exercises the actual consumers, not just the two functions side by side (that '
  + 'comparison was step 1\'s measurement tool only, run once pre-fix and discarded) -- '
  + 'reusing the createRoot/act-under-happy-dom pattern from '
  + 'at-a-glance-checklist-freshness.test.js: renders AtAGlance with a store whose weighted total '
  + 'would have graded green pre-fix (Accuracy masked by three healthy components) and asserts '
  + 'the rendered checklist/tally text shows red; renders ModelHealthBadge directly and asserts '
  + 'the displayed {total}/{gradeLabel} DOM text matches the canonical function\'s output '
  + 'exactly, including the gate, for both a gated-red case and a genuinely-healthy case (no '
  + 'false positive); and calls forecastRangeAsync/forecastRange directly (the real StoreDash '
  + 'code path) asserting modelUsed:\'simple\' only fires for the red-graded store, not the '
  + 'healthy one assigned the same engineered model. 8 new tests across '
  + 'model-health-reconcile.test.js and model-health-red-gate-forecast.test.js. Full suite green '
  + '(228 files / 2363 tests). Build clean. Entry chunk 456.86 KB gzip unchanged (forecast.js '
  + 'was already in the entry bundle -- this change adds logic inside it, moves no code across '
  + 'the lazy/eager boundary); eager total 528.40 KB -> 528.00 KB gzip (net negative, from the '
  + 'store-dash.js dead-import removal). Full resolution: '
  + 'memory/dispatch-41.md Resolution section.',
]};
