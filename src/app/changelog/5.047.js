// @ts-nocheck
export default {version:'5.047', date:'2026-08-17', changes:[
  'Issue #263 -- scripts/qsrsoft-pmix-pull.mjs could exit 0 after upserting zero rows, indistinguishable from a clean run over a day with genuinely no sales. Caught live 2026-08-17: the first Jan-2026-to-today backfill attempt hit a Playwright token-capture failure mid-login, the fallback function returned 0 with no thrown error, and the workflow run reported success. The third failure-reports-success path found in the repo this week, after mf_events\' bare catch{} (dispatch19 #1) and the previously-fixed _pagedParallel error-dropping.\n\nFix: viaPlaywright() now throws (AUTH_FAILED:playwright) instead of silently `return 0`/`return null` on a failed token capture or missing QSRSOFT_USERNAME/PASSWORD -- both prior silent-failure paths. main() now treats 0 rows upserted across a non-empty requested date window as a hard failure (exit 1), not a legitimate empty result -- a real day with zero sales across all 27 stores is not a plausible outcome worth protecting against silently, but an auth/pull failure that produces the identical shape is exactly what already happened once.\n\nAlso added: per-store row-count tallying across the whole run (not just the existing per-date/per-store log lines), logged unconditionally as "N/27 stores had at least one row upserted" plus the specific missing store list when the run is partial -- so a 4-of-27 result is visible directly instead of averaging into a plausible-looking total, the failure mode this exact issue names.\n\nNot in this PR: the backfill itself, which needs a clean re-run against this fixed script to actually validate end to end.',
]};
