// @ts-nocheck
export default {version:'5.008', date:'2026-08-12', changes:[
  'Changelog syntax guard — a leftover git conflict marker (`>>>>>>> ...`), landing at STATEMENT level (outside any string), nearly shipped inside changelog-latest.js during today\'s rebase cycle. npm test passed clean at the time: changelog-version.test.js only ever reads these two files via readFileSync + regex, never as real parsed JS, so nothing in the suite ever asked a parser to look at them. Only npm run build (which actually asks vite/rolldown to parse the module) caught it, and only because build happened to run after the bad commit rather than before — ordering luck, not a guard. Added src/__tests__/changelog-modules-parse.test.js: a real `import` of both changelog-latest.js and changelog-data.js, forcing the same parse a bundler would do, plus shape assertions (version/date/changes format) on both. Verified the test actually catches the failure mode it targets: deliberately injected a fake conflict-marker line into a scratch copy of changelog-latest.js, confirmed the import throws a PARSE_ERROR and the test fails, then restored the original and re-confirmed a clean pass. The conflict marker was a one-off rebase accident; the standing risk this guard actually earns its keep on is an UNESCAPED APOSTROPHE — every entry here is a single-quoted string full of prose (App.js\'s, SAGE\'s, the issue\'s, today\'s, …), each one an escape away from terminating its string early, on every commit that adds an entry, not just rebases. (PR review, 2026-08-14: confirmed `>>>>>>>` INSIDE a string is valid JS and not itself a syntax error — corrected the header/entry wording to stop implying the guard catches conflict-marker text anywhere in the file; it only catches it landing outside a string, which is the shape that actually breaks a build.) 1360/1360 tests pass (1358 existing + 2 new). Both modules are plain data (no DOM/browser APIs), so importing them in bare Vitest is safe — unlike App.js, which changelog-version.test.js\'s own header explains can\'t be imported directly.',
]};
