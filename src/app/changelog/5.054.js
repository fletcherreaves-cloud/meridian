// @ts-nocheck
export default {version:'5.054', date:'2026-08-18', changes:[
  'Issue dispatch16 R4 -- ratchet against color+hex-suffix string concatenation (\'var(--crit)\' + \'18\'), the shape behind #351 and #368. That concat trick only produces valid CSS onto a hex literal (\'#f87171\'+\'18\' -> a real 8-digit hex color); the day a color gets converted to a CSS var() reference, the identical concat yields the literal invalid string "var(--crit)18", which the browser drops silently with no console error -- the element just loses its background/border.\n\nNew src/__tests__/ratchet-color-alpha-concat.test.js counts every `<color-expr> + \'<2-hex-digit>\'` site in src/**/*.js (excluding __tests__ and the changelog/ prose directory) and fails on drift in either direction. Re-measured fresh against this branch rather than trusting the dispatch\'s stale "~55 sites" estimate: 93, across 20 files (task-queue.js 14, analytics.js 16, store-dash.js 12, calendar.js 8, admin.js 4, visit-readiness.js 4, feature-requests.js 4, smg-voice.js 5, and 12 more). Every candidate hand-reviewed to confirm it\'s a real color-alpha concat, not a coincidental 2-hex-char string -- one false positive found and excluded (an example snippet inside patch-heatmap.js\'s own #dispatch11 comment). This is a ceiling, not a mandate to convert all 93 now -- most concatenate onto a hex literal today (harmless) and only become a live bug the day that particular color constant gets converted to a var() token, per #351\'s own history.\n\nsrc/views/smg-voice.js was investigated by name per the dispatch\'s own suspected-live-instance flag (col === \'var(--text3)\' ? \'transparent\' : col + \'22\', two sites: tdNum line 670, cell line 857). Traced both back to their color source (metricColor/vpColor) -- each returns EITHER a hex literal OR the exact literal string \'var(--text3)\', no other var() shape reachable, and the ternary already intercepts that one case before the concat runs. Confirmed false alarm, not a live bug: `col` is provably a hex literal on every path that reaches the concat. Left unfixed and counted in the ceiling like the rest, per the investigation\'s own scope -- no code change to smg-voice.js.\n\nBoth ratchet directions verified by hand: (1) injected a fake `\'var(--fake)\' + \'99\'` line into patch-heatmap.js, ran the suite, confirmed it failed naming `src/views/patch-heatmap.js:94  \'var(--fake)\' + \'99\'` and the new count (94 vs ceiling 93), then reverted; (2) temporarily bumped CEILING to 100 in the test file, ran the suite, confirmed it failed with "Only 93 ... Lower CEILING to 93", then reverted to 93.\n\n1 new test file (2 tests), no application code changed -- no bundle-size shift.',
]};
