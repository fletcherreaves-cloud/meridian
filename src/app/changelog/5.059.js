// @ts-nocheck
export default {version:'5.059', date:'2026-08-18', changes:[
  'Issue #367 -- DI Compare\'s initial week anchor. the old initializer computed the forward distance to the NEXT Wednesday (via a getDay()-based offset with 3, for Wednesday, hardcoded in), then subtracted that distance from today TWICE trying to reach "the last complete week," instead of reading the app\'s configured week-start day. Measured (not just the dispatch\'s stated "it lands on Monday" symptom) by simulating the formula against all 7 weekdays: it lands on a DIFFERENT day of the week almost every time -- Mon/Thu/Sun/Wed/Sat/Tue/Fri across Sun-Sat -- correct only on the one day "today" already happens to be Wednesday.\n\nFix: routes through `weekStartOf` (utils/date.js), the shared helper this exact bug class already exists to prevent -- its own header names "23 distinct week-boundary computations across 13 files, of which 12 hardcoded a day and one had the subtraction inverted" as the reason it was built. The corrected initializer is the current week\'s start (honoring settings.weekStartDay) minus 7 days -- always the prior, fully-elapsed week regardless of what day of the week "today" is.\n\nVerification: renders the real exported DialedInComparisonReport (not a reimplementation) with the system clock faked to each of the 7 weekdays in turn and asserts the initial "Week of ..." text always names the same anchor weekday -- the property the old formula violated on 6 of 7 days. A second test pins the exact expected date.\n\nDispatch follow-up NOT completed, reported honestly rather than guessed: the dispatch asked to "re-run and post the new district MAPEs" once the anchor was fixed, since the old verdicts were graded over a misaligned window. Attempted this against real production data (qsr_daily_activity_rollup + labor_rows for the actual sales atoms, matching App.js\'s real ds.laborRows construction) but found Supabase\'s `user_settings` table has ZERO rows under key=\'dialed_in\' -- the Dialed-In calibration blob that #118 built a cloud-sync path for exists only in whichever browser last ran Calibrate All (localStorage `mf_dialed_in`), not in the cloud table this session can query. No calibration data reachable means no real MAPE to report; fabricating one would violate the same measure-don\'t-reason rule this fix itself is applying. Recommend a follow-up: confirm whether Calibrate All has actually pushed to Supabase recently, since a #118 sync path with zero cloud rows is itself worth checking.\n\n1425/1425 tests pass (8 new), build clean (508.84 KB gzip eager, 341.16 KB headroom, no shift -- logic-only change in an already-loaded module).',
  ]};
