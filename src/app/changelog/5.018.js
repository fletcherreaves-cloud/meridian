// @ts-nocheck
export default {version:'5.018', date:'2026-08-15', changes:[
  'Issue #312 scope 2 + first scope-3 conversion — #312\'s probe (workflow_dispatch, run 31906427264) confirmed QSRSoft\'s Cognito app client accepts USER_PASSWORD_AUTH directly (IdToken: yes, no SECRET_HASH, no ChallengeName): a stored QSRSOFT_TOKEN/QSRSOFT_COGNITO_TOKEN secret is a ~1h-TTL Cognito ID token, so it is stale ~23 of every 24 hours and every scheduled QSRSoft pull that reads one has been falling straight through to Playwright, permanently, by construction. New scripts/lib/qsrsoft-auth.mjs exports getFreshToken() -- mints a token per run via a plain fetch POST (no amazon-cognito-identity-js/SRP; the probe already answered which flow works, and SRP was never exercised, so implementing it now would ship an untested fallback that gets its first real run on the worst possible day), cached for the process lifetime only, never written to disk. The minted token is the SAME shape the stale secrets always were, so every existing fetchDirect()-style call site keeps working unchanged -- only the token\'s source changes. Converted ONE of the 14 scripts sharing this ladder -- scripts/qsrsoft-turnover-pull.mjs (single daily 11:30 UTC cron, 19/19 green in its last 19 runs) -- to mint-then-fetch, Playwright unchanged as the fallback; the other 13 are a deliberate, separate follow-up once this one has run green on its real schedule for several days. Measured, not inferred, per CLAUDE.md\'s "measure it, don\'t reason about it": read this exact script\'s most recent pre-conversion run (31882938971, 2026-08-15) end to end and confirmed both QSRSOFT_TOKEN and QSRSOFT_COGNITO_TOKEN rejected 401 before the Playwright fallback ran -- the "always falls through" premise is now measured on a real run, not just derived from the ~1h TTL. Housekeeping: the probe workflow/script and the amazon-cognito-identity-js dependency it needed for an SRP path that will never be built are retired (an unused dependency kept "just in case" is exactly the dead weight the standing rules warn against); QSRSOFT_TOKEN/QSRSOFT_COGNITO_TOKEN removed from qsrsoft-turnover-pull.yml\'s env block since the script no longer reads them (the two secrets themselves are NOT deleted -- #312 scope 4, held until the converted script has run green on schedule for several consecutive days, as its own separate change). One requested fix from the dispatch was checked and skipped rather than applied blindly: qsrsoft-inventory-history-pull.yml passing QSRSOFT_COGNITO_TOKEN was flagged as an unused env var, but scripts/qsrsoft-inventory-history-pull.mjs imports resolveEbosToken from scripts/lib/ebos-auth.mjs (extracted 2026-08-14, PR #273, a day before #312 was filed), and resolveEbosToken() reads exactly QSRSOFT_COGNITO_TOKEN||QSRSOFT_TOKEN for its SSO-exchange rung -- removing it would have silently broken that script\'s fastest auth path. Left untouched; full writeup in memory/project-qsrsoft-cognito-auth-312.md. 1371/1371 tests pass, build clean (eager 509.61 KB gzip / headroom 340.39 KB, unchanged -- Node-script-only, zero src/ importers).',
]};
