// @ts-nocheck
export default {version:'5.087', date:'2026-08-20', changes:[
  'Dispatch #50 -- Security panel scroll fix + frictionless reveal for the privileged tier, plus INV-004 after an owner correction to dispatch #48\'s premise mid-session.\n\n'
  + 'Part A: the modal scroll bug, diagnosed to the exact line by the owner -- a flex column with no overflow property never qualifies for CSS\'s "automatic minimum size is zero" exception, so it grows past ModalShell\'s 88vh cap and gets clipped instead of scrolling. minHeight:0 added at both the root flex column and the body scroll div. A grep sweep found five other ModalShell call sites with a similar custom-layout shape; none share the exact broken pattern (verified, not assumed) so none were touched. Verification renders through the real SecurityPanel with 40 synthetic findings across both tabs and an expanded finding, this repo\'s own established render-test pattern.\n\n'
  + 'Part B: new reveal_employee_identities_bulk() RPC, mirroring reveal_employee_identity()\'s exact role gate. Admin (the only real DB role "Developer/Admin/Owner" collapses to) gets names pre-populated on mount with zero clicks; supervisor/manager/GM keep the existing click-through path unchanged. Log granularity decided deliberately -- one row per session-view with a token count, not one row per token, which needed identity_reveal_log.person_token to become nullable (a real schema decision, made explicitly). Adversarially probed against a real local Postgres 16 instance (not just read as SQL) -- 11 probes covering anon, the exact NULL-role-bypass incident shape, every role tier, empty input, and log-row content -- confirmed no name ever leaks into a log row or error message.\n\n'
  + 'INV-004 (waste-log padding, manager x day-part x store): the "no day-part sales denominator" blocker from dispatch #48\'s original brief was wrong -- qsr_daily_activity already carries hourly sales, finer than day-part. Boundary determined by live measurement, not assumed: 0 of 26,443 qsr_waste rows have a busn_tm in the one window that would show whether busn_dt needs a business-date shift, so the join uses busn_dt directly, matching the column\'s own name and qsr_daily_activity\'s own established 4am alignment. Day-part bucketing reuses daypartOf() (src/engine/labor-standard.js) rather than a second boundary set. New third subject grain (loc, empToken, daypart) needed a security_findings schema change (nullable daypart column, extended subject_key) -- also verified against a real local Postgres instance, proving no collision between day-parts and full backward compatibility for every existing rule\'s rows. Lands inactive, thresholds measured live (median 12.99, min_denominator 250 clearing a real DAR data-quality tail, min_stdev built in from the start after a clean pre-check).\n\n'
  + '30 new tests across three files, full suite 1812/1812, build clean, no entry-chunk impact. Full writeup: memory/dispatch-50-implementation.md.',
]};
