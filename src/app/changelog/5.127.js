// @ts-nocheck
export default {version:'5.127', date:'2026-08-23', changes:[
  'Dispatch #82, Part A: 10 more QSRSoft pull scripts (dar/digital-app/ebos/employee-roster/'
  + 'mcdelivery/pmix/pull/roster-stats/shift-manager/variance-pull, plus the shared eBOS auth '
  + 'lib feeding qsrsoft-inventory-history-pull.mjs) converted from a static QSRSOFT_TOKEN read '
  + 'to getFreshToken() (#312) -- mints a Cognito ID token per unit of work instead of reading a '
  + 'stored secret with a ~1h TTL that reads as expired ~23 of every 24 hours by construction, '
  + 'no matter how often it\'s rotated. Every one of these was silently falling through to a '
  + 'slower Playwright browser login on nearly every scheduled run -- not broken (the fallback '
  + 'worked), but paying a full Chromium launch + SPA login for what should be a plain fetch, '
  + 'and spending the safety margin the two-path design exists to provide. Playwright fallback '
  + 'kept unchanged everywhere; qsrsoft-security-events-pull.mjs (must stay in-browser-only, '
  + '#81) and qsrsoft-explore.mjs (a manual probe with no fallback to fall through to) were '
  + 'deliberately left alone. Two corrections to the dispatch\'s own file list, found by '
  + 'measuring rather than trusting it: qsrsoft-onhand-pull.mjs never actually reads a reporting '
  + 'token (its SSO-exchange path is dead code, confirmed by its own resolveEbosToken() comment) '
  + 'so needed no change; scripts/lib/ebos-auth.mjs -- not named in the dispatch at all -- was '
  + 'genuinely carrying the same bug, reached indirectly through qsrsoft-inventory-history-pull.mjs, '
  + 'invisible to a grep for direct QSRSOFT_TOKEN reads. Live confirmation on a real QSRSoft run '
  + 'is outstanding -- this sandbox has no QSRSoft credentials or network access, same '
  + 'constraint dispatch #81 hit.\n\n'
  + 'Part B: SAGE\'s query_lifelenz_labor tool renamed gap_vlh -> gap_vlh_total. The old name '
  + 'carried no period and sat next to avg_daily_gap (a true per-day rate) with a note that '
  + 'explained only the sign -- SAGE read the WINDOW TOTAL as a daily rate twice, once reporting '
  + '"Ada-Country Club is +141 hours/day over-staffed" for a store independently confirmed '
  + 'top-quartile on labor % (SAGE itself flagged the contradiction and excluded the data from '
  + 'that answer -- not a control to rely on, since it only caught it because the store\'s labor '
  + 'happened to look good elsewhere). Verified against the real aggregation code before fixing: '
  + '141 / 30 = 4.7h/day, a second store\'s 71.6 / 30 = 2.4h/day, both exactly the kind of number '
  + 'that would have looked plausible and gone straight into a visit recommendation. Extracted '
  + 'the aggregation into a new plain-JS module (supabase/functions/sage-chat/'
  + 'lifelenz-labor-agg.js, mirroring dispatch #80\'s memory-kb.js) so a Vitest test exercises '
  + 'the exact code the Deno edge function runs, not a re-implementation -- 6 new tests, '
  + 'confirmed revert-sensitive by reverting the rename and watching 4 fail exactly as '
  + 'predicted. Suite 2143/2143, build clean, no bundle impact (Part A is Node scripts, Part B\'s '
  + 'module reaches no client panel). Full writeup: memory/dispatch-82.md.',
]};
