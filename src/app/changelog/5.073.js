// @ts-nocheck
export default {version:'5.073', date:'2026-08-19', changes:[
  'Dispatch33 -- security build Phase 0a: Register Audit auto-pull scaffold + Any Transaction probe status. Both pieces hit a hard environment blocker this session did not have going in: no QSRSoft credentials and confirmed network egress to v3.myqsrsoft.com/api.reports.myqsrsoft.com is blocked (403). The dispatch itself assumed live DevTools access for both.\n\n'
  + 'Shipped: scripts/qsrsoft-register-audit-pull.mjs -- a complete pull-script scaffold (auth via qsrsoft-auth.mjs, backfill/gap-detection mirroring qsrsoft-dar-pull.mjs, coverage+freshness via dispatch32\'s _pipeline-contract.mjs) with exactly ONE piece left unfinished: the actual Register Audit report endpoint. fetchRegisterAuditDay() throws a clear named error rather than guessing a plausible-but-unverified URL -- a wrong-but-200-OK guess could silently write incorrect rows into personnel-sensitive per-employee data, worse than not having it. Its own comment records a grounded hypothesis (cross-referencing parseRegisterAudit\'s columns against qsrsoft-ops-pull.mjs\'s COLS_CASH_EXTRACT -- near-identical fields, suggesting the same cash-sheet-extract-family report segmented by employee) for whoever does the real capture to try first, not as working code.\n\n'
  + 'Real bug found and fixed before it shipped: the dispatch\'s own literal wording ("write via the existing saveAuditRows") would have meant importing src/lib/supabase.js -- checked before trusting it, and confirmed that file crashes immediately under plain Node (import.meta.env is undefined outside Vite, and it uses the anon key, not service role). Every other pull script in this repo already avoids this for exactly that reason, reimplementing its own local upsert instead. Did the same: a local saveAuditRows() twin, column mapping copied verbatim from src/lib/supabase.js:859 so the two stay one contract in shape.\n\n'
  + 'New .github/workflows/qsrsoft-register-audit-pull.yml -- workflow_dispatch only, deliberately no cron yet (an unattended nightly job that fails 100% of the time is noise, not safety) -- so it correctly does NOT need a sync-failure-watch.yml entry yet either (that test only requires SCHEDULED workflows to be watched). audit_rows tenant_id/RLS confirmed live via a minimal 1-row query, not assumed. Manual Excel upload fallback untouched.\n\n'
  + 'Part 2 (Any Transaction probe, settling the Tier A exception-filter question) not attempted at all -- the dispatch itself calls it "a capture-and-report task," pure live QSRSoft UI investigation with no code to write. Nothing here answers the Tier A question either way; fabricating a guess would be worse than leaving it open, per the plan file\'s own text that this needs a measured request/response, not an inference. Full writeup, including exactly what unblocks both pieces: memory/dispatch33-register-audit-pull.md.',
]};
