// @ts-nocheck
export default {version:'5.031', date:'2026-08-16', changes:[
  'Issue #346 (Digital Sales tile) -- the tile read "No channel data -- upload Operations Report with Sales sheet" while the real numbers already sat in the browser. Measured: opsSalesMixRows (auto-pulled qsr_sales_mix, COLS_SALES_MIX in qsrsoft-ops-pull.mjs) carried 1,647 rows, read by nothing anywhere in src/ -- the same shape as the Labor % illusion: pull works, loader works, rows reach ds, no panel asks. Root cause of the specific blank window: sales_ledger_daily (the currently-wired auto channel source) has ZERO rows for Aug 12-16, while qsr_sales_mix has 135 for the same window (27 stores x 5 days, fully populated) -- a gap in the auto source the panel was reading, not a missing pull.\n\nFix: channelRows (at-a-glance.js) now merges THREE sources freshest-per-day instead of two -- manual labor rows, then Sales Ledger, then Sales Mix filling whatever Ledger doesn\'t cover. _loadOpsTable spreads the row\'s `metrics` JSONB directly, so qsr_sales_mix\'s raw fields are its own snake_case (kiosk_amt, net_sales_dthru_amt, ...) -- mapped onto the exact shape salesLedgerRows already produces (dtSales/bfSales/delivSales/mopSales/kioskSales/eatInSales) so every existing downstream consumer (digitalSec, the channel-mix chart, the OK/FL badges) needed zero changes.\n\nVerified end to end against production, not just that the resolver returns something: built ds through the real loadOpsSalesMix/loadSalesLedger (not a hand-built fixture), zero manual laborRows (the reported bug condition), Aug 12-16 window. Result: 135 channelRows.rows resolve, 27/27 stores present, Total Net Sales $1,307,743, Digital Sales $410,677 (31.40% digital mix), McDelivery $93,005 / MOP $193,530 / Kiosk $124,142 -- the tile renders real district numbers where it previously showed the upload prompt.\n\nAlso (#345, same audit): opsPeaksRows (qsr_peaks_sales, the sibling stream pulled by the same Ops Report block) has 4,938 rows and, unlike Sales Mix, genuinely zero consumers anywhere -- no existing blank tile waiting for it, unlike Digital Sales. Dropped loadOpsPeaksSales from the eager startup load (App.js\'s _stOpsReportStream) rather than build a new panel blind -- every session was paying startup time + egress to parse rows nobody read, which CLAUDE.md\'s performance-budget rule treats as a real cost. Pull/schema/loader untouched; filed #345 to decide (not silently drop) whether to build the panel or leave the stream dormant.\n\n1390/1390 tests pass, build clean (508.31 KB gzip eager, 341.69 KB headroom -- opsSalesMixRows was already loaded into ds before this change, so wiring it to a consumer is logic-only; dropping opsPeaksRows removes one Promise.all leg, netting a small entry-chunk decrease).',
]};
