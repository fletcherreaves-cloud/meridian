// @ts-nocheck
export default {version:'5.197', date:'2026-08-27', changes:[
  'Performance Review continuity, Phase 4a (dispatch #152): restructures the review DATA MODEL '
  + 'from one-record-per-HALF to one-record-per-person-per-YEAR, containing all four quarters, '
  + 'both half-year rollups, and a new full-year rollup. Data/engine layer only -- the creation '
  + 'form\'s H1/H2 picker, the list view\'s period column, the in-editor period selector, and the '
  + 'three print functions are Phase 4b, a separate later dispatch; leaving them unwired against '
  + 'this shape change is expected and documented in this dispatch\'s PR body, per the dispatch\'s '
  + 'own explicit scope.\n\n'
  + '`reviewId(person, year)` drops the "_H1"/"_H2" id suffix -- a review is keyed by year alone '
  + 'now. `blankReview(name, role, loc, year, cfg, person)` drops the `half` parameter entirely: '
  + '`kpis.months` now populates all 12 months (was 6), `behavioralRatings` now populates all four '
  + '`q1..q4` keys (was 2), and the top-level `half` field is gone. Approval status moves from a '
  + 'single top-level `status` to `periods.h1`/`periods.h2` (`{status, statusHistory, '
  + 'statusNotes}` each) -- a year is not approved as one atomic event; its two real-world review '
  + 'conversations are, at different points in the year. `transitionReview(id, half, newStatus, '
  + 'notes)` takes a new `half` argument and only ever touches that one period\'s own history.\n\n'
  + 'DESIGN DECISION: the review\'s person-identity field UNIFIES with the same identity space '
  + 'dispatch #150/#151 built for `staff_assignments.person`/`profiles.person` (a geid for a '
  + 'roster-sourced role, or a plain name string for AS/OM/DO) -- a new nullable `person` field, '
  + 'separate from the existing narrow `geid` shift-attribution field (NOT repurposed). Not '
  + 'auto-derived here (same manual-field precedent as `profiles.person`); `person` defaults to '
  + 'null and `reviewId` falls back to the reviewee\'s plain name, so every existing call site '
  + 'keeps working unchanged. Full reasoning in the PR body.\n\n'
  + '`computeScores`/`computeScoreBreakdown` now return q1, q2, q3, q4, h1, h2, AND year from one '
  + 'call (previously just the review\'s own half). Every rollup level reuses the identical '
  + 'combining step: metrics recomputed fresh over the union of that level\'s months (unchanged '
  + 'from how "half" always worked), behavioral averaged from the already-computed sub-period '
  + 'scores via one shared helper -- so the new year rollup is proven, by a concrete numeric test, '
  + 'to be structurally identical to how h1 already combines q1+q2, not a reinvented formula.\n\n'
  + 'One minimal UI crash fix (not Phase 4b work): `NewReviewForm`\'s create button called the '
  + 'old 6-arg `blankReview(...half, cfg)` -- unfixed, this would have shifted `half` into the '
  + 'new `cfg` parameter slot, corrupting every newly-created review and throwing the moment it '
  + 'was opened. Fixed to the new 5-arg call; everything else about how a review is created '
  + '(the still-present, now-inert H1/H2 picker, and all the wrong/hidden UI this restructure '
  + 'causes elsewhere -- always showing H2/Q3-Q4 months, the Score Breakdown panel vanishing, '
  + 'the list\'s Period/Score columns going blank) is untouched, expected, and deferred to '
  + 'Phase 4b per this dispatch\'s PR body.\n\n'
  + '`schema.sql`: `reviews.review_half` column dropped (a row is a full year now); '
  + '`reviews_year_idx` narrowed to `(review_year)`. Live production Supabase -- NOT applied yet; '
  + 'see this dispatch\'s PR body for the exact SQL and what a human must verify, including a live '
  + 'measurement (service-role Supabase REST) that found `reviews` at 3 rows (not the 0 the plan '
  + 'doc\'s days-old claim stated) -- all 3 are the owner\'s own review-system test entries, '
  + '`review_overrides` confirmed empty.',
]};
