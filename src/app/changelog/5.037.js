// @ts-nocheck
export default {version:'5.037', date:'2026-08-17', changes:[
  'Issue #363 -- supervisor-of-record had THREE independent hardcoded literals living outside constants.js\'s own effective-dated timeline (orgAssignments/whoRan/supervisorGroups, the same live source #220 already routed patch-heatmap.js through). None of them moved when a patch was reassigned through Management.\n\nmorning-brief.js\'s SUPERVISOR_PATCHES had actually gone STALE relative to the live org -- it used short last names as keys and disagreed with DEF_SETTINGS.supervisorGroups on several stores (e.g. \'43701\' sat under \'Podroza\' there, but under \'Brad Denley\' in the live org). Deleted it; the Morning Brief\'s per-store supervisor label and its supervisor-filter dropdown now read whoRan(loc, targetDate) and supervisorGroups() respectively -- the brief is date-scoped, so this also fixes a subtler bug the flat map couldn\'t have handled: a brief for a PAST date now correctly shows whoever ran that store on that date, not whoever runs it today.\n\nSTORE_STAFF (morning-brief.js) carried a hardcoded sup/supEmail string literal per store -- currently accurate, but nothing kept it that way. It was read in exactly one place in the whole app: pipeline.js\'s buildStore, which every other panel\'s `s.sup`/`s.supEmail` traces back to. Dropped sup/supEmail from STORE_STAFF\'s 27 rows (gm/gmEmail unchanged -- GMs don\'t move with patch reassignments) and pipeline.js now resolves sup via whoRan(loc, today) with CONTACTS.supervisors[sup].email for the address, falling back to the STORE_STAFF literal only if whoRan finds nothing. Fixing the one choke point fixes every downstream `.sup` reader in the app (store-dash.js, analytics.js, store-analytics.js, labor-tools.js, smg-voice.js, PanelControls.js) without touching any of them.\n\ninventory.js read INV_ORG_COORDS[loc].sup directly in two report-generation spots (the per-location PDF header and the district-summary table) -- the frozen constants.js snapshot, not the live org. Both now try whoRan(loc, today) first, falling back to the snapshot only if whoRan returns nothing.\n\nGuard test (src/__tests__/supervisor-live-resolution.test.js): a census scanner across all of src/ (excluding constants.js itself) for the two literal shapes that actually shipped -- a quoted supervisor-name assigned to `sup:`/`supEmail:`, and a quoted-name key mapped to an array of quoted loc strings (SUPERVISOR_PATCHES\' own shape). Confirmed both patterns match the REMOVED code verbatim before asserting zero hits on the current tree. Plus 3 tests exercising whoRan/supervisorGroups directly: every store in the seed org resolves to its documented supervisor, a mid-history reassignment changes the answer only after its effective date, and supervisorGroups() names the same supervisor set the guard test checked.\n\n1415/1415 tests pass (4 new), build clean (no bundle-size shift -- logic-only change, no new dependency).',
]};
