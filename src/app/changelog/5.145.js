// @ts-nocheck
export default {version:'5.145', date:'2026-08-24', changes:[
  'Dispatch #108: Event Impact Registry -- measured the 4 unmeasured retail types plus Holiday, '
  + 'Festival/Fair, and LTO/Promo into production, and added guest-count (GC) lift alongside sales '
  + 'lift for every measured type.\n\n'
  + 'Step 1 measured, not assumed: `node scripts/measure-retail-impact.mjs --dry` against production '
  + 'showed event_impact held 26 rows, ALL event_type=sports -- the 4 retail types (tax_free/'
  + 'black_friday/small_biz_sat/cyber_monday) had never actually been written despite the script '
  + 'existing and being proven. The dry run reported 101 gradable rows ready; running it for real '
  + 'landed them.\n\n'
  + 'GC source, confirmed not assumed: qsr_daily_activity_rollup.transactions -- the app\'s already-'
  + 'canonical `gc` metric (metric-source.js\'s gc chain already leads with qsrActSummaryRows, which '
  + 'reads this table). Queried live (service-role key): backfilled 2024-01-01 -> present, 24,891 '
  + 'day-rows, cross-validated exact-match against qsr_sales_mix.metrics.gross_sales_qty (store 3708, '
  + '2026-08-01: both 979 transactions, product_sales/product_sales_amt both 11431.31). ~2.5 years '
  + 'shorter than labor_rows\' 2022-01-01 sales floor -- so a store can show sales lift with no GC '
  + 'lift for pre-2024 observations alone; that is real data-coverage difference, not a bug.\n\n'
  + '`measureEventLift` (retail-events.js) generalized via a new opts.valueKey (default \'sales\', so '
  + 'every existing call site is byte-identical) -- same median-baseline/±28-day/exclusion logic now '
  + 'grades GC lift by passing valueKey:\'gc\' over rows shaped {loc,date,gc}. shrinkLifts unchanged. '
  + 'New shared scripts/lib/event-impact-write.mjs holds loadGcRows/upsertEventImpact/'
  + 'mergeSalesAndGcWrites so all 3 measurement scripts share the GC-load + per-metric-independent-'
  + 'minN merge + graceful pre-migration fallback.\n\n'
  + 'New: scripts/measure-holiday-impact.mjs -- rule-derived from HOLIDAY_MAP (15 open-holiday '
  + 'labels; Christmas Day/Eve, New Year\'s Eve, and Thanksgiving excluded as closure/partial-'
  + 'closure, same exclusion measure-retail-impact.mjs already used for its baseline, reused here to '
  + 'build the event-day list instead), all bucketed into ONE \'holiday\' event_type matching the '
  + 'panel\'s single row. New: scripts/measure-tagged-event-impact.mjs -- \'event\' (Festival/Fair) '
  + 'and \'promo\' (LTO/Promo) measured against whatever is ALREADY tagged in org_events (16 event '
  + 'rows/11 stores, sparse as expected; 756 promo rows/27 stores, solid coverage) -- never invented. '
  + '\'weather\' pools the 10 EVENT_TYPES weather subtypes into the panel\'s single Weather row, but '
  + 'production currently has ZERO org_events rows of any weather subtype, so this run wrote 0 '
  + 'weather rows -- an honest "not yet tagged" result, and the pooling choice is flagged for owner '
  + 'confirmation (see memory/dispatch-108.md Resolution), not built as a new statistical rule.\n\n'
  + 'Schema: new supabase/schema-event-impact-gc.sql adds 6 nullable columns (gc_home_impact/'
  + 'gc_away_impact/measured_gc_home/measured_gc_away/n_gc_home/n_gc_away), additive only, inherits '
  + 'the table\'s existing RLS. This session has no DDL path to production (no direct Postgres '
  + 'connection, no exec_sql RPC -- probed and confirmed absent) -- same as every other supabase/'
  + 'schema-*.sql file in this repo, it is a one-time owner step in the SQL editor. Until then, '
  + 'upsertEventImpact() catches the PGRST204 "column not found" and writes sales-lift-only, so real '
  + 'measured data still landed today (189 total rows across sports/tax_free/black_friday/'
  + 'small_biz_sat/cyber_monday/holiday/event/promo) rather than blocking on the migration. Re-'
  + 'running the same 3 scripts after the migration lands GC with no code change.\n\n'
  + 'EventImpactPanel (event-impact.js): added GC Home %/GC Away % (or GC % for non-sports types) '
  + 'editable columns beside the existing Home %/Away %, following the exact same input/measured/n/'
  + 'reset pattern -- reset now restores both sales AND GC to their measured seed; a store can carry '
  + 'one lift without the other (independent columns, independent minN gate) without breaking the '
  + 'row. loadEventImpact/saveEventImpact (lib/supabase.js) extended additively; saveEventImpact only '
  + 'sends gc_* keys when the payload actually carries a gc field, so a plain sales-only edit never '
  + 'round-trips explicit nulls over real measured GC values it never touched.\n\n'
  + 'Verified: reproduced two measurements by hand against live production data, independent of the '
  + 'scripts -- GC lift for store 10034\'s OK tax-free Friday 2026-08-02 (median baseline 1204.5 vs '
  + '1210 actual = +0.46%, one of the script\'s 9 averaged observations) and sales lift for store '
  + '3708\'s MLK Day 2025-01-20 (median baseline $8990.55 vs $8851.09 actual = -1.55%, one of 70 '
  + 'observations in its holiday bucket). Sales-lift numbers for the 4 pre-existing retail types are '
  + 'byte-identical before/after the generalization (same district means, same n, same shrunk %). '
  + '36 new/extended tests in retail-events.test.js (opts.valueKey coverage) + 4 new render tests in '
  + 'event-impact-panel.test.js (renders the real EventImpactPanel, not just the engine -- GC columns '
  + 'present, independent-coverage row renders without fabricating a "gc null", reset restores both '
  + 'fields). Full suite 2310/2310 green, build clean, entry chunk unaffected by this panel (lazy, '
  + 'kind:\'nav\') -- eager total still 522.20 KB gzip / 850 KB budget, unchanged from baseline. '
  + 'Sports (Home/Away) rows confirmed completely unchanged (still 26 rows, same values) by this work.',
]};
