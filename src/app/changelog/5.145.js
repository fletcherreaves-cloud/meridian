// @ts-nocheck
export default {version:'5.145', date:'2026-08-24', changes:[
  'Dispatch #104: Top/Bottom Performers -- cleaner selectors, FOB % added as a rankable category.\n\n'
  + 'Metric picker: the 16-metric flat pill row (two wrapped rows in the owner\'s screenshot) is '
  + 'now a single <select> bound to metricKey. Identical metric set, identical selection semantics '
  + '-- pinned by driving the real <select> in the two pre-existing ranking tests instead of '
  + 'clicking a pill button.\n\n'
  + 'Location selector: LocationSelector already WAS the shared pill-style standard '
  + '(components/PanelControls.js, the same component dispatch #100 used for Security) -- the '
  + 'actual complaint was its mode:\'full\' rendering every level (All/State/Patch/Store, 30+ '
  + 'pills) flat and simultaneously. Added a new mode:\'progressive\' alongside the existing '
  + '\'full\'/\'store\': same component, same pill styling, same {level,id} value shape and '
  + 'locationSelectorLocs() resolution (untouched) -- All + States show first, picking a State '
  + 'reveals its Patches, picking a Patch reveals its Stores, and the current selection stays '
  + 'visibly expanded (a breadcrumb) rather than collapsing. No live owner confirmation was '
  + 'reachable this session, so this is the dispatch\'s own stated fallback (stay inside the '
  + 'documented pill standard, not a dropdown) -- flagged as such, not a confirmed answer.\n\n'
  + 'FOB % rankable category: built on dispatch #102\'s now-merged qsr_fob fix, not the ~24x-'
  + 'inflated aggregation #102 replaced. metric-source.js gained the 3 missing FOB dollar legs '
  + '(condimentsAmt/empMgrMealsAmt/unexplainedAmt, mirroring the existing compWasteAmt/rawWasteAmt/'
  + 'statVarianceAmt pattern), a derive-only fobTotalAmt (6-way sum, same derive-only shape as the '
  + 'pre-existing spph/avgRate), and fobPct (fobTotalAmt / prodSalesAmt, kind:\'ratio\', '
  + 'direction:\'lower\') -- plus a matching PERFORMER_METRICS row. Confirmed this does NOT '
  + 'reproduce the #102 inflation trap: unlike summing one raw $ leg across snapshot-duplicated '
  + 'days, fobPct\'s Sum/Sum sums TWO legs on the same MTD-cumulative-snapshot basis, so the '
  + 'inflation cancels to first order (a ratio-of-sums, not a sum). Verified two ways: a unit test '
  + 'modeling 3 identical-snapshot days lands the Sum/Sum result within 1e-6 of the single day\'s '
  + 'own ratio; and a live spot-check against real qsr_fob (service-role Bearer, content-range '
  + 'confirmed 0-4/24534 rows visible -- qsr_fob.loc is zero-padded to 7 chars in the table, '
  + 'metric-source.js\'s existing _PADDED_LOC_SOURCES already handles it) for 3 real stores\' July '
  + '2026: the SHIPPED metricSumRatio(...,\'fobPct\') call reproduces the true latest-snapshot '
  + 'figure exactly -- 3708 (OK) 4.55% vs 4.55%, 6178 (FL) 3.49% vs 3.49%, 5183 (OK) 4.50% vs '
  + '4.50%.\n\n'
  + 'Verified through the real TopBottomPerformers consumer (top-bottom-performers-panel.test.js): '
  + 'new tests for FOB % ranking + Sum/Sum basis, and for the progressive selector (All+States '
  + 'only by default, State reveals its own Patches only, Patch reveals its own Stores, selecting '
  + 'a Store narrows the ranking to exactly that loc -- same locationSelectorLocs() substance as '
  + '\'full\' mode, unchanged). rollupCapableMetricKeys()/metric-sum-ratio.test.js guard tests '
  + 'updated for the new fobPct key. Did NOT touch analytics.js\'s computeFOBMetrics -- dispatch '
  + '#102\'s scope, confirmed zero lines changed there.\n\n'
  + 'Full suite 2311/2311, 0 regressions. Build clean. Entry chunk gzip 519.95 KB -> 520.05 KB '
  + '(+0.10 KB -- top-bottom-performers.js is a lazyPanel()-loaded chunk, so almost none of this '
  + 'lands in the eager bundle); eager total 521.81 KB -> 521.91 KB gzip (budget 850 KB, headroom '
  + '328.09 KB). Full resolution notes: memory/dispatch-104.md.',
]};
