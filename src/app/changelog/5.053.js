// @ts-nocheck
export default {version:'5.053', date:'2026-08-18', changes:[
  'Issue dispatch16 R3 -- ratchet against raw week/day arithmetic in panels. CLAUDE.md\'s "THE BUSINESS DAY RUNS 4:00am -> 4:00am" rule says week-start alignment must go through settings.weekStartDay and businessDate()/lastClosedBusinessDay() (src/utils/date.js), never a bare .getDay() re-derived inline -- #367 shipped exactly this bug, a "DI Compare" panel landing dates on the wrong day because (3 - getDay() + 7) % 7 used forward distance with a doubled subtraction, and the shape recurred more than once before anything checkable enforced "use the shared helper."\n\nNew src/__tests__/ratchet-week-day-arithmetic.test.js walks src/views/ + src/features/ (excluding __tests__ and *.test.js) for bare .getDay() calls and locks the count at a measured ceiling, same two-directional design as R1\'s raw-metric-rows ratchet, sync-failure-watch.test.js, and light-mode-white-alpha.test.js: a rise fails naming the new file:line, a drop fails telling you to lower the ceiling so a stale check can\'t quietly stop protecting anything. Re-measured fresh against origin/main rather than trusting the dispatch\'s "~60 across 14 files at v5.034" estimate -- landed on the identical 60 occurrences across 14 files (src/features/calendar.js, lifelenz.js, location-intel.js, projections.js; src/views/analytics.js, at-a-glance.js, dt-speedofservice.js, labor-tools.js, one-pager.js, record-day.js, schedule-summary.js, scheduling.js, store-analytics.js, store-dash.js), so no drift since the dispatch was written. src/engine/schedule-summary.js\'s weekStartOf() is explicitly exempt in the test\'s own header comment -- that file IS the shared helper, not a violation, and it also sits outside the test\'s src/views + src/features scope; the in-scope src/views/schedule-summary.js (the panel that renders what the helper builds) is a different file and its one getDay() call is counted normally.\n\nBoth fail directions verified by hand before landing: appended a throwaway `new Date().getDay()` line to store-dash.js and reran the test -- failed, naming the exact new file:line (store-dash.js:3689) in the error, then reverted with `git checkout --`; separately bumped CEILING to 65 in the test file and reran -- failed with "Only 60 raw .getDay() call(s) remain... Lower CEILING to 60", then reverted CEILING to 60. Full suite green at the real seed both before and after.',
]};
