// @ts-nocheck
export default {version:'5.065', date:'2026-08-19', changes:[
  'Dispatch23 -- fixed a real gap in #415\'s forecast-week precompute job (found while grounding the dispatch, not by the #415 review): scripts/forecast-week-precompute.mjs called forecastDay(loc, d, ds, {}, null, t) -- an EMPTY settings object. forecastDay\'s event-adjustment block reads settings._userEvents[loc][date] and settings._eventFactors[loc], and short-circuits to zero lift/dip whenever either is absent (or whenever settings.useEventRegistry itself is missing, even with real event data present -- confirmed as a second, separate way to reintroduce this same bug, now guarded by its own test). Both were undefined in the precompute script\'s cfg -- so for a fully-cached store, the At A Glance weekly total during a real tagged event (a football game, a district holiday, a price change) was silently the UN-adjusted number, diverging from what the live browser path computes for the exact same store/day.\n\n'
  + 'Fixed by fetching org_events and event_impact live (the script already fetched labor_rows/qsr_daily_activity_rollup/model overrides the same way) and building the identical _userEvents/_eventFactors shape App.js\'s own startup hydration builds -- orgEventsToDayMap() and computeEventFactors() are IMPORTED, not hand-copied, matching the "reuse the real engine" discipline #415 already established for supplementLaborWithSched.\n\n'
  + 'Verified against real Supabase data, not just "the script runs without error": found a genuine tagged event (Cottondale HS Football, Home, "High" impact/dinner daypart, 2026-08-21) and compared forecastDay\'s output with vs without the event cfg using the same real loaded data -- $9,648 (the old, event-blind number) vs $10,121 (the fixed, event-aware number), a real +$473 lift matching the tagged impact. Also measured, honestly: EVERY real store with a tagged event this week happens to be assigned the \'ae\'/\'ewma\'/\'simple\' models, which early-return inside forecastDay BEFORE reaching the event-adjustment block at all -- a structural, pre-existing forecastDay property unrelated to this fix, meaning the live real-data test above needed forceModel:\'dow\' to actually exercise the fixed code path. This fix is correct and necessary for any store assigned \'di\'/\'dow\'/an engineered model (which model-assignment backtest overrides can move a store into at any time) -- its observable effect on the current district today is smaller than the dispatch\'s own framing implied, and that is recorded plainly rather than left to be discovered later. Full trace: memory/dispatch23-precompute-event-factors.md.\n\n'
  + '2 new tests (forecast-precompute-events.test.js) prove the real-shaped fix changes forecastDay\'s output, and separately guard the useEventRegistry:true flag itself -- confirmed live that omitting just that one flag silently reintroduces the exact same bug even with correct _userEvents/_eventFactors already wired.\n\n'
  + '1533/1533 tests pass (2 new). Build clean, budget unchanged (Node-script-only change, no client bundle impact).',
]};
