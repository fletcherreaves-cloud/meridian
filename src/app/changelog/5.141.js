// @ts-nocheck
export default {version:'5.141', date:'2026-08-24', changes:[
  'Dispatch #100: Security panel gets the standard 4-level location selector and a date-range '
  + 'control. scopeMatches() already implemented All->State->Org->Store, but the rendered pill '
  + 'row (SecurityPanel) only ever built All and per-State pills -- Org (Emerald Arches/MCDOK, '
  + 'the same mapping scopeMatches already used) and Store (all 27 real INV_ORG_COORDS locs, same '
  + 'shipped flat-pill pattern OpportunityDollars\' LocationSelector already uses) were silently '
  + 'unreachable in the UI even though the filter logic underneath handled them correctly. Both '
  + 'now render in the same pill row, additively -- All/State behavior verified byte-identical '
  + '(58 pre-existing tests unchanged, all still passing).\n\n'
  + 'loadSecurityFindings() had no date parameter at all -- every row, unbounded. Live-measured '
  + 'via the service-role credential before deciding an approach (per the standing "measure, '
  + 'don\'t reason" rule): security_findings holds 29,866 rows, but window_end spans only '
  + '2026-08-20 to 2026-08-31 (the batch job is young) -- client-side filtering of the already-'
  + 'loaded findings array is simpler and sufficient; no server-side query change made. Filters '
  + 'on windowEnd, not computedAt -- verified against how this panel\'s own subject-timeline/trend '
  + 'features already order history (groupFindingsBySubject\'s per-rule window sort, '
  + 'buildSubjectTimeline()), both windowEnd-keyed with computedAt only a tiebreaker. Reuses the '
  + 'shared DateRangeControl (src/components/PanelControls.js, issue #126, previously unused by '
  + 'any view) plus a local "All dates" reset pill, rather than a bespoke date-input pair or an '
  + 'edit to the shared component. The control names its own basis explicitly ("Findings with a '
  + 'window ending:").\n\n'
  + 'Owner follow-up in the same pass: every rule-filter pill (Cash and Inventory) now shows a '
  + 'short descriptor next to its ruleId (ruleShortTag(), mechanically derived from the rule\'s '
  + 'own already-loaded security_rules.method -- e.g. "CASH-002 · POS over-ring", "INV-002 '
  + '· Dollar-variance" -- never a hardcoded ruleId->tag table), additive to the fuller '
  + 'description line that already renders below for the selected rule. Inactive CASH-003 still '
  + 'carries its ⏸ marker alongside the new tag.\n\n'
  + 'Suite 2279/2279 (21 new: scopeMatches org assertions, windowEndInRange + ruleShortTag pure '
  + 'tests, and render-based tests through the real SecurityPanel -- Org/Store pills verified '
  + 'against real INV_ORG_COORDS stores, the date control verified against a wall-clock-anchored '
  + 'real/distant fixture pair via resolveDatePreset(), rule-pill descriptors verified for both '
  + 'domains). Build clean. security-panel.js chunk (lazy, not in the entry budget): 34.37 KB '
  + 'gzip 11.62 KB, was 33.18 KB gzip 11.25 KB (+0.37 KB gzip). Entry chunk unaffected: 519.63 KB '
  + 'gzip, was 519.62 KB; eager total 521.49 KB of the 850 KB budget, headroom 328.51 KB.',
]};
