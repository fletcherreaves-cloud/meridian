// @ts-nocheck
export default {version:'5.128', date:'2026-08-23', changes:[
  'Dispatch #83: rebuilt qsrsoft-security-events-pull.mjs on a plain Node fetch. Dispatch #81\'s '
  + 'premise -- that api.security.myqsrsoft.com fingerprints the TLS/HTTP-2 client, so only an '
  + 'in-browser page.evaluate() fetch could reach it -- is OVERTURNED. A plain curl from the '
  + 'owner\'s Mac, full browser header set + a fresh token, returned 200 and 23 real event rows; '
  + 'curl\'s TLS fingerprint resembles Chrome\'s not at all. Worse for #81: the in-browser fetch '
  + 'is the one that actually fails (216/216 CORS-blocked, preflight 403). Origin and Referer are '
  + 'settable from Node but are forbidden headers in the browser Fetch API -- silently dropped '
  + 'in-page -- which is a clean, sufficient explanation for the asymmetry without invoking any '
  + 'fingerprinting mechanism at all.\n\n'
  + 'So this is a simplification, not another workaround: deleted the Chromium launch, the SPA '
  + 'login, 216 page.evaluate() calls, the screenshot artifacts, and the CORS problem -- replaced '
  + 'with one Node fetch() per unit carrying the exact 12-header set the working curl sent, and '
  + 'getFreshToken() (#312) resolved per unit of work, same pattern as every other converted '
  + 'reporting-API pull. File went from 564 lines to 380. All 10 existing pure-helper tests pass '
  + 'unmodified -- dateList/buildUrl/buildBody/extractRows untouched.\n\n'
  + 'May also explain the ORIGINAL 403s that started this whole investigation (#58, #63, #65, '
  + '#66, #67, #70): the fingerprint conclusion came from replaying a captured browser token from '
  + 'Node -- QSRSOFT_TOKEN is a ~1h Cognito token, so if more than an hour passed between capture '
  + 'and replay, every one of those was simply an expired credential. Left as an open question '
  + '(Q3) for the owner to confirm on the first live run: deliberately reuse a stale token and '
  + 'confirm it 403s, which would retire the theory completely instead of leaving it half-buried.\n\n'
  + 'Two more open questions, each requiring changing exactly one variable at a time against the '
  + 'live API (the confounded test that produced the wrong six-dispatch-old conclusion is '
  + 'precisely what these guard against): Q1, which of the 12 headers are actually load-bearing '
  + '(drop in groups, Origin/Referer last); Q2, does it work from a hosted ubuntu-latest runner '
  + 'now that the client (not the network) looks like the right explanation -- self-hosted stays '
  + 'the runner until that\'s proven. Workflow file lost its now-unneeded Playwright-install and '
  + 'screenshot-upload steps.\n\n'
  + 'Live confirmation is outstanding -- no QSRSoft credentials/network in this sandbox, same '
  + 'constraint #81 and #82 both hit. Suite 2141/2141, build clean, no bundle impact. Full '
  + 'writeup + owner action items: memory/dispatch-83.md.',
]};
