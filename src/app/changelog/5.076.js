// @ts-nocheck
export default {version:'5.076', date:'2026-08-20', changes:[
  'Dispatch37 -- Security build: identity-vault architecture (Direction B). Owner decided 2026-08-20 to build this BEFORE Phase 1 (memory/plan-security-pii-architecture-2026-08-19.md §4): Phase 1 is the first thing that will write new employee-attributed risk data, and it should write tokens from day one rather than plaintext names migrated later.\n\n'
  + 'Real finding that shaped every RLS/RPC decision below: CLAUDE.md\'s documented 8-tier RBAC (Developer/Admin/Owner/VP/DO/Supervisor/GM/Office) is NOT implemented at the DB level -- profiles.role\'s check constraint (supabase/schema.sql:13) allows exactly \'admin\'/\'supervisor\'/\'manager\', confirmed directly against the schema, not assumed. Every role check in this dispatch uses those three real values only.\n\n'
  + 'supabase/schema-identity-vault.sql: two new additive tables, does NOT touch audit_rows\' existing (loc,date,emp) PK or the emp column -- that stays dispatch #35\'s decision, unchanged. employee_identity_vault (id IS the token, no embedded info) has ZERO RLS policies -- no select/insert/update/delete for any role via the normal API; the only access is through two SECURITY DEFINER RPCs. identity_reveal_log is append-only evidence-grade (owner\'s 2026-08-20 decision: indefinite retention, no TTL, no update/delete policy at all -- admin-read only). audit_rows gains one new nullable emp_token column.\n\n'
  + 'get_or_create_employee_token(name) RPC -- the shared write path both the server-side auto-pull (service-role) and the browser manual-upload path (anon-key, blocked by the vault\'s own zero-select-policy RLS) call, so the lookup-or-create logic exists in exactly one place rather than a permissive insert policy reasoned about twice. reveal_employee_identity(token, reason) RPC -- the ONE path to a real name: admin/supervisor always allowed (a real, intentional divergence from the existing DO-and-above disclosure policy, scoped specifically to this mechanism, per owner decision); manager allowed only if org_config.gm_identity_reveal_enabled is true (an explicitly-flagged org-wide placeholder -- "GM\'s should optionally be able to" wasn\'t specified per-what); reason required; logs to identity_reveal_log BEFORE returning the name so a reveal is recorded even if the caller\'s client crashes immediately after.\n\n'
  + 'src/engine/identity-vault.js -- getOrCreateToken()/tokenizeRows() shared JS helper (one RPC call per DISTINCT employee name in a batch, not per row), imported by BOTH scripts/qsrsoft-register-audit-pull.mjs and src/lib/supabase.js (the established scripts/*.mjs <-> src/engine/*.js sharing convention this repo already uses for people-reports.js/lifelenz-shift-jobs.js). Both saveAuditRows() twins now populate emp_token alongside emp on every new row. loadAuditRows() maps the new column through as empToken. scripts/backfill-identity-vault.mjs -- idempotent one-time backfill for existing rows, owner-run against live Supabase (not run from this session, same as every schema-*.sql file).\n\n'
  + 'src/utils/register-audit.js\'s analyzeRegisterAudit retrofit: e.id is now e.empToken (falls back to \'Unknown\' only pre-backfill), and the returned employee objects carry NO plaintext name field anywhere -- the internal grouping key stays on the raw name (an implementation detail, never assigned onto the returned object). A caller that needs the real name calls reveal_employee_identity() with the token, deliberately, logged.\n\n'
  + '**Real, immediate consequence flagged loudly, not silently absorbed**: store-analytics.js\'s RegisterAuditNarrative panel reads .emp directly at 9 sites (narrative prose + 4 table columns + the AI-insight prompt builder) to display employee names. This dispatch\'s own scope explicitly excludes any UI/panel work ("no UI in this dispatch... future panel work"), so those 9 sites are UNCHANGED and will now render "Unknown"/"?" instead of a real name until a follow-up dispatch adds the reveal-button UI those call sites need. This is not accidental breakage -- it is the direct, anticipated interim cost of Direction B\'s own design (the "blind mode" bias-reduction property is explicitly named as a benefit in plan-security-pii-architecture-2026-08-19.md §4), and patching those 9 sites to silently re-resolve every name un-gated would defeat the entire logged-reveal architecture this dispatch just built. Recommending a fast-follow dispatch to wire a reveal button into that panel; the panel does not crash and continues to rank/flag employees correctly by token, it just can\'t show a name without one more click through a logged reveal, once that UI exists.\n\n'
  + '28 new fixture tests (identity-vault.test.js: getOrCreateToken/tokenizeRows against a mocked rpc(), including the exactly-once-per-distinct-name assertion; register-audit-identity.test.js: e.id is the token, no plaintext name field survives JSON.stringify, correct fallback/first-non-null-token-across-a-group behavior). 1630/1630 full suite passes, build clean (entry chunk 510.16 KB -> 510.40 KB gzip, still 337.7 KB under the 850 KB budget). Full writeup: memory/dispatch37-identity-vault.md.',
]};
