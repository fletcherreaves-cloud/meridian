// @ts-nocheck
export default {version:'5.071', date:'2026-08-19', changes:[
  'Dispatch30 -- Workstream D follow-up: two hand conversions + the panel contract. Workstream E (routing vs modals) unblocked D, but shipped TWO shells, not the one dispatch26 expected -- ModalShell.js now exports RoutePanelShell alongside ModalShell, on purpose (a route replaces the view, a modal overlays it). Re-measured adoption fresh: 0 movement since dispatch26 (ModalShell 9/56, LocationSelector/ActionMenus 1/56, DateRangeControl 0/56) -- and labor-allocation.js, merged the SAME session as the re-measurement, proved why: a complete hand-rolled modal branch instead of ModalShell.\n\n'
  + 'Two hand conversions, per dispatch26/30\'s own instruction. labor-allocation.js (the awkward one): its dead-but-real !embedded standalone-modal branch hand-rolled a backdrop/card/close button duplicating ModalShell exactly (hardcoded zIndex:460 instead of the shared Z.modal tier) -- converted to ModalShell, with body/tabBar/footerNote now shared between the embedded and modal paths instead of duplicated JSX. report-subscriptions.js (the simple one): hand-rolled backdrop -> ModalShell; hand-rolled All/OK/FL toggle + patch/store <select>s -> LocationSelector (mode:full), with two small pure translation functions keeping the persisted scope string shape unchanged so existing saved subscriptions still work. Its period picker (mtd/lastweek/lastmonth) was deliberately NOT converted to DateRangeControl -- it\'s period-anchored, dispatch26\'s own third date-mode row, and a day-count preset would misrepresent "Month-to-date."\n\n'
  + 'Panel contract shipped (memory/panel-contract.md, dispatch26\'s step 5 deliverable): shell (name both ModalShell/RoutePanelShell, don\'t unify), date mode (the three-way table, now with a real period-anchored example), scope (LocationSelector owns the UI, a panel with its own persisted shape translates at the edges), actions + empty-state (confirmed already mostly satisfied, not changed).\n\n'
  + 'New ratchet R7 (ratchet-modal-backdrop-bypass.test.js): hand-rolled position:fixed/inset:0/rgba(0,0,0 backdrops -- the exact pattern ModalShell replaces -- in src/views + src/features. Seeded at 78, measured fresh on this branch AFTER both conversions (confirmed neither converted file appears in the hit list any more), not copied from either dispatch\'s own text, per the standing re-measure-fresh rule. Bidirectional, same shape as R1-R4.\n\n'
  + 'Scope discipline: no 56-panel sweep, exactly two conversions. DateRangeControl/LocationSelector adoption counts stay un-ratcheted (only the bypass is, for now). labor-allocation.js\'s fixed 90-day window was NOT widened into a DateRangeControl (not asked for, risks scope creep into a panel reproducing one specific proven window) -- flagged as an open question in panel-contract.md, not silently decided. Full writeup: memory/dispatch30-workstream-d-followup.md.',
]};
