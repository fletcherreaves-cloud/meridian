// @ts-nocheck
export default {version:'5.229', date:'2026-08-28', changes:[
  'Dispatch #191 -- merge the "Calendar" panel (calendar-manager) into "Events & Tags" (events), '
  + 'owner-approved 2026-08-10 (memory/decisions-panel-inventory-2026-08-10.md) and re-confirmed '
  + 'directly 2026-08-28 after dispatch #54\'s "four sidebar links" framing was flagged as an '
  + 'apparent reversal -- the owner: "not sure I remember saying that. They can merge. It makes '
  + 'sense." That framing was this session\'s own inference from an ambiguous grouping answer, not '
  + 'a deliberate reversal of the merge decision (full context: memory/dispatch-191.md). Event '
  + 'Impact (event-impact) is untouched -- a separate panel, never named as a merge target.\n\n'
  + 'Harvest, not rewrite: CalendarManagerPanel (features/calendar.js -- month grid, recurring '
  + 'rules, AI-search/bulk-import pending-review queue, retail-events generator, print) and '
  + 'EventCalendar (views/store-dash.js -- searchable/filterable/sortable event list, inline edit, '
  + 'holiday auto-tag, CSV/print export) are both unchanged, byte-for-byte, apart from one new '
  + 'optional `viewToggle` prop each (rendered in their existing header row, no-op when absent -- '
  + 'so EventCalendar rendered standalone, e.g. dispatch-122-events-calendar.test.js, is untouched '
  + 'behaviorally). A new thin wrapper, EventsAndTagsPanel (App.js), owns a lifted `mode` (\'list\' '
  + '| \'calendar\') and switches between the two, passing each a small List/Calendar toggle button '
  + 'for the other\'s header -- same "one shell, mode tabs swap the body" shape as '
  + 'EOMDashboardPanel\'s eom/progress/scoreboard modes (eom-dashboard.js), not a new pattern.\n\n'
  + 'State: showCalendarManager retired; showEvents now gates the merged panel, with a new '
  + 'eventsMode (\'list\'|\'calendar\') sibling state, same shape as the existing planningTab/'
  + 'schedTab/forecastReportsTab hub-tab states. calInitScope (pre-scoping from a saved report '
  + 'launch) is reused as-is -- ReportSubscriptions\' \'calendar\' report now sets eventsMode(\'calendar\') '
  + '+ showEvents(true) instead of the retired showCalendarManager(true).\n\n'
  + 'Deep link: measured, not assumed, before "redirecting" it -- neither `?panel=calendar-manager` '
  + '(routing.js\'s parseRoute fails safe to null for any non-route:true id; calendar-manager was '
  + 'never route:true) nor `?modal=calendar-manager` (onOpenModal\'s `modal` argument is only ever '
  + 'the id a nav click passes in-app; nothing in the app reads a `modal` URL param on load) was '
  + 'ever a live URL deep link. So there was nothing to break -- but the internal dispatch id is '
  + 'still honored defensively: calendar-manager stays registered in panel-registry.js as '
  + 'kind:\'internal\' (no sidebar link, same treatment as proj-brief/smart-targets/store-kb/'
  + 'targets), and onOpenModal(\'calendar-manager\') now opens Events & Tags straight into Calendar '
  + 'mode rather than doing nothing, in case any stray call site or bookmark still uses it.\n\n'
  + 'panel-registry.js\'s Planning-cluster comment ("four sidebar links: Planning hub \xb7 Calendar '
  + '\xb7 Events & Tags \xb7 Event Impact") corrected to the new three-link reality in this same PR, '
  + 'per CLAUDE.md\'s own "a rule/comment that describes code which no longer exists costs more '
  + 'than no rule" -- both the panel-registry.js declaration comment and shell-nav-snapshot.test.js\'s '
  + 'header history were updated together so neither drifts from the other. (Was "Planning hub, '
  + 'Calendar, Events & Tags, Event Impact"; now "Planning hub, Events & Tags, Event Impact".)\n\n'
  + 'Panel-contract opportunistic check (Events & Tags): both source panels already use ModalShell-'
  + 'equivalent hand-rolled fixed-overlay chrome consistently with each other (pre-existing, not a '
  + 'new gap this dispatch introduces) and both already have working print/export (EventCalendar\'s '
  + 'ExportDropdown, CalendarManagerPanel\'s multi-month print). Converting either to route:true '
  + 'was considered and deliberately skipped -- panel-registry.test.js\'s route:true set is a hard '
  + 'ratchet (15 ids) that would need its own new App.js goRoute/routePanel/Escape/anyModalOpen '
  + 'wiring, a real routing change out of proportion to this dispatch\'s "don\'t meaningfully widen '
  + 'blast radius" guard; left as a future opportunistic candidate, not done here.\n\n'
  + 'Tests: shell-nav-snapshot.test.js\'s captured nav snapshot, its four-link Planning slice '
  + 'assertion, and its analytics.dashboard permission-hidden-set all updated to the new three-link '
  + 'reality (\'Calendar\' and its icon no longer render in the sidebar at all). panel-registry.test.js\'s '
  + 'dispatch<->registry pairing tests pass unchanged -- calendar-manager keeps a registry entry '
  + '(kind:\'internal\') specifically so its retained onOpenModal branch still satisfies that '
  + 'contract. Full suite: 3059/3059 (294 files) -- 17 unrelated PerformanceReviewsPanel/'
  + 'ToleranceRollupTile timeouts seen under full-parallelism load traced to this sandbox\'s '
  + 'environment (reproduced identically on a clean origin/main checkout, unrelated to this diff) '
  + 'and confirmed passing both in isolation and with --testTimeout=20000 across the whole suite. '
  + 'Build clean; eager total 553.37 KB, 296.63 KB under the 850 KB budget (both merged panels are '
  + 'pre-existing static/lazy imports already, no new client-loaded code added).',
]};
