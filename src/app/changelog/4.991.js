// @ts-nocheck
export default {version:'4.991', date:'2026-08-12', changes:[
  'Issue #219 — calibrate the Patch Heatmap\'s badAt bands (measured, decided, shipped). First production look flagged 18 critical / 8 watch / 1 clean — 26 of 27 stores — the same day the rest of the dashboard read 25 of 27 as trusted-healthy; those can\'t both be true. Root cause was TWO layered problems, not one: the original badAt constants (Sales 15, FOB 3, Labor 3, Speed 20) were chosen when #201 shipped, not derived from a distribution; and separately, a structural fact the measurement script itself couldn\'t flag — band=100*(1-gap/badAt) with clean>=80/watch>=50 means WATCH fires at 0.2*badAt and CRITICAL fires at 0.5*badAt, not at badAt itself, so a raw p90-of-over-target reading has to be DOUBLED before it\'s usable as badAt. Owner ran scripts/measure-patch-heatmap-bands.mjs against live production and supplied the corrected values: Sales 15->27, FOB 3->1.9 (tighter — was genuinely loose), Labor 3->8.8, Speed 20->73 (~3.7x too tight, did most of the original damage). Controls (ctrlScore) stays uncalibrated by design — a composite score, not a raw gap-to-target, correctly out of scope for this script. Expected result on the measured distribution: ~6 critical / 14 watch / 7 clean, vs the original 18/8/1 — not independently re-verified live in this sandbox (no browser/Supabase session here); if the live grid doesn\'t land near that split, that\'s a real finding to raise, not something to nudge quietly. 9 new tests (src/__tests__/patch-heatmap-bands.test.js) lock in both the corrected band/badAt relationship AND the four new constants together, each anchored exactly at its dimension\'s new critical boundary (gap = 0.5*badAt) so a regression in either the formula or a constant fails independently. #220 (patch rollup tiles), blocked behind this landing, is now unblocked. Full findings: memory/project-patch-heatmap-calibration-219.md.',
]};
