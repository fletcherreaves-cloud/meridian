// @ts-nocheck
export default {version:'5.163', date:'2026-08-25', changes:[
  'Dispatch #120: Security panel -- mobile location selector + findings-table readability.\n\n'
  + 'Location scope: replaced the hand-rolled State/Org/Store pill rows (~30 pills flat, wrapped -- '
  + 'confirmed unusable on a 390x844 mobile viewport by the owner\'s own screenshot) with the shared '
  + '`LocationSelector` in `mode:\'progressive\'` (dispatch #104) -- States show first, picking a '
  + 'State reveals its Patches, picking a Patch reveals its Stores. Org tier removed, not kept '
  + 'alongside: investigated and confirmed genuinely redundant with State for this business '
  + '(CLAUDE.md\'s own canonical mapping, MCDOK=Oklahoma/Emerald Arches=Florida, is 1:1), regression-'
  + 'tested against the whole real ~27-store INV_ORG_COORDS estate rather than assumed. Replaced '
  + 'with `LocationSelector`\'s real Patch tier (org.sup, the supervisor field) -- a genuinely finer '
  + 'scope than Org ever was: two stores in the same state/org can sit under different supervisors '
  + '(e.g. 3708 and 5183, both OK/mcdok, sup Robert Spencer vs. Krystiana Langford), which Org could '
  + 'never isolate and Patch now can. `scope` keeps this panel\'s own pre-existing {level,value} '
  + 'shape (in-memory only, nothing persisted) -- `scopeToSelectorValue`/`selectorValueToScope` '
  + 'translate at the render boundary only, per panel-contract.md §3. `scopeMatches` updated to '
  + 'match (state/patch/store), `org` level removed.\n\n'
  + 'Findings readability: investigated a click-through to an "actual event" first, per the owner\'s '
  + 'primary ask. The only candidate, `RegisterAuditTab` (store-analytics.js), does NOT qualify -- it '
  + 'reads a completely separate, manually-uploaded dataset (`ds.auditRows`), has no date-range prop '
  + '(filters only by store, the store\'s entire uploaded history), and is nested in StoreDash\'s tab '
  + 'switcher rather than route-addressable with a store+window to pre-scope to. Wiring a "go to '
  + 'event" link there would silently jump to an unrelated, possibly-empty dataset. Event-level '
  + 'detail already exists in-panel for cash findings (`SubjectEvents`/`loadQsrSecurityEventsForSubject`) '
  + '-- that already is the real "actual event" view. So took the owner\'s own explicitly-offered '
  + 'fallback: converted the findings list from an expandable div-card list to real `<table>`/`<th>` '
  + 'markup, sortable by Signals/Subject/Rule/Window (click a header to sort/toggle direction); the '
  + 'default view is a verified no-op that renders identically to the pre-table card order.\n\n'
  + 'Date-range control (dispatch #100, `DateRangeControl`) left untouched per the dispatch\'s own '
  + 'confirmation that it already does what the owner asked (display-only filter on `windowEnd`, '
  + 'never re-scoping `loadSecurityFindings`) -- added a regression test asserting the fetch effect '
  + 'never re-fires on a date-range change, on top of the existing behavioral coverage.\n\n'
  + 'Panel-contract close-button check (standing rule): SecurityPanel already renders inside '
  + '`ModalShell` at its App.js call site, which owns the close (x) button -- no change needed.\n\n'
  + 'The new findings table also needed the #192 scroll fix (`width:\'max-content\', minWidth:\'100%\'` '
  + 'instead of `width:\'100%\'` inside its `overflowX:\'auto\'` wrapper) to satisfy '
  + '`scroll-table-width.test.js`\'s ratchet.\n\n'
  + '22 new/rewritten tests in src/__tests__/security-panel.test.js (pure scopeMatches/translation/'
  + 'sort-helper coverage, a full-estate Org-removal-is-a-no-op regression check, real-UI render '
  + 'tests for the progressive selector and the sortable table, a 27-store/390x844-viewport '
  + 'structural check, and a date-range-never-refetches assertion); full suite 2437/2437 passing '
  + '(235 files, up from main\'s 2393); build clean, entry gzip unchanged at ~457.83 KB, eager-'
  + 'payload 528.97 KB (budget 850 KB).',
]};
