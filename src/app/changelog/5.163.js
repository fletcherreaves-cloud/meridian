// @ts-nocheck
export default {version:'5.163', date:'2026-08-25', changes:[
  'Dispatch #122: Events & Tags -- holiday sub-filter + full-list print.\n\n'
  + 'Two scoped changes to `EventCalendar` (src/views/store-dash.js, the "Events & Tags" panel -- '
  + 'not event-impact.js\'s separate "Event Impact Registry", untouched).\n\n'
  + 'Item 1 (holiday sub-filter): the existing Holiday type filter pooled every holiday-tagged day '
  + '(New Year\'s, Independence Day, Thanksgiving, ...) into one undifferentiated list. Each holiday '
  + 'already carries a stable name -- buildHolidays() (src/utils/holidays.js) assigns a fixed `label` '
  + 'per holiday, and the panel\'s own "Auto-Tag Holidays" flow writes it straight into the tagged '
  + 'event\'s `note` field. So selecting the Holiday type now reveals a second selector listing the '
  + 'distinct holiday names present (derived from `note`, counted, alphabetized); picking one narrows '
  + 'the results to just that holiday. Resets to "All Holidays" whenever the top-level type filter '
  + 'changes, so a stale narrow selection can\'t silently carry over into an unrelated type. Verified '
  + '(grep) that `holiday_major`, computed in the auto-tag loop\'s `evType` local, is never actually '
  + 'assigned to any event -- every auto-tagged holiday is written as `type:\'holiday\'` regardless of '
  + 'impact -- so there is exactly one real holiday type to sub-filter, not two.\n\n'
  + 'Item 2 (full-list print): this panel had zero print functionality before (grepped, zero '
  + '`window.print`/`print` hits) -- and its results render inside a `flex:1, overflowY:\'auto\'` '
  + 'container capped by the modal\'s own `maxHeight:\'92vh\'`, so a native browser print would only '
  + 'have captured whatever was currently scrolled into view. Added the panel\'s own '
  + '`ExportDropdown` (the same component RankingView and others already use in this exact file) to '
  + 'the header, feeding it `rows` built straight from the `filtered` array -- so its "HTML Report / '
  + 'Print" output (plus CSV/JSON, free from the same component) always contains the FULL current '
  + 'result set, after both the type filter and, when applicable, the holiday sub-filter, never a '
  + 'DOM-scroll-truncated subset. Chose this over a `@media print` unscroll because it is this '
  + 'repo\'s already-established print pattern for exactly this "full filtered list, not the '
  + 'scrolled viewport" problem, and needed zero new plumbing beyond a `rows` prop.\n\n'
  + '5 new render-based tests (src/__tests__/dispatch-122-events-calendar.test.js) against a real, '
  + 'multi-year/multi-holiday/multi-store 29-event fixture shaped exactly like the panel\'s own '
  + 'auto-tag output: the second selector appears only for the Holiday type and lists the correct '
  + 'distinct names + counts; picking one narrows the rendered list; the selection resets on a type '
  + 'change and back; the print path\'s generated HTML table row count exactly matches the current '
  + '`filtered.length` (29 unfiltered, 9 narrowed to one holiday) rather than a scroll-visible '
  + 'subset. Confirmed regression-catching by reverting the source change under the test unchanged: '
  + '4 of 5 fail against pre-fix code. Full suite 2420/2420 passing (236 files, up from 2415/235 on '
  + 'main); build clean, eager-payload budget unchanged (528.98 / 850 KB).',
]};
