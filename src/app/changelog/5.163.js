// @ts-nocheck
export default {version:'5.163', date:'2026-08-25', changes:[
  'Dispatch #124: Actual punch-times pull (QSRSoft, PII-safe) -- backend pull + table only, no '
  + 'panel/UI (that\'s a later, separate dispatch once #123\'s Crew Schedule panel exists).\n\n'
  + 'New `scripts/qsrsoft-punch-times-pull.mjs` hits `GET /reporting/v2/people/time-punches-matched` '
  + '(one store per call, per the finding\'s documented shape -- loops all 27), writing to a new '
  + '`qsr_punch_times` table (tenant_id + accessible_locs-scoped RLS, matching qsr_employee_tenure\'s '
  + 'own pattern). Two-path auth (getFreshToken direct, Playwright fallback) matching every other '
  + 'QSRSoft pull in this repo; watched in sync-failure-watch.yml; manual-upload fallback considered '
  + 'and skipped (closer to Register Audit than to a report upload -- nobody re-keys clock punches '
  + 'from a spreadsheet).\n\n'
  + 'PII safety (the actual point of this dispatch): SELECT_COLS is a hard-coded allowlist -- geid, '
  + 'storeNum, punchType, isPaidBreak, start/endDateTime, in/outModified, jobTitleCode, badgeType -- '
  + 'never ssn, never a name field (this endpoint\'s own capture proved a name field exists '
  + 'alongside ssn; the denylist catches it explicitly). assertNoDeniedSelectCols() runs at import '
  + 'time and is exercised by a real test that adds ssn back and asserts the throw (not just a '
  + 'comment). No ssn, name, or timeCardNumber (documented unreliable) is ever requested, logged, '
  + 'or stored.\n\n'
  + 'Identity resolution (dispatch\'s rule 5), measured live, not guessed: queried '
  + 'qsr_employee_tenure.geid against audit_rows.emp_id via the service-role credential (1000 rows '
  + 'each) -- digit-length band ranges match almost exactly across 6/7/8/9-digit values, and '
  + 'audit_rows\' own \'0\' sentinel showed up as the documented placeholder, not a contradiction. '
  + 'CONFIRMS geid and audit_rows.emp_id are the same identifier space, on far broader data than the '
  + 'finding\'s one-store-one-day sample. That is NOT the same space as dispatch #123\'s emp_token '
  + '(name-keyed via get_or_create_employee_token) -- so this pull does not tokenize geid directly. '
  + 'Instead: geid -> qsr_employee_tenure.(loc,geid) -> full_employee_name -> getOrCreateToken() -> '
  + 'emp_token, landing in the SAME name-keyed vault space #123 will use. emp_token is nullable '
  + '(no tenure row for a separated/never-active-rostered geid); geid itself is never null and is '
  + 'the reliable fallback join key either way. Known open caveat, stated plainly: name-string parity '
  + 'between QSRSoft and LifeLenz has not been verified (#123 doesn\'t exist in this repo yet) -- '
  + 'flagged for whoever builds the eventual punch-wiring dispatch.\n\n'
  + 'Business-day boundary: NOT confirmed (no compType param on this endpoint, and this sandbox had '
  + 'no live QSRSoft credentials to check real punch timestamps). Deliberately NOT guessed either '
  + 'way -- the table stores raw start/end timestamps with no derived `dt`/business-day column, so '
  + 'nothing downstream inherits an unconfirmed assumption; a future consumer applies businessDate() '
  + '(src/utils/date.js) explicitly if/when the boundary matters.\n\n'
  + '29 new tests (src/__tests__/qsrsoft-punch-times-pull.test.js) covering mapRow field mapping, '
  + 'extractRows envelope handling, the assert-guard\'s actual throw behavior (ssn + name-field '
  + 'variants + protected-class fields, case-insensitive), and resolveEmpTokens() (batched-by-name '
  + 'RPC calls, null-token fallback, error tolerance) against a mocked supabase client. Full suite '
  + '2444/2444 passing (236 files, up from 2415/235 on main); build clean, no bundle impact '
  + '(backend-only -- eager-payload unchanged at 528.99 KB / budget 850 KB).',
]};
