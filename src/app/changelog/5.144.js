// @ts-nocheck
export default {version:'5.144', date:'2026-08-24', changes:[
  'Dispatch #103: Record Day Intelligence -- same-day completeness gate + 2-decimal $/% '
  + 'formatting for this panel.\n\n'
  + 'Root cause (measured, not assumed): computeRecords\' per-day scan flagged a "New Record" '
  + 'the instant ANY day\'s value beat the running best -- including TODAY, while the DAR '
  + 'intraday pull (~8a/10a/2p CT) is still landing the day\'s later hours. Reproduced exactly '
  + 'for Tecumseh (33704) 2026-08-24: the app\'s own formula against the live rows at check time '
  + 'gave 95s OEPE, correct math over a day whose data only covered through 15:00 -- the '
  + 'owner\'s own later QSRSoft export had two more hours (16:00, 17:00), both slower (141s, '
  + '101s), that were never in Meridian\'s number.\n\n'
  + 'Fix: a same-day completeness gate, ONE shared mechanism (tryRecord/flagRecent) applied '
  + 'uniformly to every record type in the file -- Sales/GC/Breakfast/Avg-Check Day and their '
  + 'DOW variants, plus OEPE/KVS/R2P -- not six separate patches. `businessDate()` from '
  + 'utils/date.js (the existing shared 4am-cutover helper -- never re-derived inline, per this '
  + 'repo\'s standing rule) marks today\'s still-open McDonald\'s business day. A beat on today\'s '
  + 'value is never silently hidden and never silently committed either: it still computes and '
  + 'still shows in Recent Breaks, but as a visibly separate PROVISIONAL entry (new Status '
  + 'column, distinct amber "⏳ Provisional -- still accumulating" badge + tinted row + '
  + 'tooltip, vs. a green "✓ Confirmed" badge for a closed day) -- default (b) from the '
  + 'dispatch, since silently dropping today\'s number is a bigger behavior change than a '
  + 'caveat label. Critically, a provisional beat never advances the per-store running best-so-'
  + 'far or writes the permanent rec.* entry that HeroGrid/SalesVolumeTab/SpeedTab/DOWTab read '
  + 'and that mergeStores/saveMerged persist to localStorage FOREVER -- so a fast-but-partial '
  + 'reading can never get baked in as "the" all-time record before the day even closes. Weekly/'
  + 'monthly aggregate records (a separate loop, not named in the dispatch\'s own code anchors) '
  + 'were left alone -- out of scope for this pass.\n\n'
  + 'Second item: 2-decimal formatting, scoped to this file only. Added a local f$2() (2-decimal '
  + '$, `minimumFractionDigits:2, maximumFractionDigits:2`) and replaced all 13 f$( ) call sites '
  + 'in record-day.js with it; f$ itself (utils/fmt.js, 0-decimal default) is untouched -- '
  + 'confirmed via `git diff --stat` showing only record-day.js changed, so every other f$/fPct/'
  + 'fP consumer (App.js, backtest.js, forecast.js, projections.js, analytics.js, at-a-glance.js, '
  + 'one-pager.js, sage.js, store-analytics.js, store-dash.js) is byte-for-byte unchanged. The '
  + 'file\'s one percent render (Recent Breaks\' "Change" column) already used .toFixed(2); no '
  + 'fPct/fP calls existed in this file to audit.\n\n'
  + 'Verified through the REAL RecordDayPanel/RecentBreakersTab consumer, not an isolated '
  + 'helper (dispatch-103-record-day-provisional.test.js, 4 new render tests, system clock '
  + 'faked via vi.setSystemTime -- di-compare-week-anchor.test.js\'s pattern): (1) a fixture '
  + 'with an old best (97s, pre-window), a genuinely CLOSED day that legitimately breaks it '
  + '(90s), and TODAY at a faster-but-partial 85s renders 90s as Confirmed and 85s as visibly '
  + 'Provisional in Recent Breaks; (2) the Speed tab\'s permanent all-time record shows 90s, '
  + 'never 85s; (3) advancing the clock a day later with 08-24 settling at a slower 101s (the '
  + 'Tecumseh dispatch\'s own prediction) proves 90s stays the permanent record and 101s never '
  + 'displaces it, i.e. the once-provisional day is graded normally once closed with no lasting '
  + 'trace; (4) every dollar token rendered in the Sales & Volume tab carries exactly 2 decimal '
  + 'digits. Full suite 2303/2303 (measured 2265/2265 with this same fix pre-rebase; the extra '
  + '38 tests came from 4 unrelated PRs merged into main in the interim -- #100 Security '
  + 'selectors, #101 Forms per-occurrence detail, #102 FOB inflation fix, #104 Top/Bottom '
  + 'Performers -- rebased onto cleanly, no conflicts). Build clean. '
  + 'Entry chunk 519.66 KB -> 519.95 KB gzip (+0.29 KB); eager total 521.52 KB -> 521.81 KB gzip '
  + '(+0.29 KB, budget 850 KB, headroom 328.19 KB) -- record-day.js is statically imported by '
  + 'App.js (not lazyPanel()\'d), a pre-existing condition unrelated to this fix, so its small '
  + 'growth lands directly in the entry chunk rather than a separate lazy one.\n\n'
  + 'Tecumseh\'s actual 2026-08-24 trading day had not yet closed at the time this shipped, so '
  + 'the dispatch\'s own suggested live re-check (confirming the real "record" claim once the '
  + 'day fully closes) is a follow-up, not verifiable from this session -- flagged, not silently '
  + 'skipped.',
]};
