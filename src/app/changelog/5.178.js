// @ts-nocheck
export default {version:'5.178', date:'2026-08-25', changes:[
  'Dispatch #141 — Training Retention: Patch / Operator / Org / State rollup ("who is driving '
  + 'this"). Owner: "If possible do a patch and operator/org/state rollup report as well. It '
  + 'would be interesting to see who is driving this."\n\n'
  + 'BLOCKING PREREQUISITE FIXED FIRST, per the dispatch\'s own instruction: schedule-retention.js\'s '
  + '"workshop week" mark was localStorage-only (mf_sched_retention_mark) -- per-device, invisible '
  + 'across sessions, which would have made a cross-store rollup silently wrong (a store marked '
  + 'from a different device reads as "no workshop week"). New Supabase table sched_retention_marks '
  + '(supabase/schema-dispatch-141-retention-marks.sql, one row per store, tenant+accessible_locs '
  + 'RLS mirroring schema-yearly-targets.sql) is now the source of truth; src/lib/supabase.js gained '
  + 'loadRetentionMarks/saveRetentionMark/deleteRetentionMark. ScheduleRetentionSection\'s markWeek() '
  + 'now writes through to Supabase, keeping localStorage only as a same-session instant-paint cache '
  + '(never authoritative once the cloud read resolves) -- same missing-table degrade shape as #142 '
  + '(logs a clear message, falls back to [], never throws). ⚠️ OWNER ACTION REQUIRED: run '
  + 'supabase/schema-dispatch-141-retention-marks.sql in the Supabase SQL editor for cloud marks to '
  + 'persist -- measured LIVE against production (curl, 2026-08-25): the table does not exist yet '
  + '(PostgREST code PGRST205, NOT the classic 42P01 every other missing-table check here matches on '
  + '-- broadened the check for these three functions specifically). Until that migration runs, the '
  + 'per-store report still works exactly as before via its localStorage fast-path; only the new '
  + 'rollup (which reads the cloud table exclusively, by design) shows every store as unmarked.\n\n'
  + 'NEW "Retention Rollup" hub tab, alongside (not replacing) Training Retention -- dispatch #140\'s '
  + 'own broader-than-store empty state now names this tab directly instead of a placeholder. For '
  + 'every store in the selected LocationSelector scope with a USABLE mark (resolves to a real '
  + 'computed week -- a stale/renamed mark is excluded with reason "mark-not-found", never silently '
  + 'midpoint-split), reuses computeStoreWeeks/splitWeeksAtMark/aggregateSpan VERBATIM (no re-derived '
  + 'math) to get that store\'s own pre/post weeks, then buckets by the chosen grouping dimension and '
  + 'concatenates each group\'s pre-weeks (and, separately, post-weeks) into one combined list before '
  + 'calling aggregateSpan() on each -- the same dollar-weighted, ratio-of-aggregates rule '
  + 'aggregateSpan/computeScheduleSummary\'s district rollup already use, one level up across stores '
  + 'instead of within one store\'s weeks. A group\'s delta is its combined POST aggregate minus its '
  + 'combined PRE aggregate -- never a straight average of each store\'s own delta. Leaderboard table '
  + '(Labor % before->since + Δ, Sched-vs-Fcst Hrs/Wk Δ, TPMH Δ, plain-language verdict), ranked '
  + 'most-improved-first, with excluded stores called out by name and reason so a partial rollup never '
  + 'reads as complete.\n\n'
  + 'Grouping dimensions, each checked (not assumed) per the dispatch: PATCH resolves via the LIVE '
  + 'supervisorOf() (dispatch #139\'s fix -- a reassigned store shows under its current patch, not '
  + 'the static invOrgCoords[loc].sup seed). ORG (getStoreOrg) / STATE (invOrgCoords[loc].state) are '
  + 'stable per CLAUDE.md, read directly. OPERATOR -- measured, not assumed either way: grep found a '
  + 'real live/Settings-editable operator map (management.js\'s "Operator Groups" tab, '
  + 'settings.operators, already consumed by scheduling.js/one-pager.js/store-dash.js), the SAME '
  + 'shape supervisorGroups was BEFORE #139\'s fix -- but it has NOT received #139\'s specific '
  + 'treatment (no effective-dated whoRan()-style timeline, no single-store live-lookup helper). '
  + 'operatorGroupOf() resolves settings.operators live first, falling back to invOrgCoords[loc].op '
  + 'only for a store the settings map doesn\'t cover -- the same fallback SHAPE as supervisorOf(), '
  + 'without overstating what it verifies.\n\n'
  + '17 new tests (dispatch-141-retention-rollup.test.js): per-store split + all three exclude '
  + 'reasons, dollar-weighted group aggregation against a hand-computed figure, cross-group ranking, '
  + 'each grouping dimension\'s live-vs-static behavior (including a fabricated Settings override '
  + 'proving operatorGroupOf reads live data, not the static seed), and real hub-tab render tests '
  + '(panel-registry shape, tab-bar click-through, initialTab deep-link, graceful-degrade with no '
  + 'Supabase table). Full suite 2645/2645 passing (248 files, up from 2628/247 on main -- +17, all '
  + 'in the new file); build clean; entry gzip 468.37 KB (467.95 KB on main, +0.42 KB), eager payload '
  + '539.55/850 KB gzip (539.13 KB on main, +0.42 KB) -- both new sections are lazy-loaded inside the '
  + 'existing schedule-retention chunk (24.23 KB / gzip 8.17 KB total), not the entry bundle.',
]};
