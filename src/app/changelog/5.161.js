// @ts-nocheck
export default {version:'5.161', date:'2026-08-25', changes:[
  'Dispatch #119: mobile horizontal-scroll fix (Promo/Discount ROI) + a ratchet against the '
  + 'anti-pattern district-wide.\n\n'
  + 'Owner-reported, verbatim: "Throughout environment > need to add side scroll to be able to '
  + 'see all data on mobile. > Reference Promo Discount ROI." promo-roi.js\'s results-table scroll '
  + 'container was `{overflow:\'hidden\', maxHeight:300, overflowY:\'auto\'}` wrapping a '
  + '`<table style="width:\'100%\'">` -- the `overflow` SHORTHAND sets BOTH axes to hidden, and '
  + 'only overflowY was overridden, so the horizontal axis stayed hidden. On a narrow viewport, '
  + 'columns that didn\'t fit were clipped with no scrollbar and no way to reach them. Fixed by '
  + 'adding `overflowX:\'auto\'` alongside the existing `overflowY:\'auto\'`, and letting the '
  + 'table size to its content (`width:\'max-content\', minWidth:\'100%\'`) instead of forcing '
  + '`width:\'100%\'`, which had been masking that the row content needed more room than the '
  + 'container had.\n\n'
  + 'Verified against the REAL bug, not reasoned about: built a harness mounting the actual panel '
  + 'component with synthetic multi-store data at a real 390x844 mobile viewport (Playwright, the '
  + 'sandbox\'s pre-installed Chromium since cdn.playwright.dev is proxy-blocked). Pre-fix: a real '
  + 'horizontal mouse-wheel event left scrollLeft at 0 -- genuinely stuck, not just visually '
  + 'tight. Post-fix: the same wheel event moved scrollLeft to the container\'s max, and the '
  + 'previously-clipped Give-away/GP-Delta/Verdict columns became reachable. Vertical scroll '
  + '(maxHeight:300) and desktop rendering (table fills its container naturally, no stray '
  + 'scrollbar) both confirmed unchanged. Screenshots taken before/after.\n\n'
  + 'New ratchet (src/__tests__/ratchet-mobile-table-horizontal-scroll.test.js) targets the exact '
  + 'shape codebase-wide: a shorthand `overflow:\'hidden\'` style value (found via brace-matching, '
  + 'not line-regex, since these span multiple lines) with no `overflowX` override in the same '
  + 'style object, whose element has an `h(\'table\'` as a DIRECT sibling child (paren/brace/'
  + 'bracket depth-0 scan of the same call\'s argument list -- catches a table after a header div '
  + 'sibling, not just a lone first child; excludes a table nested inside another wrapping '
  + 'element). Deliberately did NOT reuse the dispatch\'s own investigation number -- its loose '
  + 'same-FILE co-occurrence grep (overflow:\'hidden\' anywhere in a file that also contains '
  + 'h(\'table\' anywhere in the same file) found 13 files and is explicitly flagged in the '
  + 'dispatch as unverified; it even missed promo-roi.js\'s own confirmed bug. Measuring the real '
  + 'detector fresh against all 243 raw overflow:\'hidden\' sites in src/views+src/features found '
  + 'the true count is 2 (both in dt-speedofservice.js -- Store Ranking and By Hour tables), not '
  + '13; every other site is unrelated to a table (badges, borders, avatar clipping, modal-shell '
  + 'cards). CEILING=2, seeded fresh on this branch AFTER the promo-roi.js fix, bidirectional '
  + '(fails if the count rises OR falls without the ceiling being lowered) -- both directions '
  + 'hand-verified by temporarily bumping/lowering CEILING and confirming the expected failure '
  + 'message, then restoring it. Also carries 5 fixture-based self-tests (including the real '
  + 'pre-fix promo-roi.js line, verified to score exactly 1 hit against the actual pre-fix commit) '
  + 'so the detector\'s own correctness doesn\'t depend on the live tree ever containing an '
  + 'instance. The 2 dt-speedofservice.js sites are left for opportunistic fixing next time that '
  + 'panel is touched, per memory/panel-contract.md\'s standing rule and this dispatch\'s explicit '
  + '"fix Promo/Discount ROI only, do not sweep" scope.\n\n'
  + 'Suite: 231/231 files, 2390/2390 tests passing (+7 new). Build clean; promo-roi.js is a lazy '
  + 'panel, eager-payload budget unaffected.',
]};
