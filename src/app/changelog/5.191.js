// @ts-nocheck
export default {version:'5.191', date:'2026-08-26', changes:[
  'Performance Review continuity, Phase 3a (dispatch #150): data layer only -- built the '
  + 'effective-dated person/role/store "reports-to" assignment graph the plan doc\'s decision #2 '
  + 'calls for (an Area Supervisor covers a patch of stores, an Ops Manager covers 2-4 AS\'s '
  + 'patches, a District Operator covers whatever MIX of OMs/AS\'s/stores is assigned to them -- '
  + 'not a uniform fixed-depth tree). Extended the existing, previously-unused `staff_assignments` '
  + 'table (zero code references anywhere in `src/` until now) from a single store-only column to '
  + 'a general `{person, role, target_type, target, start_date}` graph, where `target` is EITHER a '
  + 'store loc code or another person\'s identity.\n\n'
  + 'New `src/engine/assignment-graph.js`: generalizes `src/constants.js`\'s '
  + 'orgAssignments()/whoRan()/groupsAt() "latest start ≤ date wins" resolution to the full graph -- '
  + '`resolveScope(person, date, rows)` (recursive union: a person\'s own directly-assigned stores '
  + 'PLUS every store in the resolved scope of everyone assigned to report to them) and its '
  + 'inverse, `whoOversees(loc, date, rows)` (walks the chain UP from a store to every person '
  + 'responsible for it). Cycle-safe: a malformed reports-to loop throws `AssignmentCycleError` '
  + 'rather than infinite-looping or silently truncating.\n\n'
  + 'New `scripts/backfill-staff-assignments-2026.mjs`: reconstructs this year\'s real assignment '
  + 'history from `qsr_employee_tenure`\'s `store_start_date`/`job_title_code_start_date` for every '
  + 'currently-active GM/DM/SM (job-code-identified) and AM (DM-coded, split by `hourly_pay_rate` '
  + '-- plan doc decision #5\'s measured rule), plus Area Supervisors seeded from the existing '
  + 'orgAssignments()/`DEF_SETTINGS.supervisorGroups`. OM/DO are left unseeded -- no one holds '
  + 'either role in production yet.\n\n'
  + '`supabase/schema.sql`: `staff_assignments` gains `person` (text -- replaces the old '
  + '`profile_id` uuid FK, since most GM/AM/DM/SM/AS people have no Meridian login at all; holds a '
  + 'geid for roster-sourced roles or a supervisor name string for AS/OM/DO, matching how '
  + 'review-engine.js\'s own `review.geid` already identifies a reviewed person), `role` (a '
  + '`DEFAULT_ROLES` ladder id -- chosen over review-engine.js\'s `ROLE_KEYS` because ROLE_KEYS has '
  + 'no DO/VP/Owner rung and this graph has to express a DO\'s own assignment rows), `target_type` '
  + '(`store`|`person`), and a generalized `target` column (renamed from `store_loc`). `end_date` '
  + 'is KEPT but NOT consulted by resolution -- matches orgAssignments()\'s own explicit '
  + '"start-only model: the next assignment implicitly ends the prior one" convention, which this '
  + 'dispatch was told to generalize, not reinvent; see the table\'s own header comment and this '
  + 'dispatch\'s PR body for the full reasoning. RLS updated: the old self-read clause '
  + '(`profile_id = auth.uid()`) is gone (no longer meaningful once `person` isn\'t a uuid) -- read '
  + 'access is admin/area_supervisor only for now, same as write. NOT applied to production yet -- '
  + 'see the PR body for the exact statements to run.\n\n'
  + 'Explicitly data-layer only, per dispatch scope: NOT wired into `reviews` RLS for '
  + 'hierarchy-scoped visibility (Phase 3b, a separate later dispatch), and NOT wired into '
  + '`canOverrideLockedActual` (dispatch #149) or any review UI/scoring, which stays '
  + 'rung-distance-only for now.',
]};
