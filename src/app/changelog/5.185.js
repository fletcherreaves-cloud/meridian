// @ts-nocheck
export default {version:'5.185', date:'2026-08-26', changes:[
  'Fix: Performance Review\'s "Delivery Wait (sec)" target was null for every store — root '
  + 'cause found from a live screenshot (owner, 2026-08-26): the yearly targets workbook\'s '
  + '"McDelivery Restaurant Wait Time" is a MERGED 2-column header. The header text lands on '
  + 'the left column, which holds a literal "<" for every row; the real mm:ss value sits one '
  + 'column to the right ("It\'s in a split column so its the one to the right of \'<\'"). '
  + '`parseYearlyTargets` was reading the "<" column itself (`parseFloat(\'<\')` = NaN), matching '
  + 'the null-for-all-27-stores result measured live against Supabase the same day. Now reads '
  + 'the value column and converts mm:ss (or h:mm:ss) to seconds via the existing `hmsToSec` '
  + 'helper (`src/engine/people-reports.js`) — matching `restaurantTimeSec`\'s (the ACTUAL side\'s) '
  + 'scale, so the two sides compare correctly.\n\n'
  + 'Same section, same bug family: "McDelivery Star Rating" values are written as ">4.5" (a '
  + 'comparison prefix in the same cell, not a split column) — `parseFloat(\'>4.5\')` also '
  + 'returned NaN. Now strips a leading "<"/">" before parsing.\n\n'
  + 'Wired in McDelivery Star Rating as a new Performance Review RGR metric (owner: "let\'s wire '
  + 'in Star Rating as well"), right after Delivery Wait. Target: real, now-fixed '
  + '`tMcdStars` -> `yearly_targets.mcd_star_rating`. Actual: no data source exists anywhere in '
  + 'the app yet, so it stays manual-entry — same state EAD/EPB2B are already in. `scored:false` '
  + '(reference-only): with zero live actuals to calibrate a threshold band against, this follows '
  + 'the same reasoning `secondSide` (2nd Side Healthy Usage) used in v5.184 rather than guessing '
  + 'a weight/threshold allocation. `kpi-registry.test.js`\'s stale guard (asserted `mcdStars` had '
  + 'no picker entry, written before this metric existed) updated to match — `KPI_REGISTRY`\'s own '
  + '"flattens every default-review metric" test already requires this, so the old assertion was '
  + 'contradicted by design, not just out of date.\n\n'
  + 'New tests in `yearly-targets-parser.test.js` reproduce the real merged-header/">"-prefix '
  + 'layout from the live screenshot so a regression (reading the "<" column again, or not '
  + 'stripping the comparison prefix) fails loudly instead of silently going null.\n\n'
  + 'Full suite 2662/2662 (250 files, +2 from this change); build clean. Entry 470.17 KB gzip '
  + '(469.95 KB prior, +0.22 KB); eager payload 541.35/850 KB gzip (541.13 KB prior, +0.22 KB).',
]};
