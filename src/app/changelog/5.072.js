// @ts-nocheck
export default {version:'5.072', date:'2026-08-19', changes:[
  'Dispatch32 -- Workstream C: pipeline contract, wired. The last of the seven normalization workstreams to actually start (dispatch25/32 both measured "2 of ~19 pull/write scripts have an assert-on-zero-rows guard," found by grepping for the exact inline shape in qsrsoft-pmix-pull.mjs:444-447).\n\n'
  + 'Correction, made before writing anything (checked first, per CLAUDE.md\'s "check whether a helper exists before writing one"): that grep missed scripts/lib/pull-outcome.mjs (makeOutcomeTracker) -- a separate, already-built, already-shared module implementing assert-on-zero-rows PLUS a failure-rate threshold. Real adoption is ~40% (8/20 scripts, including two of the three highest-stakes daily pulls), not ~10% (2/19). Not building a duplicate assert function -- that would be the exact second-copy-drifts-from-the-first problem the standing rule exists to prevent.\n\n'
  + 'What was still genuinely missing everywhere, confirmed via repo-wide grep including inside the 8 already-governed scripts: unconditional per-partition SUCCESS coverage logging (pull-outcome.mjs only logs failures) and a freshness SLA checker (nothing implements this anywhere -- the piece that would catch a silent staleness like the CLAUDE.md-cited 6-day LifeLenz outage at the SOURCE, before the separate GitHub-Actions-run-level watch would ever notice).\n\n'
  + 'New scripts/_pipeline-contract.mjs: logPartitionCoverage() (generalizes pmix\'s own N/27-stores pattern to any partition kind) + checkFreshness() (warn/error SLA against an injectable now, no process.exit -- callers decide, same posture as withRetry/pull-outcome.finalize). Wired into lifelenz-pull.mjs (freshness thresholds tuned to catch the exact 6-day-outage class -- warnAfterHours=30/errorAfterHours=54 -- plus per-schedule coverage) and qsrsoft-dar-pull.mjs (same thresholds, per-store coverage threaded through both auth paths) -- chosen over the fully-ungoverned scripts specifically to prove the two NEW pieces on the highest-stakes daily pulls, not the lowest-risk ones.\n\n'
  + 'New ratchet R8 (ratchet-pipeline-contract-coverage.test.js): counts named pull/write scripts not yet importing _pipeline-contract.mjs, seeded at 18 (20 total minus the 2 just converted), measured fresh on this branch. 13 new tests (_pipeline-contract.mjs + R8). 1584/1584 pass; build unaffected (scripts/ isn\'t bundled).\n\n'
  + 'Explicitly deferred, not silently dropped: C2 (idempotent partition-replace on the write side, delete-then-insert per date, paced) -- greenfield, tracked under #336, kept out of this PR per both dispatches\' own "bounded slice, not a sweep" guidance. The other 18 named scripts still missing the contract (R8 tracks the remaining rollout). qsrsoft-pmix-pull.mjs\'s own working inline coverage-logger was left as-is rather than migrated to the new shared helper for zero behavior change. Full writeup: memory/dispatch32-pipeline-contract.md.',
]};
