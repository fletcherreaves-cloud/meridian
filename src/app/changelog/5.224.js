// @ts-nocheck
export default {version:'5.224', date:'2026-08-28', changes:[
  'Dispatch #183 -- chased the store-clustered emp/mgr-meal gap dispatch #181 left open '
  + '(memory/finding-emp-mgr-meal-reconciliation-2026-08-28.md). Root cause found and FIXED, '
  + 'Meridian-side: audit_rows is one row per (loc, date, EMPLOYEE, register_type) since #59\'s '
  + 'grain change, but metric-source.js\'s empMealAmt/mgrMealAmt/mgrMealCnt chains were treating '
  + 'it like every other source in that chain (one row per (loc, date)) -- returning the first '
  + 'row with a value as if it were the whole store\'s daily total. Two compounding bugs: (1) that '
  + 'row could be a Manager- or Preparer-type row, and live measurement showed Manager-type and '
  + 'Preparer-type register-audit API calls return IDENTICAL meal $ totals to each other '
  + '(redistributed across different employee names, not real incremental register activity) -- '
  + 'a QSRSoft report-shape quirk, not a Meridian bug in itself, but (2) even picking the correct '
  + '(Cashier-type) row was still only ONE employee\'s meal $, not summed across every cashier '
  + 'employee working that day. Fixed both together: metricDaily/metricSeriesWithSource gained an '
  + 'optional \'sum\' aggregation mode on a srcs tuple (via a new _resolveLeg() helper), and the '
  + 'auditRows leg of empMealAmt/mgrMealAmt/mgrMealCnt now reads cashier-type rows only, summed '
  + 'across employees. Every other chain\'s existing 2/3-element tuples are unaffected (default '
  + 'unchanged "first row wins" behavior).\n\n'
  + 'Verified against live Supabase data (2026-08-01..08-24, all 27 stores, the same population '
  + '#181 measured): empMealAmt match against qsr_cash_sheet rose from 71.1% to 98.0%, mgrMealAmt '
  + 'from 67.4% to 97.8% (both well clear of the dispatch\'s ~90% bar). 26/27 stores land at >=90% '
  + 'per-store match; loc 10422 lags at 66.7%, left as a measured residual, not chased further.\n\n'
  + 'New tests: src/__tests__/dispatch-183-audit-meal-cashier-only.test.js (9 tests) -- summation '
  + 'across multiple cashier employees, manager/preparer exclusion, row-order independence, a real '
  + '$0 cashier day still yielding null (not falling back to a manager/preparer duplicate), a day '
  + 'with no cashier row at all yielding null, pre-#59 rows with no registerType still resolving, '
  + 'and glimpseRows/ctrlRows still winning when they cover the day. Confirmed 8/9 fail when the '
  + 'metric-source.js change is reverted (the 9th tests unrelated fallback-order behavior). Full '
  + 'suite: 3046/3046 (293 files). Build clean; eager total 552.84 KB, 297.16 KB under the 850 KB '
  + 'budget (no new static top-level import).\n\n'
  + 'Flagged but NOT fixed here (out of #183\'s scope, a real separate lead for a future dispatch): '
  + 'the same Manager==Preparer duplication was also measured on pos_over_amt, which '
  + 'register-audit.js\'s analyzeRegisterAudit currently SUMS across all three register types for '
  + 'the live Register Audit / Security panel (CASH-003 thresholds read posOverAmt/posOverCnt) -- '
  + 'if it behaves like the meal fields, that summation is inflating displayed totals by roughly '
  + 'the duplicate amount. Not independently verified beyond a store-level total comparison; not '
  + 'touched. Full findings: memory/finding-audit-rows-registertype-duplication-2026-08-28.md.',
]};
