// @ts-nocheck
export default {version:'5.143', date:'2026-08-24', changes:[
  'Dispatch #101: Form Completions -- surfaced the per-occurrence detail loadQsrFormsCompletion '
  + 'already returns (store/form/date/completion%/time-to-complete/status), which the panel '
  + 'discarded after computing only the form-level rollup, plus a standard location selector and '
  + 'a real from/to date range. Additive only -- the existing rollup view (FormSummaryRow, pass '
  + 'rate bar, threshold input, store-days count) is unchanged.\n\n'
  + '1) Per-occurrence detail: each form row gets a "▸ Occurrences" toggle that expands into a '
  + 'table of its individual occurrences -- store resolved via sName/STORE_NAMES (not a bare loc '
  + 'code), the SAME date field the rollup buckets days on (occurrenceKey, via a newly-exported '
  + 'localDayKey), completion % (fPct(completionRatio)), time-to-complete as a human duration (new '
  + 'formatDuration() export -- "1m 50s", not raw ms), and status/missed. Sorting is a new pure '
  + 'sortOccurrencesForDisplay() export in forms-completion.js, not inline in the panel, per this '
  + 'file\'s own "panels don\'t reimplement math the engine owns" rule.\n\n'
  + '2) "Completed by" (userId) -- measured, not guessed, before writing any UI: a live service-role '
  + 'pull of qsr_forms_completion shows userId on completed rows is a QSRSoft/Cognito account UUID '
  + '(e.g. "848854c8-30f1-7076-c6f7-dcf35091bd06"), never a name. It also does NOT resolve through '
  + 'Security panel\'s RevealName/reveal_employee_identities_bulk path -- checked, not assumed: that '
  + 'RPC reads Meridian\'s own employee_identity_vault, populated only by tokenizing a plaintext name '
  + 'at ingest (src/engine/identity-vault.js), and Forms\' own normalizer explicitly never reads or '
  + 'stores the plaintext completedBy QSRSoft returns -- so no vault row for any Forms userId has '
  + 'ever been created. It is a different identity system, not a permission gap; building a new '
  + 'resolution (tokenizing completedBy at Forms-pull time, or an unconfirmed geid join) is an '
  + 'ingest/schema change, out of scope for this UI-only dispatch, and flagged as follow-up rather '
  + 'than built unreviewed here. So the UI never fabricates a name: a null userId (missed/open rows) '
  + 'always shows "—"; a real one shows the same "—" to every role EXCEPT admin/developer (the DB '
  + 'role \'admin\', same tier Security\'s dispatch #50 Part B gives frictionless identity access), '
  + 'which sees a short, explicitly-labeled diagnostic ID fragment -- never the bare UUID printed '
  + 'as if it were a name.\n\n'
  + '3) Location selector: the shared All -> State -> Patch -> Store LocationSelector '
  + '(src/components/PanelControls.js) -- the same control opportunity-dollars.js and '
  + 'top-bottom-performers.js already use, per feedback-selector-ui-standard.md -- feeding '
  + 'loadQsrFormsCompletion\'s existing locs param.\n\n'
  + '4) Real date range: DateRangeControl (same shared component) in custom-range-only mode '
  + '(no duplicate preset pills alongside the existing 7d/14d/30d window buttons, which are '
  + 'unchanged and still work) -- converts the picked from/to calendar days through '
  + 'apiWindowForDays() (already existed, unused until now) so a custom range lands on the SAME '
  + 'America/Chicago local-midnight boundary the rollup buckets days on, not a naive UTC window.\n\n'
  + 'Verified against the real panel, not isolated engine tests: 19 new/updated render + engine '
  + 'tests (forms-panel.test.js, forms-completion-rollup.test.js) exercise the actual '
  + 'FormsCompletionPanel consumer -- expanding a form shows real store/form/date/%/duration/status '
  + 'text; a store pick in the location selector changes the locs array the loader is called with '
  + '(confirmed via the mock\'s call args, not just "the control renders"); a custom date range '
  + 'changes the start/end the loader is called with to the apiWindowForDays-derived window; the '
  + 'existing rollup assertions (75.0%, "3 of 4 resolved occurrences completed", worst-form-first, '
  + 'per-form threshold inputs, freshness) are re-run unchanged and still pass. Full suite '
  + '2273/2273. Entry chunk 519.62 KB -> 519.64 KB gzip (+0.02 KB, negligible -- the panel is '
  + 'lazy-loaded; forms-panel-*.js chunk itself is 10176 bytes raw).',
]};
