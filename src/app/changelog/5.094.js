// @ts-nocheck
export default {version:'5.094', date:'2026-08-21', changes:[
  'Dispatch #55 Part B (Job C Batch 1) -- the first six overlay-to-page conversions. Scheduling '
  + '(sched-hub), Performance Reviews (perf-reviews), Food Cost (fob-analysis), End of Month '
  + '(fob-eom), Inventory Control (eom-dashboard) and Count Cycle (count-cycle) are now '
  + 'URL-addressable full-page views (?panel=<id>) via the EXISTING route:true infrastructure '
  + '(src/app/routing.js) built for dicompare/fcst-accuracy/proj/report -- routing.js itself was '
  + 'not touched, this batch just takes it from 4 route panels to 10.\n\n'
  + 'Per panel: fob-analysis and fob-eom had no internal chrome (bare content, no ModalShell), so '
  + 'they are wrapped directly in RoutePanelShell at the App.js render call site. perf-reviews and '
  + 'eom-dashboard rendered their own h(ModalShell,...) internally -- swapped for '
  + 'h(RoutePanelShell,...) in place, carrying over the same title/subtitle/icon/headerExtra (each '
  + "component's subHeader tab bar / PanelChrome, which RoutePanelShell has no slot for, moved to "
  + 'the first child in the body instead). count-cycle-panel.js hand-rolled its own backdrop/card/'
  + 'header/close-button from scratch (never used ModalShell at all) -- refactored to '
  + 'RoutePanelShell, which also drops the R7 hand-rolled-backdrop ratchet from 78 to 77. '
  + 'sched-hub was the most involved: SchedulingHubPanel (defined locally in App.js, not a '
  + 'separate view file) also hand-rolled its own bottom-sheet chrome around its six internal '
  + 'tabs (Scheduling/Analytics/Summary/Analysis/Allocation/Skills) -- refactored to '
  + 'RoutePanelShell with the existing tab-pill-bar relocated verbatim into headerExtra. All '
  + 'seven modal ids that funnel into this one hub (sched-hub, labor-analytics, scheduling, '
  + 'sched-summary, labor-analysis, labor-allocation, skills-matrix) still call setSchedTab(...) '
  + 'unchanged, only the setShowSchedHub(true) half became goRoute(\'sched-hub\').\n\n'
  + 'The six showX booleans (showSchedHub/showPerfReviews/showFOB/showFOBEOM/showEOMDash/'
  + 'showCountCycle) are removed entirely, not just unused -- deleted from their useState '
  + 'declarations, the anyModalOpen OR-chain, and the Escape-hatch sweep, matching how the '
  + 'original four route panels were already handled (a route panel has nothing rendering behind '
  + 'it that anyModalOpen would need to pause, and Escape already backs a route panel out via the '
  + 'existing routePanel check ahead of the showX sweep). schedTab stays -- it is which internal '
  + 'tab is selected, not the modal-visibility boolean.\n\n'
  + 'panel-registry.js: route:true set on all six, section/kind/label/icon/perm untouched '
  + '(presentation-only, not IA -- that was dispatch #55 Part A / #54 Job B, a separate PR).\n\n'
  + 'Verification: the registry ratchet test now enumerates all ten route ids (was hardcoded to '
  + 'the original four). The two existing generic regex tests (every route panel opened via '
  + 'goRoute(...), every route panel rendered under routePanel===id) cover all ten automatically '
  + 'once App.js is wired correctly -- no per-panel test needed there. Added one new dedicated '
  + 'test asserting NO setShowX(true) call site and NO useState declaration survives for any of '
  + 'the six removed booleans anywhere in App.js -- the exact regression class the dispatch calls '
  + 'out: a panel that renders fine via its new routePanel gate while a stale modal-open call site '
  + 'sits unnoticed elsewhere. App.js has no existing render-level test harness (it is not '
  + 'render-tested anywhere in this suite, unlike shell.js\'s AppSidebar), so that regex-based '
  + 'coverage plus the registry/goRoute/routePanel checks is the verification ceiling actually '
  + 'available in this codebase today, not a shortcut chosen over a render test that exists.\n\n'
  + '1860/1860 tests (1 net new + one ratchet ceiling lowered 78->77). Build clean: entry 1717.36 '
  + 'KB / 510.63 KB gz, vs. 1717.88 KB / 510.61 KB gz measured on the same main commit before this '
  + "PR's edits (stash-and-rebuild comparison) -- essentially flat, as expected since all six "
  + 'components were already lazy-loaded (confirmed via lazyPanel() in App.js\'s import list before '
  + 'assuming) and none became a static import. Full writeup: memory/dispatch55-part-b.md.',
]};
