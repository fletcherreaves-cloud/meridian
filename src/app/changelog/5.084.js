// @ts-nocheck
export default {version:'5.084', date:'2026-08-20', changes:[
  'Dispatch46 -- makes the Security panel legible, then analytical. Owner-requested after first real use: "Give me a legend... use plain language in addition to results... let\'s take the findings and deep dive further." Three parts.\n\n'
  + 'Part A: a dismissible legend defining Flagged/Clear/Undetermined (Undetermined is NOT the same as Clear -- the build\'s core integrity property, previously invisible), the signal-count badge, the four baseline types, and threshold-vs-sigma (a z-score rule\'s threshold means standard deviations, a plain rate rule\'s means the metric\'s own unit -- two rules on screen used the same word for different things). Every rule\'s plain-language description now renders under the rule filter and at the head of every expanded finding -- rewrote security_rules.description for the three engineer-voice rules (CASH-004, INV-001, INV-002) rather than inventing new UI copy, so SAGE and any future consumer get the same improvement. Units on every number via a small hardcoded map (five rules, five units).\n\n'
  + 'Part B: buildDecisionSentence() -- a plain-language line beside (never instead of) every metric, per CLAUDE.md\'s standing "say the number AND the decision" rule. Derives the real multiple against the baseline ("about 2.6x the peer average") rather than restating the number in words, surfaces the rule\'s own investigation_action as the "Next:" step, and does not soften large magnitudes (a measured 49x variance renders as a real, stated 18x multiple, matching the dispatch\'s own explicit instruction). Inventory subjects name the item and store; cash subjects name the person (or "This employee" if not yet revealed -- reveal stays a deliberate, logged action, never automatic).\n\n'
  + 'Part C: built the two pieces real data can actually support today. Item 1 (chronic vs. new, the dispatch\'s own named highest-value item) -- restructured groupFindingsBySubject() to dedupe each rule to its LATEST window (fixing a real latent bug: a second window for the same rule would have rendered as a duplicate chip) while preserving full history for a trend view. Measured first: security_findings currently holds exactly ONE window per rule -- the daily batch job hasn\'t run long enough to accumulate a second one -- so classifySubjectTrend() honestly reports "not enough history yet" for every real subject today, by design (fewer than 2 windows is never guessed as new/chronic), and starts working the moment tomorrow\'s run adds a second window. Item 6 (automatic exoneration) -- security_rules\' own exoneration_rules/corroboration_rules columns are unpopulated on every rule, so instead built the real check the dispatch names directly: computeWasteExoneration() sums a flagged inventory subject\'s logged raw_waste+comp_waste against its usage variance; the panel shows a note when logged waste covers >=50% ("likely explained by waste, not shrink"). Items 2 (change-point), 3 (shift/daypart attribution -- explicitly the dispatch\'s own highest-risk item, deserves its own careful pass), 4 (cross-rule fingerprints), and 5 (store-vs-person separation) are explicitly scoped and deferred, per the dispatch\'s own "not all of it needs to land at once."\n\n'
  + '23 new tests (7 batch-job unit/wiring for automatic exoneration, 13 pure-logic for history/trend/decision-sentence, 3 component wiring through the real panel). 1763/1763 suite passes, build clean, no entry-chunk budget impact. SQL for the plain-language rewrite and the new exoneration_share column handed back, not applied. Full writeup: memory/dispatch46-security-panel-legibility-analysis.md.',
]};
