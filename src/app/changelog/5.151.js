// @ts-nocheck
export default {version:'5.151', date:'2026-08-25', changes:[
  'Dispatch #110: Speed of Service panel -- Avg-DT bar, bar-chart trend, full date range, '
  + 'patch-selector fix.\n\n'
  + 'Four bounded fixes to src/views/dt-speedofservice.js, all scoped by a background '
  + 'investigation before this dispatch was written (a 5th owner ask -- "pick a metric, page '
  + 'adapts" -- was found to be a data-layer rearchitecture and explicitly left out).\n\n'
  + 'Item 1 (By-Hour bar): the "bar for avg dt" the owner asked for turned out to be a plain '
  + '<table>/<div> bar, not a chart -- added a second hand-rolled bar to the Avg DT column, '
  + 'same Math.round(v/max*60) sizing + dtColor convention the existing Trans bar already '
  + 'uses. Store Ranking / By Hour flex-basis rebalanced 2:1 -> 3:2 so By Hour has room for '
  + 'two bars per row.\n\n'
  + 'Item 2 (bar-chart conversion): DtTrendChart\'s single-series \'avg\' mode now renders as '
  + 'bars, matching DtDaypartChart\'s existing treatment (the owner\'s "use the bar '
  + 'throughout"). The multi-series \'store\'/\'patch\' modes deliberately stay as lines -- '
  + 'grouped bars across 15+ stores/patches would be unreadable at this panel\'s width, the '
  + 'trade-off the dispatch flagged for a real decision rather than a blind type-string flip. '
  + 'Reference lines get an explicit type:\'line\' override in bar mode (Chart.js mixed-chart '
  + 'requirement). The underlying weeks/series computation is untouched -- pure rendering '
  + 'change, verified via a real-render test that the redrawn chart\'s values match the '
  + 'pre-conversion computation.\n\n'
  + 'Item 3 (full date range): the hardcoded PERIODS=[30d,60d,90d] <select> is replaced by '
  + 'the shared DateRangeControl (7/14/28/30/60/90/180 presets + custom start/end), matching '
  + 'security-panel.js/forms-panel.js\'s existing adoption. loadDtHistory '
  + '(src/lib/supabase.js) now accepts EITHER the original bare day count (unchanged '
  + 'behavior) or a resolved {s,e} range object -- checked first that the panel is the only '
  + 'production call site, so this is a dual-mode addition, not a breaking signature change. '
  + 'The early/late trend-split midpoint now derives from the selected range\'s own midpoint '
  + 'instead of "now - days/2", which silently assumed the range always ended today.\n\n'
  + 'Item 4 (the actual bug, root-caused before this dispatch): useChart\'s Chart.js redraw '
  + 'effect depended on [weeks.join(\',\'), series.length, mode] -- omitting the actual '
  + 'filtered VALUES. In \'avg\' mode series always has exactly one entry and the week-set '
  + 'usually doesn\'t change across a patch switch either, so neither dependency changed and '
  + 'the canvas kept rendering the previous patch\'s stale data even though the underlying '
  + 'series genuinely changed. Fixed by adding a content signature (every datapoint of every '
  + 'series, not just their count) to the dependency array, so any value change in any of the '
  + 'three modes re-fires the redraw.\n\n'
  + 'Verification: a new real-render test (react-dom/client + happy-dom, this repo\'s '
  + 'established panel-render pattern) renders the actual DTSpeedOfServicePanel with synthetic '
  + 'qsr_daily_activity rows and a mocked chart.js/auto capturing every Chart construction. '
  + 'For item 4 specifically: two real store locs tuned to different constant avg-DT values '
  + 'across two identical weeks, so a filter switch changes ONLY the series values while both '
  + 'old dependencies stay constant -- the exact shape of the reported bug. Confirmed the test '
  + 'is not a placebo by temporarily reverting the dependency-array fix and rerunning: only '
  + 'the item-4 case failed (stale [200,200] instead of the correct [150,150]), reproducing '
  + 'the real bug before the fix was restored.\n\n'
  + 'Full suite green (226 files / 2355 tests). Build clean. Entry chunk 1,567.92 KB / '
  + '456.79 KB gzip -> 1,568.13 KB / 456.86 KB gzip (+0.21 KB / +0.07 KB gzip) -- '
  + 'dt-speedofservice.js is already a lazy panel; the small delta is a new shared chunk '
  + '(patch-heatmap\'s withAlpha() helper, reused per this repo\'s R4 color-alpha-concat '
  + 'ratchet rather than a fresh string-concat site) loaded only when the SoS panel opens, not '
  + 'entry-chunk growth. Full resolution: memory/dispatch-110.md.',
]};
