// @ts-nocheck
export default {version:'5.184', date:'2026-08-26', changes:[
  'Fix: Retention Rollup\'s "Labor % Δ" column was showing 100x the real move — e.g. a '
  + '"22.02% → 21.75%" Before→Since pair (a 0.27pp change) displayed as "-27.43pp" (owner-caught '
  + 'from a live screenshot the day #146 shipped, same session). Root cause: `laborPct` is '
  + 'already on a 0-100 percent scale everywhere in schedule-summary.js/schedule-retention.js '
  + '(schedule-summary.js\'s own `// % scale` comment), so `laborPctDelta = b.laborPct - '
  + 'a.laborPct` is already in percentage points — but the rollup table and its CSV export '
  + 'multiplied it by 100 again before display. The underlying `aggregateRetentionRollup()` '
  + 'computation was always correct (dispatch #141\'s own tests already asserted this); only the '
  + 'render/export layer double-scaled it. No existing test caught it because every dispatch-141 '
  + 'render test hit the empty "no marks" state (Supabase not configured in tests) — the bug '
  + 'shipped invisibly until dispatch #146 landed real marks. New test: '
  + 'src/__tests__/schedule-retention-rollup-labor-delta-scale.test.js mocks loadRetentionMarks '
  + 'to actually populate the table and checks the rendered pp text against the Before→Since '
  + 'cell\'s own values, not a hardcoded number that could silently re-encode the bug.\n\n'
  + 'Performance Review: "2nd Side Healthy Usage (%)" now auto-populates both sides (owner, '
  + '2026-08-26: "I know I have 2nd Side Healthy Usage inactive for scoring atm, but we need to '
  + 'populate it as well — it is KVS Usage metric. Target available in yearly targets under '
  + 'Healthy Use 2nd Side"). Actual: wired to the already-registered `kvsHealthy` metric-source '
  + '(metric-source.js — glimpse/opsService/qsrActSummary cloud streams, manual `ds.opsRows.kvsu` '
  + 'last), same `metricAvg()` auto-first pattern the KVS Time metric right above it already '
  + 'uses. Target: `secondSide: \'tKvsu\'` added to `REVIEW_METRIC_TARGET_FIELD` — real, '
  + 'already-parsed and persisted (`parseYearlyTargets`\'s "Healthy Use 2nd Side"/"KVS Usage" '
  + 'column → `yearly_targets.kvs_usage_pct`), just never wired to this metric. Stays '
  + '`scored:false` (reference-only) exactly as the owner asked — this only fills the Act/Tgt '
  + 'values, it does not turn scoring on.\n\n'
  + 'Investigated but NOT fixed: "Delivery Wait (sec)" target. `REVIEW_METRIC_TARGET_FIELD` '
  + 'already maps `delivWait: \'tMcdWait\'` and the auto-fill loop is generic — the wiring is '
  + 'correct. Measured live against Supabase: `yearly_targets.mcd_wait_time` is null for all 27 '
  + 'stores, while neighboring McDelivery columns parsed from the same workbook section '
  + '(`mcd_gcrd`, `dig_app_gcrd`) are populated for every store — strongly suggesting the real '
  + 'workbook\'s delivery-wait column header doesn\'t match either string `parseYearlyTargets` '
  + 'checks for (\'McDelivery Restaurant Wait Time\'/\'McDelivery Wait Time\'), not a genuine data '
  + 'gap. Needs the current yearly targets workbook (or the exact column header text) to fix the '
  + 'parser match — flagged, not guessed at.\n\n'
  + 'Full suite 2660/2660 (250 files, +1 from this change); build clean. Entry 469.95 KB gzip '
  + '(469.89 KB prior, +0.06 KB); eager payload 541.13/850 KB gzip (541.07 KB prior, +0.06 KB).',
]};
