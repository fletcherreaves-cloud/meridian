// @ts-nocheck
export default {version:'5.069', date:'2026-08-19', changes:[
  'Dispatch29 Workstream G -- labor allocation, wired. The last of seven workstreams, shaped differently: five owner-run probe rounds (G-1 through G-5, two boundary corrections) already PROVED the finding (memory/analysis-labor-allocation-2026-08-18.md -- Breakfast/Lunch run UNDER the VLH guide, Afternoon/Dinner/Late Night run OVER it, surplus covers deficit 1.6x, cost-neutral reallocation not a hypothesis) and a correct, tested engine (src/engine/labor-standard.js) already existed -- it just had ZERO callers anywhere in src/ (grepped directly), the same #366 failure mode ("a test that only imports the engine can\'t tell fixed from fixed-but-never-wired-in") this session\'s dispatches have now found three times.\n\n'
  + 'New "Labor Allocation" tab in the Scheduling hub (src/views/labor-allocation.js, lazy-loaded). Self-loads its own 90-day hourly window on mount rather than joining App.js\'s global startup ds -- 27 stores x 90 days x 24 hour_slots is real weight nobody should pay for on every login when most users won\'t open this tab (same reasoning dt-speedofservice.js\'s loadDtHistory already uses for the same table). Three views sharing one engine: District (the proven district-wide deficit/surplus table, scheduled-vs-guide and punched-vs-scheduled kept as SEPARATE columns -- never blended, since Breakfast/Lunch and Afternoon/Dinner fail on different legs); By Store (same breakdown, daypart-selectable, sorted worst-deficit first); Overnight (classifies open-vs-closed FIRST via the already-coded overnightOpenness(), then shows the RIGHT number per bucket -- closed stores get overnightExcessByStore()\'s verdict against the owner\'s close-down/pre-open standard, open stores get TPPH/speed -- mixing the two on one ratio is exactly what produced the retracted "Tishomingo/Elgin killer pair" finding, and this panel structurally can\'t make that mistake).\n\n'
  + 'Step 2 -- TPPH extended to hour_slot/daypart grain: allocationByStoreDaypart/allocationDistrict now also return tpph/secPerCar per (loc,daypart) bucket, computed in the SAME hour_slot iteration those functions already do. tpph uses dt_trans_cnt (drive-thru), matching these functions\' own `cars` field, not the all-channel `transactions` field, per the dispatch\'s explicit "pick one deliberately and label it." Checked before wiring it into METRIC_SOURCES as the dispatch\'s own text suggested ("add it in one line"): that resolver is keyed per-(loc,date), daypart grain is a genuinely different shape with no date axis, so extending it isn\'t a one-line change -- kept the two grains as two different, honestly-labeled things instead (the daily tpph chain untouched; this is a new daypart-grain field) rather than silently doing something different from what was asked without saying so.\n\n'
  + 'Found and fixed while wiring: loadDailyActivityRange() (supabase.js) was missing total_scheduled_hours from its select() entirely -- the exact field scheduledVsGuide/punchedVsScheduled need. Every prior caller (there were none) would have silently gotten null for both legs forever; fixed in this PR rather than filed as a separate follow-up, since it would have shipped the very panel this dispatch asked for wrong on day one.\n\n'
  + '5 new tests (labor-standard.test.js): tpph/secPerCar are ratio-of-sums using the dt_trans_cnt denominator, null (not NaN/0) on an empty bucket, and allocationDistrict sums the raw legs across stores rather than re-deriving from each store\'s own rate (caught a rounding-reintroduction bug in a first draft, fixed before committing). Real-browser verification (Playwright + dev server): the new tab renders (title, tab bar, loading state) with no crash and no crash switching tabs on empty/never-arriving data; did not get a real-data render since this sandboxed session\'s browser can\'t reach Supabase (server-side reads work; in-browser fetch doesn\'t, same limitation Dispatch27 already documented). 1565/1565 tests pass. Build clean, entry-chunk budget unaffected (own lazy chunk).\n\n'
  + 'Scope: shift-grain attribution only (rollupShiftsByEmployee stays unwired, per the dispatch\'s explicit constraint); no per-store/per-slot ranking or leaderboard; the Ardmore-Cooper/Freeport open-vs-closed classifier disagreement the dispatch names as "a named follow-up, not resolved here" is shown as two visible signals (configured hours next to the data-driven classification) rather than resolved by a new decision procedure that wasn\'t asked for. Open item, stated plainly: the pre-open-hours-in-Breakfast correction (the 1,716-hr adjustment) isn\'t yet folded into this panel\'s own Breakfast gap figure -- the correction logic exists in the engine but wiring it into this specific number is a separate join this pass didn\'t attempt. Full writeup: memory/dispatch29-labor-allocation-panel.md.',
]};
