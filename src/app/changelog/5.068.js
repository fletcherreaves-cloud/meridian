// @ts-nocheck
export default {version:'5.068', date:'2026-08-19', changes:[
  'Dispatch28 Workstream F -- voice by role, first slice. CLAUDE.md carries a standing rule (2026-08-17, "Voice by role"): every panel\'s default surface must say the number AND the decision, explicit both/and, never a simplification trade. This dispatch\'s own grounding reproduced the gap directly against current code (Count Cycle still emits \'No complete weekly count on record\', DI Compare still emits \'Not Dialed-In is better -- recalibrate\' -- both analyst-only, unchanged) and recommended the cheapest, already-mostly-built lift as the entry point: Visit Readiness\'s topDrivers already computes WHICH gap matters (the hard half); it was only missing the last mile.\n\n'
  + 'New buildVerdict(store) in visit-readiness.js, alongside the existing buildWhy(store) -- reuses topDrivers (already sorted worst-first) and the same 0.85 driver-badness threshold buildWhy already uses, no new threshold invented. Priority: an elevated food-safety flag leads (the more severe PACE consequence) even over an at-risk readiness band; then at-risk -> "Coach {worst driver} -- {actual} vs {target} target, the biggest blocker to PACE-ready"; watch -> same shape, softer verb; ready -> "On track for a graded visit -- no action needed this week." `why` is unchanged and still shown -- the two are complementary, not a replacement, per the standing rule\'s explicit both/and.\n\n'
  + 'Wired into all three surfaces that render a store\'s readiness: the Visit Readiness panel\'s COLLAPSED row (previously only the numeric score + band/FS chips -- zero decision text, and the diagnostic why line only appeared once expanded, exactly the gap CLAUDE.md\'s rule names: "never hidden behind a click that an operator won\'t make"); the per-store printable coaching one-pager; the district-wide printable report. attention-feed.js\'s visitRisk() was a single generic "coach before the next CFV/RGR" for every at-risk store regardless of what was actually wrong -- now reads the store\'s own verdict (falls back to the old generic string if absent), so the Needs Attention feed and the Visit Readiness panel never disagree about what to coach.\n\n'
  + '7 new/updated tests: an at-risk verdict names an action + the SAME worst driver topDrivers already identified (not independently re-picked) + its number; a ready store\'s verdict is genuinely different text from its why; food-safety priority holds even when also at-risk on readiness (isolated with a dedicated fixture after catching that the straightforward bad-everything fixture trips both flags at once); visitRisk prefers a present verdict and still degrades gracefully without one. 1562/1562 tests pass (7 new). Build clean, budget unaffected (all three touched views are already-lazy panels).\n\n'
  + 'Scope, stated plainly: only Visit Readiness this pass, the dispatch\'s own recommended cheapest-lift entry point. Count Cycle and DI Compare -- the dispatch\'s own two reproduced evidence strings -- are explicitly deferred, not forgotten, same staged-slice pattern this repo already uses elsewhere. Did not touch permissions.js or branch the verdict text by role (the dispatch\'s own scoping note: build voice tiers against the 3 roles actually enforced, not the 8-tier ladder the schema doesn\'t support -- and this slice doesn\'t yet answer whether a supervisor\'s verdict should differ from a GM\'s). The dispatch\'s own empirical standard ("hand the panel to someone at operator level, see if they act without being told") needs a real person and was not something this session could verify -- stated as an open item, not silently skipped. Full writeup: memory/dispatch28-voice-by-role.md.',
]};
