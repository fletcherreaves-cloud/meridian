// @ts-nocheck
export default {version:'5.055', date:'2026-08-18', changes:[
  'Issue #357-B2/3 -- Count Cycle\'s per-class denominator was every WRIN the On-Hand API ever returned for a store/class, no active/inactive distinction -- a discontinued item still carrying a residual on_hand (the Durant #5985 case, engine/eom-inventory.js) inflates the denominator without being real to-count work, and the owner explicitly forbade deriving "active" from "counted recently" (circular -- it would make a store\'s own count history define what counts as required).\n\nMeasured, not guessed, per the dispatch\'s own instruction: shipped a read-only DUMP_RAW_FIELDS probe (already merged in #371/#372) against the live On-Hand API. First single-record dump confirmed the raw payload carries `active_in_recipe`; a follow-up full-run tally (2026-08-17, 7127 items across all 27 stores) confirmed it actually VARIES -- {1: 4807 (67.4%), 0: 2320 (32.6%)}, not a constant that would be useless as a filter. That is "a column and a where, not a modelling exercise," exactly as the dispatch framed the decision.\n\nFix: `qsr_onhand` gains an `active` boolean column (supabase/schema-onhand-active-flag.sql -- owner needs to run this in the Supabase SQL Editor, no direct Postgres connection available from this session, matching the established pattern for prior schema files). `qsrsoft-onhand-pull.mjs` now persists `active_in_recipe` (normalized to boolean, NULL preserved for a genuinely missing field). `count-cycle.js`\'s `detectSessions` filters both the denominator (`classTotals`) AND the numerator (session counts) to `r.active !== false` -- treating NULL/undefined as active, so this is a no-op on every row pulled before this column existed until the next scheduled pull backfills it.\n\nIssue #357-5 (same PR, same dispatch item) -- added counted/active per class to the Count Cycle panel rows. New `perClass` field on `cycleCompliance`\'s per-store return: for each class, the item count from the MOST RECENT session that touched it at all, against the ACTIVE-only denominator. Panel renders it as a compact `Food 105/118` style row per store, independent of the weekly/mid-paper grading logic.\n\nVerification: 5 new engine tests, 3 of which were confirmed to go RED against a stash of the pre-fix engine (excluding inactive items from classTotals, excluding an inactive item\'s count from coverage, and the generalized Durant-case denominator check) before being restored -- the other 2 pin the new perClass field\'s shape. All existing count-cycle tests (which never set `active` on fixture rows) pass unchanged, confirming the filter is backward-compatible with data pulled before this column existed.\n\n1419/1419 tests pass (5 new), build clean (no bundle-size shift -- logic-only change in an already-loaded module).',
  ]};
