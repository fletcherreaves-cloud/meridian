// @ts-nocheck
export default {version:'5.228', date:'2026-08-28', changes:[
  'Dispatch #187 -- SAGE conversation cross-device persistence. The active thread (SAGE_THREAD_KEY) '
  + 'and up to 25 archived past conversations (SAGE_SESSIONS_KEY) were localStorage-only -- a '
  + 'conversation started on one device/browser was invisible everywhere else. Reused the SAME '
  + 'generic user_settings blob-sync infra labor-tools.js/analytics.js already use for '
  + 'model_backtest_summary/dialed_in (src/lib/blob-sync.js\'s pushBlob/readBlobLocal/hydrateBlob) '
  + '-- no SAGE-specific push/hydrate wrapper written, and zero new Supabase schema (CLAUDE.md\'s '
  + '"check whether a helper exists before writing one").\n\n'
  + 'Both keys now write a {data,savedAt} envelope (sage.js\'s own _normSageBlob normalizes a '
  + 'pre-#187 bare-array localStorage value to savedAt:0, mirroring blob-sync.js\'s own '
  + 'normalizeDialedIn legacy-shape handling). Hydration is SAGE-panel-mount-only (not app-wide '
  + 'startup -- SAGE is already lazy-loaded and this data is heavier/less universal than '
  + 'dialed_in/model_assignments), guarded exactly like App.js\'s _stDialedIn: cloud wins ONLY if '
  + 'its savedAt is strictly newer than local\'s, so a stale cloud read from an older session can '
  + 'never stomp an in-progress conversation on this device -- NOT the unconditional-cloud-wins '
  + '_stModelAssignments pattern, per the dispatch\'s own reasoning for why this is the '
  + '_stDialedIn case.\n\n'
  + 'Cloud pushes happen at settle points, not on every stream chunk: the streaming true->false '
  + 'transition (covers success, error, AND abort -- all three share sendMessage\'s finally block), '
  + 'archive-to-history / delete (persistSessions), and session switch/clear (reopenSession/'
  + 'clearThread). No time-based debounce was added on top -- these settle points are already '
  + 'naturally coarse (at most once per full assistant turn, which itself takes multiple seconds; '
  + 'archive/switch/clear are explicit one-off user actions), so a fixed millisecond debounce '
  + '(App.js\'s useDebouncedValue is a value-debouncer, unexported, and not the right shape here '
  + 'anyway) would not reduce write frequency further. localStorage instant-write path unchanged '
  + 'in cadence, now envelope-stamped so the savedAt it leaves behind is meaningful at the next '
  + 'mount\'s hydration comparison.\n\n'
  + 'Measured payload size (dispatch task 4) before deciding on a cap: a synthetic-but-realistic '
  + '25-session archive (2-9 turns each, SAGE\'s actual answer length -- ranked tables + root-cause '
  + '+ tiered action plans run long, per project-sage.md) serializes to 428 KB typical / 646 KB '
  + 'heavy-tail -- well past "multiple hundred KB". So SAGE_SESSIONS_KEY now gets a SIZE cap, not '
  + 'just the pre-existing 25-session COUNT cap: _capSessionsBySize drops the OLDEST sessions '
  + '(newest-first array) until the serialized blob is back under 300KB (comfortably under a '
  + 'browser\'s localStorage quota and a reasonable per-save network payload). The active thread '
  + '(a single ongoing conversation) got no cap -- much smaller than 25 archived conversations '
  + 'combined, not the thing the dispatch flagged as a concern.\n\n'
  + 'Live verification (measure it, don\'t reason about it): re-measured this session\'s own '
  + 'SUPABASE_SERVICE_ROLE_KEY (an sb_secret_... key per the 2026-08-24 rotation) against '
  + 'user_settings -- GET returned content-range: 0-2/9 (9 real rows, e.g. model_assignments, '
  + 'model_backtest_summary for the owner\'s real user_id). Then ran a live round-trip using the '
  + 'EXACT upsert/select shape saveUserSetting/loadUserSetting use (onConflict user_id,key; '
  + 'select value eq user_id eq key): POST a test row under key sage_thread_dispatch187_livecheck '
  + '(distinct from the real sage_thread/sage_sessions keys, never touching Fletcher\'s actual '
  + 'data) -> 201 Created, body echoed the exact {data,savedAt} envelope back -> GET confirmed the '
  + 'identical envelope round-tripped -> DELETE cleaned up (204, then a final GET confirmed zero '
  + 'rows left). Did NOT verify through an actual authenticated browser magic-link session (no '
  + 'email OTP access in this environment) -- the service-role round-trip confirms the '
  + 'infrastructure and exact payload shape are live and correct, but the real client SDK auth '
  + 'path (supabase.auth.getUser() inside saveUserSetting/loadUserSetting) was not separately '
  + 'exercised.\n\n'
  + 'Out of scope, unchanged: SAGE\'s model, system prompt, tool-use, and streaming UI.\n\n'
  + 'New tests: src/__tests__/sage-cloud-persistence.test.js (13 tests) -- _normSageBlob legacy-'
  + 'shape migration, the savedAt guard exercised through the REAL blob-sync.js pushBlob/'
  + 'hydrateBlob sage.js actually calls (not reimplemented in the test, per CLAUDE.md\'s "would '
  + 'this verification still pass if the change were reverted" -- a newer local write survives a '
  + 'stale cloud read arriving after it; a genuinely newer cloud thread does win; a legacy bare-'
  + 'array blob never beats a real stamped write; a fresh device hydrates straight from cloud), '
  + 'and _capSessionsBySize\'s size-based truncation including the measured 25-session scenario '
  + 'itself. Full suite: 3072/3072 (295 files, +13 tests). Build clean; eager total 553.14 KB, '
  + '296.86 KB under the 850 KB budget (sage.js stays lazy-loaded, its own chunk moved 59.16KB -> '
  + '59.36 KB / 20.18 -> 20.26 KB gzip, well outside the entry budget).',
]};
