// @ts-nocheck
export default {version:'5.228', date:'2026-08-28', changes:[
  'Dispatch #189 -- merge Count Cycle into Inventory Control as a tab, per the owner\'s '
  + '2026-08-10 merge decision ("a view of the same data, not a separate job",  '
  + 'memory/decisions-panel-inventory-2026-08-10.md).\n\n'
  + 'Harvest first: Count Cycle (registry id count-cycle) was already sharing its underlying '
  + 'data with Inventory Control\'s existing mode==\'progress\' tab (dispatch #97/#98/#112 had '
  + 'already wired cycleCompliance()/detectSessions() from engine/count-cycle.js into both), so '
  + 'the genuinely distinct thing to harvest was the PRESENTATION, not new data: Count Cycle\'s '
  + 'exceptions-first, plain-language per-store cards (severity color, "N days since last '
  + 'complete Food+Condiment count", a PAPER DUE badge, the full count-session timeline, a '
  + 'collapsible "stores on cycle" list) versus the existing tab\'s district table + FOB '
  + 'variance-trace drilldown. Both lenses are kept -- neither is a subset of the other.\n\n'
  + 'Naming collision: the existing mode==\'progress\' tab was ALREADY button-labeled "Count '
  + 'Cycle" (it renders CadenceMonitor, headed internally "Weekly Count Cadence"). Relabeled '
  + 'that tab\'s button to "Cadence" so the new tab could take the exact "Count Cycle" name/'
  + 'identity the retired standalone panel carried. Inventory Control now has four tabs: '
  + 'Scoreboard / EOM Count / Cadence / Count Cycle.\n\n'
  + 'CountCyclePanel -> CountCycleSection (src/views/count-cycle-panel.js) -- dropped its own '
  + 'qsr_onhand fetch and RoutePanelShell; it now reads Inventory Control\'s already-loaded '
  + '`onHand` state, scoped to the same state/patch/store filter as every other tab, with the '
  + 'same "asOf: today, regardless of the selected EOM period" behavior CadenceMonitor already '
  + 'had -- zero duplicate network call. Registry entry converted route:true/kind:\'nav\' -> '
  + 'kind:\'hub-tab\' (section stays truthful), same pattern as fcst-accuracy/targets-editor/'
  + 'sched-retention. Old ?panel=count-cycle links redirect into the new tab (in-app click, a '
  + 'hard page load, and browser back/forward all covered) rather than 404ing.\n\n'
  + 'Panel-contract check (opportunistic): both panels already use RoutePanelShell and already '
  + 'have CSV export; eom-dashboard.js\'s location picker already carries a documented, '
  + 'deliberate reason for not using the shared LocationSelector from a prior pass -- nothing '
  + 'new to convert. Full suite passes on every file this touches or that exercises count-cycle/'
  + 'eom-dashboard; build clean, eager entry payload 553.18 KB gzip / 850 KB budget (296.82 KB '
  + 'headroom) -- count-cycle-panel.js is no longer its own lazy chunk, absorbed into '
  + 'eom-dashboard\'s (a net simplification, same shape as ForecastAccuracyPanel/'
  + 'LifeLenzBridgePanel\'s dispatch #106 precedent).',
]};
