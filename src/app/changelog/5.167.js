// @ts-nocheck
export default {version:'5.167', date:'2026-08-25', changes:[
  'Dispatch #125: reverse identity-vault tokenization for Crew Schedule Lookup (rework of #123, '
  + 'before merge).\n\n'
  + 'Owner, directly, while PR #725 was open for review: "I flagged the PII/privacy considerations '
  + 'to you upfront via the scope question, and both #123 and #124 are built around the existing '
  + 'tokenized-identity-vault pattern rather than storing raw names > Let\'s update this > there is '
  + 'no reason to hide names for scheduling and punch times > everyone can see this data as-is." '
  + 'This is a direct policy reversal, not a PM judgment call -- the identity-vault pattern itself, '
  + 'and its use elsewhere (Register Audit, Security panel reveal-gated findings), is UNCHANGED; '
  + 'this dispatch is scoped to Crew Schedule only (companion dispatch #126 covers the QSRSoft '
  + 'punch-times side, PR #724, separately).\n\n'
  + 'scripts/lifelenz-pull.mjs: upsertShiftAssignmentRows() no longer calls tokenizeRows()/'
  + 'getOrCreateToken() -- the identity-vault import is gone from this file entirely. The resolved '
  + 'name is trimmed and stored directly as employee_name. assigned_employment_id is unchanged as '
  + 'the stable join key (it was never a privacy mechanism).\n\n'
  + 'supabase/schema-lifelenz-shift-assignments.sql: emp_token column + its employee_identity_vault '
  + 'FK dropped, replaced with a nullable employee_name text column (null only when no roster match '
  + 'was found -- same graceful-degradation case as before). Decided NOT to keep emp_token '
  + 'alongside the new column: nothing in this codebase reads it (dispatch #126\'s QSRSoft side '
  + 'resolves ITS OWN employee_name via a different join, qsr_employee_tenure, not by joining back '
  + 'to this table), so keeping it would only mean continuing to write to the identity-vault\'s '
  + 'audit-log surface for zero consumers.\n\n'
  + 'RLS re-decision, stated explicitly rather than defaulted: dropped the security_findings-tier '
  + 'gated-read policy (admin/supervisor always; manager only with '
  + 'org_config.gm_identity_reveal_enabled) -- that tier existed only because a name was being '
  + 'revealed behind it. Replaced with the SAME two-layer pattern every other loc-keyed operational '
  + 'table in this schema uses: tenant match + accessible_locs scoping via the '
  + '(select public.my_locs()) RESTRICTIVE predicate (schema-rls-phase2-loc.sql\'s own pattern, '
  + 'reproduced inline here since this table postdates that migration). A GM still only ever sees '
  + 'rows for locs in their own accessible_locs -- this is ordinary RLS scoping, not "no RBAC."\n\n'
  + 'src/views/crew-schedule-panel.js: removed the click-to-reveal gate entirely -- no RevealName, '
  + 'no securityPanelAccess()/loadGmIdentityRevealEnabled import, no permState state machine, no '
  + 'identity_reveal_log write for viewing a name here. Names render directly in the employee '
  + 'directory and search now matches a resolved name with no reveal step first. The panel no '
  + 'longer takes a userRole prop or does its own permission check at all -- matches Labor Tools/ '
  + 'Calendar Manager, which have no panel-specific gate beyond nav-level perm + RLS.\n\n'
  + 'RBAC re-decision (nav layer), stated explicitly per the dispatch\'s own request: '
  + 'panel-registry.js\'s crew-schedule entry moved from perm:\'security.view\' to '
  + 'perm:\'analytics.store\' -- the same key Labor Tools/Scheduling/Calendar Manager use. '
  + 'Reasoning: the security-tier nav gate existed only alongside the identity-reveal step it '
  + 'protected; keeping it after removing that step would leave most managers (and every gm/'
  + 'office_staff/DO/VP/owner role) locked out of an ordinary schedule lookup for no remaining '
  + 'reason. "Who\'s on shift Tuesday" is core scheduling data, the same category as the existing '
  + 'Scheduling/Schedule Summary panels already open to any manager under analytics.store -- not a '
  + 'security investigation.\n\n'
  + 'Tests: crew-schedule-panel.test.js rewritten -- the RBAC-gate/reveal-behavior tests (admin/'
  + 'supervisor/manager+flag/gm/office_staff permState branches, revealed-name search gating) '
  + 'tested behavior that no longer exists, replaced with tests proving the panel loads '
  + 'unconditionally on mount and renders a resolved name directly with no reveal affordance in '
  + 'the DOM. Pure-logic tests (groupShiftAssignmentsByEmployee/filterEmployeeDirectory) updated '
  + 'for the new employeeName-bearing row shape and assignedEmploymentId-only grouping key (the '
  + 'empToken grouping key is gone with the column). shell-nav-snapshot.test.js\'s permission-gate '
  + 'tables updated: analytics.store now hides \'Crew Schedule\' and shares 🗓 with '
  + '\'Scheduling\' again (both panels are gated the same way now); security.view no longer hides '
  + '\'Crew Schedule\'.',
]};
