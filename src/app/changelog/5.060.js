// @ts-nocheck
export default {version:'5.060', date:'2026-08-18', changes:[
  'Dispatch20 #2 -- TPPH (transactions per punched hour). The dispatch\'s premise ("TPPH reaches the app only through manual upload today") turned out to be half true, verified rather than assumed: src/engine/metric-source.js already has an auto-first `tpph` chain (qsrActSummaryRows/DAR first, Controls/Labor manual as fallback, plus a gc/hrs derive fallback) -- but two real gaps existed underneath that already-correct chain.\n\nGap 1 -- graded-visits.js\'s Visit Readiness daily summary chip computed tpph as `_num(ctrl && ctrl.tpph, lab && lab.tpph)`, skipping the `qsr` (qsrActSummaryRows) row entirely even though that exact variable was already fetched two lines above for sales/gc. So this one panel\'s daily TPPH figure really was manual-only in practice, despite the shared auto-first chain existing. Fixed by adding `qsr && qsr.tpph` as the first argument, matching metric-source.js\'s own priority order (auto DAR before manual Controls/Labor).\n\nGap 2 -- the real ask: hourMetrics() (src/views/graded-visits.js), which drives the hourly DAR table/export/day-vs-visit-hour summary, had no tpph field at all -- OEPE/DT/CTP/R2P/KVS/Labor% all existed at hour_slot grain, TPPH didn\'t. Added `tpph = transactions / actual_punched_hours`, deliberately ALL-CHANNEL (`x.transactions`), not drive-thru-only (`x.dt_trans_cnt` is a different, smaller number, named explicitly per the dispatch\'s "label the denominator deliberately" instruction). Ratio of sums, not average-of-hourly-ratios, comes for free: hourMetrics() runs once on a single hour\'s raw row and once on aggregateHours()\'s already-summed pseudo-row for the Day total -- both `transactions` and `actual_punched_hours` were already in `_SUM_FIELDS`, so the existing sum-then-divide pattern (same as laborPct/oepe) applies with no pipeline changes. New METRICS entry surfaces it in the hourly table/export next to Labor %.\n\nNo loader field-map regen needed -- both `transactions` and `actual_punched_hours` are pre-existing raw DAR fields already read elsewhere in the same function; nothing new was added to a loader.\n\n4 new tests: all-channel division, explicit dt_trans_cnt-vs-transactions distinction (would catch a future accidental swap), null on zero hours, and 0-not-null on real hours with genuinely zero transactions (a real "closed but staffed" signal the overnight/close-down analysis needs to see, not a data gap to hide).\n\n1452/1452 tests pass across 136 files (1448 on main + the 4 added here) -- measured in a clean checkout.',
]};
