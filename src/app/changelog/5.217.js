// @ts-nocheck
export default {version:'5.217', date:'2026-08-27', changes:[
  'Dispatch #171 -- Projections vs Actuals custom date-range picker (live Feature Request read '
  + 'from Supabase `feature_requests`: "Add date range picker... Ex: I would like to be able to '
  + 'select an entire month."). `ProjectionVsActualsReport` (src/views/analytics.js) previously '
  + 'had only three fixed [2,4,6]-weeks-back presets; `runBacktest()` is fundamentally week-'
  + 'shaped (it only ever evaluates complete Wed-Tue business weeks), so a custom range needed an '
  + 'explicit, stated rule for how an arbitrary pick maps onto week boundaries -- a calendar month '
  + 'never divides evenly into Wed-Tue weeks.\n\n'
  + 'Chose Option A (weeks that OVERLAP the picked range) over Option B (only weeks fully '
  + 'CONTAINED within it): Option B would silently drop the partial weeks at a range\'s start/end '
  + '-- exactly the FR\'s own "select an entire month" example, where several days at each end '
  + 'would vanish from a report with no on-screen explanation. Option A never drops a day the user '
  + 'asked to see, at the cost of a few extra days from just outside the picked range showing up '
  + 'inside whichever business week they fall in -- and the report now states exactly which weeks '
  + 'it evaluated (start/end dates + count), not just a bare success state, so that tradeoff is '
  + 'always visible, not implicit. Verified live: picking July 2026 (the FR\'s own "entire month" '
  + 'case; July 1 2026 IS a Wednesday) resolves to exactly 5 Wed-Tue weeks, the last of which runs '
  + 'to Aug 4 -- past the picked end date, by design.\n\n'
  + 'New `resolvePvsaCustomWeeks()` routes through the shared `weekStartOf()` (utils/date.js) '
  + 'rather than hand-rolling getDay() arithmetic -- this same file already made that exact '
  + 'mistake once (DialedInComparisonReport, #367), and week-start.test.js\'s ratchet on hand-'
  + 'rolled week-boundary computations (ceiling 9) would have failed on a new one. A 26-week '
  + '(~6-month) safety cap protects the per-store x per-day forecastDay() loop from a runaway '
  + 'multi-year pick; the report says so explicitly ("capped to the most recent 26 weeks") if hit.\n\n'
  + 'UI: additive alongside the existing [2,4,6]W presets (same posture as dispatch #158\'s One-'
  + 'Pager custom-range addition -- neither replaces the other). A new "Custom..." pill reveals a '
  + 'plain start/end `<input type="date">` pair matching THIS SAME FILE\'s own DateRangeReport '
  + 'precedent (~line 6222), per the dispatch\'s explicit reuse instruction -- not the shared '
  + '`DateRangeControl` component, whose day-count presets and arbitrary-calendar-day custom range '
  + 'fit neither this report\'s week-granular engine nor its existing week-count presets (checked '
  + 'memory/panel-contract.md\'s date-mode table first, per the dispatch\'s own instruction not to '
  + 'assume either way; this is the same reasoning dispatch #158\'s own PR already used for a '
  + 'period-anchored panel, not a new exception). "This month" / "Last month" quick shortcuts '
  + 'satisfy the FR\'s literal ask directly; the raw date pair works independently of them. A live '
  + 'preview line shows the resolved week span before Run is even clicked. Every "weeksBack"-only '
  + 'display (table header, KPI tiles, CSV filename, empty/computing state, Run button label) now '
  + 'reads the report\'s actual evaluated week count/mode -- this also fixed a latent bug where the '
  + '"Weeks Analyzed" KPI read the stale preset state unconditionally.\n\n'
  + 'Verification: dispatch-171-pvsa-custom-range.test.js renders the REAL exported '
  + 'ProjectionVsActualsReport (not a reimplementation of the week-resolution math) and proves (1) '
  + 'the [2,4,6]-week presets still resolve to exactly that many weeks, clicked out of declared '
  + 'order -- regression-free; (2) the July-2026 custom range resolves to a DIFFERENT, correctly-'
  + 'computed 5-week span (not just that new UI renders); (3) switching back to a preset after a '
  + 'custom run collapses the date pickers and restores preset behavior; (4) the Run button stays '
  + 'disabled in custom mode until both dates are picked. `ds.laborRows` is deliberately empty in '
  + 'the fixture -- report.weeks is built from date arithmetic alone, before any per-store data '
  + 'lookup, so forecastDay is never even called and no mocking was needed. Full suite: 2986/2986 '
  + '(288 files, +1 new file / +4 tests). Build clean; analytics.js is lazy-loaded (not in the '
  + 'entry chunk) -- entry-chunk gzip 480.60 KB -> 480.62 KB (+0.02 KB), eager total 552.53 KB -> '
  + '552.55 KB, still 297.45 KB under the 850 KB budget.',
]};
