// @ts-nocheck
export default {version:'5.217', date:'2026-08-27', changes:[
  'Dispatch #174 -- Performance Review Sales actual (mo.salesVsTgt) still manual-only, silently '
  + 'blank past July (owner report, LIVE bug, follow-up to the same symptom #159 was supposed to '
  + 'fix). Measured before fixing: the live `labor_rows` table\'s most recent upload is '
  + '2026-07-23 -- nearly 5 weeks stale as of this dispatch, with July itself only partially '
  + 'covered. review-engine.js\'s autoPopulateKPIs() set mo.salesVsTgt from sum(lr,\'sales\'), '
  + 'hand-filtering ONLY the manual ds.laborRows stream, with zero auto/cloud fallback -- the '
  + 'exact bypass CLAUDE.md\'s auto-first rule exists to prevent, and the same bug class #159 '
  + '(OEPE/R2P/KVS/Labor%) and #161 (Food Cost actual) already fixed for every other Performance '
  + 'Review metric, but never for Sales actual itself. A compounding effect: mo.foodOBTgt (the FOB '
  + '$ TARGET) derives from mo.salesVsTgt, so this same gap silently blanked the FOB dollar target '
  + 'too for any month Sales actual was unset, on top of #161\'s already-fixed FOB actual.\n\n'
  + 'Fix: mo.salesVsTgt now sums metric-source.js\'s already-registered, auto-first \'sales\' chain '
  + '(qsrActSummaryRows sales/allNetSales, THEN ds.laborRows last -- ds.laborRows was already its '
  + 'own last-resort fallback in that chain) via Object.values(metricSeries(ds, loc, range, '
  + '\'sales\')).reduce(...), the same pattern already used in src/views/sage.js and src/views/'
  + 'store-analytics.js -- not a new aggregation helper. `range` is the SAME calendar-month range '
  + 'already computed for the oepe/r2p/kvs/laborPct calls right next to this edit, reused rather '
  + 'than recomputed. The old unconditional sum(lr,\'sales\') stays as an explicit fallback, only '
  + 'when the auto path\'s per-day resolution returns nothing at all for that month -- same auto-'
  + 'then-manual precedence direction as foodOB\'s own #161 fix immediately below it in the same '
  + 'loop. Confirmed unaffected, not merely assumed: the separate, already-dead-per-its-own-'
  + 'comment salesVsTgtTgt/laborTgt manual-only TARGET fallback (dispatch #142) still reads the '
  + 'unrelated lr-based salesTgt/laborTgt fields and never touches the ACTUAL-side salesVsTgt this '
  + 'dispatch changed; mo.foodOBTgt needed no code change at all -- it already re-resolves '
  + 'correctly the moment mo.salesVsTgt does, since it reads that same variable later in the same '
  + 'per-month loop iteration.\n\n'
  + 'Verification: new dispatch-174-review-sales-auto-source.test.js (7 tests) exercises the real '
  + 'autoPopulateKPIs, fixtures shaped like #159\'s own convention -- each real side of the gap. A '
  + 'month with an auto qsrActSummaryRows day and NO ds.laborRows row at all now resolves '
  + '(the actual regression case); a genuinely manual-only store/month (only ds.laborRows, no '
  + 'auto source) still resolves unchanged via the SAME chain\'s own built-in laborRows fallback '
  + '-- no regression; the auto source wins per-day over a diverging manual figure on the SAME '
  + 'date (metric-source.js\'s own precedence, not a month-level all-or-nothing choice), while a '
  + 'day auto has nothing for but ds.laborRows covers still contributes to the monthly total (the '
  + 'chain blends per day, this new code\'s own explicit fallback branch only ever fires when the '
  + 'chain resolves NOTHING for the whole month); another store\'s auto rows never leak into this '
  + 'review\'s loc; a dead legacy salesTgt field on a labor row never contaminates salesVsTgt; and '
  + 'mo.foodOBTgt now resolves correctly (officialTgts.tFOBTarget * salesVsTgt) for an August month '
  + 'that was previously blank under the bug.\n\n'
  + 'Full suite: 2989/2989 (288 files, +1 new file / +7 tests). Build clean; entry-chunk gzip '
  + '480.60 KB -> 480.63 KB (+0.03 KB, metricSeries was already bundled via metric-source.js\'s '
  + 'existing metricAvg/metricRate imports in this same file -- no new module pulled in), eager '
  + 'total 552.53 KB -> 552.56 KB, still 297.44 KB under the 850 KB budget.',
]};
