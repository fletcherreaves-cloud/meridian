// @ts-nocheck
export default {version:'5.159', date:'2026-08-25', changes:[
  'Dispatch #116: fixed the FOB Analysis (Food Cost) panel\'s mobile scroll bug -- the '
  + 'Contributors table could render entirely below the fold on a short viewport with no way to '
  + 'reach it.\n\n'
  + 'FOBAnalysisPanel (src/views/analytics.js, the "🥗 FOB Analysis" modal) stacked its title bar, '
  + 'KPI cards, the Root-Cause Priority Matrix, and the Waste-Entry Discipline section as fixed-'
  + 'height siblings, with only the final block -- the Contributors table -- set to '
  + 'flex:1,overflowY:\'auto\' inside an ancestor with overflow:\'hidden\'. On a short mobile '
  + 'viewport the fixed stack above the table could alone exceed the available height, squeezing '
  + 'the table toward zero -- and because the ancestor clips rather than scrolls, there was no '
  + 'affordance to reach it. Reproduced against a real Chromium render (not reasoned about): a '
  + 'realistic wheel/touch-scroll gesture at 390x660 left the table at the same unreachable '
  + 'position before and after the gesture (scrollIntoView alone is not a valid proxy here -- it '
  + 'can move an overflow:hidden scrollport programmatically in a way no real touch/wheel input '
  + 'can, which is exactly why the bug read as fixed on paper before this).\n\n'
  + 'Fix (approach 1 from the dispatch -- the "make the whole content column scroll as one '
  + 'region" option, chosen as the simplest one with the least risk to the desktop layout; the '
  + 'per-section collapse alternative was not needed once one scroll region covers everything): '
  + 'KPI cards + Root-Cause Priority Matrix + Waste-Entry Discipline + Contributors table now '
  + 'live inside one new wrapper (flex:1,minHeight:0,overflowY:\'auto\') below the title bar. Each '
  + 'section keeps flexShrink:0 so it sizes to its own content inside the scrolling column '
  + 'instead of being squeezed; the table\'s own div drops its now-redundant flex:1/overflowY '
  + '(the wrapper around it scrolls instead). Title bar stays pinned outside the scroll region. '
  + 'Pure layout/overflow change -- FOB_COMP, computeFOBMetrics, the Base Food % tolerance work '
  + '(dispatch #115), and computeStoreDataDiscipline are untouched, and no global CSS class was '
  + 'added.\n\n'
  + 'Verified against a real browser, not just reasoning about the CSS: a throwaway dev harness '
  + '(mounted FOBAnalysisPanel with mock stores/fobRows/wasteRows via createRoot, discarded '
  + 'before commit) rendered at 390x660. Pre-fix, a realistic wheel-scroll gesture over the panel '
  + 'left the Contributors table\'s bounding rect unchanged and off-screen (top 1609.75, not '
  + 'visible) before and after -- confirming the reported bug reproduces. Post-fix, the identical '
  + 'gesture brought the table fully into view (top 219.75). Desktop (1440x900) confirmed '
  + 'unaffected: the table renders directly in the initial viewport with no scrolling needed, no '
  + 'double scrollbar (computed style chain shows exactly one overflow:auto ancestor with '
  + 'scrollHeight > clientHeight -- the new wrapper -- and the outer modal box\'s overflow:hidden '
  + 'no longer clips anything since its content now fits), and the existing click-a-row-to-'
  + 'expand-location-breakdown interaction in the Contributors table still works (asserted via a '
  + 'real click + DOM check, not inferred).\n\n'
  + 'Full suite green (229 files / 2376 tests -- unchanged from main, this is a layout-only diff '
  + 'with no new tests). Build clean. Entry chunk unchanged (1568.32 KB / ~456.9 KB gzip -- '
  + 'analytics.js is a lazy-loaded panel chunk, not part of the eager entry). That lazy chunk '
  + 'itself: 338.62 KB -> 338.72 KB raw, 92.70 KB -> 92.72 KB gzip (three added comment lines and '
  + 'one wrapper div, as expected for a near-zero-byte layout change).',
]};
