// @ts-nocheck
export default {version:'5.202', date:'2026-08-27', changes:[
  'Fixed: Performance Review editor was silently broken against dispatch #152\'s (v5.197) shipped '
  + 'per-year data model -- confirmed six live regressions (dispatch #157) by reading the code, not '
  + 'assuming: (1) the KPI/Behavioral tabs were permanently stuck on H2\'s months (review.half is '
  + 'undefined on every #152-era review, so halfMonths(undefined) always returned H2), with no way '
  + 'to reach H1/Q1-Q4 at all; (2) the status pill and score column were always blank/Draft '
  + '(scores.half and review.status no longer exist -- status moved to review.periods.h1/h2.status, '
  + 'scores moved to scores.q1/q2/q3/q4/h1/h2/year); (3) every Submit/Approve/Return/Reopen click '
  + 'called transitionReview(id, newStatus, notes) -- 3 args -- against the engine\'s new 4-arg '
  + 'transitionReview(id, half, newStatus, notes), shifting newStatus into the half slot and writing '
  + 'to a garbage review.periods[\'submitted\'] key instead of periods.h1/h2; (4) ReviewList\'s '
  + 'Period/Score/Half-filter columns all read the dead r.half; (5) NewReviewForm still rendered a '
  + 'Period (H1/H2) dropdown that did nothing; (6) the three print functions derived half from '
  + 'review.half internally.\n\n'
  + 'Priority 1 fix: a real Q1/Q2/Q3/Q4/H1/H2/Year period selector (PERIOD_META, this file) now '
  + 'drives the KPI tab, Behavioral tab, header score pill, and print exports off computeScores\' '
  + 'real {q1,q2,q3,q4,h1,h2,year} shape. Status/approval is fixed to read AND write '
  + 'review.periods.h1/h2.status correctly -- StatusActionBar (new, standalone component so its '
  + 'local state survives rendering two independent instances side by side for a \'Year\' '
  + 'selection) calls the now-correct 4-arg doTransition(half, newStatus, notes) chain; a quarter/'
  + 'half selection shows exactly the one half it maps to, \'Year\' shows both h1 and h2 '
  + 'independently. ReviewList\'s dead Half filter is removed (a review record no longer has a '
  + 'record-level half to filter BY); its Status filter now uses the engine\'s own '
  + 'reviewSummaryStatus() (already shipped by #152 for exactly this coarse-filtering purpose), its '
  + 'Period column becomes Year, its Score column shows the real year-overall, and its Status '
  + 'column shows both halves\' real state side by side (HalfStatusSummary). NewReviewForm\'s dead '
  + 'Period dropdown is removed outright, not wired up (a review is created once per person-year '
  + 'now, no half at creation). printBlankForm/printReview now take an explicit `period` parameter '
  + 'instead of reading review.half; printReview\'s own ScoreBreakdownPanel sub-component was found '
  + 'independently broken the same way (computeScoreBreakdown\'s new per-period return shape meant '
  + 'bd.categories was always undefined, so the whole "Score Breakdown" transparent-math panel '
  + 'silently rendered nothing) -- also fixed, indexed by the selected period.\n\n'
  + 'Priority 2: surfaces dispatch #154\'s segmented-scoring engine (computeSegmentedReview), '
  + 'shipped with zero UI. SummaryTab\'s new SegmentedReviewSection renders NOTHING when '
  + 'hasTransitions is false (the common case, verified by a dedicated render test); when true, it '
  + 'shows each segment\'s own role/store/scores, the provisional weighted rollup with the engine\'s '
  + 'own "starting point, not final" note text, a caption on the known autoPopulateKPIs actuals-'
  + 'not-segment-aware limitation, and a free-text reviewer-commentary field '
  + '(comments.segmentRollup.<period>, a new leaf under the review\'s existing comments.* free-text '
  + 'structure -- no engine or blankReview change needed, ReviewEditor\'s generic update(path,val) '
  + 'creates it on write and it round-trips through the existing onSave -> upsertReview path).\n\n'
  + 'New: staff_assignments (dispatch #150\'s reports-to graph) gets its first client-side reader -- '
  + 'loadStaffAssignments() (src/lib/supabase.js), loaded eagerly into ds.assignmentRows alongside '
  + 'coachingCycles (small, RLS-scoped table), feeding computeSegmentedReview.\n\n'
  + '5 new render-based tests (dispatch-157-*.test.js): Submit->Approve on H1 changes '
  + 'review.periods.h1.status while h2 stays draft (the test that catches the 3-vs-4-arg bug); '
  + 'switching H1/H2 shows the correct months/scores for each; the flat (hasTransitions:false) case '
  + 'renders no segment UI; a segmented review (synthetic mid-year transfer) renders both segments '
  + 'plus the provisional rollup and its framing text; reviewer commentary on the rollup saves and '
  + 'reloads correctly.',
]};
