// @ts-nocheck
export default {version:'5.157', date:'2026-08-25', changes:[
  'Dispatch #112: Weekly Count Cadence -- count-day fix, Last Count column, per-class Food/'
  + 'Condiment/Paper uncounted columns, full-class drilldown.\n\n'
  + 'Item 1 (count-day population): `detectedWeekdayName` (`cadenceFromOnHand`, eom-dashboard.js) '
  + 'used to derive only from sessions that fully cleared the 98% Food+Condiment bar. Measured live '
  + '(service-role read, real qsr_onhand, period 2026-08, single-period-scoped to match the real '
  + 'app\'s loadQsrOnHand({period}) call, 27 stores) BEFORE changing anything: that basis populated '
  + '2/27 stores (7.4%). Broadened to any touchedWeekly session (a real Food/Condiment attempt, no '
  + '98% requirement) -- re-measured at 27/27 (100%), hand-checked against real session dates for an '
  + 'obvious weekday pattern. Status grading (weeklyDone/lastWeekly/Overdue, dispatch #97\'s '
  + 'deliberate 98% bar) is completely untouched.\n\n'
  + 'Item 2: new `lastCount` field/column -- the literal most-recent count-activity date, any size, '
  + 'any class, no filter, per the owner\'s explicit "even if incomplete" ask. Measured live against '
  + 'the two other candidate readings already in the codebase (`lastAttempt`\'s size-priority pick, '
  + '`cycleCompliance`\'s `lastAny`): 19/27 real stores disagreed among the three.\n\n'
  + 'Item 3/4: per-class Food/Condiment/Paper uncounted-item columns + a full-class (incl. Paper) '
  + 'missed-item drilldown. Food/Condiment reuse dispatch #98\'s own `cycleClassCoverage()` helper '
  + '(already in this file) rather than re-deriving a third time. Paper is gated on '
  + '`cycleCompliance()`\'s `paperMissing` (reused unchanged -- mid-month-Paper detection is NOT '
  + 'reimplemented) and reads null/N-A outside its due window, never a misleading 0.\n\n'
  + 'The dispatch\'s own writeup suggested sourcing Paper\'s count from `perClassCounted()`\'s '
  + 'active-minus-counted -- implemented, then measured live against 7 real paperMissing stores and '
  + 'found two real bugs: (a) cross-period leakage (a store untouched THIS period but touched LAST '
  + 'period read as partially counted, not fully outstanding -- live example loc 43701) and (b) '
  + 'multi-session undercounting (Paper counted across two dates in the same period only ever '
  + 'showed the most recent date\'s count -- live example loc 38609, 16+13 items on two dates '
  + 'undercounted to 13). Fixed by sourcing the count/item-list from `diagnoseIncompleteCount()` '
  + '(already reused for Food/Condiment) windowed at the current period\'s start instead, pre-'
  + 'filtered to count-cycle.js\'s own `isActive()` (now exported -- a one-line addition, no '
  + 'behavior change for any existing consumer) since that engine has no active/recipeItem '
  + 'filtering of its own. Both regressions reproduced as dedicated unit tests. A separate real bug '
  + 'found while wiring this up -- `cycleClassCoverage()` misreading a Paper-only `missing` entry as '
  + '"Food/Condiment fully counted" -- is fixed and regression-guarded too.\n\n'
  + 'CadenceMonitor table gains two columns (Last count, Uncounted F/C/P chips matching the existing '
  + 'ClassChips color-coding/hover-detail pattern); the drilldown\'s "still uncounted" header now '
  + 'anchors on `lastCount` instead of the Food/Condiment-specific `lastAttempt`, since Paper can be '
  + 'the only thing missing. Per-class uncounted counts and the Paper item list hand-verified against '
  + 'raw qsr_onhand rows for real stores with a genuine gap in each class (5183/Food, 33222/'
  + 'Condiment, 43701/Paper).\n\n'
  + 'Panel-contract standing rule (2026-08-25): since this dispatch was already widening '
  + 'CadenceMonitor\'s table 5 -> 7 columns, also wrapped it in the shared `overflowX:\'auto\'` + '
  + '`width:\'max-content\'`/`minWidth` scroll pattern (it had neither before) so it scrolls on '
  + 'mobile instead of clipping -- verified against `scroll-table-width.test.js`\'s ratchet.\n\n'
  + '10 new tests (src/__tests__/dispatch-112-count-cadence.test.js); full suite 2393/2393 passing '
  + '(231 files) including the pre-existing dispatch-97/98 suites unchanged; build clean. Entry gzip '
  + '456.87 -> 456.89 KB, eager-payload 528.01 -> 528.03 KB (budget 850 KB) -- noise-level. '
  + 'Full resolution: memory/dispatch-112.md Resolution section.',
]};
