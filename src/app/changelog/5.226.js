// @ts-nocheck
export default {version:'5.226', date:'2026-08-28', changes:[
  'Dispatch #184 -- wire the eBOS "Raw Item Info" endpoint (raw_info/{itemId}) the owner captured '
  + 'live via DevTools: recipe/serving-factor (BOM), combo composition, and current distributor '
  + 'cost, per raw item. A genuine sibling of the already-working raw_detail pull in '
  + 'scripts/qsrsoft-variance-pull.mjs -- same auth, same actionable-WRIN loop (top-50 by |$| '
  + 'variance, dispatch #179), reused verbatim, not a new selection. Pull + storage only, per the '
  + 'dispatch\'s own scope -- no Pricing Engine UI/panel in this pass.\n\n'
  + 'raw_detail and raw_info are now fired CONCURRENTLY per item (Promise.allSettled), not '
  + 'sequentially -- keeps the per-store loop close to its old wall time instead of roughly '
  + 'doubling it. Measured baseline (this workflow\'s own GitHub Actions run history, no live eBOS '
  + 'credentials this session): 27 recent scheduled runs, 118s-484s, avg 220s, against a 60-min '
  + 'timeout -- worst observed run uses 13% of budget. Even a full (non-concurrent) doubling of '
  + 'that worst run stays under 30% of budget, so this addition has a wide, measured margin. A '
  + 'real workflow_dispatch run on this PR branch (QSRSOFT_DEBUG=1, single store) confirmed the '
  + 'new call lands real raw_info rows in qsr_raw_item_info alongside the unaffected raw_detail '
  + 'rows in qsr_raw_item_detail -- see PR body for the run link/log lines.\n\n'
  + 'New table qsr_raw_item_info (supabase/schema-qsr-raw-item-info.sql), keyed (loc, wrin) only '
  + '-- NOT (loc, period, wrin) like its sibling -- because raw_info is a CURRENT-STATE snapshot, '
  + 'not a per-period forensic log; a re-pull just overwrites the latest known values. Promoted '
  + 'columns (full_wrin, long_desc, invty_category_type, case_qty, latest_case_price, '
  + 'case_price_avg, primary_vdr_name, primary_vdr, mid_range_yield, recipe_item, current_upt) + '
  + 'JSONB arrays (menu_items, menu_item_combos, upt_hist), matching qsr_raw_item_detail.history\'s '
  + 'JSONB precedent. tenant_id + tenant-scoped RLS (select/insert/update/delete on '
  + 'tenant_id = current_tenant_id(), same as every other stream post multi-tenant migration) set '
  + 'up directly in one step, since this is a brand-new empty table -- no phase1/phase2 split '
  + 'needed. New mapRawItemInfo() in src/engine/eom-parsers.js, same zero-drift pull/client parser '
  + 'sharing convention as every other eBOS mapper here.\n\n'
  + 'Side-check (dispatch #184 item 3, not a blocker): could not be measured live this session (no '
  + 'eBOS credentials) -- left as an open question for whoever runs the first live pull to note, '
  + 'not assumed either way.\n\n'
  + 'New-automated-pull checklist (CLAUDE.md): (1) sync-failure-watch.yml -- already covers this, '
  + '"QSRSoft Variance Pull (EOM diagnosis data)" is the SAME workflow/script as the existing '
  + 'raw_detail pull, no new entry needed. (2) stream-freshness.js STREAMS -- measured, not '
  + 'assumed: qsr_raw_item_info\'s own sibling qsr_raw_item_detail (and qsr_variance_stat/ '
  + 'qsr_waste/qsr_transfers) are NOT in STREAMS either and have zero App.js ds call sites -- '
  + 'confirmed by grep. This whole EOM/variance data family is loaded on demand by the EOM '
  + 'dashboard panel (src/lib/supabase.js loaders), outside the ds/STREAMS architecture entirely, '
  + 'so a STREAMS entry for just this one new table isn\'t a fit without first wiring the whole '
  + 'family into ds -- a real, separate, bigger change, out of this dispatch\'s scope. (3) '
  + 'Supabase table + tenant_id + RLS: done, above. (4) manual fallback: N/A, this data has no '
  + 'manual-upload equivalent, same as its sibling.\n\n'
  + 'New tests: 2 added to src/__tests__/eom-parsers.test.js (mapRawItemInfo field mapping + safe '
  + 'defaults on a sparse response). Full suite: 3039/3039 (292 files, +2 tests). Build clean; '
  + 'eager total 552.72 KB, 297.28 KB under the 850 KB budget (no client-loaded code touched -- '
  + 'this is a pull-script + engine-module change only).',
]};
