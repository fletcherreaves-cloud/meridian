// @ts-nocheck
export default {version:'5.095', date:'2026-08-21', changes:[
  'Dispatch #56 Parts A and C -- two owner asks on the Security panel, both scoped as cheap and '
  + 'independent, shipped together in one PR (Parts B/D/E remain investigation-first or a real '
  + 'build, out of scope here).\n\n'
  + 'Part A: a rule directory in the Legend ("let\'s add a directory of what each policy covers"). '
  + 'Collapsed by default under the existing 8 vocabulary rows -- renders ENTIRELY from the live '
  + '`rules` array (loadSecurityRules()\'s own security_rules read), never a hand-written list, so '
  + 'a rule added to the table tomorrow appears here with no code change. Per rule: method + '
  + 'ruleId, description, baselineType, logicType + windowDays, investigationAction ("say the '
  + 'number AND the decision"), false_positives, severity, and active state -- an inactive rule '
  + 'is LISTED, not hidden, marked with the legend\'s own existing ⏸ wording. Both domains shown '
  + 'regardless of the panel\'s cash/inventory tab, since this is reference material ("is there a '
  + 'rule that covers X?") a tab-filtered view can\'t answer. loadSecurityRules() gained the one '
  + 'field mapping it was missing (false_positives), plus corroboration_rules/exoneration_rules -- '
  + 'the Part D "free win" the dispatch itself calls out (populated in security_rules, dropped by '
  + 'the loader until now) -- surfaced in the directory as "Corroborates with" / "Weakened by"; '
  + 'the finding-level cross-link Part D describes is not built here.\n\n'
  + 'Part C: inventory findings showed a bare WRIN ("Item CUP (store 0000001)"), forcing every '
  + 'reader to go look up what the item actually is before they can think about the finding. Now '
  + 'shows qsr_variance_stat.descr as the heading with the WRIN as a secondary identifier, plus '
  + 'the item class (cls) as a small badge -- lifecycle_category was already surfaced via the '
  + 'existing hygiene-verdict routing, nothing new needed there. Joined on (loc, wrin, period), '
  + 'never (loc, wrin) alone -- dropping period inflated a real join ~3.5x during the 0013113 '
  + 'investigation (658 rows vs the correct 188). `period` is the subject\'s own latest inventory '
  + 'verdict windowEnd sliced to \'YYYY-MM\' -- factored into a new inventoryItemKey() helper so '
  + 'the existing drill-down (dispatch #52\'s SubjectDrilldown) and this new lookup can\'t drift '
  + 'apart; SubjectDrilldown\'s own inline period derivation now calls the same helper instead of '
  + 'duplicating it.\n\n'
  + 'Fetches on viewing the Inventory tab (one loadQsrVarianceStat({period}) call per distinct '
  + 'period actually present among loaded findings, cached per-period so switching tabs back and '
  + 'forth does not refetch) -- a real, if small, behavior change from dispatch #43\'s pure '
  + '"nothing fetches before a click" discipline, which previously applied only to the heavier '
  + 'drill-down population/history pull. The dispatch #52 pinned test was updated, not weakened, '
  + 'to prove BOTH halves explicitly: the item-name preload fires once on tab view, and the '
  + "drill-down's own fetch still only fires on the explicit \"Investigate further\" click, not "
  + 'merely from expanding a row.\n\n'
  + '1880/1880 tests (6 net new -- 5 for the rule directory including the anti-hardcode fixture '
  + 'check, 1 for the product-name render). Build clean, entry chunk 1717.61 KB / 510.69 KB gz vs. '
  + '1717.49 KB / 510.68 KB gz on main before this PR (security-panel.js is already lazy -- '
  + 'essentially flat). Full writeup: memory/dispatch56-parts-a-c.md.',
]};
