// @ts-nocheck
export default {version:'5.023', date:'2026-08-16', changes:[
  'Issue #276 step 2 — mechanical follow-up to step 1: converted the remaining lone-red (no adjacent amber) #f87171 sites to var(--crit), and their rgba(248,113,113,X) tint twin (same colour, decimal notation) to rgba(244,63,94,X) -- 274 hex + 58 rgba replacements across 33 files, script-applied exact-value substitution, no manual judgment calls per site. Scope held deliberately narrow to what step 1 actually fixed: only the exact old status-trio red (#f87171 / 248,113,113) moved. The separate #ef4444 / rgba(239,68,68,...) red (Tailwind red-500, used throughout the app for generic delete/error/danger UI -- buttons, badges, tags -- never part of the good/warn/crit status trio ΔE problem #276 was about) was deliberately left untouched everywhere, including sites that sit next to a converted --crit value, since unifying two historically-distinct reds is a real design decision, not a mechanical one, and out of this issue\'s scope -- filed separately as its own issue per PM review, with measured ΔE evidence that the untouched red is exactly as close to amber as the one this step replaced. Four exclusions, each for a different reason: changelog-data.js and changelog-latest.js\'s own #f87171 mentions are historical prose describing step 1\'s fix, not live style code -- rewriting them would falsify the record; meridian.css:13\'s comment is the same kind of historical note; src/meridian.js\'s two #f87171 uses are in the CDN-load-failure and startup-error fallback HTML strings, which render before (or instead of) the app\'s own stylesheet is guaranteed loaded -- switching those to var(--crit) would risk the error message itself losing its red on exactly the failure path where meridian.css failed to load, so they stay literal hex on purpose. Verified via node --check-equivalent grep sweep: exactly 4 files still contain #f87171 afterward (the 4 exclusions above), zero elsewhere. Browser-verified: dev server load, getComputedStyle(document.documentElement) confirms --crit still resolves to #f43f5e, zero console/page errors.\n\nRenumbered 5.013 -> 5.023 and rebased onto current main at merge time -- main had moved through v5.022 (including three independent white-alpha/color-token passes, #295/#296 steps 1-2, that landed on lines this PR also touched) while this branch sat open. Six real conflicts, all the same shape: this PR\'s red-hex-to-token conversion on one half of a style object colliding with #296\'s independent rgba(255,255,255,X)-to-var(--bdr)/var(--text) conversion on the other half of the SAME line -- resolved by keeping both conversions together (e.g. analytics.js/at-a-glance.js/store-analytics.js/store-dash.js\'s border-radius-card blocks: red fixed to var(--crit)/rgba(244,63,94,X) per this PR, adjacent border/background rgba(255,255,255,X) sites left at #296\'s already-landed var(--bdr)/var(--text) rather than reverted). 1371/1371 tests pass, build clean.',
]};
