// @ts-nocheck
export default {version:'5.052', date:'2026-08-18', changes:[
  'Issue dispatch16 R1 -- first of three parallel ratchet lanes (the strategic fix behind dispatch16: nine of the last fifteen defects were five recurring patterns turning up in a file nobody had checked, each with a working fix already in the repo that nothing enforced). R1 bans panels from reading raw metric rows (ds.laborRows/ctrlRows/opsRows, + ds?. forms) directly -- src/engine/metric-source.js exists precisely so a metric comes from ONE place (auto stream first, manual fill last resort), and a direct raw-row read in a panel re-introduces the manual-only blank/-100%/-32% bug class that helper was built to retire.\n\nScoped to src/views/ ONLY, per dispatch16\'s explicit instruction -- src/features/session.js is the PIPELINE layer where `ds` itself gets assembled, and reading raw rows there is its job, not a violation. The rule is panels must not source metrics from raw rows; it was never that raw rows are untouchable anywhere.\n\nSeed re-measured fresh against current main rather than trusting the dispatch\'s own "~162/13 files at v5.034" estimate (main has moved since): landed on the identical 162 occurrences across 13 files, so no drift occurred in the interim. New ratchet-raw-metric-rows.test.js fails in BOTH directions, the property that makes a ratchet worth having rather than decorative -- confirmed by hand: a planted new violation (163rd occurrence) fails naming the exact file:line; an artificially raised ceiling (163 when the real count is 162) fails with "stale ceiling, lower to 162" rather than staying silently green.\n\nAlso folds in R2 (one definition of who supervises a store) per the dispatch\'s own instruction ("R2 is ten minutes -- fold it into whichever lane finishes first") -- #375\'s existing supervisor-live-resolution.test.js already IS a ceiling=0 census scanner for the two literal shapes that shipped as #363 (a quoted sup:/supEmail: literal, and a name-keyed locs-array literal), so no new file was needed; verified it still passes and documented it as satisfying R2\'s requirement rather than rebuilding it.\n\n1418/1418 tests pass (2 new), build clean (no bundle-size shift -- a new __tests__ file, never bundled).',
]};
