// @ts-nocheck
export default {version:'5.198', date:'2026-08-27', changes:[
  'Leadership One-Pager: fixed OEPE/R2P/TPPH silently blending an in-progress "today" into '
  + '"current week" figures at full weight, a completeness artifact found by an overnight audit '
  + '(dispatch #153, unrelated to the Performance Review continuity thread). '
  + '`qsr_daily_activity_rollup` always carries the full 24-hour_slot shape even for a day still '
  + 'in progress -- future hours are zero-filled, not absent -- so a day that is 30% through is '
  + 'structurally indistinguishable from a complete one to `metricAvg`\'s mean-of-daily. Live-'
  + 'measured 2026-08-27 against production (store 3708): 2026-08-26 sat at 744 of 1,061 '
  + 'projected transactions (70.1% of plan) yet its own-day R2P read 92.3s against 129.7-255.6s '
  + 'the rest of that 8-day window -- a completeness artifact, not a real speed win.\n\n'
  + 'Extends the already-proven `metricSumRatio` pattern (`kind:\'ratio\'`, previously only tpph '
  + 'and 9 other metrics) to oepe and r2p, rather than inventing a new completeness-tracking '
  + 'mechanism. `qsrActSummaryRows` already carries the raw per-day sums '
  + '(`_dtTotal`/`_dtStore`/`_dtHeldTime`/`_dtCars` for OEPE, `_fcServe`/`_fcDrawer`/`_fcCnt` for '
  + 'R2P) that `src/utils/oepe.js`\'s `oepeSeconds()` and `supabase.js`\'s `_finalizeQsrAct` '
  + 'already use to compute the precomputed `oepe`/`r2p` fields on that same row -- exposed as '
  + 'seven new one-source METRIC_SOURCES chains (`dtUntilServeUs`/`dtUntilStoreUs`/`dtHeldTimeUs`/'
  + '`dtTransCnt`/`fcUntilServeUs`/`fcUntilClosedDrawerUs`/`fcTransCnt`) plus two new numerator '
  + 'derives (`oepeNumSec`/`r2pNumSec`), so `oepe`/`r2p` now declare a real `derive: {inputs:['
  + 'num,den], kind:\'ratio\'}` pair mirroring tpph\'s exact shape. Live cross-check (service-role '
  + 'Supabase REST, store 3708, 2026-08-24 -- a known-complete day at 91.9% of plan): the new '
  + 'chain\'s single-day Σnum/Σden reproduces the existing formula within Math.round\'s own '
  + 'tolerance for OEPE (124 vs 124.3693, both round to 124) and exactly for R2P (129.6544 == '
  + '129.6544). mode:\'any\' throughout on the new raw legs -- a genuine zero-held-time day is '
  + 'common and \'pos\' would have silently dropped it from the sum, the same zero-discarding class '
  + '#150/#178 already fixed for park/kvsHealthy.\n\n'
  + '`src/engine/one-pager-data.js`: `buildCurrentState`/`buildMetricNow`/`buildPerLocationRows` '
  + 'now read oepe/r2p/tpph through a new `rateMetric()` helper -- `metricSumRatio` first, '
  + 'falling back to `metricAvg` ONLY when `metricSumRatio` returns null (no day in range '
  + 'resolves both raw legs, e.g. a manual-only Ops Report upload with no DAR rows) -- so a '
  + 'number that used to display is never silently dropped. laborPct is untouched (out of this '
  + 'dispatch\'s scope) even though it is already ratio-capable. Other `metricAvg(\'oepe\'/\'r2p\'/'
  + '\'tpph\')` call sites exist outside the One-Pager (morning-brief, review-engine, at-a-glance, '
  + 'signals, labor-tools, sage, store-dash, attention-now, above-store-onepager) -- listed as '
  + 'follow-up candidates in this dispatch\'s PR body, deliberately left unfixed per its own '
  + 'explicit scope.\n\n'
  + 'CLAUDE.md: added a note under the DAR/hour_slot completeness guidance that '
  + '`count(hour_slot)==24` does NOT detect an in-progress day (the DAR zero-fills future hours '
  + 'rather than omitting them) -- the exact gap this dispatch\'s own investigation had to '
  + 'rediscover the hard way.',
]};
