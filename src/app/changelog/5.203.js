// @ts-nocheck
export default {version:'5.203', date:'2026-08-27', changes:[
  'Fixed: the header FOB % tile on both One-Pagers (Leadership and Store) stayed identical no '
  + 'matter which scope was selected -- switching between FL supervisors, OK/FL, a single store, '
  + 'or any patch never changed the number (owner report, with a week of Ops Report exports for '
  + 'reference). Root cause: `fobByRange(fobRows, range)` (engine/one-pager-data.js) has no `locs` '
  + 'parameter -- it returns every store present in `fobRows`, district-wide -- and the shared '
  + '`buildCurrentState()` builder (the source of the header FOB % tile on BOTH One-Pagers) summed '
  + 'every entry in that unfiltered result regardless of the `locs` it was itself called with, '
  + 'silently ignoring its own parameter. A second, separate instance of the identical bug was in '
  + 'above-store-onepager.js\'s own vs-LY delta computation (the "vs LY" sub-line rendered directly '
  + 'under the FOB % tile). Both are fixed: `buildCurrentState` and the Leadership One-Pager\'s own '
  + 'vs-LY block now filter fobByRange\'s result to the locs actually in scope before summing, the '
  + 'same way every other metric in this file already does. New test in one-pager-data.test.js '
  + 'reproduces the exact reported symptom -- two stores at 4% and 8% FOB, selecting each store '
  + 'individually returned the correct distinct number, selecting both returned the true blend -- '
  + 'and would have failed against the pre-fix code (both single-store selections returned the '
  + 'same district-wide number).\n\n'
  + 'Fixed: the "week containing:" date picker (Store One-Pager) and the canonical `weekStartOf()` '
  + 'helper (utils/date.js, used by 16 files across the app) both returned the PREVIOUS week when '
  + 'the picked date was exactly the configured week-start weekday itself (Wednesday, McDonald\'s '
  + 'standard) -- every other day of the week resolved correctly. Root cause: a bare \'YYYY-MM-DD\' '
  + 'string -- exactly what an <input type="date"> hands back -- parses as UTC midnight; every US '
  + 'timezone then reads getDay()/getDate() back as the previous LOCAL calendar day, which is '
  + 'invisible most of the week but pushes the week-start computation a full 7 days back when the '
  + 'picked date IS the boundary day. dKey() in the same file already carries an extensive comment '
  + 'documenting this exact pitfall and guards against it; weekStartOf() (added later) never '
  + 'inherited that guard. Both are fixed to parse a bare date-only string as local midnight. New '
  + 'tests in week-start.test.js pin the exact reported case (2026-08-19, a Wednesday) and confirm '
  + 'every other weekday in that week still maps to the same, correct week start -- the existing '
  + 'test suite\'s own "accepts a date string" case never caught this because it always used a '
  + 'noon-anchored datetime string, which sidesteps the UTC-boundary crossing entirely.\n\n'
  + 'Full suite 269/269 files, 2858/2858 tests (+3 net new). Build clean, entry chunk gzip '
  + 'unchanged at 476.20 KB.',
]};
