// @ts-nocheck
export default {version:'5.174', date:'2026-08-25', changes:[
  'Dispatch #139 — "Mary missing in Crew Schedule": patch-scoped filters/groupings now resolve '
  + 'the current supervisor LIVE everywhere, not just in Settings.\n\n'
  + 'Root cause (confirmed by reading the code): two sources of truth for "which supervisor owns '
  + 'which store." The live, effective-dated timeline (constants.js\'s orgAssignments/whoRan/'
  + 'supervisorGroups(), Settings-editable via management.js\'s SupervisorAssignmentsEditor, kept '
  + 'current in Supabase org_config) was already correct the moment a reassignment saved. But '
  + 'PanelControls.js\'s buildLocationHierarchy() -- the shared function every LocationSelector '
  + 'consumer (Crew Schedule Lookup and 8+ other panels) builds its Patch tier from -- read ONLY '
  + 'the static INV_ORG_COORDS[loc].sup seed, which never moves when Settings reassigns a store. '
  + 'A new supervisor (Mary) was real in Settings and invisible to every picker built on it.\n\n'
  + 'Fix chosen: pointed the single shared buildLocationHierarchy() at the live source (option 1 '
  + 'in the dispatch), rather than patching each of PanelControls\' 9+ individual consumers -- '
  + 'highest leverage, one change reaches Crew Schedule Lookup, Targets Editor, Top/Bottom '
  + 'Performers, Opportunity Dollars, Record Days, Schedule Retention, Forms, and Report '
  + 'Subscriptions at once. New constants.js export supervisorOf(loc, fallback): live-first via '
  + 'whoRan(), falling back to the given (or INV_ORG_COORDS-seeded) value only for a loc the live '
  + 'timeline genuinely does not cover yet -- so an untouched store keeps resolving sensibly '
  + 'instead of going blank. LocationSelector\'s own progressive-mode "which patch is this store '
  + 'under" lookup was ALSO reading invOrgCoords[loc].sup directly (a second, adjacent static '
  + 'read) -- switched to deriving from tree.patches itself, so the highlighted pill can never '
  + 'disagree with the tier it belongs to.\n\n'
  + 'Then fixed the dispatch\'s remaining confirmed call sites individually, since they read '
  + 'INV_ORG_COORDS.sup outside buildLocationHierarchy entirely: security-panel.js\'s scopeMatches '
  + '(fully static -- Security Panel\'s patch scope never matched a live reassignment even though '
  + 'its own picker, now fixed above, offered the patch as an option), smg-voice.js\'s orgMatch + '
  + 'patch-filter-pill list (same), analytics.js\'s District Priority Brief patch pill/filter, '
  + 'store-dash.js\'s Ranking View patch grouping, and labor-tools.js\'s Patch grouping map (this '
  + 'one read settings.supervisorGroups -- a flat snapshot the editor only recomputes AT SAVE '
  + 'TIME -- rather than the always-current supervisorGroups() function every other already-'
  + 'correct panel, one-pager.js/smart-targets.js among them, calls directly; switched to that). '
  + 'Also fixed target-overrides.js\'s scopeIdsForLoc -- not in the dispatch\'s named list, but its '
  + 'own header comment explicitly promises "the SAME source/fields buildLocationHierarchy() '
  + 'reads," which the buildLocationHierarchy fix above would have silently broken (a patch-level '
  + 'target override set for Mary would never have matched her stores).\n\n'
  + 'Untouched, per the dispatch: orgAssignments/whoRan/groupsAt/supervisorGroups() themselves '
  + '(already correct), SupervisorAssignmentsEditor\'s write path (already correct), and pure '
  + 'display-of-already-resolved-data sites (one-pager.js, the One-Pager HTML export label, '
  + 'pipeline.js/inventory.js\'s whoRan()-first display fields -- already live via #363).\n\n'
  + 'New test file (11 tests, dispatch-139-supervisor-patch-live-source.test.js) reproduces the '
  + 'owner\'s report directly: reassigns a real store to a brand-new supervisor name via the same '
  + 'live mechanism the Settings editor uses, then asserts buildLocationHierarchy\'s Patch tier, '
  + 'LocationSelector\'s active-pill highlighting, security-panel\'s scopeMatches, and target-'
  + 'overrides\' scopeIdsForLoc all resolve her -- and that an untouched store keeps its prior '
  + 'supervisor rather than going blank. Full suite 2582/2582 passing (244 files, up from 2571/243 '
  + 'on main -- +11 new); build clean; entry 467.33 KB gzip (467.32 KB on main), eager payload '
  + '538.51/850 KB gzip (538.46 KB on main) -- effectively flat, all touched files were already in '
  + 'the eager bundle.',
]};
