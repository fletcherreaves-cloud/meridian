// @ts-nocheck
export default {version:'5.064', date:'2026-08-19', changes:[
  'Dispatch22, Workstream A (memory/plan-normalization-2026-08-17.md, "the highest-return item") -- moved At A Glance\'s weekly forecast off the render path. weekProjections was making 189 forecastDay() calls (27 stores x 7 days) on every render -- 76,503 ms of 82,221 ms measured render time (93%), up to 4.3s just to close a modal.\n\n'
  + 'forecastDay() ITSELF is unchanged -- zero forecasting-logic change, per the plan\'s own constraint. A new script, scripts/forecast-week-precompute.mjs, calls it once a day server-side for the current week (27 x 7 = 189 calls, same as before, just relocated) and writes the result to a new table, forecast_week_cache (supabase/schema-forecast-week-cache.sql -- OWNER MUST RUN THIS MIGRATION, plus grant a new GitHub Action the usual VITE_SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY secrets it already has). at-a-glance.js\'s weekProjections now reads that cache per store; a store whose current week is FULLY cached skips its 7 forecastDay() calls entirely, a store with a partial/missing cache (precompute hasn\'t run yet, a brand-new store) falls back to the exact live computation unconditionally used before -- a stale or absent cache degrades to today\'s behavior, never a blank panel.\n\n'
  + 'Traced forecastDay\'s own source for what weekProjections actually consumes off its return object -- only forecast/actual/lyAdj; every other field (oepe/tpph/labor/t2/t4/t6/varPct/pass/goal/...) is discarded by this caller. For the model every real store is assigned at the weekly horizon (confirmed live: a mix of ae/ewma/di/simple per the real cloud-persisted user_settings.model_assignments override blob, not just the DEFAULT_MODEL_ASSIGNMENTS \'ae\' the code comments claim), those three fields depend only on ds.laborRows/laborIdx/laborByLoc and ds.qsrActSummaryRows -- not ds.opsRows/ctrlRows/weatherRows/peaksRows, which only feed the discarded fields. That materially shrank what the precompute job needs to load correctly, and every piece of logic it uses is IMPORTED from the real engine (supplementLaborWithSched -- extracted from App.js into src/engine/labor-supplement.js so both the browser and the job call the identical function -- forecastDay, bLocIdx, getModelAssignment), not hand-copied, so a future change to any of it reaches this job automatically instead of silently drifting.\n\n'
  + 'A new exported forecastDay.js helper, fetchRecentActual() (extracted from the \'ae\'/\'ewma\'/\'simple\' branches\' own inline _aeAct/_ewmaAct/_sAct lookups, previously duplicated three times inside forecastDay itself), lets a cache-hit day still get a live-fresh "actual" (manual laborRows first, DAR fallback) without re-running the whole forecast computation just to reach that cheap tail of it -- and without a second hand-copy of the lookup risking drift from what forecastDay itself would compute for the exact same store/day. Verified against real Supabase data before shipping: the precompute script\'s full pipeline (labor_rows, qsr_daily_activity_rollup, user_settings model overrides, all 27 stores x 7 days) runs end-to-end and produces sane numbers across all four real model types -- see memory/dispatch22-workstream-a-forecast-precompute.md for the full trace and the honest caveat that a live before/after render-time click-trace could not be captured in this environment (no browser session) and should be captured by the owner once deployed.\n\n'
  + 'Deliberately its OWN table, not an extension of forecast_snapshots (a backtest/accuracy record with a different grain and no LY column, read by SAGE\'s query_forecast_snapshots tool) -- extending it risked two different writers under one schema and SAGE seeing rows it wasn\'t built to expect. Registered in sync-failure-watch.yml per the standing "new automated pull" rule.\n\n'
  + '1531/1531 tests pass (8 new: 5 labor-supplement, 3 at-a-glance-forecast-cache -- the latter renders the actual AtAGlance panel with a mock cache, not just the engine in isolation, per #366\'s "would this survive a revert of the wiring" lesson). Build clean, 509.21 KB / 850 KB gzip budget.',
]};
