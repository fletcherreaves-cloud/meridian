// @ts-nocheck
export default {version:'5.083', date:'2026-08-20', changes:[
  'Dispatch45 -- min_numerator, lifecycle routing, and an honest investigation into what the z-score dry run left open. Three parts.\n\n'
  + 'Part A: `min_numerator`, a new engine gate (src/engine/security-rules.js). The first live z-score run of INV-001/INV-002 validated the peer-baseline conversion (max stores flagged per WRIN 27 -> 3) but exposed a real gap: INV-002 flagged 224 subjects on pure statistical unusualness with NO materiality floor -- `min_value` (which gates the computed RATE) was correctly removed post-PR-481-review since the inherited value was unreachable, but nothing replaced the check it used to (incompletely) provide, and a rate can be statistically unusual on a denominator so large the underlying dollars are trivial regardless. `min_numerator` gates the RAW numerator sum instead, built exactly like `min_denominator` -- same shared choke point, per-rule data -- but with the OPPOSITE asymmetry: unmet exposure floor -> honest null (not enough data to compute a rate); unmet numerator floor -> a real, decided pass:false (a rate WAS computed, it just is not material). Set to $15 -- the measured population median of sum(|dol_diff|) (n=4,474, non-condiment, live 2026-08), the exact methodology INV-001\'s own min_value used ("clears roughly half"), not an invented number.\n\n'
  + 'Part B: lifecycle routing (scripts/security-rules-run.mjs, security-panel.js). qsr_variance_stat.descr carries machine-readable (Deactivated)/(Obsolete)/(New) markers the batch job already loads but never read. classifyLifecycle() tags each finding without altering its real value/verdict; the Security panel\'s verdictState() now takes the category as a priority argument -- a hygiene-classified finding reads as neither a security flag nor an exoneration, and groupFindingsBySubject() excludes it from the flaggedCount/worstValue/sort tally entirely (a separate hygieneCount tracks it) so subject-major convergence still means "independent SECURITY signals agree," not "any verdict landed here." New security_findings.lifecycle_category column.\n\n'
  + 'Part C: investigated, not implemented, per the dispatch\'s own instruction that this deliverable is a memory file. Re-measured live rather than trusting the dry-run doc\'s own numbers, and found a real discrepancy worth stating plainly: this run\'s lifecycle-marker share among flagged items (2.5%) shows no enrichment over the population rate (2.6%), where the dry run reported ~5x enrichment (13.8%) -- flagged explicitly as unresolved rather than picking whichever number is convenient. Of the still-unexplained flags, two real findings survive the measurement: one store accounts for 23.7% of them (top 4 stores: 48.3%) -- the strongest actionable lead this pass produced -- and logged waste (raw_waste/comp_waste) covers even half the usage variance for only 4.2% of them, directly supporting the still-unbuilt INV-003 (variance unmatched by logged waste) as the right next rule. Recurrence-over-time is unanswerable with current data (qsr_variance_stat holds only one period so far).\n\n'
  + '18 new tests (7 engine + 2 wiring for A, 5 unit + 2 wiring for B\'s classifyLifecycle, 4 Security-panel routing tests), mutation-tested per the dispatch\'s own bar. 1740/1740 suite passes, build clean, no entry-chunk impact. SQL for both new pieces (min_numerator, lifecycle_category) handed back, not applied. Full writeup: memory/dispatch45-min-numerator-lifecycle-investigation.md.',
]};
