// @ts-nocheck
export default {version:'5.066', date:'2026-08-19', changes:[
  'Dispatch24 Workstream B (#388) -- event scope + recurrence. org_events\' PK (unique(loc, date_start, label), loc not null) forced a district-wide event into N per-store rows -- "27 copies of Thanksgiving." Both write paths did this: applyEventToStores (Calendar Manager hand-tagging) and expandRetailEvents->saveOrgEvents (the rule engine, which is about to grow the real event count from ~733 to ~11,000+). Fixed with the RFC 5545 model: one event row + scope (store/state/all/list) + a resolved store list, expanded to per-store entries only on READ. forecastDay/computeEventFactors are UNCHANGED -- they only ever read orgEventsToDayMap()\'s output, same shape as before; proven with a round-trip test against the old flat-array behavior.\n\n'
  + 'A mid-flight correction found during grounding: the obvious fix (a new permissive scope-aware RLS policy) would have been a real cross-tenant leak. org_events\' original per-row RLS (schema-org-events.sql) was already superseded months ago by schema-multitenant-phase2-rls.sql (tenant-only permissive CRUD) + schema-rls-phase2-loc.sql (one RESTRICTIVE per-loc policy layered on top) -- a deliberate split where a naive additional permissive policy would OR past tenant isolation, not narrow it. supabase/schema-org-events-scope.sql instead REPLACES that one restrictive policy with a scope-aware version (store rows: unchanged per-row check; scope!=store rows: scope_locs array-overlap check), adding zero new permissive policies. loc stays not-null and the existing (loc,date_start,label) upsert mechanism is unchanged for the ~2,708 existing rows -- scope!=store rows get a synthetic sentinel loc (*ALL*/*STATE:OK*/*LIST:...*) that can never collide with a real store loc.\n\n'
  + 'Open design question #1 (per-store overrides on a scoped event) answered with a new org_event_exceptions table, keyed (event_id, loc) -- the RFC 5545 exception-instance model, reusing the exact same tenant+per-loc RLS pattern every other loc-keyed table already has. orgEventsToDayMap(events, iconFor, exceptions) applies them (canceled drops one store, modified overrides fields for one store); omitting exceptions is a no-op, so every pre-existing call site is unaffected. UI for creating an exception is explicitly deferred -- the mechanism was asked for, not a button with no caller yet.\n\n'
  + 'Open design question #2 (rule-based vs. manual events in one schema) answered without touching either generator: a new pure collapseScopedEvents(events, {allLocs, stateOfLoc}) groups the SAME flat per-store shape both expandRetailEvents() and applyEventToStores\' cloud-sync diff already produce, computing scope from the group\'s loc set. Wrapped in at exactly the two existing "about to call saveOrgEvents" call sites (calendar.js approveBulk, App.js syncUserEventsToCloud) -- RETAIL_EVENT_RULES/expandRetailEvents/applyEventToStores are all byte-for-byte unchanged, per the dispatch\'s explicit constraint.\n\n'
  + 'Full design writeup + both open questions\' answers: memory/dispatch24-event-scope-design.md. 11 new tests (events-scope.test.js): all 4 scope outcomes, the round-trip equivalence proof, legacy scope-less rows behaving identically to scope:store, and the exceptions mechanism. 1544/1544 tests pass (11 new). Build clean, entry-chunk budget unaffected (509.80 KB gzip vs 850 KB -- engine/lib files already in the bundle, no new panel).\n\n'
  + 'NOT done in this pass, and why: the migration has not been run against production Supabase yet (self-healing on both loadOrgEvents/saveOrgEvents until it is, so nothing breaks in the meantime) -- the real ~27:1 collapse ratio on the ~733 existing retail events is unmeasured until then. The weekProjections re-measurement the dispatch asked for after B ships (computeEventFactors still runs unconditionally every render per at-a-glance.js:1527, independent of Workstream A\'s cache-hit fix) is deferred to its own pass once the migration is live and the real post-collapse event count is known, rather than measured against a guess.',
]};
