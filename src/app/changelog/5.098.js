// @ts-nocheck
export default {version:'5.098', date:'2026-08-21', changes:[
  'Forms dashboard, Slice 2 of 3 -- the panel. Reads Slice 1\'s qsr_forms_completion table and '
  + 'renders it; still empty in production until Slice 3\'s pull script ships (an honest '
  + '"no data synced yet" state, not hidden or faked).\n\n'
  + 'New src/engine/forms-completion.js additions: computeFormStoreDayRollup() groups already-'
  + 'normalized rows to one entry per (store, form, local day), judged on RESOLVED occurrences '
  + 'only -- "open" ("--", still due) rows are excluded from both numerator and denominator, per '
  + 'the dispatch\'s own instruction, else the current day reads red at every store, every day. '
  + 'Bucketed on the LOCAL MIDNIGHT (hardcoded UTC-5) boundary the completionDetail API itself '
  + 'uses, deliberately NOT this codebase\'s 4am business-day boundary -- a different boundary on '
  + 'a different host; conflating the two would misattribute a completion to the wrong day. '
  + 'computeFormSummary() aggregates per form: passRate is Σcompleted/Σresolved across all '
  + 'store-days, never a mean of the individual rates (CLAUDE.md\'s "never average averages" -- '
  + 'a worked test fixture shows the two readings diverging from 4.9% to 51.25% on the same data). '
  + 'storeDaysPassRate is reported as a separate, legitimately different number.\n\n'
  + 'New src/views/forms-panel.js (FormsCompletionPanel, kind:\'test-kitchen\' pending Slice 3): '
  + 'per-form threshold input defaulting to 80% (owner-stated), persisted per-device in '
  + 'localStorage -- never one global bar, since a 1/day pre-shift and a 27-45/day Travel Path '
  + 'can\'t share a line. Pass rate always rendered as a number BESIDE the bar, never only implied '
  + 'by its width. Worst-performing form sorts first. Total-day attribution only, no manager '
  + 'attribution -- 0 of 3,886 missed rows in the capture carried a person, so this is the correct '
  + 'grain, not a stopgap. Wired into App.js (lazy import, showFormsCompletion state, dispatch/'
  + 'escape-hatch/render entries) and shell.js\'s ⚗ Test Kitchen list.\n\n'
  + 'Caught by the panel\'s own render-based test (not a unit test of the engine alone, per this '
  + 'repo\'s standing "would this verification still pass if the change were reverted?" rule): the '
  + 'panel was re-running the raw-payload normalizer (normalizeFormsCompletionRow, which expects '
  + 'raw.location/raw.status) on loadQsrFormsCompletion\'s output, which is already normalized '
  + '(that normalization happens once, at ingest, in Slice 3\'s pull script) -- the field-name '
  + 'mismatch silently dropped every row to null. Fixed by feeding the loader\'s output straight '
  + 'into the rollup; a raw-shaped normalizer call over an already-normalized loader would have '
  + 'shipped an always-empty panel with all unit tests green.\n\n'
  + '1944/1944 tests (12 net new: 6 pure rollup/summary tests plus 6 through the real panel, '
  + 'synthetic fixtures throughout). shell-nav-snapshot.test.js\'s Test Kitchen census ratchet '
  + 'updated 10 -> 11 (a legitimate new member, not a regression). Build clean, entry chunk '
  + 'unchanged at 511.10 KB gzip (panel is lazy). Full writeup: '
  + 'memory/project-forms-dashboard-slice2.md.',
]};
