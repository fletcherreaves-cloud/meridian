// @ts-nocheck
export default {version:'5.067', date:'2026-08-19', changes:[
  'Dispatch27 Workstream E -- routing vs modals. Confirmed before this shipped: zero history.pushState/window.location/router usage anywhere in App.js -- "route" meant a React `view` state variable with no URL sync at all, so refreshing the page or sending someone a link always landed on the default view. The four panels the plan flags as misclassified destinations (DI Compare, Forecast Accuracy, Projections, Date-Range Report) are now URL-synced routes (`?panel=<id>`) instead of showX-gated modals -- ~80 other modals (confirm/pick/log/quick-edit) are completely untouched, per the plan\'s own rule ("most current modals ARE correctly modals -- this isn\'t a mandate to route everything").\n\n'
  + 'panel-registry.js gets one new field (`route:true` on exactly those four ids) instead of parallel bookkeeping. New dependency-free src/app/routing.js (no router library, a single query param): parseRoute/isRoutePanelId fail safe to null/false for anything the registry doesn\'t mark route:true (a stale link, a typo, a hand-edited query string never resolves to an arbitrary string); pushRoute always PUSHES a new history entry both opening AND closing -- closing never calls history.back(), which would leave the app entirely on a page reached via a deep link with no prior in-app history. Real browser back/forward is handled exclusively by onRouteChange\'s popstate listener, re-deriving state from location.search (not event.state, which is null on a reload mid-route).\n\n'
  + 'App.js: routePanel state sits ABOVE view, not merged into it -- opening a route panel doesn\'t touch view/selStore, so closing it reveals whatever was already selected. Every top-level view gate (command/store/patch/org/district) gets an added !routePanel check; the four panels render via a new full-page RoutePanelShell (components/ModalShell.js -- same header visual language as ModalShell, no backdrop/centering/maxWidth, since it fills the content area in place of AtAGlance rather than overlaying it). The four are REMOVED from anyModalOpen\'s OR-chain -- safe, not a regression of the v4.212 performance fix, because AtAGlance/StoreDash/etc no longer stay mounted behind them at all once !routePanel unmounts them, exactly the "converting a panel to a route naturally resolves the concern for that panel" the dispatch predicted. Escape still backs a route panel out (via goRoute(null)), same UX as every other panel.\n\n'
  + 'Verified in a real browser (Playwright + dev server, not just unit tests): all four deep links (?panel=dicompare/fcst-accuracy/proj/report) render the correct panel directly on page load -- the actual "shareable URL" claim, confirmed working. Back button from a fresh deep link (no prior in-app history) lands on the app root, not off-origin -- the specific edge case the always-push design exists for. Browser back/forward correctly clears AND restores both URL and rendered content (tested on report). In-panel back button closes and clears the URL (tested on dicompare).\n\n'
  + 'NOT measured, stated plainly rather than guessed: the dispatch asked to re-measure the plan\'s "4.3s modal-close remount" figure via the existing _mark(\'compute:weekProjections\',...)/?clicktrace=1 instrumentation before citing it as still-current. Attempted, not obtained -- the dev server\'s localhost auth bypass skips login entirely, so RLS-scoped Supabase reads return nothing and ds never populates with real data (confirmed live via console: "No IDB data -- initialized empty ds; Supabase loads will populate"). A real number needs an authenticated session this environment can\'t produce; this stays an open item for the next session with real browser + real auth access, not assumed fine because the routing change itself tested clean. Full writeup: memory/dispatch27-routing-vs-modals.md.\n\n'
  + '12 new tests (routing.test.js): parseRoute/isRoutePanelId fail-safe behavior, pushRoute/onRouteChange against the real History API in happy-dom (push/clear, the always-push guarantee via history.length, popstate re-deriving from location.search, unsubscribe actually stopping). panel-registry.test.js: updated the four AtAGlance/StoreDash/OrgView render-gate regexes for !routePanel; new route-panels describe block (a ratchet: exactly the four expected ids carry route:true, each with a real goRoute call site and render gate). 1557/1557 tests pass (12 new). Build clean, budget ~unaffected (511.79 KB gzip vs 850 KB, +~2KB -- no new lazy panel).',
]};
