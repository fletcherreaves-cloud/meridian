// @ts-nocheck
export default {version:'5.007', date:'2026-08-12', changes:[
  'Issue #246 (instrumentation) — App.js\'s startup-timing logs used a single shared _ms() (elapsed since _t0) at every tier\'s completion line, so "T2 complete" and "T3 complete" printed the WHOLE T1+T2+T3 elapsed time, not that tier\'s own duration — a slow T3 could hide behind a fast T1+T2 and vice versa. Added _tierMs(start), which measures a tier\'s own duration from its own start timestamp, logged ALONGSIDE the existing cumulative _ms() figure (not replacing it) at all three tier-completion lines, so every log line already cited in commit history/memory stays comparable. Added _timedStage(label, fn, tierStart), which wraps a stage\'s own promise (call-site only — no _stXxx stage definition changes shape or behavior) to log its individual elapsed time against its tier\'s start; applied to all 22 of T2\'s parallel stages and all 3 of T3\'s, since Promise.all resolves on its slowest member and nothing previously identified which one that was. T3\'s per-stage timestamps are measured against _t2Done (captured in the first-registered .then() on the _t2 promise, guaranteed to run before T3\'s own .then() per JS\'s same-promise callback-ordering guarantee) rather than T3\'s own start, so they read as "time since T2 actually finished," matching how T3 is chained. Also corrected two stale in-code comment counts caught while editing these lines: "T2: 14 ds-touching stages" -> 22 (actual), "T3: 4 stages" -> 3 (actual - only _stFobRows/_stOpsRows/_stCtrlRows are called). Deliberately NOT done here, both per explicit standing instruction: did not lazy-fill opsRows/ctrlRows/fobRows (145 direct ds.x reads across 20 files would silently render "no data" — that\'s the data-sourcing-standard.md migration, not this fix), and did not un-chain T3 from T2 (manual must never override auto; serialization order preserved exactly). DEFERRED to a follow-up commit on this same branch: capturing one real production run with this instrumentation and using it to confirm or replace the App.js:~1556 comment\'s "~90 of 183 seconds" claim, which predates #191\'s manual-fallback lazy fill, the DAR rollup table change, and #248\'s xlsx lazy-load. A same-session sandbox capture attempt is in progress; if it cannot produce a trustworthy production-representative number, that will be stated honestly rather than guessed. 1360/1360 tests pass, build clean (eager 508.39 KB / headroom 341.61 KB, unchanged — this is a logging-only change, zero bundle-shape impact expected and confirmed).',
]};
