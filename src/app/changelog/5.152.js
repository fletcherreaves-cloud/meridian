// @ts-nocheck
export default {version:'5.152', date:'2026-08-25', changes:[
  'Dispatch #98: Inventory Control\'s summary tiles + By-Class row are now mode-aware -- Count '
  + 'Cycle no longer shows frozen EOM numbers.\n\n'
  + 'The four top tiles (Stores Reporting / Believe Done >=90% / Avg Count Complete / Count '
  + 'Window) and the By-Class row rendered UNCONDITIONALLY off EOM-basis `summary`/'
  + '`classSummary`/`inWindow` regardless of which tab (Scoreboard/EOM Count/Count Cycle) was '
  + 'selected, so viewing Count Cycle showed EOM\'s frozen, often-zero numbers instead of '
  + 'anything about weekly-count completion -- even though the Weekly Count Cadence table right '
  + 'below (CadenceMonitor, dispatch #97, cadenceByLoc/qsr_onhand-basis) had real data on the '
  + 'same screen at the same time.\n\n'
  + 'Fix: extracted the tiles + By-Class row into a new SummaryTiles component (eom-dashboard.js) '
  + 'that picks its source by `mode`. EOM/Scoreboard keep the ORIGINAL markup byte-for-byte -- '
  + 'this is purely additive for Count Cycle. mode===\'progress\' now swaps to '
  + 'cycleSummaryFor(rows, cadenceByLoc), a new aggregate over dispatch #97\'s own cadenceByLoc '
  + '(qsr_onhand-basis, NOT r.prog/EOM-window-basis):\n'
  + '- "Stores reporting" -- same concept, cadence-basis count.\n'
  + '- "On track (weekly)" X/N -- replaces "Believe done (>=90%)". Reuses CadenceMonitor\'s own '
  + '8-day bar (cadenceStatusOf, extracted to module scope so the tiles and the table below '
  + 'grade every store identically instead of a second, drifting copy).\n'
  + '- "Avg weekly F+C complete" -- replaces "Avg count complete". Item-weighted Food+Condiment '
  + 'coverage of each store\'s CURRENT attempt this period (compliant stores report their real '
  + 'weeklySessions counts; a short attempt derives counted = classTotals - still-missing from '
  + 'diagnoseIncompleteCount\'s own byClass rows, reusing cadenceFromOnHand\'s existing `missing` '
  + 'field -- no new fetch, no re-derivation from raw rows). Reads "--" (not a misleading '
  + '0.00%) when no store has any cadence data at all.\n'
  + '- "Overdue (>=8d)" -- replaces "Count window". A rolling weekly cadence has no single '
  + 'open/closed window the way EOM\'s last-3-days does, so this surfaces the overdue count the '
  + 'table already computes instead, with a "(+N never)" qualifier for stores with nothing on '
  + 'record this period.\n'
  + 'By-Class row swaps to Food-only/Condiment-only ("Food · weekly"/"Condiment · '
  + 'weekly" labels) -- the only two classes weekly cadence ever checks -- built on the SAME '
  + '{k,label,fob,pct,doneStores,n} shape EOM\'s own classSummary uses, so the render JSX needed '
  + 'no branching beyond picking which array to map.\n\n'
  + 'Did NOT invent a third "Daily" mode/tab -- the dispatch flagged this as a real open question '
  + '(whether the owner wants a genuinely separate Daily view, or whether "daily" just means the '
  + 'spot-check sessions already visible in Count Cycle\'s per-store drill-down) and explicitly '
  + 'said not to guess. Left open in memory/dispatch-98.md\'s Resolution.\n\n'
  + 'Verification: new tests (dispatch-98-cycle-tiles.test.js) render the actual SummaryTiles '
  + 'consumer -- not an isolated calc-function unit test -- across all three modes, plus '
  + 'engine-level checks of cycleSummaryFor against real cadenceFromOnHand() output. Confirmed '
  + 'load-bearing directly: temporarily reverted the mode branch and, separately, the By-Class '
  + 'source branch, and watched the progress-mode assertions fail each time (EOM tile text '
  + 'leaking through) before restoring both. Live spot-check against Supabase qsr_onhand '
  + '(period=2026-08, service-role Bearer token, 7,552 rows / 27 stores, asOf 2026-08-25) via the '
  + 'actual shipped cadenceFromOnHand()/cycleSummaryFor() call chain: 2/27 on track, 0 overdue, '
  + '25 never (no qualifying session yet this period), 97.51% item-weighted avg coverage -- '
  + 'consistent with dispatch #97\'s own 2026-08-24 measurement one day earlier (3/27 on track '
  + 'then; 10422 fell out of compliance as its active-item universe grew by a few SKUs day over '
  + 'day, the same single-item-flips-98%-compliance pattern #97\'s own follow-up note flagged).\n\n'
  + 'Full suite green (227 files / 2361 tests, +6 net over baseline). Build clean. Entry chunk '
  + '528.40 -> 528.41 KB gzip (+0.01 KB, noise); eom-dashboard.js (lazy panel, not in the entry '
  + 'budget) 62.29 -> 62.88 KB gzip (+0.59 KB). Full resolution: memory/dispatch-98.md.',
]};
