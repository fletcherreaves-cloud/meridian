// @ts-nocheck
export default {version:'5.079', date:'2026-08-20', changes:[
  'Dispatch40 -- Security build Phase 1b: inventory-domain TvA rule. Runs alongside dispatch #39\'s cash-domain rules in the SAME rules registry, SAME interpreter, SAME batch job -- not a second build.\n\n'
  + 'The finding that makes this buildable, verified directly rather than re-asserted: dispatch #39\'s own "no theoretical-usage table exists" claim was wrong -- QSRSoft already computes TvA (theoretical-vs-actual) variance server-side and Meridian already pulls it daily for all 27 stores (qsr_variance_stat, exp_usage IS the theoretical-usage figure, no recipe/BOM pull needed for a first version). What was actually missing was two things: a rule that reads it, and a subject shape for security_findings that isn\'t an employee.\n\n'
  + 'supabase/schema-security-rules-phase1b.sql: seeds INV-001 (item-level TvA variance rate -- |variance|/exp_usage x 100, store baseline, plan §2.2\'s own formula, single-table, no join) and INV-002 (dollarized variance rate normalized against store-month sales, store baseline). INV-002\'s denominator was a real decision, not a guess: qsr_variance_stat.pct_sales\' actual semantics are unconfirmed from this sandbox (no comment/test/prior probe settles what QSRSoft\'s "percentage" field measures), so this uses a real cross-table join instead -- qsr_fob.prod_sales_amt summed per (loc, month) -- rather than trust an unverified column as a detection rule\'s denominator. Condiment-class rows (dol_diff forced to 0 at map time) excluded from BOTH rules uniformly: even INV-001\'s ratio, whose numerator isn\'t literally zeroed for condiments, is prone to false positives on their inherently low/noisy unit-usage figures. Both rules ship with first-guess, unmeasured thresholds in the same "needs tuning against real output" tier as every rule in this build -- INV-002\'s in particular depends on the still-unverified pct_sales question above.\n\n'
  + 'supabase/schema-security-findings.sql needed NO changes -- dispatch #39 built its dual-subject shape (nullable emp_token + co-equal wrin column, NULL-safe subject_key upsert target) in anticipation of this exact dispatch, before the table ever went live. That bet paid off: zero migration required.\n\n'
  + 'scripts/security-rules-run.mjs extended with a second rule-type branch (still one script, one active-rules loop): mapVarianceStatRow() (qsr_variance_stat\'s snake_case -> camelCase, with period mapped straight onto a synthetic `date` field -- NOT period + \'-01\', which would silently break security-baselines.js\'s plain string-comparison window filter at a month boundary), joinStoreMonthSales() (the qsr_fob join, a pure function security-rules.js itself needs zero changes to consume), and computeItemFindingsForRule() -- the item-domain parallel to dispatch #39\'s computeFindingsForRule(), grouping by (loc, wrin) instead of (loc, empToken) and using ONLY storeBaseline() (the one baseline function that doesn\'t hard-require an emp field), pre-filtered to the SAME item so a store\'s rate for one item is only ever compared against other stores\' rate for that SAME item, never pooled across unrelated items. A real, non-obvious wiring detail caught while implementing, not left implicit: the seed SQL\'s logic_expression field names are camelCase (expUsage, dolDiff, storeMonthSales), matching what the batch job\'s own row mapper actually produces -- NOT qsr_variance_stat\'s raw snake_case columns, which the dispatch\'s own prose (correctly, for readability) quotes as "exp_usage." A snake_case field name in the seed SQL would have silently resolved to undefined and every finding would read as "no exposure" -- the exact silent-zero-field class CLAUDE.md\'s loader-field-map rule exists to catch elsewhere in this codebase.\n\n'
  + '11 new fixture tests (security-rules-run.test.js) -- hand-computed 2-store/1-item/1-condiment fixture proving: condiment rows never become a subject and never pollute a same-item store baseline; the store-baseline population is correctly restricted to the SAME wrin; storeBaseline\'s excludeSelf keeps a store from baselining against its own rate; and window bounds convert correctly from internal \'YYYY-MM\' period strings to the real calendar-date bounds security_findings.window_start/window_end actually require (including a leap-year February edge case). No UI, no recipe/BOM pull -- both explicitly out of scope per the dispatch (a recipe pull only matters for tracing a flagged item\'s variance back to which menu item drove it, a later drill-down capability, not detecting that a variance exists). 1663/1663 tests pass, build clean. Full writeup: memory/dispatch40-inventory-tva-rule.md.',
]};
