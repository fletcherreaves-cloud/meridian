// @ts-nocheck
export default {version:'5.222', date:'2026-08-27', changes:[
  'Dispatch #176 -- fixed the "FOB components vs target" EOM diagnosis check (id:\'fob-components\', '
  + 'order:10, the first check in the registry, the owner\'s own "ALWAYS start with FOB" step), which '
  + 'was registered/enabled but never produced a finding for any store, ever. Owner-approved fix '
  + 'ahead of the 3-day physical EOM inventory count starting Sat 2026-08-29. Note: the narrative FOB '
  + 'driver line the owner actually reads in the report (fed by fobComponentDeltas(), a different, '
  + 'correctly-wired path) already worked -- this was specifically about the structured Finding never '
  + 'showing up in result.findings/result.systemic.\n\n'
  + 'Two stacked bugs, both confirmed by reading the code: (1) eom-report-build.js\'s buildEomReport() '
  + 'received a `targets` param and used it elsewhere in the same function (the narrative FOB target '
  + 'line), but never included it in the `data:` object passed to runDiagnosis() -- so ctx.data.targets '
  + 'was always {} for every store, every time. (2) Even with that fixed, the check\'s own component '
  + 'list used long-form keys (compWaste/rawWaste/condiments/...) to index into ctx.data.targets, but '
  + 'the real DEFAULT_TARGETS field names (per the already-working FOB_COMPONENTS/fobComponentDeltas() '
  + 'sibling) are tCompWaste/tRawWaste/tCondiment/tEmpFood/tStatLoss/tUnex -- so t[key] was always '
  + 'undefined regardless of bug 1.\n\n'
  + 'Fixed both, reusing the existing FOB_COMPONENTS tuple array (eom-diagnosis.js) as the single '
  + 'source of truth for both the check and fobComponentDeltas(), rather than maintaining a second, '
  + 'independently-named component list that can silently drift out of sync the way it just did -- '
  + 'added a 4th tuple element (the long-form key ctx.data.fob carries) so the check now reads '
  + '[key, label, targetKey, longKey] straight from FOB_COMPONENTS. The check\'s own finding-title '
  + 'wording ("Completed Waste"/"Emp/Mgr Meals"/"Variance Stat") is preserved via a small local label '
  + 'map, matching the app\'s established terminology elsewhere (fob-eom.js, analytics.js) rather than '
  + 'FOB_COMPONENTS\'s own shorter narrative-line labels ("Comp Waste"/"Stat Variance") -- display text '
  + 'only, not part of the key mapping. Threshold/severity logic (the `band` param, the >1pp high/'
  + 'medium split) is unchanged.\n\n'
  + 'Also found and fixed while verifying end-to-end (measure it, don\'t reason about it): '
  + 'buildEomReport()\'s default `checks` fallback used checksConfig() -- which strips every check down '
  + 'to its serializable {id,label,order,enabled,params} shape for an editable-registry UI, with no '
  + '`run` function -- instead of the runnable DEFAULT_CHECKS array. Passed straight into runDiagnosis() '
  + 'as `checks`, this makes every check (not just fob-components) silently error (caught, logged as '
  + 'ran:[{...,error}]) and produce zero findings whenever a caller doesn\'t pass an explicit `checks` '
  + 'override -- true of every current buildEomReport() caller (the public EOM share view). This was '
  + 'masking the dispatch #176 fix itself: an end-to-end test calling buildEomReport() exactly the way '
  + 'the real app does (no `checks` override) failed until this was also fixed. Swapped the import/'
  + 'default to DEFAULT_CHECKS. eom-dashboard.js\'s separate buildDiagResult()/runDiagnosis() call (the '
  + 'in-app EOM Dashboard path) was unaffected -- it already builds its checks via applyChecksConfig(), '
  + 'which does keep `run`.\n\n'
  + 'New/updated tests: eom-diagnosis.test.js -- fixed an existing test that had (unknowingly) been '
  + 'passing synthetic wrong-shape target keys matching the bug\'s own broken lookup, and added two new '
  + 'dispatch #176 tests (a component over its real DEFAULT_TARGETS-shaped target fires a Finding with '
  + 'checkId:\'fob-components\'; every component within target produces zero findings, no false '
  + 'positives). eom-report-build.test.js -- two new end-to-end tests through buildEomReport() itself '
  + '(same call path the share view uses), one proving a finding fires and the narrative FOB '
  + 'driver-line delta stays correct alongside it, one proving zero findings + the existing narrative '
  + '"FOB 3.90%" recap line is unchanged. Full suite: 3021/3021 (291 files). Build clean; eager total '
  + '552.69 KB, 297.31 KB under the 850 KB budget (no new static top-level import).\n\n'
  + 'Out of scope (per dispatch): every other DEFAULT_CHECKS entry; the purchases-posted stub '
  + '(separate dispatch #177, different root cause -- no data source wired at all).',
]};
