// @ts-nocheck
// #230 — the full Meridian changelog, moved out of App.js's entry chunk (2026-08-12).
// Measured 2026-08-12: 366 entries, 274,943 raw chars, ~98 KB gzipped in isolation — roughly
// 12% of the entry chunk it used to live in — for prose that renders in exactly one place, the
// About modal. This module has exactly ONE importer, changelog-panel.js, itself only reached
// via lazyPanel()'s dynamic import — App.js does NOT import this file. That is deliberate:
// a module reached by both a static AND a dynamic import path gets pulled into the eager/entry
// chunk regardless of which named export is actually used (the same INEFFECTIVE_DYNAMIC_IMPORT
// class of bug #207 fixed for inventory.js/one-pager.js/above-store-onepager.js) — so the one
// piece App.js needs synchronously (the latest entry's version+date, for the footer text and
// window.__MERIDIAN_VERSION__) lives in its OWN standalone module, changelog-latest.js, which
// this file does not import and is not imported by. Keeping the two in sync is mechanical (add
// a new entry here AND update changelog-latest.js) and guarded by
// src/__tests__/changelog-version.test.js, which asserts they match.
export const MERIDIAN_CHANGELOG = [
  {version:'5.002', date:'2026-08-12', changes:[
  'Issue #244 — check-bundle-budget.mjs was gating on the entry chunk\'s own printed gzip line, blind to how many other files dist/index.html <link rel="modulepreload">s — those fetch just as eagerly as the entry file, as separate HTTP requests, before the app can render anything. Independently re-measured with gzip -9 on the two commits that bracket #238 (1c6e4c7 pre, 3980e71 post): entry-only read 698.99 KB -> 469.36 KB, a flashy -229.63 KB the old gate reported (as 722.34 KB -> 485.15 KB via vite\'s own printed number, -237.19 KB) — but the TRUE eager payload (entry + every modulepreload chunk) was 854.81 KB -> 744.09 KB, a real -110.72 KB. The number that matters here is the first one\'s implication: pre-#238, the honest eager payload was already 854.81 KB against an 850 KB budget — OVER budget — while the old entry-only gate reported 127.66 KB of headroom and passed it clean. The gate was passing an over-budget bundle. That makes #238 the fix that took the app from over budget to under budget, not the cosmetic entry-chunk trim its own PR described. check-bundle-budget.mjs now parses vite\'s printed "dist/assets/<file> ... gzip: NNN.NN kB" line for EVERY chunk (not just the entry one) into a filename->KB map, reads dist/index.html for the entry <script type="module"> src and every <link rel="modulepreload"> href (explicitly NOT rel="prefetch" — prefetch is idle-time/low-priority, not eager, and must never be counted as if it were), sums their gzip sizes from the map, and gates/reports that sum. Fails loudly (non-zero exit, explicit message) if index.html can\'t be read, the entry script tag isn\'t found, or a modulepreload target has no matching gzip size in the map — the same "regex silently matches nothing, reports a small false-passing number" failure mode #244 itself is about, so the new parser refuses to degrade into it. The 850 KB budget is UNCHANGED by this fix — it was always meant as a proxy for bytes-parsed-before-interactive, which eager-total measures correctly and entry-only did not; the metric was wrong, not the threshold. Post-#240 (v5.001, main), the corrected gate reads: entry 484.63 KB (reference only) + 11 modulepreload chunks = 768.67 KB eager total, 81.33 KB headroom under 850 -- independently cross-checked by the owner against a plain gzip -9 (744.09 KB, the ~3% spread being vite\'s reporter vs gzip -9, consistent on both sides) -- down from the old gate\'s stale entry-only headroom on the same build. That drop is the number becoming true, not a regression: headroom against the entry file alone was never real headroom against the thing the budget is actually meant to bound. Manually verified against a real build (see script output) rather than only reasoning about the regex, per this repo\'s measure-dont-reason standing rule. 1339/1339 tests pass. No new test file: scripts/*.mjs has no existing vitest coverage precedent in this repo (they run at build/CI time, not import time); verified instead by running the script against a live build and diffing its parsed sums against a hand-count of dist/index.html\'s own tags.',
  ]},
  {version:'5.001', date:'2026-08-12', changes:[
  'Issue #236 — labor_pct\'s 30-day-movement distribution had a contaminated tail (p90->p95 jumps 3.698pp->10.650pp, p99 19.497pp — a step change in kind, not degree; mean 1.940pp against median 0.841pp is heavy right skew a real operational distribution wouldn\'t show). Root cause found by dumping and reading actual rows (scripts/dump-labor-movement-outliers.mjs, new — forks the noise-threshold script\'s exact windowing, prints both window endpoints/values/observation-counts and every underlying row), not by theorizing candidate mechanisms first. Top-50 movements showed trailing-30 windows reading ~0-2% labor for real n=30-observation windows (the >=20 guard was holding, not a thin-window artifact) paired against a normal ~21-24% window 30 days earlier — a store cannot run near-0% labor for 30 straight days. Pulled one flagged store\'s raw history directly: labor_pct=0/tpph=0/ot_hrs=0 on EVERY day 2026-06-01 through 2026-07-11 (41 consecutive days, no gaps) while sales was real and varied normally ($9,089-$19,880/day) -- a data-pipeline defect, not operations, and a CHRONIC one: parsers/index.js\'s parseSalesLedger (the QSRSoft Sales Ledger channel-mix report parser, a different report from Labor Analysis) stamped every row\'s labor fields as literal 0 ("Sales Ledger has no labor data"), and pipeline.js:578 merges those rows into ds.laborRows so the sales figure can supplement Labor Analysis history when that report lags (confirmed intentional plumbing -- the Data Manager panel\'s own UI already says "Sales Ledger... merges into Labor Analysis"). App.js\'s manual-upload path then pushed the merged rows straight to the labor_rows Supabase table; saveLaborRows\'s `r.laborPct ?? null` mapping treats 0 as a real value (0 ?? null is 0), so the stub got upserted (onConflict: loc,report_date) and silently overwrote or filled in real Labor Analysis percentages with a false zero. Never visibly noticed in the live app because metric-source.js\'s laborPct chain uses mode:\'pos\' ("a %\'s never legitimately 0") and already skips a stub 0 in favor of glimpseRows/ctrlRows -- the exposure was the persisted labor_rows table itself (which measurement scripts and any future coaching-loop code read directly) plus any panel reading ds.laborRows directly rather than through the shared metric-source helpers (not fully audited here, flagged as still-open). MEASURED count, per the issue\'s explicit "report how many store-days, not just the mechanism": labor_pct=0 AND sales>0 matches 994 of 42,156 labor_rows (2.36%), ALL 27 stores, 2025-01-22 through 2026-07-23 (18 months chronic, not a recent-only outage -- the June-July 2026 block found first was just the most recent batch) -- every one of the 994 also has tpph=0 AND ot_hrs=0, verified before writing any cleanup script so no partial-real-data row could be swept up. Fixed at three layers per the #192 padding precedent (fix root cause, not the symptom): parseSalesLedger\'s labor fields are null now, not 0, tagged _salesLedgerSupplement; loadSalesLedger (the cloud sales_ledger_daily loader) gets the same null fix for consistency (nothing currently reads that field live, so this is hygiene not a live fix); App.js\'s manual-upload save path now filters _salesLedgerSupplement rows out of what reaches saveLaborRows entirely -- they stay in-memory in ds.laborRows (still usable for same-session DI-calibration continuity) but never touch the labor_rows table, since supplementLaborWithSched (App.js\'s DAR-based sales supplement, already verified 2026-08-04) already covers "sales when Labor Analysis lags" without ever persisting anything, so nothing is lost by keeping this path narrower. New test (src/__tests__/sales-ledger-labor-stub.test.js, 4 tests): asserts every parsed row\'s labor fields are null not 0, asserts the _salesLedgerSupplement tag, and source-parses parseSalesLedger to guard against the literal 0 stub ever being reintroduced. Cleanup script for the 994 already-corrupted rows (scripts/cleanup-labor-pct-stub-zeros.mjs, --dry by default) is written, dry-run-verified (994/994 match, zero false positives), and NOT yet executed against production -- per this repo\'s own v4.839 precedent ("does NOT run the two scripts, needs the owner\'s go-ahead"), a write to 994 live rows needs explicit sign-off, not an agent\'s unilateral call. Full incident, the still-open items (run cleanup, re-run #237\'s labor measurement post-cleanup since its 2.30x reduction is driven almost entirely by this same contaminated tail, audit remaining direct ds.laborRows consumers), and definition-of-done tracking in memory/project-labor-pct-tail-236.md. 1336/1336 tests pass (1332 + 4 new). Entry chunk essentially flat (comment/logic-only change in already-loaded modules).',
  ]},
  {version:'5.000', date:'2026-08-12', changes:[
  'Issue #237 — read-only measurement, no product change, gating whether coaching verdicts (#208) can turn on. The owner\'s target coaching improvement is 0.25-0.50pp; a prior measurement (measure-coaching-noise-threshold.mjs) found labor_pct\'s ordinary 30-day drift median is 0.841pp and fob_total_pct\'s p75 is 0.416pp — both target sizes sit INSIDE that noise band, so a raw single-store before/after verdict would fire on drift, which #208 forbids. Hypothesis tested: most of that drift is district-wide (commodity/promo/weather/seasonality) and differencing it out (store_delta - district_delta, per-store per-30-day-window) leaves a materially tighter residual. Built scripts/measure-district-relative-noise.mjs with the four guards the issue named explicitly as silent corruptors if missed: district aggregate dollar-weighted (Sigma$/Sigma sales$, never an average of 27 percentages) for EVERY component including labor_pct (added labor_rows.sales as the weight column for this — labor\'s own store-side number stays an unweighted mean per the existing metric-source.js convention, since "what did this store do" and "what did the market do" are different questions); leave-one-out (the measured store excluded from its own district aggregate); same day universe (the district delta computed over the EXACT calendar dates in that store\'s own trailing-30 window, not a district trailing-30-by-index that happens to end on the same date); and the >=20-observation guard applied to the district side too. Sanity check: the script\'s own raw |store_delta| column reproduces the prior run\'s table exactly to 3 decimals on every component/percentile before trusting the residual comparison. RESULT, reported plainly per the issue\'s own instruction not to massage a negative result into a threshold: the hypothesis mostly FAILS. All 4 FOB-family components show a reduction factor of ~0.98x-1.07x (raw p95 / residual p95) — essentially no noise removed; FOB drift is store-idiosyncratic, not a shared district signal, and fob_total_pct\'s residual p50 (0.226pp) is still inside the 0.25pp target floor. labor_pct shows a real 2.30x reduction, but it\'s almost entirely a tail effect sitting in the exact region #236 already flagged as contaminated (broken data, not operations) — at p50/p75, the percentiles describing typical movement, labor\'s reduction is only ~1.13-1.14x. Conclusion: district-wide drift is not the dominant noise source for any of the 5 components measured; NOISE_THRESHOLDS stays {} (verdicts stay off) — not because the measurement hasn\'t run, but because it ran and this fix doesn\'t work. Per #237\'s own contingency, the next candidate is longer measurement windows or confidence-based (not binary) verdicts. Also surfaced, not acted on: store 43701 is the single worst-dispersion store on every component by a wide margin AND has roughly a fifth of every other store\'s sample size (labor n=73 vs ~505-508 for the rest) — a coverage gap reading as apparent noise, a data-completeness problem to name when scoping #236, not evidence against the measurement. Script + full numbers from both runs committed to memory/project-noise-measurement-237.md in the same commit (the prior run\'s output previously existed only in a chat thread — precisely how project-aag-tiles-reimagine.md and two other memory pointers went dangling). memory/project-coaching-loop-208.md updated to point at this result instead of describing the measurement as still-pending. 1332/1332 tests pass (no new tests — a read-only Node script against live Supabase data has nothing for the Vitest suite to assert against; verified by reproducing the prior run\'s numbers exactly, see above).',
  ]},
  {version:'4.999', date:'2026-08-12', changes:[
  'Issue #232 (Finding 1, analytics.js slice) — analytics.js (316 KB raw) was the single biggest static import left in App.js, and tracing why it stayed static turned up more than the one import line the issue named. All 19 real analytics.js exports used in App.js (AIInsightsTab was a 20th, dead — imported and never rendered, store-analytics.js is its only real consumer and already imports it directly) are modal/report panels individually gated behind their own showX flag, so none are needed to paint the home screen — textbook lazyPanel() candidates. But removing App.js\'s own import line alone would NOT have gotten analytics.js\'s bulk out of the entry chunk: store-dash.js (Finding 3\'s grab-bag, itself still static and not touched here — that split is separate, larger, deferred work) reached back into analytics.js for one small component, ModelHealthBadge, and since a module reached by ANY static path is bundled eagerly regardless of how many dynamic paths also reach it, that one import alone would have re-pinned the whole 316 KB module in place no matter what App.js did. Same mechanism, two more paths: projections.js and pace-to-target.js were both themselves fully static in App.js and each reached into analytics.js too (ForecastAudit/CurrentMonthPaceSection). Fixed all four paths, not just the one named in the issue: (1) extracted ModelHealthBadge into its own src/views/model-health-badge.js — store-dash.js and store-analytics.js (already lazy) now both import it from there instead of analytics.js; (2) lazy-loaded all 19 analytics.js exports via a new _analytics() group; (3) lazy-loaded projections.js\'s ProjectionWorkflow/PreForecastBrief via a new _projections() group — its loadLockedProjections/saveLockedProjections/getLockedAmount/lockProjectionWeek exports were ALSO dead in App.js (lockedProjections state has its own useState initializer and its own local saveLockedProjections callback that was silently shadowing the import); (4) lazy-loaded pace-to-target.js\'s PaceToTargetPanel directly. New guard test (src/__tests__/lazy-panel-not-static.test.js, 3 tests, per the issue\'s own explicit ask): source-parses App.js to build the set of lazyPanel()-registered modules and the set of statically-imported modules and asserts they\'re disjoint — the invariant silently violated seven times before #230/#232 started fixing it. 1335/1335 tests pass (1332 + 3 new). MEASURED, per the issue\'s own explicit "do not quote a gzip figure until it is built and weighed" instruction, and the naive number is misleading: the entry FILE\'s own gzip (what check-bundle-budget.mjs\'s CI gate actually measures) dropped 722.34 KB -> 485.07 KB (-237.27 KB), but that overstates the real win — splitting analytics.js/projections.js out also caused supabase.js/blob-sync.js/labor-basis.js/swing-feed.js/fmt.js (previously inlined in the entry file because only the entry needed them) to become separate chunks that index.html still <link rel=modulepreload>s, so they\'re still fetched eagerly at initial load, just as separate files. Summing the entry file + everything index.html actually modulepreloads (the honest "bytes parsed before interactive" number) goes 883.57 KB -> 769.10 KB gzip, a real reduction of 114.47 KB (2,920.26 KB -> 2,501.05 KB raw, -419.21 KB) — not nothing, but well short of the flashy entry-file-only delta. What genuinely deferred to first-open, not in the modulepreload list at all: analytics-*.js (91.42 KB gzip / 333.21 KB raw), projections-*.js (17.18 KB gzip / 58.24 KB raw), pace-to-target-*.js (1.10 KB gzip / 2.30 KB raw). Flagging, not fixing here (separate scope): check-bundle-budget.mjs only parses the entry file\'s own printed gzip line, so it has been blind to this modulepreload-inflation effect since #207 — worth a follow-up issue, not a silent expansion of this one. Verified in a headless Chromium smoke pass (dev server, since this sandbox has no authenticated Supabase session to click-walk every panel against): app loads clean, zero console/page errors beyond expected blocked-network ones, Date-Range Report opens with no chunk-load or "element type is invalid" errors.',
  ]},
  {version:'4.998', date:'2026-08-12', changes:[
  'Issue #231 (P1) — Patch Heatmap accepted a dateRange prop and never referenced it again in the file. Owner-reported via three phone screenshots: This Week (8d, Aug 12-18), Last Week (8d, Aug 5-11), and a custom 32-day range (Jul 1-31) — Last Week and the July range returned BYTE-IDENTICAL output, not just the same tally but the same worst dimension on every individual store. Confirmed in code before touching anything: patch-heatmap.js:198 destructured dateRange and it appeared nowhere else in the file; at-a-glance.js:1557 passed it in faithfully. Root cause measured, not assumed, per the issue\'s own explicit instruction not to guess: traced all 5 dimensions back through pipeline.js\'s buildStore(), which builds the app-wide `stores` array once per data-load with NO dateRange parameter at all. Sales (store.pSales/pLY) is a fixed matched-day trailing-28-day window (cut4/now4, anchored to lastClosedBusinessDay()). Labor/Speed/Controls (store.p.laborPct/oepe, store.ctrlScore) all derive from compute6wk(loc, ds, settings.weeksBack||6) — a fixed trailing N-week window, default 6, same one StoreCard/RankingView use. FOB (fobByStoreLatest) is explicitly latest-snapshot-only, not a window at all. None of the five ever touched the page\'s date-range selector — the byte-identical Last-Week-vs-July output wasn\'t a coincidence, it was the only possible outcome. Decision (per the issue\'s own framing — "decide what dateRange should mean for each dimension," not "wire all five to it no matter the cost"): re-parameterizing buildStore/compute6wk\'s core pipeline to accept an arbitrary UI-selected date range would touch the SAME `stores` array dozens of other consumers throughout the app depend on — wildly disproportionate risk for a widget whose own design intent (worst-of-N absolute health, "how is this store doing overall," per the file\'s pre-existing header comment) was never period-comparison to begin with. Fixed by being honest instead of silently wrong: removed the dead dateRange prop entirely, both from PatchHeatmap\'s destructure and at-a-glance.js\'s call site (a control that looks live and does nothing is worse than no control), rewrote the file header to name each dimension\'s REAL fixed window explicitly, changed the subtitle to state plainly "rolling ~4-6wk window (not the date range above)", and fixed all 4 empty-state/tooltip strings that falsely asserted period-scoping ("No data this period" / "No auto/emailed data landed for...in the selected period") to describe reality instead ("No data available" / "...landed for this store/patch yet"). This also un-refutes the #219 band calibration exactly as the issue flagged: the #219 measurement was taken over a defined window, the heatmap was never honouring one, so the two tallies (8/15/4 live vs 6/14/7 predicted) were never comparable in the first place — NOT evidence the bands are wrong, and not re-measured here per the issue\'s own explicit instruction to wait. New guard test (src/__tests__/patch-heatmap-daterange.test.js, 7 tests): source-parses both files to assert PatchHeatmap\'s signature never re-destructures dateRange and at-a-glance.js never passes it again (the exact "destructured-and-dropped prop invisible to every existing test" failure the issue named), asserts no UI string in the file claims period-scoping via regex, and asserts storeDimensions/patchDimensions/bandFromGap all still take exactly the arguments they claim to (2/1/2 respectively) with no smuggled period parameter. 1332/1332 tests pass (1325 + 7 new). Entry chunk 724.22 KB -> 722.26 KB gzip (a slight decrease, not flat as expected -- both patch-heatmap.js and at-a-glance.js are already lazy via App.js\'s at-a-glance chunk, so this is noise/rounding from the removed dateRange plumbing, not a deliberate optimization).',
  ]},
  {version:'4.997', date:'2026-08-12', changes:[
'Issue #211 (Push 4, "the generic shell") — first slice, plus the scoping pass that precedes the shell itself. #211 is explicitly the largest structural job on the board (memory/project-inventory-control-redesign.md: consolidate 6 inventory/food-cost panels into one scope×period×class shell modeled on above-store-onepager.js, then instantiate a second time for Labor) and the issue itself says several design questions must be confirmed "before starting," not guessed at. Ran a full research pass against the live code (not the design doc\'s own 2026-08-11 reading) before writing any shell code: confirmed above-store-onepager.js\'s exact scope/period/section/drilldown/print architecture as the pattern to copy; found the design doc\'s "three report templates" claim needs a correction — Chronic Offenders is structurally a flat per-item table (template C\'s shape), not a per-store loop like its claimed siblings Count Reliability/Rubber-band, and EOM Supervisor needs a genuine 4th template (live editable cells with real Supabase persistence, DOM-based print instead of eom-dashboard\'s popup-window pattern, a Year/Month+grouping period unit, and a distinct "shaded boxes" rollup formula that explicitly excludes Total Food Cost/OT $ per a 2026-08-04 owner directive already in the code) rather than a preset of the district-KPI template; confirmed eom-share-view needs only a fixed single-store/single-period slice, never live scope/period controls, which resolves how the shell\'s planned LINK output mode should work; and confirmed the "identical fragmentation" claim for Labor concretely, not just by analogy — labor-tools.js\'s OperatorSummaryPanel and LaborAnalyticsPanel carry two independently-retyped copies of the same PERIODS array that have already silently drifted by one entry, and scheduling.js carries a third, independent Wednesday-anchored week vocabulary with two only-partially-shared implementations in the same file. Full findings appended to memory/project-inventory-control-redesign.md §12b (superseding §12\'s open-questions list) rather than filed as a separate doc, since #211\'s existing memory file is the one future sessions will read before touching this area. CORRECTION (owner, same day): the initial version of this entry framed Labor\'s "class" axis as an open crew-vs-management filter-vs-split judgement call — that premise was out of date. It was already decided in Notes 35 and is already shipped: parsers/index.js:313-320 carries THREE named labor-basis fields on every row (punchLaborPct — punched, all-hourly, comparable at all 27 stores, the default; crewLaborPct — QSRSoft\'s "Crew Labor %," which adds salaried-manager $ where a store is configured that way (FL is, OK isn\'t) and is NOT comparable across markets; totalLaborPct — full accounting, only meaningful where configured). So it\'s mode:\'filter\' over three named bases defaulting to Punched, not a two-way split — the earlier filter-vs-column-split framing itself was resolved by the owner\'s own prior decision, not a design choice for this session to make. memory/project-labor-pct-punched-vs-crew.md — cited at parsers/index.js:315 and :441, decided in Notes 35, but never actually committed in any prior session — has been reconstructed in this commit from those two code comments, the v4.583 changelog entry, and five hard requirements the owner supplied: default basis is always Punched; Crew/Total must render "not configured" (never 0, never blank) for stores without salaried-manager setup, same discipline as darSchedHrs\'s `?? null`; any scope mixing configured and unconfigured stores must suppress or hard-flag a Crew/Total rollup rather than silently averaging incomparable numbers together; per-store-per-period configuration status is DERIVED from salariedManagerHours (qsr_labor_summary) / salMgr (Ops Report), never a hardcoded FL/OK list, since a future tenant may configure this differently; and crewLaborPct/tCrewLabor/monthly_targets.crew_labor_pct are NOT renamed (display labels only) given the blast radius across constants.js, store-dash.js, analytics.js, smart-targets.js, labor-basis.js and a live DB column. SECOND CORRECTION, same day: the owner separately flagged that his own stated formula (Crew % + Manager % = Total %, Punched = hourly-only) contradicts the shipped code comment\'s claim that Crew already includes salaried-manager $ where configured — this is a real, unresolved contradiction about what the Crew field actually measures, and it needs a measurement against real qsr_labor_summary rows that neither of us can run from this sandbox (anon key returns [] on that table). Labor instantiation is ON HOLD pending the owner running that measurement; Food Cost is unaffected and unblocked. Also confirmed and recorded: labor targets/projections are hourly-basis in both markets, so punched actuals grade against them correctly — no target/actual basis mismatch to chase. Shipped as this commit\'s actual code change, since it\'s low-risk and independently valuable regardless of how the rest of the shell design lands: the design doc\'s "free perf win" — eom-dashboard.js\'s load() independently re-fetched the FULL unscoped ~13,190-row qsr_fob table via loadQsrFob() on EVERY period change (worse than the design doc\'s own §10, which said "on panel open" — corrected in the same commit), duplicating the copy App.js already loads once at startup into ds.qsrFobRows. Confirmed still present and untouched by #214/#218 via git log before fixing, not assumed; confirmed App.js\'s own startup call uses the identical default 500-day window, so reading the copy carries no truncation risk. PM REVIEW CAUGHT A REAL REGRESSION in the first version of this fix, same day: reading ds.qsrFobRows with a bare `|| []` fallback meant a failed-or-still-loading startup fetch left this panel\'s FOB data permanently empty with no retry, while the panel\'s own `loading` flag (driven by 7 OTHER loads) would flip false regardless — a food-cost panel presenting an affirmative "no FOB data" on an unresolved load, the exact #192 P0 failure shape from three weeks earlier in this same file. Fixed by holding a distinct fobPending state (the v4.870 rule already used for RegisterAuditTab) that\'s folded into the panel\'s loading gate: the copy is read as the fast path with zero fetches in the common case, a one-shot loadQsrFob() fallback fires ONLY when the copy genuinely isn\'t there yet, and the panel keeps showing "Loading…" instead of an empty state until one of the two resolves. The remaining perf win\'s SIZE is explicitly NOT measured live in this sandbox (no authenticated Supabase session) — the design doc\'s old ~14s figure predates #191\'s switch to _pagedParallel and was never itself re-verified after that switch, so quoting it here would misstate a number nobody has actually measured post-#191; what\'s certain is qualitative (an entire duplicate ~13,190-row read, removed), not quantitative. This removes a fetch rather than rescheduling one, which is NOT the shape supabase.js\'s own standing note warns about (two prior scheduling changes that made wall time worse fetching the same bytes) — noted explicitly so a future reader doesn\'t have to re-derive that distinction. The rest of #211 — the actual shell scaffold, report-template consolidation, and Labor instantiation (on hold, see above) — is real, substantial, unstarted work; this commit is scoping + one safe, independently-shippable slice of it, not the shell itself. 1322/1322 tests pass (no new tests — this slice changes data-loading/loading-state plumbing with no new business logic to assert against beyond what existing eom-dashboard coverage, if any, already exercises). Entry chunk gzip grew with this entry\'s own text (measured exact delta in the commit body, not restated here since it will drift again with the PM-review revision) — still comfortably within budget. Separately flagged by the same PM review, NOT addressed in this commit: MERIDIAN_CHANGELOG itself (this array) is ~98 KB gzipped in isolation, roughly 12% of the entire entry chunk — see #230.',
  ]},
  {version:'4.996', date:'2026-08-12', changes:[
  'Issue #230 — MERIDIAN_CHANGELOG (366 entries, 274,943 raw chars) was landing in the entry chunk despite rendering in exactly one place, the About modal: ~98 KB gzipped in isolation, roughly 12% of the compressed entry bundle. Surfaced by the owner while measuring an unrelated PR\'s bundle delta, and it explains why #207\'s lazy-load realization ratios kept undershooting a naive prediction by ~4x — a ~275 KB blob of highly repetitive English prose dominates the chunk\'s gzip dictionary, so moving CODE out of a bundle whose compressed weight is 12% prose reclaims far less than raw-byte arithmetic suggests. #207\'s calibration was measured against a bundle this array was distorting the whole time — treat those ratios as stale; this issue doesn\'t re-derive them, just removes the distortion for whoever does next. Fixed per the issue\'s own preferred option (1: own module + dynamic import), with one refinement the naive version would have gotten wrong: App.js cannot simply import {LATEST_CHANGELOG_ENTRY} from the same module that holds the full array, because a module reached by BOTH a static and a dynamic import path gets pulled into the eager entry chunk regardless of which named export is actually used — the identical INEFFECTIVE_DYNAMIC_IMPORT class of bug #207 already fixed three times this session (inventory.js/one-pager.js/above-store-onepager.js), just at the module-export level instead of the whole-file level. Split into three files instead: changelog-latest.js (LATEST_CHANGELOG_ENTRY only, ~4 KB — the one entry App.js needs synchronously for the footer text and window.__MERIDIAN_VERSION__, standalone, never touches the big array), changelog-data.js (the full MERIDIAN_CHANGELOG array, imported ONLY by the new lazy changelog-panel.js), and App.js itself (now imports exclusively from changelog-latest.js). MERIDIAN_VERSION stays fully derived, never hand-typed — changelog-version.test.js (rewritten, 8 tests) now also asserts App.js imports ONLY from changelog-latest.js and never from changelog-data.js (the exact mistake that would silently undo this fix), and byte-for-byte diffs LATEST_CHANGELOG_ENTRY against MERIDIAN_CHANGELOG[0] so the two hand-maintained copies can\'t drift — the same discipline this file already enforces for the version number itself. Consequence for every future commit: adding a changelog entry now means updating changelog-data.js (prepend) AND changelog-latest.js (replace) in the same commit, not one array-prepend — a small, bounded, and test-guarded cost, not a design nobody will remember to honor. Measured, not the isolated 98 KB figure the issue itself warned not to quote: entry chunk gzip 818.02 KB -> 721.87 KB (-96.15 KB), headroom 31.98 KB -> 128.13 KB. changelog-panel.js + changelog-data.js land in their own lazy chunk together, 269.72 KB raw / 101.08 KB gzip, never fetched unless the About modal is actually opened. 1325/1325 tests pass.',
  ]},
  {version:'4.995', date:'2026-08-12', changes:[
    'Issue #222 — FOB Analysis\'s Total Food Cost tile read "—, needs Ops Report" on cloud-only data, even fully loaded (27 locations / 297 cloud records, "Cloud auto" badge showing). Root cause: analytics.js\'s FOBAnalysisPanel hardcoded pLFoodPct:null for every qsr_fob row in its cloudFobRows mapping, with a comment claiming the feed doesn\'t carry Total Food Cost % — it does. qsr_fob carries all six pnl_food_cost_* fields (begin/purchases/adjustments/transfers/promotions/end), and eom-inventory.js\'s fobSnapshotByStore already derives pLFoodPct from exactly those fields for other panels (At-A-Glance\'s FOB tile shows P&L Food Cost 27.54% MTD off the SAME qsr_fob data — proof the formula already works from this stream, one panel just never called it). Fixed by extracting the Begin+Purchases+Adjustments+Transfers-Promotions-End build-up into a new shared pLFoodCostFromRow(row,sales) (eom-inventory.js), which fobSnapshotByStore now calls too (refactor, not a second copy — the two callers can\'t drift), and wiring FOBAnalysisPanel\'s cloud mapping to call it instead of hardcoding null. The existing manual-override-wins logic at fobRowsEff (":2961", man.pLFoodPct!=null&&!==0 wins) and computeFOBMetrics\' null-aware weighting (rows missing a metric are excluded from that metric\'s Σpct·sales/Σsales, never treated as a false 0) both already handled this correctly — the bug was purely the null-vs-computed value one layer up, not the aggregation math. "Check while in there" per the issue\'s own second ask, at computeMonthActuals\' manual-FOB accumulator ("(r.pLFoodPct||0)*s"): measured, not assumed — the Ops Report parser (parsers/index.js) already coerces a missing "P & L Food Cost %" column to 0 at ingestion (parseFloat(...)||0, unlike salesVsLY\'s ||null), so r.pLFoodPct on a manual row is never actually null by the time it reaches computeMonthActuals; the `||0` there is dead code against today\'s data path, not a live bug. The real ambiguity (missing column vs genuinely-reported 0%) lives upstream in that parser default, touches every other manual pLFoodPct reader (store-dash.js, labor-tools.js), and is flagged as a separate decision rather than silently changed here — documented in a new code comment at the accumulator so a future reader doesn\'t misdiagnose this as live. Stale :2908 comment (claimed the feed "doesn\'t carry" Total Food Cost %) corrected to describe what actually ships now. 4 new tests (src/__tests__/fob-snapshot.test.js) on the extracted pLFoodCostFromRow: the six-field build-up, missing-fields-default-to-0 (matching the existing FOB $ convention, not a fabricated result), sales:0 → null (never a false 0% or divide-by-zero), and a regression guard shaped exactly like loadQsrFob()\'s row (the same object cloudFobRows\' .map() receives) proving the #222 null can\'t recur. 1322/1322 tests pass. analytics.js is a static import already in the entry chunk, so this landed there: entry chunk gzip 818.97 KB -> 820.18 KB (+1.21 KB — the extracted helper + new comments/changelog text, no new markup), still 29.82 KB under budget.',
  ]},
  {version:'4.994', date:'2026-08-12', changes:[
    'Issue #225 — At A Glance was unreachable below the fold on a phone. Owner: "six flexShrink:0 blocks stacked above the only scroll region... on a phone those six eat the viewport and the scroller squeezes to nothing, so the whole lower page is unreachable. The Patch Heatmap was the straw, not the cause." Root cause: the outer container (display:flex, flexDirection:column) pinned SIX blocks (heatmap, movers strip, action-checklist controls, action-checklist body, section-config panel, Loaded Data summary) as flexShrink:0 siblings above the one flex:1/overflowY:auto scroll region — desktop has spare height to absorb that, a phone viewport does not. Fix, per the owner\'s own spec: only the greeting header stays pinned; the other six move inside the scroll region as normal flow. Two things caught while in there, also per the owner\'s instruction rather than left implicit: (1) the outer container ALSO had overflowY:\'auto\' set, so there were nested scrollers — a phone touch-scroll gesture over the pinned area could grab the wrong one; changed to overflowY:\'hidden\' since the outer container has no content of its own to scroll once the header is its only child besides the real scroller. (2) headerScrolled\'s onScroll listener was bound to the outer (now non-scrolling) container — moved onto the actual scroller so it still fires — and Loaded Data\'s own headerScrolled-driven maxHeight collapse (built when Loaded Data was pinned screen-space competing with the rest of the page) no longer serves a purpose now that Loaded Data is normal flow content that scrolls away on its own; removed that collapse and left it always full-height. headerScrolled itself is still real and still used — it continues to drive the pinned greeting header\'s own compact-on-scroll padding/font-size, which is unaffected by this fix. Zero logic changes elsewhere: every moved block\'s JSX and event handlers are unchanged, only their position in the tree and (where it was doing something) their flexShrink move. 1318/1318 tests pass (no new tests — this is a pure layout/DOM-structure change with no new business logic to assert against; verified structurally by reading the resulting render tree). Entry chunk unaffected — at-a-glance.js is lazy via App.js\'s dynamic import, so this landed entirely in its own chunk (112.37 KB gzip 30.07 KB, both essentially flat vs v4.993\'s 112.54/30.11 — this is a reorder, not new code). Entry chunk itself: 818.01 KB gzip, 31.99 KB headroom (unchanged from v4.993 modulo rounding). Not verified live — no browser/phone in this sandbox; the owner asked for this to be tested on a real phone, not devtools emulation, before considering it closed.',
  ]},
  {version:'4.993', date:'2026-08-12', changes:[
    'Issue #220 — Patch Heatmap gets patch-level rollup tiles, unblocked by #219\'s band calibration. Owner, on first use of #213: "It\'s labeled Patch Heatmap, yet shows all locations individually (I like) — but why not include a tile for each patch rolled up as well?" The trap the issue named explicitly: colouring a patch tile by its worst store is that store again wearing a bigger badge — zero new information, and it goes red the moment any single store struggles. Built the other way: new patchDimensions() AGGREGATES the underlying raw numbers across a patch\'s member stores FIRST, then derives each dimension from the aggregate, reusing storeDimensions\'/#219\'s bandFromGap()/bandColor() and the same worst-of-N rule (factored into a shared statusFromDims() both store and patch tiles now call). Sales and FOB have real dollar figures this far upstream (store.pSales/pLY, fobRow.sales/fob) so those sum raw $ directly — a true dollar-weighted aggregate; Labor and Speed have no raw labor-$ or car-count surviving to this layer, so both use sales-weighted averages of the per-store value (the same convention labor-tools.js\'s own district rollup already uses for labor%, not a new invention — and an honest size-proxy for Speed specifically, since OEPE has no car-count weight anywhere else in this app either). FOB\'s target is also sales-weighted across the same member stores, mirroring computeFOBMetrics\' (analytics.js) own wTgt pattern. Controls (ctrlScore) is deliberately excluded from the patch rollup, same reasoning as #219: a composite computeOpsScore builds from several inputs, not a raw gap-to-target this function can aggregate honestly without re-deriving that scoring logic per patch. Grouping comes from supervisorGroups() (constants.js) — the LIVE, Management-editable org chart App.js already keeps in sync on every settings load — not the frozen INV_ORG_COORDS.sup snapshot the issue also mentioned as an option; they match today but only one stays correct after a reassignment. First-writer-wins on the loc->patch inverse lookup guards the issue\'s "don\'t double-count a store in two patches" requirement. UI: a patch row above the store grid (different unit, different question, laid out separately per the issue\'s own suggestion), click-to-expand showing per-dimension detail with store-membership counts, same gray-? unknown state for insufficient data, no coaching hook added (a supervisor-level "Coach this" is a natural future extension but out of scope here). 9 new tests (src/__tests__/patch-heatmap-rollup.test.js): every aggregation is checked against a hand-computed dollar/sales-weighted answer using fixtures deliberately shaped so a naive per-store average would diverge from the correct one, Controls exclusion, missing-data handling, and the core anti-pattern guard itself — a small store bad enough to be critical alone bands clean once aggregated into a much larger healthy patch. Entry chunk barely moves from the feature itself — patch-heatmap.js is lazy via at-a-glance.js, so all new code landed in that chunk (108.52->112.54 KB raw / 29.43->30.11 KB gzip); the small entry-chunk delta that remains is this changelog text, same pattern as every other entry here. Not verified live — no browser/Supabase session in this sandbox; the owner should confirm the patch row groups stores as expected and no supervisor shows an empty or duplicate patch. Full findings: memory/project-patch-heatmap-calibration-219.md (folded in — this issue was explicitly sequenced right after #219 on the same feature).',
  ]},
  {version:'4.992', date:'2026-08-12', changes:[
    'compute6wk\'s kvsu window silently excluded the last closed business day\'s own rows during roughly a 4-hour window each night — found live while re-running the full test suite (a wall-clock date rollover exposed it, not code I was touching). Root cause: the kvsu tally\'s upper bound compared row.date directly against _lastClosed6wk (a MIDNIGHT boundary from lastClosedBusinessDay()), but rows are stored at NOON (the same convention labor-tools.js\'s own sodOf() comment documents) — so whenever businessDate()\'s "now minus 4 hours" rolls the calendar date back (i.e. actual local time is between roughly midnight and 4am), a real noon-stamped row on the newly-computed last-closed day compares as later than that day\'s midnight and gets wrongly dropped. Reproduced with node -e against the live clock before fixing, not assumed. Fixed with eodOf(_lastClosed6wk) instead of the raw midnight value — the same day-boundary idiom already used elsewhere in this repo for exactly this class of bug. Caught by forecast.test.js\'s own kvsu regression test (#150/#178 item 6), which failed for the first time this session the moment the wall clock crossed into the danger window — a genuine, reproducible production bug, not test flakiness, though it was a test with real wall-clock-relative fixtures that happened to surface it. No new test added: the existing test already covers this exact scenario and now passes deterministically once the boundary itself is fixed, regardless of what hour it runs.',
  ]},
  {version:'4.991', date:'2026-08-12', changes:[
    'Issue #219 — calibrate the Patch Heatmap\'s badAt bands (measured, decided, shipped). First production look flagged 18 critical / 8 watch / 1 clean — 26 of 27 stores — the same day the rest of the dashboard read 25 of 27 as trusted-healthy; those can\'t both be true. Root cause was TWO layered problems, not one: the original badAt constants (Sales 15, FOB 3, Labor 3, Speed 20) were chosen when #201 shipped, not derived from a distribution; and separately, a structural fact the measurement script itself couldn\'t flag — band=100*(1-gap/badAt) with clean>=80/watch>=50 means WATCH fires at 0.2*badAt and CRITICAL fires at 0.5*badAt, not at badAt itself, so a raw p90-of-over-target reading has to be DOUBLED before it\'s usable as badAt. Owner ran scripts/measure-patch-heatmap-bands.mjs against live production and supplied the corrected values: Sales 15->27, FOB 3->1.9 (tighter — was genuinely loose), Labor 3->8.8, Speed 20->73 (~3.7x too tight, did most of the original damage). Controls (ctrlScore) stays uncalibrated by design — a composite score, not a raw gap-to-target, correctly out of scope for this script. Expected result on the measured distribution: ~6 critical / 14 watch / 7 clean, vs the original 18/8/1 — not independently re-verified live in this sandbox (no browser/Supabase session here); if the live grid doesn\'t land near that split, that\'s a real finding to raise, not something to nudge quietly. 9 new tests (src/__tests__/patch-heatmap-bands.test.js) lock in both the corrected band/badAt relationship AND the four new constants together, each anchored exactly at its dimension\'s new critical boundary (gap = 0.5*badAt) so a regression in either the formula or a constant fails independently. #220 (patch rollup tiles), blocked behind this landing, is now unblocked. Full findings: memory/project-patch-heatmap-calibration-219.md.',
  ]},
  {version:'4.990', date:'2026-08-11', changes:[
    'Issue #208 — coaching feedback loop v1, the fifth leg of Push 3 (identify -> assign -> intervene -> measure -> VERIFY) and per the owner\'s own framing the only genuine differentiator on the table (QSRSoft\'s CoachQ is an AI chat assistant, not measure-and-verify coaching — SAGE already matches it). Today Meridian tells the owner what is wrong; this is the piece that tells him whether what he did about it worked. New Supabase-persisted coaching_cycles table (tenant_id + RLS, owner needs to run supabase/schema-coaching-cycles.sql) — one row per coaching intervention: store, a SPECIFIC metric (food cost and labor only, per the issue\'s own scope discipline: labor_pct + 4 FOB components), an auto-captured baseline, a note, an auto-set +30-day review date, and a result/verdict recorded once the follow-up window closes. Five design-doc rules (memory/project-coaching-feedback-loop.md), each enforced structurally rather than by convention: (1) baseline is ALWAYS auto-captured by engine/coaching-loop.js\'s snapshotMetricValue — there is no code path anywhere that accepts a typed number; (2) the follow-up comes to him — a due cycle surfaces as a new "coaching-review" item type inside the EXISTING Needs Attention feed (attention-feed.js gains a coachingReviewFeedItems passthrough + buildAttentionFeed a coachingItems input), not a new panel; (3) starts from an existing finding — loc+metric are always caller-supplied (Patch Heatmap\'s FOB/Labor dimension rows get a new "🎯 Coach" button, scoped to only those two dimensions); (4) verdict is measured, never self-reported — computeVerdict() reads a NOISE_THRESHOLDS map that starts EMPTY, per the issue\'s own explicit v1 fallback ("if this measurement isn\'t ready, ship the loop recording cycles WITHOUT verdicts rather than with wrong ones") — no live Supabase session in this sandbox to run scripts/measure-coaching-noise-threshold.mjs, so every verdict is null today, a real cycle log with an honest gap rather than a confident wrong one; (5) single-user first — no ack/notification/GM-login machinery anywhere in this feature. New shared src/views/coaching-modal.js (ModalShell-based) handles both "start coaching" (note only — everything else auto) and "record follow-up" (shows a live baseline/result preview before saving, so the number shown is never a guess at what will be recorded) — genuinely two taps, not a form. A real correctness fix found while building this: FOB baselines/results are DOLLAR-WEIGHTED (Σamt/Σsales over the trailing window) from ds.qsrFobRows, matching computeFOBMetrics\' (analytics.js) own established convention — NOT a mean of daily ratios, which the already-shipped scripts/measure-coaching-noise-threshold.mjs (written in an earlier segment) was doing; fixed that script\'s FOB rolling-movement math too, so the eventual measured threshold applies to the same quantity the verdict will use. labor_pct baselines instead reuse metricAvg(...,\'laborPct\') as-is (a simple mean) — deliberately, so a coaching baseline reads the same number every other Labor Tools panel already shows, not a more "correct" but different one nobody else displays. 16 new tests (src/__tests__/coaching-loop.test.js): the dollar-weighting distinction (with a fixture deliberately shaped so a naive mean-of-ratios would diverge from the correct answer), the in-progress-day exclusion (signature #4), auto-capture (never a typed baseline), due-for-review filtering, the null-verdict v1 fallback proven against the ACTUAL empty NOISE_THRESHOLDS state (not just asserted in a comment), and the future-state verdict classification once a threshold IS registered. Not chased: coaching-modal.js/coaching-loop.js land in the entry chunk (analytics.js, which needs the review modal, is itself still fully static — the same open "could analytics.js split its landing tiles from its deeper panels" question #207 flagged and deferred, not resolved here). Entry chunk gzip 813.82 KB -> 815.30 KB (+1.48 KB — almost entirely this changelog text; the feature\'s own code landed close to net-neutral, see memory doc for the exact before/after breakdown). Still comfortably within budget, 34.70 KB headroom. Full findings: memory/project-coaching-loop-208.md.',
  ]},
  {version:'4.989', date:'2026-08-11', changes:[
    'Issue #210 — split the labor gap two ways, from data already pulled. qsr_daily_activity has always carried total_needed_hours, total_scheduled_hours and actual_punched_hours hourly, but loadQsrActSummary never selected total_scheduled_hours on either of its two read paths, and the preferred rollup table (qsr_daily_activity_rollup) never summed it either — so Meridian only ever showed the combined actual-vs-needed gap. New engine/labor-gap-split.js splits it: Needed→Scheduled = planning accuracy (coach the scheduler/forecast), Scheduled→Actual = execution (coach the shift manager) — the owner\'s "2 defined areas to coach and teach and push," aimed at two different people instead of one blended number. Buckets by the Wed–Tue PAY week explicitly (never Mon–Sun — mixing conventions "produces a labor % that is wrong and does not look wrong," per the issue\'s own caution), excludes any day after the last closed business day before bucketing (signature #4 — an in-progress day, even the literal last day of its own pay week, must not leak into a week total), and always surfaces the latest COMPLETE week per store so the coaching number never moves while you look at it. Scheduled hours can be genuinely UNKNOWN, not zero: src/lib/supabase.js now threads total_scheduled_hours through both loadQsrActSummary paths — the hourly fallback (column always present, ordinary || 0) and the rollup path (?? null, since the column doesn\'t exist in qsr_daily_activity_rollup until the owner runs the new supabase/schema-qsr-rollup-scheduled-hours.sql, mirroring the dt_heldtime rollout from #183) — and a pay week containing any such row reports planningGapHrs/executionGapHrs as null rather than a fabricated number built on a partial sum; combinedGapHrs (the old single figure) is unaffected since actual/needed have always been carried. scripts/qsrsoft-dar-pull.mjs\'s refreshRollup() now sums total_scheduled_hours too — non-fatal ahead of the migration, same non-fatal-upsert-failure pattern dt_heldtime already established, so this ships safely before the owner runs the SQL. Surfaced as a new "🎯 Planning/Execution" tab in Labor Tools\' Labor Analytics panel (labor-tools.js, lazy-loaded — zero entry-chunk cost), independent of the panel\'s own flexible period selector since the split is inherently a pay-week construct: per-store Needed/Scheduled/Actual/Planning-Gap/Execution-Gap/Combined plus a "Coach: Scheduler / Shift Manager / On plan" column — the on-plan gate reuses the panel\'s own already-shipped Act-vs-Need color banding (30/60 hrs) rather than inventing a new unmeasured threshold, and shows a visible amber banner (not a silent blank column) while the migration hasn\'t run. Deliberately deferred, not built in this commit: §4.2\'s rate/hours/sales decomposition (needs actual_punched_dollars threaded through the same two loader paths and its own rollup migration — a comparable-sized follow-up, not "mostly wiring") and §4.3\'s optional intraday heat map (the issue\'s own instruction: drop rather than squeeze in if bundle headroom is tight — and Signals\' existing HourlyDetail already prototypes the identical per-hour gap-vs-need/gap-vs-sched math for today\'s single day, worth generalizing rather than rebuilding when this is picked up). 8 new tests (src/__tests__/labor-gap-split.test.js): pay-week bucketing, the split arithmetic, the in-progress-day exclusion using Aug 11 2026 (a Tuesday — the deliberately awkward case of today being the LAST day of its own pay week), complete-vs-current-week detection, the null-vs-fabricated-zero distinction, multi-store bucketing, and the district summary rollup. Cannot verify against live data or confirm the migration\'s real-world effect — no authenticated Supabase session in this sandbox; the owner needs to run the new SQL file for the split to populate beyond "—". Entry chunk essentially unchanged (812.21 KB -> 813.78 KB gzip — this changelog text itself, plus the new supabase.js/dar-pull.mjs code, both in the entry chunk; labor-tools.js is lazy, so the new tab\'s own markup landed entirely in its own chunk, 26.42 KB -> 27.85 KB gzip).',
  ]},
  {version:'4.988', date:'2026-08-11', changes:[
    'Issue #209 — data-discipline score, the trust leg ahead of #210 (labor diagnosis) and #208 (coaching verify). QSRSoft\'s own Food Cost Routines KB says enter Raw and Completed waste daily; unrecorded waste doesn\'t vanish, it lands in Unexplained and inflates it — a store with missing entries has a FOB% that isn\'t comparable to any other store\'s. New engine/waste-discipline.js derives each store\'s OWN expected submission days-of-week from 8 weeks of observed qsr_waste history (>=3 occurrences and >=75% hit rate — reusing count-cycle.js\'s already-measured COVER_FRAC, not inventing a new unmeasured threshold), never assuming daily-for-everyone; flags missing days over the trailing 14 against that derived pattern; excludes full-closure holidays (isHoliday); and estimates $ impact per missing day as the store\'s own average entry for that type — an estimate, not a decomposition of Unexplained (confirmed no code in this repo traces Unexplained back to a specific entry). "Missing != zero" throughout: qsr_waste rows are discrete per-entry events with no null-vs-zero column, so a real $0 entry and a genuinely absent row are tracked as different facts everywhere in this file, and a store with too little history reports pctOnTime:null rather than a false 100%. wasteRows joins auditRows in LAZY_FILL_SOURCES (metric-source.js) — load-on-open only, never eager at startup, reached via explicit ensureLazyFill() calls rather than a metricDaily/metricSeries chain, since day-presence pattern detection doesn\'t fit that resolver\'s per-day scalar model. New metric-source.js export isLazyFillError(src), so a "load on open" consumer can show a failed-badge instead of reading a failed fetch identically to "loaded, zero rows" — the same false-all-clear class the FOB Report fix (v4.976) addressed for a different stream, now available to every LAZY_FILL_SOURCES consumer. Surfaced in FOBAnalysisPanel (analytics.js) — the trust context is exactly where the coaching money conversation already happens — as a "📋 Waste-Entry Discipline" section: district summary line, per-store missing-day counts by type and estimated $ impact, sorted worst-first, hidden entirely (not a false "all clear") when there\'s nothing to flag or the pattern can\'t be derived yet, and a visible red badge distinguishing a failed cloud read from genuinely-zero missing. 11 new tests (src/__tests__/waste-discipline.test.js) covering derived-pattern correctness, the COVER_FRAC threshold, holiday exclusion, and the missing-vs-zero distinction; 1 existing test updated (metric-source-lazy-fill.test.js\'s #191 scoping assertion, now covering both LAZY_FILL_SOURCES entries). Entry chunk gzip 809.45 KB -> 811.04 KB (+1.59 KB — analytics.js is a static import, this section\'s markup and the new engine module both land in the entry chunk; still 38.96 KB under budget). Not yet built: the UI wiring is read-only (no acknowledgment/dismissal machinery) — #208\'s coaching loop is the natural place to add that, not this issue.',
  ]},
  {version:'4.987', date:'2026-08-11', changes:[
    'Issue #214 — wire the Inventory Intelligence panel (Service/Production/Overstock/Transfers, four-section report, haversine-based transfer suggestions, Paper/Food/Condiment class filters) to auto-pulled qsr_inventory_summary, auto-first with manual XLSX upload as gap-fill only — never overriding a cloud row. Critical finding the issue\'s own framing missed, verified by grep not assumed: qsr_inventory_summary has NO producer script anywhere in this repo (saveQsrInventorySummary is defined, never called) — the schema/loader exist, live data does not yet. The wiring is correct and will populate automatically the moment a pull ships; today it shows an honest "no cloud data yet" badge and falls back to manual upload, same as before but now with a visible, correct signal instead of reading as silently broken. Two traps addressed: (1) cls vocabulary — mapInvClass() maps 2 known QSRSoft synonyms explicitly and passes anything unrecognized through as-is (visible under "All Classes," not silently forced into a wrong bucket) plus a header badge naming any unrecognized values seen; (2) a failed cloud read never looks like an empty one — copied the FOB panel\'s exact 3-state fix (loading/failed/has-rows) with a visible header pill, not tooltip-only. One thing flagged, not guessed: eachFmt (cases vs eaches) has no signal in the cloud schema the way the manual parser gets it from the filename — defaulted to cases with an explicit ⚠️ UNVERIFIED code comment for a future live-data check. Folded in #207 batch-2\'s first item as a genuine two-for-one: inventory.js (76KB) was still a static import in App.js because pipeline.js also statically imported parseInventoryData from the same file — split parseInventoryData/INV_MASTER/classifyInvArea into new parsers/inventory-parse.js so pipeline.js\'s import no longer defeats inventory.js\'s new lazyPanel() wrapper. Measured: entry chunk gzip 818.69 KB -> 808.30 KB (10.39 KB reclaimed), confirmed the INEFFECTIVE_DYNAMIC_IMPORT warning for inventory.js is gone from build output. Unhidden per the issue\'s own "then unhide it" instruction: panel-registry.js kind changed optional->nav, removed from constants.js OPTIONAL_PANELS. New src/__tests__/inventory-cloud-wiring.test.js (12 tests) found and fixed one real bug before shipping: avgDailyTxnsByLocMonth crashed on a null array element. Full findings: memory/project-inventory-auto-wiring-214.md.',
  ]},
  {version:'4.986', date:'2026-08-11', changes:[
    'Issue #201 correction — Patch Heatmap now colors by "how is this store doing overall" (worst-of-N vs the store\'s own targets), not v4.985\'s Needs Attention severity. Root cause of the miss: the owner\'s actual decision was made live in conversation and recorded only in a PR comment; the issue BODY still said Needs Attention was the strongest candidate, and v4.985 built exactly what the body said. Body corrected at source, and this fixes the code to match. Owner, verbatim: "I believe how the store is doing overall… green is good and red is bad for analysis, therefore we would be drawn to red needing attention" -> "I\'m good with worst of n." New design: per store, evaluate 5 dimensions against that store\'s OWN targets — Sales (matched-day vs LY), FOB (vs tFOBTarget), Labor (vs resolveLaborTarget), Speed/OEPE (vs tOepe), Controls (ctrlScore) — color by the worst one, and NAME it in the cell (e.g. "Ardmore-Broadway 🔴 FOB", "Tishomingo 🟡 Labor"). Within-store and absolute, never a cross-store ranking: all healthy -> nothing red, all bad -> all 27 red, by construction (each cell\'s color is a pure function of that one store\'s own numbers). Normalizes every dimension to a common 0-100 "health band" before comparing worst — Ops-type scores are already 0-100, FOB/Labor gaps are percentage points, Sales/Speed gaps are percent, so a raw min() would have compared 3pp against 12% and picked a wrong worst; bandFromGap() converts all of them to the same band first. A dimension missing its inputs for a store is excluded from that store\'s worst-of-N, never scored as good or bad by a fabricated default; a store missing ALL dimensions renders the same gray "unknown" state as before (still never green). groupAttentionByStore stays exactly as it was, unmodified, still driving its own panel — deliberately not blended into this grid, per the issue\'s explicit instruction that they answer different questions. Kept from v4.985: the grid, glyphs, click-to-expand (now lists all 5 dimensions sorted worst-first instead of Needs Attention findings), the "Open store" button. New export: attention-now.js\'s fobByStoreLatest (was module-private, now reused here — same calc, second consumer).',
  ]},
  {version:'4.985', date:'2026-08-11', changes:[
    'Issue #201 — Patch Heatmap: all 27 stores as a colored status grid at the top of At A Glance, answering "which store needs me today?" spatially instead of by reading a tile strip and a list. Color = Needs Attention severity (engine/attention-feed.js\'s groupAttentionByStore — the SAME per-store rollup AttentionPanel already uses, not a new score, per the issue\'s own "use what already exists" instruction). Answers the issue\'s own open question ("needs me right now, or how is this store doing overall?") as "needs me right now" — the issue names Needs Attention as the strongest candidate for exactly that reading; Ops Score / tracking-to-plan are the "overall health" / "today\'s pace" readings and are deliberately not blended in. No charting library (CSS grid + colored cells), no animation (owner declined it in the issue — draws the eye to what\'s live rather than what\'s important), colorblind-safe (color is always paired with a glyph and, where relevant, a count — never color alone: ! for critical, • for watch, ✓ for clean, ? for no data). A store with no data renders as a distinct gray "unknown" state, never green — the same failure class the FOB Report false-all-clear fix (v4.976) addressed earlier this session. Click a cell to expand an inline detail (worst findings + an "Open store" button wired to the existing onOpenStore callback) instead of a second navigation layer. New src/views/patch-heatmap.js, lazy by construction — imported only from at-a-glance.js, which App.js already dynamic-imports (confirmed before writing anything, per the standing "check whether an affordance already exists" rule), so this adds zero weight to the entry chunk; measured, not assumed — entry chunk gzip actually went DOWN (831.60 KB -> 817.16 KB) in the same build that added this, most likely because attention-feed.js/attention-now.js gained a second (lazy) import path and rolldown re-chunked accordingly — a genuine, measured outcome, not a claimed mechanism.',
  ]},
  {version:'4.984', date:'2026-08-11', changes:[
    'Issue #207 — reclaim entry-chunk gzip budget, and make the 850KB ceiling load-bearing instead of a number a human has to remember to look at. The build itself had been printing two [INEFFECTIVE_DYNAMIC_IMPORT] warnings unread: one-pager.js (64KB) and above-store-onepager.js (55KB) were already behind lazyPanel() in App.js, but forms-library.js (6KB) and report-subscriptions.js (13KB) — the panels that each statically import one of them — were themselves still static imports in App.js, which pins the whole dependency chain into the entry chunk regardless of the lazy wrapper further down. Converted both to lazyPanel(), matching the other 26 lazy panels already in App.js. Measured, not assumed: entry chunk 2786.50 KB -> 2750.40 KB raw, gzip 841.72 KB -> 831.60 KB — 10.12 KB reclaimed (smaller than a naive raw-bytes-times-ratio estimate; real bundler chunking split some formerly-shared code differently than a flat ratio predicts). Both warnings gone from the build output; the remaining INEFFECTIVE_DYNAMIC_IMPORT (supabase.js) is a different, expected case — that module is legitimately both a static and dynamic import target app-wide and is not part of this fix. Second half: added scripts/check-bundle-budget.mjs, now the actual `build` npm script (`tsc -b && node scripts/check-bundle-budget.mjs`, replacing the bare `vite build` call) — it runs vite build, prints the same output as always, and FAILS (exit 1) if the entry chunk\'s gzip figure exceeds 850KB. No CI workflow changes needed: ci.yml and deploy.yml already invoke `npm run build`, so both are gated automatically, and it fails identically for a local `npm run build`. Deliberately parses vite\'s OWN printed gzip line rather than re-gzipping the output file independently — measured a real ~10KB gap between a plain `gzip -9` recompression and what vite/rolldown\'s build reporter prints for the same file, so an independent re-measurement would have silently enforced a different (more lenient) number than every gzip figure already cited in this repo\'s commit history. Not done: a redundant Vitest test that re-runs the build (would roughly double `npm test`\'s ~11s runtime for no additional safety `npm run build`\'s own gate doesn\'t already provide — CI already runs Test and Build as separate steps). Wider bundle-reduction candidates identified but explicitly deferred to a separate pass, not bundled here: record-day.js (40KB), feature-requests.js (40KB), management.js (38KB), location-intel.js (50KB), lifelenz.js (43KB), smart-targets.js (51KB) — all still static; and whether analytics.js (528KB, currently static since At-A-Glance is the landing view) could split its landing tiles from its deeper panels is an open question for that pass, not this one.',
  ]},
  {version:'4.983', date:'2026-08-11', changes:[
    'Issue #197 Slice 1 (first cut) — de-materialize Type A calendar events. The design doc traced 25,783 org_events rows to "a rule generated them and wrote 27 rows per date" (US federal holidays x 27 stores). Traced the actual write path before touching it: there are FOUR separate holiday-tagging mechanisms in this codebase, not one — autoTagHolidays() (utils/holidays.js, ran automatically on EVERY data load: pipeline.js buildDS/mergeDS ds.loaded blocks + App.js IDB-restore path), Review Pack generation\'s inline auto-tag (calendar.js), the "Tag All Holidays" button (analytics.js), and the "Auto-Tag Holidays" button (store-dash.js). Only the first is the silent-runaway one — zero owner action, compounding across months of uploads; the other three are explicit, owner-clicked, bounded actions and are untouched by this change (a separate decision the owner hasn\'t made). Removed only autoTagHolidays()\'s 3 automatic call sites. Verified safe by grep, not assumption, per the standing "measure don\'t reason" rule: every real forecast exclusion/adjustment site (forecast.js\'s candidate-day exclusion and holiday-LY adjustment) already calls isHoliday()/HOLIDAY_MAP directly and never read the materialized tag — a fullClosure-based "excluded from calibration upstream" code comment turned out to be stale/aspirational documentation, not a real dependency. So the automatic materialization was pure redundant computation; forecast accuracy is unaffected by removing it. Added scripts/cleanup-materialized-holiday-events.mjs (dry-run first) for the owner to optionally purge the rows that already accumulated, scoped precisely to event_type=\'holiday\' + a known HOLIDAY_MAP label — leaves real manual entries and all retail-events rows untouched. Also checked and can report: #192 P0 item 4 ("52 more" unreachable in Calendar Manager) does NOT actually need this fix — the day cell is already clickable and opens an unpaginated modal listing every event for that day; the design doc\'s prediction that volume was the cause is unconfirmed and should be re-checked against a live report before assuming. Full scope, what\'s deferred, and why: memory/project-events-redesign.md §3.',
  ]},
  {version:'4.982', date:'2026-08-11', changes:[
    'Issue #192 P0 (item 3) — Event Impact Registry only populated the sports category, reading as "no impact" rather than "not measured" for the other 8 declared categories. Scoped before building: sports was already seeded; the 4 retail categories (tax_free/black_friday/small_biz_sat/cyber_monday) already had a COMPLETE measurement pipeline (engine/retail-events.js + scripts/measure-retail-impact.mjs, reads labor_rows sales history, n-shrunk lift per store) that had simply never been scheduled or run — added .github/workflows/measure-retail-impact.yml (monthly cron + manual dispatch, idempotent upsert on (loc,event_type)) and added it to sync-failure-watch.yml per the standing automated-pull rule. 5 of 9 categories now populate. Also found two things that DON\'T need building: the panel (event-impact.js:119-120) already distinguishes "not measured yet" from "measured, zero impact" with explicit copy — the v4.870 concern the P0 raised doesn\'t actually apply to the UI, only to the missing pipelines; and `holiday` isn\'t a registry gap at all — it already has a separate, working, auto-applied mechanism (utils/holidays.js, wired directly into forecast.js) that doesn\'t route through this registry. Genuinely still open, not attempted here: `event` (festival/fair) and `weather` have no measurement pipeline (would need one built following the retail script\'s shape); `promo` (LTO) has no structured date source in the codebase at all to measure from. Full scope + per-category verdicts: memory/project-events-redesign.md §9.',
  ]},
  {version:'4.981', date:'2026-08-11', changes:[
    'Issue #192 P0 — the Calendar Manager and store-dash tag-audit modal both warned that heavy tagging leaves a store with no LY comparison. True before v4.924, when tag presence alone excluded a candidate day (the exact bug that broke Tishomingo district-wide on 2026-08-08 with 450 tagged days); false since — forecast.js\'s tag-independent _robustCandidates now excludes a candidate only when its own sales/GC is a statistical outlier against its same-DOW peers. The banner kept asserting the old mechanism above a list of all 27 stores each showing ~950 tagged days, reading as a district-wide alarm for something that no longer happens. Promoted to P0 in a #192 comment: the app asserting something false about its own forecast behavior outranks a cosmetic bug, and it explicitly blocks #197 ("fix the stale banner first" — otherwise the next reader re-derives the old, wrong model from the UI). Rewrote both sites to state the real mechanism and reframed from a warning (amber) to an informational note (blue, matching the existing info-banner convention already used elsewhere in calendar.js) since tagging no longer has any forecast-exclusion effect to warn about; kept the tagged-day count as a "worth checking these are real events" prompt. Scope note: this is the #192 P0 item gating #197 in the owner-relayed queue (#192 remaining P0s -> #197 -> #201 -> #202/#198); the other four #192 P0 items (Snapshot fear, Event Impact Registry unwired categories, Calendar "52 more" unreachable, yearly-target root cause) are not addressed here — Event Impact Registry is next (also on #197\'s critical path), the rest are deferred as non-blocking.',
  ]},
  {version:'4.980', date:'2026-08-11', changes:[
    'Issue #189 — the click-trace instrument (?clicktrace=1) could not decompose the ~8.6s startup block it measured; fixed the instrument, not the app. Root cause: App.js\'s and AppSidebar\'s render-start->layout-effect spans both end at the SAME commit flush (layout effects fire bottom-up in one synchronous burst after commit) — nested, not additive, exactly the misreading the issue recorded happening once already ("App tree 8821ms, AppSidebar 8647ms" does NOT mean the sidebar cost 8647ms; it means AppSidebar\'s span is CONTAINED in App\'s). Extended the same span pattern one level deeper onto all 4 possible active-panel views (AtAGlance/DistrictGrid/OrgView/StoreDash, whichever is actually mounted, siblings of AppSidebar under App) and added automatic same-commit correlation + subtraction to the report (utils/click-trace.js\'s new selfTimeLines()) instead of requiring the reader to eyeball two numbers and subtract by hand. Verified the correlation math against a hand-simulated 3-level commit: reproduces the issue\'s own by-hand 174ms App-self figure exactly, and the three self-time pieces sum back to the original total. Still NOT solved: the active-panel residual isn\'t decomposed further (needs a live capture to know which panel to instrument next), and DOM-commit time is still bundled with JS render inside that residual (needs React Profiler, already established unavailable in this app\'s production build). Not measured live — this sandbox cannot run the app in a browser; owner needs to re-run ?clicktrace=1 in production and paste the new self-time report section. Full writeup: memory/project-instrument-fix-189.md.',
  ]},
  {version:'4.979', date:'2026-08-11', changes:[
    'Issue #190 — computeInsights logged one line per signal runner UNCONDITIONALLY: 34 runners (not the 39 the issue estimated — measured via the actual array, not assumed) x 2 startup calls (live + restore) = ~68 lines on every load, on top of everything else the app logs. Guarded behind a new ?signals=1 URL param (same convention as ?clicktrace=1, persisted via sessionStorage so it survives SPA navigation) — off by default, so the dump is opt-in. The null-branch line used to log fn.name, which is meaningless in a production build (minified to tB/nB/wz/...); now reads the signal\'s own stable id straight out of the function\'s source text via fn.toString() — minifiers rename identifiers, not string literal VALUES, so the id:\'gc_avg_check\' literal each runner already returns survives intact (verified against the actual minified production bundle, not just reasoned about). Self-maintaining: no hand-typed id lookup table to keep in sync when a runner is added or removed. Swept App.js, engine/, and features/ for other unguarded per-item console.log loops in the automatic-startup path per the issue\'s ask — found none: everything else is one summary line per load stage (App.js\'s ~54 "[Meridian] Loaded N rows" style logs), a materially different, much smaller volume class than the 78-line-per-computation pattern this issue is about.',
  ]},
  {version:'4.978', date:'2026-08-11', changes:[
    'Issue #191 — startup waterfall: manual-fallback lazy fill + qsr_fob parallel pagination. Part 1: auditRows (the highest-volume manual-fallback stream, 21,929 of ~42,500 rows in the eager T3 startup batch) no longer loads unconditionally on every login — metric-source.js now triggers an on-demand load the first time a metric chain that includes it is actually resolved, instead of App.js\'s _stAuditRows eager stage (removed). Deliberately scoped to auditRows only, per the issue\'s own sequencing (laborRows/opsRows/ctrlRows/fobRows unchanged); deliberately NOT gap-scoped (loads the whole stream once, on demand, rather than just the missing loc/date range — the owner\'s fuller demand-queue design is real follow-up work, not required to capture this win). The 3 non-resolver auditRows consumers each got a different, deliberate treatment: RegisterAuditTab (store-analytics.js) now triggers the fill on mount and shows a distinct "Loading…" state instead of a possibly-wrong "No data" (v4.870 rule); pipeline.js\'s empRisk deliberately does NOT trigger it — measured (not assumed) that ds.empRisk is written in 5 places and read in zero, so wiring a trigger would just reintroduce the eager cost for a field nothing consumes; DataManagerPanel\'s coverage tiles (analytics.js) trigger the fill on modal-open (a genuine "load on open," not the "coverage tile in always-mounted UI" trap the issue warned about, since this panel is a rarely-opened modal) and show a neutral "…"/"(loading…)" annotation instead of a misleading red zero while pending. Part 2: loadQsrFob (supabase.js) was the last qsr_* loader on fetchAll\'s strictly-serial pagination — measured ~30 back-to-back requests, ~14s (22% of a 63s startup) for a 500-day read. Switched to _pagedParallel (already used by labor_rows/peaks_rows/audit_rows), which gained an inCol/inVals option so loadQsrFob\'s dates-array mode keeps working identically. Same 400-row page size preserved (Notes 60 bug #1). ⚠️ Neither change\'s wall-clock effect is verified live — this sandbox has no authenticated Supabase session. supabase.js\'s own standing note records two PRIOR scheduling changes that made wall time WORSE fetching the same bytes; per the issue\'s explicit instruction, a measured regression means revert, not tune. Full reasoning + what was cut in memory/project-lazy-fill-191.md.',
  ]},
  {version:'4.977', date:'2026-08-11', changes:[
    'Issue #192 P1 — the modal/scroll sizing defect reported five times, fixed as one class plus a guard test rather than five patches. Item 4 (Projections → supervisor expand) turned out to already be fixed (v4.966/#178) — no code change, confirmed via git log. The other four map to genuinely separate root causes, not one shared ModalShell bug (none of the five actually go through ModalShell): (1) District EOM Summary\'s per-store table — and 3 more tables in the same file — used `overflowX:\'auto\'` + `table{width:\'100%\'}` with no minWidth, which caps the table at its wrapper\'s width so there is nothing to scroll and the browser compresses/cuts off columns instead; fixed to `width:\'max-content\',minWidth:\'100%\'`, the pattern already correct elsewhere (analytics.js\'s MonthlyProjectionsPanel). A repo-wide guard test (scroll-table-width.test.js) found the SAME anti-pattern in 12 more tables across 6 other files (above-store-onepager.js, analytics.js, at-a-glance.js, sage.js, scheduling.js, smg-voice.js) — all fixed identically; one true exception (scheduling.js\'s StoreScheduleTable, which already forces the same overflow via per-column minWidths) is documented in the test\'s EXEMPT list. (2) Planning → Monthly had column-freeze (sticky left) but not row-freeze (sticky top) on its table header — the one table in the codebase with only half the freeze; added, plus the Planning/Scheduling hub shells\' missing paddingBottom, which let a horizontal scrollbar sit flush against the browser\'s physical bottom edge. (3) Planning → PACE embeds CurrentMonthPaceSection, which hardcodes maxHeight:240 — correct when it\'s one section on a bigger page, wrong when it IS the whole panel body; added a fillHeight prop so pace-to-target.js can let it grow to fill instead of leaving most of the modal empty below a small table. (5) Calendar grid event chips were single-line-truncated; switched to a 2-line clamp so a longer label is actually readable instead of relying entirely on the hover tooltip.',
  ]},
  {version:'4.976', date:'2026-08-11', changes:[
    'Issue #192 P0 (part 1) — EOM FOB Report was resolving 0 stores and rendering a green "clean ✓" checkmark: a food-cost panel silently reporting an affirmative pass on zero data instead of surfacing that nothing was checked. Root cause: fobReport\'s fobByMonth never remapped fobByStore\'s padded qsr_fob loc keys through unpad(), unlike allRows 80-odd lines below in the same file, which already carries that exact remap. Fixed narrowly at that one call site — NOT at the loadQsrFob() loader or inside fobSnapshotByStore(), both considered and reverted mid-session after a repo-wide grep found 4 call sites (eom-supervisor.js, labor-tools.js, sage.js, eom-share-view.js) that explicitly pad to match the loader\'s current output; normalizing there would have silently broken all four. Independently: buildFobReport\'s summary now reports nInputStores alongside nStores, so the panel (and its printable/PDF export) can tell "checked N stores, none over target" (genuine clean) from "0 of N stores returned data" (unresolved — now a yellow warning, never the green checkmark). A coordinated loc-padding migration across all ~30 qsr_fob consumers, plus a class-level guard test, is real follow-up work that belongs in its own issue.',
  ]},
  {version:'4.975', date:'2026-08-11', changes:[
    'Issue #176 closed — the last item of the #184 dispatch. `tJuneLaborPct` retired: it duplicated the approved `tCrewLabor` target (same organizational number, two workbooks) but outranked it in scheduling.js:445 and morning-brief.js:352\'s fallback chains, so Scheduling and Morning Brief graded against a stale June Projections upload whenever no fresher one had been uploaded that session. Both readers now call resolveLaborTarget() (#164\'s resolver) instead; applyProjectionsToTargets (parsers/index.js) no longer writes a labor % into the DEFAULT_TARGETS module constant (the raw parsed value stays available, unmutated, in ds.projRows — already existed, still unread — for a future ranked layer if ever wanted); the tJuneLaborPct key is gone from all 27 constants.js seed lines. Left alone per the issue\'s own "check siblings before touching" instruction: tJuneProj/tOperatorProj/tQSRSoftProj/tJuneTpph still ride the same mutation and the same seed block — three have live readers in morning-brief.js not audited this pass, one appears to have none but wasn\'t confirmed. DEFAULT_TARGETS is still mutated at runtime for those three.',
  ]},
  {version:'4.974', date:'2026-08-11', changes:[
    'Labor-basis migration COMPLETE — issue #164 closed, item 5 of the #184 dispatch. (Version jumps 970→974: this branch\'s own v4.964-969 sit alongside main\'s separate, already-informally-claimed v4.966-973 range from an unrelated PR title; 974 avoids the collision once this merges.) Final batch: labor-tools.js (24 reads across the district scoreboard, comparison tables, sparklines, findings, and CSV export), analytics.js (6, incl. the AI Pre-Forecast context object and the district text-brief export), at-a-glance.js (1, incl. a district-average labor target that itself averages the resolved per-store field rather than the legacy one), and smart-targets.js\'s officialVal (1 — its declared officialCol was already crew_labor_pct; the field it actually read was not, an inconsistency now fixed). All now route through resolveLaborTarget (tCrewLabor) instead of t.tLabor directly, joining the engine batch (v4.969) and the first view batch (v4.970, store-dash.js/store-analytics.js). Final repo-wide triage of the remaining reads: parsers/index.js\'s 2 real sites (:708, :790) are WRITES, not reads — they SET t.tLabor from an uploaded sheet and were never migration targets (triage category E); scheduling.js:445 (`t.tJuneLaborPct || t.tLabor || 0.22`) is issue #176 (item 6, retire tJuneLaborPct) and is deliberately left for that issue, not folded in here; smart-targets.js\'s officialVal still reads DEFAULT_TARGETS directly rather than the merged settings.targets chain — a separate sourcing bug (same shape as #153\'s defect) intentionally NOT fixed here per the original triage\'s instruction to keep it a separate commit. Zero remaining `.tLabor` grading/display reads outside of #176\'s scope, parser writes, and this file\'s own historical changelog text. New: memory/labor-park-oepe-score-attribution.md — a requested four-stage before/after (baseline → #183 OEPE fix → #181 park removal → #164 labor-basis fix) computed against Ponce de Leon\'s real targets and representative (not live) performance numbers, showing each fix\'s score contribution is separable: the OEPE fix moved this example +15.0pts (a correction — park time was being double-counted against flow), park removal moved it -0.9pts (structural — redistributing weight among the remaining components, not a judgment on the store), and the labor-basis fix moved it -5.9pts (a correction — the store was being credited against an unapproved, looser target). Cannot verify against live production Ops Scores — this sandbox has no authenticated Supabase session; the owner should re-run the real 27-store district through both the old and new code to see actual movement.',
  ]},
  {version:'4.971', date:'2026-08-11', changes:[
    'Reverted v4.966\'s monthly_targets.park_pct/oepe_target persistence (issue #174\'s priority pair) — owner confirmed the real monthly workbook has neither a Park% nor an OEPE column, so v4.966\'s parser detection was always going to find nothing (it said as much at the time: "UNVERIFIED against the real monthly workbook... silently finds nothing if the header text differs" — it does differ, permanently, because the columns are not there). Separately, #181 (v4.968) removed tPark from computeOpsScore entirely, so even if the columns existed, tPark would have no scoring consumer left to justify a cloud-persisted target for it. Reverted: parseMonthlyTargets\'s Park/OEPE column-detection (parsers/index.js), saveMonthlyTargets/loadMonthlyTargets/loadAllMonthlyTargets\'s park_pct/oepe_target read-write (src/lib/supabase.js), the monthly_targets schema columns (supabase/schema.sql) and the standalone migration file (supabase/schema-monthly-targets-park-oepe.sql — deleted; never run against the live project, so no data migration is needed), and the two test files that covered the reverted behavior. #174\'s OTHER finding — four of computeOpsScore\'s six target fields had no cloud path at all — is now moot for tPark (no longer scored) and remains true only for tKvst/tKvsu, which were never in scope for this pair. tOepe is unaffected: #183 already settled it stays UNCHANGED, and it was never part of this persistence work either way.',
  ]},
  {version:'4.970', date:'2026-08-11', changes:[
    'Backfill: labor-basis migration, view batch 1 (store-dash.js, store-analytics.js) — issue #164, item 5 of the #184 dispatch, continuing v4.969\'s engine batch. Shipped without its own changelog entry when it landed; recorded here so the version history matches what actually shipped. Migrated the remaining t.tLabor reads in these two files to resolveLaborTarget (tCrewLabor): store-dash.js\'s radar chart, laborCol band coloring, the 6-week comparison table, the coaching/action-plan builder (issue/target/gap/cost text), the store-card labor row, and OpsBarChart\'s labor bar; store-analytics.js\'s store-comparison table labor diff/ok flag, the AI Labor & Shift Analysis prompt\'s target text, the labor-overage $ opportunity calculation, the KPI card\'s labor color/target, the store-brief ok/warn/bad thresholds, and the AI Priority Actions prompt\'s issue text. Still not migrated at this point: labor-tools.js (~24 reads), analytics.js (~6), at-a-glance.js (1), and the remaining ~18 sites from the issue\'s own triage.',
  ]},
  {version:'4.969', date:'2026-08-11', changes:[
    'Labor-basis migration, engine batch — issue #164, item 5 of the #184 dispatch (first of several per-surface commits). computeOpsScore already graded labor against tCrewLabor (the owner-approved target, via #163\'s resolveLaborTarget on branch labor-basis-resolver-deferred), but every OTHER labor-target reader in the engine still read t.tLabor directly — a field the monthly-approval cycle never populates. That produced a live contradiction, reproduced on real fixture data (Ponce de Leon 43701: graded 26.00% legacy vs 24.00% approved): a store could score full labor credit from computeOpsScore while simultaneously emitting a WATCH-LABOR finding quoting the wrong, unapproved 26.00% target. Migrated to the named resolver (engine/labor-basis.js, resolved to tCrewLabor by default): pipeline.js\'s WATCH-LABOR finding (text + gate), TREND ALERT (gate + gap arithmetic + printed target), and the `concern` flag; finding-rules.js\'s labor dollar-impact calculation (the MONEY attached to a labor finding was being computed off a different target than the score that raised it); one-pager-data.js\'s two displayed labor-target fields. Also aligned two sites that had already independently reached the right answer by hand (insights.js, eom-supervisor.js) to route through the same named resolver instead of a duplicated inline `tCrewLabor || tLabor` fallback — kept the exact same fallback semantics, just named in one place now. Left alone on purpose, with a comment: pipeline.js\'s seeded demo-data block (triage category D — synthetic data, not a grading path). New invariant test (src/__tests__/labor-basis-invariant.test.js) using the Ponce de Leon fixture, proving the Ops Score, the WATCH-LABOR finding text, AND the finding\'s dollar figure all now cite the SAME number (tCrewLabor) — that agreement is the entire point of #164. Not yet migrated: the ~55 displayed-target reads across store-dash.js/store-analytics.js/labor-tools.js/analytics.js (per-surface commits to follow, per the issue\'s own plan) — so a live app right now would still show some panels quoting tLabor while the score and findings already cite tCrewLabor; this commit does not claim the contradiction is fully closed yet, only that the engine layer (the grading paths) is. Entry chunk 2750.10 KB → 2752.83 KB gzip 827.47 KB → 828.55 KB (+1.08 KB gzip, almost entirely this changelog text).',
  ]},
  {version:'4.968', date:'2026-08-11', changes:[
    'Park removed from Ops Score, replaced with a park% × OEPE quadrant diagnostic — issue #181, item 4 of the #184 dispatch. computeOpsScore\'s asymmetric park band (shipped in #180/#178 item 6, tuned toward a continuous taper in #181\'s earlier comments) is reverted entirely, not retuned. The owner\'s own explanation of what parking is FOR settled it: "keep the wheels moving... park cars with complex orders or other operational barriers." A measured 27-store park%-vs-OEPE-w/o-park quadrant (90d DAR × the #183-corrected OEPE) showed the district\'s heaviest parkers — Elgin 30.5%, Ponce de Leon 33.6% — also beat the median on flow, the tool working exactly as intended; several near-zero parkers sat at or below median flow too, refuting "under-parking always stalls the line" for them. A single-axis band scores both groups as failures; both are fine. New engine/park-oepe-quadrant.js (computeParkOepeQuadrants) classifies every store into 4 quadrants against DISTRICT MEDIANS RECOMPUTED FROM THE CURRENT DATA every call, never frozen constants — A/high-park-good-flow (tool working, look at production/KVS), B/high-park-bad-flow (parking AND still slow — highest priority), C/low-park-good-flow (doesn\'t need the tool), D/low-park-bad-flow (not using the tool, line stalling — the real under-parking case). Surfaced in a new Signals panel tab (🅿️ Park × OEPE) grouping all 27 stores into their quadrant with the district medians shown. parkMaxPts\'s 9 points are NOT silently dropped — computeOpsScore\'s score/max*100 formula self-normalizes, so removing a component from max automatically and proportionally increases the remaining 5 components\' weight; no separate redistribution code was needed, and this is called out explicitly rather than left implicit. The #150 mode:\'any\' zero-handling for park stays (metric-source.js) — a genuine 0% is still real data the diagnostic needs, not "missing." tPark stays parseable/persistable (#174) for the diagnostic\'s own use, just no longer a computeOpsScore input. Also corrected a same-day memory/project-backlog.md entry that had recorded an EARLIER decision in this exact conversation (ship a taper, defer the under-target side) as though it were final — it was superseded before the file was updated to say so; the entry now explains the timeline instead of just the latest state, and a standing rule was added: amend memory/ in the same turn a recorded decision changes. 5 new/rewritten tests: computeOpsScore output is now provably identical whether park data is present or absent (pipeline-park-kvsu-score.test.js), plus a fresh park-oepe-quadrant.test.js reproducing the issue\'s own Elgin/Ponce de Leon (quadrant A) and Cottondale/Lindsay (quadrant C) classifications from real district data. Cannot verify per-store Ops Score movement or live quadrant assignments against production — this sandbox has no authenticated Supabase session; the owner should review both after this ships. Entry chunk 2747.19 KB → 2750.10 KB gzip 826.23 KB → 827.47 KB (+1.24 KB gzip, almost entirely this changelog text — the new Signals tab itself is lazy-loaded).',
  ]},
  {version:'4.967', date:'2026-08-11', changes:[
    'OEPE switched app-wide to exclude parked/held time — issue #183, item 3 of the #184 dispatch. src/lib/supabase.js\'s loadQsrActSummary (the cloud-fresh DAR-derived OEPE, backing the One-Pager and the DAR fallback in metric-source.js) computed OEPE as (dt_untilserve − dt_untilstore) ÷ dt_trans_cnt — the WITH-parked-time variant. src/views/graded-visits.js\'s hourMetrics already had the correct w/o-park formula (subtracting dt_heldtime), so the same metric name meant two different numbers depending on which panel showed it. #185 (2026-08-11) already settled which formula is right: reconciled EXACTLY against a real QSRSoft Service report, 27 stores × 10 days, r(our gap, their gap) = 0.9958 — subtract dt_heldtime, keep every car in the denominator; the alternative (drop held cars from the denominator too) is ruled out. New shared src/utils/oepe.js (oepeSeconds/oepeWithParkSeconds) is now the ONE definition both callers use — graded-visits.js\'s hourMetrics refactored to call it (zero behavior change there, confirmed by its existing test suite passing unchanged), loadQsrActSummary\'s _finalizeQsrAct now calls it too and additionally exposes the old with-park value as a named diagnostic field (oepeWithPark) — never scored, never tOepe\'s basis, per the dispatch\'s explicit instruction. Required summing a field the rollup pipeline never carried: qsr_daily_activity_rollup gains a dt_heldtime column (supabase/schema-qsr-rollup-dt-heldtime.sql — owner needs to run this against the live project) and scripts/qsrsoft-dar-pull.mjs\'s refreshRollup() now sums it; historical rows outside the pull\'s normal recent-window re-cover need a one-time QSRSOFT_DAR_FORCE_FULL=1 run to backfill, or will simply age out of the 60-90 day table. tOepe (the TARGET) is deliberately left unchanged — the owner already measured the district-wide impact of switching bases and it moves exactly one store, not a district-wide fake win (see #185). Also corrected two now-stale doc comments describing the old with-park formula (engine/metric-provenance.js\'s Metric Lineage panel text, engine/metric-source.js). 6 new tests for the shared helper (src/__tests__/oepe-shared.test.js): heldtime subtraction, missing-heldtime treated as 0 not null-propagating, the with-park diagnostic staying >= the w/o-park value. Entry chunk 2744.15 KB → 2747.19 KB gzip 825.10 KB → 826.23 KB (+1.13 KB gzip, almost entirely this changelog text).',
  ]},
  {version:'4.966', date:'2026-08-11', changes:[
    'monthly_targets gets a cloud path for tPark and tOepe — the priority pair from issue #174 (item 2 of the #184 dispatch). Before this, four of computeOpsScore\'s six scored targets (tOepe, tKvst, tKvsu, tPark) had no cloud path at all, reaching the scorer only from constants.js\'s hardcoded DEFAULT_TARGETS — changeable only by editing source and deploying. tPark/tOepe go first because issue #181\'s deferred park-scoring re-baseline is explicitly gated on this landing (a spreadsheet upload once #174 lands, not a deploy). Corrects the parent issue\'s premise, found while implementing rather than assumed: its quoted code excerpt was parseYearlyTargets, not parseMonthlyTargets — parseMonthlyTargets had NO column-detection for Park/OEPE at all before this change (verified by reading its column map), so there was nothing for saveMonthlyTargets to have been dropping. The underlying structural gap the issue flagged (no cloud path for these targets) was real regardless. New: monthly_targets.park_pct/oepe_target columns (supabase/schema-monthly-targets-park-oepe.sql — owner needs to run this against the live project; supabase/schema.sql updated too), parseMonthlyTargets column-detection reusing the exact header strings already proven correct in parseYearlyTargets (\'Park %\', \'OEPE PACE\') — UNVERIFIED against the real monthly workbook from this sandbox, silently finds nothing if the header text differs, same as every other optional field in this parser — and saveMonthlyTargets/loadMonthlyTargets/loadAllMonthlyTargets round-tripping both fields. tLabor is parsed but deliberately NOT persisted, held until #164 (labor-basis rollout) lands so it doesn\'t become a seventh competing labor number. 8 new tests across 3 files: parser column-detection (present + absent-column no-op), and a real parse→save→load round-trip proving the exact gap the issue described is now closed (plus a regression test that tLabor specifically does not round-trip). Also regenerated the loader field map per the standing dev rule (node scripts/gen-loader-emits.mjs --write) after touching a loader — incidentally cleaned up unrelated stale drift in opsCashRows/opsLaborRows/opsServiceRows\' emit lists. Entry chunk 2741.50 KB → 2744.15 KB gzip 824.20 KB → 825.10 KB (+0.90 KB gzip, almost entirely this changelog text).',
  ]},
  {version:'4.965', date:'2026-08-11', changes:[
    'monthly_targets loaders no longer let a NULL column erase a good DEFAULT_TARGETS value on merge (issue #166, item 1 of the #184 dispatch). Both loadMonthlyTargets and loadAllMonthlyTargets (src/lib/supabase.js) mapped every column unconditionally, so a NULL column produced a PRESENT key holding null rather than an absent one — and App.js\'s mergedTargets spreads DEFAULT_TARGETS first, then the monthly-target row on top, so a present-but-null key wins the spread and erases the default (an absent key would have fallen through instead). Downstream, computeOpsScore gates each metric on `t.tX > 0`; a null target silently drops that component from the store\'s score with no error, no warning, and no comparability to peers graded on all six. This was inert before issue #163 (buildStore discarded the merged object entirely) and is not inert now. Fixed by stripping null/undefined keys from both loaders\' output before returning, via one shared helper — a missing column now falls through to DEFAULT_TARGETS exactly as the merge order already intends. Also removed the argless branch\'s `.order(year desc).order(month desc).limit(27)` — with no tiebreak on `loc`, 34 rows in the newest month returns an arbitrary 27 of them (the exact same shape as the loadQsrActSummary 1000-row-cap bug already fixed in this repo). All 3 real callers (analytics.js:7777/7900/8051) already pass an explicit year/month, so the branch was confirmed dead rather than assumed dead; loadMonthlyTargets now returns {} without one instead of silently keeping a landmine no caller was tripping today. 6 new tests (src/__tests__/monthly-targets-null-strip.test.js): a null crew_labor_pct leaves DEFAULT_TARGETS[loc].tCrewLabor intact after the real App.js merge order, a real value still wins normally, 27 well-formed stores resolve byte-identical targets (no-op for good data), and the dead branch is confirmed gone rather than silently kept. Entry chunk 2739.42 KB → 2741.50 KB gzip 823.38 KB → 824.20 KB (+0.82 KB gzip, almost entirely this changelog text).',
  ]},
  {version:'4.964', date:'2026-08-11', changes:[
    'Startup render storm, item 0 of the #184 dispatch — batched the tiered startup loader\'s 22 ds-touching stages (of 28 total) behind 3 explicit per-tier commits instead of 22 independent ones. The v4.846 tiering already made the 22 stages\' underlying Supabase fetches run concurrently; each stage still called setDs the moment it individually resolved, so React still committed and re-rendered once per stage — 22 of the roughly 42 renders behind the measured 47-render/167s startup render storm. Fixed with a local queueing stand-in that shadows setDs for the scope of the tiered loader only (JS closures resolve it lexically, so all 22 _stXxx functions — including the ones with prev-dependent merge logic — needed zero internal changes), flushed once after each of T1/T2/T3\'s Promise.all resolves. Every one of the 22 stages already used a functional setDs(prev=>...) updater, so reduce-chaining the queued updaters reproduces byte-identical final state to today\'s sequential commits — only the commit COUNT changes, from 22 to 3. This is a genuinely partial fix, said plainly: roughly 19 more renders come from OTHER startup effects (IDB restore, the loadLaborRows Supabase merge, six org_config syncs, email/PDF auto-ingest, cross-device sync) that were not touched in this pass — full list and rationale in memory/project-startup-render-storm.md. Also corrected a comment near rawStores (App.js ~line 2914) that had gone stale describing "a dozen-plus" commits from this specific effect. Cannot verify the actual render-count/wall-clock improvement live — this sandbox has no authenticated Supabase session, so the loader never runs; the owner should re-run ?clicktrace=1 after this ships. Verified: full test suite (1218/1218, unaffected — no React test harness exists in this repo) + clean build. Entry chunk 2737.29 KB → 2739.42 KB gzip 822.51 KB → 823.38 KB (+0.87 KB gzip, almost entirely this changelog text).',
  ]},
  {version:'4.963', date:'2026-08-10', changes:[
    'Insight Ledger step 0 — instrumentation only, no ledger table/writers/panel built (issue #143). The Ledger design (memory/project-insight-ledger.md) rests entirely on one unmeasured assumption: that a detector firing for the same store day after day collapses into ONE row with fire_count:N under its situation key, not N rows — 10 detectors × 27 stores × daily is a firehose otherwise. New engine/insight-ledger-measure.js observes buildAttentionFeed\'s per-detector output (the same shape the engine already builds, now exposed before the flatten instead of re-derived) and writes a day-bucketed snapshot to a throwaway Supabase user_settings blob — one bucket per calendar day, so opening the app 20 times today overwrites the same bucket 20 times, not 20 rows. Situation key = the item\'s own `.id`, the exact key analytics.js already acks attention items on — not a new scheme. Wired via an optional `onFireVolume` callback on buildAttentionFeed: every existing caller that omits it (there were none passing anything before this) gets a byte-identical return value, verified by a test that runs the same inputs with and without the callback and diffs the output. Read-side `summarizeFireVolume()` turns a week of buckets into the actual numbers asked for — collapse ratio (total fires ÷ distinct keys), per-detector loudness, first/last-seen per key — ready to run once real multi-device usage accumulates; not run yet, there\'s no data. Scaffolding, not a feature: this file + its one call site in attention-now.js\'s useAttentionFeed hook is the entire footprint, removable in one commit per the issue\'s own instruction. 16 new tests (byte-identical-output guard, snapshot/record/hydrate/summarize logic, the actual collapse-ratio math). Entry chunk 2728.15 KB → 2731.14 KB gzip 819.45 KB → 820.54 KB (+1.09 KB gzip — attention-feed.js and attention-now.js are both in the entry chunk via analytics.js\'s static import; this changelog text itself is part of that delta). Verified: full test suite + build pass, plus an unauthenticated boot-and-render pass — app boots clean, nav renders, zero pageerror.',
  ]},
  {version:'4.962', date:'2026-08-10', changes:[
    'Events & Tags: fixed the silent overwrite, and found + fixed a bigger silent-drop bug while measuring the duplicate-detection ask (issue #142). The reported complaint was "we need to find duplicates and remove them" — measured the live org_events table (2,651 rows) against the cloud\'s own definition of a duplicate (same loc+date+label, its unique constraint) and found ZERO: the suspected sync-gap in diffUserEventsForCloudSync has not actually produced any. So the duplicate-finder shipped small and cheap rather than growing merge machinery nothing needs today — a "⧉ Possible duplicates" filter now lives in the Events & Tags panel (Notes 62\'s existing Event Calendar), using that same cloud definition, ready to answer the question again as data changes. What the same measurement actually found, real and live: 261 (loc, date) pairs across all 27 stores — nearly always a school closure and a sports game sharing a day — where org_events correctly holds two rows, but orgEventsToDayMap\'s cloud-to-local downprojection was silently keeping only ONE, because mf_events is structurally one entry per (loc,date). That is real information loss on every login, not a cosmetic duplicate, and it is the same root defect the issue named ("a second write to an occupied slot silently overwrites") just at the automated hydration site instead of the interactive one. Fixed by combining same-day org events into one entry instead of dropping the loser — joined label/note/icon so nothing vanishes from what the owner sees, plus a `tags` array (reusing the exact shape calendar.js\'s own multi-type hand-tag already writes) so forecast.js\'s event-impact factor now averages BOTH events\' registry impact instead of only ever seeing one. The Events & Tags list expands combined days back into individually visible/searchable rows. Also shipped the originally-scoped, smaller fix: the panel\'s Add/Edit form now confirms before silently replacing an existing tag when you save into an already-occupied (loc,date) slot with a different event — editing that same slot in place still saves silently, since that\'s just editing. Verified: full test suite (5 new tests: 2 on the combine logic, matching the measured real cases) + build pass, plus an unauthenticated boot-and-render pass — app boots clean, nav renders, zero pageerror. Entry chunk 2728.15 KB → 2732.28 KB gzip 819.45 KB → 820.79 KB (+1.34 KB gzip — events-import.js and store-dash.js are both in the entry chunk, not lazy; this changelog text itself is part of that delta).',
  ]},
  {version:'4.961', date:'2026-08-10', changes:[
    'EOM Change Monitor now surfaces variance quantity, not just dollars (issue #141). The engine already computed everything asked for — this was pure UI surfacing, not new data-model work. Session, each Recount, and Final columns in the Baseline-diff table now show the raw unit variance under the dollar figure, with an explicit secondary "= X.XX case(s)" reference wherever the item\'s case-pack size is known (never replacing the raw qty; a missing or zero case size just shows the qty alone, same guard the three existing case-conversion call sites already use — nothing new invented). One real gap found while wiring this up: the live ledger path already attached qty variance to the session and final counts, but was computing it for each individual recount and then dropping it before returning — so "the variance at each submission," specifically, was unreachable even though the session/final numbers always worked. Fixed with one line plus a regression test. Entry chunk 2728.15 KB → 2729.23 KB gzip 819.45 KB → 819.86 KB (+0.41 KB gzip, from this changelog text itself — both touched engine/UI files are already lazy-loaded).',
  ]},
  {version:'4.960', date:'2026-08-10', changes:[
    'A persistent home for acknowledged items (issue #140). Acknowledging a swing alert or a Needs Attention finding used to be a one-way door — the item vanished with no record of what was cleared, by whom, or when. The Acknowledged section in Needs Attention (already near the top of the panel) now reads BOTH ack stores directly from Supabase — the swing alarm\'s swing_acks and the attention feed\'s own attention_acks, kept as two separate rows on purpose (sharing one blob would let one feed\'s aging silently drop the other\'s acks) — and merges them into one list: store, what was acknowledged, who, and when. If the underlying finding has since resolved and dropped off the live feed, the ack still shows (marked "resolved") until it ages out at 120 days, same as before — this doesn\'t fight that aging, it just makes it visible instead of a surprise. Nothing about how or when an ack is written changed: the situation key (store + week + severity for swings, the finding\'s own id for attention items) is untouched, so an escalating store still surfaces as new. Verified: full test suite (7 new tests on the merge logic) + build pass, plus an unauthenticated boot-and-render pass in a real Chromium — app boots clean, nav (including Needs Attention) renders, zero console errors; opening the panel itself needs the owner\'s own login (Supabase CORS blocks that from this sandbox). Entry chunk 2726.22 KB → 2728.15 KB gzip 818.79 KB → 819.47 KB (+0.68 KB gzip).',
  ]},
  {version:'4.959', date:'2026-08-10', changes:[
    'Spine 1, step 2 (issue #126) — the first change in this redesign that actually looks different on screen: Inventory Control (the app\'s most button-dense panel, ~3,300 lines) migrated onto the shared PanelChrome + ActionMenus built in step 1. The 16-button toolbar (grown from "14" since the issue was scoped) collapses into 4 grouped dropdowns — Reports / Scans / Monitor / Pulls — each still 2 clicks away, nothing hidden, tooltips preserved via a small new title-passthrough on ActionMenu. The mode toggle (Scoreboard / EOM Count / Count Cycle) moved into PanelChrome\'s dedicated view-tabs slot. Two things this panel deliberately does NOT adopt from the shared kit, and why: DateRangeControl doesn\'t fit a monthly-period select (kept the native picker); LocationSelector\'s patch tier reads a different, static supervisor-assignment source than this panel\'s own live one — confirmed these can diverge, couldn\'t confirm from this environment whether they currently have, so the patch/location picker stayed bespoke rather than risk silently mis-grouping a store on a financially-scoped filter on an unverified assumption. Full reasoning in memory/spine1-panel-controls-126.md. Entry chunk 2724.59 KB → 2726.22 KB gzip 818.16 KB → 818.80 KB (+0.64 KB gzip, almost entirely this changelog text — Inventory Control itself is lazy-loaded and the shared components already landed in step 1). Verified: full build + test suite pass and every prop/handler was checked by hand against the original markup — browser verification was attempted but blocked by a Chromium/proxy incompatibility specific to this sandbox, said plainly rather than glossed over.',
  ]},
  {version:'4.958', date:'2026-08-10', changes:[
    'Harvested and retired AI Insights Log (issue #128) — a panel with full render code and its own localStorage journal (mf_insights) that had no way to open it from anywhere in the app; confirmed via grep before touching anything that its toggle was only ever set to false. Checked with the owner whether that local journal held anything worth carrying forward before deleting it — it was empty. What was genuinely worth keeping from its design: a category taxonomy (Operations / Controls / Labor / Sales / Weather / Anomaly / Other) for grouping findings, which Task Queue didn\'t have any equivalent of. Task Queue tasks can now optionally carry a category (colored badge, picker on create and on an existing task, filter pills that only appear once something is actually categorized) and an optional store scope (tasks.category / tasks.loc, both now live in Supabase). AIInsightsLog, its localStorage read/write helpers, the dead lazy import, and the orphaned show/hide state are all removed. The AI Backtest Scanner does NOT auto-file findings as Task Queue entries — an earlier pass here built exactly that, on an earlier version of the same issue\'s ask, but the owner settled the actual design in issue #134 in the meantime: a standalone Insight Ledger panel with situation-key dedup behind a measure-first gate, not a title-deduped auto-filer bolted onto the scanner. That belongs to the Ledger\'s build, not this one, so it was cut back out rather than shipped as a second, competing implementation. Also fixed while addressing PM review, unrelated to the scope change: Task Queue\'s "Add Task" sheet used to silently close and discard your entry if the Supabase save failed — it now stays open and tells you, instead of losing the task with no indication anything went wrong.',
  ]},
  {version:'4.957', date:'2026-08-10', changes:[
    'Harvested the orphaned Anomaly Detection panel\'s one real capability — excluding weather-tagged days from its day-by-day sales baseline, so a multi-week disruption can\'t quietly drag down what counts as "normal" for a store — into the live AI Backtest Scanner (issue #127). The scanner already trims outliers, but trimming only catches a disruption if it happens to be extreme enough to land in the trimmed 10%; a 2-week synthetic test case confirms a real disruption can still slip through trimming alone and moves the baseline ~6% once it\'s excluded outright instead. This is a deliberate, bounded exception to the "measured, not tagged" rule adopted district-wide in v4.924 after tag-driven exclusion silently zeroed calibration for a heavily-tagged store — it\'s safe here specifically because an emptied day-of-week bucket produces no baseline rather than a wrong one, and now warns by name (store + day) if event tags ever empty one, instead of quietly going blind on that day the way the original orphan would have. The ported check also referenced two event-type strings, "closure" and "remodel", that don\'t actually exist anywhere in this app\'s real event-type list — only "weather" does — so they were removed rather than left in as dead code pretending to check for cases that can\'t occur. Production currently has zero events tagged with the qualifying type, so this doesn\'t change anything visible today — it\'s ready for the next time one gets tagged. The orphaned panel itself — whose render had drifted so far from its own engine\'s field names that every row would have shown blank anyway — is deleted outright, along with its dead state and its ORPHANS registry entry.',
  ]},
  {version:'4.956', date:'2026-08-10', changes:[
    'Harvested the orphaned Developer Dashboard\'s Data Audit tab (issue #123) — a per-store × per-source coverage grid (row counts, first/last date, coverage %, Full/Partial pill) that nothing live had an equivalent for. This is exactly the diagnostic that would have caught the labor_rows staleness incident at a glance instead of two weeks late. Now lives in Data Manager as a new Coverage tab, same math, same 90/70 grading. The Settings Dump (a curated forecast-config readout) moved into the real, reachable Developer Dashboard in Settings; the Engine Trace tab was dropped outright — the standalone Forecast Audit panel (v4.947) already does that job better, including fixing a rendering bug the orphan\'s version had. The orphan itself is deleted, along with its dead import and unreachable state — there were two different components both named "DevDashboard" in this codebase; now there\'s one.',
  ]},
  {version:'4.955', date:'2026-08-10', changes:[
    'RLS table audit (issue #119): swept all 82 live Supabase tables comparing an authenticated-role read against a service-role read, the way a real reviewer would rather than guessing. The Food Cost panel bug this started from (period dropdown stuck at May) traced to qsr_fob supposedly returning nothing for a normal login — measured directly, qsr_fob is actually healthy right now (full data through today, correct tenant, unrestricted store list), so that specific symptom didn\'t reproduce and got no fix bolted onto it. The sweep did turn up two real, currently-live gaps: the tenants/tenant_stores tables had no RLS policy at all (same "empty for no visible reason" shape as the Food Cost symptom, just on tables nothing reads yet — confirmed dead code, so nothing user-facing was ever affected), and the qsr_daily_activity_daily rollup view was missing its permission grant. That second one caught something more serious on review: the obvious fix (just re-apply the grant) would have been a real security bug — a Postgres view runs with its OWNER\'s row-level security by default, not the querying user\'s, so granting it broadly would have handed every authenticated user every store\'s hourly data regardless of their own access restrictions. Fixed properly with security_invoker on the view instead, which makes it respect the same per-caller scoping the underlying table already enforces. Both fixed forward with new SQL files for the owner to run; nothing here changes app behavior on its own. The comparison script (scripts/rls-table-audit.mjs) is meant to be re-run any time a panel looks silently empty, not a one-off.',
  ]},
  {version:'4.952', date:'2026-08-10', changes:[
    'Spine 1, step 1 (issue #126) — the redesign\'s foundation, and nothing renders differently yet. Today there are 7 hand-rolled date-range controls in 3 different visual styles, and location/store selectors implemented separately in a dozen-plus panels. Built the shared replacements — DateRangeControl (preset pills + real custom-range picking), LocationSelector (the existing All → State → Patch → Store standard, extended, plus a single-store mode for panels that only ever show one store), ActionMenus (grouped dropdowns — the fix for Inventory Control\'s 14-button wall going to 3 menus), and PanelChrome (lays the shared bands out in a fixed order: location · date · export, then actions · view tabs) — plus two small opt-in ModalShell additions (a page-scrolling layout variant, a tinted header option) needed to host them. This PR only adds the components; no existing panel calls any of them yet, so nothing changes on screen. Migrating a real panel to it (Inventory Control is the pilot) is separate follow-up work. Measured: entry chunk 844.26 KB → 844.30 KB gzip (+0.04 KB, effectively nothing) — the new files aren\'t imported anywhere yet, so only the two tiny ModalShell option flags landed in the bundle at all.',
  ]},
  {version:'4.950', date:'2026-08-10', changes:[
    'Entry-chunk budget relief (issue #124): analytics.js was 692 KB and stayed in the entry bundle because it held AtAGlance — the home screen and the single biggest thing in that file. Five consecutive PRs had eaten the 850 KB gzip ceiling down to ~5.9 KB of headroom without any one of them being a bad call; the next feature-sized change was going to cross it. AtAGlance (plus its 3 exclusive tiles: SAGE Scheduled Runs, EOM Scoreboard, Items Recounted) now lives in its own module, src/views/at-a-glance.js, loaded the same on-demand way the other secondary panels already are — but with its own fallback (renders nothing, no dark overlay) since this is the always-visible landing view, not a modal, plus an eager prefetch of the chunk at app-mount so it is normally already in flight well before the several-second startup data load finishes and this component gets its first real paint. Along the way, extracting AtAGlance exposed and fixed a separate, unrelated regression: Rollup\'s automatic chunking had been silently relying on the exact set of existing lazy-panel boundaries to keep the 142 KB-gzip xlsx (SheetJS) library split out of the entry chunk — adding one more lazy boundary flipped that heuristic and would have inlined all of xlsx into the entry, undoing the whole point of this change. Pinned xlsx to its own chunk explicitly in vite.config.ts so the budget no longer depends on Rollup guessing right. Net: entry chunk 844.26 KB → 815.32 KB gzip (2822.70 KB → 2715.47 KB raw), ~29 KB of headroom recovered.',
  ]},
  {version:'4.949', date:'2026-08-10', changes:[
    'Small cleanup, no behavior change: 3 more places that hand-built "the last full business day" from scratch now share the one helper that already existed for it — the Above-Store One-Pager\'s month-to-date range and two of Labor Tools\' trailing-window pickers. This is the exact kind of copy-pasted date math that has caused a real bug five separate times when someone accidentally left off the "exclude today" part; fewer places holding a hand-copy means fewer places that can quietly drift.',
  ]},
  {version:'4.948', date:'2026-08-10', changes:[
    'Dialed-In calibration now follows you across devices. It used to live only in this browser\'s local storage — calibrating on the desktop did nothing for the phone or iPad, and calibrating on a preview link before it went live never reached production at all, silently. It now syncs the same way Model Assignments and Smart Targets already do: instant on this device, cloud copy follows within moments, newer always wins if you calibrate on two devices close together.',
    'Resetting calibration now clears the cloud copy too, not just this device\'s. Without that, a reset would have appeared to work and then had the old numbers quietly reappear the next time the app loaded — the specific failure mode this change was built to prevent, not one that shipped.',
  ]},
  {version:'4.947', date:'2026-08-10', changes:[
    'Forecast Audit — the panel that shows every input behind a single day\'s forecast, one step at a time — is now reachable directly from the nav (Forecasting section), not just from inside Monthly Projections. It was already fully built and live there, just missing a front door; the registry had it mislabeled as dead code, which is now corrected. Opening it before selecting a store now dims the nav item instead of doing nothing when clicked.',
    'Fixed a bug in that same panel where the detail view could silently disagree with the date list next to it — the detail view was reading a tagged event\'s impact from the wrong place, so it usually saw none at all even when the sidebar\'s preview for the exact same day did. A test now pins the two views to always agree.',
    'A second bug in the same panel: for a store/day using the district\'s standard forecast model, the Trend Signal step could display a wildly wrong percentage — a $12,846 forecast reading as "+1,284,600.00%" — instead of a real trend like "up 5%." That model doesn\'t compute a trend percentage at all, it computes the forecast dollar figure directly, so the panel was showing the wrong number in the wrong place. It now shows "n/a — this model doesn\'t compute a trend" instead. Deciding whether that model SHOULD compute a display trend is a separate, larger question, deliberately left open.',
  ]},
  {version:'4.946', date:'2026-08-10', changes:[
    'Needs Attention — Part 2 of the Attention Now merge: new layout. Instead of a list you click through one store at a time, it\'s now a single ranked list — worst store first, each one expands in place to show every finding. Clickable Critical / Watch / Acknowledged counts at the top act as filters. A clean, non-urgent store now shows up in a collapsed "Running Well" section instead of just not being there, and a district-wide alert that isn\'t tied to any one store (like "sales data is 9 days old, auto-sync may be down") now gets its own pinned strip at the top instead of having nowhere to go.',
    'New: acknowledge any finding so it moves out of the active list until the situation actually changes — same idea as the sales-swing alarm\'s acknowledgement, extended here. Acknowledging one issue at a store never silently acknowledges a different issue at the same store, and acknowledgements sync across devices the same way the swing alarm\'s do.',
    '"Attention Now" as a separate menu item is retired — everything it showed (including the sync-health alert) is now inside Needs Attention. Checked carefully that nothing it used to show got lost in the move, since that alert has caught two real multi-day data outages in the past.',
  ]},
  {version:'4.944', date:'2026-08-10', changes:[
    'Data-integrity sweep, signature #4 — closed out for good: 9 more trailing-window comparisons found to still be counting today\'s still-filling business day as if it were a full day (the same bug behind 4.917/4.924, recurring because each fix had been done inline instead of from one shared rule). Fixed: the Anomaly Detection panel, the AI Backtest Scanner, Avg Check Momentum, the forecast engine\'s 2-week trend (which had no cutoff at all), the district-wide sales-vs-last-year figure used on 5 different screens, and 4 lower-priority spots (Morning Brief\'s store norms, the 90-day metric averages, the AI Pre-Forecast context, and the Performance Calculator baseline). All 9 now share one function instead of each re-deriving the cutoff date by hand. Checked against real store data: the forecast engine\'s underlying 6-week averages moved the most — one adjustment factor shifted by up to 10% for 2 stores — but a full before/after backtest came back byte-identical on accuracy (the backtest\'s own calibration re-fits around whatever baseline it\'s given, so historical grading doesn\'t change even though a live forward forecast now starts from a cleaner number). The 90-day metric averages moved more than first estimated when this was triaged as lower-risk — one store\'s T-Red-A rate shifted 21.6% — corrected here rather than left mis-labeled. Two of the nine sites (the sales-vs-LY figure and the 2-week trend) showed no measured change today only because the manually-uploaded Labor Report hasn\'t received a new row since 2026-07-23 — both are still fixed for when that resumes.',
  ]},
  {version:'4.943', date:'2026-08-10', changes:[
    'Needs Attention now runs on the same engine as Attention Now (Part 1 of merging the two into one panel — same look, same tabs, nothing removed yet). It used to only check cash/labor/speed/scheduling; it now also picks up food-cost outliers, drive-thru speed, visit-readiness risk, sync health, and fading saved signals — the same things Attention Now already caught. Checked against real store data before shipping: nothing that used to show up in either panel dropped out of this one.',
  ]},
  {version:'4.942', date:'2026-08-10', changes:[
    'HOTFIX — the sales-decline check added yesterday (Needs Attention + Attention Now) compared a still-in-progress today against a full day from last year, which could make an ordinary day look like a decline and a real decline look worse than it was, depending purely on what time of day someone happened to look. Both now stop counting today until it’s actually closed out, the same way the rest of the app already handles trailing-week comparisons. Re-checked the real store numbers with the fix in place — same store still stands out as the one with a real problem, nothing else changes.',
  ]},
  {version:'4.941', date:'2026-08-10', changes:[
    'FIXED — "Attention Now"\'s sales-behind-LY check only looked at whichever single week you had selected, so a real multi-week decline could slip through if that one week happened to look okay (and vice versa, a genuinely bad single week could get buried under a longer-term average). It now also checks the trailing 4 weeks and surfaces whichever view — this week or the last month — shows the worse decline, so a real trend can\'t hide behind a decent week.',
  ]},
  {version:'4.940', date:'2026-08-10', changes:[
    'FIXED — Needs Attention could never catch a pure sales decline, no matter how severe. It only checked cash/controls/labor/speed signals, so a store on a real, sustained sales slide (found: Atoka, down 15.4% vs last year over the trailing 4 weeks, worsening every week) never showed up there even though other panels track sales separately. Added a dedicated sales-decline check using the same trailing-month comparison the rest of the app already uses, tuned against the real district numbers so it flags a genuine outlier without flooding the list on an ordinary soft week.',
  ]},
  {version:'4.939', date:'2026-08-09', changes:[
    'SAGE moved from a full-screen overlay to a drawer that opens from any screen without losing your place — a persistent button lives in the top bar now instead of being buried in the nav list. The minimize-to-a-pill option is still there for anyone who wants it fully out of the way, and reopening a minimized session now actually reopens it (it could get stuck invisible before).',
    'Signals is no longer forced full-screen either — it opens as the same centered panel as everything else, just wider to give the Scanner and Live Ops tables room.',
  ]},
  {version:'4.938', date:'2026-08-09', changes:[
    'Every panel in the app now closes the same way. Close buttons had drifted into five different shapes across the app — a wrong "×" character, the correct "✕", the word "Close", "✕ Close", and "Cancel" — sometimes two of them on the same panel depending on whether it had data yet. All 31 panels that open as an overlay now share one modal component with one header and one close button, so the fix cannot drift back panel by panel the way it did before.',
  ]},
  {version:'4.937', date:'2026-08-09', changes:[
    'Data-integrity sweep, signature #2 continued: 4 more reports fixed the same way, one of them a real live bug — the Anomaly Detection panel and the AI Backtest Scanner build their day-by-day baseline from the manually-uploaded Labor Report, which had quietly stopped receiving new rows about two weeks earlier while every other data source kept updating. Both had been silently blind to two weeks of real sales anomalies. Also fixed: a stale slider baseline in the Performance Calculator, the district 6-week sales trend, and FOB% in the Operator Summary panel, which now falls back to the automatic FOB pull when the monthly report hasn’t been uploaded yet.',
  ]},
  {version:'4.936', date:'2026-08-09', changes:[
    'Data-integrity sweep, signature #2: 12 more reports, panels, and one AI prompt were reading only from manually-uploaded files, so a store covered solely by an automatic or emailed data feed dropped out of them entirely. Fixed across the Date-Range Report, the printed Store One-Pager, the Metric Correlation Explorer, the Ops Anomaly Cross-Check, the district AI brief, four pieces of the Revenue Opportunity panel, the Model Comparison chart, the Scheduling Opportunity Report, and Morning Brief. The sharpest case was the Records feature: it only checked service-speed data on a day the Labor Report already covered, so a record set on an automatic-only day could never be recognized at all.',
  ]},
  {version:'4.935', date:'2026-08-09', changes:[
    'New: Projection Accuracy report (Signals → 📐 Projection Accuracy). Tracks whether QSRSoft’s hourly sales and guest-count projections run consistently high or low by time of day, accumulating daily so the pattern gets more reliable the longer it runs — a first look already suggested projections run under in the afternoon/evening and over late at night, though it will take a few weeks of data to confirm.',
  ]},
  {version:'4.934', date:'2026-08-09', changes:[
    'RankingView’s grouped guest-count vs-last-year figure now pulls from the same automatic-first data source as every other column in that table — it had been the one column still reading only the manually-uploaded file.',
  ]},
  {version:'4.933', date:'2026-08-09', changes:[
    'Data-integrity sweep, signature #5: 5 more places where a missing reading was silently graded as if it were a real zero. A store with no Controls upload at all was scoring a perfect Controls Elite rating; a store missing labor data ranked #1 (best) in the district instead of last; a store with no guest comments showed the worst possible color and star rating instead of "—"; and two rolling averages let a missing reading drag the average toward zero instead of being left out.',
  ]},
  {version:'4.932', date:'2026-08-09', changes:[
    'Graded Visits: the hourly last-year comparison now requires a minimum sample size before showing a percentage. Measured against real data first — a last-year guest count of 1 produced comparisons like +2200%, and turned out to be rare (0.3% of hours), not the normal case it looked like.',
  ]},
  {version:'4.931', date:'2026-08-09', changes:[
    'Fixed a destructive bug in the mistagged-event cleanup tool shipped one version earlier, caught in review before it reached anyone: for long date ranges (like a full back-to-school month) the one-click "Remove all mistagged" button could delete a correctly-tagged real event, not just the mistagged ones.',
  ]},
  {version:'4.930', date:'2026-08-09', changes:[
    'Calendar Manager can now detect and clean up mistagged recurring-event dates in one click — the same class of bug as 5 Black Friday tags landing on the wrong day, generalized to Small Business Saturday, Cyber Monday, and tax-free weekends.',
  ]},
  {version:'4.929', date:'2026-08-09', changes:[
    'Fixed a bug in the cloud sync for hand-tagged calendar events: editing or deleting the one event visible on a day with multiple tagged events (a sports game AND a school closure on the same date, for example) could silently delete the OTHER event sharing that date too, with no error and no local sign anything was wrong.',
  ]},
  {version:'4.928', date:'2026-08-09', changes:[
    'Data-integrity sweep, signature #1: fixed 3 more places a near-zero denominator could produce an impossible percentage, matching the 1,200,000%-chart-axis bug from 4.912 — a cloud-sourced year-over-year figure that fed sort order, color coding, and a printed report; a per-day forecast-error percentage that could read "-111,000%" in the Forecast Table; and a dead calculation that was computed but never actually used, which was deleted rather than fixed.',
  ]},
  {version:'4.927', date:'2026-08-09', changes:[
    'Recurring calendar rules get a safer default duration and a guard against two rules landing on the same floating date.',
  ]},
  {version:'4.926', date:'2026-08-09', changes:[
    'Forecasting: a last-year comparison is no longer skipped just because that day carries a tag. It’s now skipped only when the day is a measured statistical outlier against its own peer days — tagging a day must never by itself remove it from the comparison.',
  ]},
  {version:'4.925', date:'2026-08-09', changes:[
    'Events & Tags: hand-entered tags now sync to the cloud, not just this device’s local storage.',
  ]},
  {version:'4.924', date:'2026-08-09', changes:[
    'Data-integrity sweep, signature #4: the still-open, still-filling business day is now excluded everywhere it could taint a grade or a trend — 7 more places found beyond the Biggest Miss fix in 4.917, including Dialed-In’s MAPE columns, every period preset in Labor Tools, and the live Smart Targets calculator.',
  ]},
  {version:'4.923', date:'2026-08-09', changes:[
    'Events & Tags: restored search, filter, and sort, which had landed in the wrong modal.',
  ]},
  {version:'4.922', date:'2026-08-09', changes:[
    'Interaction tracer (?clicktrace=1) improvements: an on-screen report with a copy button for testing on a phone with no attached debugger, each React render now attributed to the specific click that caused it, click-triggered renders separated from unrelated startup/background work, and AppSidebar’s own render time instrumented directly.',
  ]},
  {version:'4.921', date:'2026-08-09', changes:[
    'Fixed the mobile app feeling slow to open menus and panels — a modal-tracking gate had regressed since 4.212, so opening the hamburger menu (or several other panels) forced the whole app to re-render instead of just the panel that opened. Found with a real capture from the interaction tracer, not guessed. Also debounced the startup data loaders so their 32-loader burst no longer coalesces into a visible stall.',
  ]},
  {version:'4.920', date:'2026-08-08', changes:[
    'FIXED — washed-out text across the whole app. The muted colour used for almost every label and caption failed readability standards in six of the eight theme-and-mode combinations, worst at roughly half the required contrast. The gold accent failed in three light themes. All eight now meet the accessibility standard, and a test blocks any future change that drops below it.',
  ]},
  {version:'4.919', date:'2026-08-08', changes:[
    'Events & Tags gains a location filter, with each store showing how many days it has tagged and the heaviest listed first. Any store tagged on more than a year of days now carries a warning, because tagged days are skipped when the forecast looks for the same day last year — one store had 450 tagged days and nothing in the app said so.',
    'Local News now states when it updates in the footer, not only when there is nothing to show.',
  ]},
  {version:'4.918', date:'2026-08-08', changes:[
    'Render timing in the tracer now actually works in the live app. The previous attempt used a React feature that is removed from production builds, so it silently recorded nothing.',
  ]},
  {version:'4.917', date:'2026-08-08', changes:[
    'Forecast Table and Backtest Accuracy no longer grade the current day. A day still filling has only its sales so far, and comparing that against a whole-day forecast made it the Biggest Miss almost every time while dragging the accuracy, bias and pass-rate figures with it. Today is now shown separately as in-progress rather than scored.',
    'The tracer now also records React render time, which is the one place the previous instrumentation could not see.',
  ]},
  {version:'4.916', date:'2026-08-08', changes:[
    'The interaction tracer now names the heavy startup work — index building, the register-audit analysis and the insights pass — so a freeze on load can be attributed to something specific instead of appearing as anonymous script.',
  ]},
  {version:'4.915', date:'2026-08-08', changes:[
    'Clicks no longer wait for the store rebuild. Every time a data loader finished, the app rebuilt all 27 stores immediately and the screen froze until it was done — measured at up to half a second each, and it ran sixteen times in one session. That work now happens at low priority, so a click, a menu or closing a panel goes through first. The numbers are unchanged; they just no longer block what you are doing.',
  ]},
  {version:'4.914', date:'2026-08-08', changes:[
    'Added an interaction tracer for the sluggish-click report. Load the app with ?clicktrace=1, click around, then run mfClickTrace() in the console — it lists which clicks blocked the screen and for how long, and names the work that ran. Off unless switched on.',
  ]},
  {version:'4.912', date:'2026-08-08', changes:[
    'FIXED — the 6-Week Performance chart could plot impossible numbers (a y-axis reaching 1,200,000%). Year-over-year growth was measured against last year\'s same weekday even when that day was a closure with almost no sales — Christmas, Thanksgiving, the January ice storm. Dividing by an $8.98 day turns a normal day into a six-figure percentage. Those days are now left out of the trend instead of being treated as a real comparison.',
    'The cutoff is drawn from the data, not picked: of 40,000 store-days, 98.4% fall at or above 70% of their own store\'s typical day and only 76 fall below 25%. Genuine strong years and real declines are still reported in full.',
  ]},
  {version:'4.911', date:'2026-08-08', changes:[
    'Local News now shows a chip for every location that has a story. It was capped at eight, so the header could say 17 locations while only 8 were selectable. The header now reads how many of the 27 stores have any coverage at all.',
  ]},
  {version:'4.910', date:'2026-08-08', changes:[
    'FIXED — Dialed-In calibration and the 6W/4W/2W/1W trend columns. A last-year comparison was skipped whenever the day it pointed at carried an event tag, and one store had 450 tagged days, so every candidate was rejected and the comparison came back empty for 146 of 146 days. Real sales existed on every one of those days. Last-year lookups now fall back to using a tagged day rather than giving up; genuine closures are still excluded.',
    'The version shown in the footer is now taken from the top of this changelog instead of being typed separately. The two had drifted 20 versions apart, so a hard refresh looked like it had failed.',
  ]},
  {version:'4.909', date:'2026-08-08', changes:[
    'Dialed-In diagnostics now report WHY a last-year lookup was rejected, not just whether the day existed. The previous version only proved the index key was present — which it was — while the value still came back empty, so it answered the wrong question.',
  ]},
  {version:'4.908', date:'2026-08-08', changes:[
    'Added instrumentation to the Dialed-In calibration log instead of shipping a fourth guess at why Tishomingo fails and the 6W/4W/2W/1W trend columns render blank.',
  ]},
  {version:'4.907', date:'2026-08-08', changes:[
    'Fixed a date bug that shifted any text-form date back one day (a date string is read as UTC midnight but displayed in local time). It builds the index key for every store-and-day lookup in the app, so a cached row could sit one day off from where everything else looked for it.',
    'New stores are now reported honestly. Ponce de Leon showed a window starting in April 2027, which read like corruption but was arithmetic — it opened in March 2026 and Dialed-In needs a full year of history. It now says so plainly and no longer counts as a failure.',
  ]},
  {version:'4.906', date:'2026-08-08', changes:[
    'Fixed a regression that broke Dialed-In calibration for all 27 stores. The auto-first sourcing added in 4.904 was applied to a row set shared by the grid search, the clean-data detector and the last-year lookup; it is now scoped to the trend columns alone, which are the only part that needs recent days.',
  ]},
  {version:'4.905', date:'2026-08-08', changes:[
    'Corrected the metric resolver: 30 of 35 source chains were consulting manual uploads BEFORE the automatic feeds, so a stale manual value could override cloud-fresh data. Being stored in the cloud does not make a stream automatic — what matters is what feeds it. Now enforced by a test, so it cannot drift back.',
  ]},
  {version:'4.904', date:'2026-08-08', changes:[
    'Dialed-In trend columns re-sourced to auto-first data. Superseded by 4.906 — see above.',
  ]},
  {version:'4.903', date:'2026-08-08', changes:[
    'Register Audit: refund COUNT had refund DOLLARS added into it. The visible symptom was stray cents in the total; the real damage was the amber warning thresholds firing on dollar amounts, so anyone with cashless refund activity looked like an outlier. Split into a count and a dollar total.',
    'The afternoon daypart is now labelled Snack rather than PM.',
  ]},
  {version:'4.902', date:'2026-08-08', changes:[
    'Two standing rules recorded: a measured performance budget for every change, and a commitment that manually-sourced metrics are always temporary and get automated.',
  ]},
  {version:'4.901', date:'2026-08-08', changes:[
    'Mobile performance: 795 KB removed from the initial download (3518 KB to 2722 KB). Slow first load, an unresponsive menu and the long blank screen when returning from another app were all the same cause — too much code parsed before the app could respond.',
    'Opening a panel that loads on demand no longer blanks the entire app while it downloads.',
  ]},
  {version:'4.900', date:'2026-08-08', changes:[
    'The loader field map is fully generated — no hand-maintained supplement remains.',
  ]},
  {version:'4.899', date:'2026-08-08', changes:[
    'The loader field map is now generated from the code rather than typed by hand, after going stale four times in one day. Employee and manager meal figures seeded from the real file.',
  ]},
  {version:'4.898', date:'2026-08-08', changes:[
    'Meal counts wired alongside the amounts, with header names verified against a real Daily Glimpse file.',
  ]},
  {version:'4.897', date:'2026-08-08', changes:[
    'Fixed the meals data path, which was broken one link from the end.',
  ]},
  {version:'4.896', date:'2026-08-08', changes:[
    'All 28 six-week comparison fields now resolve from data, and the manual-only metric list is empty.',
  ]},
  {version:'4.895', date:'2026-08-08', changes:[
    'Opportunity cost is now derived rather than uploaded, and a new Actual-vs-Scheduled opportunity metric was added.',
  ]},
  {version:'4.894', date:'2026-08-08', changes:[
    'LifeLenz now resolves four more metrics. Floor hours turned out never to have been saved at all.',
  ]},
  {version:'4.893', date:'2026-08-08', changes:[
    'The six-week comparison is routed through the metric resolver — the payoff for the sourcing work.',
  ]},
  {version:'4.892', date:'2026-08-08', changes:[
    'Derivation became a gap-filling fallback rather than a replacement, so a directly-reported value still wins when one exists.',
  ]},
  {version:'4.891', date:'2026-08-08', changes:[
    'Metric coverage now means “can we compute it”, not “does a feed carry it”.',
  ]},
  {version:'4.890', date:'2026-08-08', changes:[
    'Actual-vs-needed hours are auto-available after all — an incorrect rejection caught by the owner.',
  ]},
  {version:'4.881', date:'2026-08-07', changes:[
    'NEW — Local News. Stories from nine local outlets covering the towns our restaurants are in, attributed to the right store and filtered to what could plausibly affect a restaurant: road closures, crime, weather, business openings. Prep sports and obituaries are filtered out. A story in a two-store town (Ardmore, DeFuniak Springs) is shown as belonging to EITHER store rather than being guessed at. Also searches YouTube nightly, though these towns carry almost no video coverage.',
  ]},
  {version:'4.880', date:'2026-08-07', changes:[
    'The swing alarm now shows what was happening locally around a store\'s decline. Crucially it looks BACK before the decline started, because a cause comes before its effect — for the Atoka alert it surfaces storm-damaged roads and restoration work from weeks earlier. Headed "worth checking", never presented as the explanation.',
  ]},
  {version:'4.875', date:'2026-08-07', changes:[
    'Inventory variance trace now loops back to the last ACTUAL PHYSICAL COUNT instead of the calendar month, so the chart brackets the window a variance actually occurred in. The header names the window ("since the last count (Aug 5)"). It deliberately walks back past a count taken in the last few days — anchoring on yesterday would collapse the chart to two points. Note the trace still cannot cross a month boundary: qsr_fob snapshots are month-to-date cumulative and reset at month start.',
  ]},
  {version:'4.874', date:'2026-08-07', changes:[
    'A manual report that is too large to sync (one 12.37 MB file) was being re-fetched and failing on EVERY load. It now stops retrying after two attempts and names the file so a report that never synced is visible instead of silently absent.',
  ]},
  {version:'4.871', date:'2026-08-07', changes:[
    'Startup: removed ten full-table row-count scans that ran on every single login purely to write numbers to the browser console. Two of them exceeded the database statement timeout and failed outright, and all ten competed with the real data loads during startup. Time to a usable app dropped by about 7 seconds. The diagnostic is still available at ?tablecounts=1.',
  ]},
  {version:'4.870', date:'2026-08-07', changes:[
    'Failed data loads are no longer silent. A read that errored was indistinguishable from a table that genuinely had no data — the failure marker was being destroyed before any panel could see it, across 37 loaders. That is how six tables returning server errors went unnoticed, each one rendering as a normal "no data yet" empty state. Failures now raise the DATA INCOMPLETE banner and name the source.',
  ]},
  {version:'4.869', date:'2026-08-07', changes:[
    'Swing alarm now measures only CLOSED business days. It was counting today as a full day, so a store part-way through trading looked worse than it was and the headline number drifted through the day. Honours the 4am business-day cutover.',
  ]},
  {version:'4.868', date:'2026-08-07', changes:[
    'NEW — Count Cycle panel (Operations → Count Cycle). Enforces the count rules per store: every weekly count needs a full Food AND Condiment count, and Paper is mandatory on the mid-month count. The mid-month count floats with each store\'s own count day, so it is identified by Paper being counted outside the close window rather than by a fixed date. Opens on the stores that need chasing, with the rest collapsed. Reads the full item universe (qsr_onhand) rather than the top-variance table, which carries no Condiment items at all.',
  ]},
  {version:'4.867', date:'2026-08-07', changes:[
    'NEW — Swing alarm. A store whose sales or guest counts fall sharply for two weeks running now opens a blocking alert that cannot be dismissed by clicking away; acknowledging is the only exit, and it records who acknowledged and when. Thresholds were calibrated against 676 store-weeks of real data rather than picked: two consecutive weeks at or below -10% vs LY isolates exactly the genuinely struggling store. Acknowledging one week does NOT silence the next, and does not silence an escalation — so a store that keeps getting worse keeps surfacing.',
  ]},
  {version:'4.866', date:'2026-08-07', changes:[
    'Count history is now recorded permanently. On-hand data only ever kept each item\'s LAST count date, so a recount erased the previous one — meaning "was the last weekly complete?" was answerable but "were all four weekly counts complete last month?" never could be. A daily snapshot now preserves each count session as it happens. History builds forward from today; counts already overwritten cannot be recovered.',
  ]},
  {version:'4.864', date:'2026-08-07', changes:[
    'The Items Recounted tile was reporting "No ledger detail" for a period that held 695 rows across all 27 stores (54 items recounted, $3,680 net recovered). The read was timing out on a 16 MB request, not coming back empty. Fixed the request size, and the tile now distinguishes a failed load from a genuinely empty one, with a Retry.',
  ]},
  {version:'4.859', date:'2026-08-07', changes:[
    'Drive-thru speed now contributes to the attention feed. The detector had been written and tested but was never given data, so it silently contributed nothing.',
  ]},
  {version:'4.858', date:'2026-08-07', changes:[
    'Watch counts were always zero. The "Watch Flags" tile, the Watch filter on Store Dashboard, and the Watch tab in Needs Attention all filtered for a severity tag the system has never emitted, so all three had silently read zero since they were built. About 12 categories of watch findings were being detected and then dropped. Expect these counts to jump off zero.',
  ]},
  {version:'4.856', date:'2026-08-07', changes:[
    'Panel plumbing rebuilt (no visible change) and 31 gaps closed as a result: 15 panels left the dashboard recomputing behind them, and 16 could not be closed with the Escape key. Adding a panel used to mean updating six separate lists by hand; the build now fails if any of them disagree.',
  ]},
  {version:'4.779', date:'2026-08-03', changes:[
    'At-A-Glance polish: the Sales & Guest Counts, Labor, and Service tiles now show "Loading…" instead of "No … data for this period" during the brief post-reload window before the cloud streams finish loading. The empty-state was reading as "no data exists" when the data was simply still in flight (the blank-tile-on-hard-reload report) — now it only says "no data" once the streams have loaded and the selected period genuinely has none.',
  ]},
  {version:'4.778', date:'2026-08-03', changes:[
    'Productization (multi-operator groundwork): removed hard-coded "27 stores"/"27 Locations" and the "MCDOK & Emerald Arches" org name from user-facing chrome — the About footer and the Sales Anomaly export footer now read the district name from settings and the store count dynamically from the store list, and the correlation subtitle, projections/smart-targets loading text, morning-brief header, calendar "Search All" button, and the analytics correlation tooltips all derive the count. Identical for the current org; correct for any future operator.',
  ]},
  {version:'4.777', date:'2026-08-03', changes:[
    'Above-Store One-Pager FOB depth cont. (Notes 47 v2): the FOB panel now shows EOM count completion (Food+Condiment — the classes that move FOB$) for the window\'s month — X/Y stores complete, average % counted, and the stores furthest behind by name. Only appears once counting has actually started (mid-month reads of 0% are correctly treated as "not started", not "behind"). Loaded on demand from the persisted per-store count status; folded into the AI narrative + print brief.',
  ]},
  {version:'4.776', date:'2026-08-03', changes:[
    'Above-Store One-Pager FOB depth (Notes 47 v2): the FOB panel now shows FOB% vs last year, dollar-weighted with the IDENTICAL Σ components ÷ Σ prod-sales method as the current figure (from the qsr_fob ly_* columns) — a defensible direction signal (lower than LY = better, shown in percentage points). Folded into the AI narrative + print brief. FOB count-completion + recount help/hurt are next.',
  ]},
  {version:'4.775', date:'2026-08-03', changes:[
    'Above-Store One-Pager FOB% fix: the panel was reading FOB from the manual XLSX stream (percentage fields) but the dollar-weighted FOB engine needs the QSRSoft qsr_fob dollar stream (prodSalesAmt/compWasteAmt/…). The mismatch meant every FOB row was skipped and the FOB% tile, the per-store FOB column, and FOB in the AI/print all rendered blank since v4.767. Now sourced from ds.qsrFobRows — FOB% populates correctly.',
  ]},
  {version:'4.774', date:'2026-08-03', changes:[
    'Above-Store One-Pager: Controls panel now names the outliers behind the scope averages (Notes 47 v2) — the worst store-days for Cash Over/Short %, T-Reds After %, and Discount %, with store + date. Cash O/S ranks by absolute deviation from zero (a big short and a big over are both exposure) and keeps its sign (over +, short −); T-Reds and Discount rank highest-first and drop clean days. Sourced through the shared metric resolver (auto-first per day, never a raw ctrl-row filter). Shown for multi-store scopes and folded into the AI narrative + print brief.',
  ]},
  {version:'4.773', date:'2026-08-03', changes:[
    'Above-Store One-Pager: added the Scheduling / VLH panel (Notes 47) — the sixth panel the rollup was missing. Shows Scheduled vs Forecast hours and the gap, Schd TPMH, and Fixed / Floor / Combined labor % against the owner standard (each segment 10–15%, combined ≤25%), for the selected scope + period. Reconciled to the weekly Scheduling panel via a new range-scoped computeScheduleRollup (ratios of aggregates, dollar/hour-weighted — never an average of daily %s). Selectable in "build your own" panels + My Reports; flows into the AI narrative and print. Falls back to a clear note when LifeLenz has no schedule rows for the scope/window.',
  ]},
  {version:'4.728', date:'2026-08-01', changes:[
    'Faster load / hard refresh: code-split the 11 heaviest secondary panels (EOM Dashboard, Signals, SAGE, One-Pager, SMG Voice, FOB EOM, Delivery Mix, Yearly Projections, Visit Readiness, Graded Visits, Performance Reviews) into on-demand chunks via React.lazy behind a single Suspense boundary. Initial bundle 4.2MB → 3.34MB (~555KB of panel code now loads only when a panel is opened). The home/At-a-Glance view and the Loaded Data strip (incl. Sales) stay in the eager bundle — high-priority, never deferred.',
    'Ops pull (OT source) robustness: when the Operations Report page doesn\'t fire an api.reports request (so the token listener sees nothing), the Playwright fallback now also visits the Daily Activity page to capture the same X-Auth-Token — the likely reason the pull was returning 0 rows. Refresh QSRSOFT_TOKEN or re-run the pull to repopulate qsr_labor_summary → OT.',
  ]},
  {version:'4.727', date:'2026-08-01', changes:[
    'EOM Supervisor OT: now sourced AUTO-FIRST from the Operations Report labor-summary stream (ds.opsLaborRows → overTimeTotalHours/$), device-independent, no manual upload needed; manual daily-labor upload is the fallback and manual entry still overrides. (The auto stream is currently empty because the Operations Report pull is returning 0 rows on a stale QSRSOFT_TOKEN — refresh the token and OT auto-populates with no further app change.)',
    'FOB Leadership Summary: renamed "laggards/achievers" → "Focus Stores / Top Performers" and softened the language ("ran $X over" not "burned") for an above-store-leader doc.',
  ]},
  {version:'4.725', date:'2026-08-01', changes:[
    'FOB Report FIX: the Avg FOB was averaging averages (a simple mean of per-store %s). Now DOLLAR-WEIGHTED (Σ FOB$ ÷ Σ sales) per the standing rule — small stores no longer over-count.',
    'FOB Report → new "⎙ Leadership Summary" report + an in-modal narrative for above-store leaders: the district dollar-weighted FOB vs target, and the MATH of laggards eroding achievers\' gains — "N stores under target banked $X/mo; M over target burned $Y/mo, erasing $Z of it; net the district is $ over/under target; fixing the top 3 laggards recovers $/mo." Plus laggard (with action) and achiever tables. Existing full Print retained.',
  ]},
  {version:'4.724', date:'2026-08-01', changes:[
    'Fountain-yield look-back baselines (task #52). Each store now carries a 3-month baseline of its fountain-beverage short total (mean + sd). The bib-yield check uses it: at a self-serve-tower store, a short WITHIN the store\'s norm stays an info note (structural free refills, not a loss) — but a short ABNORMALLY beyond the store\'s own baseline (≥2σ or ≥1.6×) escalates to a real flag ("check BIB connections / syrup-to-water ratios", tower or not). Notes now cite the store\'s usual $/mo. Engine src/engine/fountain-yield.js, +tests. Validated on real data (e.g. #5183 ~$1,783/mo, #3708 ~$551/mo).',
  ]},
  {version:'4.723', date:'2026-08-01', changes:[
    'FIX (crash): opening the FOB Report threw "$ is not defined" — the money formatter used by the report modal + print/CSV wasn\'t in scope (it was defined only inside the sibling FOB-Analysis modal). Declared it in the component body so both share it. FOB Report opens again.',
    'EOM "Verify & Clear" pre-close check (task #38, KB-grounded): flags deactivated/obsolete WRINs still holding inventory (submit a zero inventory / waste before close) and the same item carrying inventory under 2+ WRINs (zero the unused one — duplicate WRINs must count down to zero). Matched by DESCRIPTION, not leading digits, so items that merely share a WRIN-family prefix are NOT falsely grouped. Quiet when clean (the usual case) — a pre-close safety net. Engine src/engine/eom-verify-clear.js, +tests.',
  ]},
  {version:'4.722', date:'2026-08-01', changes:[
    'FOB Report now has ⎙ Print (→ PDF) + ⬇ CSV export, so the all-location read can drop straight into a DO/GM review — summary, ranked opportunities with their action, and the market→store tables with top item losers. Honors the standing "print/export on every EOM helper" directive.',
  ]},
  {version:'4.721', date:'2026-08-01', changes:[
    'FOB Report (Inventory Control → 📊 FOB Report) — Notes 42 #1. All-location EOM-lean read, scoped to the current filter (one / all / patch): OK/FL summary tiles, a ranked "Biggest opportunities" list, then a market → patch → store hierarchy. Each store shows FOB% vs target, month-over-month trend (▼ improving / ▲ regressing), its worst FOB components vs target, top item losers, a masking flag (large losses offset by gains — the QSRSoft Variance-Card pattern), and a plain-language action plan of easy real-work steps (e.g. "Variance Stat is the driver — audit portioning + waste-recording on [top losers]"). Engine src/engine/fob-report.js, dollar-weighted throughout, +tests. Reports the latest month with real FOB (early in a month the MTD row is all zeros, so it falls back to last completed EOM — same lesson as the count-timing work).',
  ]},
  {version:'4.720', date:'2026-08-01', changes:[
    'Count-timing artifact detection (baked in as a reusable engine, src/engine/count-timing.js). A variance % can be huge yet meaningless when a count lands early in a period — the sales denominator is tiny AND the count reconciles several days of theoretical-only drift since the item’s last physical count. New EOM-diagnosis context note fires ONLY early in a period (≈ first 10% of the month) with material loss: "counted only through day N of ~M — judge the absolute $ and trend, not the %." Stays quiet at month-end. Proven on Durant #5985: 5.60% Variance Stat = $559 loss over 1 day’s $9,979 sales = 0.086% over a full month. Ledger-verified the point-to-point model (Actual Usage = carried POS-Open beginning inv − ending count) to the unit. Shared piece for the coming all-location FOB report.',
  ]},
  {version:'4.719', date:'2026-08-01', changes:[
    'Change Monitor: new "Month-over-month" baseline toggle. Compares each item’s authoritative period variance this period vs last (QSRSoft Variance Stat), flagging improving (variance shrank toward zero / resolved off the list) vs regressing (grew / newly on the list); stores rank by most regressing → laggard detection. KB-grounded framing: variance is a point-to-point reconciliation (Actual Usage = Starting Inv + Purchases ± Transfers − Waste − Ending Inv), and each period already begins from the prior EOM’s ending count, so this compares two reconciliations rather than re-baselining.',
    'Change Monitor drill-down now shows the point-to-point anchor: a "Period start · beginning inventory (carried from last EOM)" row = the first POS Open reading, so the reconciliation window (beginning inventory → binding count = variance) is visible and verifiable right in the ledger table.',
  ]},
  {version:'4.718', date:'2026-08-01', changes:[
    'Second-Look: new "Count accumulation?" check. When a manager re-enters a full TOTAL on top of an already-submitted count while the device count-timer is still active (the app ADDS unless "Replace Count" is used), the on-hand ~doubles into a large positive overage — a likely driver of FOB worsening after counts post. Fires on a multi-entry session that nets a ≥$150 positive overage (normal area build-up nets ≈ $0, so FRIES-type counts are not flagged). Non-accusatory: recommends verifying the true on-hand and using Replace Count / recounting after the timer resets, and names the honest alternative (an un-approved purchase/transfer also reads as an overage — check receipts). KB-grounded in the Mobile Inventory App counting mechanics.',
  ]},
  {version:'4.717', date:'2026-08-01', changes:[
    'FIX (crash): Inventory Control panel threw "Cannot access before initialization" (temporal-dead-zone) — the FOB Root-Cause (riddle) memo referenced `rows` before it was declared. Moved the memo after `rows`. Panel loads again.',
    'Change Monitor v2 → SESSION model (KB-substantiated). An item is counted BY AREA (fries live in 2–3 places), and the QSRSoft app ADDS each SAVE (only "Replace Count" replaces), so one honest count is several submissions in one session — only the FINAL (BINDING) entry counts ("most recent count overrides all previous"). A "recount" now means a SEPARATE session (another day / large gap). Fixes the FRIES/#3708 case that read as "recount hurt / held worse": a −$1,103 fry-station read then +$1,106 freezer add is normal area build-up, net ≈ $0, official period variance $86 — clean, not harmful.',
    'Progression view rebuilt to read like QSRSoft Raw Item Detail: per item, the authoritative PERIOD variance (qsr_variance_stat) is the headline; expand to a table of every count session · entry (Date·Time · Counter · On-hand entered · $ impact) with the binding final marked, plus a plain-language story. "Recount hurt / held worse" fire ONLY across sessions now.',
    'Recount-swing integrity check is session-aware: a big same-day swing between area entries of ONE session is no longer flagged as padding — only genuine cross-session recounts are. Removes the false positive on normal area-by-area counting.',
  ]},
  {version:'4.716', date:'2026-08-01', changes:[
    'FOB Root-Cause Analysis modal (Inventory Control → 🔬 FOB Analysis) — the recount-impact + consistency study, in-app and scoped to the current filter (one / all / patch). ① Recount Impact ranks stores by whether their recounts move variance TOWARD $0 or AWAY (net-harmful = the coaching opportunities), expandable to the exact items that hurt. ② FOB Consistency ranks stores by month-final FOB% standard deviation (steadiest first). Full methodology shown for trust; same engine as the batch scan so numbers are verifiable to the dollar. First run confirmed #3708 / #29760 / #33704 / #10422 as net-harmful-recount opportunities and #38609 / #34222 / #43380 as the steadiest.',
  ]},
  {version:'4.715', date:'2026-08-01', changes:[
    'Change Monitor v2 — a new "📈 Progression" view (now the default) reads each item\'s variance journey straight from the raw count ledger: base count → each recount as a step (✅ moved toward zero / ⚠️ moved away / • held), the net vs base, a verdict (improved / recount hurt / held), and flags (held-worse, $0-improbable). No lock or baseline needed — works for whatever period is selected, and per-store it tallies improved vs hurt vs recounted. The old snapshot "📸 Baseline diff" stays behind the toggle. Hover any value for who/when.',
  ]},
  {version:'4.714', date:'2026-08-01', changes:[
    'Change Monitor v2 engine (Notes 41): eom-variance-progression.js — per-item variance progression from the raw ledger (base count + recount steps with improved/hurt/held direction, net movement, verdict, and flags: recount-worsened / held-worse / zero-variance). Groundwork for the ledger-derived Change Monitor. +7 tests.',
  ]},
  {version:'4.713', date:'2026-08-01', changes:[
    'Trading-day comparison foundation (P0): a new pure engine (trading-days.js) for fair partial-period comparisons. Because we run on daily pulls and sales swing by weekday, a partial range that is NOT a whole number of weeks must be compared weekday-aligned — the clean trick being a 52-week (364-day) shift that preserves the day-of-week exactly, vs a naive same-calendar-date LY. It reports whether trading-day alignment was required (partial range) or calendar was already fair (whole weeks). +6 tests. Next: wire it through vs-LY, projections pace, and movers.',
  ]},
  {version:'4.712', date:'2026-08-01', changes:[
    'Weekly Count Cadence drill-in: click any store in the cadence monitor to expand its biggest between-count variance windows — for each item, how much it moved and BETWEEN WHICH TWO COUNTS (e.g. "100% PURE BEEF moved -$430 between 07/09 and 07/16"). That brackets WHEN the product shrank/grew to a specific window, so the investigation points at the right days instead of the whole month.',
  ]},
  {version:'4.711', date:'2026-08-01', changes:[
    'Fixed SAGE FOB numbers reading near-zero. SAGE was pulling FOB from the stale/unscaled MANUAL food-cost upload AND averaging percentages; it now reads the authoritative auto qsr_fob stream and dollar-weights everything (district FOB + each component = Σ$ ÷ Σ sales), the same engine the dashboard uses. District FOB, the component breakdown, and the per-store ranking are now real, and SAGE is told these are authoritative over any uploaded file. Client-side (system-prompt context) — no redeploy needed.',
  ]},
  {version:'4.710', date:'2026-08-01', changes:[
    'Weekly Count Cadence monitor — Inventory Control → Count Cycle now surfaces, per store: the detected weekly count day ("counts Wednesdays"), the last full Food+Condiment count + days since, this window\'s weekly-vs-daily-spot session mix, and a status (On track / Overdue ≥8d / No full weekly on record) — overdue stores first. Turns Count Cycle from a progress re-skin into a real year-round count-cadence watch (feeds off the raw item history already loaded). It also denotes each store\'s weekly count day, so setting up the weekly cadence is easier.',
  ]},
  {version:'4.709', date:'2026-07-31', changes:[
    'Second-Look roll-up now condenses "Negative usage" too — it was still repeating the full "impossible… inner bags entered as full cases. Recount {item}" paragraph per item because the item name sat in the tail, breaking the shared-coaching detection. Dropped the redundant item name from the coaching (it\'s already the list label), so 11 items collapse to one coaching line + a compact list.',
  ]},
  {version:'4.708', date:'2026-07-31', changes:[
    'Renamed the EOM Dashboard → "Inventory Control" (nav + panel header). It long outgrew "EOM": it now spans counts across cadences, food-cost/FOB diagnosis, integrity forensics, waste, and shareable reports. Internal id unchanged, so nothing else is affected. Weekly/daily count monitoring plugs in here next.',
  ]},
  {version:'4.707', date:'2026-07-31', changes:[
    'Historical logging (longitudinal spine): the nightly cron now writes an eom_count_status_history row per store/period — % counted, on-time vs late timing, count duration, the COUNTER NAMES (primary + all), granted exceptions (+ who approved), integrity-flag counts + who they attribute to, and the FOB result. eom_integrity_flags also gains a person column. Backbone for recurring-pattern detection, manager accountability, and SAGE trend queries. Run the new SQL block to activate.',
    'EOM Summary header now reads "6 Locations with uncounted Food/Condiment (9 Items Total)" — location count plus the item total, on screen and in print (owner QW).',
  ]},
  {version:'4.706', date:'2026-07-31', changes:[
    'Integrity loop closed: the recount forensic scan now writes implausible padding batches to a new eom_integrity_flags table, and the Needs-Attention panel reads them — so #6838-style "8 corrections in 3 minutes" lands automatically in the one-glance triage feed, no digging. Fail-soft (panel + scan both no-op if the table isn\'t created yet). NOTE: run the eom_integrity_flags block in supabase/schema.sql to activate persistence.',
  ]},
  {version:'4.705', date:'2026-07-31', changes:[
    'Needs-Attention panel gains an Integrity category + FOB-over-target: stores over their OWN FOB target now surface (distinct from the vs-district outlier signal), granted early-count exceptions show as an Integrity awareness item, and there\'s a pass-through pipe for pre-computed integrity flags (e.g. implausible recount-swing batches from the forensic scan) so #6838-style padding signals can land in the one-glance triage feed. The panel now loads eom_count_exceptions + store FOB targets to feed these.',
  ]},
  {version:'4.704', date:'2026-07-31', changes:[
    'Second-Look roll-up is now GENERIC + consequence-aware. (1) Any repeated diagnosis condenses — not just recount swings: negative-on-hand (was 12 identical paragraphs), uom-sanity, etc. now show ONE shared coaching line + a compact item list, via automatic longest-common-coaching detection (no per-check templates). (2) A recount swing OUTSIDE the final EOM count window is labeled mid-cycle — it washes out of THIS period\'s number (opening + final count drive the P&L), so it reads as a weekly-count + process-coaching signal, not a period-binding loss. (3) Forensics scan reports pre/post net variance and flags only the true red-flag direction (a net shortage lifted toward zero to hide a loss) — which fired ZERO times in the July district scan (recounts consistently REVEAL shortages, honest direction).',
  ]},
  {version:'4.703', date:'2026-07-31', changes:[
    'Second-Look Signals roll-up: when many items share the SAME diagnosis (e.g. 14 recount swings with identical coaching), the report now shows ONE coaching block + a compact item list instead of repeating the same paragraph 14 times. Cleaner, shorter, and reads smarter. First applied to recount swings (grouped by counter + day, with the timing verdict once); generalizes to other repeated integrity signals.',
  ]},
  {version:'4.702', date:'2026-07-31', changes:[
    'Recount-swing TIMING forensics (padding detection): a recount swing is healthy when genuine, but when a store is actually short product and the loss is negated by offsetting "corrections," the tell is TIME — you can\'t physically walk the building and recount N scattered items in seconds. The recount-swing check now groups the same-manager offsetting swings per day and tests whether enough time elapsed to have recounted them (floor: ~2 min/item, so ~29 min for 14 items — owner\'s standard). If the corrections landed in a window too short, the batch escalates to HIGH with a non-accusatory note ("the N corrections landed within X min — recounting N items needs ≥ Y min — walk me through these before accepting the count"). Never false-flags a spread-out or gap-then-batch (pen-and-paper) recount. Engine eom-recount-forensics.js + district scan scripts/recount-forensics-scan.mjs.',
  ]},
  {version:'4.701', date:'2026-07-31', changes:[
    'Share links are now LIVE, not frozen: a shared EOM report opens on the as-sent snapshot instantly, then re-pulls the freshest synced data and rebuilds itself — with a 🔄 Refresh button and a "synced through / refreshed at" line. So after a GM corrects counts, they (or anyone with the link) can hit Refresh and watch FOB + the uncounted list update, no re-send. The report is rebuilt client-side with the SAME engine the dashboard uses (new shared builder eom-report-build.js); the eom-share edge function serves live rows strictly scoped to that one store+period; the frozen snapshot remains a safe fallback. Corrections appear after the next sync (noted on the page).',
  ]},
  {version:'4.700', date:'2026-07-31', changes:[
    'EOM count-timing rule (owner): Food/Condiment/Paper should be counted on the 2nd & 3rd day out from EOM — the last day is reserved for Non-Product. The diagnosis now adds a soft "📅 Count timing" coaching note when a store bulk-counted those classes on the last day (still counted, just off the ideal schedule). Completion math is unchanged — this is a coaching signal, not a gap.',
  ]},
  {version:'4.699', date:'2026-07-31', changes:[
    'EOM dashboard now dates each store by when it ACTUALLY counted, not a stray last-touch or the close date. New prog.fullCountDate = the day the bulk of items were counted (the mode); the row shows the approved early-count date (exception) if one was granted, else the bulk-count day. Fixes Ponce de Leon reading "7/31" when the real count was 07/28.',
  ]},
  {version:'4.698', date:'2026-07-31', changes:[
    'EOM diagnosis no longer declares "clean sweep" while a store is OVER its FOB target. Root cause: the Top-5 only inspected item-level Food/Condiment variance, but a FOB overage often lives in the COMPONENTS (raw waste, stat variance, condiments). Now: (1) the FOB headline names the drivers — "Driving the +0.27pp overage: Raw Waste +0.16pp ($1,842) · Stat Variance +0.09pp ($3,313)"; (2) those component levers are folded into the SAME Top-5 ranking (compete by dollars-over-target) so an over-target store always surfaces real actions — "Cut Raw Waste", "Investigate Stat Variance", "Tighten Condiments"; (3) the "🏆 clean sweep" celebration only fires when FOB is genuinely at/under target. Factored the report pipeline into one shared builder (eom-report-build.js) so the dashboard + shared link stay identical.',
  ]},
  {version:'4.697', date:'2026-07-31', changes:[
    'EOM diagnosis now flags a granted count-date exception where it matters: a store with an accepted early count gets a "⚠️ Count-date exception granted" banner near the top (dated + approver) AND the "all counted" line reads "all counted, ⚠️ via an accepted early-count exception — off standard process" instead of the plain "all counted (in the final window)". Keeps the awareness that the count was off-process traveling with the numbers, in both the on-screen report and the store message.',
  ]},
  {version:'4.696', date:'2026-07-31', changes:[
    'SAGE → 📖 Docs: a QSRSoft Help Center viewer built into SAGE. Search the vendor\'s own docs (the same 208-article corpus SAGE cites), expand any article to read it, and hit "🧠 Explain with SAGE" to hand the article to SAGE for a plain-English read against your operation. Closes the KB loop end-to-end: pulled → stored → grounds SAGE → browsable.',
  ]},
  {version:'4.695', date:'2026-07-31', changes:[
    'Waste Analysis drill-in (EOM Dashboard → 🗑 Waste): click any flagged store to expand its underlying waste events — every entry biggest-$ first (date/time, $, type, manager, EDITED flag, reason) — so a flag ("$350 session", "same $20 on 5 days") is one click from the raw evidence. Each store also gets a "🧠 Ask SAGE" button that hands SAGE the store\'s waste picture (flags + $-by-manager + largest events) and asks for non-accusatory coaching questions, weighting Food/Condiment over paper. Verify, don\'t accuse.',
  ]},
  {version:'4.694', date:'2026-07-31', changes:[
    'SAGE now grounds QSRSoft questions in the vendor\'s own docs: a new search_qsr_kb tool queries the 208-article QSRSoft Help Center corpus (auto-pulled into qsrsoft_kb) so "how does QSRSoft calculate stat variance?", "what is OEPE / R2P / KVS?", "how do I run the raw item report?" are answered from the source and cited — instead of guessed. Vendor documentation, kept separate from your live store numbers (SAGE still uses the live data tools for those).',
  ]},
  {version:'4.693', date:'2026-07-31', changes:[
    'EOM count exception now DATES the accepted count: when you grant "accepted early count", Meridian reads the store\'s items to find the day the bulk of the count actually happened (the perceived full-count date, e.g. Ponce\'s 07/28) and stamps the exception with it — the green tag now shows "✓ count accepted 07/28 · {approver}" so the count is attributed to when it really took place, not the close date.',
    'var-0 fix now flows through the Change Monitor BASELINE too: the nightly baseline auto-lock (eom-snapshot cron) reads per-item variance from the raw item ledger (posts the instant a manager submits) instead of the lagging aggregate Variance/Stat report — so both sides of the day-over-day diff use the same immediate as-counted variance the live side already uses. Re-lock a baseline to see it apply.',
  ]},
  {version:'4.689', date:'2026-07-31', changes:[
    'EOM → count-date EXCEPTIONS: accept a store\'s early count as its EOM count when an above-store leader approves it (e.g., a store that counted on the 28th and won\'t recount). "grant count exception" on the store row records who approved it + a reason; the store then reads complete and the recap stops nudging a recount, but every exception is logged + attributed (green "✓ early count accepted · {approver}" tag) so the pattern stays visible.',
    'EOM integrity checks are now named "Second-Look Signals" (non-accusatory), branded on the recap + a dedicated "verify, don\'t accuse" report section. FOB breakdown matrix colors each component vs its OWN target (red = over / green = under) with a sales-weighted Target row. QSRSoft KB pull (#41) built — crawls the help center for grounding SAGE + diagnostics.',
  ]},
  {version:'4.677', date:'2026-07-31', changes:[
    'New integrity check — HIGH-$ WASTE SESSION: flags one manager\'s waste on one day when it totals far above a typical session (distinct from the store-wide daily spike + overall per-manager share checks). Two-path, non-accusatory framing: if it was expired product pulled at once, it\'s real waste → coach ordering-to-on-hands + FIFO rotation; if not, verify it was thrown, not entered to absorb a variance.',
  ]},
  {version:'4.676', date:'2026-07-31', changes:[
    'New integrity check — RECOUNT SWING: catches a large same-day count-to-count change on one item (the "counted 24, recounted 413" overshoot) even when there are only two counts. A swing this big with no delivery between is exactly where a third count by a different manager should be required before saving — framed as "verify which count was right," never an accusation. Feeds the recap\'s soft "worth a look" note.',
    'EOM recap now lands the actual uncounted items (with $ on hand) right under the FOB line — Food/Condiment counted-early + never-counted and Paper never-counted, time/class-aware (Non-Product omitted, not due today).',
  ]},
  {version:'4.674', date:'2026-07-31', changes:[
    'EOM store messages now default to a super-abbreviated RECAP — FOB line first, then the Top-5 do-now, a one-line net-variance, and a punchy "time\'s the lever" close. It scales: a clean store collapses to a FOB line + "🏆 clean sweep — finalize." When an integrity pattern fires (uniform/inflated waste, count churn), it adds a soft, non-accusatory "worth a look together" note with the why — never an accusation. The full report is one click away ("Full report" toggle), and "Message all" copies the recap by default. Same computed numbers as the full report — they can\'t drift.',
  ]},
  {version:'4.673', date:'2026-07-31', changes:[
    'EOM → 🔒 "Lock baseline" + 📸 "Change Monitor" — freeze every store\'s full EOM state (FOB + all 6 components, per-item count qty / on-hand $ / variance / last-counted, per-class completion) into a snapshot, then watch what changes. The Change Monitor diffs the live pull against the locked baseline and, per store AND per item, shows whether a recount HELPED (variance moved toward $0 / FOB down) or HURT (moved away). Includes a district roll-up (improving vs worse, $ moved toward/away from zero) and a per-store secondary-review mark/flag. A nightly Action auto-locks the baseline at ~4:30am CT and checkpoints at 10a/4p CT.',
    'Verified the "Product Net sales off by $2.5M" report was NOT a data bug — Meridian\'s per-store Product Sales matches QSRSoft to the dollar; the gap was a scope mismatch (District EOM Summary on OK vs a QSRSoft report on ALL STORE = the 7 FL stores). Added an explicit scope subtitle to the Prod Sales tile so a scoped figure can\'t be eyeballed against an all-store total.',
  ]},
  {version:'4.660', date:'2026-07-30', changes:[
    'EOM → 📣 "Message all" — generate the EOM follow-up message for every location at once (ordered by who needs it first), each with Copy / Open / Mark-sent. Fire off follow-ups to all stores in one place.',
    'EOM → 🔍 "AI Cross-Check" — paste an external AI\'s FOB analysis (e.g. CoachQ) and Meridian reconciles it against its own real numbers: flags rows the external tool FABRICATED (its own components don\'t sum to its stated FOB$) and rows that diverge >$50 from Meridian. Meridian\'s real data is the ground truth.',
  ]},
  {version:'4.657', date:'2026-07-30', changes:[
    'EOM count timing is now class-aware: Food, Condiment AND Paper are due to 100% by EOD; Non-Product isn\'t counted until tomorrow — so it no longer holds a store below 100%, shows as "N tmrw" (muted) on the chips, and is framed as expected (not a gap) in the diagnosis. Progress %, "believes done", and the scoreboard all reflect TODAY\'s target.',
    'No more "no waste logged / verify waste" flags on Paper / Non-Product — waste is a Food/Condiment concern only. "Counted over N days" now uses each item\'s most-recent count date (a store that re-counted everything today reads 1 day). Obsolete / to-count / early tables now show On-hand qty alongside On-hand $.',
  ]},
  {version:'4.656', date:'2026-07-30', changes:[
    'Food-Cost Diagnosis rewrite — class-aware "Finish today\'s count to 100%" (Food/Condiment = real recovery, Paper = due-today completeness, Non-Product = tomorrow); Top-5 always shows 5 and CELEBRATES a clean sweep when a store has none; counted-early + obsolete items render as tables (WRIN, class, on-hand, last-counted, class-aware action). Store message now renders as an on-screen table with a safe plain-text copy.',
  ]},
  {version:'4.653', date:'2026-07-30', changes:[
    'Inventory-integrity layer complete (11 detectors): count re-entry, bulk-submit (a single dominant timestamp = travel-path skipped), waste-inflation, unrealistic-over (fries), negative-usage, negative-on-hand, UOM-sanity, phantom-transfer timing, RUBBER-BAND (padding→collapse), UNMATCHED transfers (no mirror at the sister store), and a self-serve-tower fountain-yield exemption. All in the editable diagnosis registry, framed as verify-not-accuse.',
    'EOM → 🪃 "Rubber-band" scan (padding that ran OVER for months then snapped to a big short) joins Chronic Offenders + Count Reliability. All three scans now have drill-down facts, CSV/Print, 1–2-month look-back, and a patch/operator filter. FOB % vs target + location numbers added to the dashboard.',
  ]},
  {version:'4.636', date:'2026-07-30', changes:[
    'Inventory-integrity detection (new) — the Food-Cost Diagnosis now flags COUNT MANIPULATION: more than 4 count entries for the same item on one day (2-4 is normal for travel-path counting across multiple storage locations), especially when a later entry walks the variance back toward zero — the tell-tale of a re-count to negate an unfavorable result. When the same negate move shows on 2+ items it escalates to critical (intentional, not a correction). More integrity checks (waste inflation, unrealistic over/gain) to follow.',
    'The "Top 5 — do these now" list is now focused to Food & Condiment items only (the profit-driver classes) per owner; the full all-classes analysis stays below.',
  ]},
  {version:'4.634', date:'2026-07-30', changes:[
    'EOM Dashboard → new "📊 Count Reliability" scan: grades each store (A–F) on how CONSISTENTLY it counts across a past window — a store whose same items swing wildly month-to-month (big over-count→correction reversals) is counting unreliably, which skews its numbers and risks a bad opening for next month. Real losses do NOT count against the grade. Least-reliable stores rank first, with the offending items named. Operationalizes "accuracy + consistency is king."',
  ]},
  {version:'4.633', date:'2026-07-30', changes:[
    'EOM count timing is now the LAST count date only — first→last recorded time that day, shown in H:M:S. And "Earlier-count context" is reframed as an accuracy/consistency signal (mid-cycle counts wash out of the final EOM number, which is anchored only by the opening + this EOM count) rather than recoverable dollars.',
  ]},
  {version:'4.632', date:'2026-07-30', changes:[
    'EOM Food-Cost Diagnosis: new interactive "Verify & clear" panel for obsolete/discontinued/inactive items — each shows class + on-hand $ and one-tap buttons to log the decision (✓ Counted; class-aware ✗ Wrote off for Food/Condiment or ◦ Kept-usable for non-product), tracked "X/Y decided". Persists to Supabase (no QSRSoft write-back).',
  ]},
  {version:'4.631', date:'2026-07-30', changes:[
    'SAGE "where to focus" is now class-weighted — leads with Food + Condiment and won\'t chase paper/non-product waste unless materially out of line.',
  ]},
  {version:'4.630', date:'2026-07-30', changes:[
    'EOM count-timing metric: the Food-Cost Diagnosis modal now shows when each store\'s count began→ended + total duration (from the raw-item count timestamps). Plus a class-focus note on the Reference table (Food+Condiment ~22-29% of revenue is where waste attention pays; Paper ~3-4% rarely is).',
  ]},
  {version:'4.629', date:'2026-07-30', changes:[
    'Food-Cost Diagnostics Reference — full detail now leads with Food + Condiment and breaks Paper/Non-Product into a separate section, with a Class column added. The 3/6/12 look-back selectors are labeled as months.',
  ]},
  {version:'4.628', date:'2026-07-30', changes:[
    'Chronic Offenders: fixed a store-loc format mismatch that returned "no results on any window", and the empty state now explains why (no data / only 1 period / genuinely clean).',
  ]},
  {version:'4.627', date:'2026-07-30', changes:[
    'Controls: Discount % is now cloud-fresh from the Operations Report cash-sheet (discount $ ÷ net sales), no manual upload.',
  ]},
  {version:'4.626', date:'2026-07-30', changes:[
    'EOM Dashboard modals: the close ✕ is now pinned to the top-right corner consistently.',
  ]},
  {version:'4.625', date:'2026-07-30', changes:[
    'Operations Report streams (Controls / Labor-OT / Service / sales-mix / 3 Peaks, all with LY) now load into the app, ready for tile wiring as the pull populates.',
  ]},
  {version:'4.624', date:'2026-07-30', changes:[
    'Yearly-targets file: now captures EVERY target column on the sheet (owner req), not just the speed/labor/FOB ones — Voice OSAT (5★) + Execute-as-Designed + OSAT B2B (1★) + 1-800 Contacts, Digital App % of sales + GC/R/D, McDelivery GC/R/D + wait time + star rating, Crew Staffing / Shift Leader / Manager / Total Headcount targets, and TTM Shift-Leader / 0-90 Crew / YTD Crew turnover targets. All 22 flow to ds.targets for every consumer on re-import. Also made all three pipeline paths (full rebuild + incremental + multi-sheet) use the fixed yearly parser so a re-drop lands regardless of how the file is processed.',
  ]},
  {version:'4.622', date:'2026-07-30', changes:[
    'FIXED the real reason yearly targets weren\'t landing: the yearly-targets file (a "Table 1" layout with an Index row-counter column before the Restaurant column, and a category row above the real header) was parsed as 0 stores — so NONE of the file\'s targets flowed and the app silently fell back to static defaults. The parser now anchors on the Restaurant/Loc/Store column (not the Index counter) and picks the true header row, so every yearly target the file provides — OEPE, KVS time/usage, R2P, TPPH, Labor, FOB, Voice OSAT (5★, from VOICE OSAT PACE) and OSAT B2B (1★, from Overall Satisfaction B2B) — now flows to the reviews and every other target consumer on re-import. Regression test added.',
  ]},
  {version:'4.620', date:'2026-07-30', changes:[
    'EOM Obsolete/Discontinued/Inactive guidance is now class-aware and written in terms managers use: always verify with a physical count first, then — Food/Condiment: if it won\'t be used before expiration, waste to zero to account for the balance, then deactivate the WRIN at a verified zero on-hand; Non-Product (promo / Happy Meal items / paper): count and KEEP it if usable (donation / local giveaway) — do not discard — deactivate only once genuinely used up and verified at zero. SAGE gives the same class-specific direction so it never tells a manager to discard usable non-product.',
  ]},
  {version:'4.619', date:'2026-07-30', changes:[
    'Operations Report auto-pull — groundwork: a new pull (scripts/qsrsoft-ops-pull.mjs + daily workflow) captures the whole QSRSoft Operations Report as store-level daily REST data — Controls (discount, T-Reds before/after, meals, drawer, refunds), Labor (OT hours/$, crew, needed hours), Service (CTP/OEPE/DT/MFY/KVS/RTP), channel sales mix, and the 3 Peaks — all WITH last-year values. Kills the manual Operations/Controls upload dependency. (Ingestion layer; the tile wiring lands next.)',
    'Leadership One-Pager: the OSAT B2B (1★) target now reads the effective/yearly targets (was only reading the static defaults), so the "Overall Satisfaction B2B" target from the yearly-targets file lands.',
    'EOM diagnosis: removed the unverified "QSRSoft force-zeros deactivated items ~30–45 days" phrasing (owner is verifying) — the write-off-before-close guidance stands on its own.',
  ]},
  {version:'4.618', date:'2026-07-30', changes:[
    'Leadership One-Pager (Owner→DO): the Supervisor / Patch Accountability section now pre-fills on the filled form — each supervisor with their patch\'s focus store and biggest gap (the patch\'s largest weekly $ opportunity + sales-vs-LY), leaving Committed action blank for the meeting. Maps by who RUNS the stores (live supervisor assignments — responsibility), not ownership, so a supervisor running stores they don\'t own is credited correctly.',
  ]},
  {version:'4.617', date:'2026-07-30', changes:[
    'Leadership One-Pager: the OSAT B2B (1★) target now auto-fills from the yearly-targets file\'s "Overall Satisfaction B2B" column (re-import the yearly targets to pick it up).',
  ]},
  {version:'4.616', date:'2026-07-30', changes:[
    'EOM Dashboard → new "🔁 Chronic offenders" scan: on demand, across the current location scope and a past window (3/6/12 periods), it surfaces the items that are chronically High-Variance / Loss-Forming / Fluctuating on our own pattern principles — ranked by how many stores carry the problem (a systemic/spec issue outranks a one-store fluke), with per-store drill-down and the month-over-month trail. Reads only when you click Run, scoped to your filter, so it stays light on data usage.',
    'Leadership One-Pager: added one-click "‹ Last week" and "This week" buttons next to the week picker.',
  ]},
  {version:'4.615', date:'2026-07-30', changes:[
    'KVS Time per GC now fills from the auto-pulled DAR — the report has no KVS field, but the KVS stations are the MFY make-lines, so it is computed as total MFY serve time ÷ total MFY transactions (reconciled exactly to QSRSoft\'s KVS Time Per GC column). With KVS Healthy Usage (v4.614), both KVS metrics are now cloud-fresh from the DAR and no longer depend on the manual Ops Report or the emailed Glimpse. KVS Healthy Usage measures whether a store opens the 2nd prep-table side when item volume calls for it (blank is correct when volume doesn\'t call for it — not a penalty).',
  ]},
  {version:'4.614', date:'2026-07-30', changes:[
    'Review scorecards: KVS Healthy Usage now fills from the auto-pulled DAR (healthy ÷ total order-health counts, cloud-fresh) so it no longer depends on the emailed Daily Glimpse — recent weeks populate. Voice metrics rewired per owner: "Voice OSAT" now uses the 5★ share only (rated 5 is all that counts), and "Voice B2B (Accuracy)" becomes "OSAT B2B" using the 1★ (worst-box) share where lower is better. A footnote now clarifies these Voice numbers are the latest full SMG month (monthly), not the review week. (KVS Time per GC still pending its DAR field.)',
  ]},
  {version:'4.613', date:'2026-07-30', changes:[
    'EOM Action Items now show ONE line per item: when a single product trips several checks at once, the most urgent result + action stays on the surface and the other checks, the pattern chip, and the month-over-month history all collapse into the expand — so a manager sees one clear decision per item, not several rows for the same product.',
  ]},
  {version:'4.611', date:'2026-07-30', changes:[
    'EOM Food-Cost Diagnosis now carries a Decision guide (2×2) grounded in verified fact: because QSRSoft anchors variance at the period boundary, a mid-month COUNT ERROR washes out of the monthly figure — so the report/SAGE now separate a locked, verified one-off ("drop it") from a recurring real loss ("can\'t recover this month, but fix the cause — it comes back") from an early count never re-counted at EOM ("still fixable — recount to protect next month\'s opening").',
    'Added a UOM-sanity check: a variance whose quantity is a clean whole-case multiple (the classic "3 cases entered as 3 eaches" blunder) is now flagged as verify-first — a possible units-entry error, not a confirmed loss — before it corrupts the monthly number and next month\'s baseline.',
  ]},
  {version:'4.610', date:'2026-07-30', changes:[
    'EOM Food-Cost Diagnosis Action Items now carry their own provenance: click any item to expand its month-over-month variance history (with the worst month flagged and full-case counts where known), and each item is auto-tagged with a pattern chip — Within Tolerance, High Variance, Fluctuating, Loss Pattern Forming, or Inconsistent Count(s) — so a one-off reads differently from a chronic bleeder, and a real-usage loss reads differently from a count-integrity swing. A Look-back selector (3 / 6 / 12 periods) tunes the window.',
  ]},
  {version:'4.609', date:'2026-07-30', changes:[
    'Fixed the EOM Dashboard header — the Scoreboard / EOM Count / Count Cycle toggle no longer squishes when a sync-status message appears; the controls row wraps instead.',
  ]},
  {version:'4.608', date:'2026-07-30', changes:[
    'EOM now logs each store’s count completion over time: the On-Hand pull appends a timestamped snapshot (overall % + per-class %) once per store per hour, building a trajectory of WHEN each store counts each class through the cycle — useful for coaching pace and spotting padding. A trajectory view is the follow-on.',
  ]},
  {version:'4.607', date:'2026-07-30', changes:[
    'EOM Dashboard now shows completion BY CLASS across the district — Food, Condiment, Paper, Non-Product — with Food + Condiment highlighted as the profit drivers to finish first, and Non-Product treated as a last-day class (a low % early is expected, not behind). Renamed the "Year-Round" view to "Count Cycle".',
  ]},
  {version:'4.606', date:'2026-07-30', changes:[
    'EOM Food-Cost Diagnosis: the FOB Analysis report now sits ABOVE the Action Items (reversed per owner preference — it is where the work happens), and the report now prints an itemized "To-count list" of the never-counted products a store must complete before close (not just a count) — which also flows into SAGE.',
  ]},
  {version:'4.605', date:'2026-07-29', changes:[
    'At-A-Glance Service tile now fills R2P from the auto DAR even when the manual Operations Report is stale (it was hard-wired to manual-only). Service metrics now merge field-by-field so each takes its freshest source (DAR, then Glimpse, then manual Ops). KVS Time/Healthy, DT-Parked, and Controls T-Reds/Cash-O/S still require the Operations Report / Controls upload (stopped Jul 15) — the durable fix is auto-pulling that report.',
  ]},
  {version:'4.604', date:'2026-07-29', changes:[
    'Fixed At-A-Glance Digital Sales showing 0% / "0 of 27 reporting": the tile pulled its channel breakdown (McDelivery / MOP / Kiosk) from a source that has no channel split once manual uploads stop; it now reads from the emailed Sales Ledger (which carries the channels and stays current). (Service R2P and KVS on the AAG tile are a separate known gap being worked next.)',
  ]},
  {version:'4.603', date:'2026-07-29', changes:[
    'EOM diagnosis now prints an "Obsolete / Discontinued / Inactive — verify & clear" list — the stale / likely-deactivated items carrying a residual on-hand, each with its on-hand $, last-count date, and the two-way call: present & usable → count it ($0); obsolete/gone → write off now (−$X) so you time the loss cleanly before the period locks.',
  ]},
  {version:'4.602', date:'2026-07-29', changes:[
    'One-Pager: KVS Time per GC + KVS Healthy Usage now appear on all three review forms (Organization / Patch / Restaurant), not just the store form.',
    'EOM Food-Cost Diagnosis: recount lists + the full item table now show the quantity variance expressed in full CASES (e.g. "~+3.0 cs") next to units — easier for a manager to know what to physically look for on a recount. (Uses the raw-item case size.)',
  ]},
  {version:'4.601', date:'2026-07-29', changes:[
    'EOM Food-Cost Diagnosis now shows actual-vs-standard YIELD (the over-portioning fingerprint) — e.g. "over-portioned 52% of std" — with a "Portioning watch" section listing items running below their recipe yield band, so the fix points at the station\'s portioning, not another recount. Matches the strongest part of QSRSoft CoachQ\'s report. (The variance pull already computed the yield band; it now persists it — the yield_lo/yield_hi columns backfill on the next Variance pull, and need the schema.sql snippet run once.)',
  ]},
  {version:'4.600', date:'2026-07-29', changes:[
    'EOM Food-Cost Diagnosis + SAGE now frame "uncounted" items correctly. The report has a Count-Integrity section that splits them into NEVER counted (true blanks — real recovery, count before close), counted EARLY (already counted; recount only if the count looks wrong — dollars are locked this period), and STALE / obsolete / discontinued / inactive items (verify → count if still usable, or write off before close). SAGE is told to respect this split so it never hands a GM a "go count $X of blanks" instruction unless that money is truly never-counted.',
  ]},
  {version:'4.599', date:'2026-07-29', changes:[
    'EOM uncounted-item diagnosis now explains WHY each item reads uncounted: NEVER counted (a true blank), counted EARLY this period (QSRSoft shows it counted — a cascade discussion, not free money), or STALE / prior-period (likely an obsolete / discontinued / inactive item carrying a residual on-hand). The class-chip hover shows each item\'s state + last-counted date, and the engine now separates true blanks from early/stale so value-at-risk isn\'t overstated. Resolves the "12 uncounted that QSRSoft doesn\'t flag" question.',
  ]},
  {version:'4.598', date:'2026-07-29', changes:[
    'EOM Dashboard: on-demand "↻ On-Hand" and "↻ Variance" buttons pull fresh count-progress / raw-item data right now instead of waiting for the next scheduled run. (Requires the trigger-dar-sync edge function to be redeployed with the new allowlist entries.)',
    'The scheduled intraday On-Hand pull now only runs 8am–6pm Central during the count window (managers count during the day), cutting wasted overnight pulls. A manual pull button overrides this anytime.',
  ]},
  {version:'4.597', date:'2026-07-29', changes:[
    'EOM Item Journeys now show the TIME each entry was logged next to the date (emphasized on count events), and same-day events sort by time. Seeing when a count went in — e.g. right at cutoff or re-entered late — helps spot a count that was padded or "fixed" to improve results.',
  ]},
  {version:'4.596', date:'2026-07-29', changes:[
    'SAGE tables read cleaner: numeric columns (dollars, %, seconds, counts) now right-align with tabular figures instead of everything left-aligned, so columns line up like the rest of the app\'s data tables.',
  ]},
  {version:'4.595', date:'2026-07-29', changes:[
    'EOM count progress: when a class list is ≥90% counted but not finished, hovering its chip (e.g. "F 92% ·4") now lists exactly which items are still uncounted, ranked by dollars at risk — so the store can close the last few instead of guessing. The store message already itemizes the gaps; this puts them one hover away in the dashboard.',
  ]},
  {version:'4.594', date:'2026-07-29', changes:[
    'Faster current-data load: the Daily Glimpse, Cash, Sales Ledger, Labor, Ops and Controls streams now load their pages in parallel (like the DAR summary already does) instead of one after another — the current-day sales/service/controls appear noticeably quicker after login.',
    'Fixed the At-A-Glance "stores at red model health" count mismatch (header said 1, checklist said 2): a store with no model-health score yet was being counted as red in one place; both now ignore null scores.',
  ]},
  {version:'4.593', date:'2026-07-29', changes:[
    'At-A-Glance tiles no longer get stuck "as of" the last manual-upload date. The date-window logic checked only the manual Operations Report to decide whether the selected range had data — so once manual uploads stopped, it silently fell back to a 30-day window ending on the last manual date (e.g. Jul 15), pinning Sales & Guest Counts, Service, Controls and the district morning brief to that old date even though the auto DAR / Daily Glimpse were current. It now recognizes data from the auto streams too and anchors to the freshest date across all of them.',
  ]},
  {version:'4.592', date:'2026-07-29', changes:[
    'One-Pager (Supervisor→GM store review): KVS Time per GC and KVS Healthy Usage now auto-fill from the Daily Glimpse (and manual Ops Report) with their targets, instead of KVS Healthy Usage always being blank.',
  ]},
  {version:'4.591', date:'2026-07-29', changes:[
    'Leadership One-Pager now uses the McDonald\'s work week — WEDNESDAY through Tuesday — for its weekly range and label, honoring the Week Start setting instead of assuming Monday–Sunday. The header reads "Week of {Wed date}" and the window shows the correct Wed–Tue span.',
    'Sped up the current-data load: the QSRSoft daily-activity summary (At-A-Glance + One-Pager sales/speed) now fetches its pages in parallel instead of ~39 sequential round-trips — the "took a while" delay before recent data appeared is largely gone — and tolerates a partial read (keeps the newest days) instead of failing.',
  ]},
  {version:'4.590', date:'2026-07-29', changes:[
    'Fixed the blank vs-LY on the Leadership One-Pager: Guest-Count-vs-LY in the scorecard and the "vs LY" column in District Outliers were empty because the vs-LY helper compared date-typed rows against the form\'s text date range (a type mismatch that silently dropped every day). Both now populate from the auto DAR\'s last-year figures. Added a regression test.',
    'Fixed the "Loaded Data" FOB pill showing "Invalid Date" and a doubled store count — it now parses each stream\'s date format and normalizes store numbers.',
  ]},
  {version:'4.589', date:'2026-07-29', changes:[
    'FIXED the real "data reverts to an old date" bug. At-A-Glance sales used an all-or-nothing rule: if ANY manual Operations-Report day fell in the range, it used ONLY the manual data and ignored the auto QSRSoft sync entirely — so the moment the (older) manual upload finished loading, the tile flipped from current back to the last upload date, and any range spanning that date dropped every newer day. It now merges freshest-per-day: the auto DAR fills every day, a manual upload only overrides the specific day it covers. Sales & Guest Counts, its channel mix, vs-LY, and the "as of" date now stay current.',
    'The "Loaded Data" strip now reflects the freshest date across BOTH manual and auto/emailed streams per category (Sales, Service, Controls, FOB), instead of showing only the manual-upload coverage — so it no longer reads weeks-stale while the app is actually running on current cloud data.',
  ]},
  {version:'4.588', date:'2026-07-29', changes:[
    'Extended the recent-data fix to every large cloud read: the emailed Daily-Glimpse, Cash, Sales-Ledger and the manual Labor streams now also load newest-first, so Service (OEPE/KVS), Controls, and Labor % repopulate for the current window even when a read is throttled. (The emailed pipeline was healthy the whole time — the data was being written; the app just wasn\'t reading the newest rows.)',
    'Added a "⚠ Daily data is N days stale" guard banner to the Leadership One-Pager: if the freshest sales/speed/labor date is more than 2 days behind today, the form warns you (with the newest date) instead of quietly printing incomplete numbers.',
  ]},
  {version:'4.587', date:'2026-07-29', changes:[
    'Data-integrity fix: large cloud reads (starting with the QSRSoft daily-activity summary that feeds At-A-Glance + the One-Pager forms) could silently return only PARTIAL data if a read was cut off mid-way — and because they loaded oldest-first, the missing rows were the most RECENT days, so the app quietly showed data "stuck" a couple weeks back while the live data was actually current. Reads now load newest-first (a truncated read keeps the recent days that matter) and a cut-off read now logs a loud warning instead of pretending it was complete. Note: streams fed by the emailed QSRSoft reports (KVS / cash controls / labor %) can still lag until that email pipeline is caught up — that is a separate data-refresh, not a display bug.',
  ]},
  {version:'4.586', date:'2026-07-29', changes:[
    'EOM Dashboard: counted + actively-counting locations now sort to the top of the store list in every mode (matching the Scoreboard), so the stores in play surface first.',
  ]},
  {version:'4.585', date:'2026-07-29', changes:[
    'EOM Food-Cost Diagnosis report redesigned to be manager-first: it now leads with a "👉 Focus now" short-list of the current count\'s actionable items (each with compact colour chips — SHORT/OVER $, recount-worthy, yield off?, no waste logged — and a one-line action), then a quiet, rolled-up "Earlier-count context" summarizing items whose variance is locked in from earlier counts (present, not lost, but no longer competing for attention), then systemic patterns, with the full item table + tiered breakdown kept below as reference. Same depth, far more readable.',
    'The Diagnosis Print/PDF now renders the formatted report (headings, tables, chips) instead of dumping raw markdown text — it matches the on-screen report.',
  ]},
  {version:'4.584', date:'2026-07-29', changes:[
    'EOM Full report: the Unit/Qty Variance column was showing 0.0 for every item (it read a field that only exists on journey events) — it now shows the real quantity variance from the Variance Stat report alongside the $ variance.',
    'EOM Item Journeys: the report figure is now framed as one "Variance" with clear Qty and $ sub-values, and reconciliation now confirms BOTH — a "✓ Variance matches report" when the ledger $ and quantity both tie out, or a specific "⚠ doesn\'t fully match — $ off by … / qty off by …" when they don\'t. Quantity also shows an approximate full-case count where the case size is known.',
    'EOM "Ask SAGE" now closes the EOM dashboard when it hands off, so SAGE opens in front instead of behind the dashboard (it was opening hidden underneath).',
    'Leadership One-Pager review titles renamed: Owner→DO = Organization Business Review & Checkpoint, DO→Supervisor = Patch Business Review & Checkpoint, Supervisor→GM = Restaurant Business Review & Checkpoint.',
  ]},
  {version:'4.583', date:'2026-07-29', changes:[
    'Labor % is now PUNCHED labor for every location, so Florida and Oklahoma compare like-for-like. Background: "Crew Labor %" includes Salaried Manager Labor $ where a store is configured that way (FL is; OK isn\'t), which made FL read higher than OK for the same real performance. The headline Labor % now always uses Punched Labor %, sourced auto-first from Controls → the cloud-fresh Daily Glimpse → manual labor uploads. Crew Labor % and Total Labor % are still available separately (and the Store Dashboard\'s Crew Labor % tile, which was silently blank, now populates).',
  ]},
  {version:'4.582', date:'2026-07-29', changes:[
    'Fix: the One-Pager "opportunity on the table" $ was wildly inflated (some stores showed millions/week). Root cause — current guest counts were read from a near-empty kitchen signal instead of real transactions, which blew up average check. Guest counts now use real transactions everywhere, and the guest/traffic pillar is reframed as Sales-to-Plan: the $ a store is running behind its own QSRSoft projected product sales — a bounded, sane figure that can\'t explode. Fixes the guest-count-vs-last-year actuals at the same time.',
    'Weekly Business Review form — auto-fill + polish: the Period now shows the exact calendar window (e.g. Jul 21 – Jul 27, 2026); the scorecard pre-fills the group\'s rolled-up Targets and auto-checks On-Track (Yes/No) from actual-vs-target; Guest Count vs LY, Voice OSAT and Voice B2B now populate their Actuals from live data; the signature line is labelled with the recipient\'s job title (DO / Supervisor / GM) instead of a generic "Leader"; a single-store review shows both the restaurant # AND its name; "Net Sales" is renamed "Product Sales" throughout; and a compact Discussion Checklist (People Development/Training, Promotions, Controls/Cash, Cleanliness Walkthrough, Other — each Y/N) was added to every variant. Still prints on one page.',
  ]},
  {version:'4.567', date:'2026-07-29', changes:[
    'Leadership One-Pager — Weekly Business Review: a polished, leader-led review the leader completes before each cascade discussion. It leads with last week\'s Wins, a performance scorecard (Product Sales, GC vs LY, OEPE, R2P, Labor %, FOB %, Voice OSAT + B2B — with blank Target / Actual / On-Track for the leader to fill), and closes with Commitments. The form ADAPTS to the cascade level: Owner→DO shows district outliers + opportunities + supervisor accountability; DO→Supervisor shows store-by-store + GM coaching focus; Supervisor→GM shows the store deep-dive (speed/food/labor) + a 10-line shift-manager tracker. Print to PDF, download as an editable Word (.doc), or a fully-blank fillable version. When not blank, actuals auto-fill from live data and shift-manager names pre-list for the store.',
    'New: Forms Library (🗂) — the leadership review forms catalogued by cascade level (Owner→DO / DO→Supervisor / Supervisor→GM), each with Print-blank and Word-blank actions.',
    'New: Metric Lineage (🔍) — a searchable, 100%-transparent registry of every metric that is calculated from other metrics, showing its exact formula, upstream inputs, and the source report/table so any number can be verified against source. Same registry powers the KPI info tooltip.',
    'Speed metrics decoded from the Daily Activity report and now cloud-fresh for the current day: R2P (Receipt to Print) = (fc_untilserve − fc_untilclosedrawer) ÷ fc_trans_cnt, and OEPE (Order-to-Exit) = (dt_untilserve − dt_untilstore) ÷ dt_trans_cnt — both reconciled exactly to the report, so the One-Pager\'s R2P/OEPE tiles populate intraday instead of waiting on a manual upload.',
    'One-Pager Opportunity $ now paces Guest Count vs the store\'s own QSRSoft projection (not a best-in-class store), so an industry-wide sales dip no longer inflates the guest-count opportunity. Recoverable $ are also shown as a relatable weekly figure. Added a per-location breakdown table + supervisor top/bottom-store highlights (FL→FL, OK→OK).',
    'Performance Reviews: added Department Manager + Shift Manager roles; the manager-attribution dropdown (score a DM/AM/Shift-Manager on their OWN shifts) now populates from the Shift Manager Summary data for the selected store.',
    'Fix: the End-of-Month On-Hand inventory pull was failing every run (stale browser-login path) — it now mints a fresh session each run like the variance pull, so EOM count progress flows automatically through month-end.',
  ]},
  {version:'4.556', date:'2026-07-27', changes:[
    'Precautionary hardening batch: fixed a data-completeness bug where the Daily Glimpse / Cash / Sales-Ledger loaders could silently drop the newest ~3 weeks of data on large date ranges (Supabase 1000-row cap) — At-A-Glance tiles now always see the full window. Fixed the Calendar Manager Florida/Oklahoma pills (FL pill was empty). Refreshed this version/changelog and removed stale debug logging.',
    'EOM: Item Journey visual guide (per-item count-cycle timeline with verified-fact vs likely-inference signals, qty + $ variance, reconciled exactly to the Variance Stat report, click-through flow chips), two modes (EOM count-completion + year-round progress), FOB multi-location variance matrix, and the comms draft now carries the full food-cost action plan.',
    'New: "What Needs My Attention Now" (🎯) — one ranked cross-domain triage fusing FOB outliers, behind-last-year, sync health, drive-thru speed, visit-readiness/food-safety risk, and fading saved signals.',
    'FOB Analysis is now cloud-first (works with no upload). Graded Visits shows a most-likely daypart/channel bar. EOM Supervisor exports OP Supplies per store. Signals Scanner supports multi-select bulk-tracking.',
    'Performance Reviews: named, savable, org-shared templates with hard 100%-weight enforcement, plus template-snapshot isolation so changing a template never re-scores finished reviews. (More coming: drag-reorder + editable job titles.)',
  ]},
  {version:'4.533', date:'2026-07-25', changes:[
    'Signals can now correlate against WEATHER and DAY-OF-WEEK, not just business metrics. The auto-pulled weather (high/low/avg temp, rainfall, wind) is now a metric group in the Scanner and Signal Lab — so "warmer days → more sales?" or "rain → drive-thru mix" surface automatically. Rainfall correctly keeps its dry (zero) days so it correlates real weather, not just rainy ones.',
    'New "Calendar" factors — Weekend, Friday, Monday (0/1 flags per day) — let you correlate the common-sense weekly patterns: does this store actually run hotter on weekends? Is there a Friday lift? (Friday is broken out on purpose as the anchor for the eventual Filet-O-Fish-Fridays product-mix check.) Four new seeded signals ship on: Weekend→Sales, High Temp→Sales, Rainfall→Guests, Friday→Sales.',
    'Under the hood: temperature metrics are concept-grouped (high/low/avg won\'t clutter the scanner by "correlating" with each other), and two calendar factors never pair with each other — every cross-relationship to real business metrics still surfaces, with the same effect-size + false-discovery guardrails as before.',
  ]},
  {version:'4.532', date:'2026-07-25', changes:[
    'Simple Models are now first-class across the whole forecasting engine — not just Smart Targets. The trailing-average family that beat every engineered model on store totals (T3M/T6W/T3W, the "simple wins" finding) is now a selectable forecast model ("✨ Simple") everywhere forecasts are produced: Monthly Projections, the forecast table, Forecast Accuracy, Proj-vs-Actuals, and Model Assignment. Under the hood it reuses the exact same proven math as Smart Targets (one implementation, not a copy).',
    'How it works day-to-day: Simple takes the store\'s robust trailing daily rate (3-month / 6-week / 3-week blend, most weight on recent, outlier days dropped) and shapes it to the weekday — so a single day still respects Saturday-vs-Tuesday, while a full month sums back to the exact trailing method that won. It reads only data BEFORE the day it forecasts (strictly leak-free), so any backtest win is real.',
    'Re-validation built in (per the ripple warning): the Model Assignment backtest now competes Simple head-to-head against DOW / AE / EWMA / DI on each store\'s own held-out actuals — re-run it and Simple gets auto-assigned wherever it actually wins, engineered models stay assigned where they win. Nothing was removed; every existing model is preserved. Forecast Accuracy adds a "Simple" column (+ district card) so you can see Simple vs AI vs LY MAPE side-by-side, and you can hand-pick Simple per store/horizon in Model Assignment.',
  ]},
  {version:'4.531', date:'2026-07-25', changes:[
    'Recent-window metrics fill in from the auto streams: OEPE, KVS, Labor %, TPPH and the loss-prevention numbers were showing "—" (or "-100%") on recent periods when a manual Operations Report / Controls upload hadn\'t landed yet. They now fall back to the emailed Daily Glimpse / auto DAR — so Rankings, Org Summary, and the forecast table populate on the current week instead of going blank.',
    'Under the hood (the part that keeps this from ever creeping back): all of that metric sourcing now runs through ONE shared resolver with a per-metric source-priority list — manual first, then auto — instead of each screen reading the raw data its own way. Paired with the shared vs-LY helper from earlier, this is now the single global system for where operating numbers come from; any future data-source change is one edit and every screen benefits.',
  ]},
  {version:'4.530', date:'2026-07-25', changes:[
    'Fix: the forecast table (District View + Store Analytics) left the current week\'s completed-day Actual and GC columns blank. The forecast engine read actuals from manual Operations Report uploads only, so recent days that had only auto-synced (DAR) data showed nothing. Actuals + guest counts now fall back to the auto DAR when a manual upload isn\'t present — so completed days of the current week fill in (Actual, GC, and the AI-vs-Actual variance) as the daily sync lands. (OEPE / TPPH / Labor% for past days still come from the Ops/Controls uploads.)',
  ]},
  {version:'4.529', date:'2026-07-24', changes:[
    'Under the hood: the "current vs last year" math (auto-first sourcing + matched-day comparison) is now ONE shared helper (engine/vs-ly.js) instead of being re-written separately in each panel — which is exactly why the vs-LY bug kept reappearing in different places. Org Summary and Rankings now call the shared helper; future changes are global. Covered by its own tests.',
  ]},
  {version:'4.528', date:'2026-07-24', changes:[
    'Profile menu now has "↑ Load files" — and the Load button was removed from the top bar to declutter it (Notes 27 #8). Same file-upload flow, just tucked into the avatar menu.',
    'Data Manager: the auto-synced sources (LifeLenz Schedule, QSRSoft FOB / eBOS / Daily Activity) now also carry a one-line description of what they pull and on what cadence — matching the source labels added to the manual/emailed sources (Notes 27 #9).',
  ]},
  {version:'4.527', date:'2026-07-24', changes:[
    'Fix: Visit Readiness couldn\'t see your graded visits — the Graded Visits panel loaded them into its own state, but Visit Readiness reads them from the shared data set, which was never populated. So Model Check said "0 stores with a recent visit" and the Visit Patterns section stayed hidden even though 60 visits were loaded. Graded visits now load into the shared data at startup (same Supabase source), so Visit Readiness sees them — Model Check gets real numbers, the per-store "last visit" shows, and Visit Patterns (day/daypart/channel + cadence) appears.',
  ]},
  {version:'4.526', date:'2026-07-24', changes:[
    'Fix (the real Org Summary this time): the "everyone ~-32% vs LY" was coming from the Org Summary panel\'s OWN sales/LY calc (a different code path than the one patched in v4.522). It summed the full current window for sales but the full last-year window for LY with no day-matching — so when the current period is missing its most recent days (those land in the auto DAR, not a manual Operations Report), last year looks ~30% bigger on every store. Now the current sales pull is auto-first (manual upload OR the auto-synced DAR, so recent days aren\'t missing) and vs-LY is matched-day (a day counts only when BOTH years have real sales). Applies to the Company / Org / Operator / Patch rollups and the per-store rows.',
    'Fix: the same artifact made Rankings show "-100%" on GC vs LY (current guests empty vs a full last-year). Now matched-day + auto-first (reads the DAR guest counts) — shows a true YoY or "—" when there is genuinely no comparable data.',
  ]},
  {version:'4.525', date:'2026-07-24', changes:[
    'Changelog / About footer refreshed to match reality: the architecture and data-source lines were badly out of date (they still said "single-file HTML · React 18 · IndexedDB" and, incorrectly, "all data stored locally · no cloud upload"). They now describe the real stack — Vite + React 19, Supabase cloud-first with row-level security, the auto-pull + emailed data sources — and the "Rows Validated" stat is now a live count of rows actually loaded.',
  ]},
  {version:'4.524', date:'2026-07-24', changes:[
    'Data Manager now shows where each Data Type comes from — a small source line under each row (and a hover tooltip) naming the actual report or feed behind it (e.g. Labor Analysis ← QSRSoft "Labor Analysis"/Operations Report; Daily Glimpse ← emailed QSRSoft Daily Glimpse; Daily Activity ← auto-pulled DAR). Makes it obvious what to run/upload when something is missing.',
  ]},
  {version:'4.523', date:'2026-07-24', changes:[
    'Rankings (renamed from "Store Scorecard") now ranks GROUPS too, not just stores — a new "Rank by" toggle switches between Stores, Patch, Operator, and State. Group rows are computed correctly (member rows pooled and aggregated the same way a single store is — rates as a row-mean, sales and guest counts summed, scores as the member average — never averaging pre-rolled averages), so you can see which patch or operator leads on any KPI.',
  ]},
  {version:'4.522', date:'2026-07-24', changes:[
    'Fix (systematic): the "vs LY" comparison was showing almost every store down ~26–33% — inaccurate. The shared pipeline summed the full current 4-week window for this year but last year only over whatever days happened to be in the data, so any gap in last-year coverage looked like a real ~30% decline. It now uses a matched-day comparison: a day only counts when BOTH this year and last year have real sales for it, so the two sides always span the identical calendar days (apples to apples). This corrects the Org Summary district vs-LY AND every per-store vs-LY at once. Where there genuinely isn\'t comparable last-year data, it now honestly shows "unavailable" instead of a false decline.',
  ]},
  {version:'4.521', date:'2026-07-24', changes:[
    'Visit Readiness — new "📄 Coaching report" button on each expanded store: prints (or saves to PDF) a clean one-pager you can hand or send to that store. It shows the readiness score + band, the plain-language "Why", a ranked "Recommended focus" (the specific gaps to close, biggest-impact first), the score breakdown by area, a full metric-vs-target table, and the last actual visit — in the app\'s workbook style.',
  ]},
  {version:'4.520', date:'2026-07-24', changes:[
    'Visit Readiness — new "Visit Patterns" section (bottom of the panel): a statistic tracker over your ACTUAL graded visits broken down by the known variables — day of week, daypart, weekpart, and channel (each showing count, pass-rate, and average score) — plus a per-store frequency table (how many visits, average days between them, days since the last, and pass rate). Filter by CFV / RGR / all. Surfaces patterns like "Friday lunch visits underperform" or "this store hasn\'t been visited in 90 days."',
  ]},
  {version:'4.519', date:'2026-07-24', changes:[
    'Visit Readiness — explainability & trust: expanding a store now shows a plain-language "Why" line that names the specific gaps driving its score (e.g. "At risk — the biggest gaps are OEPE at 210s vs 165s target and SMG accuracy at 92% vs 95%"). New "Model check" card at the top validates the estimate against your ACTUAL graded-visit scores — a rank-correlation + direction-match hit-rate across the stores that have had a recent CFV/RGR/EcoSure visit — so you can see whether stores rated lower really do score lower in real life (and it says plainly when there aren\'t enough visits yet to trust it).',
  ]},
  {version:'4.518', date:'2026-07-24', changes:[
    'New: Panel Manager (Admin → Panel Manager 🧩) — one place that lists every optional / experimental panel with a short description of what it does, and a switch to show or hide each one in the sidebar. Fourteen lower-traffic experiments (Record Days, Revenue, Inventory, Performance Calc, Metric Correlations, Store Compare, GM Letters, Channel Intel, DAR Analysis, Product Mix, District Lens, Anomaly Scan, Why Engine, Priority Actions) are now hidden by default to declutter the sidebar — flip any back on here anytime (your choices are remembered on this device). Nothing was deleted, and the forecast / diagnostic model tools are untouched. "Show all" / "Hide all" toggles the whole set at once.',
  ]},
  {version:'4.517', date:'2026-07-24', changes:[
    'Test Kitchen tidy-up (reversible): removed two redundant sidebar entries — "Proj Workflow" (an exact duplicate of "Projections") and "Calendar Manager" (its recurring rules already live in Events & Tags). Nothing was deleted — both panels still open and are one uncomment away from returning; the forecast/diagnostic model tools are untouched.',
  ]},
  {version:'4.516', date:'2026-07-24', changes:[
    'New "People / HR" sidebar section that groups the people-facing panels together — Performance Reviews, Visit Readiness, and Graded Visits — instead of scattering them across Performance and Analytics. Same panels, just easier to find as a set (room to grow as coaching tools are added).',
  ]},
  {version:'4.515', date:'2026-07-24', changes:[
    'New: a "Scheduling" hub under a new "Labor & Scheduling" sidebar section, merging five labor panels — Labor Analytics, Scheduling, Weekly Schedule Summary, Labor Analysis, and Employee Skills — into one place with tabs across the top (companion to the Planning hub). Each tab loads on demand, and your role controls which tabs appear. Old links still work — anything that opened e.g. Schedule Summary now opens Scheduling on that tab.',
  ]},
  {version:'4.514', date:'2026-07-24', changes:[
    'SAGE Prompt Library: the ★ Save button is now always clickable — if the box is empty it tells you why instead of looking dead (fixes "Save wasn\'t enabled"). Added a ★ Save prompt button under every SAGE answer that drops the exact question that produced it into the library. And a new "This chat\'s prompts" checklist lets you multi-select the questions you asked in a session and either save each one or combine them into a single saved prompt.',
    'At-A-Glance "Sales & Guest Counts" tile: restored the vs-LY figures that went blank on devices without a manual Operations Report upload. Guest-count vs LY now falls back to the auto DAR last-year transactions (it previously only read manual uploads), and sales vs LY also reads the emailed Sales Ledger\'s last-year column — so the tile shows real year-over-year again from the cloud streams, not just when a spreadsheet was loaded. (Channel Mix still needs the emailed Sales Ledger stream to be flowing — if that section is empty, the Sales Ledger email isn\'t landing.)',
  ]},
  {version:'4.513', date:'2026-07-24', changes:[
    'New: a single "Planning" panel that merges the five forward-looking views — Targets, Monthly Projections, Pace to Target, Yearly Projections, and Smart Targets — into one place with tabs across the top, replacing five separate sidebar entries. Same tools, less sidebar clutter, and related planning work now lives side by side. Each tab loads on demand (so opening Planning is fast and the heavy Smart-Targets backtest only runs when you click its tab). Old links still work — anything that used to jump to e.g. Smart Targets now opens Planning on that tab.',
  ]},
  {version:'4.512', date:'2026-07-24', changes:[
    'New: a profile menu (the round avatar at the top-right) that gathers the account + utility actions that used to crowd the top bar — theme (light/dark), Save session to file, Help & guide, User management, Show/Hide Test Kitchen, Change password, and Sign out — under one tap, with your email and role shown at the top. Several of these were previously unreachable on mobile.',
    'Mobile fix: the All / OK / FL location scope pills are now shown in the top bar on phones (they were desktop-only before), so you can switch state focus without a laptop. Settings (⚙) and Load (↑) stay in the bar; everything else moved into the profile menu to declutter.',
  ]},
  {version:'4.511', date:'2026-07-24', changes:[
    'New: Panel Index in the Knowledge Base (📖) — a live, plain-language map of every panel grouped like the sidebar, so you (or a new user) can see at a glance what each panel does and where a workflow lives. Open Knowledge Base → "App Guide" → Panel Index.',
  ]},
  {version:'4.510', date:'2026-07-24', changes:[
    'Data Manager Sync buttons now confirm which stream actually got triggered. If you click one source (e.g. LifeLenz Schedule) but the server starts a different sync, the toast warns you loudly instead of silently trusting it — the tell-tale sign the sync Edge Function needs a redeploy. Each toast also names the stream it dispatched.',
  ]},
  {version:'4.509', date:'2026-07-24', changes:[
    'Per-station breakdown (v4.507) now uses the verified LifeLenz shift query, so the daily sync can actually pull it. Also excludes rejected/unassigned shifts so a dropped shift no longer adds phantom hours to a station. (No visible change until the data lands after the next LifeLenz sync.)',
  ]},
  {version:'4.508', date:'2026-07-24', changes:[
    'Fix: the At-A-Glance "Sales & Guest Counts" tile showed a wildly inflated "vs LY" (e.g. +330%). Current-period sales were summed over the whole window but last-year sales only over whatever days happened to have data — so when last year covered just a fraction of the current days, the ratio exploded. vs LY (sales, guests, and avg check, district + OK/FL) is now a matched-day comparison: each store contributes only days where BOTH this year and last year have real data, so the percentages are true year-over-year again.',
    'Fix: the At-A-Glance "This Week — District Projection" table left Actual, vs LY, and Acc% blank all week. Actuals were only read from manual labor uploads; the current week\'s live numbers arrive via the auto-synced daily activity (DAR), which the projection wasn\'t reading. It now fills each completed day\'s Actual (and real last-year sales) from the auto-synced stream when a manual upload isn\'t present — so Actual/vs LY/Acc% populate as the week fills in. (Note: the header "vs LY %" can still read ~0% for stores on the adaptive-ensemble model — a separate, deeper item.)',
    'Fix: SAGE\'s 📚 Prompts library felt "dead" — nothing was clickable — whenever the SAGE composer was empty. The Save controls were gated on there being text in the composer, so with an empty composer and no saved prompts yet, everything was disabled. The modal now has its own prompt box (pre-filled from the composer when it has text): type or paste any prompt and save it directly, no composer text required.',
  ]},
  {version:'4.507', date:'2026-07-24', changes:[
    'New: per-station hours & cost breakdown in the Weekly Schedule Summary. Expand any store and, below the daily grid, you now see where its scheduled hours and labor dollars go by LifeLenz station — Drive Thru, Grill, Lobby, Maintenance, Opening, and the rest — each tagged Variable / Floor / Fixed, with shift count, regular vs overtime hours, cost, and $/hr, plus a category summary and a total row. Pulled automatically from LifeLenz (ShiftsForSchedulePeriod) into a new cloud table so it\'s the same on every device; it fills in after the daily LifeLenz sync.',
  ]},
  {version:'4.506', date:'2026-07-24', changes:[
    'Weekly Schedule Summary now shows Fixed Hours and Floor Hours as SEPARATE segments — Fixed %, Floor %, and a combined Fixed+Floor % — instead of one lumped Fixed Labor %. Each is scheduled hours in that segment ÷ total scheduled hours. Color flags apply the standard: each segment should run 10–15% (green in-band, amber outside), and the combined Fixed+Floor must stay at or under 25% of total scheduled hours (green ok, red over the cap). Applies at both the store and district level, rolled up as a true ratio of aggregate hours.',
  ]},
  {version:'4.505', date:'2026-07-24', changes:[
    'Fix: Weekly Schedule Summary labor % was reading far too high (e.g. a store showing 72% instead of ~24%). The daily labor % is an ACTUAL figure — null on future days and temporarily enormous on the current, partial day (labor has accrued but the day’s sales haven’t landed yet, so a mid-day read can be 400%+). That single partial day was dominating the weekly dollar-weighted average. Now the weekly labor % is dollar-weighted over the completed days only; future/partial/garbage days are dropped and show blank in the daily grid.',
  ]},
  {version:'4.504', date:'2026-07-24', changes:[
    'New: Weekly Schedule Summary (Operations → 🗓). The LifeLenz weekly-schedule "top section" — Labor % Sales, Sales & GC Forecast, Scheduled vs Forecast hours with the daily over/unders, Schd TPMH, Fixed Labor % — but across ALL stores at once (LifeLenz only shows one at a time), with a week stepper and per-store daily grid. Derived from the lifelenz_schedule data already synced daily — verified to reconcile to the LifeLenz screen to the penny and the minute. (The per-job hours/cost breakdown is a separate LifeLenz endpoint, not yet pulled.)',
  ]},
  {version:'4.503', date:'2026-07-24', changes:[
    'SAGE is now minimizable. Hit "—" and SAGE collapses to a floating pill (bottom-right) while the session keeps running — so you can pull up other Meridian data at the same time. The pill glows red while SAGE is thinking and green when the answer\'s ready; click it to jump back in. A matching status dot sits in the SAGE header.',
    'SAGE conversation history: every chat is archived when you start a new one, and a 🕘 History button lets you reopen or delete past conversations — so closed sessions and previous searches are always recoverable.',
  ]},
  {version:'4.502', date:'2026-07-24', changes:[
    'Fix: SAGE\'s 📚 Prompts library (and the 🐞 Log modal) opened but ignored all clicks/typing — the modals were trapped inside SAGE\'s stacking context and covered by other app layers. They now render at the top level (portal) above everything, so they\'re fully interactive.',
    'Fix: the 🐞 Log button was capturing the wrong prompt — if you\'d answered a SAGE suggestion with "Yes, please", that thin reply became the logged context. It now walks back to the actual substantive prompt and captures the last few turns of the conversation, so multi-prompt sessions log accurately.',
  ]},
  {version:'4.501', date:'2026-07-24', changes:[
    'New: Visit Readiness (Analytics → 🛡️). Estimates how each store would fare on a 2026 PACE graded visit (Customer First / Running Great Restaurants / EcoSure Food Safety) from the operational metrics you already track — so you coach the at-risk stores before the (mostly unannounced) visit lands. A 0–100 readiness score weighted Speed 35% / Accuracy 30% / Quality 20% / Leadership 15% (each metric scored against that store\'s own target), a separate Food-Safety risk flag from waste/holding proxies, per-store top risk drivers (actual vs target), and last actual visit score when available. Ranked most-at-risk first. Transparent early-warning estimate, not a predicted percentage; Cleanliness is an acknowledged data gap.',
  ]},
  {version:'4.500', date:'2026-07-24', changes:[
    'SAGE can now answer promo/discount ROI questions directly — ask "are our promos paying off?" or "is Durant\'s discounting worth it?" and it runs the same matched-day analysis as the Promo/Discount ROI panel (server-side, RBAC-scoped) and explains the verdict. (Activates after a sage-chat edge-function redeploy.)',
  ]},
  {version:'4.499', date:'2026-07-24', changes:[
    'New: Promo / Discount ROI (Operations → 🎟️). Answers "are our promos and discounts paying for themselves?" with a matched-day analysis — each store\'s promo-heavy days are compared against its promo-light days within the same weekday (controls for the weekly pattern and for running promos on slow days), and the sales/guest lift is weighed against the give-away at a configurable incremental margin. Per-store verdicts (Pays / Costs / Neutral) sorted worst-ROI first, plus a district rollup. Reads the auto-synced Daily Glimpse (promo) and Controls (discount) streams. Directional — a screen for where to dig, not a randomized trial.',
  ]},
  {version:'4.498', date:'2026-07-24', changes:[
    'Fix: the home "Sales vs Last Year" and "Guest Count vs Last Year" figures were wildly inflated (district +532%, FL +2390%) because they averaged each store\'s year-over-year % — so a brand-new store with a near-zero last-year baseline (e.g. Ponce de Leon, opened 03/2026) dominated the average. They\'re now true comp-store comparisons: dollar/guest-weighted (Σ this year − Σ last year) ÷ Σ last year, including only stores with a real prior-year baseline (last year ≥ 20% of current). New/ramping stores still count in the sales totals, just not in the vs-LY comparison. Tiles relabeled "vs LY (comp)". The Today\'s Movers strip got the same guard so a new store can no longer top it with a nonsense "+2390%".',
  ]},
  {version:'4.497', date:'2026-07-24', changes:[
    'Fix: the "This Week — District Projection" table on the home screen had its OK/FL state tags inverted — every Florida store was labeled OK and every Oklahoma store FL. Root cause was a local org-mapping helper that was backwards relative to the rest of the app (canonical: MCDOK = Oklahoma, Emerald Arches = Florida). Labels now match each store\'s real state.',
  ]},
  {version:'4.496', date:'2026-07-24', changes:[
    'Signals Scanner — smarter results: near-identical metric pairs are now suppressed (the same measure pulled from two sources — e.g. manual Sales vs cloud Sales — or the same event as count/$/%). Those always correlate ~1.0 and were crowding out the real discoveries; the scanner now surfaces genuine cross-metric relationships instead.',
  ]},
  {version:'4.495', date:'2026-07-24', changes:[
    'Signals — new 🔎 Scanner tab: auto-correlation engine that cycles every metric pair and surfaces the strongest relationships, ranked by Pearson r with a Spearman cross-check. Guardrails keep it honest — a minimum sample size, an effect-size floor, and a Benjamini–Hochberg false-discovery correction so pairs that only look strong by chance are flagged out. Results are framed as "move together," never cause-and-effect; any discovery can be one-click promoted into Signal Lab. Ships with a set of predefined "obvious" signals so the panel has value before you scan.',
    'Signals — expanded metric library: the Controls family now includes T-Reds Before AND After Total (% and count), regular cash + cashless refunds, the full promo group, discount count/$, POS override $, and cash over/short $ — plus the cryptic "Red B %" is now correctly labeled "T-Reds Before Total %". Food Cost adds Disc Coupon %.',
    'Signals — the correlation engine can now read the daily-synced cloud streams (Daily Glimpse, Cash Sheet, Sales Ledger, DAR summary), not just manual uploads, so signals compute off cloud-fresh data on every device. These appear as new "(Cloud)" metric groups.',
  ]},
  {version:'4.494', date:'2026-07-23', changes:[
    'SAGE is now RBAC-aware: what SAGE can see and recommend is scoped to each user\'s role and accessible stores. A restricted user sees their own store detail plus district totals and their rank — but never another store\'s individual figures. Owner/admin access is unchanged. (Activates after a sage-chat edge-function redeploy.)',
  ]},
  {version:'4.492', date:'2026-07-23', changes:[
    'Yearly Projections: new view that rolls the 12 official monthly sales targets into an annual picture per store — Annual Target, YTD Actual, YTD-vs-plan (current month prorated), Projected Full Year, and FY-vs-target — with OK/FL/grand subtotals and a year stepper. Nav: PLANNING → Yearly Projections.',
  ]},
  {version:'4.491', date:'2026-07-23', changes:[
    'Signals: tracking-to-plan now shows guest-count pace alongside dollar pace, with a traffic-vs-sales divergence flag — guests running ahead of sales warns of a check-average slip before it shows up in the dollars.',
  ]},
  {version:'4.490', date:'2026-07-23', changes:[
    'Pace to Target: new view — current-month MTD actual sales vs the official monthly target, with run-rate pace and % ahead/behind, plus a Store / Patch / Operator toggle. Nav: PLANNING → Pace to Target.',
  ]},
  {version:'4.489', date:'2026-07-23', changes:[
    'Smart Targets: added a FOB % metric (matching the At-A-Glance food-cost formula) and an "Apply as Official" action — per-store or all-shown — that writes the Smart number into the official monthly targets for the upcoming month, feeding Projections.',
  ]},
  {version:'4.488', date:'2026-07-23', changes:[
    'SAGE: saved-prompt library (📚 Prompts) — save, run, and schedule your go-to questions to auto-run daily or weekly, with results surfaced on a new "SAGE Scheduled Runs" At-A-Glance tile.',
  ]},
  {version:'4.487', date:'2026-07-23', changes:[
    'SAGE: every answer now has a 🐞 Log action that turns it into a tracked Task or Feature Request — auto-suggesting the destination, capturing the question + answer, and drafting a ready-to-paste troubleshooting prompt.',
  ]},
  {version:'4.486', date:'2026-07-23', changes:[
    'Smart Targets: per-store known-event adjustments — exclude one-off days (holidays, outages, remodels) from the learning window, and add a signed event delta to the target.',
  ]},
  {version:'4.485', date:'2026-07-23', changes:[
    'Labor Analysis: weekly Fixed-Labor-Hours inputs now derive automatically from the daily LifeLenz schedule (cloud-fresh on every device); a manual MBI upload only gap-fills stores the auto source doesn\'t cover. Added week navigation and an Auto/Manual source chip.',
  ]},
  {version:'4.484', date:'2026-07-23', changes:[
    'Smart Targets: added Labor % (sales-weighted) and DT Speed / OEPE (car-weighted) as target metrics, alongside Sales.',
  ]},
  {version:'4.483', date:'2026-07-23', changes:[
    'Smart Targets model: a 27-store backtest found simple trailing methods beat the engineered models for monthly store sales, so the recommended number is now the median of three simple methods (T3M/T6W/T3W · recent-3wk · 3-mo-avg). The engineered models are preserved as on-demand diagnostics, and the backtest was deepened.',
  ]},
  {version:'4.482', date:'2026-07-22', changes:[
    'New Smart Targets, Labor Analysis (Fixed-Labor-Hours), and Employee Skill Levels panels. The LifeLenz People pull is now token-independent (captures the login session\'s own token), so it no longer breaks on monthly token expiry.',
  ]},
  {version:'4.426', date:'2026-07-20', changes:[
    'Data-Refresh sprint: emailed QSRSoft reports (Sales Ledger, Daily Glimpse, Cash Sheet) now parse server-side into Supabase, so channel mix / 3PO / OEPE / KVS / controls are cloud-fresh on every device. At-A-Glance tiles use freshest-wins (manual upload overrides same-day; auto fills the gap), the FOB tile is dollar-weighted, and there\'s a "movers" strip, in-app Sync buttons, and an intraday DAR pull.',
  ]},
  {version:'4.389', date:'2026-07-09', changes:[
    'Task Queue: mobile-first panel for autonomous + manual work tracking. Two tabs — Queue (add/prioritize/status) and AI Notes (drop session context). Tier 1/2/3 safety classification, priority 🔴🟡🟢, status lifecycle. Fixed ⊕ FAB, bottom-sheet add form. Supabase-backed (tasks + session_notes tables). Nav: ANALYTICS → Task Queue.',
  ]},
  {version:'4.388', date:'2026-07-09', changes:[
    'QSRSoft field definitions: load 412 definitions from Supabase at startup (ds.qsrFieldDefs). FOBAnalysisPanel: hover any food cost category row to see QSRSoft\'s definition. SAGE: field definitions for FOB, DAR, Ops, Cash pages injected into system prompt so SAGE can explain what any metric means.',
  ]},
  {version:'4.316', date:'2026-07-05', changes:[
    'Feature Requests module: Supabase-backed panel for all users to submit ideas and vote. Pre-seeded with roadmap history (13 items). Dev mode: status changes + notes. Nav: ANALYTICS section.',
  ]},
  {version:'4.315', date:'2026-07-05', changes:[
    'Data Manager: cloud-first header, Supabase section now shows Labor/Ops/Controls/FOB/DAR operational coverage.',
  ]},
  {version:'4.314', date:'2026-07-05', changes:[
    'Org Summary: renamed from District Summary, group selector updated to Company / Org / Operator / Patch (replaces Operator / Supervisor / Market).',
  ]},
  {version:'4.313', date:'2026-07-05', changes:[
    'District grid: fix FL chip — use INV_ORG_COORDS[loc].state for FL detection (STORE_COORDS has no state field). Swap colors: FL=gold, OK=blue.',
  ]},
  {version:'4.312', date:'2026-07-05', changes:[
    'District grid: fix FL/OK org chip — now correctly uses store.state==="FL" instead of org field (FL stores defaulted to MCDOK org, causing FL stores to show gold OK chip instead of blue FL chip).',
  ]},
  {version:'4.311', date:'2026-07-05', changes:[
    'District grid StoreCard redesigned (Option A+C): 4px top accent bar (blue=FL, gold=OK), FL/OK state chip, 4-metric rows (Sales with vs-LY%, Labor, OEPE, TPPH all color-coded vs target), model health dot + label + combined score at bottom, critical/watch flag truncated at bottom.',
  ]},
  {version:'4.310', date:'2026-07-05', changes:[
    'District Priority Brief: redesigned to 4-column tile grid (Option A+C). Each store tile now has a 4px top accent bar (blue=FL, gold=OK), FL/OK state chip, 4-metric row (4W Sales, Labor%, OEPE, TPPH) with on/near/over status coloring, Ops Score, and finding sections. Panel widened to 1200px max. SMG VOICE: auto-calibrate smart targets from historical data using p75/p25 percentile engine. LifeLenz: extended schedRows load window from 90 days to 5 years for signal correlation history.',
  ]},
  {version:'4.309', date:'2026-07-04', changes:[
    'Signals: expanded from 11 to 36 correlation signals across 6 domains. New SERVICE signals: Park Rate→OEPE, Park Rate→Sales, DT Mix→OEPE, R2P Pace→Sales, Avg Check→OEPE. New LABOR signals: TPPH→Labor%, Avg Check↔TPPH (speed/ticket tradeoff), Scheduling Gap→OT Hours, Avg Wage Rate→Labor%, Guest Count→Labor% (volume leverage). New FINANCIAL/CONTROLS signals: Discount%→Sales, Drawer Opens→Cash O/S, Manual Refund→Labor%, Waste (Red B)→Food Cost%, POS Overrides→TPPH. New SALES signals: Monthly Sales→Food Cost% (leverage), TPPH→Food Cost%, Avg Check→Daily Sales. New CUSTOMER signals: Park Rate→OSAT, Avg Check→OSAT, Scheduling Gap→OSAT, Discount%→OSAT, Guest Count↔Avg Check (traffic/ticket tradeoff).',
  ]},
  {version:'4.308', date:'2026-07-04', changes:[
    'Signals: New store health exemption — stores with recentOnly flag and no DI calibration return score:null / grade:"New Store" from both modelHealthScore and computeModelHealth. At a Glance counter and red list correctly skip null-score stores.',
  ]},
  {version:'4.306', date:'2026-07-04', changes:[
    'Nav restructure: 4 named sections (DAILY / PERFORMANCE / OPERATIONS / ANALYTICS) replace the flat list. DAILY adds Daily Brief (was Morning Brief). PERFORMANCE: Org Summary (was Org Overview), Store Scorecard (was Store Rankings). OPERATIONS: Labor Analytics (was Labor). ANALYTICS section graduates Signals, SAGE, Forecast Brief (was Intel Brief), Market Intelligence (was Location Intel), District View, Store One-Pager out of Test Kitchen. STORE OPS section removed. Store Notes moved into Settings sidebar (Settings → Store Notes → Open Editor).',
  ]},
  {version:'4.305', date:'2026-07-04', changes:[
    'Signals: major enhancement — 4 new cascade chain signals (OEPE→KVS, OEPE→Sales, KVS→Sales, ScheduleGap→Sales) completing the scheduling→OEPE→KVS→Sales path. Domain tags on all 11 signals (service/labor/sales/food_cost/customer). Domain filter pills to view signals by category. Per-store selector re-runs correlation engine for a single location. Threshold labels replaced with No Effect / Within Tolerance / Out of Range taxonomy. Cascade chain banner appears when 2+ scheduling cascade signals are confirmed. normLoc() exported from insights.js for reuse.',
  ]},
  {version:'4.304', date:'2026-07-04', changes:[
    'Data Manager: added "FOB EOM Troubleshooter (per-store)" section listing the 6 QSRSoft inventory files needed per store (Contributors, On Hand, Summary, Variance Stat, Total P&L, History). Added EOM Supervisor auto-population callout showing which standard Meridian data types feed it (FOB Report → food cost, Operations Report → sales/labor, Controls → cash, Monthly Targets → projections).',
  ]},
  {version:'4.303', date:'2026-07-04', changes:[
    'EOM Supervisor: class filter picker — multi-select pill row (All / Food / Paper / Condiment / etc.) filters all four tabs and the printed report by QSRSoft inventory class. Appears only when loaded data has more than one class. Printed report header shows the active class filter.',
  ]},
  {version:'4.302', date:'2026-07-04', changes:[
    'Monthly Projections patch reports: previous month actuals now populate from the auto-synced LifeLenz schedule data (ds.schedRows) when manual labor uploads are absent — computeMonthActuals supplements laborRows with schedRows for any loc+date not already covered, preventing double-counting.',
    'Supabase persistence for fobRows, opsRows, ctrlRows, darRows: save on upload, load on startup — cloud-first cross-device sync (v4.301 code, version number correction).',
  ]},
  {version:'4.281', date:'2026-07-03', changes:[
    'SAGE AI Assistant: Claude Opus 4.8-powered advisor with access to all Meridian data. Chat with SAGE about district performance, store trends, labor opportunities, food cost, and correlation signals. Opens via 🧠 SAGE in the sidebar. Requires ANTHROPIC_API_KEY set in Supabase Edge Function secrets and deployment of the sage-chat Edge Function.',
  ]},
  {version:'4.279', date:'2026-07-03', changes:[
    'Signals: smarter empty state — when data is loaded but no patterns found, shows "No patterns detected yet" with recompute hint instead of the misleading "Upload data" prompt.',
  ]},
  {version:'4.278', date:'2026-07-03', changes:[
    'Signals: fix loc format mismatch — LifeLenz parser was padding store numbers to 7 digits ("0003708") while all other parsers use short format ("3708"), causing all cross-dataset joins to find zero pairs. Fixed parser + added normLoc() to join helpers for robustness against existing Supabase data.',
    'Signals: added console logging showing pairs/r per signal to aid diagnostics.',
  ]},
  {version:'4.276', date:'2026-07-03', changes:[
    'Service Worker: bumped cache name to mf-share-v4276 — forces all browsers to install the new SW and drop stale JS bundles (fixes Signals nav item not appearing for users on old SW).',
  ]},
  {version:'4.275', date:'2026-07-03', changes:[
    'Signals: now computed on OPFS restore (startup) in addition to file upload — panel shows data immediately after hard refresh without needing to re-upload files.',
  ]},
  {version:'4.274', date:'2026-07-03', changes:[
    'Monthly Targets: startup now loads ALL available periods from Supabase (not just the most recent month) into ds.allMonthlyTargets — persisted in OPFS so available immediately on reload. EOM Supervisor reads the correct period\'s targets directly from this index on every month change.',
    'EOM Supervisor: removed per-period Supabase round-trip on month change; period lookup is instant from allMonthlyTargets.',
  ]},
  {version:'4.273', date:'2026-07-03', changes:[
    'EOM Supervisor: fetch period-specific monthly targets from Supabase when EOM month changes — June targets load for June view even when July is the most recently loaded. All projections are strictly month-specific with no DEFAULT_TARGETS fallback.',
    'EOM Supervisor: actSales uses sum of Labor Analysis daily rows (Operations Report) as primary; FOB fallback only. actLaborPct likewise. Crew Labor % is sales-weighted average of daily rows.',
    'Guest Voice: smgVoicePerf data now persisted in OPFS blob — survives reload without waiting for async Supabase load.',
  ]},
  {version:'4.272', date:'2026-07-03', changes:[
    'EOM Supervisor: actSales and Crew Labor % now use Operations Report (Labor Analysis rows) as primary source — daily rows summed for the month — with FOB as fallback only. Fixes inflated single-day values from FOB partial data.',
  ]},
  {version:'4.271', date:'2026-07-03', changes:[
    'EOM Supervisor: fix actSales to use fobRow.sales (correct field name — prodSales/netSales were wrong); fix OT Hours and OT $ to sum all daily labor rows for the month instead of using peak-day values; fix Crew Labor % to use sales-weighted average from monthly rows when FOB does not supply it.',
    'Weather: fix persistence on reload — OPFS path now falls back to IDB when weather missing from OPFS blob (common after weather fetch predates next file upload); auto-fetch now also saves to OPFS and updates idbCoverage so Data Manager shows fresh dates immediately; removed manual Fetch All Weather button (auto-fetch handles it).',
  ]},
  {version:'4.270', date:'2026-07-03', changes:[
    'Signals: new cross-metric correlation engine. Automatically detects statistical relationships between scheduling gaps, labor, OEPE, OSAT, OT, exceptions, food cost, and DT mix. Signals panel shows r value, strength, direction, and data readiness. Reruns on every upload.',
  ]},
  {version:'4.269', date:'2026-07-02', changes:[
    'DI Calibration persistence: labor rows now saved to Supabase on every upload and merged back on startup — history survives browser cache clears and accumulates across devices. Requires the labor_rows table (run schema.sql block in Supabase SQL editor).',
  ]},
  {version:'4.268', date:'2026-07-02', changes:[
    'Monthly Targets: add 📋 Patch Sheet — vertical-layout group report matching the Excel patch sheet format. Metric rows × columns of (Next Month Target | Action Items | Current Month Actual | vs Projection | Opportunity $). Pick Supervisors or Operators, choose the group, opens printable HTML with group rollup first then individual stores.',
  ]},
  {version:'4.267', date:'2026-07-02', changes:[
    'Monthly Targets: add Flat / Operators / Supervisors group view toggle — grouped mode shows each operator or supervisor section with individual stores, a GROUP TOTAL rollup row (sales-weighted averages), and a DISTRICT TOTAL at the bottom.',
  ]},
  {version:'4.266', date:'2026-07-02', changes:[
    'Labor Analytics: fix Days column showing +1 extra day (Math.round → Math.floor on range end-time fraction).',
    'Monthly Targets: Sales column now shows full dollar amount ($xxx,xxx.xx) instead of abbreviated $xxxK.',
    'Monthly Targets: switching to a period with no Supabase data now correctly shows empty table instead of falling back to the currently-loaded month.',
    'EOM Supervisor: mtOK check now uses row-level _year/_month stamps (set by Supabase load) in addition to monthlyTargetsMeta, so projections populate even when the in-memory data came from Supabase rather than a fresh file upload.',
    'EOM Supervisor: OT Hours target in Projections row now shows "0" (target is always 0).',
    'DI Calibration: error log now shows the first stack frame alongside the error message for easier diagnosis.',
  ]},
  {version:'4.265', date:'2026-07-02', changes:[
    'Remove debug logging from SMG VOICE Performance PDF upload pipeline.',
  ]},
  {version:'4.264', date:'2026-07-02', changes:[
    'Fix: VOICE Performance PDFs dropped/loaded manually now parse and display immediately — previously fell through to "Unrecognized PDF" because only smg-voice type was handled in the PDF upload path.',
  ]},
  {version:'4.263', date:'2026-07-02', changes:[
    'SMG VOICE Performance Reports: full pipeline wired up — Gmail poller detects monthly "Voice Performance Report" emails (SMGMailMgr@whysmg.com), downloads operator PDFs, stores in Supabase. Browser auto-parses PDFs using PDF.js, extracts per-store data (DT Sat, DT Dissat, IR Sat, IR Dissat, Accuracy B2B, Quality B2B, Fries B2B, Snack Wrap B2B) for all 3 report types (Monthly / Trailing 90d / YTD), saves to new smg_voice_performance Supabase table.',
    'SMG VOICE panel: new Performance tab shows all-store ranking table with color-coded metrics, period selector (6 months), and report type toggle (Monthly / T90 / YTD). Metric columns are clickable to re-sort.',
  ]},
  {version:'4.262', date:'2026-07-02', changes:[
    'Data Manager: Daily Glimpse, Cash Sheet, and Labor Exceptions now show file count and date range after page reload — coverage derived from pending_reports table (same approach as Sales Ledger) instead of session-only in-memory rows.',
  ]},
  {version:'4.261', date:'2026-07-02', changes:[
    'Fixed "Manifest: Syntax error" appearing twice on every load — index.html had stale /meridian/ paths for the manifest, favicon, and apple-touch-icon left over from GitHub Pages era. All three now point to / (Netlify root). Deleted stale root-level manifest.webmanifest with old paths.',
    'Fixed Supabase 400 error on pending report download — the Gmail poller pipeline was also picking up manually-uploaded reports (source=manual) and trying to download them from Storage, where they don\'t exist (they\'re stored as base64 in file_data). Filter now excludes source=manual; those are correctly handled by the cross-device sync block.',
  ]},
  {version:'4.260', date:'2026-07-02', changes:[
    'Supabase persistence: user target overrides (mf_targets) now sync across devices via org_config key "app_user_targets" — load on login, push on save. EOM Supervisor manual overrides now sync per-month via org_config key "eom_manual_{y}_{m}" — fetched on month change, pushed on every field edit.',
    'AtAGlance scope fixes: weekly trend sparkline, Sales channel totals, Labor district averages, Service times, Controls percentages, and FOB averages all now correctly aggregate only the stores in the active scope (All / OK / FL) instead of the full unfiltered row set.',
    'Data Manager: SMG VOICE Comments now shows report date range instead of just a count.',
    'Performance Reviews: removed dead ORG_FULL/getOrgFull functions with hardcoded operator names — org name set via Customize → Organization.',
  ]},
  {version:'4.259', date:'2026-07-02', changes:[
    'Nav rename pass: Command Center→Home, Priority Brief→Action Items, Labor Analytics→Labor, FOB Analysis→Food Cost, FOB EOM Check→End of Month, Guest Voice→Voice (SMG), Scheduling Intel→Scheduling, District Summary→Organization Overview, Delivery Mix→3PO Delivery, Morning Brief→Daily Brief, Store KB→Store Notes, Rankings→Rankings and Dashboards, Perf Reviews→Performance Reviews.',
  ]},
  {version:'4.258', date:'2026-07-01', changes:[
    'detectType now recognises QSRSoft underscore-separated filenames (labor_analysis_daily, sales_ledger_daily, cash_sheet_extract_daily, daily_glimpse_daily, labor_exceptions_daily). Previously sales_ledger and daily_glimpse were undetected, cash_sheet fell through to the wrong type (ctrl), and labor_analysis was caught only by a fuzzy low-confidence match.',
  ]},
  {version:'4.257', date:'2026-07-01', changes:[
    'EOM Summary: OT Hours and OT $ now auto-populated from Operations Report period-summary row (manual entry still overrides). projLaborPct now checks tCrewLabor OR tLabor — fixes blank Crew Labor projection when monthly targets were loaded from Supabase (which stored tCrewLabor, not tLabor). Monthly targets + meta now persisted to OPFS alongside row data — survive refresh without Supabase round-trip.',
  ]},
  {version:'4.256', date:'2026-07-01', changes:[
    'EOM Summary data wiring fixes: actSales and actLaborPct now pulled from laborRows (Operations Report Sales sheet) when not present in FOB rows — uses the row with highest sales (period-summary totals >> single-day totals) as the monthly figure. Cash auto-population rounded to 2 decimal places (no more -363.560000000). EditCell initial value displays with 2 decimal places instead of raw float string.',
  ]},
  {version:'4.255', date:'2026-07-01', changes:[
    'Operations Report date parsing hardened: now accepts single-date filenames (was requiring 2+ dates — silently ignored "Operations Report 2026-06-30.xlsx" style names), handles MM/DD/YYYY and MM-DD-YYYY filename formats, adds month-name fallback ("June 2026 Operations Report" → uses last day of June), and validates all extracted dates before using them. Fixes bug where June 30 rows were being assigned June 29 as their date.',
  ]},
  {version:'4.254', date:'2026-07-01', changes:[
    'EOM Supervisor Summary (nav: EOM Summary): new panel that recreates the monthly supervisor patch summary in-app. Auto-populates Net Sales, Total Food Cost %, Food Over Base %, and Crew Labor % from uploaded FOB reports and Monthly Projections (tProdSales, tFOBTotal, tFOBTarget, tLabor, tOpSupply). DEFAULT_TARGETS used as fallback for sales/labor targets when QSRSoft monthly file not loaded. Yellow editable cells for Op Supplies actual, Cash +/−, OT Hours, OT Dollar, labor Transfers and Unclocked Labor — saved to localStorage per month. Filter by Supervisor, Operator, or All Stores. Patch rollup (sales-weighted %) shown at top. Printable (landscape, no chrome). Variance $ amounts calculated as (actual% − proj%) × actual sales. Total shaded boxes = FC$ + FOB$ + Labor New Total$ + OT$.',
  ]},
  {version:'4.253', date:'2026-07-01', changes:[
    'Morning Brief: food cost and SMG OSAT signals added. Brief now shows Base Food % and OSAT in each store\'s metric grid (when FOB/SMG data is loaded). Two new correlation rules — FOOD_COST_HIGH (flags ≥33% red, ≥30% amber) and SMG_OSAT_LOW (flags <65% red, <72% amber) — with full detail and coaching action. Data source coverage pills in panel header show which data types are loaded (Labor / Controls / 3 Peaks / Food Cost / SMG OSAT). Data source line in expanded store card lists FOB month and SMG month when available.',
  ]},
  {version:'4.252', date:'2026-07-01', changes:[
    'Data Manager: staleness indicators — colored dot and "Xd" age suffix on each row (green ≤3d, amber ≤10d, red 11+d). SMG FullScale shown as individual per-period rows (June 2026 · 12 stores, etc.). Delivery Mix coverage row added. Weather Data row added. Upload Files shortcut button closes the panel and opens the file picker. Staleness legend at the bottom.',
  ]},
  {version:'4.251', date:'2026-07-01', changes:[
    'Operator Summary: now driven by settings.operators / settings.supervisorGroups instead of hardcoded INV_ORG_COORDS. FOB food cost columns (Base Food % and Total Food %, sales-weighted rollup) shown when FOB data is loaded. Focus Group dropdown filters to a single operator. Sort, Group, and Focus controls collapsed to two rows.',
  ]},
  {version:'4.250', date:'2026-07-01', changes:[
    'Guest Voice FullScale: fixed three bugs — (1) parser now searches all workbook sheets for the data sheet instead of blindly taking SheetNames[0]; (2) auto-detects OSAT % column instead of hardcoded index; (3) fixed stale selPeriod and tab initialization so scores show immediately after Supabase load without requiring a manual tab click.',
  ]},
  {version:'4.249', date:'2026-07-01', changes:[
    'Scheduling Intel: Get Data panel now detects missing weeks (last 4) and shows one-click quick-select buttons for each gap — click a missing week pill to pre-fill the date range, then copy the terminal command. Red badge on Get Data button shows the count of missing weeks at a glance.',
  ]},
  {version:'4.248', date:'2026-07-01', changes:[
    'Scheduling Intel: panel-level week navigator — prev/next arrows, week pills (up to 8 most recent), date picker jump, data-loaded badge. All tabs (Opportunity, District, Store) now respect the single selected week. OpportunityReport hides its own week picker when panel controls selection.',
  ]},
  {version:'4.247', date:'2026-07-01', changes:[
    'Monthly Targets: 📧 Group Report button opens print/email-ready HTML — one section per operator group with stores, weighted rollup row, and district total. Columns: Sales target vs MTD actual, Crew Labor %, Base Food %, Total Food %, TPPH — each with target and vs-target delta. Data coverage note shows days loaded and through-date.',
    'Guest Voice: SMG FullScale filename detector now matches "Full Scale Report" (with space) in addition to fullscale/full_scale; sheet-name fallback added (Small Graph sheet = FullScale workbook). Run smg_fullscale Supabase table SQL, then re-upload FullScale file.',
  ]},
  {version:'4.246', date:'2026-07-01', changes:[
    'Beta Mode: admins can click "β" in the topbar to collapse the nav to stable-only panels (Rankings, Targets, Monthly Targets, Perf Reviews, Labor Analytics, FOB Analysis, FOB EOM Check, Guest Voice, District Summary, Store KB, Delivery Mix, Scheduling Intel, Morning Brief, Settings, Data Manager). Experimental/forecasting panels are hidden. Toggle persists in localStorage.',
    'SMG VOICE thresholds: configurable via ⚙ Thresholds button — standard, yellow band, per-metric. Color bands (green/yellow/red) applied to all table values.',
    'FOB tolerances: configurable via ⚙ Tolerances button — green = at/under target, yellow = within 0.25% over, red = beyond. Yellow band is customizable.',
  ]},
  {version:'4.245', date:'2026-06-30', changes:[
    'Monthly Projections period switching: flexible filename parsing now detects month/year from underscored names, year-first formats, and numeric patterns (April_2026, 2026-April, 04-2026, etc.) so all uploaded months save correctly to Supabase',
    'Monthly Projections panel: period dropdown now shows for any number of saved periods (was >1 only); manual 📅 picker lets you load any year/month from Supabase regardless of what is in the dropdown',
  ]},
  {version:'4.244', date:'2026-06-30', changes:[
    'Performance Review KPI inputs: OSAT, EPB2B, Labor %, turnover, retention, and food safety pct fields now accept 0–100 values (e.g. type "87.5" for 87.5% OSAT) — auto-fill and storage remain in 0–1 decimal format',
    'Monthly Projections panel (nav: Monthly Targets) — view QSRSoft-uploaded monthly targets for all stores; period selector shows all available Supabase periods; 16 target columns grouped by Sales & Labor, Food Cost, and Other Costs',
  ]},
  {version:'4.237', date:'2026-06-28', changes:[
    'Permission Engine (permissions.js): roles are now fully configurable — create custom roles with any name and level, toggle individual permissions per role, stored in Supabase org_config and synced on login. Admin Panel adds a "Roles & Permissions" tab with an accordion editor (click any role to see and toggle its 19 permission checkboxes grouped by area). Level-1 roles bypass all permission checks. Review Approve/Return/Reopen buttons now gate on the reviews.approve permission (on by default for Area Supervisor, off for Manager). Admin Panel button in topbar gates on users.manage.all permission.',
  ]},
  {version:'4.236', date:'2026-06-28', changes:[
    'Admin Panel (👤 button in topbar): in-app user management for admins — view all users, change roles (Admin/Supervisor/Manager), assign accessible store codes per user, and invite new users via magic-link email. No SQL Editor required. Role is fetched from the Supabase profile on login and threads through to the performance review approval workflow (only admins see Approve/Return buttons).',
  ]},
  {version:'4.235', date:'2026-06-28', changes:[
    'Performance Reviews — Approval Workflow: reviews now have a status lifecycle: Draft → Submitted for Review → Approved (or Returned for Revision). Each review shows a color-coded status badge in the list and in the editor header. Action buttons appear contextually: "Submit for Review" on a Draft, "Approve" and "Return for Revision" for admins reviewing a Submitted review, and "Reopen" on an Approved review. Returning a review prompts for a reason note shown inline. Submitted and Approved reviews are read-only (Save is disabled). Status filter added to review list toolbar. Full status history is stored on each review and synced to Supabase.',
  ]},
  {version:'4.234', date:'2026-06-28', changes:[
    'Performance Reviews — Score Breakdown: click any metric ▶ row to expand a full month-by-month table (Actual · Target · Deviation · Rating) for the half period. Gap hint shows how many avg rating points are needed to reach the next level and the resulting impact on the overall score. Monthly data uses all 6 half-months, showing nulls where data was not entered.',
  ]},
  {version:'4.233', date:'2026-06-28', changes:[
    'Removed all hardcoded "Murphy Family Restaurants" / "MFR" references from the codebase. Competency text now uses generic org language. Login screen subtitle is now dynamic — set your organization name in Customize → Organization. The org name persists in localStorage and appears on the login screen and in print headers.',
  ]},
  {version:'4.232', date:'2026-06-28', changes:[
    'Performance Reviews — Score Breakdown panel added to Summary tab. Expand "SCORE BREAKDOWN" to see the full step-by-step math: each scored metric\'s avg rating, weight, and contribution to its category score; category scores weighted into the Metrics total; Behavioral quarterly averages; and the final formula (Metrics×70% + Behavioral×30% = Overall). Each metric below Exceeds shows exactly how many avg rating points are needed for the next level and the resulting impact on the overall score.',
  ]},
  {version:'4.231', date:'2026-06-27', changes:[
    'Supabase integration (Stack A): added @supabase/supabase-js, AuthGate login screen (magic-link email), Supabase sync layer in review-engine.js, and Sign Out button in topbar. App runs in local-only mode when env vars are absent — no behavior change until Supabase is configured. Schema and RLS policies in supabase/schema.sql.',
  ]},
  {version:'4.230', date:'2026-06-27', changes:[
    'Performance Reviews — Competencies: each item now has an active/inactive toggle (checkbox). Inactive items are hidden from the rating UI and excluded from behavioral scoring, but keep their index so existing ratings stay intact. Also supports custom behavioral categories: use "+ Category" in Customize → Competencies to add your own categories (editable label, deletable).',
    'Performance Reviews — Weights: metric rows now show "Active" instead of "Scored" with a clearer label. Delete button (×) per metric removes it from scoring calculations (KPI data is preserved). Deactivating via checkbox excludes from scoring without removing the metric.',
    'Performance Reviews — Rating Thresholds: "Current Meaning" column now shows actual values with direction context (e.g. "4 ≥+5% · 3 ≥0% · 2 ≥-5% · 1 else") instead of generic t1/t2/t3 placeholders. Updated header explains what raising/lowering each threshold boundary does in plain English.',
    'Behavioral scoring engine updated to respect active/inactive competency flags and include custom categories in calculations.',
  ]},
  {version:'4.229', date:'2026-06-27', changes:[
    'Performance Reviews: 4 demo reviews pre-loaded — Ronald McDonald (GM/3708, Exceeds Expectations ~94%), Grimace (GM/29760, Needs Improvement ~33%), Hamburglar (AM/5985, Meets Expectations ~73%), Mayor McCheese (AS/6178 Chipley FL Emerald Arches, Below Expectations ~52%). Load via "📚 Demo Reviews" button in the review list toolbar. Data persists in localStorage alongside real reviews.',
  ]},
  {version:'4.228', date:'2026-06-27', changes:[
    'Performance Reviews: org auto-detection — stores auto-assign to McDOK (Oklahoma) or Emerald Arches (Florida) based on existing store mapping, shown in review editor header.',
    'Performance Reviews: logo upload — Customize → Logos tab stores one PNG/JPG per org in browser storage; logos embed in Print/PDF output header automatically.',
    'Performance Reviews: Help guide — "? Help" button in panel header opens a full methodology reference covering rating scale, 70/30 scoring formula, category weights, and a metric source guide (QSRSoft/SMG/Altametrics paths for every KPI).',
  ]},
  {version:'4.227', date:'2026-06-27', changes:[
    'Record Day Intelligence expanded: added Guest Count records (day/week/month), Day-of-Week records for sales and GC (best Monday ever, best Tuesday ever, etc.), KVS and R2P speed records alongside OEPE, Breakfast sales records, and Average Check records. Records now accumulate across uploads via localStorage — all-time bests persist even when uploading different date windows. New 6-tab UI: Overview (district heroes), Recent Breaks (filterable by type + window), Sales & Volume, Speed of Service, Day of Week (DOW selector + ranked leaderboard), Top Days. Reset button clears the saved all-time record history.',
  ]},
  {version:'4.225', date:'2026-06-27', changes:[
    'Record Day Intelligence: new panel (nav: Performance → Record Day Intel) that scans all uploaded data to surface all-time records per store — best day, week, and month sales plus best OEPE. Shows district-level champion stats, a chronological "recent record breakers" table (configurable 30/60/90/180-day window) with previous record comparison, a sortable all-time records grid by store, and a district top-15 days leaderboard.',
  ]},
  {version:'4.224', date:'2026-06-27', changes:[
    'Performance Reviews Phase 2: Dev Plan tab with narrative fields + structured action items; Print/PDF export via window.open()/print(); Wage section in Summary tab fully wired and editable.',
  ]},
  {version:'4.223', date:'2026-06-27', changes:[
    'Performance Reviews: full salaried management performance review system — GM, AM, AS, and OM reviews with 70/30 metrics/behavioral split, auto-populate KPIs from uploaded data, behavioral competency ratings (1-4) per quarter, quarterly/half-year score rollup, and Customize panel for editing scoring thresholds, category/metric weights, and all behavioral competency text per role. Accessible via Performance Reviews in the nav under Performance.',
  ]},
  {version:'4.222', date:'2026-06-27', changes:[
    'Channel Intelligence root cause fix: dedup merge now rescues channel sales/pct fields (bfSales, mopSales, kioskSales, delivSales, and their GC/AvgChk/PctTotal counterparts) from discarded rows into the surviving row. This fixes the case where a Labor Analysis file loaded after an Operations Report would silently overwrite the richer channel data with zeros for the same dates, causing Breakfast/MOP/Kiosk/Delivery to show 0% in Channel Intelligence and the DOW Heat-Map even though the Operations Report was present.',
  ]},
  {version:'4.221', date:'2026-06-27', changes:[
    'Location Overview Tab: redesigned as Hybrid Intelligence Panel — Context Strip (KB notes + tags + recent calendar events), Metric Vitals (5 traffic-light KPI tiles: OEPE, Labor%, TPPH, Cash O/S, Ops Score with color-coded status), Priority Findings (top 4 risk/watch items in 2-column grid), Predictive Alerts (trend alerts inline), and Charts Section (collapsed by default with ▼ Show Charts toggle)',
  ]},
  {version:'4.220', date:'2026-06-27', changes:[
    'Channel Intelligence: fixed non-DT channel data (Breakfast, MOP, Kiosk, Delivery) not displaying — added fallback to per-store % fields (bfPctTotal, mopPctTotal, etc.) when dollar-amount columns are not populated in the Operations Report Sales sheet',
    'Channel Intelligence diagnostic: warning banner now lists the exact column names Meridian looks for in the Operations Report, so column name mismatches can be identified and reported',
    'DOW Channel Heat-Map (Shift Analysis): applied same pctKey fallback so Breakfast/MOP/Kiosk/Delivery rows now appear in the heatmap when pctTotal data is available',
    'Shift Analysis guide strip: DOW Ops Metrics, OEPE Revenue Opportunity, 3 Peaks × Labor Gap, and Competitive Impact buttons now scroll to their respective sections on click (previously display-only)',
    'FOB Root-Cause Priority Matrix: swapped display order — Location (store name) now appears before Component label, matching natural priority-coaching order',
    'Base Food KPI card: removed "Theoretical cost — for reference only" label; now shows ▲/✓ vs-target comparison when tFOBBase target is available, or "No target set" when not',
    'Base Food target column: added more fallback patterns to column name matching (Base Food %, Base Food%, BaseFoodPct, Base Food Target)',
    'Channel column fallbacks: expanded patterns for Breakfast (BF Sales), MOP (MOB Sales, Mobile All Net Sales), Kiosk (KSK Sales, SOK Net Sales), Delivery (3PD All Net Sales, 3rd Party Net Sales)',
    'Print / PDF: added 🖨 Print button to Revenue Intelligence Engine, FOB Analysis, and Channel Intelligence panels',
    'Store KB: replaced free-text tag field with clickable Quick Tags organized into Performance / Management / Location / Physical / Context groups — single-click to toggle, auto-updates tag list',
    'Competitive Impact: replaced empty state (null/blank) with explanatory message directing user to Calendar to tag competition events',
  ]},
  {version:'4.219', date:'2026-06-27', changes:[
    'Fixed 3 Peaks × Labor Gap showing nothing — root cause: r.date.toISOString() called on ISO string after IDB round-trip (strings don\'t have .toISOString()). Added _toD()/_toDK() helpers; laborByDate is now built once outside the per-slice loop instead of rebuilt 3 times',
    'Fixed case where all OEPE readings are above target (was returning null for every slice, hiding the section) — now compares worst-half vs best-half OEPE days with a label explaining the split',
    'Fixed DOW Heat-Map .getDay() calls on laborRows — same date-type safety fix applied to the DOW data builder (r.date.getDay() → _toD(r.date).getDay())',
    'Fixed Competitive Impact runtime error: r.date.toISOString() on string dates in the DOW average and row-lookup code — replaced with _toDK() helper',
    'Fixed Weekly Narrative "Unable to generate narrative" — was reading settings.anthropicKey (undefined) instead of localStorage.getItem("mf_anthropic_key")',
    'Fixed FOB Root-Cause Matrix showing rollup rows (fobPct sep:true, pLFoodPct isTotal:true) — added !c.sep&&!c.isTotal filter; Base Food excluded via actionable:false',
    'Fixed District Lens Opportunity Store/Dist Average all showing — computeMetricAverages was comparing r.date (Date) against a string cutDate; fixed to compare Date objects',
    'Fixed District View blank screen — showCohorts&&cohorts?A:B parsed as showCohorts&&(cohorts?A:B); added parentheses to fix operator precedence',
    'Fixed mdToNodes is not defined crash in forecast.js InfoIcon — circular dependency prevented import from store-dash.js; defined mdToNodes inline in forecast.js',
    'Fixed (userEvents||[]).filter is not a function — userEvents is {[loc]:{[dk]:event}} object, not array; flattened with nested Object.entries loops; fixed e.date → e.evDate',
    'Weekly Narrative: added error message propagation — if API returns {error:{...}}, shows error message instead of falling through to "Unable to generate narrative"',
    'Added Predictive Alerts callout at top of Overview tab when TREND ALERT findings exist',
    'Added feature guide strip at top of Shift Analysis tab with DOW Heat-Map / OEPE Opportunity / 3 Peaks / Competitive Impact status pills',
    'Added 3 Peaks × Labor Gap cross-reference note in PeaksTab linking users to the Shift Analysis section',
  ]},
  {version:'4.216', date:'2026-06-19', changes:[
    'Fixed the triple-redundant read found while reviewing the restore path: performFullIDBRestore() was reading the same large stores from IndexedDB up to 3 times — once in loadDsFromIDB(), again in idbGetCoverage() just for date ranges, a third time for the weather cache bridge',
    'Added coverageFromLoadedRows() — computes the identical coverage stats from data already in memory, zero additional IndexedDB reads. Weather cache now reuses the already-loaded weather array directly',
    'Net effect: Restore Session now reads your data once instead of up to three times',
  ]},
  {version:'4.215', date:'2026-06-19', changes:[
    'Stopped patching the restore mechanism and fixed the actual design flaw: session restore was automatic and blocking — if it hung for any reason, the app gave you no way to interact with anything, including closing a stuck modal',
    'Session restore is now opt-in. The app loads instantly and empty on every load. A lightweight, hard-timeout-bounded check (count() only, 3-second cap, cannot block the shell no matter what) detects a previous session and shows a dismissible banner — you click Restore Session when ready, instead of it happening automatically before you can do anything else',
    'Added a universal Escape-key handler that closes every modal in the app unconditionally — does not depend on diagnosing why something got stuck, just guarantees a way out',
    'This directly resolves the stuck Dialed-In Comparison modal — that flag (showDICompare) is included in the Escape handler',
    'All prior fixes this session (compute6wk indexing, getModelAssignment caching, AtAGlance/StoreDash/OrgView modal-gating, the IDB schema and race-condition fixes) remain in place — none of that work is lost, this addresses the structural risk sitting on top of it',
  ]},
  {version:'4.214', date:'2026-06-19', changes:[
    'Critical finding: fully quitting Chrome (not just closing tabs) and the freeze STILL happened in the regular profile — that ruled out every stale-tab/blocked-connection theory, since nothing could survive a full quit',
    'Re-examined every Incognito test with that ruled out: Incognito ALWAYS starts with empty IndexedDB, meaning every "fast in Incognito" result was also a "nothing to restore" result — the same confound from the very first extension test, missed a second time',
    'Found the real bug: openIDB() only cached the RESOLVED value, not the in-flight request. loadDsFromIDB() fires 8 idbGetAllRows() calls simultaneously via Promise.all — every one of them called openIDB() before any had resolved, so all 8 independently fired their own indexedDB.open() against the same database instead of sharing one connection',
    'This is a within-tab, within-session bug — explains why it persisted through a full Chrome quit, and why it only manifests with substantial existing data (Incognito\'s always-empty DB has nothing for 8 racing connections to meaningfully contend over)',
    'Fixed: cache the in-flight promise itself, not just the eventual value — every concurrent caller now awaits the same single open() instead of starting 8 redundant ones',
  ]},
  {version:'4.213', date:'2026-06-19', changes:[
    'Different symptom this time: freeze on INITIAL load with zero data loaded — ruled out the AtAGlance fix as the cause, since there was nothing to compute',
    'Found a real gap: openIDB() had no onblocked handler. If another open tab/window holds an older-version connection to MeridianDB, a version-change request just hangs forever — never firing onerror, onsuccess, or onupgradeneeded. No heavy CPU, nothing to catch in a Performance trace — just a promise that never settles',
    'Given how many file versions have been opened back-to-back across this session\'s testing, a stale older-version tab is a very plausible explanation',
    'Fixed: added onblocked to both IndexedDB connections (MeridianDB and the separate McForecastPro_Sessions backup) — now rejects cleanly into the existing try/catch instead of hanging silently',
    'Practical step alongside this fix: close any other open Meridian tabs/windows before reloading, since that\'s the actual trigger condition if this theory is right',
  ]},
  {version:'4.212', date:'2026-06-19', changes:[
    'Found the actual root cause via a Chrome Performance recording (console-level instrumentation had ruled out everything it could see — this needed a real profiler)',
    'AtAGlance — the main dashboard, a 1600-line component — was rendering and fully recomputing every time ANY of 53 separate modal flags opened, even though it was 100% visually hidden behind the full-screen overlay the entire time',
    'Confirmed directly in the profile: AtAGlance\'s own render function was the dominant cost in a 177-second interaction, not Priority Brief, not buildStore, not React itself',
    'Fixed: added a single anyModalOpen check (OR of every modal-visibility flag in App\'s own scope — 7 candidates were excluded after verifying they\'re declared in other components, which would have caused an immediate crash) and gated AtAGlance, StoreDash, and OrgView on it — none of them render while a modal covers them',
    'Tradeoff worth knowing: these views fully unmount while hidden rather than just visually hiding, so local UI state (like scroll position) resets when a modal closes. Worth it given the alternative was multi-minute freezes.',
  ]},
  {version:'4.211', date:'2026-06-19', changes:[
    'Incognito test confirmed the freeze was a Chrome extension — \'message\' handler violation dropped from 174,745ms to 242ms with extensions disabled. That variable is resolved.',
    'Found the real cause of the remaining IDB error: weatherRows was never in IDB_STORES at all — not a stale-version gap like darRows/pmixRows, a flat-out missing entry from day one',
    'Pinpointed via the exact failure location: Promise.all index 7 in loadDsFromIDB maps directly to idbGetAllRows(\'weatherRows\') — confirmed reproducible even in a guaranteed-fresh Incognito IndexedDB, which ruled out versioning as the cause for this specific store',
    'Fixed: weatherRows added to IDB_STORES. IDB_VERSION bumped 3→4 so a re-download to the same exact filename still gets the store created correctly',
    'Swept every idbGetAllRows/idbPutRows call site in the codebase — all 9 distinct store names now present in the schema, no other mismatches found',
  ]},
  {version:'4.210', date:'2026-06-19', changes:[
    'Found via real console data: the [PERF] instrumentation proved Priority Brief\'s own code is fast (rawStores 418ms, tiered/pulse/render-body all under 1ms) — the freeze was NOT in this code path',
    'Real culprit: IndexedDB schema mismatch. IDB_VERSION was never bumped when IDB_STORES grew (darRows/pmixRows/peaksRows added later) — onupgradeneeded only fires on a version increase, so existing browsers never got those object stores created',
    'Every read/write against a missing store threw "object store not found" — repeatedly, including 16x on a single auto-save, visible directly in console output',
    'Fixed: IDB_VERSION bumped 2→3. The upgrade handler is purely additive (only creates missing stores) so this is safe regardless of how outdated any browser\'s existing copy is',
    'Flagged but not fixed: "listener indicated an asynchronous response" errors in the console are a near-textbook Chrome extension signature, not page code — Meridian has no chrome.runtime API usage. Worth testing in Incognito to rule in/out extension involvement in the remaining freeze time',
  ]},
  {version:'4.209', date:'2026-06-19', changes:[
    'Diagnostic instrumentation (temporary) — Priority Brief still hard-freezing the browser despite two rounds of targeted fixes means something is being missed, not guessed around',
    'Console timing added at: rawStores (all 27 buildStore calls, broken down into compute6wk vs buildBrief time), and DistrictPriorityBrief\'s own mount/tiered/pulse computations',
    'Open the browser console before clicking Priority Brief — the [PERF] log lines will show exactly where the time goes on the next freeze, replacing speculation with real numbers from the actual session',
  ]},
  {version:'4.208', date:'2026-06-19', changes:[
    'Performance: found a second hot-path issue — getModelAssignment() re-parsed the full localStorage assignment blob on every single call, with no caching',
    'Called directly from forecastDay() — the single most-invoked function in the app. A Why Engine district scan alone makes 1,500+ forecastDay calls, each independently re-parsing the same JSON',
    'Also called once per store inside District Priority Brief\'s tiered computation — 27 full re-parses on every filter-pill click',
    'Added a module-level cache, invalidated explicitly on all 3 write paths (saveModelOverride, clearOvr, the backtest engine) — parse once, never silently stale',
    'Confirmed via real data: the LY fix (v4.205) is working correctly — District Priority Brief now shows properly differentiated 4W vs LY per store (e.g. Elgin +12.3%, Chickasha -6.9%) instead of uniform ~-93%',
  ]},
  {version:'4.207', date:'2026-06-19', changes:[
    'Fixed runtime crash: GM Coaching Letters threw "userEvents is not defined" — Why Engine cross-wiring (v4.203) referenced userEvents inside buildContext but never added it to the component\'s props or the call site',
    'GMCoachingBrief now correctly receives userEvents — isolated to this one component; DistrictPriorityBrief and LifeLenzBridgePanel were already wired correctly from their original construction',
    'Swept the rest of the codebase for the same pattern (referencing a variable without receiving it as a prop) — no other instances found',
  ]},
  {version:'4.206', date:'2026-06-19', changes:[
    'Performance: found the actual cause of recurring slowness — compute6wk() makes ~28 avg6() calls per invocation, each independently re-scanning the FULL multi-year, all-27-store array for one field',
    'Runs 3x per store (p/p2/p4 windows) x 27 stores = 2,000+ full array passes every time the store list recomputes — on load, on settings save, on Dialed-In calibration',
    'Added a per-store row index (laborByLoc/opsByLoc/ctrlByLoc/darByLoc), built once at every data-load and session-restore path — same 5 places the existing per-day index already gets built',
    'compute6wk and buildStore\'s pSales/pLY loop now operate on the pre-filtered per-store slice instead of the full district-wide array — identical math, identical semantics, far less to scan',
    'Fixed the most common load path too (App startup IndexedDB restore) — confirmed via existing code comments to be more frequent in practice than fresh Excel upload',
  ]},
  {version:'4.205', date:'2026-06-19', changes:[
    'Critical fix: buildStore\'s pLY (4-week LY comparison) was missing an upper date bound — summed ~392 days of LY sales against pSales\'s 28 days',
    'Caused a uniform ~92-93% "decline vs LY" on every single store regardless of actual performance — first surfaced via District Priority Brief\'s aggregate, but affected every consumer of store.pSales/store.pLY',
    'Also silently affected GM Coaching Letters\' "Sales (4wk) vs LY" line — every letter generated before this fix would have shown a false catastrophic decline',
    'Fixed at the source in buildStore — cascades correctly to District Priority Brief, GM Coaching Letters, and all other vsLY consumers with no per-feature changes needed',
  ]},
  {version:'4.204', date:'2026-06-19', changes:[
    'Model Assignment Backtest — first real-data run: 65 of 81 store×horizon assignments updated, all known problem stores (Elgin, Sulphur, Madill, Tishomingo) resolved correctly per their existing notes',
    'Found + fixed: Mossy Head yearly horizon showed 355%+ MAPE across all models — one contaminated period the recentOnly window didn\'t fully exclude was dominating the average',
    'Backtest MAPE is now a trimmed mean — worst ~5% of individual-day errors excluded before averaging (min sample guards apply), so one bad data day can\'t decide the model winner',
    'Trimming is always surfaced in the evidence ref ("N outlier days excluded") — never silently hidden, since a high trim count is itself a data-quality signal worth noticing',
  ]},
  {version:'4.203', date:'2026-06-18', changes:[
    'Engine cross-wiring: Why Engine ↔ GM Coaching Letters, Priority Brief, LifeLenz Bridge',
    'GM Coaching Letters: buildContext() now runs a 4-week Why Engine scan per store — accuracy + attribution injected into the prompt',
    'GM Coaching Letters: INSIGHT instruction is now specific (low explained% → surfaces missing event context or worst-DOW pattern as the coaching insight)',
    'Priority Brief: calibration gap detection via stored model MAPE — green stores with MAPE >12% promoted to amber with a 🔬 badge (zero recomputation)',
    'LifeLenz Bridge: each DOW bias stat now shows why LifeLenz is biased (weather-driven vs model gap vs situational) — changes how confidently to make the adjustment',
  ]},
  {version:'4.202', date:'2026-06-18', changes:[
    'LifeLenz Bridge — complementary to LifeLenz scheduling, not competing with it (no API, manual entry only)',
    'LEVEL: forward adjustment % per store/day — direct comparison when LifeLenz\'s own "Projected Sales" exists for that date in the loaded file, historical DOW-bias pattern fallback when it doesn\'t',
    'SHAPE: hourly distribution curves built from real darRows history, flagged for deviation when a tagged Calendar event (school release, local event, weather) suggests the normal hourly shape won\'t hold',
    'Single-store 14-day forward view + district-wide ranked scan, Copy Table for fast manual entry',
    'Every adjustment is labeled Direct or Pattern-based — no false confidence when LifeLenz\'s own forward number isn\'t in the loaded file',
  ]},
  {version:'4.201', date:'2026-06-18', changes:[
    'Why Engine — systematic miss attribution, the answer to "why did we miss" across every day, not one click at a time',
    'diagnoseMiss/crossStoreCheck extracted from ForecastTable closures to top-level functions — reused, not duplicated',
    'New: dollar-quantified forecast composition (weather/ops/trend/event $ contribution) via exact algebra on the known forecast formula',
    'Single-store scan: MAPE, explained-vs-unexplained miss rate, DOW miss pattern, worst misses each with full diagnosis',
    'District scan: ranks all 27 stores by MAPE and explained%, surfaces calibration candidates (high MAPE + low explained = model gap, not missing event data)',
    'Every miss card can tag an event directly, closing the loop back into calibration',
  ]},
  {version:'4.200', date:'2026-06-18', changes:[
    'Calendar Manager — proactive event calendar, converts event system from reactive to forward-looking',
    'School calendar event types added (early release, no-school, breaks, year start/end)',
    'Recurring rules engine — register an annual pattern once instead of re-tagging every year',
    'Proactive AI search — finds school district calendars + local events via web search, single-store or all-27 batch',
    'Unified Pending Review queue — AI-search and recurring-rule instances both require human approval before writing',
    'Month-grid calendar view with District/OK/FL/single-store scope, reuses existing EventEntryModal for entry',
    'Every write goes through the same mf_events storage — no separate code path, every existing system sees these identically',
  ]},
  {version:'4.199', date:'2026-06-18', changes:[
    'GM Coaching Letters — evolved from single-store on-demand to district-wide batch engine',
    'Batch mode: "Generate All 27" — one supervisor can maintain a coaching cadence with every GM',
    'Data source upgraded — now pulls buildStore\'s findings/opsScore/ctrlScore/trend instead of raw rows',
    '6wk→4wk→2wk trend direction precomputed and stated explicitly (was previously absent entirely)',
    'Critical findings now force the letter\'s FOCUS section to address them directly, not generically',
    'Every letter is an editable draft with a Reviewed checkbox — human review before copy/print',
  ]},
  {version:'4.198', date:'2026-06-18', changes:[
    'District Priority Brief — tiered (Action Required / Watch Closely / Running Well) above-store view',
    'Synthesizes existing store findings/scores — no new computation, pure intelligence layer',
    '"This Week\'s Focus" — auto-derived from most common issue type district-wide',
    'Filterable by org (OK/FL) and supervisor patch · linked from sidebar and AtAGlance',
  ]},
  {version:'4.197', date:'2026-06-18', changes:[
    'Structural audit complete — DevDashboard Engine Trace now wires _userEvents into forecastDay',
    'ShiftAnalysisTab reviewed — pure historical display, no forecast pipeline calls, no wiring needed',
    'Labor Analytics ⚡ Insights tab — 10-rule engine, ranked findings with $ impact estimates',
  ]},
  {version:'4.196', date:'2026-06-18', changes:[
    'Model Assignment Backtest Engine — re-runnable, tests DOW/AE/EWMA/DI per store × horizon',
    'Uses forecastDay\'s forceModel param — same pipeline as production, zero duplicated math',
    'Model Assignment panel — Re-run Backtest button, live progress, change summary, preserves manual overrides',
  ]},
  {version:'4.195', date:'2026-06-17', changes:[
    'calibrateStore full rewrite — holiday/event/DOW-specific/plus-up now in the evaluation formula',
    'ds.laborRows date-sort fix before .slice(-400) — root cause of inflated historical anomaly MAPEs',
    '_userEvents wired to every calibrateStore/forecastDay/forecastModels call site (was silently empty in many)',
    'detectCleanDataStart — automatic bad-data-period detection for Tishomingo/Elgin/Mossy Head/Ponce de Leon',
    'Holiday model redesign — fullClosure/partialClosure flags, real prior-year per-store holiday data',
    'autoTagHolidays — now runs automatically on every data load (Excel upload and IDB restore)',
  ]},
  {version:'4.192', date:'2026-06-10', changes:[
    'Morning Intelligence Brief — 9 correlation rules, 27 stores sorted by priority',
    'June 2026 projections — embedded all 27 stores, drag-and-drop parser for future months',
    'Projected/VS Proj in brief — falls back to June monthly target ÷ 30',
    'TPPH/Labor% in brief — ctrl row + June target fallbacks',
    'Inventory Excess Cases — fixed for Display as Each format (÷ caseSize)',
    'About/Changelog modal (this screen)',
  ]},
  {version:'4.191', date:'2026-06-01', changes:[
    'Open-Meteo weather API — all 27 stores including FL, 2022–present, IDB-persistent',
    'LFZ Gap panel corrected — honest 9.29% AE vs 9.51% LFZ same-period comparison',
    'Weather notes on anomaly detection, isWeatherExtreme in AE calibration',
    'STORE_COORDS — all 27 locations for weather API (coord.lng→.lon fix)',
  ]},
  {version:'4.190', date:'2026-05-28', changes:[
    'Adaptive Ensemble (AE) model — default weekly, 40,262 rows walk-forward validated',
    'AE auto-recalibration on data load from Sep 2025–May 2026 window',
    'Model Assignment Panel, IndexedDB persistent storage',
  ]},
  {version:'4.18x', date:'2026-05-15', changes:[
    'Florida stores — all 7 Emerald Arches locations integrated',
    'Lifelenz Gap panel, DAR daypart panel, Product Mix panel',
    'Mobile v2 — Convention Demo Mode, Beat LFZ badges, head-to-head hero card',
  ]},
];