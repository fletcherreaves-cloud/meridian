// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.021', date:'2026-08-15', changes:[
  'Issue #324 — Labor Analytics still showed OT HRS/DAY 0.0, OT COST $0, ACT VS NEED +0 hrs on v5.020, even though #323 (merged same day) proved 23 of 135 store-days carried real OT (1.08-21.18 hrs, $25.63-$437.49) in the 8/11-8/15 window and closed every historical gap back to 2024-01-01. Not a #323 failure -- LaborAnalyticsPanel (labor-tools.js:1656) never asked the auto-first resolver. laborPct/tpph were migrated to metric-source.js\'s metricAvg() back on 2026-08-05; the panel\'s own comment named avgRate/otHrs/actHrs/actVsNeed as needing their own registered sources first and left them on a raw ctrlRows/laborRows-only read (_perDay/_avg/_avgZ) with no opsLaborRows backstop -- `grep -n opsLaborRows src/views/labor-tools.js` returned exactly one hit, the comment. All four already had a registered METRIC_SOURCES entry by the time this landed (otHrs/actVsNeed mode:\'any\'; actHrs mode:\'pos\', sourced from the DAR qsrActSummaryRows/ctrlRows, unrelated to #323\'s fix; avgRate a DERIVED metric, laborPct x sales / actHrs) -- migrated all four to metricAvg(ds,loc,range,key), matching the laborPct/tpph precedent exactly.\n\nSame block\'s salaryMgrHrs is also registered (metric-source.js:232, schedRows->ctrlRows) -- migrated. crewHrs has NO metric-source.js entry anywhere, opsLaborRows included -- left on the manual _avg() read; adding a new registry entry was out of this issue\'s stated scope (migrate metrics that already have a registered source, not add new ones).\n\nSecond question the issue posed, now answerable because #323 populated the column: does otDollar reliably replace otCostEst\'s hrs x 0.5 x rate estimate? Measured before deciding, per the issue\'s explicit instruction -- queried qsr_labor_summary directly (service-role, bypasses RLS): 1647/1647 rows (100%) over the trailing 60 days carry a real, non-null over_time_total_dollars across all 27 stores. Retired otCostEst and its "~" approximation flag (otCostEd) outright -- removed from all 8 render sites that read it (per-store table cells, district tile + its subtitle, CSV export, two insight-feed detail strings). otCost is a PERIOD TOTAL (unlike otHrs, a daily average), so it sums metricSeries(\'otDollar\')\'s daily values directly rather than using metricAvg\'s mean -- and only when at least one day resolved a value, preserving #309\'s null-vs-zero fix (a period with real $0 OT every day still reads $0, not "--"; a period metric-source couldn\'t resolve at all reads "--", not a false $0). #309\'s own guards (otCostKnown at the district level, the null-vs-zero distinction itself) are untouched, exactly as the issue asked -- only the sourcing feeding them changed.\n\nVerified against real production data, not just a passing build, per the issue\'s explicit "checkable against a known answer" bar: built ds.opsLaborRows from real qsr_labor_summary rows for 8/11-8/15 (the same transform _loadOpsTable/loadOpsLaborSummary apply) and called the actual metricAvg/metricSeries from src/engine/metric-source.js directly. Store 35242: OT HRS/DAY = 0.87, OT COST = $124.45 (all from a single real $124.45/4.37hr OT day on 8/11, zero the other four days -- exactly the null-vs-zero-preserving behavior described above). District-wide: OT HRS/DAY = 1.75 hrs/day (simple mean across 27 stores), OT COST (Period) = $5,134.63. Non-zero, as required.\n\n1371/1371 tests pass, build clean (eager 508.55 KB gzip / headroom 341.45 KB, no bundle-size-relevant change -- comment/logic-only in an already-loaded module). No memory/ file needed -- the fix is self-contained and the verification numbers live in the PR body per the issue\'s own instruction.\n\nStill open per #322 dispatch, explicitly NOT part of this fix: Labor % on At A Glance separately reads "No labor data" while Labor Analytics shows 22.47% -- different cause (laborPct\'s only non-manual source is the emailed glimpseRows, where tpph leads with the DAR rollup instead), needs max(dt) on daily_glimpse_daily measured before any code changes. Noted here so it isn\'t lost, not started.',
  ]};
