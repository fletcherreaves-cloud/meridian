// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.005', date:'2026-08-12', changes:[
  'Changelog restructure — four PRs open at once in this session (#240/#244/#232/#242) each inserted their entry at the top of this array, right after `MERIDIAN_CHANGELOG = [`, so every one of them collided with every other on rebase: four rebase cycles in one session, purely from a shared insertion line. Fixed at the root: new entries now APPEND at the end of this array (see the header comment above) instead of inserting at the top, and changelog-panel.js sorts the array by version, newest-first, at render time (versionSort, exported for its own unit tests) instead of relying on storage order. This is the FIRST entry to follow the new convention — landing here, at the tail, rather than at the top where every prior entry landed, is the demonstration. changelog-version.test.js updated to match: the "matches LATEST_CHANGELOG_ENTRY" guard now finds the newest entry by MAX version rather than assuming array position (first or last), and the old "array is sorted newest-first" invariant (no longer true of storage) is replaced by a source-parse guard that changelog-panel.js actually calls MERIDIAN_CHANGELOG.slice().sort(...) before rendering. New test file changelog-panel-sort.test.js (4 tests) exercises versionSort directly: descending by major, numeric not lexicographic minor comparison (5.10 > 5.9), duplicate-version stability, and the exact append-order-to-display-order case this fix targets. Historical entries above are left in their old physical (already newest-first) order — only new entries follow the append rule, since sort-at-render makes physical order irrelevant to what the reader sees either way.',
  ]};
