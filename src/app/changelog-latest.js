// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.022', date:'2026-08-16', changes:[
  'Issue #327 — At A Glance Labor % read "No labor data" on the Aug 12-18 window while Labor Analytics showed a real 22.47% on its 4-week window. Measured max(date) on daily_glimpse_daily (service-role, bypasses RLS) before writing any code, per the dispatch\'s explicit stop condition: 2026-08-11, zero rows from 08-12 on — confirmed stale, not an RLS-blind false empty (cross-checked against a date #323 already proved had 135 real rows). laborPct\'s only 3 sources (glimpseRows/ctrlRows/laborRows) are all emailed-or-manual — glimpseRows is forward-only from 2026-07-01 with no backfill possible, and ctrlRows/laborRows go stale the moment uploads stop, so any window past the Glimpse floor with no manual upload resolved null district-wide, even though the atoms to derive it (qsr_labor_summary crew_labor_dollars + the DAR\'s already-registered `sales`) are pulled daily and backfilled to 2024-01-01 by #323.\n\nAdded a derive fallback to laborPct (tpph\'s precedent — precomputed source wins, derivation only fills gaps metric-source\'s existing srcs loop left unresolved): crew labor $ / sales. Numerator: gross_dollars (COLS_LABOR_SUM) does NOT reconcile — it is crew_labor_dollars + salaried_manager_dollars, and the headline "Punched Labor %" this chain feeds is crew-only (parsers/index.js\'s punchLaborPct). Store 6178 makes this unambiguous: gross 2978.60 vs crew 2661.50, a $317.10 gap that is exactly its salaried_manager_dollars for that day — gross would have shipped a Labor % inflated by the manager line on every FL store with salaried managers, silently. Denominator: net_sales_amt has a systematic ~0.2-0.3pp low bias vs product_sales_amt — the DAR\'s existing `sales` metric-source already resolves to product_sales first (supabase.js\'s `_qsrActFromSummed`, `sales: r.product_sales`), so no new source was needed for that leg. New loadOpsLaborSummary field: `laborDollar` (crew_labor_dollars aliased). Unit convention confirmed rather than assumed: glimpseRows.laborPct is a fraction (0.2428, not 22.28) — every render site multiplies by 100 before display — so the derive is `d/s`, not `d/s*100`.\n\n**Correction after PR review (owner-caught):** the first verification pass reported 192/192 store-days matching Glimpse to 4 decimals, 0 mismatches — this was WRONG, a tautology, not a bug fix. The test fixture included glimpseRows in `ds`, and laborPct\'s srcs list glimpseRows FIRST — metricSeries returns the real source before ever reaching the derive on any day Glimpse covers, so the comparison was Glimpse against itself on every sampled day, and would have passed identically with the derivation deleted. Re-ran with glimpseRows/ctrlRows/laborRows excluded from `ds`, forcing every resolution through the derive path, then compared against Glimpse\'s real values kept separately: 648 store-days, 27 stores, 2026-07-19 to 2026-08-11. **582/648 (89.8%) match within 0.001.** The other 66 (10.2%) do not — mean signed diff +0.0050 (58 positive / 8 negative, i.e. the derivation mostly runs high), mean |diff| 0.0074, max 0.0276. Spread across 25 of 27 stores with no store showing 100% mismatch (ruling out a fixed per-store definitional gap) — it is day-specific. A deep-dive on one mismatch (store 31357, 2026-07-19) found the disagreement is NOT just the sales denominator: neither product_sales_amt nor net_sales_amt implies Glimpse\'s reported labor_pct from that day\'s crew_labor_dollars — the numerator itself disagrees with whatever Glimpse used. Leading hypothesis, NOT confirmed: qsr_labor_summary pulls on a calendar-day boundary (`compType:\'calendar\'`) while Daily Glimpse may report on a business-day boundary, which would also explain the positive skew and day-specific (not store-fixed) pattern — flagged for a fallback-accuracy decision rather than asserted as fact. Full derivation is correct against 2026-08-16 confirmed data of both legs (gross-vs-crew and net-vs-product); the remaining ~10% gap is real, unexplained, and disclosed rather than hidden.\n\nDistrict laborPct over 2026-08-12 – 08-18 (8-store sample) now resolves to a real number (0.2127) instead of null across the whole window — the "No labor data" gate no longer fires there, with the above accuracy caveat. avgRate (which derives FROM laborPct, `metric-source.js:280`) still resolves correctly with glimpseRows stripped — forcing the new derive leg — confirming the depth guard (`_depth > 3`) at metric-source.js:487 has room for the added recursion level.\n\n1371/1371 tests pass, build clean. Regenerated the loader field map (`node scripts/gen-loader-emits.mjs --write`) per the standing rule after touching loadOpsLaborSummary — also picked up unrelated pre-existing drift (`darSchedHrs` was missing from qsrActSummaryRows\' emit list).',
  ]};
