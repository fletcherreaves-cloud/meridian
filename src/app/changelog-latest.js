// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.015', date:'2026-08-15', changes:[
  'Issue #296 step 1 — generalized #295\'s finding: 427 hardcoded rgba(255,255,255,X) sites across 20+ view files bypass the --bdr/--bdr2/--surf/--surf2/--surf3 tokens that 1135 sites already use correctly (a 73% adoption rate before this commit). Every one renders wrong in the four light themes; because Meridian is dark-first and dark-tested, the light half of the theme matrix was effectively unreviewed. Split into #276\'s proven two-step shape: step 1 (this commit) converts the 202 BORDER/STROKE-role sites — the ones that render genuinely INVISIBLE in light mode (a white wash against a light surface), not just low-contrast — across 28 files; step 2 (separate PR) will do the remaining background/color-role sites, which degrade to low-contrast rather than disappearing and are lower-urgency. Same reasoning #276 gave for splitting: a single ~430-site diff would make the handful of lines needing real judgment unreviewable. Alpha->token mapping: the measured distribution clustered into two clear bands — 189 sites at alpha .025-.14 (the "subtle structural border" convention already used by 1135 correct sites) mapped to --bdr; 5 sites at alpha .15-.4 (deliberately stronger — active/focused states paired against a colored alternative in the same ternary, e.g. an amber "selected" border vs a white "unselected" one) mapped to --bdr2, preserving the relative emphasis the original alpha values encoded (same logic #295 used for the Bullseye tile\'s own ring hierarchy). Classification was per-OCCURRENCE, not per-line: many lines carry two rgba(255,255,255,X) values with different roles (e.g. background AND border on the same style object) and only the border-role one converts — built a script that finds, for each occurrence, the nearest preceding CSS-in-JS key (border/borderTop/.../outline/stroke vs background/color/fill/boxShadow) or, absent an explicit key, classifies by conventional local variable name (bdr/tileBdr/brd/SBDR = border; bg/rowBg/clr/surf = background/color) — 5 sites needed this variable-name path and were individually context-verified before converting (store-dash.js:1768, analytics.js:4904, eom-supervisor.js:424, morning-brief.js:449/836). A first pass of this classifier missed eom-supervisor.js:424 (a bare `const bdr = ...` with no CSS-in-JS key on the line) because the conversion script re-derived roles with a simpler regex than the analysis script that built the candidate list — caught by re-running the analyzer against the POST-conversion tree and finding a border-role site still literal; fixed by making the apply step trust the analyzer\'s precomputed per-occurrence role and character offset directly instead of re-deriving it, eliminating the class of bug rather than patching the one instance. The one deliberate, non-mechanical judgment call: 12 files (analytics.js, sage.js, store-dash.js, inventory.js, scheduling-deck.js, morning-brief.js, and 6 others) contain functions that build a COMPLETE STANDALONE `<!DOCTYPE html>` document as a string — printable/emailed/exported reports with their own literal-hex mini stylesheets that never load meridian.css. Converting rgba(255,255,255,X) to var(--bdr) inside those functions would trade an invisible-in-light-mode bug for a broken-everywhere bug (the custom property simply wouldn\'t resolve). Found the exact boundary of each such function with an AST walk (acorn) rather than guessing a line range by eye: for every literal `<!DOCTYPE html>` occurrence in the 12 files, located the SMALLEST enclosing function node and excluded every rgba(255,255,255,X) site inside it — 2 of the 202 border-role sites fell inside these ranges and were left untouched on purpose (analytics.js\'s Anomaly Report generator, scheduling-deck.js\'s slide deck; inventory.js and morning-brief.js\'s own standalone generators happened not to overlap any border-role occurrence this pass). bullseye-tile.js\'s 3 ring/tab sites also deliberately excluded — PR #295 (separate, unmerged) already fixes those with these exact tokens; converting them here too would just duplicate that diff and create an avoidable merge conflict between the two PRs. New guard test src/__tests__/light-mode-white-alpha.test.js: greps src/**/*.js (excluding __tests__ itself, whose own descriptive comments would otherwise inflate the count) for rgba(255,255,255 and fails above a ratcheting ceiling — set to 269, the measured count immediately after this step, not a round number; lower it again when step 2 lands, same "measure it" discipline as the rest of this commit. Verified per CLAUDE.md\'s "measure it, don\'t reason about it": re-ran the classifier against the converted tree and confirmed the only remaining border/stroke-role occurrences are exactly the deliberate exclusions (2 standalone-HTML sites + bullseye-tile.js\'s 3, owned by #295) — zero unintentional misses. 1371/1371 tests pass (1370 existing + 1 new guard), build clean (eager 509.57 KB gzip / headroom 340.43 KB, noise-level — a token-value swap across existing style objects, zero new imports). Not full browser-verified for the same reason as #295: this session\'s dev-server + headless-Chromium check hits a sandbox-specific CSS-loading limitation unrelated to either change (documented in #295\'s own entry) — verified instead by the same token mechanism already proven correct elsewhere and a scripted, offset-precise, role-aware conversion checked against the source both before and after.',
  ]};
