// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.034', date:'2026-08-16', changes:[
  'Issue #171 -- per-stream data freshness. at-a-glance.js pooled 5 feeds (including manual laborRows) into one array and took Math.max, so the staleness alert fired only when EVERY feed was stale at once. Two real outages proved this invisible for days: lifelenz_schedules dark 6 days (Aug 6-11, surfaced when someone noticed stale labor data) and sales_ledger_daily dark 5 days (Aug 12-16, surfaced only while debugging an unrelated blank tile, #346). #171 itself (written Aug 11) named sales_ledger_daily as the next victim of the exact same pooling gap -- it went dark five days later and nothing said a word. The two bugs are the same shape: a check that runs, passes, and cannot fail in the case it exists for.\n\nNew engine/stream-freshness.js checks each auto stream INDEPENDENTLY and reports the worst one by name. Streams enumerated from the loaders (App.js setDs call sites), not from #171\'s own table -- confirmed stale by its own admission -- and now covers opsCashRows/opsLaborRows/opsServiceRows/opsSalesMixRows the app gained since #171 was filed. qsr_product_mix and qsr_onhand are pulled daily too but are NOT wired into the shared ds yet (zero call sites, confirmed by grep) so they cannot be freshness-checked from ds -- documented in the module rather than silently assumed fresh. Manual laborRows is EXCLUDED entirely, per the auto-first rule and #362 -- a stale device-local upload making the app look healthy is exactly backwards. LifeLenz stays IN the check (clamped to <=today so its forward-published schedule cannot read as fresher than it is) rather than being dropped, which is what let it go dark unnoticed before.\n\nThresholds are per-stream cadence + grace (warnAt = cadence+1, critAt = cadence+3), not the old hardcoded 14 days -- tuned to the owner\'s own framing, \"a daily stream silent 2 days is an incident.\" Wired into both places at-a-glance.js ran the identical pooled check: the checklist alert (autoItems) and the rule-based state comment (ruleComment) -- same bug, same fix, in the same file. The positive \"freshest data we have\" banner (latestLab/dataAge, the footer\'s \"Data: Aug 16 (0d ago)\") is UNCHANGED -- a pooled max is the right tool for that question; only the ALERTING logic (which needs \"is anything dark\", not \"what\'s our best\") moved to the per-stream check.\n\nVerification bar, both as tests: (1) four fresh feeds + one 30 days stale names that one stream (LifeLenz) -- PLUS a regression guard that reimplements the exact pre-fix pooled Math.max logic and confirms it stays GREEN (0 days old) on the identical fixture, which is the silence being fixed. (2) sales_ledger_daily\'s real measured gap (max(date) = 2026-08-11 as of this fix, with DAR/FOB/LifeLenz/Ops Labor all current at 2026-08-16, queried live against Supabase) names the Sales Ledger specifically -- real history, not a synthetic that could pass for the wrong reason.\n\n1407/1407 tests pass (9 new), build clean (508.47 KB gzip eager, 341.53 KB headroom).',
  ]};
