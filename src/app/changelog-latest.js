// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.001', date:'2026-08-12', changes:[
  'Issue #236 — labor_pct\'s 30-day-movement distribution had a contaminated tail (p90->p95 jumps 3.698pp->10.650pp, p99 19.497pp — a step change in kind, not degree; mean 1.940pp against median 0.841pp is heavy right skew a real operational distribution wouldn\'t show). Root cause found by dumping and reading actual rows (scripts/dump-labor-movement-outliers.mjs, new — forks the noise-threshold script\'s exact windowing, prints both window endpoints/values/observation-counts and every underlying row), not by theorizing candidate mechanisms first. Top-50 movements showed trailing-30 windows reading ~0-2% labor for real n=30-observation windows (the >=20 guard was holding, not a thin-window artifact) paired against a normal ~21-24% window 30 days earlier — a store cannot run near-0% labor for 30 straight days. Pulled one flagged store\'s raw history directly: labor_pct=0/tpph=0/ot_hrs=0 on EVERY day 2026-06-01 through 2026-07-11 (41 consecutive days, no gaps) while sales was real and varied normally ($9,089-$19,880/day) -- a data-pipeline defect, not operations, and a CHRONIC one: parsers/index.js\'s parseSalesLedger (the QSRSoft Sales Ledger channel-mix report parser, a different report from Labor Analysis) stamped every row\'s labor fields as literal 0 ("Sales Ledger has no labor data"), and pipeline.js:578 merges those rows into ds.laborRows so the sales figure can supplement Labor Analysis history when that report lags (confirmed intentional plumbing -- the Data Manager panel\'s own UI already says "Sales Ledger... merges into Labor Analysis"). App.js\'s manual-upload path then pushed the merged rows straight to the labor_rows Supabase table; saveLaborRows\'s `r.laborPct ?? null` mapping treats 0 as a real value (0 ?? null is 0), so the stub got upserted (onConflict: loc,report_date) and silently overwrote or filled in real Labor Analysis percentages with a false zero. Never visibly noticed in the live app because metric-source.js\'s laborPct chain uses mode:\'pos\' ("a %\'s never legitimately 0") and already skips a stub 0 in favor of glimpseRows/ctrlRows -- the exposure was the persisted labor_rows table itself (which measurement scripts and any future coaching-loop code read directly) plus any panel reading ds.laborRows directly rather than through the shared metric-source helpers (not fully audited here, flagged as still-open). MEASURED count, per the issue\'s explicit "report how many store-days, not just the mechanism": labor_pct=0 AND sales>0 matches 994 of 42,156 labor_rows (2.36%), ALL 27 stores, 2025-01-22 through 2026-07-23 (18 months chronic, not a recent-only outage -- the June-July 2026 block found first was just the most recent batch) -- every one of the 994 also has tpph=0 AND ot_hrs=0, verified before writing any cleanup script so no partial-real-data row could be swept up. Fixed at three layers per the #192 padding precedent (fix root cause, not the symptom): parseSalesLedger\'s labor fields are null now, not 0, tagged _salesLedgerSupplement; loadSalesLedger (the cloud sales_ledger_daily loader) gets the same null fix for consistency (nothing currently reads that field live, so this is hygiene not a live fix); App.js\'s manual-upload save path now filters _salesLedgerSupplement rows out of what reaches saveLaborRows entirely -- they stay in-memory in ds.laborRows (still usable for same-session DI-calibration continuity) but never touch the labor_rows table, since supplementLaborWithSched (App.js\'s DAR-based sales supplement, already verified 2026-08-04) already covers "sales when Labor Analysis lags" without ever persisting anything, so nothing is lost by keeping this path narrower. New test (src/__tests__/sales-ledger-labor-stub.test.js, 4 tests): asserts every parsed row\'s labor fields are null not 0, asserts the _salesLedgerSupplement tag, and source-parses parseSalesLedger to guard against the literal 0 stub ever being reintroduced. Cleanup script for the 994 already-corrupted rows (scripts/cleanup-labor-pct-stub-zeros.mjs, --dry by default) is written, dry-run-verified (994/994 match, zero false positives), and NOT yet executed against production -- per this repo\'s own v4.839 precedent ("does NOT run the two scripts, needs the owner\'s go-ahead"), a write to 994 live rows needs explicit sign-off, not an agent\'s unilateral call. Full incident, the still-open items (run cleanup, re-run #237\'s labor measurement post-cleanup since its 2.30x reduction is driven almost entirely by this same contaminated tail, audit remaining direct ds.laborRows consumers), and definition-of-done tracking in memory/project-labor-pct-tail-236.md. 1336/1336 tests pass (1332 + 4 new). Entry chunk essentially flat (comment/logic-only change in already-loaded modules).',
  ]};
