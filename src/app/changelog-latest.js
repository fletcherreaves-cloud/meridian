// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.011', date:'2026-08-14', changes:[
  'Issue #276 step 1 — Meridian\'s status trio (green/amber/red), run through the dataviz validator against the app\'s own dark surface #0f1117, failed the normal-vision separation floor: #f87171 (red) vs #f59e0b (amber) measured ΔE 14.6, below the hard floor of 15 — amber and red are hard to tell apart even with full colour vision, not a colourblindness edge case. Swapping only the red to #f43f5e clears the floor (ΔE 21.6, tritan CVD 16.6, contrast vs surface still >=3:1 for all three) without moving green or amber. Added --good/--warn/--crit tokens to :root in src/meridian.css (mode-independent, matching how the status trio was already used everywhere -- bare hardcoded hex, not per-theme -- so this is a value-for-value swap with zero visual change outside the moved red). Converted the 66 lines the issue\'s own grep (`f59e0b.*f87171|f87171.*f59e0b`) identified as adjacency sites -- every place amber and red appear as alternatives in one scale (ternaries, meridian.css:279 .hi/.md/.lo, priorityColors maps) across 13 files (store-analytics.js 20, at-a-glance.js 16, analytics.js 9, projections.js 5, inventory.js 4, store-dash.js 3, model-health-badge.js 2, labor-tools.js 2, meridian.css/session.js/calendar.js/forecast.js/shell.js 1 each) -- confirmed exactly 66 by re-running the same grep before and after (66 before, 0 after). store-dash.js:1680\'s CRITICAL priorityColors entry also carries a red-tinted background (rgba(239,68,68,...)) alongside the text color; updated both to the new red\'s rgb (244,63,94) so the text isn\'t on a red wash that no longer matches it. The remaining ~276 lone-red (no amber alternative nearby) sites are deliberately NOT touched here -- Step 2, mechanical, no new decisions, per the issue\'s own two-step split (a ~600-site diff would make the six lines that matter unreviewable). Browser-verified: dev server load, confirmed getComputedStyle(document.documentElement) resolves --good/--warn/--crit to the expected hex, zero console/page errors. 1360/1360 tests pass, build clean (eager 507.62 KB gzip / headroom 342.38 KB, unchanged -- CSS custom-property additions are trivial as expected, measured not assumed).',
  ]};
