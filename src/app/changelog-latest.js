// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.028', date:'2026-08-16', changes:[
  'Issue #343 -- filed after finding the qsr_fob HEAD 500 (a console-only error; FOB still rendered, so something fell back silently). Root cause, not just the symptom: _pagedParallel (src/lib/supabase.js), the shared paginator behind 10 loaders (qsr_fob, labor_rows, peaks_rows, audit_rows, ops_rows, ctrl_rows, daily_glimpse_daily, cash_sheet_daily, sales_ledger_daily, qsr_product_mix), destructured only `count` off its upfront count-HEAD query and silently dropped `error`. Supabase-js does not throw on an HTTP error -- it resolves {data, error, count} -- so a failed count (e.g. a statement timeout on an exact count over a big table; qsr_fob is described elsewhere in this file as "a wide, daily x 27-store table") left `count` undefined, and `count || 0` defaulted `pages` to 1. The read did not abort -- it silently proceeded as if there were exactly one page, fetched the newest pageSize rows via a plain ranged SELECT (a different, cheaper query shape that can succeed even when the count-HEAD times out), and returned that as if complete. Recent data rendered fine, which is exactly why this went unnoticed: a wrong number that still looks like a number doesn\'t get reported.\n\nBlast radius, confirmed before fixing anything per explicit instruction (worth more than fixing one table blind): grepped every count:\'exact\'+head:true occurrence in supabase.js, not just _pagedParallel\'s callers. Found three independent instances of the identical pattern -- _pagedParallel itself (10 callers above), a hand-duplicated copy in loadQsrActSummary\'s qsr_daily_activity hourly-pagination fallback (used when the qsr_daily_activity_rollup table is unavailable -- silently would have capped at the newest 1000 of a ~40,000-row 60-day window), and a third in loadEmployeeSkills (silently would have capped the roster at 1000 rows, ~19 of 27 stores per that function\'s own header comment). All three shared the exact same shape and are fixed the same way.\n\nFix: on a count error, fall back to fetchAll\'s sequential-until-short-page strategy (the sibling paginator already in this file), which needs no upfront count and so cannot under-fetch from a bad one. Deliberately slower than the parallel happy path on what should be a rare error path -- correctness over speed, matching the same tradeoff fetchAll\'s own header comment already documents. Does not double-report: the count-HEAD failure itself only warns to console; _recordDataError (the DataErrorBanner-feeding registry) only fires if the FALLBACK read then also errors, since a self-healing fallback that completes cleanly is not a partial load and should not raise a false \'DATA INCOMPLETE\' banner.\n\nVerified with a real test, not just inspection -- src/__tests__/paged-parallel-count-fallback.test.js mocks @supabase/supabase-js to force a count-HEAD failure deterministically (the only way to do it; can\'t be manufactured on demand against live infra) and drives it through the real exported loadQsrFob: confirms a 12-row dataset that a bad count would have silently returned as 0-or-truncated instead returns complete, confirms the happy path (count succeeds) is unchanged, and confirms a genuinely empty table still returns empty rather than being misread as an error. The other two sites share the identical fix mechanically (fall back to fetchAll) and are verified by inspection + the full suite + build passing, not a second dedicated end-to-end mock -- loadQsrActSummary\'s rollup-probe branching and per-row aggregation would need substantially more mock scaffolding for materially the same confidence the shared-pattern test already establishes.\n\n1390/1390 tests pass (3 new), build clean (508.50 KB gzip eager, 341.50 KB headroom, no meaningful shift -- logic-only change in an already-loaded module).',
  ]};
