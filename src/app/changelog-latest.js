// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.003', date:'2026-08-12', changes:[
  'Issue #232 (Findings 2+3) — the remaining two findings, landed together per the issue\'s own sequencing ("split store-dash.js, then all three Chart.js paths leave together"). Investigation (Explore agent, full report before any edit) found the premise simpler than assumed: of store-dash.js\'s 47 exports statically imported by App.js, only 3 pure functions (ymKey, loadTargetsV2, migrateTargetsToV2 — a mount-time migration effect + a mergedTargets memo) were ever used unconditionally; every panel App.js actually renders from that file (DistrictGrid/OrgView/RankingView/PerformanceCalculator/UnifiedTargetsPanel/MonthlyTargetManager/EventCalendar) was already gated behind view/show state, and ~38 of the 47 imports were dead code in App.js entirely (only used by analytics.js/labor-tools.js/store-analytics.js, which are already lazy). No file split was needed to fix this — moved the 3 functions to engine/monthly-targets-v2.js (saveTargetsV2 travels too, migrateTargetsToV2\'s own internal dependency), lazy-wrapped the 7 real panels via a new _storeDash() group, and dropped the rest. That alone removed store-dash.js\'s only static path, taking its whole 145 KB (chart-component library, Chart.js runtime, a dozen panel components) with it. Finding 2 (Chart.js) fell out for free at that point except one more site: dt-speedofservice.js\'s DTSpeedOfServicePanel was ALSO statically imported (gated behind showDtSoS) and carries its own chart.js/auto import — converted to lazyPanel(). App.js\'s own `import { Chart } from \'chart.js/auto\'` (dead — referenced nowhere in the file, confirmed by grep before deleting) removed outright. All three Chart.js paths #232 named (App.js\'s own dead import, store-dash.js, dt-speedofservice.js) now leave together, as the issue required. MEASURED, and this build is itself the clearest illustration yet of why the entry-FILE-only number check-bundle-budget.mjs prints is unreliable: that number went UP, 485.07 KB -> 506.54 KB gzip (+21.47 KB) — NOT a regression. Rollup\'s automatic chunking re-inlined several modules that used to be split into separate-but-modulepreloaded files (react, constants.js, supabase.js, blob-sync.js, labor-basis.js, swing-feed.js, fmt.js, preload-helper — all previously shared between the entry and store-dash.js\'s static reach) directly into the entry chunk once store-dash.js stopped forcing that split; those bytes were already being fetched eagerly before, just as separate files, so absorbing them into one file is neutral, not worse. The number that matters, entry file + everything index.html actually modulepreloads: 769.10 KB -> 649.28 KB gzip, a REAL reduction of 119.82 KB (2,501.05 KB -> 2,126.65 KB raw, -374.40 KB) — and modulepreload itself shrank from 11 items to 3 (just rolldown-runtime/xlsx/date), meaning the re-inlined modules are gone from the eager set\'s file *count* too, not just relocated. Genuinely deferred to first-open, confirmed absent from the modulepreload list: store-dash-*.js (44.52 KB gzip / 154.63 KB raw) and the chart.js/auto runtime\'s own chunk, auto-*.js (69.29 KB gzip / 202.20 KB raw) — 113.81 KB gzip / 356.83 KB raw combined, never fetched unless a store-dash.js panel or DT Speed of Service is actually opened. Independently re-verified against the now-corrected check-bundle-budget.mjs (#244, landed earlier in this stack): the gate\'s own eager-total number is what confirms this Finding\'s claim rather than a hand computation asserted alongside it. 1335/1335 tests pass, unchanged from Finding 1 (no new tests — this is import-graph restructuring with zero logic changes; the existing lazy-panel-not-static.test.js guard already covers the new lazy registrations by construction, since it walks App.js\'s full lazyPanel()/static-import sets generically).',
  ]};
