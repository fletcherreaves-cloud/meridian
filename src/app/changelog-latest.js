// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.024', date:'2026-08-16', changes:[
  'Issue #337 — TEMPORARY DIAGNOSTIC, not a fix. At A Glance Labor % still reads "No labor data" on v5.023 despite #327/#329\'s derive, and this session\'s own sandbox harness (real loaders, real production data via service-role, real metricSeries/metricAvg -- no hand-built fixture) proved the derive resolves laborPct correctly end to end (district 23.3%, tpph in the same range as the owner\'s reported 5.7). Extended the tenant_id check across the full Aug 12-18 window (not just the single spot-checked date from #337\'s own elimination list) -- completely clean, uniform, non-null on qsr_labor_summary/qsr_daily_activity_rollup/qsr_sales_mix alike. Also read src/app/App.js\'s setDs pattern and ruled out a stale-cache-on-mutated-ds hypothesis: every setDs call spreads into a fresh object and metric-source.js\'s per-source index cache is defined non-enumerable, so it could never leak across a spread even if ds were reused.\n\nThat leaves nothing static analysis or an out-of-browser reproduction can settle -- every code-level and data-level candidate is eliminated, and the delta between the sandbox harness (works) and production (broken) has to be the ACTUAL RUNTIME ARGUMENTS in the owner\'s live session, which only their browser can show. Added temporary console instrumentation to laborSec (at-a-glance.js, right after laborPct/tpph are computed, before the early-return gate so it fires regardless of outcome): effectiveDateRange (the untested candidate -- is it really Aug 12-18 live?), allLocsSample vs opsLaborSample.loc (the loc-padding-format comparison never actually made at runtime, only in static code reading), laborDollarKeys/salesKeys from a live metricSeries call, and laborPctResolved next to tpphResolved so the one-resolves-one-doesn\'t asymmetry is captured in a single dump.\n\nDeliberately NOT a fix -- per explicit instruction, after two prior "fixed" claims that shipped still-broken, a confirmed observation from the owner\'s console is worth more than a third guess. This PR\'s only job is making the bug visible; the console dump decides what #337\'s actual fix PR does. Fires once per ds change (~5x per load), no gating needed (single-user). Will be removed in that fix PR -- referencing this PR number there.\n\n1371/1371 tests pass, build clean (508.00 KB gzip eager, 342.00 KB headroom, noise-level -- a console.log plus two metricSeries calls in an already-loaded module).',
  ]};
