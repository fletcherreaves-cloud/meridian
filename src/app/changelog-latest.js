// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.010', date:'2026-08-14', changes:[
  'Issue #256 (instrumentation) — extended #189\'s span pattern one level deeper inside src/views/at-a-glance.js: wrapped all 28 useMemo compute blocks and 11 top-level tile sections with mark(\'compute:X\'/\'tile:X\', fn) from utils/click-trace.js, via an AST-based (acorn) transform so every wrap site is mechanically a no-op except for the added timing. PR review caught that the tile:* half measured something other than what its name claimed: mark() wraps a plain function call and cannot see inside React\'s render/commit (click-trace.js\'s own header already says so) — every tile:* wrapper timed element CONSTRUCTION (h()/div() calls), not render+commit. tile:sage wrapped a single h(SageRunsTile,...) createElement call, measuring ~nothing and never clearing the 1ms floor — removed entirely rather than kept as a mark that would always read empty. The other 10 (tile:intelligence, tile:projections, tile:sales, tile:labor, tile:service, tile:controls, tile:fob, tile:digital, tile:radar, tile:leaderboard) do construct a real element tree via h()/div() and run their inline IIFEs eagerly, so they measure something real -- renamed build:* to say so, since "render time" was never what they measured. mark()\'s own doc comment in click-trace.js now states the ambiguity plainly: absence of a mark in a report means either "ran under 1ms" or "structurally unmeasurable" -- indistinguishable without checking what the span actually wraps, which is exactly the confusion the tile:* naming caused. compute:* marks (the 28 useMemo blocks) are unaffected -- those already measure real JS work and were not in question. 1358/1358 tests pass, build clean (eager 507.89 KB gzip / headroom 342.11 KB, unchanged -- naming/removal only, zero new call sites).',
  ]};
