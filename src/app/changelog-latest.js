// @ts-nocheck
// #230 — the ONE changelog entry App.js needs synchronously: the latest version+date, for the
// footer text ('Meridian. v'+MERIDIAN_VERSION) and window.__MERIDIAN_VERSION__. Deliberately
// standalone — does NOT import from changelog-data.js, and is not imported by it — so App.js's
// static import of this file stays tiny regardless of how large the full changelog grows. The
// full history (including this same entry, duplicated) lives in changelog-data.js, read only by
// the lazy-loaded changelog-panel.js. See changelog-data.js's header for why the two must not
// share a module. Keeping this in sync with changelog-data.js's newest entry is mechanical —
// update both in the same commit — and guarded by src/__tests__/changelog-version.test.js.
export const LATEST_CHANGELOG_ENTRY = {version:'5.017', date:'2026-08-15', changes:[
  'Issue #296 step 2 — the background/color-role half of step 1\'s split, converting the highest-priority subset per PM review rather than the full remaining ~240 sites in one pass: text/color-role rgba(255,255,255,X) occurrences that degrade in light mode not to low-contrast but to effectively invisible text -- a user reads it as "no data," not as a rendering fault, which is why these jump the queue ahead of the larger background-role batch. Classified with a refined per-occurrence analyzer distinguishing three CSS-in-JS key families (border/stroke, text/color -- `color:`/`fill:`/`textShadow:` but not `backgroundColor:`, and background/boxShadow), plus a variable-name fallback for sites with no explicit key on the same line (clr/txt/text = text; bg/surf/background = background; bdr/border/stroke = border). Converted 19 text/color-role sites across 4 files, alpha magnitude mapped to token: skills-matrix.js (1, an unrated-cell placeholder dot), store-dash.js (4, muted trailing-week/target table cells -- 3 of these matched an alpha the same row\'s SIBLING cell had already converted to var(--text3), so this closes an inconsistency within the same table, not just a new site), analytics.js (1 heatmap-cell text color, the weak-correlation branch of a background that itself blends toward the surface at low alpha -- same white-on-tinted-surface hazard as a flat white-on-white site once the tint is faint enough), at-a-glance.js (13: the header greeting text/sub-greeting, an AI-narrative comment color, a cloud-stream-not-loaded badge label, FOB Disc/Coupon figure + its footnote, the Digital Sales remainder-percent label, a mover-tile hint, plus the shared laborSec.avn "Act vs Need" color -- same line #303 fixes for its data-sourcing bug; this PR only converts the color token since #303\'s metricAvg routing is a separate concern, and the two will need reconciling at merge time since #303\'s branch doesn\'t stack on this one). Also restructured 8 SVG bare-attribute sites (analytics.js 3, at-a-glance.js 5) that could not simply swap the string value: React sets non-style SVG presentation props like `fill` via setAttribute, which does not resolve var() -- confirmed working pattern from #295\'s bullseye-tile.js is to move the color into a nested `style:{fill:...}` object alongside the other top-level attrs, which goes through the CSSOM path instead. One dead-code site (at-a-glance.js\'s unused leaderboard-row `clr` variable, never referenced by its own render body) converted anyway since it was free and zero-risk. Also caught and fixed one genuine step-1 miss found while cross-checking step 2\'s own residual "border/stroke" classification: at-a-glance.js\'s priority-item border, missed because its `border:` key sat on the line ABOVE the rgba occurrence, outside step 1\'s same-line-only classifier window. Explicitly excluded, each for a different reason: Chart.js/canvas config objects (dt-speedofservice.js, store-dash.js x2) -- read directly by Chart.js for canvas rendering, never through the DOM, so var() cannot resolve there, same reasoning as the standalone-HTML exclusion; the same 12 standalone-HTML-document-generator functions step 1 already mapped by AST walk (inventory.js, scheduling-deck.js, morning-brief.js, analytics.js, and a newly-confirmed scheduling.js site all fall inside those previously-computed ranges); and 3 classifier false positives where a naive nearest-preceding-key heuristic misattributed an rgba to an unrelated EARLIER key on a busy multi-property line (analytics.js:7172, signals.js:1342, feature-requests.js:480 -- all three are really background-role `bg:`/`background:`, deferred to the follow-up background sweep, not text/color). Guard test src/__tests__/light-mode-white-alpha.test.js CEILING lowered 266 -> 241 to match the measured post-conversion count (19 converted plus 6 already-caught during classification -- a step-1 miss and duplicate occurrences within already-touched style objects). 1371/1371 tests pass, build clean (eager 509.62 KB gzip / headroom 340.38 KB, noise-level -- token-value swaps and one nested-style restructure across existing objects, zero new imports). Browser-verified against the built dist per the corrected recipe (npm run build && serve -s dist, not the dev server): 241 CSS rules attach as a real stylesheet, --text/--text2/--text3/--bdr all resolve to real values in golden/light (confirmed --text -> #1c1a17, matching meridian.css\'s own golden-light block), app boots with zero page errors (the only console noise is the weather-API CORS failures expected in this sandbox, unrelated to this change).',
  ]};
